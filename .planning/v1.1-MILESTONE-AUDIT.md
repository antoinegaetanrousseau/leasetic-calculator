---
milestone: v1.1
milestone_name: Hosted Web App Foundation
audited: 2026-05-11
status: passed
scores:
  requirements: 108/108
  phases: 6/6
  integration: 6/6
  flows: 5/5
counts:
  reqs_complete: 105
  reqs_partial_marker: 3
  reqs_actually_partial: 1
verifier_disabled: true
nyquist_enabled: false
gaps:
  requirements: []
  integration: []
  flows: []
warnings:
  - id: WR-AUDIT-01
    severity: warning
    affected_requirement: ADMIN-05
    where: src/lib/auth/actions.ts (redeemToken + sign-in callbacks)
    issue: |
      `users.last_login_at` is declared in the schema and read by
      `listPartnersWithCounts()` for the accounts-page "last login" column,
      but no auth-flow code path ever writes it. Result: every partner row
      renders `'—'` for last login.
    impact: ADMIN-05 operationally incomplete (column shown, never populated)
    blocking: false
    note: Phase 6 follow-up; not a security or correctness issue
  - id: WR-AUDIT-02
    severity: warning
    affected_requirement: CUT-04
    where: scripts/seed-partner-launch.ts + package.json
    issue: |
      `scripts/seed-partner-launch.ts` has no npm script entry; its header
      documents invocation as `npx tsx ...` without the
      `_preload-mock-server-only.cjs` preload, which the script's transitive
      imports require. Direct invocation could fail with a confusing
      `Cannot find module 'server-only'` error.
    impact: One-time launch-day tooling friction; cosmetic
    blocking: false
  - id: WR-AUDIT-03
    severity: warning
    affected_requirement: BOOT-03
    where: Vercel project DATABASE_URL routing
    issue: |
      Neon 3-branch split (production / preview / development) is deferred;
      all Vercel scopes currently point at the `main` Neon branch. Production
      traffic now writes to `main`. Documented in STATE.md as Phase 5
      follow-up; flagged as `[~]` partial in REQUIREMENTS.md.
    impact: Degraded isolation between deploy scopes; not a runtime risk for
      the current production-only workflow
    blocking: false
deferred_correctly:
  - id: PROP-01-stale-marker
    note: |
      REQUIREMENTS.md still shows PROP-01 as `[~]` partial referencing
      Phase 7 empty-state shell. Phase 8 plans PROP-02..05 (proposals list
      population) all completed and verified. The `[~]` checkbox is a stale
      marker; the requirement is functionally satisfied. Optional: update to
      `[x]` in REQUIREMENTS.md.
  - id: CALC-07-stale-marker
    note: |
      REQUIREMENTS.md still shows CALC-07 as `[~]` partial referencing
      Phase 7's client-preview-only seam. Phase 8 plan 08-07's
      `submitProposal()` adds the server-side `computeLoyer()` recompute
      (CALC-07 second clause). Stale `[~]` marker; functionally satisfied.
      Optional: update to `[x]` in REQUIREMENTS.md.
deferred_acknowledged:
  - id: BOOT-03
    note: Neon branch split — Phase 5 follow-up, partial by design
  - id: DATA-11
    note: 10-year PDF retention — legal counsel sign-off pending (Open Q3)
  - id: CUT-09
    note: OVH live smoke deploy — September 2026 (capability is shipped, execution deferred)
followups_pending_human_action:
  - admin password rotation (Phase 6 follow-up #1, before first partner onboard)
  - Thomas privacy-coverage confirmation (D-10-18)
  - Antoine to walk launch-checklist.md 9 steps (post-audit)
verifier_policy_note: |
  The project deliberately runs with `verifier_enabled: false` in
  .planning/config.json. No phase has a VERIFICATION.md file. The
  workflow's strict reading flags this as 6 unverified-phase blockers; in
  this project's context it's a documented policy choice. Verification
  rigor is provided by:
    - SUMMARY.md per plan (46 files, all phases)
    - REVIEW.md per phase (Phases 8/9/10)
    - REVIEW-FIX.md (Phases 9/10)
    - SECURITY.md (Phases 9/10 — 42+55 = 97 threats verified closed)
    - Manual smoke checks (this audit confirms production deploy is healthy
      after the `4879831` partner-page fix)
  This audit assesses the milestone as if `verifier_enabled: true` had run
  — substituting SUMMARY+REVIEW+SECURITY+integration-checker as the
  verification evidence.
---

# Milestone v1.1 — Hosted Web App Foundation: Audit

**Audited:** 2026-05-11
**Status:** ✓ PASSED (with 3 non-blocking warnings + documented deferrals)
**Recommendation:** Proceed with `/gsd-complete-milestone 1.1`

---

## Top-line numbers

| Metric | Result |
|---|---|
| Requirements coverage | **108/108** (105 `[x]` complete + 3 `[~]` partial-marker; 2 of the 3 partial markers are stale and functionally satisfied) |
| Phases shipped | **6/6** (Phase 5 → Phase 10, all SUMMARY.md present across 46 plans) |
| Cross-phase junctions verified | **6/6** (5→6, 6→7, 7→8, 8→9, 9→10, 6↔10) |
| End-to-end flows traced | **5/5** (partner happy path, admin coefficients edit, admin partner management, PDF immutability, soft-delete + purge cron) |
| Cross-cutting invariants | **5/5** (ADMIN-09 commission invisibility, no-Vercel-only-imports, Drizzle migration discipline, i18n SHELL-06, locale SHELL-09) |
| Drizzle correlated-subquery latent bugs | **0** (the one fixed in commit `4879831` was the only instance) |
| Phase code reviews | 3 of 6 phases (Phases 8/9/10); 11 critical+warning findings all fixed |
| Phase security audits | 2 of 6 phases (Phases 9/10); **97/97 threats verified closed** (ASVS L1) |

## Cross-phase wiring (6/6 verified)

| Junction | Verified seam | Status |
|---|---|---|
| Phase 5 → 6 | `lib/storage` + `lib/db` adapters consumed by Better Auth via `drizzleAdapter` | ✓ |
| Phase 6 → 7 | `requireUser()` gates `(authed)/proposals/new/page.tsx` (defense-in-depth: layout + page) | ✓ |
| Phase 7 → 8 | `ProposalForm.onSubmit` → `fetch('/api/proposals', { 'Idempotency-Key': uuid })` → server-side validate + `computeLoyer()` + snapshot + PDF + blob | ✓ |
| Phase 8 → 9 | `getLatestGlobalParams()` + `audit_log` + soft-delete primitives → admin pages via `src/lib/admin/actions.ts` wrappers | ✓ |
| Phase 9 → 10 | `<SeedBanner>` on coefficients page; purge cron via shared `src/lib/admin/purge.ts`; `CRON_SECRET` rename propagated everywhere | ✓ |
| Phase 6 ↔ 10 | `LoginForm.tsx` privacy URL from `NEXT_PUBLIC_PRIVACY_URL_FR/EN` with leasetic.fr canonical fallbacks | ✓ |

## End-to-end flows (5/5 traced)

| Flow | Verdict | Notes |
|---|---|---|
| Partner happy path | ✓ | unauthenticated → /login → home (empty proposals) → /proposals/new → live preview → POST → server compute + snapshot + PDF + blob → /proposals/{id} embedded preview |
| Admin coefficients edit | ✓ | login → /[adminSegment] → /coefficients → editor + history + explain → save → diff modal (CR-01 isSaving guard verified) → `adminUpdateGlobalParams` → new global_params row → audit_log → router.refresh() → SeedBanner disappears |
| Admin partner management | ✓ | /accounts → 6-col table → "Create new partner" modal → `adminCreateInvitation` → `InviteUrlModal` one-time URL surface |
| PDF immutability invariant | ✓ | `proposals.params_snapshot` JSONB never mutated; later coefficient edits create new `global_params` row but stored proposals re-render byte-identically; PROP-17 CI gate verifies SHA-256 determinism |
| Soft-delete + purge cron | ✓ (logic-verified) | partner soft-delete → 30+ days → cron fires → dual-auth route → `purgeSoftDeleted()` → blob + row removed + audit log; **cron has never actually fired in prod** (next: 1st or 15th 03:00 UTC; expected — not a blocker) |

## Cross-cutting invariants (5/5 verified)

- **ADMIN-09 commission invisibility** — `commission_pct` confined to 6 authorized files (CoefficientsEditor, ExplainTool, SaveConfirmModal, HistoryDiff, HistoryTable, adminUpdateGlobalParams); CR-03 fix in `actions.ts:118-124` redacts server-log error path. Exhaustive grep across `app/` + `src/` confirms zero leak paths to partner-facing surfaces.
- **No-Vercel-only-imports rule (BOOT-05/06)** — two-layer (ESLint `no-restricted-imports` + CI grep gate). Zero violations.
- **Drizzle migration discipline (BOOT-09/10)** — 4 versioned SQL files committed; `check:no-drizzle-push` CI gate; production migrations require typed `MIGRATE PROD` confirmation + GitHub Environment approval.
- **i18n discipline (SHELL-06)** — ESLint blocks hardcoded JSXText (one documented `labelFr()` deviation in proposal detail page is ESLint-safe).
- **Locale discipline (SHELL-09)** — `formatCurrency/Number/Date` from `src/lib/i18n/format.ts` with explicit `fr-FR`/`en-GB`; ESLint blocks zero-arg `Intl.NumberFormat()`.

## Drizzle correlated-subquery latent-bug audit

The bug pattern fixed in commit `4879831` (`${schema.users.id}` interpolation emitting unqualified `"id"`, binding to the wrong inner-table column) was specifically audited across all `sql\`...\`` template usages in `src/lib/db/queries/`:

- `proposals.ts` lines 46, 166, 221, 225 — tuple comparisons and ILIKE on FROM-table columns; no cross-table name resolution → **SAFE**
- `global-params.ts` lines 102, 107-125 — tuple comparison on FROM-table; LEFT JOIN uses Drizzle builder (not raw template) → **SAFE**
- `users.ts` lines 48-53 — the fixed correlated subquery; uses raw `users.id` literal → **SAFE (fix is correct)**
- `audit-log.ts` — no `${schema.*}` interpolations in subqueries → **SAFE**

**Conclusion:** The bug pattern existed in exactly one location and has been fixed. No other latent instances in the codebase.

## Known follow-ups (acknowledged; none milestone-blocking)

| Follow-up | Source | Status | Blocker for v1.1 close? |
|---|---|---|---|
| BOOT-03 Neon 3-branch split | STATE.md Phase 5 follow-up | OPEN | NO — functionally green |
| Admin password rotation (`leasetic2026` → strong) | STATE.md Phase 6 follow-up #1 | PENDING | NO — must complete *before first partner onboarded*, not before milestone close |
| Better Auth `trustedOrigins` hardening | STATE.md Phase 6 follow-up #2 | DEFERRED v1.2 | NO |
| `lastLoginAt` never written (ADMIN-05 operational gap) | This audit (WR-AUDIT-01) | OPEN | NO — column nullable; `timeAgo()` handles null |
| DATA-11 10-year PDF retention legal sign-off | Open Q3 | PENDING | NO — Thomas reply pending; stub committed |
| `seed-partner-launch.ts` missing npm script (IN-01) | Phase 10 review | OPEN | NO — one-time launch tooling |
| CUT-09 OVH live smoke deploy | D-10-01 explicit | DEFERRED Sept 2026 | NO — capability shipped |
| Stale `[~]` markers (CALC-07, PROP-01) | This audit | COSMETIC | NO — functionally satisfied; optional checkbox update |

## What landed this milestone (5 lines per phase)

**Phase 5: Bootstrap & Deploy** — Next.js + Vercel + Neon Postgres + Vercel Blob + Drizzle 0.45.2; `lib/storage` + `lib/db` portable adapters (BOOT-05); ESLint + CI grep gates enforce no-Vercel-only-imports; `db-migrate.yml` workflow with `MIGRATE PROD` typed confirmation; `/healthz` live exercising DB + blob round-trip.

**Phase 6: Auth & Shell** — Better Auth 1.6.9 with argon2id; admin-invited flow (no SMTP, admin-mediated invitation + reset); hidden `/[adminSegment]` 2-layer gate (env-segment notFound → requireAdmin); 231-key FR/EN i18n dictionary with compile-time parity proof; SHELL-12 error boundary + SHELL-13 localized 404.

**Phase 7: Calc Engine + Proposal Form** — Pure-TS `src/lib/calc/` (v10 formula preserved); 30-case golden corpus + assertCalc/assertValidity ports; live preview with 300ms-debounced `computeLoyer()`; 14-input proposal form with RHF + Zod blur-validation; LC reference + Validity selector preserving v10 ergonomics.

**Phase 8: Persistence + PDF Pipeline** — `proposals` + `global_params` (append-only) + `audit_log` tables; `params_snapshot` immutability invariant (PROP-23); `@react-pdf/renderer` with byte-deterministic CI gate (`__pdf-fixtures__/expected.sha256.txt`); cursor-paginated home list + ILIKE search; soft-delete with 30-day purge window; manual purge CLI.

**Phase 9: Admin Surface** — `/[adminSegment]/coefficients` (editor + history + ExplainTool — sole non-editor commission_pct surface); `/[adminSegment]/accounts` (6-col partners list with proposal counts); `src/lib/admin/actions.ts` 6 wrappers (audit-aware); 42/42 threats verified closed (ASVS L1); code review caught + fixed 3 critical + 6 warning findings.

**Phase 10: Cutover & Polish** — `docs/operations/deploy-ovh.md` runbook + `scripts/smoke-ovh.ts` 7-step lifecycle (OVH execution → September); twice-monthly Vercel Cron → `/api/internal/purge-soft-deleted` with `timingSafeEqual` auth; `scripts/purge-test-data.ts` + email-pattern discriminator; `<SeedBanner>` on coefficients page; 55/55 threats verified closed.

## Phase-by-phase verification depth

| Phase | SUMMARY.md plans | REVIEW.md | SECURITY.md | VERIFICATION.md |
|---|---|---|---|---|
| 5 — Bootstrap & Deploy | 7 | — | — | — (verifier_enabled: false) |
| 6 — Auth & Shell | 9 | — | — | — |
| 7 — Calc + Form | 6 | — | — | — |
| 8 — Persistence + PDF | ~14 | ✓ (status: clean post-fix) | — | — |
| 9 — Admin Surface | 4 | ✓ (12 findings → 9 fixed; 3 INFO deferred) | ✓ (42/42 threats closed) | — |
| 10 — Cutover & Polish | 6 | ✓ (9 findings → 7 fixed; 2 INFO deferred) | ✓ (55/55 threats closed) | — |

**Project policy:** `verifier_enabled: false` in `.planning/config.json` skips per-phase VERIFICATION.md. Verification evidence comes from SUMMARY/REVIEW/SECURITY artifacts + this milestone-level integration check.

## Recommendation

**Proceed with `/gsd-complete-milestone 1.1`.**

All 108 requirements have cross-phase wiring verified or are self-contained by design. All 6 cross-phase junctions are wired end-to-end. All 5 end-to-end flows trace cleanly. All cross-cutting invariants hold. Code reviews caught and fixed every critical issue. Security audits verified 97 threats closed in Phases 9+10 (the more security-sensitive phases). The Drizzle correlated-subquery latent-bug pattern is isolated to the one location that was fixed.

The 3 documented warnings (`lastLoginAt` unwired, `seed-partner-launch.ts` script missing, Neon branch split deferred) are operational gaps with low risk and clear future-work paths. None compromise the milestone goal of "Hosted Web App Foundation."

The two stale `[~]` markers (CALC-07, PROP-01) can be optionally cleaned up to `[x]` in REQUIREMENTS.md before archiving — this audit confirms both are functionally satisfied.

**One pre-launch action remains** (not a milestone close blocker, but a launch-day blocker for the *first real partner*): rotate the shared admin password `leasetic2026` to individual strong passwords. The `adminCreatePasswordReset` flow is wired and ready.
