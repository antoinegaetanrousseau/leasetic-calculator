---
phase: 22-partner-types-commission-free-proposals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/auth/index.ts
  - scripts/backfill-partner-type.ts
  - package.json
  - src/lib/pdf/no-commission.test.ts
autonomous: false
requirements: [PTYPE-01, PTYPE-02, PTYPE-06]
must_haves:
  truths:
    - "The users table has a partner_type column constrained to ('Agent','Commercial','Partenaire') with default 'Partenaire'"
    - "session.user.partnerType is populated by Better Auth for every authenticated user"
    - "Every pre-existing account has partner_type = 'Partenaire' after the backfill runs"
    - "proposals.params_snapshot type carries partnerType + commissionApplied so future snapshots can record them"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "partner_type column + CHECK constraint on users; paramsSnapshot type extension"
      contains: "partner_type"
    - path: "src/lib/auth/index.ts"
      provides: "partnerType registered in Better Auth user.additionalFields (input:false)"
      contains: "partnerType"
    - path: "scripts/backfill-partner-type.ts"
      provides: "idempotent PTYPE-02 backfill with Neon-prod typed-confirmation gate"
      min_lines: 40
    - path: "drizzle"
      provides: "new generated migration adding partner_type"
  key_links:
    - from: "src/lib/auth/index.ts"
      to: "src/db/schema.ts"
      via: "additionalFields registration mirrors the schema column"
      pattern: "partnerType"
    - from: "scripts/backfill-partner-type.ts"
      to: "users.partner_type"
      via: "UPDATE ... WHERE partner_type IS NULL idempotency"
      pattern: "partner_type"
---

<objective>
Add the `partner_type` dimension to the data layer: a new `users.partner_type` column (enum-constrained, default `Partenaire`), its Better Auth `additionalFields` registration so it rides on `session.user`, the `proposals.params_snapshot` TypeScript type extension (`partnerType` + `commissionApplied`), a generated Drizzle migration, and an idempotent backfill that sets every existing account to `Partenaire`.

Purpose: PTYPE-01 (schema for the selector), PTYPE-02 (backfill so behavior is unchanged for current partners), and the PTYPE-06 snapshot-type foundation. This is the schema-layer foundation Wave 2 builds on.
Output: schema.ts column + CHECK, auth registration, snapshot type, generated migration, backfill script, npm script, KNOWN_MIGRATIONS update.

[BLOCKING] This repo FORBIDS `drizzle-kit push` (CI guard `scripts/check-no-drizzle-push.sh`). Schema sync is `db:generate` → `db:migrate` → backfill. The schema-sync task is mandatory: build/type checks pass without it (types come from schema.ts), creating a false-positive verification state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md
@.planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add partner_type column + auth registration + snapshot type</name>
  <files>src/db/schema.ts, src/lib/auth/index.ts</files>
  <read_first>
    - src/db/schema.ts (current users additionalFields block ~lines 44-64; paramsSnapshot jsonb type on proposals ~lines 212-222; type exports ~lines 121-135)
    - src/lib/auth/index.ts (additionalFields block ~lines 160-175 — role/displayName/language/theme/sessionVersion/createdBy registration)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"src/db/schema.ts" and the additionalFields + CHECK constraint pattern; §"Zod schema — z.enum" note)
  </read_first>
  <action>
    In src/db/schema.ts: add `partnerType: text('partner_type').notNull().default('Partenaire')` inside the users additionalFields block, alongside the existing `role`/`displayName`/`language`/`theme`/`sessionVersion` columns. Add `check('users_partner_type_check', sql\`${table.partnerType} IN ('Agent', 'Commercial', 'Partenaire')\`)` to the users table constraints array (the same array holding `users_role_check`). The `default('Partenaire')` is mandatory — it satisfies the PTYPE-02 invariant that existing rows become Partenaire when the column is added with NOT NULL.

    Still in src/db/schema.ts: extend the `paramsSnapshot` jsonb `$type<...>()` annotation on the proposals table to additionally carry `partnerType: 'Agent' | 'Commercial' | 'Partenaire'` and `commissionApplied: boolean`. This is a TypeScript-only type widening (jsonb has no column-level shape constraint); no migration is required for this field. Keep all existing snapshot fields (commissionPct, maxAmount, validityDays, coefficients).

    In src/lib/auth/index.ts: register `partnerType: { type: 'string', required: false, defaultValue: 'Partenaire', input: false }` in the `user.additionalFields` object. Use `input: false` (matching `role` and `sessionVersion`) so the field is locked against client-side `/api/auth/update-user` mutation — a partner must NOT be able to change their own type (privilege-escalation threat T-22-01-E). This makes `session.user.partnerType` available everywhere session is read.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "partner_type" src/db/schema.ts` shows the column definition with `.default('Partenaire')` and `.notNull()`
    - `grep -n "users_partner_type_check" src/db/schema.ts` shows the CHECK constraint listing exactly 'Agent','Commercial','Partenaire'
    - `grep -n "commissionApplied" src/db/schema.ts` shows the paramsSnapshot type extension
    - `grep -n "partnerType" src/lib/auth/index.ts` shows the additionalFields registration with `input: false`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>users.partner_type column + CHECK exist in schema.ts; partnerType registered in Better Auth additionalFields with input:false; paramsSnapshot type carries partnerType + commissionApplied; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write idempotent backfill script + npm script + KNOWN_MIGRATIONS</name>
  <files>scripts/backfill-partner-type.ts, package.json, src/lib/pdf/no-commission.test.ts</files>
  <read_first>
    - scripts/backfill-coefficient-history.ts (the analog: dotenv import, REQUIRED_CONFIRM_VALUE, URL.hostname Neon-prod gate, lazy db import, idempotency count-check, process.exit discipline)
    - scripts/backfill-partner-company-name.ts (the closest single-column UPDATE analog with the _preload-mock-server-only.cjs preload pattern in package.json)
    - package.json (the scripts block — existing `db:backfill:coefficient-history` and `db:backfill:partner-company-name` entries use `tsx -r ./scripts/_preload-mock-server-only.cjs`)
    - src/lib/pdf/no-commission.test.ts (the KNOWN_MIGRATIONS Set in the drizzle-guard test ~lines 565-592)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"scripts/backfill-partner-type.ts")
  </read_first>
  <action>
    Create scripts/backfill-partner-type.ts following scripts/backfill-coefficient-history.ts exactly: `import 'dotenv/config'`; read `process.env.DATABASE_URL` (exit 2 if unset); derive `hostname = new URL(databaseUrl).hostname` (use `.hostname` NOT `.host` — the bug_011 trap; exit 2 on malformed URL); if `hostname.endsWith('.neon.tech')` require `process.env.BACKFILL_CONFIRM === 'YES'` (exit 2 with a message naming the hostname otherwise). Then lazy-import `{ db, schema }` from '../src/lib/db/index'. Idempotency: `SELECT count(*) FROM users WHERE partner_type IS NULL` — if 0, log "Already backfilled" and `process.exit(0)`. Otherwise run a single `UPDATE users SET partner_type = 'Partenaire' WHERE partner_type IS NULL`, log the affected count, exit 0. Wrap with `main().catch((err) => { console.error('[backfill] FATAL:', err); process.exit(1); })`.

    In package.json scripts block, add `"db:backfill:partner-type": "tsx -r ./scripts/_preload-mock-server-only.cjs scripts/backfill-partner-type.ts"` mirroring the existing `db:backfill:partner-company-name` entry verbatim in form.

    In src/lib/pdf/no-commission.test.ts, add the newly generated migration filename (the exact name drizzle-kit produces in Task 3 — e.g. `0005_*.sql` or `0006_*.sql` depending on the on-disk ordinal) to the `KNOWN_MIGRATIONS` Set so the drizzle-guard test stays green after the migration lands. Do NOT hardcode `0006`; use the actual generated filename from Task 3.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit scripts/backfill-partner-type.ts 2>&1 | tail -3; grep -c "db:backfill:partner-type" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "URL(databaseUrl).hostname" scripts/backfill-partner-type.ts` confirms `.hostname` (not `.host`) is used
    - `grep -n "BACKFILL_CONFIRM" scripts/backfill-partner-type.ts` shows the Neon-prod typed-confirmation gate
    - `grep -n "partner_type IS NULL" scripts/backfill-partner-type.ts` shows BOTH the idempotency count-check and the UPDATE WHERE clause
    - `grep -c "db:backfill:partner-type" package.json` returns 1
    - The migration filename added to KNOWN_MIGRATIONS in no-commission.test.ts exactly matches the file produced in Task 3
  </acceptance_criteria>
  <done>backfill script created with hostname gate + idempotency; npm script added; KNOWN_MIGRATIONS updated to the actual generated migration filename.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Generate + apply migration, run backfill</name>
  <files>drizzle/</files>
  <read_first>
    - scripts/check-no-drizzle-push.sh (confirm `push` is forbidden — do NOT run it)
    - drizzle/0004_phase12_drafts_and_history.sql (header-comment + ALTER TABLE ADD COLUMN + CHECK statement-breakpoint pattern)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"drizzle/...partner_type (new migration)")
  </read_first>
  <action>
    Run `npm run db:generate` to produce a new numbered SQL migration from the schema.ts change in Task 1. Drizzle-kit assigns the next ordinal automatically — DO NOT hand-name or hardcode `0006`; the highest on-disk migration is `0004`, so the generated file may be `0005_*.sql`. Inspect the generated SQL to confirm it contains `ALTER TABLE "users" ADD COLUMN "partner_type" text NOT NULL DEFAULT 'Partenaire'` and the `users_partner_type_check` CHECK constraint, and that it adds NOTHING else (no proposals change — the snapshot type is jsonb, no DDL).

    Apply it: run `npm run db:migrate` against the configured DATABASE_URL. If `db:migrate` requires a non-suppressible interactive confirmation against the live DB, STOP and surface it (this plan is `autonomous: false` for exactly this reason). Then run the backfill: `npm run db:backfill:partner-type`.

    Go back to Task 2's deliverable and confirm the KNOWN_MIGRATIONS Set in no-commission.test.ts now lists the ACTUAL generated filename from this step (correct it if the ordinal differs from the guess).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && ls drizzle/*.sql | tail -1 && npm run check:no-drizzle-push 2>&1 | tail -2 && npm run test -- no-commission 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A new `drizzle/000N_*.sql` file exists containing `ADD COLUMN "partner_type"` and `users_partner_type_check`
    - `npm run check:no-drizzle-push` exits 0 (no push usage introduced)
    - The new migration filename is present in the KNOWN_MIGRATIONS Set in src/lib/pdf/no-commission.test.ts and the drizzle-guard test passes
    - After backfill, `SELECT count(*) FROM users WHERE partner_type IS NULL OR partner_type NOT IN ('Agent','Commercial','Partenaire')` returns 0 (all existing accounts are 'Partenaire')
  </acceptance_criteria>
  <done>Migration generated (next ordinal, not hardcoded), applied, backfill run; no-push guard green; drizzle-guard test green with the real migration filename.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/auth/update-user | A partner could attempt to set their own partner_type via the Better Auth update-user endpoint |
| local shell → Neon prod DB | The backfill script can write to production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-E | Elevation of Privilege | partnerType additionalField | mitigate | Register with `input: false` in auth/index.ts so Better Auth rejects client-side updates; type changes go only through the admin-gated server action (Plan 22-03). |
| T-22-01-T | Tampering | backfill script vs prod DB | mitigate | Neon-prod typed-confirmation gate via `URL.hostname` + `BACKFILL_CONFIRM=YES`; idempotency count-check prevents double-apply. |
| T-22-01-D | DoS / data integrity | NOT NULL column add on existing rows | accept | DEFAULT 'Partenaire' on the ADD COLUMN makes the migration backfill-safe; no NULL window. Low row count (single-digit partners). |
| T-22-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; nothing to audit. If `db:generate` pulls a transitive update, the existing lockfile pins prevent drift. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run test -- no-commission` passes (drizzle-guard recognizes the new migration)
- `npm run check:no-drizzle-push` exits 0
- The generated migration adds only the partner_type column + CHECK
- All existing users rows have partner_type = 'Partenaire' post-backfill
</verification>

<success_criteria>
users.partner_type exists and is enum-constrained; session.user.partnerType is populated and client-immutable; every existing account is 'Partenaire'; the params_snapshot type can record partnerType + commissionApplied; the schema was synced via generate→migrate (never push).
</success_criteria>

<output>
Create `.planning/phases/22-partner-types-commission-free-proposals/22-01-SUMMARY.md` when done. Record the EXACT generated migration filename (ordinal) so downstream plans and the verifier reference the right file.
</output>
