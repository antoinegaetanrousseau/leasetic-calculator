---
phase: 22-partner-types-commission-free-proposals
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/calc/calc.golden.test.ts
autonomous: true
requirements: [PTYPE-04]
must_haves:
  truths:
    - "computeLoyer with commissionPct: 0 yields loyer = montant HT × coefficient / 100 to ±0.01 €"
    - "computeLoyer with the Partenaire commission percentage yields the unchanged frozen formula to ±0.01 €"
    - "The golden corpus covers the commission-free branch across the full 4-tranche × 3-duration matrix"
    - "src/lib/calc/formula.ts is NOT edited — the commission-free result is achieved purely via the commissionPct:0 call-site seam"
  artifacts:
    - path: "src/lib/calc/calc.golden.test.ts"
      provides: "commission-free golden describe block (≥12 individual it() cases)"
      contains: "commissionPct: 0"
  key_links:
    - from: "src/lib/calc/calc.golden.test.ts"
      to: "src/lib/calc/formula.ts computeLoyer"
      via: "commissionPct: 0 argument exercises the existing override seam"
      pattern: "commissionPct: 0"
---

<objective>
Prove the commission-free calc variant (PTYPE-04) with a golden corpus, WITHOUT editing the frozen formula. The commission-free loyer (`montant HT × coefficient / 100`) is the existing `applyFormula` evaluated with `commissionPct: 0` — a valid argument to the existing override seam, not a formula change. This plan adds golden cases that lock that behavior to ±0.01 €, so Wave 2's call-site changes (passing 0 for Agent/Commercial) are verified against a corpus.

Purpose: PTYPE-04 — golden coverage for all three partner types (Partenaire = unchanged formula; Agent/Commercial = commission-free, identical to each other per D-01).
Output: a new commission-free `describe` block in calc.golden.test.ts.

FROZEN-FORMULA INVARIANT: `src/lib/calc/formula.ts` (computeLoyer / applyFormula) must NOT be edited. The seam already exists. This is verified as an acceptance criterion (git diff shows no edits to formula.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md
@.planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add commission-free golden corpus (RED → GREEN)</name>
  <files>src/lib/calc/calc.golden.test.ts</files>
  <behavior>
    - Given commissionPct: 0, computeLoyer returns loyer = +((amount * Number(coeffStr)) / 100).toFixed(2) for every (tranche, duration) cell in the fixture.
    - Given the fixture commission percentage (fixtureComm = 5), computeLoyer returns the unchanged frozen formula +((amount * (1 + 5/100) * coeff) / 100).toFixed(2) — the existing corpus already proves this; keep it green.
    - The commission-free loyer is strictly less than the Partenaire loyer for the same inputs whenever commission > 0 (sanity assertion that the branch actually drops the factor).
    - All assertions hold to ±0.01 € (.toFixed(2) parity).
  </behavior>
  <read_first>
    - src/lib/calc/calc.golden.test.ts (fixtureCoeffs/fixtureComm/fixtureMax setup ~lines 32-56; the expectedLoyer helper and call() helper; the existing happy-path it() matrix that uses individual it() invocations to satisfy a static grep gate — see STATE.md note "Golden corpus happy-path matrix uses individual it() invocations")
    - src/lib/calc/formula.ts (the commissionPct override seam ~lines 33-35, 143-144; applyFormula ~lines 113-121 — DO NOT EDIT, read only to confirm the seam)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"src/lib/calc/calc.golden.test.ts" — the expectedLoyerFree helper guidance and the individual-it() requirement)
  </read_first>
  <action>
    Add a second helper `expectedLoyerFree(amount, coeffStr)` returning `+((amount * Number(coeffStr)) / 100).toFixed(2)` (the commission-free formula). Add a `call0(amount, durationMonths)` helper identical to the existing `call()` but passing `commissionPct: 0`. Add a new `describe('commission-free corpus (Agent/Commercial — PTYPE-04)', ...)` block with INDIVIDUAL `it()` cases (NOT a for-loop — preserve the static-count grep gate discipline from STATE.md) covering the full 4-tranche × 3-duration matrix (12 cases minimum). Each case asserts `call0(amount, dur).computed.loyer` equals `expectedLoyerFree(amount, coeff)` to ±0.01 €, and asserts that value is strictly less than the corresponding `call(amount, dur)` Partenaire loyer. Keep the existing Partenaire corpus untouched and green.

    RED: write the new it() cases first and run them — they must compile and run against the existing computeLoyer (which already accepts commissionPct). GREEN: because the seam already exists, no production code changes — the cases should pass immediately once the math is correct. If any case fails, the bug is in the test's expected math, not in formula.ts (which is frozen). Commit RED then GREEN per the TDD cycle even though production is untouched (the test file is the deliverable).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run test -- calc.golden 2>&1 | tail -8 && git diff --stat src/lib/calc/formula.ts | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- calc.golden` passes including the new commission-free describe block
    - `git diff src/lib/calc/formula.ts` shows NO changes (frozen-formula invariant preserved)
    - `grep -c "commissionPct: 0" src/lib/calc/calc.golden.test.ts` returns ≥ 1
    - `grep -c "  it(" src/lib/calc/calc.golden.test.ts` increased by ≥ 12 vs the pre-change count (individual it() cases, not a loop)
    - At least one assertion confirms commission-free loyer < Partenaire loyer for the same inputs
  </acceptance_criteria>
  <done>Commission-free golden corpus added (≥12 individual it() cases) and green; formula.ts untouched; Partenaire corpus still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a test-only calc-corpus addition; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-T | Tampering | frozen formula integrity | mitigate | Acceptance criterion asserts `git diff formula.ts` is empty; commission-free achieved via call-site argument only. |
| T-22-02-I | Information disclosure | commission leakage via test fixtures | accept | Golden corpus is internal calc math; no commission AMOUNT is rendered to any user surface here. PTYPE-07 grep gates (Plan 22-05) cover surfaces. |
| T-22-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to audit. |
</threat_model>

<verification>
- `npm run test -- calc.golden` green (both Partenaire and commission-free corpora)
- `git diff src/lib/calc/formula.ts` empty
- Commission-free cases use commissionPct: 0 and assert montant HT × coefficient / 100 to ±0.01 €
</verification>

<success_criteria>
The commission-free formula is locked by a golden corpus across the full tranche×duration matrix; the Partenaire formula is unchanged and still proven; formula.ts is frozen (no edits). Wave 2 call-site changes can be verified against this corpus.
</success_criteria>

<output>
Create `.planning/phases/22-partner-types-commission-free-proposals/22-02-SUMMARY.md` when done.
</output>
