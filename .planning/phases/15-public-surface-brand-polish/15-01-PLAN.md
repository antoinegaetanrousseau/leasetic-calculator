---
phase: 15-public-surface-brand-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/layout.tsx
  - app/globals.css
  - app/(public)/layout.test.tsx
autonomous: true
requirements: [PUB-01, PUB-02]

must_haves:
  truths:
    - "Visiting /login renders the Leasétic SVG logo lockup (BrandLogo) centered above the form card instead of the v1.1 plain-text 'Leasétic' header"
    - "Visiting /invite/[token] renders the same BrandLogo lockup centered above the invite form (inherits via shared (public) layout)"
    - "Visiting /reset/[token] renders the same BrandLogo lockup centered above the reset form (inherits via shared (public) layout)"
    - "The body background on all 3 public routes is var(--paper); the form card stays var(--surface) — chrome-fill rule preserved"
    - "Light theme shows logo-light.svg (mark #6DC388 + --navy wordmark); dark theme shows logo-dark.svg (mark #6DC388 + #e6e9ef wordmark) — picked by the existing Phase 11 CSS picker keyed on html[data-theme]"
    - "The LocaleToggle + ThemeToggle stay top-right (position: absolute, top: 24, right: 24) on all 3 routes — NOT relocated to a sidebar"
    - "The 3 public routes share one reusable app/(public)/layout.tsx (no logo+paper-bg duplication across login/invite/reset page files)"
    - "No flash of unstyled content on reload — Phase 6 no-flash inline script sets data-theme before first paint, BrandLogo CSS picker rides this synchronously"
  artifacts:
    - path: "app/(public)/layout.tsx"
      provides: "PublicLayout server component with BrandLogo invocation replacing plain-text header"
      contains: "<BrandLogo"
    - path: "app/globals.css"
      provides: "Phase 15 .public-page-logo CSS class with clamp() responsive sizing"
      contains: ".public-page-logo"
    - path: "app/(public)/layout.test.tsx"
      provides: "Vitest layout test asserting BrandLogo rendered with .public-page-logo and plain-text header removed"
      contains: "public-page-logo"
  key_links:
    - from: "app/(public)/layout.tsx"
      to: "src/components/ui/BrandLogo.tsx"
      via: "named import + JSX invocation with className=\"public-page-logo\" and alt={t('sidebar.brand', lang)}"
      pattern: "import \\{ BrandLogo \\} from '@/components/ui/BrandLogo'"
    - from: "app/(public)/layout.tsx"
      to: "app/globals.css"
      via: "className=\"public-page-logo\" matched by .public-page-logo CSS rule"
      pattern: "className=\"public-page-logo\""
    - from: ".public-page-logo (wrapper)"
      to: ".public-page-logo img (inner img)"
      via: "CSS cascade — wrapper clamps width, inner img width: 100% fills wrapper (Phase 11 BrandLogo renders <img width=190 height=32> HTML attrs that need overriding)"
      pattern: "\\.public-page-logo img"
---

<objective>
Replace the v1.1 plain-text "Leasétic" header in `app/(public)/layout.tsx` with the Phase 11 `<BrandLogo />` SVG component, add a `.public-page-logo` CSS class with responsive clamp() sizing in `app/globals.css`, and add one Vitest layout test asserting the swap. All 3 public routes (`/login`, `/invite/[token]`, `/reset/[token]`) inherit the change via the shared `(public)` layout — no per-route file modifications.

Purpose: Closes PUB-01 + PUB-02. Brings the public surfaces in line with the Phase 11 brand foundation (light/dark logo lockup, mark `#6DC388`, theme-correct wordmark ink). Phase 15 is shell-only — forms, footer, toggle cluster, color tokens all preserved verbatim.

Output: One modified layout (~10 line swap), one CSS append (~12 lines), one new test file (~70 lines). Two atomic commits (RED test + GREEN implementation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-public-surface-brand-polish/15-CONTEXT.md
@.planning/phases/15-public-surface-brand-polish/15-UI-SPEC.md
@.planning/phases/15-public-surface-brand-polish/15-PATTERNS.md

# Source files the executor will read or modify
@app/(public)/layout.tsx
@src/components/ui/BrandLogo.tsx
@src/components/ui/BrandLogo.test.tsx

<interfaces>
<!-- Phase 11 BrandLogo contract — consumed verbatim, NOT modified by Phase 15 -->

From src/components/ui/BrandLogo.tsx (lines 13-22):
```typescript
export interface BrandLogoProps {
  width?: number;   // default 190 — Phase 15 OMITS this; CSS class drives sizing
  height?: number;  // default 32  — Phase 15 OMITS this; CSS class drives sizing
  alt?: string;     // default ''  — Phase 15 passes alt={t('sidebar.brand', lang)} = 'Leasétic'
  className?: string; // Phase 11 prepends 'brand-logo' — passing 'public-page-logo' yields 'brand-logo public-page-logo'
}

export function BrandLogo(props: BrandLogoProps): JSX.Element;
```

Rendered DOM (verbatim from BrandLogo.tsx lines 31-53):
```html
<span class="brand-logo public-page-logo" style="display:inline-block; line-height:0;">
  <img class="brand-logo-light" src="/logo-light.svg" alt="Leasétic" width="190" height="32" />
  <img class="brand-logo-dark"  src="/logo-dark.svg"  alt="Leasétic" width="190" height="32" />
</span>
```

Phase 11 CSS picker already in globals.css (lines 543-545 — DO NOT MODIFY):
```css
/* === Phase 11 brand-logo theme picker (UI-SPEC §6.1, D-09) === */
html[data-theme="light"] .brand-logo-dark  { display: none; }
html[data-theme="dark"]  .brand-logo-light { display: none; }
```

From app/(public)/layout.tsx (current — lines being REMOVED, 56-68):
```tsx
{/* Leasétic logo — displayed above the card, weight 700, 22px, color --navy */}
<div
  style={{
    fontWeight: 700,
    color: 'var(--navy)',
    fontSize: 22,
    marginBottom: 16,
    userSelect: 'none',
  }}
>
  {t('sidebar.brand', lang)}
</div>
```

From app/(public)/layout.tsx (line 1-3, existing imports — ADD BrandLogo to this cluster):
```typescript
import { ThemeToggle } from '@/components/ThemeToggle';
import { LocaleToggle } from '@/components/LocaleToggle';
import { getCurrentLang, getCurrentTheme, t } from '@/lib/i18n';
```

From src/lib/i18n/dictionaries.ts:
- `sidebar.brand` key returns `'Leasétic'` in both FR (line 41) and EN (line 707) — no new keys needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap plain-text header for BrandLogo + add .public-page-logo CSS class + Vitest layout test (RED → GREEN)</name>
  <files>app/(public)/layout.tsx, app/globals.css, app/(public)/layout.test.tsx</files>
  <read_first>
    - app/(public)/layout.tsx (the file being modified — note current import cluster lines 1-3, the plain-text logo block lines 56-68, and surrounding preserved chrome lines 27-54 + 70-93)
    - src/components/ui/BrandLogo.tsx (Phase 11 contract — named export `BrandLogo`, accepts `className` + `alt`, renders `<span class="brand-logo {className}"><img class="brand-logo-light"><img class="brand-logo-dark"></span>`)
    - src/components/ui/BrandLogo.test.tsx (closest test analog — `render` + `container.querySelector` + `expect(...).toHaveClass(...)` pattern, with `afterEach(() => cleanup())`)
    - app/globals.css lines 543-545 (Phase 11 brand-logo CSS picker — append the new Phase 15 block AFTER these existing rules and after the Phase 11 Stepper banner at lines 547-551, at end of file)
    - .planning/phases/15-public-surface-brand-polish/15-UI-SPEC.md §6.1 (BrandLogo invocation contract) + §6.3 (exact CSS declaration) + §9 (test contract)
    - .planning/phases/15-public-surface-brand-polish/15-PATTERNS.md (file role mapping + analog patterns)
  </read_first>

  <behavior>
    RED phase (failing test commit):

    Test 1 — "renders BrandLogo with .public-page-logo class in the public layout":
      Given: `PublicLayout` rendered (async server component) with arbitrary children.
      When: the resulting DOM is queried for `span.brand-logo.public-page-logo`.
      Then: exactly one matching element exists, containing exactly two `<img>` children — one with class `brand-logo-light` (src ending `/logo-light.svg`), one with class `brand-logo-dark` (src ending `/logo-dark.svg`). Both `<img>` tags have `alt="Leasétic"`.

    Test 2 — "does NOT render the v1.1 plain-text Leasétic header":
      Given: `PublicLayout` rendered with arbitrary children.
      When: the DOM is searched for a `<div>` with inline style `fontSize: '22px'` AND `fontWeight: '700'`.
      Then: no such element exists (`expect(oldHeader).toBeUndefined()`).

    Test 3 — "preserves the top-right LocaleToggle + ThemeToggle cluster":
      Given: `PublicLayout` rendered with arbitrary children.
      When: the DOM is queried for the absolute-positioned toggle wrapper (`div` with inline style containing `position: absolute`, `top: 24`, `right: 24`).
      Then: exactly one such wrapper exists and contains both the LocaleToggle and ThemeToggle (verified via test-ids or rendered button text per existing Phase 5/6 convention — adapt to whatever the components render in jsdom).

    All 3 tests MUST fail before the GREEN edits land (assertions 1 + 3 fail because BrandLogo not yet imported; assertion 2 fails because the plain-text `<div>` is still present).

    GREEN phase (passing implementation commit):

    Edit 1 — app/(public)/layout.tsx:
      - Add `import { BrandLogo } from '@/components/ui/BrandLogo';` to the existing import cluster (after the `getCurrentLang, getCurrentTheme, t` import on line 3).
      - Remove the entire plain-text logo block (current lines 56-68 — the `{/* Leasétic logo ... */}` comment + the styled `<div>` with `fontWeight: 700, color: 'var(--navy)', fontSize: 22, marginBottom: 16`).
      - Insert in its place: `<BrandLogo className="public-page-logo" alt={t('sidebar.brand', lang)} />` (no width/height props — CSS class drives sizing per UI-SPEC §6.1 + D-Claude-discretion).
      - Preserve EVERYTHING ELSE verbatim: the outer flex-center `<div>` with `background: 'var(--paper)'` + `padding: '24px 16px'`, the top-right toggle cluster (`position: absolute, top: 24, right: 24, gap: 12, zIndex: 10`), `{children}`, and the footer (`marginTop: 32, fontSize: '10.5px', color: 'var(--muted)'` + Mentions légales link).

    Edit 2 — app/globals.css (append at end of file, AFTER line 551 Phase 11 Stepper rule):
      - Add a banner comment line: `/* === Phase 15 public-page logo sizing (PUB-01, PUB-02 — UI-SPEC §6.3, D-08) === */`
      - Add the `.public-page-logo` rule: `width: clamp(140px, 50vw, 200px);` (D-02 + D-05), `height: auto;` (D-07), `display: inline-block;` (matches Phase 11 BrandLogo wrapper default), `margin-bottom: 16px;` (D-04 — matches v1.1 plain-text marginBottom).
      - Add the cascade rule `.public-page-logo img { width: 100%; height: auto; display: block; }` — required because Phase 11 BrandLogo renders `<img>` tags with literal HTML `width="190"` / `height="32"` attributes which take precedence over `auto`; this cascade forces the inner img to fill the clamped wrapper.
      - DO NOT modify the existing Phase 11 brand-logo selectors at lines 543-545 (D-09 explicit).

    Edit 3 — app/(public)/layout.test.tsx (NEW file):
      - Imports: `{ afterEach, describe, expect, it, vi }` from `vitest`, `{ cleanup, render }` from `@testing-library/react`, `PublicLayout` from `./layout`.
      - Mock `@/lib/i18n` via `vi.mock('@/lib/i18n', () => ({ getCurrentLang: async () => 'fr', getCurrentTheme: async () => 'light', t: (key: string) => key === 'sidebar.brand' ? 'Leasétic' : key === 'shell.footer.copyright' ? '© Leasétic 2026' : key === 'shell.footer.privacy' ? 'Mentions légales' : key }))` — server-component dependencies stubbed so jsdom can render without Next.js cookie() runtime.
      - `afterEach(() => cleanup())`.
      - In each `it(...)`, await `PublicLayout({ children: <div data-testid="child" /> })` then `render(ui)` and assert on `container.querySelector(...)`.
      - Implementation note: this is the first Vitest test in the codebase that renders an async server component layout — the `await PublicLayout({ children })` pattern is novel here. Document it in the test file's top-of-file comment so future planners can follow the convention.
  </behavior>

  <action>
    Implement as a strict RED → GREEN cycle (per workflow.tdd_mode TDD convention used in Phases 12, 13, 14):

    Step 1 (RED): Create `app/(public)/layout.test.tsx` with the 3 tests described in `<behavior>`. Run `npm test -- app/\\(public\\)/layout.test.tsx` and CONFIRM all 3 fail (the import of `BrandLogo` will work since the component already ships from Phase 11, but the assertions fail because the layout has not yet been swapped). Commit RED:
      `test(15-01): add failing public layout BrandLogo test (PUB-01, PUB-02)`

    Step 2 (GREEN — file 1): Modify `app/(public)/layout.tsx` per Edit 1 in `<behavior>` — add the named import, remove the plain-text block (lines 56-68), insert the `<BrandLogo className="public-page-logo" alt={t('sidebar.brand', lang)} />` invocation. Preserve all other chrome verbatim. Do NOT touch the toggle cluster, the outer flex-center div, the children placeholder, or the footer.

    Step 3 (GREEN — file 2): Append the new `.public-page-logo` block + `.public-page-logo img` cascade rule to `app/globals.css` per Edit 2 in `<behavior>`. Insert AFTER the existing Phase 11 Stepper rule (currently lines 547-551, at end of file), with the banner comment `/* === Phase 15 public-page logo sizing (PUB-01, PUB-02 — UI-SPEC §6.3, D-08) === */`. Use the EXACT clamp formula `clamp(140px, 50vw, 200px)` (per UI-SPEC §6.3 — no variations).

    Step 4 (GREEN — verify): Run `npm test -- app/\\(public\\)/layout.test.tsx` and CONFIRM all 3 tests pass. Run `npm test` (full suite) and CONFIRM the 872 pre-existing tests still pass (no regression). Run `npm run typecheck`, `npm run lint`, `npm run build` and CONFIRM all 3 exit 0.

    Step 5 (GREEN — grep contracts per UI-SPEC §9.3): Run the following grep gates and CONFIRM each returns the expected count:
      - `grep -c "BrandLogo" app/\\(public\\)/layout.tsx` → 2 (the import + the JSX invocation)
      - `grep -c "public-page-logo" app/\\(public\\)/layout.tsx` → 1 (the className on the JSX invocation)
      - `grep -c "\\.public-page-logo" app/globals.css` → 2 (the wrapper rule + the inner-img cascade rule)
      - `grep -c "clamp(140px, 50vw, 200px)" app/globals.css` → 1
      - `grep -rln "fontSize: 22\\|fontSize: '22'" app/\\(public\\)/` → 0 (plain-text header is gone)
      - `grep -rln "BrandLogo" app/\\(public\\)/` → 1 (mounted only in shared layout, not duplicated per route)

    Step 6 (commit GREEN):
      `feat(15-01): swap public layout plain-text header for BrandLogo + public-page-logo CSS (PUB-01, PUB-02)`

    Implementation discipline notes:
      - Phase 15 is shell-only per D-10. Do NOT modify `<LoginForm>`, `<SetPasswordForm>`, `<InviteForm>`, or any per-route page file under `app/(public)/login/`, `app/(public)/invite/`, `app/(public)/reset/`. The shared layout swap is sufficient — all 3 routes inherit automatically.
      - Do NOT introduce new i18n keys. Reuse the existing `sidebar.brand` key for the BrandLogo `alt` prop (returns `'Leasétic'` in both languages per `src/lib/i18n/dictionaries.ts` lines 41 + 707).
      - Do NOT add `width=` or `height=` props to `<BrandLogo>`. The CSS `.public-page-logo` class is the authoritative sizing source per UI-SPEC §6.1 + D-Claude-discretion. The Phase 11 component's `width={190}` / `height={32}` defaults stay as the underlying `<img>` HTML attribute fallback (overridden by the CSS cascade).
      - Do NOT wrap the BrandLogo in a `<Link href="/login">` or `<a>` tag. Per D-Claude-discretion: making the logo clickable would just refresh `/login` or create a circular nav. The logo is a non-interactive brand anchor (UI-SPEC §6.1 AC-15-BL-06).
      - Do NOT introduce any new color tokens, new `--variables`, or modify existing tokens. Color refresh deferred to v1.3 per PROJECT.md decision row + UI-SPEC §4.5.
      - Do NOT modify the existing Phase 11 `.brand-logo` / `.brand-logo-light` / `.brand-logo-dark` CSS at globals.css lines 543-545 (D-09 explicit).
  </action>

  <verify>
    <automated>
npm test -- app/\\(public\\)/layout.test.tsx \
  && npm test \
  && npm run typecheck \
  && npm run lint \
  && npm run build \
  && [ "$(grep -c 'BrandLogo' 'app/(public)/layout.tsx')" -eq 2 ] \
  && [ "$(grep -c 'public-page-logo' 'app/(public)/layout.tsx')" -eq 1 ] \
  && [ "$(grep -c '\.public-page-logo' app/globals.css)" -eq 2 ] \
  && [ "$(grep -c 'clamp(140px, 50vw, 200px)' app/globals.css)" -eq 1 ] \
  && [ -z "$(grep -rln 'fontSize: 22\|fontSize: '"'"'22'"'"'' 'app/(public)/')" ] \
  && [ "$(grep -rln BrandLogo 'app/(public)/' | wc -l | tr -d ' ')" -eq 1 ]
    </automated>
  </verify>

  <done>
    - app/(public)/layout.test.tsx exists with 3 passing Vitest assertions (BrandLogo with .public-page-logo present, plain-text v1.1 header absent, top-right toggle cluster preserved).
    - app/(public)/layout.tsx imports `BrandLogo` from `@/components/ui/BrandLogo` and renders `<BrandLogo className="public-page-logo" alt={t('sidebar.brand', lang)} />` in place of the v1.1 plain-text `<div>` block. The outer flex-center page wrapper, top-right toggle cluster, `{children}`, and footer are unchanged.
    - app/globals.css has a new section banner `=== Phase 15 public-page logo sizing ===` followed by `.public-page-logo` rule (width clamp(140px, 50vw, 200px); height auto; display inline-block; margin-bottom 16px) and `.public-page-logo img` cascade (width 100%; height auto; display block). The Phase 11 brand-logo CSS at lines 543-545 is unchanged.
    - All 872 pre-existing Vitest tests + the 3 new tests pass.
    - npm run typecheck, lint, build all exit 0.
    - 6 grep gates listed in `<verify>` all return their expected counts.
    - Two atomic commits land: `test(15-01): ...` (RED) and `feat(15-01): ...` (GREEN).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → /login | Untrusted browser → public layout (no auth gate). The 3 public routes (login, invite, reset) are reached by anyone with the URL; layout must not leak secrets. |
| unauthenticated visitor → /invite/[token] | Token in URL is the only credential at this gate; the layout itself adds no new attack surface. |
| unauthenticated visitor → /reset/[token] | Same shape as invite. |
| static SVG asset (public/logo-light.svg, public/logo-dark.svg) → DOM | SVG content is not user-controlled — files on disk, shipped by repo. No CSRF/XSS vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information disclosure | app/(public)/layout.tsx rendered output | mitigate | Grep gate in Vitest test: rendered DOM of PublicLayout does not contain the strings `commission`, `admin`, `[adminSegment]`, or any commission-bearing path segment. The layout only renders BrandLogo + toggle cluster + {children} + footer; no admin URLs are interpolated. Verified by the negative assertion in the test file. |
| T-15-02 | Tampering / XSS | <BrandLogo> SVG injection | accept | BrandLogo renders two `<img>` tags pointing to static files in `public/` (logo-light.svg, logo-dark.svg) shipped by the repo. The `src` attribute is a hard-coded string literal in BrandLogo.tsx — no user input flows in. No DOM-XSS vector. Phase 11 already validated the SVG files have no script tags. |
| T-15-03 | Repudiation / CSRF | BrandLogo wrapper as nav element | accept | Per D-Claude-discretion + AC-15-BL-06: BrandLogo is NOT wrapped in `<Link>` or `<a>`. It is non-interactive. No CSRF surface introduced. Verified by grep contract (`grep -A2 "<BrandLogo" app/(public)/layout.tsx` returns no parent Link/a). |
| T-15-04 | Denial of service | clamp() with pathological viewport widths | accept | The `clamp(140px, 50vw, 200px)` formula handles ALL viewport widths within CSS spec: 0px → 140px (min); 100000px → 200px (max). No layout breakage, no infinite-loop computation. Verified by UI-SPEC §6.3 mobile responsive table covering 240px through 1512px. |
| T-15-05 | Information disclosure | Theme-flash exposing wrong-theme logo | mitigate | Phase 6's no-flash inline `<script>` in `app/layout.tsx` sets `<html data-theme="...">` BEFORE first paint. Phase 11's CSS picker (`html[data-theme="light"] .brand-logo-dark { display: none; }`) is in `app/globals.css`, synchronously linked. Both `<img>` tags render but exactly one is hidden via `display: none` before the user sees anything. Already covered by Phase 6's no-flash test; Phase 15 introduces no new flash vector. Manual smoke per ROADMAP §Phase 15 success criterion #5 reconfirms. |
| T-15-06 | Elevation of privilege | public layout adopting authed/admin chrome | accept | The shared `(public)` layout MUST stay distinct from the `(authed)` and `(admin)` layouts. Phase 15 does NOT pull in `<Topbar>`, `<RetractableSidebar>`, or `requireUser()`. The top-right LocaleToggle + ThemeToggle cluster stays where it is (PUB-01 success criterion #3) and explicitly does NOT relocate to a sidebar. Verified by file-scope: only `app/(public)/layout.tsx` is modified, not `app/(authed)/` or `app/(admin)/`. |

**Disposition summary:** 2 mitigate / 4 accept. All mitigations have automated verification (grep gates + Vitest assertions). Phase 15 introduces NO new attack surface — it swaps one static SVG component for one static text node within an existing public layout. The threat register is intentionally lighter than admin surfaces because no user input, no auth gate, no DB writes, no new endpoints are added.
</threat_model>

<verification>
## Overall Phase 15 verification

**Automated (CI-gated):**
1. `npm test` — all 872 prior tests + 3 new layout tests pass (875 total).
2. `npm run typecheck` exits 0.
3. `npm run lint` exits 0 (no new ESLint violations on the changed files).
4. `npm run build` exits 0.
5. Grep contracts (from Task 1 `<verify>`):
   - `<BrandLogo` appears 1× in `app/(public)/` (the JSX invocation in layout.tsx) → also counted as 2× in layout.tsx alone (import + invocation).
   - `t('sidebar.brand'` count = 1 in `app/(public)/layout.tsx` (the alt prop on BrandLogo).
   - `.public-page-logo` count = 2 in `app/globals.css` (wrapper rule + inner-img cascade).
   - `clamp(140px, 50vw, 200px)` count = 1 in `app/globals.css`.
   - `fontSize: 22` count = 0 in `app/(public)/` (plain-text header removed).
   - `paper` count = 1 in `app/(public)/` (only `background: 'var(--paper)'` in layout.tsx line 35 — not duplicated per route).

**Threat-model gates (T-15-01 verified automatically by negative-assertion gate):**
- Rendered DOM of `<PublicLayout>` (via Vitest container.innerHTML) does NOT contain `commission`, `admin`, or `[adminSegment]`.

**Manual smoke (executor surfaces in SUMMARY.md; NOT blocking per project verifier_enabled=false policy):**

Browsers: Chrome + Edge (the deliverable's two required browsers per PROJECT.md).

For EACH of the 3 routes (`/login`, `/invite/<some-token>`, `/reset/<some-token>`) AND for EACH theme (`[data-theme=light]`, `[data-theme=dark]`):
1. Open the URL.
2. Confirm the BrandLogo SVG renders centered above the form card.
3. Confirm the mark (clover) renders in `#6DC388` green.
4. Confirm the wordmark color matches the theme:
   - Light theme → dark navy (`--navy = #112c3b`).
   - Dark theme → light ink (`#e6e9ef`).
5. Confirm body background is `--paper`:
   - Light theme → `#f5f7fc`.
   - Dark theme → `#0c121c`.
6. Confirm form card background is `--surface`:
   - Light theme → `#ffffff`.
   - Dark theme → `#161e2d`.
7. Confirm the LocaleToggle + ThemeToggle are absolute-positioned top-right (24px from top + right).
8. Reload the page — confirm NO flash of unstyled content (no white background flash on dark theme, no plain-text "Leasétic" flash before SVG paints).
9. Switch theme via ThemeToggle — confirm logo variant swaps correctly without a flash.
10. Resize the browser to 320px width — confirm logo scales down smoothly via clamp (should be ~160px wide at viewport=320).

Total: 3 routes × 2 themes × 2 browsers = **12 smoke checkpoints**. Executor records the result of each in SUMMARY.md.
</verification>

<success_criteria>
Per ROADMAP §Phase 15 (5 success criteria) + REQUIREMENTS.md PUB-01 + PUB-02:

1. ✅ `/login` replaces its v1.1 plain-text "Leasétic" header with the official `logo-light.svg` / `logo-dark.svg` lockup centered above the existing form card; form structure unchanged.
   - **Verified by:** Vitest layout test (Task 1, assertion 1) + grep `fontSize: 22` returns 0 in `app/(public)/`.

2. ✅ Body background on `/login`, `/invite/[token]`, `/reset/[token]` is `--paper`; the centered form card remains `--surface`.
   - **Verified by:** existing `background: 'var(--paper)'` on layout line 35 is preserved (no edit to lines 27-40); form cards are owned by Phase 6 form primitives (also unchanged); manual smoke confirms in Chrome + Edge under both themes.

3. ✅ Language and theme toggles remain in the top-right of all 3 public routes.
   - **Verified by:** Vitest layout test (Task 1, assertion 3) — toggle cluster wrapper with `position: absolute, top: 24, right: 24` is present and contains both LocaleToggle + ThemeToggle. No code path moves them to a sidebar.

4. ✅ All 3 public routes share a single reusable `(public)` layout component.
   - **Verified by:** grep contract — `grep -rln BrandLogo app/(public)/` returns exactly 1 file (`app/(public)/layout.tsx`); the logo + paper-bg pattern is not duplicated per route file.

5. ✅ Manual smoke: opening each of the 3 routes under both `[data-theme=light]` and `[data-theme=dark]` shows the correct logo variant; no flash of unstyled content on reload.
   - **Verified by:** manual smoke runbook in `<verification>` (12 checkpoints across 3 routes × 2 themes × 2 browsers). Executor records sign-off in SUMMARY.md per project verification policy (`verifier_enabled: false` → rigor via SUMMARY + manual smoke + Vitest CI).

Additional Phase 15 success criteria (from UI-SPEC §6.1 + §6.3 acceptance criteria):

6. ✅ AC-15-BL-01..06 (BrandLogo consumption on public surfaces) — verified by Vitest test 1 + grep contracts.
7. ✅ AC-15-CSS-01..05 (.public-page-logo CSS class) — verified by grep contracts (`grep -c .public-page-logo app/globals.css` = 2, `grep -c clamp(140px, 50vw, 200px)` = 1, no new `--variables` in `git diff app/globals.css`).
</success_criteria>

<output>
After completion, create `.planning/phases/15-public-surface-brand-polish/15-01-SUMMARY.md` using the standard summary template. The summary MUST include:

- The two atomic commit SHAs (RED test + GREEN implementation).
- The final 6 grep-gate counts (BrandLogo × 2 in layout.tsx, public-page-logo × 1 in layout.tsx, .public-page-logo × 2 in globals.css, clamp × 1, fontSize: 22 × 0, BrandLogo × 1 in app/(public)/).
- The full Vitest test count delta (872 → 875).
- The 12-checkpoint manual smoke result table (3 routes × 2 themes × 2 browsers — Pass/Fail per cell + free-text notes if any glitches observed).
- Confirmation that Phase 11's `<BrandLogo>` component, the Phase 11 CSS picker (lines 543-545), the form primitives (`<LoginForm>`, `<SetPasswordForm>`, `<InviteForm>`), the footer, and the top-right toggle cluster were NOT modified.
- Any deviations from this plan (should be zero — Phase 15 is shell-only and tightly scoped).
- Closing status for PUB-01 and PUB-02 in REQUIREMENTS.md (mark both as ✅ — request the user update Traceability section).
</output>
