---
phase: 41-typography-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOC-09]

must_haves:
  truths:
    - "REQUIREMENTS.md DOC-09 no longer demands the design's ten-step type scale (6.8 … 21pt); the clause is moved to Phase 43's requirement set, not silently dropped (D-02)."
    - "REQUIREMENTS.md DOC-01 (Phase 43) now carries the ten-step type-scale clause, so the scale has an owning requirement in the milestone."
    - "ROADMAP Phase 41 success criterion 1 reads 'renders in Inter' with no type-scale clause, so it no longer contradicts criterion 4 (D-02)."
    - "ROADMAP Phase 41 success criterion 3 asks for Inter TTFs only — the 'and Inter Tight' clause is gone (D-04)."
    - "Both documents carry a dated scope-reconciliation note citing 41-CONTEXT.md as the authority, so a future reader does not rediscover the contradiction."
    - "The DOC-09 requirement ID still exists in both the requirement block and the traceability table — narrowed in place, never deleted."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Narrowed DOC-09 + type-scale clause relocated to DOC-01 + scope note + Out-of-Scope entries"
      contains: "DOC-09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 41 criteria 1/3 reconciled with D-01/D-02/D-04; Phase 43 carries the type-scale criterion"
      contains: "Phase 41: Typography Migration"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/41-typography-migration/41-CONTEXT.md"
      via: "cited authority in the scope-reconciliation blockquote"
      pattern: "41-CONTEXT\\.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/41-typography-migration/41-CONTEXT.md"
      via: "cited authority in the scope-reconciliation blockquote"
      pattern: "41-CONTEXT\\.md"
---

<objective>
Reconcile the two upstream planning documents that currently contradict this phase's own
boundary and each other, before any code moves. ROADMAP Phase 41 criterion 1 and
REQUIREMENTS `DOC-09` both demand "Inter **at the design's type scale (6.8 … 21pt)**" while
ROADMAP criterion 4 forbids any visual change; criterion 3 also asks for Inter Tight, which
neither `Quote-FR-A.dc.html` nor `Quote-EN-A.dc.html` references. Per D-02 and D-04 the
type-scale clause moves to Phase 43's requirement set and the Inter Tight clause is dropped.

Documentation-only: no source code, no tests, no font binaries. This plan follows the
amendment-in-plan precedent set by `26-03-PLAN.md` Tasks 1-2 (Phase 26 D-01 descope), including
its grep-based verification proving the requirement ID survives the edit.

Purpose: a phase whose own success criteria are unsatisfiable cannot be verified. Fixing the
criteria first means Plan 02 and Plan 03 are judged against what was actually decided.
Output: edited `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/41-typography-migration/41-CONTEXT.md
@.planning/phases/41-typography-migration/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow DOC-09 to a family swap and relocate the type-scale clause to Phase 43 (D-02, D-04)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the `### Document — the redesigned PDF` heading (line 16), DOC-01 (lines 18-22), DOC-09 (lines 41-43), the `## Out of Scope` section (lines 96-101) and the Traceability table (lines 113-131). Read the current wording before editing; the DOC-09 line wraps across three lines.
    - .planning/phases/41-typography-migration/41-CONTEXT.md — D-01 (type scale frozen), D-02 (the amendment and its exact resolution text), D-04 (Inter Tight dropped). This file is the authority; its wording is what the amendment must reflect.
    - .planning/phases/41-typography-migration/41-PATTERNS.md — the `.planning/ROADMAP.md + .planning/REQUIREMENTS.md — AMENDED IN-PLACE (D-02)` section, which spells out the rewrite-in-place model (not the strike-through model) and the already-drafted replacement sentence for DOC-09.
    - .planning/milestones/v1.5-phases/26-active-expired-row-actions/26-03-PLAN.md — Task 1, the amendment-in-plan precedent: scope-reduction blockquote, ID-preserving rewrite, Out-of-Scope row, grep-based acceptance criteria.
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` in place. Four edits, no others:

    1. Insert a scope-reconciliation blockquote immediately after the `### Document — the redesigned PDF`
       heading (line 16), before the DOC-01 bullet. Content: "> **Scope reconciled 2026-09-08 (Phase 41
       discussion — `41-CONTEXT.md` D-02 / D-04).** DOC-09 is narrowed to a font-family swap: the design's
       ten-step type scale (6.8 … 21pt) moves to DOC-01 / Phase 43, where the card geometry that gives a
       6.8pt caption a reason to exist also lands. Inter Tight is not committed in Phase 41 (D-04) —
       neither `Quote-FR-A.dc.html` nor `Quote-EN-A.dc.html` references it. Authority:
       `.planning/phases/41-typography-migration/41-CONTEXT.md`."

    2. Rewrite DOC-09 (lines 41-43) in place, keeping the `- [ ] **DOC-09**:` prefix and the ID visible.
       New text: "The PDF renders in Inter, replacing Plus Jakarta Sans, with no missing-glyph or
       font-registration failure in any rendered proposal. *(Narrowed 2026-09-08 by Phase 41 D-02: the
       design's ten-step type scale moved to DOC-01 / Phase 43. Phase 41 keeps today's `pdfFontSizes`
       — 8 / 9 / 10 / 22 / 32pt — untouched per D-01.)*" Do NOT delete the bullet, do NOT renumber, do NOT
       change its checkbox state.

    3. Append one sentence to the end of DOC-01's text (Phase 43): "Every text node uses the design's
       ten-step type scale (6.8 / 7.5 / 8 / 8.5 / 9 / 9.5 / 10 / 11 / 13 / 21pt) — relocated here from
       DOC-09 by the Phase 41 D-02 amendment, since the scale only has meaning alongside this layout."

    4. Add two bullets to the `## Out of Scope` section (lines 96-101), matching the existing bullet style
       (bold lead-in, em dash, rationale):
       - "**The design's ten-step type scale inside Phase 41** — Phase 41 is a font-family swap only
         (D-01 / D-02). Introducing the scale there would make the byte-determinism delta 'font + scale',
         so a regression could not be attributed to either. It ships with DOC-01 in Phase 43."
       - "**Inter Tight anywhere in v1.9** — Phase 41 commits four static Inter TTFs only (D-04). Inter
         Tight is the design system's display face for the web app, not the document's; if Phase 43's
         header lockup proves it needs a display cut, Phase 43 commits it in the same phase that proves
         it renders."

    Leave the Traceability table untouched — DOC-09 stays mapped to Phase 41 and coverage stays 24/24
    (D-02 narrows an existing requirement, it does not add or descope one). Leave the `## Future
    Requirements` and `### Rule lifted by this milestone` sections untouched.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && grep -c "DOC-09" .planning/REQUIREMENTS.md && grep -c "41-CONTEXT.md" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "DOC-09" .planning/REQUIREMENTS.md` returns >= 3 (requirement bullet, traceability row, and at least one amendment reference) — the ID survives the edit.
    - Source: the DOC-09 requirement bullet no longer contains the type-scale list — `sed -n '/\*\*DOC-09\*\*/,/^- \[/p' .planning/REQUIREMENTS.md | grep -c "6.8"` returns 0.
    - Source: `grep -c "6.8 / 7.5 / 8 / 8.5 / 9 / 9.5 / 10 / 11 / 13 / 21pt" .planning/REQUIREMENTS.md` returns >= 1 and the match sits inside the DOC-01 bullet (verify by `grep -n`), proving the clause was relocated rather than deleted.
    - Source: `grep -c "41-CONTEXT.md" .planning/REQUIREMENTS.md` returns >= 1 (the blockquote cites its authority).
    - Source: `grep -ci "Inter Tight" .planning/REQUIREMENTS.md` returns >= 1 and every match is in the scope note or Out-of-Scope section (not in a requirement bullet) — verify by `grep -n`.
    - Source: `grep -c "Coverage:** 24/24" .planning/REQUIREMENTS.md` returns 1 (traceability tally unchanged).
  </acceptance_criteria>
  <done>DOC-09 reads as a family swap with an explicit narrowing note, DOC-01 carries the ten-step scale for Phase 43, a dated scope note cites 41-CONTEXT.md, and Out of Scope names both the type scale and Inter Tight. The DOC-09 ID and the 24/24 coverage tally are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP Phase 41 criteria 1 and 3, and give Phase 43 the type-scale criterion (D-02, D-04)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — the `### Phase 41: Typography Migration` block (lines 763-774: Goal, Depends on, Requirements, the four success criteria, `**Plans:**`), the `### Phase 43: New PDF Layout` block (lines 790-806, five success criteria), the v1.9 phase-list bullet at line 142, and the footer "Updated 2026-09-08 …" line at line 880.
    - .planning/phases/41-typography-migration/41-CONTEXT.md — D-02 (exact replacement wording for criterion 1), D-04 (Inter Tight dropped from criterion 3), D-01 (why criterion 4 wins).
    - .planning/phases/41-typography-migration/41-RESEARCH.md — § "Standard Stack / Font source (D-05 — researcher's choice, pinned)" for the concrete source now named in criterion 3 (`rsms/inter` release `v4.1`, four static TTFs, SHA-256 pinned).
    - .planning/milestones/v1.5-phases/26-active-expired-row-actions/26-03-PLAN.md — Task 2, the criterion-by-criterion rewrite precedent.
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` in place. Only the Phase 41 block, the Phase 43 criteria list, and the
    footer line change. Do not touch any other phase.

    1. Phase 41 success criterion 1 — replace with: "Every text node in a generated proposal PDF (FR and
       EN, every partner type) renders in Inter, with Plus Jakarta Sans fully retired from the PDF
       font-registration path and its nine committed binaries deleted from `public/fonts/`." (The type-scale
       clause is removed — it now lives in DOC-01 / Phase 43 criterion 6.)

    2. Phase 41 success criterion 2 — leave verbatim.

    3. Phase 41 success criterion 3 — replace with: "The four static Inter TTF files (weights 400 / 500 /
       600 / 700) are committed into the repo as self-hosted assets (not remote-linked), acquired from the
       upstream `rsms/inter` tagged release `v4.1` and pinned by SHA-256 — the design bundle vendored only
       token CSS and SVGs into `.planning/assets/v1.9-quote-design/`, so the binaries are acquired
       separately." The words "and Inter Tight" must not survive anywhere in this block.

    4. Phase 41 success criterion 4 — leave verbatim. It is what D-02 resolves the contradiction in favour of.

    5. Insert a scope-reconciliation blockquote immediately after criterion 4 and before the `**Plans:**`
       line of the Phase 41 block: "> **Criteria 1 and 3 reconciled 2026-09-08** (Phase 41 discussion —
       `41-CONTEXT.md` D-02 / D-04, executed by Plan 41-01). Criterion 1 previously demanded the design's
       ten-step type scale while criterion 4 forbade any visual change; the type-scale clause moved to
       REQUIREMENTS `DOC-01` and Phase 43 criterion 6. Criterion 3 previously asked for Inter Tight, which
       neither design file references."

    6. Phase 43 — add a sixth success criterion: "6. Every text node uses the design's ten-step type scale
       (6.8 / 7.5 / 8 / 8.5 / 9 / 9.5 / 10 / 11 / 13 / 21pt), replacing the five-role scale Phase 41
       deliberately froze (`pdfFontSizes` at 8 / 9 / 10 / 22 / 32pt) — relocated here from Phase 41 by the
       D-02 amendment."

    7. Footer line 880 — append to the existing "Updated 2026-09-08 …" sentence: " Amended 2026-09-08 by
       Plan 41-01: Phase 41 criteria 1 and 3 reconciled to a font-family-only swap (D-02 / D-04); the
       ten-step type scale relocated to Phase 43 criterion 6 and REQUIREMENTS DOC-01."

    Leave the v1.9 phase-list bullet at line 142 unchanged — it already describes a font-registration-only
    change with no type-scale claim. Verify this before deciding not to edit it.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -c "41-CONTEXT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Source: the Phase 41 block contains no type-scale list — `sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -c "6.8"` returns 0.
    - Source: the Phase 41 block contains no Inter Tight clause — `sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -ci "Inter Tight"` returns 0.
    - Source: the Phase 41 block names the pinned source — `sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -c "rsms/inter"` returns >= 1.
    - Source: the Phase 41 block cites its authority — `sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -c "41-CONTEXT.md"` returns >= 1.
    - Source: the Phase 43 block gained the relocated scale — `sed -n '/### Phase 43: New PDF Layout/,/### Phase 44:/p' .planning/ROADMAP.md | grep -c "6.8 / 7.5 / 8 / 8.5 / 9 / 9.5 / 10 / 11 / 13 / 21pt"` returns >= 1.
    - Source: criterion 4's visual-freeze wording survives — `sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -c "touches font registration only"` returns 1.
    - Source: `grep -cF '**Requirements:** DOC-09' .planning/ROADMAP.md` returns 1 — the phase's requirement mapping is unchanged.
  </acceptance_criteria>
  <done>ROADMAP Phase 41 criteria 1 and 3 describe a font-family-only swap sourced from a SHA-256-pinned rsms/inter release, no criterion mentions the type scale or Inter Tight, criterion 4 is verbatim, a dated blockquote cites 41-CONTEXT.md, Phase 43 owns the ten-step scale as criterion 6, and the footer records the amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. Edits two `.planning/*.md` files. No runtime surface, no code, no user input, no network call, no dependency install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | `.planning/REQUIREMENTS.md` DOC-09 | mitigate | A requirement silently disappearing during an in-place rewrite breaks milestone traceability. Mitigated by the ID-preserving rewrite model (never delete the bullet) plus the `grep -c "DOC-09" >= 3` and `Coverage:** 24/24` acceptance criteria, copied from the `26-03-PLAN.md` precedent. |
| T-41-01-02 | Repudiation | `.planning/ROADMAP.md` Phase 41 criteria | mitigate | An unattributed criteria rewrite is indistinguishable from scope creep at audit time. Mitigated by the dated scope-reconciliation blockquote citing `41-CONTEXT.md` D-02/D-04, asserted by a grep acceptance criterion in both files. |
| T-41-01-03 | Denial of Service (scope) | Phase 43 requirement set | mitigate | Removing the type-scale clause from DOC-09 without an owner would silently drop it from the milestone. Mitigated by relocating it into DOC-01 and ROADMAP Phase 43 criterion 6, both asserted by grep. |
</threat_model>

<verification>
Run from the repository root:

```
grep -c "DOC-09" .planning/REQUIREMENTS.md                                  # >= 3
grep -n "6.8 / 7.5" .planning/REQUIREMENTS.md                                # inside DOC-01 only
sed -n '/### Phase 41: Typography Migration/,/### Phase 42:/p' .planning/ROADMAP.md | grep -ci "Inter Tight"   # 0
sed -n '/### Phase 43: New PDF Layout/,/### Phase 44:/p' .planning/ROADMAP.md | grep -c "21pt"                 # >= 1
git diff --stat                                                              # only the two .planning/*.md files
```

No test suite runs in this plan (documentation-only), but `npm test` must still be green afterwards
because nothing it asserts on changed. `tests/planning-docs-resolver.test.ts` reads planning documents —
confirm it stays green with `npx vitest run tests/planning-docs-resolver.test.ts`.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` DOC-09 demands Inter, not Inter at a type scale, and says so explicitly.
- The ten-step type scale has a named owner (DOC-01, Phase 43) in both documents.
- ROADMAP Phase 41 criteria 1 and 3 are internally consistent with criterion 4.
- Both documents cite `41-CONTEXT.md` as the amendment authority with a date.
- `git diff --stat` shows exactly two modified files, both under `.planning/`.
- `npx vitest run tests/planning-docs-resolver.test.ts` exits 0.
</success_criteria>

<output>
Create `.planning/phases/41-typography-migration/41-01-SUMMARY.md` when done. Record the before/after
text of DOC-09 and of ROADMAP criteria 1 and 3 verbatim, so the amendment is auditable without a git diff.
</output>
