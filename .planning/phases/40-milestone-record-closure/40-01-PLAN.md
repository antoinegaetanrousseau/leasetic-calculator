---
phase: 40-milestone-record-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [CLOSE-06, CLOSE-07, OPS-02]

must_haves:
  truths:
    - "ROADMAP.md's v1.6 section header agrees with its own milestone list: both say SHIPPED 2026-09-04"
    - "Phase 28 appears in the ROADMAP progress table, attributed to v1.6, flagged as retro-documented outside the workflow"
    - "Phase 31.1 appears in the ROADMAP progress table, attributed to v1.6"
    - "The v1.6 milestone bullet no longer claims v1.6 was never formally archived"
    - "Phase 20's success criterion 3 names Better Auth trustedOrigins instead of a middleware Origin gate that does not exist"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected v1.6 header, progress-table rows for 28 and 31.1, corrected Phase 20 criterion 3"
      contains: "### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04"
  key_links:
    - from: ".planning/ROADMAP.md progress table row for Phase 28"
      to: "the fresh v1.6 audit run in plan 40-03"
      via: "the audit reads the progress table to attribute phases to v1.6"
      pattern: "^\\| 28\\. ReUI / base-maia Design-System Migration \\| v1\\.6 \\|"
---

<objective>
Make `.planning/ROADMAP.md` stop contradicting itself about v1.6, and give Phase 28 and
Phase 31.1 the progress-table rows they never got.

This plan is **first in the phase and load-bearing for plan 40-03** (D-40-07): the fresh
`/gsd-audit-milestone v1.6` run must see Phase 28 already attributed to v1.6 and flagged as
retro-documented. Without the flag, the audit reads Phase 28 as a v1.6 phase that lost its PLAN
and VERIFICATION files and reports a false gap that this phase would then have to dispose of.

Also corrects Phase 20's success criterion 3 in place (39-CONTEXT.md D-15 / ROADMAP § Phase 40
criterion 9): it describes a middleware-level Origin gate on `/api/auth/sign-in/*` that does not
exist anywhere in `src/proxy.ts` (91 lines, coarse auth-cookie gate only). Phase 20's actual gate
is Better Auth's `trustedOrigins` allow-list.

Purpose: ROADMAP.md is the document the audit, the milestone entry and the archive map all read.
Every later plan in this phase inherits whatever it says, so it is corrected before anything reads it.
Output: One modified file, `.planning/ROADMAP.md`. No new files. No code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-milestone-record-closure/40-CONTEXT.md
@.planning/phases/40-milestone-record-closure/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the v1.6 section header and retire the "never formally archived" clause</name>

  <read_first>
    - `.planning/ROADMAP.md` lines 1-20 (the `## Milestones` list — line 11 is the v1.6 bullet)
    - `.planning/ROADMAP.md` lines 98-125 (the v1.6 section header at line 102 and its v1.7 sibling at line 117, which is the target shape)
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-06
    - `.planning/phases/40-milestone-record-closure/40-PATTERNS.md` § "5. `ROADMAP.md` header flip (D-40-06)" — carries the confirmed before/after text
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Two single-line edits in `.planning/ROADMAP.md`, per D-40-06.

    (a) Line ~102. Replace the header
    `### 🚧 v1.6 — CRM Foundation (Phases 29-34) — IN PROGRESS`
    with
    `### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04`.
    The target shape is its own sibling four lines below at line ~117:
    `### ✅ v1.7 — Sales Motivation (Phase 35) — SHIPPED 2026-09-05`. Match emoji, dash style and
    date placement exactly.

    D-40-06 is explicit that this is a **single-line header edit**: the six phase bullets under
    the header (Phases 29, 30, 31, 31.1, 32-removed, 33, 34) and the whole `## Phase Details`
    block stay exactly where they are. Several planning documents cite `ROADMAP.md` by line
    number; do not trim, reflow or relocate any section. Keep total line count changes confined
    to the two rows Task 2 inserts.

    (b) Line 11, the v1.6 bullet in the `## Milestones` list. Its trailing italic clause
    `*never formally archived; captured in `milestones/v1.7-ROADMAP.md`*` becomes false the
    moment plan 40-05 writes `milestones/v1.6-ROADMAP.md` and plan 40-06 performs the archive
    `git mv`. Replace that trailing clause with a pointer to the real archive, matching the
    other six milestone bullets' `— see \`milestones/vX.Y-ROADMAP.md\`` form. Keep the rest of
    the bullet (the ✅, the name, `Phases 29-34 (shipped 2026-09-04)`, and the
    `CRM registry, proposal reconciliation, pipeline, activity` summary) word-for-word.
  </action>

  <verify>
    <automated>grep -qF '### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04' .planning/ROADMAP.md && ! grep -qF '🚧 **v1.6' .planning/ROADMAP.md && ! grep -qF 'v1.6 — CRM Foundation (Phases 29-34) — IN PROGRESS' .planning/ROADMAP.md && ! grep -qF 'never formally archived' .planning/ROADMAP.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF '### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04' .planning/ROADMAP.md` returns `1`
    - `grep -F 'IN PROGRESS' .planning/ROADMAP.md` returns only the v1.8 header line (`### 🚧 v1.8 — Deferred Items (Phases 36-40) — IN PROGRESS`) — no v1.6 hit
    - `grep -F 'never formally archived' .planning/ROADMAP.md` returns no matches (exit status 1)
    - `grep -F 'milestones/v1.6-ROADMAP.md' .planning/ROADMAP.md` returns at least one match (the line-11 bullet's new pointer)
    - Line 11 still contains the literal substrings `✅ **v1.6 — CRM Foundation**`, `Phases 29-34 (shipped 2026-09-04)` and `CRM registry, proposal reconciliation, pipeline, activity`
    - `grep -c '^### Phase 3' .planning/ROADMAP.md` is unchanged from its pre-edit value (no Phase Details section removed)
  </acceptance_criteria>

  <done>ROADMAP.md's v1.6 section header and its milestone-list bullet both say the same thing: shipped 2026-09-04, archived under `milestones/v1.6-ROADMAP.md`. No section was trimmed or moved.</done>
</task>

<task type="auto">
  <name>Task 2: Add the missing Phase 28 and Phase 31.1 progress-table rows and correct Phase 39's row name</name>

  <read_first>
    - `.planning/ROADMAP.md` lines 1040-1075 (the `## Progress` table — it currently jumps from `| 27. Status-Pill Rendering Fix |` straight to `| 29. Migration Safety Net |`, and from `| 31. Reconciliation Engine ... |` straight to `| 32. HubSpot Import |`)
    - `.planning/phases/28-reui-design-system-migration/28-CONTEXT.md` — the "executed outside the GSD workflow … deliberately no `28-NN-PLAN.md` files" statement that justifies the Status wording
    - `.planning/phases/28-reui-design-system-migration/28-01-SUMMARY.md` — confirms `**Completed:** 2026-08-31`, 23 commits on `migration/phase-0-baseline`
    - `.planning/ROADMAP.md` line ~112 (the Phase 31.1 bullet: `**Phase 31.1: App Shell Refresh** (INSERTED)`, executed 2026-09-02) and lines 531-569 (its Phase Details block, which lists plans 31.1-01 through 31.1-07)
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-07
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Three edits inside the `## Progress` table only. The table's column order is
    `| Phase | Milestone | Plans Complete | Status | Completed |`.

    (a) **Phase 28 row (D-40-07).** Insert a new row immediately after
    `| 27. Status-Pill Rendering Fix | v1.5 | 2/2 | Complete    | 2026-05-30 |`
    and immediately before `| 29. Migration Safety Net | v1.6 | 2/2 | Complete    | 2026-08-31 |`.
    Required cell values, exactly:
    - Phase: `28. ReUI / base-maia Design-System Migration`
    - Milestone: `v1.6`
    - Plans Complete: `—` (an em dash, the same "no plan count" convention the Phase 32 removed
      row at line ~1065 already uses). D-40-07 is explicit: **no plan count**. Do not write
      `0/0`, `1/1` or `n/a`.
    - Status: `Complete (retro-documented, outside workflow)`
    - Completed: `2026-08-31`

    The Status flag is load-bearing, not decoration: plan 40-03 runs a fresh
    `/gsd-audit-milestone v1.6` and the flag is what stops that audit from reporting Phase 28 as
    a v1.6 phase missing its PLAN and VERIFICATION files. Write it verbatim.

    (b) **Phase 31.1 row.** The table has the same hole for the inserted Phase 31.1 — it is a
    v1.6 phase with seven completed plans (`31.1-01-PLAN.md` … `31.1-07-PLAN.md`, all with
    matching SUMMARYs on disk) and no row. Insert immediately after
    `| 31. Reconciliation Engine & Proposal Extraction | v1.6 | 8/8 | Complete   | 2026-09-02 |`
    and before the `| 32. HubSpot Import |` row. Cell values:
    - Phase: `31.1. App Shell Refresh (INSERTED)`
    - Milestone: `v1.6`
    - Plans Complete: `7/7`
    - Status: `Complete`
    - Completed: `2026-09-02`

    (c) **Phase 39 row name.** The row reads `| 39. Operational & Credential Gates | v1.8 | 5/5 |
    Complete    | 2026-09-06 |`. That is the phase's pre-rename title; it was re-scoped and
    renamed to **Database Guard Correctness** on 2026-09-06 (see
    `.planning/phases/39-database-guard-correctness/39-CONTEXT.md`, whose own footer records the
    rename, and `.planning/REQUIREMENTS.md`'s traceability row
    `| OPS-05 | Phase 39 — Database Guard Correctness | Complete |`). Change the Phase cell to
    `39. Database Guard Correctness`. Leave every other cell in that row untouched.

    Do not touch the `| 40. Milestone Record Closure | v1.8 | 0/? | Not started | - |` row — the
    phase-close workflow owns it.
  </action>

  <verify>
    <automated>grep -qE '^\| 28\. ReUI / base-maia Design-System Migration \| v1\.6 \| — \| Complete \(retro-documented, outside workflow\) \| 2026-08-31 \|$' .planning/ROADMAP.md && grep -qE '^\| 31\.1\. App Shell Refresh \(INSERTED\) \| v1\.6 \| 7/7 \|' .planning/ROADMAP.md && grep -qE '^\| 39\. Database Guard Correctness \| v1\.8 \|' .planning/ROADMAP.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^\| 28\. ' .planning/ROADMAP.md` returns `1`
    - That row matches `^\| 28\. ReUI / base-maia Design-System Migration \| v1\.6 \| — \| Complete \(retro-documented, outside workflow\) \| 2026-08-31 \|$`
    - `grep -cE '^\| 31\.1\. ' .planning/ROADMAP.md` returns `1` and the row's Milestone cell is `v1.6` and Plans Complete cell is `7/7`
    - `grep -cE '^\| 39\. Database Guard Correctness \|' .planning/ROADMAP.md` returns `1`
    - `grep -cF '| 39. Operational & Credential Gates |' .planning/ROADMAP.md` returns `0`
    - The Phase 28 row's line number is greater than the `| 27. Status-Pill Rendering Fix` row's and less than the `| 29. Migration Safety Net` row's (verify with `grep -n`)
    - `grep -cF '| 40. Milestone Record Closure | v1.8 | 0/? | Not started | - |' .planning/ROADMAP.md` returns `1` (untouched)
  </acceptance_criteria>

  <done>Every phase from 1 through 40 that exists on disk has a row in the progress table. Phase 28's row carries `v1.6` and the retro-documented flag with no plan count; Phase 31.1's row carries `v1.6` and `7/7`; Phase 39's row carries its current name.</done>
</task>

<task type="auto">
  <name>Task 3: Correct Phase 20's success criterion 3 — the middleware Origin gate does not exist</name>

  <read_first>
    - `.planning/ROADMAP.md` lines 238-250 (§ `### Phase 20: Infra Hardening` — its Goal line at ~240 and Success Criterion 3 at ~247)
    - `.planning/ROADMAP.md` line ~70 (the v1.3 phase bullet: `**Phase 20: Infra Hardening** — … Better Auth trustedOrigins middleware gate (completed 2026-05-27)`)
    - `.planning/ROADMAP.md` lines 1020-1026 (§ Phase 40 success criterion 9, which commissions this correction)
    - `.planning/phases/39-database-guard-correctness/39-CONTEXT.md` — **D-14, D-15, D-16** and the `<stale_premises>` table's ROADMAP-criterion-3 row
    - `src/proxy.ts` — 91 lines, the coarse auth-cookie gate, containing no Origin check
    - `src/lib/auth/index.ts` around line 210 — `trustedOrigins: allowedOrigins`, the real mechanism
    - `tests/trusted-origins.test.ts` — asserts allow-list membership, never a status code
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Correct in place, per 39-CONTEXT.md D-15 and ROADMAP § Phase 40 criterion 9. Two sites.

    (a) **Phase 20 Success Criterion 3** (`ROADMAP.md` line ~247). It currently reads:
    "A middleware-level Origin gate on `/api/auth/sign-in/*` mutations hard-blocks requests whose
    `Origin` header is not in the `trustedOrigins` list; verified by a test that sends an
    untrusted Origin and asserts a non-2xx response."

    Both halves are wrong about this codebase. There is no middleware-level Origin gate: `src/proxy.ts`
    is 91 lines of coarse auth-cookie gating and contains no `Origin` read, and no `middleware.ts`
    in this repo's history ever added one. And per D-16 the verification deliberately does **not**
    assert a response status code — Phase 20's research established that Better Auth's rejection
    status varies between rejection paths and point releases, so `tests/trusted-origins.test.ts`
    asserts **allow-list membership** instead.

    Rewrite criterion 3 so it names what actually enforces the allow-list: Better Auth's
    `trustedOrigins` option, explicitly configured at `src/lib/auth/index.ts:210` (Phase 20-01),
    rejecting requests whose `Origin` is not in the list — verified by `tests/trusted-origins.test.ts`
    asserting membership of the configured allow-list, not a response status code. Append a dated
    correction marker in the house amend-in-place form used at `.planning/REQUIREMENTS.md`
    lines 184-187, i.e. a trailing italic parenthetical of the form
    `*(Corrected 2026-09-07 by D-15 (Phase 39 context, executed in Phase 40): <what was wrong> — see <pointers>.)*`
    so the original claim's retraction is visible rather than silently overwritten. Cite
    `src/proxy.ts`, `src/lib/auth/index.ts:210` and `tests/trusted-origins.test.ts`.

    Wording is Claude's Discretion (40-CONTEXT.md hands it forward from 39-CONTEXT.md), subject to
    two hard constraints: it must name Better Auth `trustedOrigins` as the mechanism, and it must
    not assert a status-code expectation.

    (b) **The v1.3 phase bullet** at `ROADMAP.md` line ~70 and the **Phase 20 Goal line** at ~240
    both call the deliverable a "`trustedOrigins` middleware gate". Drop the word `middleware`
    from both so they name the Better Auth option rather than a middleware layer. These are
    single-word edits; do not restructure either line.

    Do **not** build the missing middleware gate. D-15 rejects that explicitly: it would be a new
    capability duplicating Better Auth and would violate `src/proxy.ts`'s deliberate minimalism
    (PITFALLS §1.5).
  </action>

  <verify>
    <automated>! grep -qF 'A middleware-level Origin gate on' .planning/ROADMAP.md && ! grep -qF 'trustedOrigins` middleware gate' .planning/ROADMAP.md && grep -qF 'src/lib/auth/index.ts:210' .planning/ROADMAP.md && grep -qF 'tests/trusted-origins.test.ts' .planning/ROADMAP.md && grep -qF 'Corrected 2026-09-07 by D-15' .planning/ROADMAP.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -F 'A middleware-level Origin gate on' .planning/ROADMAP.md` returns no matches
    - `grep -F 'trustedOrigins` middleware gate' .planning/ROADMAP.md` returns no matches (both line ~70 and line ~240 cleaned)
    - The rewritten Phase 20 criterion 3 line contains the literal substrings `trustedOrigins`, `src/lib/auth/index.ts:210` and `tests/trusted-origins.test.ts`
    - The rewritten criterion 3 does **not** contain the substrings `non-2xx`, `status code` or `403` — `grep -nE 'non-2xx|status code|403' .planning/ROADMAP.md` returns no hit inside the Phase 20 section (lines 238-250)
    - `grep -cF 'Corrected 2026-09-07 by D-15' .planning/ROADMAP.md` returns `1`
    - `git diff --stat -- src/proxy.ts` is empty (no middleware gate was built)
  </acceptance_criteria>

  <done>ROADMAP.md describes the CSRF allow-list mechanism this codebase actually has. The retraction of the middleware-gate claim is dated and attributed, and `src/proxy.ts` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies one markdown planning document. It adds no code path, no input parser, and no network or database surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Repudiation | `.planning/ROADMAP.md` Phase 20 criterion 3 | mitigate | The honest risk here is record-accuracy, not runtime: rewriting criterion 3 could record a CSRF control as satisfied by a mechanism that is not actually present. Task 3's `<read_first>` requires reading `src/lib/auth/index.ts` around line 210 and `tests/trusted-origins.test.ts` before writing, and the acceptance criteria require the rewritten text to cite both. The claim being retired (a middleware Origin gate) is the *stronger* one, and it was verified absent from `src/proxy.ts` — so the correction narrows the recorded control to what exists rather than widening it. |
| T-40-01-02 | Tampering | `src/proxy.ts` | accept | D-15 rejects building the middleware gate. Task 3's acceptance criteria assert `git diff --stat -- src/proxy.ts` is empty, so a well-meaning executor cannot silently add an auth path under cover of a documentation edit. |
| T-40-01-03 | Information Disclosure | ROADMAP prose | accept | The corrected text names file paths and a config option already public in the repo. No secret, credential, endpoint or hostname is introduced. |

**No new attack surface.** This plan edits planning prose only; the risk it manages is
record-accuracy, not runtime exposure. No package is installed, so the Package Legitimacy Gate
does not apply and no `T-40-01-SC` supply-chain threat exists for this plan.
</threat_model>

<verification>
- `grep -cF '### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04' .planning/ROADMAP.md` → `1`
- `grep -cE '^\| 28\. ReUI / base-maia Design-System Migration \| v1\.6 \| — \| Complete \(retro-documented, outside workflow\) \|' .planning/ROADMAP.md` → `1`
- `grep -F 'A middleware-level Origin gate on' .planning/ROADMAP.md` → no matches
- `npm run lint:check` exits 0 (no source file was touched; this confirms the working tree is still clean for downstream plans)
- `git diff --name-only` lists exactly `.planning/ROADMAP.md`
</verification>

<success_criteria>
1. `.planning/ROADMAP.md` line ~102 reads `### ✅ v1.6 — CRM Foundation (Phases 29-34) — SHIPPED 2026-09-04` and no v1.6 text says IN PROGRESS.
2. The `## Milestones` v1.6 bullet points at `milestones/v1.6-ROADMAP.md` and no longer says "never formally archived".
3. The progress table has a Phase 28 row (`v1.6`, `—` plans, `Complete (retro-documented, outside workflow)`, `2026-08-31`) and a Phase 31.1 row (`v1.6`, `7/7`, `Complete`, `2026-09-02`).
4. The Phase 39 progress row reads `39. Database Guard Correctness`.
5. Phase 20 criterion 3 names Better Auth `trustedOrigins` with a dated D-15 correction marker, cites `src/lib/auth/index.ts:210` and `tests/trusted-origins.test.ts`, and asserts no status code.
6. `.planning/ROADMAP.md` is the only file in `git diff --name-only`.
</success_criteria>

<output>
Create `.planning/phases/40-milestone-record-closure/40-01-SUMMARY.md` when done.
Record: the exact new line numbers of the Phase 28 and Phase 31.1 progress rows, and the final
wording chosen for Phase 20 criterion 3 (plan 40-03's pre-flight check reads the former; plan
40-04's OPS-02 amendment must agree with the latter).
</output>
