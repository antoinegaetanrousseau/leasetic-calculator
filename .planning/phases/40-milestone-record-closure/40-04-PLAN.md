---
phase: 40-milestone-record-closure
plan: 04
type: execute
wave: 2
depends_on: ["40-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/ops-03-ovh-cutover-december-2026.md
  - .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, GAP-05, HOUSE-05, HOUSE-06]

must_haves:
  truths:
    - "The requirement ledger no longer asserts that the leasetic2026 password is unretired, that last_login_at is written nowhere, that trustedOrigins is left to default, or that Thomas's sign-off is pending"
    - "Each of the five corrected requirements is ticked with a pointer to where it was really closed, and its original text is still readable"
    - "The December 2026 OVH cutover survives the OPS-03 tick as a pending todo naming Antoine's next step"
    - "38-WALK-SURFACES.md carries a dated errata block correcting the four pagination-control descriptions, with Table 1 itself unedited"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected, ticked OPS-01/02/03/04, GAP-05, HOUSE-05, HOUSE-06 with amend-in-place parentheticals and updated traceability rows"
      contains: "Amended 2026-09-07"
    - path: ".planning/todos/pending/ops-03-ovh-cutover-december-2026.md"
      provides: "The December 2026 OVH cutover commitment, carried past the OPS-03 tick"
      contains: "status: pending"
    - path: ".planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md"
      provides: "Dated HOUSE-06 errata block; original Table 1 preserved"
      contains: "2026-09-07 errata — Phase 40, HOUSE-06"
  key_links:
    - from: ".planning/REQUIREMENTS.md OPS-01 tick"
      to: "docs/operations/phase-21-gate-evidence.md § GATE-01"
      via: "a dated amend-in-place parenthetical citing the evidence document"
      pattern: "docs/operations/phase-21-gate-evidence\\.md"
    - from: ".planning/REQUIREMENTS.md GAP-05 tick"
      to: "src/lib/auth/index.ts:195"
      via: "a dated parenthetical citing the session.create.after hook"
      pattern: "src/lib/auth/index\\.ts:195"
    - from: ".planning/REQUIREMENTS.md HOUSE-05 amendment"
      to: "plan 40-02's ProposalForm deletion commit"
      via: "a dated parenthetical recording the delete decision taken"
      pattern: "40-02"
---

<objective>
Stop the requirement ledger asserting five things that are not true, close HOUSE-05's
delete-or-document question against the decision plan 40-02 already executed, and correct
`38-WALK-SURFACES.md`'s description of four pagination controls.

Phase 39's scouting found that **five of six** items in its original scope were already closed or
misdescribed — OPS-01 and OPS-04 closed on 2026-05-29 by Phase 21, GAP-05 wired since Phase 12,
`trustedOrigins` configured since Phase 20-01, and ROADMAP criterion 3 citing a middleware Origin
gate that does not exist. Those five moved here as record corrections rather than engineering. The
decisions governing them are 39-CONTEXT.md **D-09 through D-16**, and the evidence base is that
file's `<stale_premises>` table. **Read them; they are deliberately not restated in
40-CONTEXT.md.**

Nothing here is re-implemented. Per ROADMAP § Phase 40 criterion 5, each of the five is **ticked
with a pointer to where it was really closed**, and each amendment preserves the original text —
the house `*(Amended <date> by <D-ref>: … — see <pointer>.)*` convention already executed in this
same file at lines 184-187 (D-36-02, recorded in STATE.md).

Purpose: the requirement ledger is what the milestone audits, the roadmap and the next milestone's
scoping all read. Five stale premises in one phase, on top of Phase 38's dead-`ProposalForm`
finding, is the drift this phase exists to stop.
Output: `.planning/REQUIREMENTS.md` corrected, one new pending todo, one errata block appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/39-database-guard-correctness/39-CONTEXT.md
@.planning/phases/40-milestone-record-closure/40-CONTEXT.md
@.planning/phases/40-milestone-record-closure/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend and tick OPS-01, OPS-02, OPS-03, OPS-04 and GAP-05</name>

  <read_first>
    - `.planning/phases/39-database-guard-correctness/39-CONTEXT.md` — **the `<stale_premises>` table** (the evidence base) and **D-09 through D-16**. Non-optional. These decisions are not repeated in 40-CONTEXT.md.
    - `.planning/REQUIREMENTS.md` lines 88-89 (GAP-05) and 93-105 (OPS-01, OPS-02, OPS-03, OPS-04) — the exact current text you must preserve
    - `.planning/REQUIREMENTS.md` lines 184-187 — the **already-executed** house amend-in-place example (HOUSE-04 / D-36-02). Copy its mechanism exactly: original sentence untouched, dated italic parenthetical appended.
    - `.planning/REQUIREMENTS.md` lines 204-222 — the traceability table, whose completed rows read e.g. `| OPS-05 | Phase 39 — Database Guard Correctness | Complete |`
    - `docs/operations/phase-21-gate-evidence.md` § GATE-01 — OPS-01's evidence: both admins rotated off `leasetic2026` on 2026-05-29, old password tested and rejected
    - `docs/legal/privacy-coverage-confirmation.md` — OPS-04's evidence: notice published 2026-05-29, `Status: Closed`
    - `src/lib/auth/index.ts` around line 195 — GAP-05's evidence: `updateLastLoginAt(session.userId)` inside the `session.create.after` hook
    - `src/lib/auth/index.ts` around line 210 — OPS-02's evidence: `trustedOrigins: allowedOrigins`
    - `tests/trusted-origins.test.ts` — asserts allow-list membership, never a status code (D-16)
    - `src/proxy.ts` — 91 lines, no Origin check; the fact D-15 corrects
    - `scripts/smoke-ovh.ts` — 358 lines, 7-step lifecycle, ready and deliberately unrun
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-15, D-40-16
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
    Per D-40-16, amend each of the five **in place with the original preserved**, flip its checkbox
    from `- [ ]` to `- [x]`, and append a dated italic parenthetical in the exact form already used
    at lines 184-187:
    `*(Amended 2026-09-07 by <D-ref>: <what actually happened> — see <pointer(s)>.)*`

    Do not delete, reflow or reword a single character of any original requirement sentence. The
    parenthetical goes after it. Wording of each parenthetical is Claude's Discretion (handed
    forward from 39-CONTEXT.md), subject to one hard rule: each must name the real mechanism **and**
    cite where the item was actually closed.

    Required D-ref and pointer for each:

    - **OPS-01** (lines 93-95) — D-ref `D-09`. Closed 2026-05-29 by Phase 21: both admins rotated
      off the shared `leasetic2026` password to individual credentials via the `/parametres`
      self-service flow; the old password was tested and rejected and the new one verified. Pointer:
      `docs/operations/phase-21-gate-evidence.md` § GATE-01. Note the correction that the mechanism
      was the `/parametres` self-service flow — no admin↔admin fallback was used, and **no
      reset-token script was built or is needed** (D-11; `createPasswordReset()` already exists at
      `src/lib/auth/actions.ts:174` as the fallback).

    - **OPS-02** (lines 96-98) — D-refs `D-14`/`D-15`/`D-16`. `trustedOrigins` was **explicitly
      configured in Phase 20-01**, not left to default. The inherited reasoning is **revised, not
      re-affirmed** (D-14): the original claim that "SameSite=Lax + `__Secure-` cookies are the
      ACTUAL CSRF defence" was the justification for *deferring* `trustedOrigins`, and Phase 20-01
      shipped the allow-list anyway — so the accurate dated position is **defence in depth: both
      layers are present and neither is claimed to make the other unnecessary.** The old hierarchy
      is retired. Pointers: `src/lib/auth/index.ts:210` and `tests/trusted-origins.test.ts`.
      Per D-16, do **not** write any claim about a rejection status code — verification asserts
      allow-list membership, because Better Auth's rejection status varies between rejection paths
      and point releases. Per D-15, note that no middleware Origin gate exists (`src/proxy.ts`) and
      none was built.

    - **OPS-03** (lines 99-102) — D-refs `D-12`/`D-13`/`D-40-15`. **Ticked `[x]`.** The requirement's
      own text closes on "*or* the OVH cutover is formally re-dated with a decision", and the dated
      decision **is** the closure. Record: the blocker is that no OVH environment is provisioned,
      not that nobody ran a command; `scripts/smoke-ovh.ts` (358 lines, 7-step black-box lifecycle)
      stays ready and unrun; the cutover is re-dated to **December 2026** with Antoine owning the
      next step (provisioning a Node + Postgres + S3-compatible OVH target). Rehearsing the smoke
      test against the Vercel deployment was rejected — it would prove the harness works but not
      portability, and would create and delete a real proposal in production. Pointer: the pending
      todo Task 2 creates, `.planning/todos/pending/ops-03-ovh-cutover-december-2026.md`.
      **Do not run `scripts/smoke-ovh.ts`.**

    - **OPS-04** (lines 103-105) — D-ref `D-09` era / Phase 21 D-01. Closed 2026-05-29: Phase 21
      D-01 superseded the "ask Thomas" framing — Antoine owns leasetic.fr, so publication **is**
      the artifact. The notice is live with both additions and the document's Status reads Closed.
      Pointer: `docs/legal/privacy-coverage-confirmation.md`.

    - **GAP-05** (lines 88-89) — D-ref `D-10`. `users.last_login_at` **is written**:
      `updateLastLoginAt()` is wired to Better Auth's `session.create.after` hook, with unit tests.
      D-10 explicitly requires correcting the requirement's own "written nowhere" wording — so in
      addition to the appended parenthetical, strike through or explicitly retract that clause
      inside the amendment text (do not silently delete it; the amend-in-place discipline means the
      reader must be able to see what the ledger used to claim). Pointers: `src/lib/auth/index.ts:195`
      and `src/lib/auth/index.test.ts`.

    Then update the **traceability table** (lines 204-222): the rows
    `| GAP-05 | Phase 40 — Milestone Record Closure | Pending |`,
    `| OPS-01 | … | Pending |`, `| OPS-02 | … | Pending |`, `| OPS-03 | … | Pending |` and
    `| OPS-04 | … | Pending |` each flip their third cell from `Pending` to `Complete`, matching
    the existing completed-row shape (e.g. `| OPS-05 | Phase 39 — Database Guard Correctness | Complete |`).
    Leave the CLOSE-06 and CLOSE-07 rows on `Pending` — plan 40-06 owns those.

    Before writing any pointer, **verify it resolves**: open each cited file and confirm the cited
    line or section says what the amendment claims. A ledger that ticks a security control on a
    citation that does not resolve is the failure mode this phase exists to end.
  </action>

  <verify>
    <automated>grep -qE '^- \[x\] \*\*OPS-01\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*OPS-02\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*OPS-03\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*OPS-04\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*GAP-05\*\*' .planning/REQUIREMENTS.md && ! grep -qE '^\| (OPS-0[1-4]|GAP-05) \|.*\| Pending \|' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^- \[x\] \*\*(OPS-01|OPS-02|OPS-03|OPS-04|GAP-05)\*\*' .planning/REQUIREMENTS.md` returns `5`
    - `grep -cE '^- \[ \] \*\*(OPS-01|OPS-02|OPS-03|OPS-04|GAP-05)\*\*' .planning/REQUIREMENTS.md` returns `0`
    - `grep -cF 'Amended 2026-09-07' .planning/REQUIREMENTS.md` returns at least `5`
    - The OPS-01 block contains the literal string `docs/operations/phase-21-gate-evidence.md`
    - The OPS-02 block contains the literal strings `src/lib/auth/index.ts:210` and `tests/trusted-origins.test.ts`, and contains **neither** `non-2xx` nor `status code` (D-16)
    - The OPS-02 block contains the literal string `defence in depth`
    - The OPS-03 block contains the literal strings `December 2026` and `ops-03-ovh-cutover-december-2026`
    - The OPS-04 block contains the literal string `docs/legal/privacy-coverage-confirmation.md`
    - The GAP-05 block contains the literal string `src/lib/auth/index.ts:195`
    - Each of the five original sentences survives verbatim — `git diff .planning/REQUIREMENTS.md` shows the original requirement text unchanged apart from the `[ ]`→`[x]` flip and appended text; no original sentence appears on a `-` line except where D-10 requires the "written nowhere" retraction, which must appear as a visible retraction rather than a deletion
    - `grep -cE '^\| (OPS-01|OPS-02|OPS-03|OPS-04|GAP-05) \| Phase 40 — Milestone Record Closure \| Complete \|' .planning/REQUIREMENTS.md` returns `5`
    - `grep -cE '^\| (CLOSE-06|CLOSE-07) \| Phase 40 — Milestone Record Closure \| Pending \|' .planning/REQUIREMENTS.md` returns `2` (untouched — plan 40-06 owns them)
    - `git diff --name-only -- src app scripts` is empty (nothing was re-implemented)
  </acceptance_criteria>

  <done>All five requirements are ticked, each carrying a dated parenthetical that names the real mechanism and cites a pointer that resolves. Their original text is still readable. Their traceability rows read Complete. No source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Open the December 2026 OVH cutover as a pending todo</name>

  <read_first>
    - `.planning/todos/pending/wr-07-db-guard-skip-rule.md` — **the shape to reproduce**: YAML frontmatter with `id`, `title`, `created`, `source`, `severity`, `status`, then an H1 matching the title, a "why it was deferred, not skipped" section, a "why it needs an operator decision" section, and an `## Acceptance` checklist
    - `.planning/phases/39-database-guard-correctness/39-CONTEXT.md` — **D-12 and D-13** (the OVH decisions; not restated in 40-CONTEXT.md)
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-15
    - `scripts/smoke-ovh.ts` — the 358-line, 7-step black-box lifecycle harness this todo exists to give a target
    - `.planning/STATE.md` § Deferred Items — the list D-40-15 explicitly chose **not** to bury this in (it already carries eight inherited items)
  </read_first>

  <files>.planning/todos/pending/ops-03-ovh-cutover-december-2026.md</files>

  <action>
    Create `.planning/todos/pending/ops-03-ovh-cutover-december-2026.md`, reproducing the frontmatter
    keys and section shape of `.planning/todos/pending/wr-07-db-guard-skip-rule.md`.

    Frontmatter values:
    - `id: ops-03-ovh-cutover-december-2026`
    - `title: Provision an OVH-compatible target and run scripts/smoke-ovh.ts`
    - `created: 2026-09-07`
    - `source: 40-CONTEXT.md (D-40-15) / 39-CONTEXT.md (D-12, D-13)`
    - `severity: info`
    - `status: pending`

    Body must record, concretely:
    - **Why it exists past the tick.** OPS-03 is ticked `[x]` because its own text closes on
      "*or* the OVH cutover is formally re-dated with a decision", and that decision was taken. The
      todo carries the commitment forward so the tick does not bury it. D-40-15 rejected burying it
      in STATE.md's deferred list (already eight inherited items) and rejected filing a v1.9
      requirement against a milestone that does not exist.
    - **The date:** December 2026.
    - **Antoine's next step, named:** provision an OVH-compatible target — Node runtime + Postgres
      + S3-compatible object storage — so `scripts/smoke-ovh.ts` has something to run against.
    - **The blocker is the environment, not the command.** `scripts/smoke-ovh.ts` is 358 lines and
      exercises a 7-step black-box lifecycle; it stays ready and unrun.
    - **What was rejected and why (D-12):** rehearsing the harness against the Vercel deployment
      would prove the harness works but not portability, and would create and delete a real
      proposal in production.

    `## Acceptance` checklist, unchecked:
    - [ ] An OVH-compatible target is provisioned (Node + Postgres + S3-compatible storage)
    - [ ] `scripts/smoke-ovh.ts` runs against it and all 7 lifecycle steps pass
    - [ ] The result is recorded, and OPS-03's amendment in `.planning/REQUIREMENTS.md` is updated to cite the run rather than the re-dating decision

    **Do not run `scripts/smoke-ovh.ts`.** D-40-15 and D-12 keep it deliberately unrun.
  </action>

  <verify>
    <automated>test -f .planning/todos/pending/ops-03-ovh-cutover-december-2026.md && grep -q '^status: pending$' .planning/todos/pending/ops-03-ovh-cutover-december-2026.md && grep -q 'December 2026' .planning/todos/pending/ops-03-ovh-cutover-december-2026.md && grep -q 'scripts/smoke-ovh.ts' .planning/todos/pending/ops-03-ovh-cutover-december-2026.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/todos/pending/ops-03-ovh-cutover-december-2026.md` exits 0
    - Its frontmatter contains, each on its own line: `id: ops-03-ovh-cutover-december-2026`, `created: 2026-09-07`, `status: pending`, and a `source:` line naming both `40-CONTEXT.md` and `39-CONTEXT.md`
    - The body contains the literal strings `December 2026`, `scripts/smoke-ovh.ts`, `Node`, `Postgres` and `S3-compatible`
    - The body contains an `## Acceptance` heading followed by at least three `- [ ]` items
    - `grep -c 'Antoine' .planning/todos/pending/ops-03-ovh-cutover-december-2026.md` returns at least `1` (the owner is named)
    - `.planning/STATE.md` is **not** in `git diff --name-only` (D-40-15 chose the todo over the deferred list)
    - No process ran `scripts/smoke-ovh.ts` — `git status --porcelain scripts/smoke-ovh.ts` is empty and no proposal was created
  </acceptance_criteria>

  <done>The December 2026 OVH commitment lives in `.planning/todos/pending/` with Antoine named as owner and an acceptance checklist, so the OPS-03 tick does not lose it.</done>
</task>

<task type="auto">
  <name>Task 3: Amend HOUSE-05 and HOUSE-06, and append the dated errata block to 38-WALK-SURFACES.md</name>

  <read_first>
    - `.planning/phases/40-milestone-record-closure/40-02-SUMMARY.md` — plan 40-02's record of the `ProposalForm` deletion, including the commit and the final line count. HOUSE-05's amendment cites it.
    - `.planning/REQUIREMENTS.md` lines 143-158 (HOUSE-05 and HOUSE-06) and lines 184-187 (the amend-in-place example)
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` **in full** (110 lines) — Table 1 at lines 38-46 (rows 1-5), Table 2 at line 50, `## Row-alignment inspection spots` at line 82, and the document's ending
    - `app/(admin)/[adminSegment]/partners/PartnersList.tsx` around line 213, `app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx` around line 167, `app/(admin)/[adminSegment]/coefficients/HistoryTable.tsx` around line 169, and `src/components/proposals/LoadMoreButton.tsx` — confirm each is a "Charger plus" control inside a `{nextCursor && …}` guard before writing that it is
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-12, D-40-13, D-40-14
    - `.planning/phases/40-milestone-record-closure/40-PATTERNS.md` § "10. HOUSE-06 — 38-WALK-SURFACES.md errata" — note it records that **no errata precedent exists anywhere in `.planning/`**, and offers a starting draft
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md</files>

  <action>
    **(a) Errata block in `38-WALK-SURFACES.md` (D-40-13, D-40-14).**

    Append a dated errata block. D-40-13 explicitly **rejects rewriting Table 1 in place**: that
    table is what a reader uses to judge whether the Phase 38 walk was sound, and silently editing
    it removes the evidence for that judgement. Table 1's rows stay byte-for-byte as they are.

    Placement within the document is Claude's Discretion (40-CONTEXT.md). Required heading, exactly:
    `## 2026-09-07 errata — Phase 40, HOUSE-06`

    Required content — **all four sites**, not the two HOUSE-06 names (D-40-14):
    `PartnersList.tsx:213`, `LcReferencesList.tsx:167`, `HistoryTable.tsx:169`, and
    `LoadMoreButton`. State that each is a **"Charger plus" pagination control**, rendered only
    inside `{nextCursor && …}`, so none renders on a single-page dataset — which is why the Phase
    38 walk could not observe them — and that verifying them needs a multi-page dataset. Table 1
    rows 2, 3 and 5 are the rows affected; row 5's "per-row link" language is the specific
    mis-description. State that the original Table 1 text above is deliberately left unedited.
    Per D-40-14, **do not file a follow-up item** to actually observe them: the correction is the
    closure, and observing them is recorded in 40-CONTEXT.md's Deferred Ideas.

    Add a second, separately-labelled paragraph in the same block for **Table 1 row 1**
    (`HOUSE-05 cross-reference`): row 1 cites `src/components/proposal/ProposalForm.tsx` `.btn-out`
    (line 537, Cancel) and `.btn-navy` (line 547, submit). Plan 40-02 deleted that component on
    2026-09-07 as HOUSE-05, so those two line citations now point into removed code. This is the
    same finding F-38-03 already recorded (38-CONTEXT.md D-38-04's premise "every CLOSE-08 surface
    renders a `.btn-out`" is FALSE for wizard step 1). Keep this paragraph distinct from the
    HOUSE-06 pagination correction — do not merge the two findings into one claim.

    Shape: since no errata convention exists in this planning tree, reuse the amend-in-place
    discipline from `.planning/REQUIREMENTS.md` lines 184-187 in block form — preserve the original
    entirely, append a dated attributed correction. 40-PATTERNS.md § 10 offers a starting draft; it
    is a starting point, not a locked pattern.

    **(b) HOUSE-05 amendment (D-40-12), `.planning/REQUIREMENTS.md` lines 143-150.** Flip `- [ ]`
    to `- [x]` and append the dated parenthetical in the house form. Record that the
    delete-or-document question is answered: **delete**. `ProposalForm` and its action row were
    removed in Phase 40 plan `40-02`; `ProposalFormProvider` was kept (it is imported by
    `app/(authed)/proposals/new/parametres/page.tsx` and exercised by `ParametresFormCard.test.tsx`),
    and the four stale line-number citations in `src/lib/calc/schema.ts`,
    `ParametresFormCard.tsx`, `RecapSection.tsx` and `DuplicatePrefillToast.tsx` were repaired in
    the same plan (D-40-11). Preserve the original sentences and the existing italic
    *Found 2026-09-06 …* note verbatim.

    **(c) HOUSE-06 amendment, `.planning/REQUIREMENTS.md` lines 152-158.** Flip `- [ ]` to `- [x]`
    and append the dated parenthetical citing the errata block:
    `.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` §
    `2026-09-07 errata — Phase 40, HOUSE-06`. Record that the correction was made by dated errata
    rather than an in-place Table 1 rewrite (D-40-13), and that it covers all four sites (D-40-14).
    Preserve the original sentences and the *Found 2026-09-06 (F-38-06) …* note verbatim.

    **(d) Traceability rows.** Flip `| HOUSE-05 | Phase 40 — Milestone Record Closure | Pending |`
    and the HOUSE-06 row's third cell from `Pending` to `Complete`. Leave CLOSE-06 and CLOSE-07 on
    `Pending` — plan 40-06 owns those.
  </action>

  <verify>
    <automated>grep -qF '## 2026-09-07 errata — Phase 40, HOUSE-06' .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md && grep -qE '^- \[x\] \*\*HOUSE-05\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*HOUSE-06\*\*' .planning/REQUIREMENTS.md && ! grep -qE '^\| HOUSE-0[56] \|.*\| Pending \|' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF '## 2026-09-07 errata — Phase 40, HOUSE-06' .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` returns `1`
    - That errata block contains all four literal site names: `PartnersList.tsx:213`, `LcReferencesList.tsx:167`, `HistoryTable.tsx:169`, `LoadMoreButton`
    - It contains the literal strings `Charger plus`, `nextCursor` and `multi-page dataset`
    - It contains a distinct `HOUSE-05` paragraph naming `ProposalForm.tsx` and plan `40-02`
    - `git diff .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` shows **only added lines** — no `-` line at all, proving Table 1 was not edited in place
    - `sed -n '38,46p' .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` is byte-identical to `git show HEAD:.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md | sed -n '38,46p'`
    - `grep -cE '^- \[x\] \*\*(HOUSE-05|HOUSE-06)\*\*' .planning/REQUIREMENTS.md` returns `2`
    - The HOUSE-05 block contains the literal strings `40-02`, `ProposalFormProvider` and `delete`
    - The HOUSE-06 block contains the literal string `2026-09-07 errata — Phase 40, HOUSE-06`
    - `grep -cE '^\| (HOUSE-05|HOUSE-06) \| Phase 40 — Milestone Record Closure \| Complete \|' .planning/REQUIREMENTS.md` returns `2`
    - `grep -cE '^\| (CLOSE-06|CLOSE-07) \| Phase 40 — Milestone Record Closure \| Pending \|' .planning/REQUIREMENTS.md` returns `2`
    - `git diff --name-only -- src app` is empty (this task edits planning documents only)
  </acceptance_criteria>

  <done>`38-WALK-SURFACES.md` carries a dated errata block correcting all four pagination-control descriptions plus the HOUSE-05 cross-reference, with Table 1 provably unedited. HOUSE-05 and HOUSE-06 are ticked with pointers, and their traceability rows read Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded security posture ↔ actual runtime posture | This plan does not change code, but it does flip two **security-relevant** requirements — OPS-01 (credential rotation) and OPS-02 (CSRF allow-list) — from open to closed. If a citation does not resolve, a real control is recorded as satisfied when it is not. That is the only boundary this plan can affect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | Repudiation | OPS-01 tick (credential rotation) | mitigate | The ledger would falsely record credential rotation as done if the evidence does not exist. Task 1's `<read_first>` requires opening `docs/operations/phase-21-gate-evidence.md` § GATE-01 before writing, and the acceptance criteria require that literal path in the OPS-01 block. The evidence document exists on disk and records both admins rotated 2026-05-29 with the old password tested and rejected. If the section does not say that on inspection, the tick must not be written — report instead. |
| T-40-04-02 | Spoofing | OPS-02 tick (CSRF allow-list) | mitigate | Ticking OPS-02 records a CSRF control as configured. Task 1's `<read_first>` requires reading `src/lib/auth/index.ts:210` (`trustedOrigins: allowedOrigins`) and `tests/trusted-origins.test.ts` before writing, and the acceptance criteria require both paths in the block. Per D-14 the amendment records **defence in depth** rather than the retired claim that SameSite alone suffices, so the ledger does not overstate either layer. Per D-16 it asserts allow-list membership and is forbidden from asserting a rejection status code — the acceptance criteria fail the task if `non-2xx` or `status code` appears. |
| T-40-04-03 | Repudiation | OPS-03 tick (OVH portability, unproven) | mitigate | OPS-03 is ticked without the smoke test ever running. The honest risk is that "closed" reads as "portability proven". Mitigated three ways: the requirement's own text explicitly closes on a re-dating decision; the amendment must state that `scripts/smoke-ovh.ts` stays ready and unrun and that the blocker is the environment; and Task 2 opens a pending todo whose acceptance checklist keeps the actual run outstanding. |
| T-40-04-04 | Tampering | `.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` Table 1 | mitigate | Editing Table 1 in place would destroy the evidence a reader uses to judge the Phase 38 walk. Task 3's acceptance criteria assert the diff contains **only added lines** and that lines 38-46 are byte-identical to `git show HEAD:`, which makes an in-place edit mechanically detectable. |
| T-40-04-05 | Tampering | `scripts/smoke-ovh.ts` execution against production | mitigate | D-12 rejected rehearsing the harness against the Vercel deployment because it would create and delete a **real proposal in production**. Both Task 1 and Task 2 forbid running it, and Task 2's acceptance criteria assert no run occurred. |
| T-40-04-06 | Information Disclosure | credentials named in the ledger | accept | The amendments name `leasetic2026` as a **retired** password — a string already present verbatim in the un-amended requirement text, in `docs/operations/phase-21-gate-evidence.md`, and throughout the planning history. No live credential, token or connection string is introduced. |

**No new attack surface.** This plan edits planning documents and creates one todo file; it changes
no runtime behaviour. Its genuine risk is record-accuracy — specifically, recording two security
controls as closed — and it is managed by requiring every citation to be opened and verified
before the tick is written. No package is installed, so the Package Legitimacy Gate does not apply
and there is no `T-40-04-SC` supply-chain threat for this plan.
</threat_model>

<verification>
- `grep -cE '^- \[x\] \*\*(OPS-01|OPS-02|OPS-03|OPS-04|GAP-05|HOUSE-05|HOUSE-06)\*\*' .planning/REQUIREMENTS.md` → `7`
- `grep -cE '^\| (OPS-01|OPS-02|OPS-03|OPS-04|GAP-05|HOUSE-05|HOUSE-06) \| Phase 40 — Milestone Record Closure \| Complete \|' .planning/REQUIREMENTS.md` → `7`
- `git diff .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md | grep -c '^-[^-]'` → `0` (append-only)
- `test -f .planning/todos/pending/ops-03-ovh-cutover-december-2026.md` exits 0
- `git diff --name-only -- src app scripts tests` is empty
- `npm run lint:check` exits 0 (confirms plan 40-02's tree is still clean for the wave-3 and wave-4 plans)
</verification>

<success_criteria>
1. OPS-01, OPS-02, OPS-03, OPS-04 and GAP-05 are ticked `[x]`, each with a dated `*(Amended 2026-09-07 by …)*` parenthetical naming the real mechanism and a pointer that was opened and verified.
2. Every original requirement sentence is still readable; GAP-05's "written nowhere" clause is visibly retracted rather than deleted.
3. OPS-02's amendment records defence in depth and asserts no status code.
4. `.planning/todos/pending/ops-03-ovh-cutover-december-2026.md` exists, `status: pending`, naming December 2026 and Antoine's provisioning step.
5. `38-WALK-SURFACES.md` gained a `## 2026-09-07 errata — Phase 40, HOUSE-06` block covering all four sites plus the HOUSE-05 cross-reference, with a provably append-only diff.
6. HOUSE-05 and HOUSE-06 are ticked; all seven traceability rows read Complete; CLOSE-06 and CLOSE-07 still read Pending.
7. Nothing under `src/`, `app/`, `scripts/` or `tests/` changed, and `scripts/smoke-ovh.ts` was not run.
</success_criteria>

<output>
Create `.planning/phases/40-milestone-record-closure/40-04-SUMMARY.md` when done.
Record: the final wording of each of the seven amendments, and — for each of the five OPS/GAP
pointers — a one-line note confirming the cited file and line were opened and say what the
amendment claims. Plan 40-06 flips CLOSE-06/CLOSE-07 and needs the traceability table's post-edit
state.
</output>
