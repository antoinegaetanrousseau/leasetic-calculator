---
phase: 40-milestone-record-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/proposal/ProposalForm.tsx
  - src/lib/calc/schema.ts
  - app/(authed)/proposals/new/parametres/ParametresFormCard.tsx
  - app/(authed)/proposals/new/_components/RecapSection.tsx
  - src/components/proposals/DuplicatePrefillToast.tsx
autonomous: true
requirements: [HOUSE-05]

must_haves:
  truths:
    - "The dead ProposalForm component no longer exists in the tree"
    - "ProposalFormProvider still mounts and the parametres wizard step still renders and validates"
    - "No source comment cites a ProposalForm line number that no longer exists"
    - "The lint gate stays clean — no import left unused by the deletion"
  artifacts:
    - path: "src/components/proposal/ProposalForm.tsx"
      provides: "ProposalFormProvider only — the RHF context consumed by the parametres wizard step"
      contains: "export function ProposalFormProvider"
  key_links:
    - from: "app/(authed)/proposals/new/parametres/page.tsx"
      to: "src/components/proposal/ProposalForm.tsx"
      via: "import { ProposalFormProvider } and mount"
      pattern: "ProposalFormProvider"
    - from: "app/(authed)/proposals/new/parametres/ParametresFormCard.tsx"
      to: "src/components/proposal/ProposalForm.tsx type ProposalFormValues"
      via: "a line-number citation in a comment that must be re-derived after the deletion shifts it"
      pattern: "see ProposalForm\\.tsx:[0-9]+"
---

<objective>
Delete the `ProposalForm` component — 449 lines of React that no route or component renders — and
repair the four source comments that cite it by line number.

This is the **only production code change in Phase 40** (D-40-10, D-40-11). `ProposalFormProvider`
in the same file is live and stays: it is imported by
`app/(authed)/proposals/new/parametres/page.tsx:52`, mounted at `:288`, and exercised by
`ParametresFormCard.test.tsx`. `ProposalForm` itself is rendered nowhere — a repo-wide search for
`<ProposalForm` finds only hits inside that file's own doc comments.

D-40-10 rejected the alternative (retain it with a "kept on purpose" comment): dead code that four
live files cite by line number is worse than dead code alone, because it makes the file read as
load-bearing.

Purpose: HOUSE-05 has sat open as a delete-or-document question since the Phase 38 CLOSE-08 walk
found it (F-38-03). This closes it by deleting, and stops four live files pointing at a component
that will not exist.
Output: One component removed, five files edited, `npm test` and `npm run lint:check` both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-milestone-record-closure/40-CONTEXT.md
@.planning/phases/40-milestone-record-closure/40-PATTERNS.md
@src/components/proposal/ProposalForm.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the ProposalForm component and prune the imports it alone used</name>

  <read_first>
    - `src/components/proposal/ProposalForm.tsx` **in full** (557 lines) — you must see which of the top-of-file imports the surviving `ProposalFormProvider` actually uses before pruning anything
    - `app/(authed)/proposals/new/parametres/page.tsx` around lines 45-60 and 280-295 — the only real consumer (`import { ProposalFormProvider }` at :52, mounted at :288)
    - `app/(authed)/proposals/new/parametres/ParametresFormCard.tsx` lines 40-60 — it declares its own local `DURATION_OPTIONS` at line 53, which is why `ProposalForm.tsx`'s copy is safe to delete
    - `app/(authed)/proposals/new/parametres/ParametresFormCard.test.tsx` — the test that mounts `ProposalFormProvider` and must stay green
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-10
    - `.planning/phases/40-milestone-record-closure/40-PATTERNS.md` § "8. `src/components/proposal/ProposalForm.tsx` — the code deletion (HOUSE-05)"
  </read_first>

  <files>src/components/proposal/ProposalForm.tsx</files>

  <action>
    Per D-40-10, delete the dead `ProposalForm` component and everything that exists only to serve
    it, keeping `ProposalFormProvider`.

    **Delete (current line numbers, verify against the file before cutting):**
    - `export interface ProposalFormProps` — lines 84-86. Used only as `ProposalForm`'s prop type.
    - `const DURATION_OPTIONS` — lines 88-92. Grep confirms exactly two references repo-wide:
      its declaration here and one use at `ProposalForm.tsx:490`, inside the function being
      deleted. `ParametresFormCard.tsx` declares its own identical copy at line 53 and does not
      import this one.
    - The `/** Phase 7 proposal entry form (PROP-06 + PROP-08). … */` doc block — lines 94-108.
    - `export function ProposalForm({ lang }: ProposalFormProps)` — line 109 through the file's
      closing brace at line 557, including the trailing action row (`.btn-out` Cancel +
      `.btn-navy` Generate buttons).

    **Keep:** lines 1-83 — the `'use client'` directive, the `ProposalFormValues` doc block and
    `type ProposalFormValues = z.input<typeof proposalInputSchema>` (currently line 36),
    `export interface ProposalFormProviderProps` (41-45), the provider's doc block (47-52), and
    `export function ProposalFormProvider` (53-83).

    **Then prune the import block.** This is the part that fails CI if skipped: this repo's CI
    runs `eslint --max-warnings=0`, so an import left unused by the deletion passes `tsc` and
    `vitest` but breaks the pipeline. Determine the surviving set by reading the
    `ProposalFormProvider` body, not from this list — but as a cross-check, the provider body
    references only: `useForm` and `FormProvider` (react-hook-form), `zodResolver`
    (@hookform/resolvers/zod), `type ReactNode` (react), `type z` (zod), and `proposalInputSchema`
    + `type ProposalInput` (@/lib/calc). Every other current import — `SectionTitle`, `Field` /
    `FieldError` / `FieldLabel`, `Input`, `useState`, `useRef`, `useRouter`, `Controller`,
    `useFormContext`, `toast`, `ArrowRightIcon`, `RotateCcwIcon`, `t` / `Lang` / `DictKey`,
    `DurationSegmented`, `YesNoToggle`, `NumberInputAmount`, `PhoneInput`, `SirenInput` — exists
    only for the deleted component and must go, along with any import statement left with no
    specifiers.

    **Do not** rename the file, move `ProposalFormProvider` elsewhere, or delete
    `DurationSegmented.tsx` / `YesNoToggle.tsx` / `NumberInputAmount.tsx` / `PhoneInput.tsx` /
    `SirenInput.tsx` — those sibling components have other live consumers and are out of HOUSE-05's
    scope.

    Finally, run `grep -n 'ProposalFormValues' src/components/proposal/ProposalForm.tsx` and
    **record the new line number** of the `type ProposalFormValues = …` declaration in the plan
    summary. Pruning the import block shifts it upward from line 36, and Task 2 needs the real
    post-edit number.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm run lint:check && npm test -- ParametresFormCard</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF 'export function ProposalForm({' src/components/proposal/ProposalForm.tsx` returns `0`
    - `grep -cF 'export function ProposalFormProvider' src/components/proposal/ProposalForm.tsx` returns `1`
    - `grep -cF 'DURATION_OPTIONS' src/components/proposal/ProposalForm.tsx` returns `0`
    - `grep -cE 'ProposalFormProps\b' src/components/proposal/ProposalForm.tsx` returns `0` (the standalone `ProposalFormProps` interface is gone; `ProposalFormProviderProps` is a different identifier and does not match this pattern)
    - `wc -l < src/components/proposal/ProposalForm.tsx` returns a value under 80 (was 557)
    - `grep -cE '^import' src/components/proposal/ProposalForm.tsx` returns a value of 5 or fewer
    - `npx tsc --noEmit` exits 0
    - `npm run lint:check` exits 0 — this is the gate that catches an unpruned import; a passing `tsc` alone does not prove it
    - `npm test -- ParametresFormCard` exits 0 (the live `ProposalFormProvider` consumer)
    - `npm test` exits 0 with no newly failing suite
    - `grep -rn 'from .*components/proposal/ProposalForm' src app` returns only imports of `ProposalFormProvider` (no importer of the deleted `ProposalForm` exists)
  </acceptance_criteria>

  <done>`src/components/proposal/ProposalForm.tsx` contains `ProposalFormProvider` and nothing else. Typecheck, `eslint --max-warnings=0` and the full test suite are green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repair the four stale ProposalForm citations in live source files</name>

  <read_first>
    - `src/components/proposal/ProposalForm.tsx` **after Task 1's edit** — you need the real post-deletion line number of `type ProposalFormValues`
    - `src/lib/calc/schema.ts` lines 1-14 (the "Single-source discipline" doc block; line 6 is the stale citation)
    - `app/(authed)/proposals/new/parametres/ParametresFormCard.tsx` lines 44-56 (the comment at ~50 citing `ProposalForm.tsx:36`)
    - `app/(authed)/proposals/new/_components/RecapSection.tsx` lines 82-95 (the comment at ~88 citing `ProposalForm.tsx:213-219`)
    - `src/components/ui/SectionTitle.tsx` and `app/globals.css` — where the `.ctitle` / `.dot` markup **actually** lives (grep-confirmed; it is not and never was at `ProposalForm.tsx:213-219`, which contains `<form onSubmit>` / `<section className="card">` / `<SectionTitle>`)
    - `src/components/proposals/DuplicatePrefillToast.tsx` lines 10-26 (the comment at ~18 describing a `duplicatedFromId` POST-body mechanism)
    - `app/(authed)/proposals/new/parametres/page.tsx` lines ~120-213 — the **current** duplicate-prefill mechanism (D-25/D-26: the source proposal's `inputs` are spread server-side when the draft is minted; there is no client-side `duplicatedFromId` field feeding a POST body any more)
    - `.planning/phases/40-milestone-record-closure/40-CONTEXT.md` § D-40-11
    - `.planning/phases/40-milestone-record-closure/40-PATTERNS.md` § "9. The four stale line-number citations (D-40-11) — concrete before/after"
  </read_first>

  <files>src/lib/calc/schema.ts, app/(authed)/proposals/new/parametres/ParametresFormCard.tsx, app/(authed)/proposals/new/_components/RecapSection.tsx, src/components/proposals/DuplicatePrefillToast.tsx</files>

  <action>
    Repair all four citations, per D-40-11. Committed scope is **repairing the numbers and the
    dead-component references**; wholesale re-pointing the comments at the live surfaces
    (`ParametresFormCard` / `WizardStep1Wiring`) was offered during discussion and not chosen, so
    keep each repair minimal and do not rewrite the surrounding prose beyond what is false.

    **(a) `src/lib/calc/schema.ts` line 6.** Currently:
    `*   1. Plan 07-04's <ProposalForm> via @hookform/resolvers/zod`.
    `<ProposalForm>` no longer exists. The claim it makes is still true of the surviving code:
    `ProposalFormProvider` calls `zodResolver(proposalInputSchema)` at
    `src/components/proposal/ProposalForm.tsx`. Rewrite the bullet to name
    `ProposalFormProvider` as the `@hookform/resolvers/zod` consumer instead of the deleted
    component. Keep the numbered-list shape and the sibling bullet 2 untouched.

    **(b) `ParametresFormCard.tsx` line ~50.** Currently the comment ends
    `— see ProposalForm.tsx:36).` and line 36 was `type ProposalFormValues = z.input<typeof
    proposalInputSchema>`. That target survives Task 1, but Task 1's import pruning removes ~14
    lines **above** it, so the declaration is no longer at line 36. Run
    `grep -n 'type ProposalFormValues' src/components/proposal/ProposalForm.tsx`, take the number
    it prints, and write that number into the citation. Do not guess and do not leave `36`.

    **(c) `RecapSection.tsx` line ~88.** Currently:
    "Header reuses the .ctitle / .dot pattern lifted verbatim from
    `src/components/proposal/ProposalForm.tsx:213-219`."
    This citation is **already wrong today**, before any deletion: a direct read of
    `ProposalForm.tsx:213-219` shows `<form onSubmit>` / `<section className="card">` /
    `<SectionTitle>` markup with no `.ctitle` or `.dot` anywhere. Grep places the real
    `.ctitle` / `.dot` markup in `src/components/ui/SectionTitle.tsx`, with the rule set in
    `app/globals.css` — which is consistent with the very next JSX line in this file rendering
    `<SectionTitle>`. Repoint the citation at `src/components/ui/SectionTitle.tsx` (and
    `app/globals.css` for the `background: var(--gd)` rule). Keep the "canonical accent
    treatment" sentence.

    **(d) `DuplicatePrefillToast.tsx` line ~18.** Currently: "Note: ProposalForm captures
    duplicatedFromId via useState lazy init (window.location.search at mount time) so the POST
    body still receives the source ID even after this component strips the URL flag."
    Doubly stale: the `duplicatedFromId` lazy-init lived only inside the deleted `ProposalForm`
    (grep confirms it appears in no other live file, including `WizardStep1Wiring.tsx`), and the
    POST-body mechanism it describes predates the wizard refactor — duplicate prefill is now
    server-side in `app/(authed)/proposals/new/parametres/page.tsx`, which spreads the source
    proposal's `inputs` when minting the draft. Replace the note with a one-or-two-sentence
    description of that current server-side mechanism, citing
    `app/(authed)/proposals/new/parametres/page.tsx`. Leave the numbered 1./2. list above it and
    the `fired` ref paragraph below it untouched.

    Across all four: these are comment-only edits. No exported symbol, prop, function signature or
    runtime expression changes.
  </action>

  <verify>
    <automated>! grep -rn 'ProposalForm\.tsx:213-219\|ProposalForm\.tsx:36\b' src app && ! grep -rn '<ProposalForm>' src app && npx tsc --noEmit && npm run lint:check && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'ProposalForm.tsx:213-219' src app` returns no matches
    - `grep -rn '<ProposalForm>' src app` returns no matches
    - `grep -rnE 'ProposalForm\.tsx:[0-9]' src app` returns at most one hit — the `ParametresFormCard.tsx` citation — and the number in it equals the line number printed by `grep -n 'type ProposalFormValues' src/components/proposal/ProposalForm.tsx`
    - `grep -c 'ProposalFormProvider' src/lib/calc/schema.ts` returns at least `1`
    - `grep -c 'SectionTitle' "app/(authed)/proposals/new/_components/RecapSection.tsx"` shows the comment now cites `src/components/ui/SectionTitle.tsx`
    - `grep -c 'duplicatedFromId' src/components/proposals/DuplicatePrefillToast.tsx` returns `0`, and the file cites `app/(authed)/proposals/new/parametres/page.tsx`
    - `git diff --stat` shows changes confined to comment lines in the four files (no `+`/`-` on an `export`, `function`, `const` or JSX line)
    - `npx tsc --noEmit` exits 0
    - `npm run lint:check` exits 0
    - `npm test` exits 0 with no newly failing suite
  </acceptance_criteria>

  <done>No live source file cites a `ProposalForm.tsx` line that does not exist or a mechanism the app no longer uses. Typecheck, lint and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/proposals/new/parametres` | The wizard step where `ProposalFormProvider` supplies the RHF context and `zodResolver(proposalInputSchema)` runs client-side validation. This plan deletes code adjacent to that boundary, so the question is whether anything enforcing it is removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Tampering | `src/components/proposal/ProposalForm.tsx` — deleting a component that might carry a validation path | mitigate | The deleted function is unreachable: no route or component renders `<ProposalForm`, verified by repo-wide grep (the only hits were inside the file's own doc comments). The validation path that matters, `zodResolver(proposalInputSchema)`, lives in the **surviving** `ProposalFormProvider`, and Task 1's acceptance criteria assert `export function ProposalFormProvider` is still present and `npm test -- ParametresFormCard` passes. Client-side validation was never the security control regardless — the server route re-parses with the same schema (the "single-source discipline" `src/lib/calc/schema.ts` documents). |
| T-40-02-02 | Elevation of Privilege | deleted action row (`.btn-out` Cancel + `.btn-navy` Generate) | accept | The deleted action row contained no authorization check; auth is enforced by `proxy.ts`'s cookie gate and the server route handlers, neither of which this plan touches. Removing unreachable UI cannot widen an authorization envelope. |
| T-40-02-03 | Information Disclosure | comment edits in four files | mitigate | The repaired comments name file paths and component names already public in the repo. Task 2's acceptance criteria confine the diff to comment lines (`git diff --stat` must show no change to an `export`, `function`, `const` or JSX line), so a comment repair cannot smuggle a behavioural change. |
| T-40-02-04 | Denial of Service | over-broad import pruning | mitigate | Pruning too much breaks the build rather than shipping a defect; `npx tsc --noEmit`, `npm run lint:check` and the full `npm test` run are all acceptance criteria, and `lint:check` (`eslint --max-warnings=0`) is the specific gate for the under-pruned case that `tsc` alone would pass. |

**No new attack surface.** This plan is subtractive: it removes unreachable client code and edits
comments. No dependency is added — `package.json` is not in `files_modified` — so the Package
Legitimacy Gate does not apply and there is no `T-40-02-SC` supply-chain threat for this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run lint:check` exits 0 (the CI gate: `eslint --max-warnings=0`)
- `npm test` exits 0, with `ParametresFormCard.test.tsx` among the passing suites
- `grep -rn '<ProposalForm>' src app` → no matches
- `grep -rnE 'ProposalForm\.tsx:[0-9]' src app` → at most one hit, whose number matches the live `type ProposalFormValues` declaration
- `git diff --name-only` lists exactly the five files in `files_modified`
</verification>

<success_criteria>
1. `src/components/proposal/ProposalForm.tsx` is under 80 lines and exports only `ProposalFormProvider`, `ProposalFormProviderProps` and the `ProposalFormValues` type.
2. `DURATION_OPTIONS` and `ProposalFormProps` are gone from that file; `ParametresFormCard.tsx`'s own `DURATION_OPTIONS` is untouched.
3. No import in that file is unused — `npm run lint:check` exits 0.
4. All four stale citations are repaired; the one surviving `ProposalForm.tsx:<N>` citation carries the grep-derived post-edit line number.
5. `npx tsc --noEmit` and `npm test` both exit 0.
6. `git diff --name-only` lists exactly the five declared files.
</success_criteria>

<output>
Create `.planning/phases/40-milestone-record-closure/40-02-SUMMARY.md` when done.
Record: the final line count of `ProposalForm.tsx`, the post-edit line number of
`type ProposalFormValues`, the exact list of imports removed, and the final wording of each of the
four repaired comments. Plan 40-04's HOUSE-05 requirement amendment cites this plan and this
commit by ID.
</output>
