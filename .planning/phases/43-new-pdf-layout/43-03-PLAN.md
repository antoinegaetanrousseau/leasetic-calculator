---
phase: 43-new-pdf-layout
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/lib/i18n/dictionaries.ts
  - src/lib/i18n/dictionaries.test.ts
autonomous: true
requirements: [DOC-02, DOC-03, DOC-05, DOC-06, DOC-07, DOC-08, DOC-10]
must_haves:
  truths:
    - "Every label the new layout renders exists as an FR/EN pair sourced verbatim from the two design files"
    - "The conditions paragraph interpolates the proposal's actual validityDays, never a hardcoded 30"
    - "The EN conditions paragraph spells the brand Leasetic with no accent"
    - "The five layout-orphaned pdf.* keys are gone and nothing references them"
    - "The three pdf.partnerType.* pairs survive, annotated with D-02 as the reason they are unused"
  artifacts:
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "the full pdf.* FR/EN key set for the Claude Design layout"
      contains: "pdf.card.client.title"
    - path: "src/lib/i18n/dictionaries.test.ts"
      provides: "updated phase8Keys list, new Phase 43 exact-string assertions, annotated D-02 orphan test"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "src/lib/i18n/dictionaries.ts"
      via: "t(key, lang) lookups"
      pattern: "t\\('pdf\\."
---

<objective>
Land every FR/EN string the redesigned document renders, sourced verbatim from the two design files,
and dispose of the keys the redesign orphans.

Purpose: D-01 makes the design files authoritative for copy. Every card label, table row,
acceptance-block label and the full legal conditions paragraph differ between `Quote-FR-A.dc.html`
and `Quote-EN-A.dc.html` (DOC-10), and DOC-06 requires one deliberate deviation from both — the
validity period must interpolate the proposal's own `validityDays` rather than the design's
hardcoded "30 jours" / "30 days".

Output: ~32 new `pdf.*` FR/EN pairs, four existing pairs re-valued to the design's wording, five
orphaned keys deleted, three annotated, and the pinning tests updated in lockstep.

This plan writes no rendering code. It is deliberately parallel to 43-02 and 43-04 — no shared files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the design's FR/EN copy as pdf.* pairs and re-value the four keys the design rewords</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` — the FR copy, verbatim. Header §33-37,
      title + pills §41-50, cards §52-78, hero + table §80-99, conditions §101-104, acceptance
      §106-119, footer §121-130.
    - `.planning/assets/v1.9-quote-design/Quote-EN-A.dc.html` — the EN copy, verbatim. Same structure,
      every string differs.
    - `src/lib/i18n/dictionaries.ts` §470-491 (FR `pdf.*` block) and §1785-1806 (EN `pdf.*` block) —
      the flat `'pdf.<dotted.path>': '<string>'` convention, the paired ordering, and the existing
      use of raw `’` / ` ` escapes rather than literal typographic characters.
    - `.planning/phases/43-new-pdf-layout/43-PATTERNS.md` — § `src/lib/i18n/dictionaries.ts`, which
      documents the paired-key convention and the escape-sequence rule.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — D-01, D-02, D-04, D-06, D-09.
  </read_first>
  <action>
    Add the following keys to BOTH language blocks, at the position matching the design's document
    order, grouped by section with the same comment style the file already uses. Values are exact.
    Use `’` for every French and English typographic apostrophe and ` ` for a French narrow
    no-break space, matching the existing block — never a literal `'` or a plain space.

    Header (DOC-01). Uppercase strings are stored already-uppercased; do NOT rely on
    `textTransform: 'uppercase'` at render time, so the FR `N°` and the accented `É` are exact.
      `pdf.header.proposition.eyebrow` = FR `PROPOSITION N°` / EN `PROPOSAL NO.`
      `pdf.header.issued` = FR `Établie le {0}` / EN `Issued {0}`

    Title and pills (DOC-01).
      `pdf.pill.partnerRef` = FR `Réf. partenaire · {0}` / EN `Partner ref. · {0}`
      `pdf.pill.term` = FR `{0} mois` / EN `{0} months`

    Client card (DOC-02).
      `pdf.card.client.title` = FR `SOCIÉTÉ CLIENTE` / EN `CLIENT COMPANY`
      `pdf.card.client.siren` = FR `SIREN` / EN `SIREN`
      `pdf.card.client.siret` = FR `SIRET` / EN `SIRET`
      `pdf.card.client.recipient` = FR `Destinataire` / EN `Recipient`
      `pdf.card.client.role` = FR `Fonction` / EN `Role`
      `pdf.card.client.phone` = FR `Téléphone` / EN `Phone`
      `pdf.card.client.email` = FR `Email` / EN `Email`

    Contact card (DOC-03, in D-04's partner-first order — the row LABELS keep the design's wording,
    only the block ORDER inverts).
      `pdf.card.contact.title` = FR `VOTRE CONTACT` / EN `YOUR CONTACT`
      `pdf.card.contact.salesRep` = FR `Commercial` / EN `Sales rep`
      `pdf.card.contact.partnerPhone` = FR `Téléphone` / EN `Phone`
      `pdf.card.contact.advisorName` = FR `Conseiller` / EN `Advisor`
      `pdf.card.contact.advisorRole` = FR `Fonction` / EN `Role`
      `pdf.card.contact.advisorPhone` = FR `Téléphone` / EN `Phone`
      `pdf.card.contact.advisorEmail` = FR `Email` / EN `Email`
    `pdf.card.contact.advisorName` is the one label the design does not supply: under D-04 the
    advisor's name demotes from the 11pt headline to a key/value row, and a row needs a key. The
    wording matches the vocabulary Phase 42 already shipped at `admin.advisor.hero.title`
    (`Conseiller Leasetic` / `Leasetic advisor`). Comment it with that derivation and cite D-04.
    Note the design's card has no `Partenaire` row because the partner company is the 11pt headline
    under D-04 — do not add one.

    Financial table (DOC-05).
      `pdf.table.title` = FR `CONDITIONS FINANCIÈRES` / EN `FINANCIAL TERMS`
      `pdf.table.amount` = FR `Montant financé HT` / EN `Financed amount excl. VAT`
      `pdf.table.term` = FR `Durée de location` / EN `Lease term`
      `pdf.table.monthlyRent` = FR `Loyer mensuel HT` / EN `Monthly rent excl. VAT`
      `pdf.table.total` = FR `Total des loyers HT` / EN `Total rents excl. VAT`
    The `Coefficient appliqué` / `Applied coefficient` row reuses the EXISTING
    `pdf.computed.coefficient.label`, whose current FR/EN values already match the design exactly.
    Do not add a duplicate key.

    Conditions (DOC-06).
      `pdf.conditions.title` = FR `CONDITIONS` / EN `CONDITIONS`
    Re-value the EXISTING `pdf.validity.caption` (do not add a new key — it already carries the
    `{0}` / `{1}` placeholders the two-arg interpolation test pins) to the design's full paragraph,
    with the design's hardcoded "30 jours" / "30 days" replaced by `{1}`:
      FR: `Proposition valable jusqu’au {0} ({1} jours), sous réserve de l’accord du comité des engagements de Leasetic. Document non contractuel. Les loyers sont exprimés hors taxes et hors prestations annexes. Le coefficient appliqué est susceptible d’évoluer en cas de modification du montant financé, de la durée ou des conditions de marché à la date de mise en loyer.`
      EN: `This proposal is valid until {0} ({1} days), subject to approval by Leasetic’s credit committee. Non-contractual document. Rents are stated excluding taxes and excluding ancillary services. The applied coefficient may change if the financed amount, the term, or market conditions at the lease start date change.`
    The EN value today reads `Leasétic` — the stale accented spelling. The replacement above
    spells it `Leasetic`. That correction is part of this task, not incidental.

    Acceptance block (DOC-07).
      `pdf.acceptance.title` = FR `ACCEPTATION DU CLIENT` / EN `CLIENT ACCEPTANCE`
      `pdf.acceptance.note` = FR `Bon pour accord — signature et cachet de l’entreprise` / EN `Agreed and accepted — company signature and stamp`
      `pdf.acceptance.place` = FR `Fait à` / EN `Place`
      `pdf.acceptance.date` = FR `Le` / EN `Date`
      `pdf.acceptance.signatory` = FR `Nom et qualité du signataire` / EN `Name and role of signatory`
      `pdf.acceptance.signature` = FR `Signature` / EN `Signature`
      `pdf.acceptance.stamp` = FR `Cachet de l’entreprise` / EN `Company stamp`

    Legal footer (DOC-08). The design renders the brand word in navy semibold and the rest in teal,
    so the first line is two nodes and therefore two keys.
      `pdf.footer.legal.brand` = FR `Leasetic` / EN `Leasetic`
      `pdf.footer.legal.rest` = FR ` — SAS, société par actions simplifiée · 63 chemin des Arnis, 31130 Balma` / EN ` — SAS, société par actions simplifiée · 63 chemin des Arnis, 31130 Balma, France` (note the leading space in both, and that the EN variant appends `, France`)
      `pdf.footer.legal.line2` = FR `SIREN 830 733 606 · SIRET (siège) 830 733 606 00040 · TVA FR06830733606` / EN `SIREN 830 733 606 · SIRET (registered office) 830 733 606 00040 · VAT FR06830733606`
    The right-hand footer text (`{lcRef} · Page 1/1`) needs no key — the design uses the identical
    English-neutral wording in both files and it is produced by react-pdf's `render={({ pageNumber,
    totalPages }) => ...}` callback in plan 43-06.

    Re-value these EXISTING keys to the design's wording:
      `pdf.title` — FR stays `Proposition de location financière` (already matches the design h1).
        EN changes from `Financial lease proposal` to `Equipment lease financing proposal`.
      `pdf.loyer.label` — FR stays `LOYER MENSUEL HT`. EN changes from `MONTHLY RENT (EX-VAT)` to
        `MONTHLY RENT EXCL. VAT`.
      `pdf.loyer.subtext` — FR changes from `sur {0} mois` to `sur {0} mois, hors taxes`. EN changes
        from `over {0} months` to `over {0} months, excl. taxes`.
      `pdf.loyer.on.demand` — unchanged (`Sur demande` / `On request`). D-09 keeps it.
      `pdf.project.placeholder` — unchanged. Still the fallback when `projectDesc` is empty.

    Add nothing else. Do not delete anything in this task — deletion is Task 2.
  </action>
  <acceptance_criteria>
    - `grep -c "'pdf.card.client.title'" src/lib/i18n/dictionaries.ts` returns 2 (one FR, one EN).
      The same count-of-2 holds for `pdf.card.contact.advisorName`, `pdf.table.total`,
      `pdf.acceptance.stamp`, `pdf.footer.legal.line2`, `pdf.header.proposition.eyebrow`.
    - `grep -c "SOCIÉTÉ CLIENTE" src/lib/i18n/dictionaries.ts` returns 1 and
      `grep -c "CLIENT COMPANY" src/lib/i18n/dictionaries.ts` returns 1.
    - `grep -c "Total des loyers HT" src/lib/i18n/dictionaries.ts` returns 1 and
      `grep -c "Total rents excl. VAT" src/lib/i18n/dictionaries.ts` returns 1.
    - `! grep -q "Leas\\\\u00e9tic" src/lib/i18n/dictionaries.ts` — the stale accented brand spelling
      is gone from the whole file.
    - `! grep -q "(30 jours)" src/lib/i18n/dictionaries.ts` and `! grep -q "(30 days)" src/lib/i18n/dictionaries.ts`
      — DOC-06: the hardcoded validity period is not copied from the design.
    - `grep -c "{1} jours" src/lib/i18n/dictionaries.ts` returns 1 and `grep -c "{1} days" src/lib/i18n/dictionaries.ts`
      returns 1.
    - `grep -q "Equipment lease financing proposal" src/lib/i18n/dictionaries.ts`.
    - `npm test -- src/lib/i18n/dictionaries.test.ts` passes: FR↔EN parity holds and every existing
      assertion still resolves.
    - `npm run typecheck` exits 0 (the compile-time `_EnHasAllFrKeys` guard in `dictionaries.ts`
      catches any unpaired key).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test -- src/lib/i18n/dictionaries.test.ts</automated>
  </verify>
  <done>All new pairs exist in both language blocks, the four reworded keys carry the design's values, the EN brand spelling is corrected, DOC-06's `{1}` interpolation replaces the design's hardcoded 30, and parity + typecheck are green.</done>
</task>

<task type="auto">
  <name>Task 2: Dispose of the orphaned keys and update the pinning tests in lockstep (D-02 / D-05 / D-06)</name>
  <files>src/lib/i18n/dictionaries.ts, src/lib/i18n/dictionaries.test.ts</files>
  <read_first>
    - `src/lib/i18n/dictionaries.ts` §472-491 and §1787-1806 — the exact keys being removed and kept.
    - `src/lib/i18n/dictionaries.test.ts` §188-202 — the `phase8Keys` list with its `// 7.8 PDF
      document copy (13)` count comment, and §620-625 — the `pdf.partnerType.*` pinning test.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — D-02 (the partnerType keys lose their PDF
      consumer), D-05 (the interests block is dropped), D-06 (tagline and PROJET label are gone), and
      the Deferred Ideas entry "Translating `partnerType` on the admin and partner web surfaces".
    - `.planning/ROADMAP.md` — the disposition plan 43-01 recorded in the Phase 43 planning note.
  </read_first>
  <action>
    Delete these five keys from BOTH language blocks. Each has lost its only consumer to the redesign
    and has no candidate future consumer:
      `pdf.tagline` (D-06 — the design replaced the whole header region with the lockup)
      `pdf.section.project` (D-06 — the `PROJET` section label is gone)
      `pdf.section.interests` (D-05 — the SLB / évaluation-de-parc block is dropped from the PDF)
      `pdf.ref.label` (the bare `N°` / `No.` prefix is replaced by `pdf.header.proposition.eyebrow`)
      `pdf.footer.left` (the `N° {0} · Créée le {1}` footer line is replaced by the legal footer)

    Do NOT delete `proposal.interests.slb` or `proposal.interests.eval` — the wizard still renders
    them; only the PDF stops printing them. Do NOT delete `pdf.computed.coefficient.label`,
    `pdf.project.placeholder`, `pdf.project.ref.prefix`, `pdf.loyer.on.demand` or
    `pdf.validity.caption` — all four keep a consumer in the new layout.
    (`pdf.project.ref.prefix` is superseded by `pdf.pill.partnerRef`; if plan 43-05 finds it truly
    unreferenced, its deletion belongs there, not here.)

    KEEP the three `pdf.partnerType.*` pairs in both blocks. Replace the existing Phase 42 comment
    above each block with one that names `D-02` as the reason they are now unused: under Phase 43's
    card layout the partner block gains no `Fonction` row (`Quote-FR-A.dc.html:72-74` is
    `Partenaire` / `Commercial` / `Téléphone` only), so the PDF no longer consumes them; they are
    retained because CONTEXT.md's Deferred Ideas lists translating `partnerType` on the admin and
    partner web surfaces as a live later decision.

    In `dictionaries.test.ts`: remove `'pdf.tagline'`, `'pdf.section.project'`,
    `'pdf.section.interests'`, `'pdf.ref.label'` and `'pdf.footer.left'` from the `phase8Keys` array
    and update its `// 7.8 PDF document copy (13)` comment to the new count. Remove `'pdf.footer.left'`
    from the `twoArgKeys` interpolation list (`pdf.validity.caption` stays — it still carries `{0}`
    and `{1}`). Leave `'pdf.loyer.subtext'` in `singleArgKeys`.

    Add a `D-02` annotation line to the `pdf.partnerType.*` pinning test's `it(...)` description or
    as a leading comment, stating the keys are deliberately retained without a PDF consumer. The five
    assertions inside it stay unchanged and green.

    Add one new `describe('Phase 43 — Claude Design layout copy (DOC-01..DOC-08, DOC-10)')` block with
    exact-value assertions for the strings a silent reword would break: the two card titles in both
    languages, `pdf.table.total`, `pdf.acceptance.stamp`, `pdf.footer.legal.line2`, and — as the
    DOC-06 gate — an assertion that both `pdf.validity.caption` values contain the literal `{1}` and
    contain neither `30 jours` nor `30 days`. Add a brand-spelling assertion that neither language's
    `pdf.validity.caption` matches `/Leas[ée]tic/u` with an accent, mirroring the existing
    `admin.advisor.hero.title` spelling test at the end of the file.

    Raise the `toBeGreaterThanOrEqual(1011)` key-count floor to the new post-delta count and update
    its `it(...)` description to name Phase 43's PDF-layout namespace, following the existing pattern
    of that assertion's history.
  </action>
  <acceptance_criteria>
    - `! grep -q "'pdf.tagline'" src/lib/i18n/dictionaries.ts` and the same for `'pdf.section.project'`,
      `'pdf.section.interests'`, `'pdf.ref.label'`, `'pdf.footer.left'`.
    - `! grep -rq "pdf.tagline\|pdf.section.project\|pdf.section.interests\|pdf.ref.label\|pdf.footer.left" src/ app/ __pdf-fixtures__/ tests/`
      — no consumer anywhere references a deleted key. (`document.tsx` still references some of them
      at this point in the wave order; if this criterion fails on `document.tsx` alone, that is the
      expected wave-3 handoff — record it and confirm the failure is confined to `src/lib/pdf/document.tsx`.)
    - `grep -c "'pdf.partnerType.Agent'" src/lib/i18n/dictionaries.ts` returns 2 — the pairs survive.
    - `grep -c "D-02" src/lib/i18n/dictionaries.ts` returns at least 2 — both blocks carry the
      annotation naming the deciding decision.
    - `npm test -- src/lib/i18n/dictionaries.test.ts` exits 0, including the new Phase 43 describe block.
    - `npm run lint:check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/i18n/dictionaries.test.ts && npm run lint:check</automated>
  </verify>
  <done>The five layout orphans are deleted from both blocks and from `phase8Keys`; the three `pdf.partnerType.*` pairs are retained with a D-02 annotation and a still-green pinning test; a new Phase 43 describe block pins the design copy and DOC-06's `{1}` interpolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design file copy → dictionary → rendered PDF | a mistranscribed legal string ships to a client in a document Leasetic signs |
| dictionary deletion → runtime `t()` lookup | a deleted key with a surviving consumer degrades silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Tampering | `pdf.validity.caption` legal paragraph | mitigate | Values transcribed verbatim from both `.dc.html` files; exact-value assertions added in Task 2 pin the SIREN/SIRET/VAT footer line and the `{1}` interpolation |
| T-43-03-02 | Repudiation | DOC-06 hardcoded validity period | mitigate | Grep gates assert `(30 jours)` and `(30 days)` are absent and `{1} jours` / `{1} days` are present |
| T-43-03-03 | Spoofing | stale accented `Leasétic` brand spelling in client-facing EN copy | mitigate | The existing EN value is corrected in Task 1; a regex assertion in Task 2 blocks reintroduction |
| T-43-03-04 | Denial of service | deleted key with a live consumer → `t()` returns the raw key or throws | mitigate | Cross-tree grep gate in Task 2; the compile-time `_EnHasAllFrKeys` guard plus `npm run typecheck` catch unpaired additions |
| T-43-03-05 | Information disclosure | commission-adjacent copy entering the PDF dictionary | mitigate | No new key names or values contain `commission`, `_pct` or a rate. Gate: `! grep -in "commission" <the new pdf.* block>` |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No install. Gate: `git diff --stat package.json package-lock.json` empty at plan close |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (the `_EnHasAllFrKeys` compile guard proves FR↔EN parity)
- `npm test -- src/lib/i18n/dictionaries.test.ts` exits 0
- `npm run lint:check` exits 0
- `__pdf-fixtures__/render-fixtures.test.ts` stays red from plan 43-01 — expected, closed by 43-07.
  Re-valuing `pdf.title` / `pdf.loyer.label` / `pdf.loyer.subtext` / `pdf.validity.caption` adds
  further drift; that is intended.
</verification>

<success_criteria>
- Every label, table row, acceptance label and legal line in both design files exists as an FR/EN pair
- The conditions paragraph interpolates `{1}` for the proposal's real validity period (DOC-06)
- The EN paragraph spells the brand `Leasetic`
- The five layout orphans are deleted and `phase8Keys` is pruned to match
- The three `pdf.partnerType.*` pairs are kept, annotated with D-02, and their test is still green
- A Phase 43 describe block pins the design copy against silent rewording
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-03-SUMMARY.md` when done.
</output>
