---
phase: 43-new-pdf-layout
plan: 09
type: execute
wave: 7
depends_on: [43-08]
files_modified:
  - src/lib/pdf/document.tsx
  - src/lib/pdf/layout.test.ts
autonomous: true
gap_closure: true
requirements: [DOC-01]
must_haves:
  truths:
    - "The rendered FR title reads 'Proposition de location financière' with no wrap-hyphen inside any word"
    - "The rendered EN title reads 'Equipment lease financing proposal' with no wrap-hyphen inside any word"
    - "The test suite is capable of FAILING when the hyphenation defect is present — proven by a deliberate revert"
  artifacts:
    - path: "src/lib/pdf/document.tsx"
      provides: "Font.registerHyphenationCallback disabling @react-pdf/renderer's default mid-word hyphenator"
      contains: "registerHyphenationCallback"
    - path: "src/lib/pdf/layout.test.ts"
      provides: "DOC-01 positive guard asserting no wrap-hyphen break in the title, replacing the dehyphenate() normaliser"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "@react-pdf/renderer Font"
      via: "Font.registerHyphenationCallback at module load, beside Font.register"
      pattern: "Font\\.registerHyphenationCallback"
    - from: "src/lib/pdf/layout.test.ts"
      to: "rendered PDF text"
      via: "reconstructVisibleTextFontAware returning UN-normalised text"
      pattern: "not\\.toContain\\('finan- cière'\\)"
---

<objective>
Close Gap 1 of `43-VERIFICATION.md`: the proposal title breaks mid-word in both languages
("Proposition de location finan-cière", "Equipment lease financing pro-posal") because
`Font.registerHyphenationCallback` is registered nowhere under `src/lib/pdf/`, so
`@react-pdf/renderer`'s default hyphenator is active. The reference PNGs break cleanly at a
space with no hyphen.

Purpose: DOC-01 ("matches the design spec") is BLOCKED on this. It is also a test-integrity
defect: `dehyphenate()` at `src/lib/pdf/layout.test.ts:130` normalises the break away *before*
any DOC-10 phrase assertion runs, so the green suite has never once proven the title renders
correctly. Two prior automated passes (43-05, 43-07) absorbed this defect rather than fixing it.
This plan fixes the render AND converts the normaliser into a guard that can fail.

Output: one `Font.registerHyphenationCallback` call in `document.tsx`, `dehyphenate()` deleted
from `layout.test.ts`, and a new DOC-01 describe block with explicit negative assertions —
falsified by a deliberate revert before the plan closes.

**Byte-determinism sequencing — read this before touching anything.** This change moves rendered
bytes. `__pdf-fixtures__/render-fixtures.test.ts` and `__pdf-fixtures__/commission-free-fixture.test.ts`
WILL GO RED after Task 1 and are EXPECTED to stay red until plan 43-12. Do **NOT** run
`npm run pdf:update-fixture` in this plan. Regeneration happens exactly once, in 43-12, after all
byte-changing work (43-09, 43-10, 43-11, 43-12) has landed. A mid-phase regeneration followed by
more byte changes leaves a stale baseline and destroys the D-16 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-08-SUMMARY.md
@.planning/phases/43-new-pdf-layout/43-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the hyphenation callback that disables mid-word breaking</name>
  <files>src/lib/pdf/document.tsx</files>
  <read_first>
    - src/lib/pdf/document.tsx (lines 1-60 — the import line and the whole Font.register block, so the callback lands beside it and inherits the existing comment discipline)
    - .planning/phases/43-new-pdf-layout/43-08-SUMMARY.md ("Defect 1 — Title row hyphenation break", which names the exact fix and fix location)
  </read_first>
  <action>
    Immediately after the closing `});` of the existing `Font.register({ family: 'Inter', ... })`
    call (currently ending around line 48), add `Font.registerHyphenationCallback((word) => [word]);`.
    `Font` is already imported on line 1 from `@react-pdf/renderer` — add no new import.

    Returning a single-element array containing the whole word tells react-pdf that no word has
    any legal break point, so the layout engine may only break at spaces. This is the exact fix
    diagnosed in 43-08-SUMMARY.md Defect 1.

    Above the call, add a comment block in the style of the surrounding Font.register comments
    stating: (a) DOC-01 / Gap 1 of 43-VERIFICATION.md is what this closes; (b) the default
    @react-pdf/renderer hyphenator broke "financière" into "finan-cière" (FR) and "proposal" into
    "pro-posal" (EN), which the reference PNGs do not do; (c) this is module-load global state for
    the renderer, deliberately registered beside Font.register so the two never drift apart;
    (d) it is deterministic — a pure function of the word, no I/O, no state.

    Do NOT touch any other part of document.tsx in this task. Do NOT run pdf:update-fixture.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; grep -c "Font.registerHyphenationCallback" src/lib/pdf/document.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/lib/pdf/document.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Font.registerHyphenationCallback" src/lib/pdf/document.tsx` outputs `1`
    - `grep -n "registerHyphenationCallback" src/lib/pdf/document.tsx` reports a line number greater than the line number reported by `grep -n "family: 'Inter'" src/lib/pdf/document.tsx` (the callback sits after the register call, not before it)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/lib/pdf/document.test.tsx` passes (all cases, including the D-13 null-advisor and FIELD-03 legacy cases)
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0 — the baseline is untouched by this plan
  </acceptance_criteria>
  <done>`src/lib/pdf/document.tsx` registers exactly one hyphenation callback, placed after `Font.register`, and the document still renders.</done>
</task>

<task type="auto">
  <name>Task 2: Convert dehyphenate() from a normaliser into a DOC-01 guard, and falsify it</name>
  <files>src/lib/pdf/layout.test.ts</files>
  <read_first>
    - src/lib/pdf/layout.test.ts (lines 1-45 — the file header comment that documents dehyphenate() as "already accepted" behaviour; lines 126-182 — dehyphenate(), reconstructVisibleTextFontAware() and its return statement; lines 285-380 — the DOC-10 required/forbidden phrase lists that consume the reconstructed text)
    - .planning/phases/43-new-pdf-layout/43-VERIFICATION.md (Gap 1's `missing[]` — the second bullet is this task verbatim)
  </read_first>
  <action>
    Three edits, all in `src/lib/pdf/layout.test.ts`:

    1. **Delete `dehyphenate()`** (the function at ~line 131 and its JSDoc at ~line 130) and change
       the return statement of `reconstructVisibleTextFontAware` at ~line 178 from
       `return normalizeWhitespace(dehyphenate(out.join('')));` to
       `return normalizeWhitespace(out.join(''));`. Also delete the now-false inline comment at
       ~line 172 that says "dehyphenate() below undoes this exact separator". Leave the rest of
       that comment (explaining the run separator) intact. After this edit the reconstructed text
       preserves wrap-hyphen breaks verbatim, so the existing DOC-10 assertions
       `toContain('Proposition de location financière')` and
       `toContain('Equipment lease financing proposal')` become real guards rather than
       assertions on a laundered string.

    2. **Rewrite the file header comment** (the paragraph at lines ~27-40 beginning "A second,
       related finding while validating this decoder"). It currently records the hyphenation break
       as known and accepted per 43-05. Replace it with: the break was a real defect, caught by the
       D-15 human pass in 43-08 after two automated passes absorbed it; it is fixed by
       `Font.registerHyphenationCallback` in `document.tsx` (plan 43-09 Task 1); `dehyphenate()` was
       removed rather than left as dead code because a normaliser that silently stops proving
       anything is worse than no helper. Name the pattern explicitly: an automated gate normalising
       a rendering artifact away is not the same as that artifact being correct.

    3. **Add a new top-level describe block** placed immediately before the existing
       `describe('DOC-10: language parity...')` block:
       `describe('DOC-01: the title never wrap-hyphenates (Gap 1, 43-VERIFICATION.md)', ...)` with
       two `it` cases, one per language, each rendering its fixture via `renderProposalPdf`,
       reconstructing the text, and asserting BOTH directions:
       - FR: `expect(text).toContain('Proposition de location financière')` and
         `expect(text).not.toContain('finan- cière')` and
         `expect(text).not.toMatch(/finan-\s/)`
       - EN: `expect(text).toContain('Equipment lease financing proposal')` and
         `expect(text).not.toContain('pro- posal')` and
         `expect(text).not.toMatch(/pro-\s/)`
       Give every assertion a message string naming DOC-01 and the defect, so a future failure
       reads as "the hyphenator is back", not as a mystery.
       Rationale for the exact literals: `reconstructVisibleTextFontAware` pushes a single space
       between consecutive text-show runs, so a wrap break renders as `finan- cière` / `pro- posal`
       in the reconstructed string.

    4. **Falsify the guard before closing.** Temporarily comment out the
       `Font.registerHyphenationCallback` line added in Task 1, run
       `npx vitest run src/lib/pdf/layout.test.ts`, and confirm the new DOC-01 cases FAIL (and
       ideally that the DOC-10 title phrases fail too). Then uncomment and re-run to confirm GREEN.
       Record both outputs verbatim in the plan SUMMARY under a "RED→GREEN falsification" heading.
       This step is mandatory: Gap 1 exists precisely because two prior passes shipped a suite that
       could not fail on this defect.

    Do NOT edit `src/lib/pdf/document.tsx` in this task except for the temporary comment-out in
    step 4, which must be reverted. Do NOT run `npm run pdf:update-fixture`.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ! grep -q "dehyphenate(" src/lib/pdf/layout.test.ts &amp;&amp; grep -q "DOC-01: the title never wrap-hyphenates" src/lib/pdf/layout.test.ts &amp;&amp; npx vitest run src/lib/pdf/layout.test.ts src/lib/pdf/document.test.tsx &amp;&amp; npm run lint:check</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "dehyphenate(" src/lib/pdf/layout.test.ts` succeeds — the function definition and every call site are gone. The rewritten header comment MAY still name the removed helper when recording why it was removed; what must not survive is any callable form.
    - `grep -c "DOC-01: the title never wrap-hyphenates" src/lib/pdf/layout.test.ts` outputs `1`
    - `grep -c "not.toContain('finan- cière')" src/lib/pdf/layout.test.ts` outputs `1` and `grep -c "not.toContain('pro- posal')" src/lib/pdf/layout.test.ts` outputs `1`
    - `npx vitest run src/lib/pdf/layout.test.ts` passes with the callback in place
    - The SUMMARY contains a "RED→GREEN falsification" section quoting the failing vitest output produced with the callback commented out, naming at least one failing test by title
    - `npm run lint:check` exits 0
    - `npx vitest run src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` passes (ADMIN-09 gates untouched)
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0
  </acceptance_criteria>
  <done>The suite proves the title renders without a wrap-hyphen in both languages, and has been demonstrated to fail when the fix is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render path → client-facing PDF | The PDF is delivered to the end client. Anything rendered crosses out of Leasetic's trust domain permanently. |
| module-load global state → every render | `Font.registerHyphenationCallback` is process-global for @react-pdf/renderer. A wrong callback silently changes every proposal ever rendered by that process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09-01 | Information disclosure | rendered PDF text | mitigate | The callback changes line-breaking only; no new data reaches the document. Task 2 re-runs `no-commission.test.ts` and `admin-09-grep-contracts.test.ts` as an acceptance criterion so ADMIN-09 invisibility is re-proven after the byte change. |
| T-43-09-02 | Tampering | `src/lib/pdf/layout.test.ts` | mitigate | The gap itself is a weakened assertion that let a defect ship. The falsification step (Task 2 step 4) is the mitigation: a guard that has not been observed failing is not a guard. |
| T-43-09-03 | Denial of service | `Font.registerHyphenationCallback` | accept | The callback is a pure, allocation-bounded `(word) => [word]`. No recursion, no I/O, no unbounded growth; a single-element array per word is strictly cheaper than the default hyphenator. |
| T-43-09-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. `Font` comes from the already-vendored `@react-pdf/renderer`. If any install becomes necessary, stop and escalate — the Package Legitimacy Gate has not been run for this gap closure. |
</threat_model>

<verification>
- `npx vitest run src/lib/pdf/layout.test.ts src/lib/pdf/document.test.tsx src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` — all green.
- `npm run lint:check` exits 0.
- `npx tsc --noEmit` exits 0.
- `__pdf-fixtures__/render-fixtures.test.ts` and `commission-free-fixture.test.ts` are EXPECTED RED. Record their failure output in the SUMMARY and state that plan 43-12 regenerates the baseline. Do not "fix" them here.
</verification>

<success_criteria>
- `Font.registerHyphenationCallback((word) => [word])` present exactly once in `src/lib/pdf/document.tsx`, after `Font.register`.
- `dehyphenate` no longer exists anywhere in `src/lib/pdf/layout.test.ts`.
- A DOC-01 describe block asserts, per language, both the clean title phrase and the absence of the wrap-hyphen artifact.
- The RED→GREEN falsification is recorded verbatim in the SUMMARY.
- `expected.sha256.txt` is byte-unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-09-SUMMARY.md` when done.
</output>
