---
phase: 43-new-pdf-layout
plan: 05
type: execute
wave: 3
depends_on: [43-01, 43-02, 43-03, 43-04]
files_modified:
  - src/lib/pdf/document.tsx
  - src/lib/pdf/components/eyebrow.tsx
  - src/lib/pdf/components/card-key-value-row.tsx
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-10, DOC-11]
must_haves:
  truths:
    - "The page frame uses the design's 14/15/10mm padding, 9.5pt base size and 1.45 line height"
    - "The header band is the real Leasetic lockup left, PROPOSITION N° / reference / Établie le right, over a 2px navy rule"
    - "The title row is a 21pt two-line title with the project description beneath and two pills right-aligned"
    - "A SOCIÉTÉ CLIENTE card and a VOTRE CONTACT card sit side by side, each one 11pt headline over six key/value rows"
    - "The VOTRE CONTACT card headlines the partner company, with the Leasetic advisor beneath as the supporting contact"
    - "Every absent card value renders an em dash, so both cards keep identical geometry across proposals"
    - "The tagline, the PROJET section label and the LEASETIC text node no longer appear anywhere in the render tree"
  artifacts:
    - path: "src/lib/pdf/components/eyebrow.tsx"
      provides: "Eyebrow — the 7.5pt uppercase section label used by every block in the new layout"
      exports: ["Eyebrow"]
    - path: "src/lib/pdf/components/card-key-value-row.tsx"
      provides: "CardKeyValueRow — the 8.5pt label/value row of the design's two-column card grid"
      exports: ["CardKeyValueRow"]
    - path: "src/lib/pdf/document.tsx"
      provides: "header band, 2px rule, title + pills, and the two-card grid"
      contains: "LeaseticLockup"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "src/lib/pdf/components/leasetic-lockup.tsx"
      via: "LeaseticLockup at height 19.5pt in the header band"
      pattern: "<LeaseticLockup"
    - from: "src/lib/pdf/document.tsx"
      to: "src/lib/pdf/em-dash.ts"
      via: "emDash() at every optional card value"
      pattern: "emDash\\("
---

<objective>
Rebuild the top half of the document: page frame, header lockup band, the 2px navy rule, the 21pt
title row with its pills, and the two side-by-side cards.

Purpose: DOC-01, DOC-02 and DOC-03. This is where D-06's deletions (tagline, `PROJET` label) and
D-07's lockup land, where D-04's partner-first / advisor-second card restructure is honoured rather
than "fixed" back to the design's ordering, and where DOC-11's em dash becomes a geometry guarantee
rather than a per-field ternary.

Output: `document.tsx`'s render tree from `<Page>` down to the end of the card grid, plus the two new
style primitives the rest of the layout also uses.

**Everything below the card grid is left untouched by this plan.** The old computation card, loyer
card, interests block, validity caption and footer keep rendering beneath the new cards until plan
43-06 replaces them. The intermediate document is ugly and correct: it compiles, renders and keeps
`SectionLabel` / `KeyValueRow` alive so `eslint --max-warnings=0` stays green.

**The design files are authoritative (D-01).** Where a value below and a design file disagree, the
design file wins and the discrepancy goes in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-PATTERNS.md
@.planning/phases/43-new-pdf-layout/43-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Eyebrow and CardKeyValueRow primitives</name>
  <files>src/lib/pdf/components/eyebrow.tsx, src/lib/pdf/components/card-key-value-row.tsx</files>
  <read_first>
    - `src/lib/pdf/components/section-label.tsx` and `src/lib/pdf/components/key-value-row.tsx` — the
      one-component-per-file shape: named props interface, named export function, inline style object
      reading from `../styles`. Both are superseded but not deleted here (43-06 removes them).
    - `src/lib/pdf/styles.ts` — the ten-step scale, the palette, and the module comment recording the
      px→pt (0.75) and em→pt letterSpacing conversions plan 43-01 wrote.
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` §54-66 — the eyebrow and card-grid
      inline styles these two primitives encode.
  </read_first>
  <action>
    Create `src/lib/pdf/components/eyebrow.tsx` exporting `Eyebrow` with props
    `{ children: string; marginBottom?: number }`. It renders a single `<Text>` at
    `fontSize: pdfFontSizes.eyebrow` (7.5), `color: pdfColors.labelTeal` (`#3a6a75`),
    `letterSpacing: 0.45` (the design's `.06em` at 7.5pt, converted to points), default
    `fontWeight: pdfFontWeights.regular`, and `marginBottom` defaulting to `6` (the design's 8px).
    Do NOT set `textTransform` — the dictionary strings plan 43-03 added are already stored
    uppercased, which keeps the FR `N°` and accented capitals exact and keeps the output deterministic.

    Create `src/lib/pdf/components/card-key-value-row.tsx` exporting `CardKeyValueRow` with props
    `{ label: string; value: string }`. It renders a `<View style={{ flexDirection: 'row',
    marginBottom: 3 }}>` (the design's 4px row gap) containing:
      - a label `<Text style={{ width: 54, fontSize: pdfFontSizes.cardKv, color: pdfColors.labelTeal }}>`
      - a value `<Text style={{ flex: 1, fontSize: pdfFontSizes.cardKv, color: pdfColors.bodyBlue }}>`
    The design uses `grid-template-columns:auto 1fr` with a 10px column gap; react-pdf has no grid, so
    the fixed 54pt label column plus `flex: 1` is the port. 54pt is sized for the widest label in
    either language (`Destinataire` at 8.5pt) and leaves the design's ~7.5pt gutter. Comment that
    derivation. If the visual pass in plan 43-08 reports a wrapped label, this width is the one number
    to adjust.

    Both files: no `Date.now()`, no `Math.random()`, colours only via `pdfColors`.
  </action>
  <acceptance_criteria>
    - `grep -q "letterSpacing: 0.45" src/lib/pdf/components/eyebrow.tsx`.
    - `! grep -q "textTransform" src/lib/pdf/components/eyebrow.tsx`.
    - `grep -q "width: 54" src/lib/pdf/components/card-key-value-row.tsx`.
    - `grep -q "pdfColors.labelTeal" src/lib/pdf/components/eyebrow.tsx` and
      `grep -q "pdfColors.bodyBlue" src/lib/pdf/components/card-key-value-row.tsx`.
    - `npm run typecheck` exits 0 and `npm run lint:check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check</automated>
  </verify>
  <done>Both primitives exist as named exports following the directory's one-component-per-file shape and read every value from `pdfColors` / `pdfFontSizes`.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the page frame, header band, 2px rule and title row (DOC-01, D-06, D-07)</name>
  <files>src/lib/pdf/document.tsx</files>
  <read_first>
    - `src/lib/pdf/document.tsx` — the whole file. Specifically §100-110 (the `<Document>` metadata
      block, untouched), §111-119 (the `<Page>` style object), §120-160 (the header band and the
      1px rule being replaced), §162-168 (the old title `Text`), §170-190 (the old project block).
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` §28-50 and
      `.planning/assets/v1.9-quote-design/Quote-EN-A.dc.html` §28-50 — the page frame, header,
      rule and title-row inline styles, and the EN label variants.
    - `.planning/assets/v1.9-quote-design/reference/Quote-FR-A.reference.png` — D-14's visual
      acceptance target. The `.dc.html` alone does not show what the layout resolves to.
    - `src/lib/pdf/components/leasetic-lockup.tsx` — `LeaseticLockup`'s `{ height }` prop.
    - `src/lib/pdf/styles.ts` — `pdfPageMargins`, `pdfPageBase`, the scale and the palette.
    - `.planning/phases/43-new-pdf-layout/43-02-SUMMARY.md` — the D-07 verdict and the confirmed
      rendered extents of the marks.
  </read_first>
  <action>
    **Page frame.** Replace the `<Page size="A4" style={{...}}>` style object with:
    `paddingTop: pdfPageMargins.top`, `paddingBottom: pdfPageMargins.bottom`,
    `paddingHorizontal: pdfPageMargins.horizontal`, `fontFamily: 'Inter'`,
    `fontSize: pdfPageBase.fontSize` (9.5), `lineHeight: pdfPageBase.lineHeight` (1.45),
    `color: pdfColors.navy`, `backgroundColor: pdfColors.surface`, `flexDirection: 'column'`.
    The explicit `flexDirection: 'column'` matters — plan 43-06 pins the acceptance block with
    `marginTop: 'auto'` inside this column, which is the design's own mechanism.

    **Header band.** Delete the `LEASETIC` `<Text>` node (D-07) and the `pdf.tagline` `<Text>` node
    (D-06) entirely. Replace the whole header `<View>` with a row at
    `flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start'`:
      - Left: `<LeaseticLockup height={19.5} />` — the design's `height:26px` at 0.75pt/px.
      - Right: a column at `alignItems: 'flex-end'`, three `<Text>` nodes with 1.5pt vertical spacing
        (the design's 2px gap):
        1. `t('pdf.header.proposition.eyebrow', lang)` at `pdfFontSizes.eyebrow` (7.5),
           `letterSpacing: 0.45`, `color: pdfColors.labelTeal`.
        2. `lcRef` at `pdfFontSizes.propositionNo` (13), `fontWeight: pdfFontWeights.semibold`,
           `letterSpacing: -0.13`, `color: pdfColors.navy`.
        3. `t('pdf.header.issued', lang).replace('{0}', formatDate(createdAt, lang))` at
           `pdfFontSizes.pill` (8), `color: pdfColors.labelTeal`.

    **The rule.** Replace the existing 1px `pdfColors.border` divider with `height: 1.5`
    (the design's 2px), `backgroundColor: pdfColors.navy`, `marginTop: 7.5`, `marginBottom: 10.5`
    (the design's `margin:10px 0 14px`).

    **Title row.** Delete the old standalone title `<Text>` and the entire old project block
    (its `SectionLabel` for `pdf.section.project`, the description `<Text>`, and the conditional
    `partnerRefText` `<Text>`). Replace with a row at `flexDirection: 'row',
    justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 10.5`:
      - Left column (`flex: 1`, so long titles do not push the pills off the page):
        - `t('pdf.title', lang)` at `pdfFontSizes.h1` (21), `fontWeight: pdfFontWeights.semibold`,
          `letterSpacing: -0.525`, `lineHeight: 1.1`, `color: pdfColors.navy`, `marginBottom: 3`.
        - the project description at `pdfFontSizes.projectDesc` (10), `color: pdfColors.bodyBlue`.
          Keep today's `projectText` derivation exactly: `inputs.projectDesc?.trim() ||
          t('pdf.project.placeholder', lang)`.
      - Right column at `flexDirection: 'row'`, 4.5pt gap (the design's 6px),
        `justifyContent: 'flex-end'`, containing two pills. A pill is a `<View>` with
        `borderWidth: 0.75`, `borderColor: pdfColors.hairline`, `borderRadius: 999`,
        `paddingVertical: 3`, `paddingHorizontal: 8.25` wrapping a `<Text>` at `pdfFontSizes.pill`
        (8) in `pdfColors.bodyBlue`:
        1. `t('pdf.pill.partnerRef', lang).replace('{0}', emDash(inputs.partnerRef))`
        2. `t('pdf.pill.term', lang).replace('{0}', String(inputs.durationMonths))`
      The partner-reference pill renders **unconditionally**. Today's code hides it when `partnerRef`
      is empty; DOC-11 and D-01 replace that with an em dash so the row keeps its geometry. Remove
      the `partnerRefText` variable and its `null` branch entirely.

    If `pdf.project.ref.prefix` now has no consumer anywhere in the tree, delete the pair from both
    language blocks of `src/lib/i18n/dictionaries.ts` and from `phase8Keys` in
    `src/lib/i18n/dictionaries.test.ts`, following the disposition plan 43-03 executed for the other
    five orphans. Verify with a repo-wide grep before deleting.

    Leave everything below the title row untouched in this task.
  </action>
  <acceptance_criteria>
    - `grep -q "<LeaseticLockup" src/lib/pdf/document.tsx` and `grep -q "height={19.5}" src/lib/pdf/document.tsx`.
    - `! grep -q ">LEASETIC<" src/lib/pdf/document.tsx` and `! grep -q "pdf.tagline" src/lib/pdf/document.tsx`
      and `! grep -q "pdf.section.project" src/lib/pdf/document.tsx` — D-06 and D-07 deletions are complete.
    - `! grep -q "partnerRefText" src/lib/pdf/document.tsx` — the conditional pill is gone.
    - `grep -q "pdf.header.proposition.eyebrow" src/lib/pdf/document.tsx` and
      `grep -q "pdf.pill.partnerRef" src/lib/pdf/document.tsx` and `grep -q "pdf.pill.term" src/lib/pdf/document.tsx`.
    - `grep -q "letterSpacing: -0.525" src/lib/pdf/document.tsx` (the 21pt h1) and
      `grep -q "letterSpacing: -0.13" src/lib/pdf/document.tsx` (the 13pt reference).
    - `grep -q "pdfPageBase" src/lib/pdf/document.tsx` and `grep -q "flexDirection: 'column'" src/lib/pdf/document.tsx`.
    - `npm run typecheck` exits 0; `npm run lint:check` exits 0.
    - `npm test -- src/lib/pdf/document.test.tsx` exits 0 — all six cases (happy path, sha256 shape,
      magic bytes, on-demand, English, plus 43-04's null-advisor and legacy-proposal cases) still render.
    - Render a real FR proposal via the fixture path and extract its visible text with the
      `reconstructVisibleText` approach from `src/lib/pdf/no-commission.test.ts` §372: the inventory
      contains `PROPOSITION N°`, `LC-12345`, `Proposition de location financière` and `Réf. partenaire`.
      The EN fixture's inventory contains `PROPOSAL NO.`, `Equipment lease financing proposal` and
      `Partner ref.`. Neither inventory contains `Location financière IT` (the deleted tagline) or
      `PROJET`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm test -- src/lib/pdf/document.test.tsx</automated>
  </verify>
  <done>The page frame carries the design's margins and base type; the header is the real lockup over a 2px navy rule with the eyebrow/reference/issued stack; the title row is 21pt with an unconditional two-pill group; the tagline, PROJET label and LEASETIC text node are gone from the tree and from the rendered text inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Build the SOCIÉTÉ CLIENTE and VOTRE CONTACT cards (DOC-02, DOC-03, DOC-11, D-04)</name>
  <files>src/lib/pdf/document.tsx</files>
  <read_first>
    - `src/lib/pdf/document.tsx` — the tree as Task 2 left it.
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` §52-78 and
      `.planning/assets/v1.9-quote-design/Quote-EN-A.dc.html` §52-78 — the two cards' inline styles
      and both label sets.
    - `.planning/assets/v1.9-quote-design/reference/Quote-FR-A.reference.png` and
      `Quote-EN-A.reference.png` — D-14's visual target for the card grid.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — **D-02 and D-04 in full.** D-04 is the one
      deliberate deviation from the design in this card and Phase 43 must NOT "fix" it back. D-02
      explains why the partner block gains no `Fonction` row.
    - `src/lib/pdf/em-dash.ts` — `emDash`'s contract.
    - `src/lib/pdf/components/card-key-value-row.tsx` and `eyebrow.tsx` — Task 1's primitives.
  </read_first>
  <action>
    Insert a card grid immediately after the title row: a `<View style={{ flexDirection: 'row',
    marginBottom: 10.5 }}>` holding two cards. Each card is `flexGrow: 1, flexBasis: 0` so they split
    the width evenly (the design's `grid-template-columns:1fr 1fr`); give the left card
    `marginRight: 7.5` for the design's 10px gap rather than relying on `gap`.

    Card chrome, identical for both: `backgroundColor: pdfColors.cardFill` (`#F8FAFF`),
    `borderRadius: 10.5` (the design's 14px), `paddingVertical: 9.75`, `paddingHorizontal: 11.25`
    (the design's `13px 15px`).

    Card body, identical for both: an `<Eyebrow>` with the card title, then an 11pt headline `<Text>`
    at `fontWeight: pdfFontWeights.semibold`, `letterSpacing: -0.11`, `color: pdfColors.navy`,
    `marginBottom: 4.5` (the design's 6px), then six `<CardKeyValueRow>` elements.

    **Left card — SOCIÉTÉ CLIENTE (DOC-02).**
    Eyebrow `t('pdf.card.client.title', lang)`. Headline `inputs.clientCo`. Six rows, in this order,
    every value through `emDash(...)`:
      `pdf.card.client.siren` → `inputs.clientSiren`
      `pdf.card.client.siret` → `inputs.clientSiret`
      `pdf.card.client.recipient` → `inputs.clientName`
      `pdf.card.client.role` → `inputs.clientRole`
      `pdf.card.client.phone` → `inputs.clientTel`
      `pdf.card.client.email` → `inputs.clientEmail`
    `clientSiret` is optional on the props interface (FIELD-03) — a pre-Phase-42 proposal renders an
    em dash here and keeps the card's height.

    **Right card — VOTRE CONTACT (DOC-03, restructured per D-04).**
    Eyebrow `t('pdf.card.contact.title', lang)` — the title stays `VOTRE CONTACT` / `YOUR CONTACT`;
    the blocks are re-ordered, not relabelled. Headline `inputs.partnerCo` — the PARTNER COMPANY, not
    the advisor. The design makes `advisorName` the 11pt headline; Phase 42 D-10 inverted that
    deliberately because the client meets the partner first and the advisor second. **Do not restore
    the design's ordering.** Put a comment naming D-04 directly above the headline so a future reader
    does not read it as drift.
    Six rows, in this order, every value through `emDash(...)`:
      `pdf.card.contact.salesRep` → `inputs.partnerName`
      `pdf.card.contact.partnerPhone` → the partner company's line (see resolution below)
      `pdf.card.contact.advisorName` → `advisor?.name`
      `pdf.card.contact.advisorRole` → `advisor?.fonction`
      `pdf.card.contact.advisorPhone` → `advisor?.telephone`
      `pdf.card.contact.advisorEmail` → `advisor?.email`
    There is NO `Fonction` row for the partner (D-02: `Quote-FR-A.dc.html:72-74` is
    `Partenaire` / `Commercial` / `Téléphone` only) and no `Partenaire` row, because the partner
    company is the headline under D-04.

    **Partner-phone resolution.** Compute it once above the JSX as a local:
    the immutable `inputs.partnerTel` wins when it is a non-empty trimmed string; otherwise fall back
    to the live `partner.companyTelephone`; otherwise `emDash` produces the dash. Comment the ordering:
    `inputs.partnerTel` is what the draft captured at creation and is the authoritative snapshot;
    the live `companyTelephone` is the FIELD-03 fallback for a proposal finalized before Phase 42
    hydrated that field. Both are the partner COMPANY's line — `users.telephone` (the individual's,
    PROF-02's finalization gate) is deliberately not rendered anywhere in this document.

    **The advisor's fonction renders as typed** — free text from `leasetic_advisor.fonction`, no
    translation, no enum. D-03 retracted the FR/EN label-pair clause; plan 43-01 reconciled DOC-03 and
    the ROADMAP note. Do not reach for `pdf.partnerType.*`.

    Leave everything below the card grid untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "<CardKeyValueRow" src/lib/pdf/document.tsx` returns 12 — six rows per card, D-04's
      "one 11pt headline over six key/value rows" holds for both.
    - `grep -q "pdf.card.contact.advisorName" src/lib/pdf/document.tsx` and
      `! grep -q "pdf.partnerType" src/lib/pdf/document.tsx` — the advisor's fonction is not translated.
    - `grep -q "D-04" src/lib/pdf/document.tsx` — the deliberate deviation is documented at the headline.
    - The `VOTRE CONTACT` headline expression references `inputs.partnerCo`, not `advisor`. Verify by
      reading the block; assert `grep -c "inputs.partnerCo" src/lib/pdf/document.tsx` returns at least 1.
    - `grep -c "emDash(" src/lib/pdf/document.tsx` returns at least 12 — every optional card value
      passes through the single formatter, and no inline `? :` absence ternary was hand-written.
      Confirm with `! grep -q "|| '—'" src/lib/pdf/document.tsx`.
    - `npm test -- src/lib/pdf/document.test.tsx` exits 0, including the null-advisor case from 43-04.
    - Rendered-text proof, FR fixture: the extracted visible text contains `SOCIÉTÉ CLIENTE`,
      `VOTRE CONTACT`, `Destinataire`, `Commercial`, `Conseiller` and the fixture's advisor name.
      EN fixture: contains `CLIENT COMPANY`, `YOUR CONTACT`, `Recipient`, `Sales rep`, `Advisor`.
    - Em-dash geometry proof: render the FR fixture twice, once with `advisor: null` and
      `inputs.clientSiret: undefined`, and assert the extracted text contains at least five U+2014
      characters and that the rendered page count is still 1 in both variants.
    - `npm test -- tests/admin-09-grep-contracts.test.ts src/lib/pdf/no-commission.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm test -- src/lib/pdf/ tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <done>Both cards render side by side with identical chrome, one 11pt headline over six key/value rows each; the contact card headlines the partner company per D-04; every optional value routes through `emDash`; both language label sets appear in the rendered text inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposal `inputs` jsonb → rendered card | partner-supplied client data reaches a client-delivered document |
| live `leasetic_advisor` row → rendered card | admin-controlled contact details reach the same document |
| design spec → implementation | a mis-ported card structure ships a document that misrepresents who the client should call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05-01 | Information disclosure | commission leakage into the new header/title/card nodes | mitigate | No node reads `computed.coeff`, a rate or any commission-derived value; `no-commission.test.ts` (4 layers) and the 20 `admin-09-grep-contracts` gates are run as acceptance criteria on this plan |
| T-43-05-02 | Information disclosure | `users.telephone` (the individual's private line) rendered as the partner phone | mitigate | The partner-phone resolution reads only `inputs.partnerTel` and `partner.companyTelephone`; `users.telephone` never reaches `ProposalDocumentProps` at all (plan 43-04's interface) |
| T-43-05-03 | Denial of service | an absent optional field collapsing a row and breaking card geometry | mitigate | Every optional value passes through `emDash`; grep gate forbids a hand-written `|| '—'`; a null-advisor + absent-SIRET render is asserted to stay one page |
| T-43-05-04 | Tampering | `<Svg>` lockup failing silently and shifting the whole header height | mitigate | Plan 43-02 proved the marks emit ≥16 Bézier operators at the correct extents before this plan consumed them; D-15's human pass is the final check |
| T-43-05-05 | Repudiation | a future reader "fixing" D-04's deliberate inversion back to the design | mitigate | A `D-04` comment sits directly above the headline; a grep gate asserts it is present |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No install. Gate: `git diff --stat package.json package-lock.json` empty at plan close |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint:check` exits 0
- `npm test -- src/lib/pdf/ tests/admin-09-grep-contracts.test.ts` exits 0
- **Expected transient failures, do not fix here:**
  - `__pdf-fixtures__/render-fixtures.test.ts` — red since plan 43-01. D-16 requires the single
    regeneration to land with the completed layout. Plan 43-07 closes it. Do NOT run
    `npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE` in this plan.
  - `__pdf-fixtures__/inter-typography.test.ts` may still pass here because the un-rewritten blocks
    below the card grid still use weights 500 and 700. If it goes red early, leave it — plan 43-07
    owns its reconciliation.
</verification>

<success_criteria>
- The page frame uses 14/15/10mm margins, 9.5pt base and 1.45 line height
- The header is the real lockup + `PROPOSITION N°` / reference / `Établie le` over a 2px navy rule
- The 21pt title, project description and two unconditional pills render in one row
- Two cards render side by side, each one 11pt headline over exactly six key/value rows
- The contact card headlines the partner company per D-04 and carries the advisor beneath
- Every optional card value routes through `emDash`; a null-advisor proposal still renders one page
- Tagline, `PROJET` label and the `LEASETIC` text node are absent from source and from rendered text
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-05-SUMMARY.md` when done.
</output>
