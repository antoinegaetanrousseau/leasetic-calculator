---
phase: 43-new-pdf-layout
plan: 10
type: execute
wave: 8
depends_on: [43-09]
files_modified:
  - src/lib/i18n/dictionaries.ts
  - src/lib/pdf/document.tsx
  - src/lib/pdf/layout.test.ts
autonomous: true
gap_closure: true
requirements: [DOC-03]
must_haves:
  truths:
    - "The VOTRE CONTACT card shows the partner as a headline over a Partenaire row and a Téléphone row, then the Leasetic advisor as a second headline over Fonction, Téléphone and Email rows"
    - "The card no longer renders two identically-labelled Téléphone rows under one undifferentiated headline"
    - "No 'Commercial' or 'Conseiller' label appears in the FR render; no 'Sales rep' or 'Advisor' label appears in the EN render"
  artifacts:
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "pdf.card.contact.partner FR/EN pair; pdf.card.contact.salesRep and pdf.card.contact.advisorName deleted"
      contains: "pdf.card.contact.partner"
    - path: "src/lib/pdf/document.tsx"
      provides: "the 7-row VOTRE CONTACT structure (2 headlines + 4 key/value rows) agreed verbatim in 43-08-SUMMARY.md"
    - path: "src/lib/pdf/layout.test.ts"
      provides: "DOC-03 structural ordering guard plus updated DOC-10 / DOC-11 label expectations"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "src/lib/i18n/dictionaries.ts"
      via: "t('pdf.card.contact.partner', lang)"
      pattern: "pdf\\.card\\.contact\\.partner"
    - from: "src/lib/pdf/layout.test.ts"
      to: "rendered VOTRE CONTACT card"
      via: "index-ordering assertion on the reconstructed text"
      pattern: "indexOf\\('Partenaire'\\)"
---

<objective>
Close Gap 2 of `43-VERIFICATION.md`: `src/lib/pdf/document.tsx` lines ~288-316 render the
VOTRE CONTACT card as one undifferentiated 6-row list under a single headline, mixing two
entities (the selling partner and the Leasetic advisor) with two identically-labelled
"Téléphone" rows and no divider. This produced the confusion Antoine reported verbatim in
43-08-SUMMARY.md: "I am confused as to who is the advisor and who is partner in the contact
section".

Purpose: DOC-03 is BLOCKED on this. Antoine specified the exact replacement structure and gave a
scope reduction ("Let's delete and not use salesRep field"). No further operator input is
required — the target structure, the dictionary work and the geometry measurements are all
recorded in 43-08-SUMMARY.md.

Output: a 7-row card (2 headlines + 4 key/value rows), one deleted-and-one-added dictionary pair
per language, and updated layout assertions.

**Amendment note — 43-08 supersedes CONTEXT D-04.** `43-CONTEXT.md` D-04 says "Phase 43 must not
'fix' this back", and `document.tsx` carries an inline comment to the same effect. D-04 was
written before the D-15 human pass. Antoine's 43-08 verdict amends it: the partner still comes
FIRST (D-04's ordering intent survives), but the partner company moves OFF the headline and INTO
an explicitly labelled `Partenaire` row, and the advisor regains its own headline. Delete the
stale "do NOT fix this back" comment and replace it with a note naming 43-08-SUMMARY.md as the
amending source. Do not leave a comment that contradicts the code.

**Geometry is already measured — do not widen the label column.** 43-08-SUMMARY.md measured, with
real Inter metrics at 8.5pt against the fixed 54pt label column in
`src/lib/pdf/components/card-key-value-row.tsx`: Partenaire 41.1pt, Téléphone 42.1pt, Fonction
34.6pt, Email 21.4pt. All fit with headroom. Net row count DROPS (2 headlines + 4 data rows, from
1 headline + 6 data rows). `card-key-value-row.tsx` is deliberately NOT in `files_modified`.

**Byte-determinism sequencing.** This change moves rendered bytes.
`__pdf-fixtures__/render-fixtures.test.ts` and `commission-free-fixture.test.ts` are already RED
from 43-09 and stay red. Do **NOT** run `npm run pdf:update-fixture` in this plan — regeneration
happens exactly once, in 43-12, after all byte-changing work has landed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-08-SUMMARY.md
@.planning/phases/43-new-pdf-layout/43-VERIFICATION.md
@.planning/phases/43-new-pdf-layout/43-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the VOTRE CONTACT card and move its dictionary keys in the same diff</name>
  <files>src/lib/i18n/dictionaries.ts, src/lib/pdf/document.tsx</files>
  <read_first>
    - .planning/phases/43-new-pdf-layout/43-08-SUMMARY.md — the "Agreed target structure" 7-row block quoted verbatim from Antoine, the "Dictionary work implied" bullet, and the "Geometry — verified, no column widen needed" bullet. This is the specification; do not re-derive it.
    - src/lib/pdf/document.tsx lines 260-320 — the current card grid, so the SOCIÉTÉ CLIENTE card's style object is mirrored exactly and only the VOTRE CONTACT branch changes
    - src/lib/i18n/dictionaries.ts lines 495-520 (FR contact block) and lines 1868-1890 (EN contact block)
    - src/lib/pdf/components/card-key-value-row.tsx — the fixed 54pt label column this card renders into
    - src/lib/pdf/em-dash.ts — the DOC-11 absence formatter; every optional value in this card passes through it
  </read_first>
  <action>
    Both files change in ONE task (and one commit) because each dictionary deletion must land in
    the same diff as its last `t()` call, or `npx tsc --noEmit` goes red at the plan boundary. Both
    deleted keys have exactly one consumer each, both in `document.tsx` — verified in 43-08.

    dictionaries.ts, FR block (currently ~lines 502-513):
    - DELETE the line `'pdf.card.contact.salesRep': 'Commercial',`
    - DELETE the line `'pdf.card.contact.advisorName': 'Conseiller',` together with the three-line
      comment above it that begins "pdf.card.contact.advisorName has no design source"
    - ADD `'pdf.card.contact.partner': 'Partenaire',` positioned directly after
      `'pdf.card.contact.title': 'VOTRE CONTACT',` so key order matches render order
    - Keep `'pdf.card.contact.partnerPhone'`, `'pdf.card.contact.advisorRole'`,
      `'pdf.card.contact.advisorPhone'`, `'pdf.card.contact.advisorEmail'` unchanged
    - Rewrite the block comment that currently reads "Contact card (DOC-03, D-04 partner-first
      order — row LABELS keep the design's wording, only the block ORDER inverts...)" so it records
      the 43-08 amendment: partner-first ordering stands; the partner company moved from the
      headline to an explicit `Partenaire` row; the advisor regained its own headline; `salesRep`
      was deleted at the operator's instruction

    dictionaries.ts, EN block (currently ~lines 1874-1885): the identical three edits, with
    `'pdf.card.contact.partner': 'Partner',`, deleting `'pdf.card.contact.salesRep': 'Sales rep',`
    and `'pdf.card.contact.advisorName': 'Advisor',` plus its comment, and the same rewritten block
    comment.

    document.tsx, the VOTRE CONTACT `<View>` (currently ~lines 288-316): replace the single
    headline plus six `CardKeyValueRow`s with exactly this 7-element structure, top to bottom,
    keeping the `<Eyebrow>{t('pdf.card.contact.title', lang)}</Eyebrow>` line and the card's outer
    `<View>` style object exactly as they are:

    1. Headline 1 — a `<Text>` reusing the SAME inline style object as today's headline
       (`fontSize: pdfFontSizes.cardHeadline`, `fontWeight: pdfFontWeights.semibold`,
       `letterSpacing: -0.11`, `color: pdfColors.navy`, `marginBottom: 4.5`) rendering
       `{emDash(inputs.partnerName)}`. Note the change from a raw value to `emDash(...)` — DOC-11
       is a geometry guarantee and every card value goes through the single formatter.
    2. `<CardKeyValueRow label={t('pdf.card.contact.partner', lang)} value={emDash(inputs.partnerCo)} />`
    3. `<CardKeyValueRow label={t('pdf.card.contact.partnerPhone', lang)} value={emDash(partnerPhone)} />`
    4. Headline 2 — a `<Text>` with the SAME style object as headline 1 plus `marginTop: 4.5`
       (so the two entities separate visually; 4.5 matches the existing headline rhythm and the 3pt
       row `marginBottom` in `card-key-value-row.tsx`), rendering `{emDash(advisor?.name)}`
    5. `<CardKeyValueRow label={t('pdf.card.contact.advisorRole', lang)} value={emDash(advisor?.fonction)} />`
    6. `<CardKeyValueRow label={t('pdf.card.contact.advisorPhone', lang)} value={emDash(advisor?.telephone)} />`
    7. `<CardKeyValueRow label={t('pdf.card.contact.advisorEmail', lang)} value={emDash(advisor?.email)} />`

    DELETE the inline comment block currently above the headline that begins "D-04: the design
    headlines the Leasetic advisor's name here" and ends "Do NOT 'fix' this back to the design's
    ordering." Replace it with a comment stating: DOC-03 as amended by the D-15 human verdict
    recorded in 43-08-SUMMARY.md; the card is two entities, each with its own headline; the partner
    company is an explicitly labelled `Partenaire` row so a company is never mistaken for a person;
    `salesRep` was deleted at the operator's instruction; every value goes through `emDash` per
    DOC-11.

    Change nothing else in `document.tsx` — the SOCIÉTÉ CLIENTE card, the hero, the table, the
    acceptance block and the footer are all verified and out of scope. Do NOT touch
    `card-key-value-row.tsx`. Do NOT run `npm run pdf:update-fixture`.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ! grep -q "salesRep" src/lib/i18n/dictionaries.ts &amp;&amp; ! grep -q "advisorName" src/lib/pdf/document.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/lib/i18n/dictionaries.test.ts src/lib/pdf/document.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "salesRep" src/lib/i18n/dictionaries.ts` succeeds and `! grep -q "salesRep" src/lib/pdf/document.tsx` succeeds
    - `! grep -q "advisorName" src/lib/i18n/dictionaries.ts` succeeds and `! grep -q "advisorName" src/lib/pdf/document.tsx` succeeds
    - `grep -c "'pdf.card.contact.partner':" src/lib/i18n/dictionaries.ts` outputs `2` (one FR, one EN)
    - `grep -q "'pdf.card.contact.partner': 'Partenaire'," src/lib/i18n/dictionaries.ts` succeeds and `grep -q "'pdf.card.contact.partner': 'Partner'," src/lib/i18n/dictionaries.ts` succeeds
    - `grep -c "t('pdf.card.contact" src/lib/pdf/document.tsx` outputs `5` (1 title + 4 row labels)
    - `grep -c "emDash(inputs.partnerName)" src/lib/pdf/document.tsx` outputs `1` and `grep -c "emDash(advisor?.name)" src/lib/pdf/document.tsx` outputs `1`
    - `! grep -q "fix.*this back" src/lib/pdf/document.tsx` succeeds — the stale D-04 comment is gone
    - `npx tsc --noEmit` exits 0 — the deletions and their last consumers landed in the same diff
    - `npx vitest run src/lib/i18n/dictionaries.test.ts` passes (FR/EN key-parity and the `pdf.card.contact.title` pins at lines 638-639 still hold)
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0
  </acceptance_criteria>
  <done>The card renders 2 headlines over 4 labelled rows, `Partenaire`/`Partner` exists as a dictionary key, and `salesRep`/`advisorName` exist nowhere in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Update the layout proof's label expectations and add a DOC-03 structural guard</name>
  <files>src/lib/pdf/layout.test.ts</files>
  <read_first>
    - src/lib/pdf/layout.test.ts lines 285-380 — the DOC-10 EN and FR `required` / `forbidden` phrase arrays that currently pin 'Sales rep', 'Advisor', 'Commercial', 'Conseiller'
    - src/lib/pdf/layout.test.ts lines 386-440 — the DOC-11 max-absence case, whose `labels` array at line ~424 also pins 'Commercial' and 'Conseiller', and whose em-dash count comment states the old row arithmetic
    - __pdf-fixtures__/fixtures.ts — `SHARED_BASE.inputs.partnerName` ('Antoine Rousseau'), `partnerCo` ('Memento IT') and `advisor.name` ('Camille Martin'), the anchors the ordering assertion reads
  </read_first>
  <action>
    Five edits in `src/lib/pdf/layout.test.ts`:

    1. EN `required` array (~lines 302-303): remove `'Sales rep',` and `'Advisor',`; add
       `'Partner',`. Leave every other entry untouched.
    2. EN `forbidden` array (~lines 323-331): add `'Partenaire',`. Do NOT add `'Partner'` to the FR
       `forbidden` array — the FR render contains 'Partenaire' and the pairing adds substring risk
       for no proof value.
    3. FR `required` array (~lines 349-350): remove `'Commercial',` and `'Conseiller',`; add
       `'Partenaire',`.
    4. DOC-11 `labels` array (~line 424): remove `'Commercial'` and `'Conseiller'`; add
       `'Partenaire'`. Then recount the em-dash floor asserted just above it (~lines 415-419): the
       contact card now contributes 2 headlines (`partnerName`, `advisor?.name`) plus 4 data rows,
       all through `emDash`, instead of 1 raw headline plus 6 rows. Rewrite that comment to state
       the NEW arithmetic explicitly per card, and adjust the `toBeGreaterThanOrEqual` floor only if
       the recount requires it. Keep it a floor, not an equality.
    5. Add a new top-level describe block
       `describe('DOC-03: the VOTRE CONTACT card separates the partner from the Leasetic advisor (Gap 2, 43-VERIFICATION.md)', ...)`
       placed immediately after the DOC-10 block, with two `it` cases:

       - FR structural ordering. Render `FR_FIXTURE.data`, reconstruct the text with
         `reconstructVisibleTextFontAware`, and assert the contact-card anchors appear in this
         order using `indexOf`:
         `partnerName` < `'Partenaire'` < `advisor.name` < `text.lastIndexOf('Email')`.
         Read the anchor values from `FR_FIXTURE.data.inputs.partnerName` and
         `FR_FIXTURE.data.advisor?.name` rather than hardcoding them, so the test survives a fixture
         rotation. Assert each `indexOf` is `> -1` FIRST, with a message naming the missing anchor
         (an ordering assertion on two `-1`s passes vacuously otherwise — that is exactly the class
         of hole that produced this gap). Then assert
         `expect(text).not.toContain('Conseiller')` and `expect(text).not.toContain('Commercial')`,
         each with a message naming the deleted dictionary key.
       - EN parity. Same render for `EN_FIXTURE.data`, asserting `'Partner'` sits between the
         partner name and the advisor name, plus `expect(text).not.toContain('Advisor')` and
         `expect(text).not.toContain('Sales rep')`.

       This is the assertion that would have caught the original defect: the old card had no
       `Partenaire` label at all and put the advisor's name in a row rather than a headline, so the
       ordering chain could not have held.

    Do NOT change `reconstructVisibleTextFontAware` or any extraction helper — 43-09 settled those.
    Do NOT run `npm run pdf:update-fixture`.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ! grep -q "'Conseiller'" src/lib/pdf/layout.test.ts &amp;&amp; grep -q "DOC-03: the VOTRE CONTACT card separates" src/lib/pdf/layout.test.ts &amp;&amp; npx vitest run src/lib/pdf/layout.test.ts &amp;&amp; npm run lint:check</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "'Conseiller'" src/lib/pdf/layout.test.ts` succeeds and `! grep -q "'Sales rep'" src/lib/pdf/layout.test.ts` succeeds
    - `grep -c "DOC-03: the VOTRE CONTACT card separates" src/lib/pdf/layout.test.ts` outputs `1`
    - `grep -c "'Partenaire'" src/lib/pdf/layout.test.ts` outputs at least `3` (FR required, DOC-11 labels, EN forbidden)
    - `npx vitest run src/lib/pdf/layout.test.ts` passes — including the DOC-01 guard added by 43-09, which must remain green
    - `npx vitest run src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` passes — ADMIN-09's 20 gates plus the rendered-output check survive the card change
    - `npm run lint:check` exits 0
    - `npx tsc --noEmit` exits 0
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0
  </acceptance_criteria>
  <done>The layout proof pins the new labels, proves the partner/advisor split by render order, and the deleted labels are asserted absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leasetic_advisor row → client-facing PDF | Advisor name, fonction, phone and email are internal-staff PII printed into a document handed to an external client. |
| users.companyName / partnerName → client-facing PDF | Partner-identifying data printed under an explicit `Partenaire` label. A wrong binding mislabels a person as a company. |
| dictionary key deletion → typecheck | A deleted key whose consumer survives is a runtime `t()` miss, not a compile error, unless both land in one diff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10-01 | Information disclosure | advisor PII in the rendered card | accept | Printing the Leasetic advisor's name, role, phone and email to the client is the card's entire purpose (DOC-03) and is operator-specified in 43-08. No new field is exposed; the same four columns render, only their labelling changes. |
| T-43-10-02 | Spoofing | `Partenaire` row bound to `inputs.partnerCo` | mitigate | Task 1 binds the row to `inputs.partnerCo` only, never to `partnerName`. Plan 43-11 closes the upstream fallback that can put a person's name into `partnerCo`. Task 2's ordering assertion pins `partnerName` (headline) and the `Partenaire` label as distinct anchors. |
| T-43-10-03 | Information disclosure | ADMIN-09 commission invisibility | mitigate | The card change introduces no commission-adjacent identifier. Acceptance criteria re-run `no-commission.test.ts` and the 20 `admin-09-grep-contracts.test.ts` gates after the byte change. |
| T-43-10-04 | Tampering | orphaned `t()` call after key deletion | mitigate | Both deletions land in the same diff as their only consumer; `npx tsc --noEmit` is an acceptance criterion, and `! grep -q` absence checks are written with `! grep -q` (never `grep -qv`, which cannot prove absence in this shell). |
| T-43-10-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. If an install becomes necessary, stop and escalate — the Package Legitimacy Gate has not been run for this gap closure. |
</threat_model>

<verification>
- `npx vitest run src/lib/pdf/layout.test.ts src/lib/pdf/document.test.tsx src/lib/i18n/dictionaries.test.ts src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` — all green.
- `npx tsc --noEmit` exits 0; `npm run lint:check` exits 0.
- `__pdf-fixtures__/render-fixtures.test.ts` and `commission-free-fixture.test.ts` remain EXPECTED RED. Record their output in the SUMMARY and state that 43-12 regenerates the baseline. Do not "fix" them here.
</verification>

<success_criteria>
- The VOTRE CONTACT card renders 2 headlines and exactly 4 key/value rows, in the order Antoine specified verbatim.
- `pdf.card.contact.salesRep` and `pdf.card.contact.advisorName` exist nowhere in the repository.
- `pdf.card.contact.partner` exists in both FR and EN with values `Partenaire` / `Partner`.
- The layout proof asserts the partner/advisor split by render order and asserts the deleted labels are absent.
- `expected.sha256.txt` is byte-unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-10-SUMMARY.md` when done.
</output>
