---
phase: 43-new-pdf-layout
plan: 12
type: execute
wave: 10
depends_on: [43-11]
files_modified:
  - __pdf-fixtures__/fixtures.ts
  - __pdf-fixtures__/expected.sha256.txt
  - src/lib/pdf/layout.test.ts
autonomous: true
gap_closure: true
requirements: [DOC-02]
must_haves:
  truths:
    - "A populated client SIRET renders beside the SIRET label in the SOCIÉTÉ CLIENTE card"
    - "A fixture without clientSiret still renders SIRET followed by an em dash, so the FIELD-03 legacy path stays covered"
    - "The committed byte-determinism baseline matches the document as it exists after all four gap-closure plans"
  artifacts:
    - path: "__pdf-fixtures__/fixtures.ts"
      provides: "clientSiret on the two happy-path fixtures; the agent-commission-free fixture deliberately without it"
      contains: "clientSiret"
    - path: "src/lib/pdf/layout.test.ts"
      provides: "DOC-02 positive assertion that a populated SIRET renders, plus the retained absent-branch control"
    - path: "__pdf-fixtures__/expected.sha256.txt"
      provides: "regenerated contentHash baseline reflecting 43-09 through 43-12"
      contains: "happy-path-fr:"
  key_links:
    - from: "__pdf-fixtures__/fixtures.ts"
      to: "src/lib/pdf/document.tsx"
      via: "emDash(inputs.clientSiret) at the SIRET row"
      pattern: "clientSiret"
    - from: "__pdf-fixtures__/expected.sha256.txt"
      to: "__pdf-fixtures__/render-fixtures.test.ts"
      via: "name:contentHash lines, one per fixture"
      pattern: "happy-path-fr:[0-9a-f]{64}"
---

<objective>
Close Gap 3 of `43-VERIFICATION.md` and re-baseline the byte-determinism fixture ONCE, after all
byte-changing work from 43-09, 43-10, 43-11 and this plan's Task 1 has landed.

Gap 3 is a fixture and coverage hole, not a live defect. NONE of the three fixtures in
`__pdf-fixtures__/fixtures.ts` carries a `clientSiret` key — all three carry only
`clientSiren: '123456789'` — so `emDash(inputs.clientSiret)` at `document.tsx:281` has never once
been rendered with a value in any test or fixture. Every test that touches the field exercises the
ABSENT branch (`document.test.tsx:99`, `layout.test.ts:395`, `layout.test.ts:424`). The one test
carrying a real value, `no-commission.test.ts:178`, asserts only commission invisibility and never
that SIRET appears.

Root cause of the blind spot, recorded so it is not repeated: `document.tsx:82` declares
`clientSiret?: string` — optional by design, because FIELD-03 requires pre-Phase-42 proposals with
no `clientSiret` key to still render. That deliberate optionality let the fixtures omit the field
and still typecheck, so the legacy escape hatch became the only branch under test. It is the same
failure shape as Gap 1 from the opposite direction: there a test normalised the bad output away,
here the fixtures never produced the good output. Both left a green suite that never asked the
question.

Both write paths already enforce the field: `finalize-wizard.ts:172` throws `LegacyDraftIncomplete`
when the key is absent, and `submit.ts:77` runs `proposalInputSchema.safeParse`, which requires
`clientSiret` and refines that its first 9 digits equal `clientSiren`. A wizard-finalized proposal
cannot lack it. **This plan changes no write path and no schema.** It closes the render-side
coverage hole only.

Output: `clientSiret` on the two happy-path fixtures, the agent-commission-free fixture deliberately
left without it, a positive render assertion with an absent-branch control, and a regenerated
`expected.sha256.txt`.

## Regeneration is the LAST step, and it happens here, exactly once

`__pdf-fixtures__/render-fixtures.test.ts` and `commission-free-fixture.test.ts` have been RED since
43-09 by design. Task 3 is where they go green again. Regeneration must run AFTER Task 1's fixture
change, or the baseline records a document that no longer exists. If any task in this plan forces a
corrective edit to `document.tsx`, re-run the regeneration after that edit.

The pre-gap-closure baseline, for the diff record:

```
agent-commission-free:c9d71060a52bdc0f1111e1e76aabe075299a305549a11c7106c9365aad14f191
happy-path-en:f4ba3fa84befdfe7e42171394fe0e0e9cdb5a4fe8b45b589c0bfbd7340158b98
happy-path-fr:076e362322735c4df07d5504557a0111398ab268c7957b95a78337ea617089e9
```

All three MUST change: every fixture is affected by the hyphenation callback (43-09) and the
VOTRE CONTACT restructure (43-10); the two happy-path fixtures are additionally affected by the
SIRET value. A fixture whose hash does NOT change is evidence that a change did not reach the
render — investigate rather than accept.

**D-16 precision, restated because it is easy to get wrong.** `src/lib/pdf/render.ts:16-34`
documents that the raw `sha256` is NOT stable across renders — @react-pdf/renderer's Fiber
scheduler orders PDF objects differently on each call. `contentHash` (sorted, inflated stream
hashes) is the deterministic contract and is what the fixture file stores. Do not write or add any
assertion comparing raw `sha256` between two renders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-VERIFICATION.md
@.planning/phases/43-new-pdf-layout/43-07-SUMMARY.md
@.planning/phases/43-new-pdf-layout/43-11-SUMMARY.md
@src/lib/pdf/render.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the happy-path fixtures a valid clientSiret, keep one fixture without it</name>
  <files>__pdf-fixtures__/fixtures.ts</files>
  <read_first>
    - __pdf-fixtures__/fixtures.ts — the whole file: `SHARED_BASE`, `AGENT_COMMISSION_FREE_BASE` (which spreads `SHARED_BASE`), and the `pdfFixtures` array
    - src/lib/calc/schema.ts lines 190-230 — `requiredSirenSchema`, `requiredSiretSchema`, and the `.refine((data) => data.clientSiret.slice(0, 9) === data.clientSiren, ...)` the fixture value must satisfy
    - src/lib/pdf/no-commission.test.ts lines 172-184 — `buildDraftInputs`, which already uses the synthetic constant `'12345678900012'` with the comment "required, must match clientSiren prefix". Reuse that exact value; it is this repo's established synthetic SIRET.
    - src/lib/pdf/document.tsx — the SOCIÉTÉ CLIENTE card's SIRET row, `emDash(inputs.clientSiret)`
  </read_first>
  <action>
    Add `clientSiret: '12345678900012',` to `SHARED_BASE.inputs`, positioned immediately after
    `clientSiren: '123456789',` so fixture field order matches the card's render order and the
    wizard's step-1 client card.

    The value is not arbitrary: `'12345678900012'.slice(0, 9)` is `'123456789'`, which equals
    `SHARED_BASE.inputs.clientSiren`, satisfying the schema refine. Add a comment beside it naming
    that refine, and stating that the value is synthetic — a sequential-digit placeholder, not a real
    French company's establishment identifier — and that it is the same constant already used by
    `src/lib/pdf/no-commission.test.ts`.

    `SHARED_BASE` flows into all three fixtures, so this alone would give `agent-commission-free` a
    SIRET too. **Keep the agent-commission-free fixture WITHOUT `clientSiret`** so the FIELD-03
    legacy-render path (a pre-Phase-42 proposal with no `clientSiret` key at all) stays covered by a
    committed fixture rather than by ad-hoc test overrides alone.

    Implement that omission with a small module-local helper rather than a destructuring rest
    pattern:

    - Add a function that takes `SHARED_BASE['inputs']`, shallow-copies it into a local `const`,
      `delete`s the `clientSiret` property from the copy, and returns it. `delete` is valid here
      because `clientSiret` is declared optional on the props type.
    - Build `AGENT_COMMISSION_FREE_BASE` as today (`...SHARED_BASE`, overridden `computed`) plus an
      `inputs` key set to that helper's return value.
    - Do NOT use `const { clientSiret: _unused, ...rest } = ...` — `npm run lint:check` runs
      `eslint . --max-warnings=0`, and an unused destructured binding fails CI even though `tsc` and
      `vitest` pass.

    Document the split in a comment on the `pdfFixtures` array: `happy-path-fr` and `happy-path-en`
    carry a populated SIRET (DOC-02 positive branch, Gap 3); `agent-commission-free` deliberately
    omits the key (FIELD-03 legacy branch). Anyone adding a fourth fixture must preserve at least one
    of each.

    Do NOT change `clientSiren`, `amountHT`, `durationMonths`, `coeff` or `loyerHT` — the
    commission-free proof in `commission-free-fixture.test.ts` depends on the 1687.50 vs 1771.88
    arithmetic. Do NOT run `npm run pdf:update-fixture` yet; that is Task 3.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; grep -q "clientSiret: '12345678900012'," __pdf-fixtures__/fixtures.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint:check &amp;&amp; npx vitest run src/lib/pdf/document.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "clientSiret: '12345678900012'," __pdf-fixtures__/fixtures.ts` succeeds
    - `! grep -q "_unused" __pdf-fixtures__/fixtures.ts` succeeds — no unused destructured binding
    - This command prints `true` for `happy-path-fr` and `happy-path-en` and `false` for `agent-commission-free`, and its output is pasted into the SUMMARY: `npx tsx -r ./scripts/_preload-mock-server-only.cjs -e "import('./__pdf-fixtures__/fixtures').then(m => console.log(m.pdfFixtures.map(f => [f.name, 'clientSiret' in f.data.inputs])))"`
    - The SUMMARY states explicitly that `'12345678900012'.slice(0, 9) === '123456789'`, so the schema refine is satisfied
    - `npx tsc --noEmit` exits 0 and `npm run lint:check` exits 0
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0 — regeneration has not run yet
  </acceptance_criteria>
  <done>Two fixtures carry a schema-valid SIRET; one deliberately does not.</done>
</task>

<task type="auto">
  <name>Task 2: Assert that a populated SIRET renders, with the absent branch as the control</name>
  <files>src/lib/pdf/layout.test.ts</files>
  <read_first>
    - src/lib/pdf/layout.test.ts — `reconstructVisibleTextFontAware`, `countCodepointOccurrences`, `EM_DASH`, `FR_FIXTURE`/`EN_FIXTURE`, and the DOC-11 max-absence case at lines ~386-440, which is the model this new case mirrors from the opposite direction
    - __pdf-fixtures__/fixtures.ts — as updated by Task 1
    - .planning/phases/43-new-pdf-layout/43-VERIFICATION.md — Gap 3's second `missing[]` bullet: "Add a positive assertion that a populated SIRET renders in the SOCIÉTÉ CLIENTE card, mirroring how DOC-11's em-dash case is asserted"
  </read_first>
  <action>
    Add a new top-level describe block
    `describe('DOC-02: a populated client SIRET renders in the SOCIÉTÉ CLIENTE card (Gap 3, 43-VERIFICATION.md)', ...)`
    with three `it` cases:

    1. FR populated branch. Render `FR_FIXTURE.data`, reconstruct the text, and assert: the fixture
       value is defined first (`expect(FR_FIXTURE.data.inputs.clientSiret).toBeDefined()` with a
       message saying Gap 3 requires a populated fixture — this stops the whole case going vacuous
       if a future fixture rotation drops the key); then `expect(text).toContain('SIRET')`;
       `expect(text).toContain(siretValue)` where `siretValue` is read from
       `FR_FIXTURE.data.inputs.clientSiret`; and
       `expect(text.indexOf('SIRET')).toBeLessThan(text.indexOf(siretValue))` so the value is proven
       to sit AFTER its own label rather than merely somewhere on the page.
    2. EN parity. The same four assertions against `EN_FIXTURE.data`.
    3. Absent branch control (FIELD-03). Build a variant of `FR_FIXTURE.data` with
       `inputs: { ...FR_FIXTURE.data.inputs, clientSiret: undefined }`, render it, and assert its
       em-dash count via `countCodepointOccurrences(..., EM_DASH.codePointAt(0)!)` is exactly the
       populated render's count PLUS 1 — proving the populated branch genuinely replaced an em dash
       rather than adding a stray glyph elsewhere. Also assert `expect(absentText).toContain('SIRET')`
       (the label never disappears) and `expect(absentText).not.toContain(siretValue)`.

    Case 3 is the falsifiability control for cases 1 and 2: without it, a reconstruction bug that
    returned a superset of the page text would let `toContain` pass on nothing real.

    Do NOT modify the existing DOC-11 max-absence case, the DOC-01 guard from 43-09, the DOC-03
    guards from 43-10 and 43-11, or any extraction helper. Do NOT run `npm run pdf:update-fixture`;
    that is Task 3.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; grep -q "DOC-02: a populated client SIRET renders" src/lib/pdf/layout.test.ts &amp;&amp; npx vitest run src/lib/pdf/layout.test.ts &amp;&amp; npm run lint:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DOC-02: a populated client SIRET renders" src/lib/pdf/layout.test.ts` outputs `1`
    - The block contains exactly 3 `it(` cases, one of which asserts the em-dash delta is exactly `+1`
    - `npx vitest run src/lib/pdf/layout.test.ts` passes, with the DOC-01 (43-09), DOC-03 (43-10) and Finding 3 (43-11) guards all still green
    - `npm run lint:check` exits 0 and `npx tsc --noEmit` exits 0
    - `git status --porcelain` lists no untracked `*.test.ts*` file
    - `git diff --exit-code __pdf-fixtures__/expected.sha256.txt` exits 0 — regeneration is still pending
  </acceptance_criteria>
  <done>The populated SIRET render path is covered by an assertion that can fail, with the absent path as its control.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the byte-determinism baseline once, and prove the whole suite green</name>
  <files>__pdf-fixtures__/expected.sha256.txt</files>
  <read_first>
    - scripts/update-pdf-fixture.ts — the `--confirm UPDATE-FIXTURE` gate, the dry-run diff output, and that it writes sorted `name:contentHash` lines
    - src/lib/pdf/render.ts lines 16-34 and 50-104 — the determinism contract and `computeContentHash`, so the SUMMARY records why `contentHash` and not raw `sha256` is the baseline
    - __pdf-fixtures__/render-fixtures.test.ts and __pdf-fixtures__/commission-free-fixture.test.ts — the two tests that have been RED since 43-09 and that this task turns green
    - .planning/phases/43-new-pdf-layout/43-07-SUMMARY.md — the D-16 regeneration process as it was run the first time
  </read_first>
  <action>
    Run in this exact order and paste each output into the SUMMARY:

    1. `npm run pdf:update-fixture` — DRY-RUN. Read the printed diff. Confirm all three fixture
       hashes differ from the pre-gap-closure baseline quoted in this plan's objective. If any hash
       is unchanged, STOP: a change from 43-09/43-10/43-11/Task 1 did not reach the render. Diagnose
       before writing anything.
    2. `npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE` — APPLY. This is the ONLY
       regeneration in the entire gap closure.
    3. `npx vitest run __pdf-fixtures__` — `render-fixtures.test.ts`, `commission-free-fixture.test.ts`
       and `inter-typography.test.ts` must all be green.
    4. `npx vitest run src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` —
       ADMIN-09's rendered-output check and its 20 grep gates, re-proven against the new bytes. These
       are non-negotiable.
    5. `npm test` — the FULL suite. This is the first fully-green run since 43-09. Record the
       files/passed/failed counts.
    6. `npm run lint:check` then `npm run build`. Both must exit 0. Passing `tsc` and `vitest` is not
       sufficient — CI runs `eslint . --max-warnings=0` and the production build separately.
    7. `npm run pdf:preview` — refresh `.preview/happy-path-fr.pdf`, `.preview/happy-path-en.pdf` and
       `.preview/agent-commission-free.pdf` so plan 43-13's operator checkpoint has current artifacts
       to look at. Record each file's byte size; all three must exceed 4,000 bytes.

    Do NOT run any database migration, `db:migrate`, or any command that opens a DB connection —
    `.env.local` points at the production/main Neon branch and nothing in this gap closure touches
    the database. `scripts/update-pdf-fixture.ts` and `scripts/render-pdf-preview.ts` are both DB-free
    by design; keep it that way.

    If step 5 or 6 fails, fix the cause and RE-RUN step 2 if the fix touched anything that renders.
    A baseline written before a corrective render edit is a stale baseline.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm test &amp;&amp; npm run lint:check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `__pdf-fixtures__/expected.sha256.txt` contains exactly 3 lines matching `^[a-z-]+:[0-9a-f]{64}$`, verified with `grep -cE "^[a-z-]+:[0-9a-f]{64}$" __pdf-fixtures__/expected.sha256.txt` outputting `3`
    - None of the three hashes equals its pre-gap-closure value: `! grep -q "c9d71060a52bdc0f1111e1e76aabe075299a305549a11c7106c9365aad14f191" __pdf-fixtures__/expected.sha256.txt`, `! grep -q "f4ba3fa84befdfe7e42171394fe0e0e9cdb5a4fe8b45b589c0bfbd7340158b98" __pdf-fixtures__/expected.sha256.txt`, `! grep -q "076e362322735c4df07d5504557a0111398ab268c7957b95a78337ea617089e9" __pdf-fixtures__/expected.sha256.txt` — all three succeed
    - `npx vitest run __pdf-fixtures__` passes
    - `npx vitest run src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` passes
    - `npm test` reports 0 failed, and the SUMMARY records the files/passed counts alongside 43-07's baseline of 200 files / 2711 passed for comparison
    - `npm run lint:check` exits 0 and `npm run build` exits 0
    - `.preview/happy-path-fr.pdf`, `.preview/happy-path-en.pdf` and `.preview/agent-commission-free.pdf` all exist and each exceeds 4,000 bytes; sizes recorded in the SUMMARY
    - `git log --oneline -1 -- __pdf-fixtures__/expected.sha256.txt` shows this plan's commit — the file was regenerated exactly once across 43-09..43-12
  </acceptance_criteria>
  <done>The committed baseline matches the document as it now exists, and the full suite, lint gate and build are green for the first time since 43-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture data → repository | Fixture values live in git forever and are rendered into `.preview/*.pdf` artifacts. A real company identifier committed here is a permanent third-party data disclosure. |
| render output → byte-determinism baseline | `expected.sha256.txt` is the only automated evidence that a proposal re-renders identically. A stale baseline silently disables that evidence. |
| rendered PDF → external client | Anything that reaches the SOCIÉTÉ CLIENTE card leaves Leasetic's trust domain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-12-01 | Information disclosure | the committed `clientSiret` fixture value | mitigate | `'12345678900012'` is a sequential-digit synthetic value, not a real French establishment identifier, and is already an established constant in `src/lib/pdf/no-commission.test.ts`. Task 1 requires a comment stating its synthetic status so it is never mistaken for production data or rotated to a real SIRET. |
| T-43-12-02 | Repudiation | stale `expected.sha256.txt` | mitigate | Regeneration is sequenced last and exactly once. Task 3 acceptance asserts all three hashes changed and that `git log` shows a single commit touching the file across the gap closure. A hash that did not move is treated as a defect signal, not a pass. |
| T-43-12-03 | Information disclosure | ADMIN-09 commission invisibility across new bytes | mitigate | Task 3 step 4 re-runs `no-commission.test.ts` and the 20 `admin-09-grep-contracts.test.ts` gates against the regenerated render before the baseline is accepted. |
| T-43-12-04 | Tampering | production database | mitigate | No migration, no `db:migrate`, no DB-touching command. `update-pdf-fixture.ts` and `render-pdf-preview.ts` are DB-free via `scripts/_preload-mock-server-only.cjs`; the action forbids changing that. |
| T-43-12-05 | Denial of service | FIELD-03 legacy render path | mitigate | The agent-commission-free fixture deliberately keeps no `clientSiret` key, and Task 2 case 3 asserts the absent branch still renders its label with an em dash — so relaxing coverage on the populated branch does not silently remove coverage of the legacy branch. |
| T-43-12-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. If an install becomes necessary, stop and escalate — the Package Legitimacy Gate has not been run for this gap closure. |
</threat_model>

<verification>
- `npm test` — full suite, 0 failed.
- `npm run lint:check` exits 0; `npm run build` exits 0; `npx tsc --noEmit` exits 0.
- `npx vitest run __pdf-fixtures__ src/lib/pdf/no-commission.test.ts tests/admin-09-grep-contracts.test.ts` — green.
- `expected.sha256.txt` regenerated exactly once, after every byte-changing edit.
- `.preview/*.pdf` refreshed for the 43-13 operator checkpoint.
</verification>

<success_criteria>
- `happy-path-fr` and `happy-path-en` carry `clientSiret: '12345678900012'`; `agent-commission-free` has no `clientSiret` key at all.
- A DOC-02 describe block proves a populated SIRET renders after its label in both languages, with an absent-branch em-dash-delta control.
- All three committed contentHashes differ from their pre-gap-closure values.
- The full suite, the lint gate and the production build are green.
- No database command was run.
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-12-SUMMARY.md` when done.
</output>
