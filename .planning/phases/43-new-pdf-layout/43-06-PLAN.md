---
phase: 43-new-pdf-layout
plan: 06
type: execute
wave: 4
depends_on: [43-05]
files_modified:
  - src/lib/pdf/document.tsx
  - src/lib/pdf/styles.ts
  - src/lib/pdf/components/financial-row.tsx
  - src/lib/pdf/components/section-label.tsx
  - src/lib/pdf/components/key-value-row.tsx
autonomous: true
requirements: [DOC-04, DOC-05, DOC-06, DOC-07, DOC-08, DOC-11]
must_haves:
  truths:
    - "The monthly rent renders in a navy-outlined hero card beside the CONDITIONS FINANCIÈRES table"
    - "The financial table ends in a bold Total des loyers HT equal to monthlyRent × termMonths"
    - "The conditions paragraph states the proposal's own validityDays, never a hardcoded 30"
    - "The acceptance block sits at the bottom of the page regardless of how much content precedes it"
    - "The legal footer carries the company registration line, the proposal reference, the page number and the 14%-opacity icon mark"
    - "An on-demand proposal renders the on-request string in the hero and em dashes for coefficient and total"
    - "The SLB / évaluation-de-parc interests block no longer prints"
  artifacts:
    - path: "src/lib/pdf/components/financial-row.tsx"
      provides: "FinancialRow — the 42/58 flex port of the design's table row"
      exports: ["FinancialRow"]
    - path: "src/lib/pdf/document.tsx"
      provides: "hero, financial table, conditions paragraph, bottom-pinned acceptance block, legal footer"
      contains: "marginTop: 'auto'"
    - path: "src/lib/pdf/styles.ts"
      provides: "token set with the five legacy font roles and the D-08 green tokens removed"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "src/lib/pdf/components/leasetic-icon.tsx"
      via: "LeaseticIcon at 13.5pt / 14% opacity in the legal footer"
      pattern: "<LeaseticIcon"
---

<objective>
Rebuild the bottom half of the document and delete everything the redesign retires.

Purpose: DOC-04, DOC-05, DOC-06, DOC-07 and DOC-08. This is where D-05's deliberate content removal
lands (the interests block), where D-09's on-demand em-dash rule resolves a state the design has no
slot for, and where D-08's consequence is finally realised — `pdfColors.green` and `greenTint` lose
their only consumer when the loyer hero becomes navy-outlined on white.

Output: `document.tsx` complete from the hero row to the legal footer, with every superseded block
and token deleted.

**Deletions in this plan are deliberate and operator-approved, not incidental casualties.** The
interests block (D-05) is the only place the redesign removes something a partner deliberately chose:
`inputs.slb` and `inputs.evalParc` stay in the wizard and stay in the immutable `inputs`, and simply
stop printing.

**This repo runs `eslint --max-warnings=0`.** Removing the last consumers of `SectionLabel`,
`KeyValueRow`, `pdfColors.green` / `greenTint` / `ink` / `muted` / `border` and the five legacy font
roles creates orphans that pass `tsc` and `vitest` but fail CI. Cleaning them up is part of this plan,
not a follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-CONTEXT.md
@.planning/phases/43-new-pdf-layout/43-PATTERNS.md
@.planning/phases/43-new-pdf-layout/43-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the loyer hero and the CONDITIONS FINANCIÈRES table (DOC-04, DOC-05, D-08, D-09)</name>
  <files>src/lib/pdf/document.tsx, src/lib/pdf/components/financial-row.tsx</files>
  <read_first>
    - `src/lib/pdf/document.tsx` — the tree as plan 43-05 left it, and specifically the blocks this
      task replaces: the computation-breakdown `<View>` built from `KeyValueRow`, and the green
      loyer feature card with its `pdfColors.green` border and `greenTint` fill.
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` §80-99 and
      `.planning/assets/v1.9-quote-design/Quote-EN-A.dc.html` §80-99 — the hero card, the
      `1.05fr 1.15fr` grid, and the `table-layout:fixed` 42/58 colgroup with its five rows.
    - `.planning/assets/v1.9-quote-design/reference/Quote-FR-A.reference.png` — D-14's visual target
      for this row.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — D-08 (green enters only through the logo)
      and **D-09 in full** (the on-demand resolution).
    - `src/lib/pdf/components/card-key-value-row.tsx` — the sibling primitive whose shape
      `FinancialRow` follows; its 54pt-fixed geometry is deliberately NOT reused here, because the
      table's columns are proportional.
    - `src/lib/i18n/format.ts` and `src/lib/pdf/sanitize-number.ts` — `formatCurrency`,
      `formatNumber` and the `sanitizePdfNumber` wrapper every numeric slot routes through.
  </read_first>
  <action>
    Create `src/lib/pdf/components/financial-row.tsx` exporting `FinancialRow` with props
    `{ label: string; value: string; emphasis?: boolean; last?: boolean }`. It renders a
    `<View style={{ flexDirection: 'row' }}>` with:
      - label `<Text style={{ flexBasis: '42%', fontSize: pdfFontSizes.heroCaption }}>` coloured
        `pdfColors.bodyBlue` normally and `pdfColors.navy` at `fontWeight: pdfFontWeights.semibold`
        when `emphasis` is set;
      - value `<Text style={{ flexBasis: '58%', textAlign: 'right', fontSize: pdfFontSizes.heroCaption,
        fontWeight: pdfFontWeights.semibold, color: pdfColors.navy }}>`.
    Row chrome: `paddingVertical: 3.75` and `borderBottomWidth: 0.75` /
    `borderBottomColor: pdfColors.hairline` for a normal row; for `last`, `paddingTop: 4.5`,
    `paddingBottom: 0` and no bottom border — the design's `padding:6px 0 0` total row.
    The 42/58 `flexBasis` pair is the direct port of the design's `<colgroup>`; react-pdf has no table
    primitive. Comment that derivation.

    In `document.tsx`, delete the computation-breakdown `<View>` and the green loyer feature card
    entirely, and replace both with one row: `<View style={{ flexDirection: 'row', alignItems:
    'stretch', marginBottom: 10.5 }}>` holding the hero and the table card. The hero gets
    `flexGrow: 1.05, flexBasis: 0, marginRight: 7.5`; the table card gets `flexGrow: 1.15,
    flexBasis: 0` — the design's `1.05fr 1.15fr` with a 10px gap.

    **Hero (DOC-04).** `borderWidth: 1.125` (the design's 1.5px), `borderColor: pdfColors.navy`,
    `borderRadius: 10.5`, `padding: 15` (20px), `flexDirection: 'column'`,
    `justifyContent: 'center'`. No background fill and no green anywhere — D-08. Contents:
      - `<Eyebrow marginBottom={4.5}>{t('pdf.loyer.label', lang)}</Eyebrow>`
      - the value `<Text>` at `pdfFontSizes.h1` (21 — down from today's 32),
        `fontWeight: pdfFontWeights.semibold`, `letterSpacing: -0.63`, `lineHeight: 1.05`,
        `color: pdfColors.navy`. Keep today's branch verbatim in shape: when
        `computed.state === 'on-demand' || !computed.loyerHT`, render `t('pdf.loyer.on.demand', lang)`;
        otherwise `sanitizePdfNumber(formatCurrency(Number(computed.loyerHT), lang))`. D-09 is explicit
        that the hero keeps the on-request string — it is a real translated operator-facing value, not
        an absence, so it does NOT become an em dash.
      - the caption `<Text>` at `pdfFontSizes.heroCaption` (9), `color: pdfColors.bodyBlue`,
        `marginTop: 4.5`, reading
        `t('pdf.loyer.subtext', lang).replace('{0}', String(inputs.durationMonths))`.

    **Table card (DOC-05).** Same chrome as the two cards above it: `backgroundColor:
    pdfColors.cardFill`, `borderRadius: 10.5`, `paddingVertical: 9.75`, `paddingHorizontal: 11.25`.
    An `<Eyebrow marginBottom={6.75}>{t('pdf.table.title', lang)}</Eyebrow>` then five
    `<FinancialRow>`s, in this exact order:
      1. `t('pdf.table.amount', lang)` → `sanitizePdfNumber(formatCurrency(Number(inputs.amountHT), lang))`.
         Always renders (D-09).
      2. `t('pdf.table.term', lang)` → `t('pdf.pill.term', lang).replace('{0}', String(inputs.durationMonths))`.
         Always renders (D-09).
      3. `t('pdf.computed.coefficient.label', lang)` → on-demand renders `emDash(null)`; otherwise
         `` `${sanitizePdfNumber(formatNumber(Number(computed.coeff), lang, { minimumFractionDigits: 4, maximumFractionDigits: 4 }))} %` ``.
         Keep the ` %` suffix exactly as today's code emits it — the design shows no unit but the
         coefficient IS a percentage (`loyer = amountHT × coeff / 100`), and dropping the unit would
         change the document's meaning rather than its styling.
      4. `t('pdf.table.monthlyRent', lang)` → the same value expression as the hero, including the
         same on-demand branch to `t('pdf.loyer.on.demand', lang)`. This row restates the hero's
         quantity, so under D-09's own reasoning it carries the same real translated value rather than
         an absence. D-09 enumerates only rows 3 and 5 as em-dash cases; record this reading in the
         SUMMARY and flag it for the D-15 visual pass to confirm or overrule.
      5. `t('pdf.table.total', lang)` with `emphasis` and `last` → on-demand renders `emDash(null)`;
         otherwise `sanitizePdfNumber(formatCurrency(total, lang))` where
         `total = Math.round(Number(computed.loyerHT) * inputs.durationMonths * 100) / 100`.
         Round explicitly to the cent before formatting — a bare float multiply is not deterministic
         to the last digit across inputs, and PROP-17 gates on the rendered bytes.

    Do not touch the interests block, the validity caption or the footer in this task.
  </action>
  <acceptance_criteria>
    - `! grep -q "pdfColors.green" src/lib/pdf/document.tsx` and `! grep -q "greenTint" src/lib/pdf/document.tsx`
      — D-08: no green in the document tree.
    - `grep -q "borderWidth: 1.125" src/lib/pdf/document.tsx` and `grep -q "letterSpacing: -0.63" src/lib/pdf/document.tsx`.
    - `grep -c "<FinancialRow" src/lib/pdf/document.tsx` returns 5.
    - `grep -q "flexBasis: '42%'" src/lib/pdf/components/financial-row.tsx` and
      `grep -q "flexBasis: '58%'" src/lib/pdf/components/financial-row.tsx`.
    - `grep -q "Math.round(" src/lib/pdf/document.tsx` — the total is rounded to the cent before formatting.
    - `! grep -q "pdfFontSizes.loyer" src/lib/pdf/document.tsx` — the 32pt role is no longer consumed.
    - Rendered-value proof, FR happy-path fixture (`amountHT: '75000'`, `durationMonths: 48`,
      `loyerHT: '1771.88'`): the extracted visible text contains `Total des loyers HT` and the
      formatted value of `85 050,24` (that is `1771.88 × 48`, rounded to the cent, in the FR locale
      `formatCurrency` produces). EN fixture: contains `Total rents excl. VAT`.
    - On-demand proof: render the on-demand fixture from `document.test.tsx` and assert the extracted
      text contains `Sur demande` (the hero and row 4) and at least two U+2014 characters (rows 3 and 5),
      and does NOT contain a `%` on the coefficient row.
    - `npm run typecheck` exits 0; `npm run lint:check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm test -- src/lib/pdf/document.test.tsx</automated>
  </verify>
  <done>The navy-outlined hero sits beside the five-row financial table, the total equals monthlyRent × termMonths rounded to the cent, the on-demand state renders the on-request string in the hero and em dashes for coefficient and total, and no green remains in the document tree.</done>
</task>

<task type="auto">
  <name>Task 2: Build the conditions paragraph, the bottom-pinned acceptance block and the legal footer (DOC-06, DOC-07, DOC-08, D-05)</name>
  <files>src/lib/pdf/document.tsx</files>
  <read_first>
    - `src/lib/pdf/document.tsx` — the tree as Task 1 left it, and specifically the three blocks this
      task replaces: the interests block, the validity-caption `<Text>`, and the
      `position: 'absolute'` footer.
    - `.planning/assets/v1.9-quote-design/Quote-FR-A.dc.html` §101-130 and
      `.planning/assets/v1.9-quote-design/Quote-EN-A.dc.html` §101-130 — the conditions block, the
      acceptance card with its two grids and the dashed stamp box, and the two-line legal footer.
    - `.planning/assets/v1.9-quote-design/reference/Quote-FR-A.reference.png` and
      `Quote-EN-A.reference.png` — D-14's visual target for the bottom third of the page.
    - `.planning/phases/43-new-pdf-layout/43-PATTERNS.md` — § "Bottom-pinned footer", which documents
      both candidate mechanisms and states that the `render={({ pageNumber }) => ...}` + `fixed`
      pattern must be preserved verbatim wherever page numbering appears.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — D-05 (the interests block is dropped) and
      the Claude's-Discretion note on how the acceptance block is pinned.
    - `src/lib/pdf/components/leasetic-icon.tsx` — `LeaseticIcon`'s `{ size, opacity }` props.
  </read_first>
  <action>
    **Conditions (DOC-06).** Replace the validity-caption `<Text>` with a `<View>` at
    `borderTopWidth: 0.75`, `borderTopColor: pdfColors.hairline`, `paddingTop: 7.5`,
    `marginBottom: 9` containing an `<Eyebrow marginBottom={3}>{t('pdf.conditions.title', lang)}</Eyebrow>`
    and a body `<Text>` at `pdfFontSizes.pill` (8), `lineHeight: 1.65`, `color: pdfColors.bodyBlue`
    reading `t('pdf.validity.caption', lang).replace('{0}', formatDate(expiresAt, lang)).replace('{1}', String(inputs.validityDays))`.
    Keep today's `expiresAt` derivation verbatim. `{1}` is DOC-06's whole point: the design hardcodes
    "(30 jours)" / "(30 days)" while `validityDays` is operator-selectable at 15 / 30 / 60, and a
    15-day proposal would otherwise contradict its own `validUntil` date. The design is wrong here
    and the implementation does not copy it.

    **Delete the interests block (D-05).** Remove the entire `{(inputs.slb || inputs.evalParc) && (...)}`
    conditional and both `✓`-prefixed `<Text>` nodes. `inputs.slb` and `inputs.evalParc` stay in the
    wizard and in the immutable `inputs`; they simply stop printing. Leave a one-line comment at the
    deletion site naming D-05 so the omission reads as a decision rather than an oversight.

    **Acceptance block (DOC-07).** Insert a `<View>` at `marginTop: 'auto'`, `borderWidth: 0.75`,
    `borderColor: pdfColors.hairline`, `borderRadius: 10.5`, `paddingVertical: 12`,
    `paddingHorizontal: 13.5`. `marginTop: 'auto'` inside the `<Page>`'s flex column is the design's
    own mechanism and Yoga supports it. If it does not push the block down, insert a
    `<View style={{ flexGrow: 1 }} />` spacer immediately before it instead — that is the only
    fallback; do not reach for `position: 'absolute'`, which would let the block overlap content.
    Contents:
      - a header row (`flexDirection: 'row'`, `justifyContent: 'space-between'`,
        `alignItems: 'flex-end'`, `marginBottom: 10.5`) with `<Eyebrow marginBottom={0}>` carrying
        `t('pdf.acceptance.title', lang)` and a `<Text>` at 8pt / `pdfColors.labelTeal` carrying
        `t('pdf.acceptance.note', lang)`. The design uses `align-items:baseline`; react-pdf's Yoga
        does not implement baseline alignment, so `flex-end` is the port — note that substitution in
        the SUMMARY for the D-15 pass.
      - a three-column row (`flexDirection: 'row'`, `marginBottom: 10.5`) with column flex grows
        `1 / 1 / 1.5`, each `flexBasis: 0`, separated by `marginRight: 12`. Each column is a label
        `<Text>` at 8pt / `pdfColors.labelTeal` with `marginBottom: 16.5` (the design's 22px) above a
        rule `<View style={{ borderBottomWidth: 0.75, borderBottomColor: pdfColors.navy }} />`.
        Labels: `pdf.acceptance.place`, `pdf.acceptance.date`, `pdf.acceptance.signatory`.
      - a two-column row (`flexDirection: 'row'`) with flex grows `1 / 1.5`, `flexBasis: 0`,
        `marginRight: 12` on the first. First column: `pdf.acceptance.signature` at 8pt /
        `pdfColors.labelTeal` with `marginBottom: 22.5` (the design's 30px) above the same rule.
        Second column: the stamp box — `borderWidth: 0.75`, `borderStyle: 'dashed'`,
        `borderColor: pdfColors.stampBorder` (`#BAC4D0`), `borderRadius: 7.5` (the design's 10px),
        `height: 40.5` (the design's 54px), `paddingVertical: 6`, `paddingHorizontal: 7.5`,
        containing a `<Text>` at 8pt / `pdfColors.stampText` (`#94A7B6`) reading
        `t('pdf.acceptance.stamp', lang)`.

    **Legal footer (DOC-08).** Delete the `position: 'absolute'` footer entirely and replace it with a
    normal-flow `<View>` after the acceptance block at `flexDirection: 'row'`,
    `justifyContent: 'space-between'`, `alignItems: 'flex-end'`, `marginTop: 9`, `paddingTop: 6.75`,
    `borderTopWidth: 0.75`, `borderTopColor: pdfColors.hairline`. Contents:
      - Left: a `<View>` with two `<Text>` nodes at `pdfFontSizes.legalFooter` (6.8),
        `lineHeight: 1.5`, `color: pdfColors.labelTeal`. The first wraps a nested
        `<Text style={{ fontWeight: pdfFontWeights.semibold, color: pdfColors.navy }}>` carrying
        `t('pdf.footer.legal.brand', lang)` followed by `t('pdf.footer.legal.rest', lang)`. The second
        is `t('pdf.footer.legal.line2', lang)`.
      - Right: a `<View style={{ flexDirection: 'row', alignItems: 'center' }}>` containing a `<Text>`
        at the same 6.8pt / `labelTeal` with
        `render={({ pageNumber, totalPages }: { pageNumber: number; totalPages: number }) => \`${lcRef} · Page ${pageNumber}/${totalPages}\`}`
        and `fixed` — react-pdf's own per-page callback, preserved verbatim from today's footer, not
        replaced by a static string — and `<LeaseticIcon size={13.5} opacity={0.14} />` with
        `marginLeft: 6` (the design's `height:18px;opacity:.14` and 8px gap).
  </action>
  <acceptance_criteria>
    - `! grep -q "pdf.section.interests" src/lib/pdf/document.tsx` and
      `! grep -q "proposal.interests" src/lib/pdf/document.tsx` and `! grep -q "inputs.slb" src/lib/pdf/document.tsx`
      — D-05's deletion is complete in the render tree.
    - `grep -q "D-05" src/lib/pdf/document.tsx` — the deletion is recorded as a decision at the site.
    - `grep -q "marginTop: 'auto'" src/lib/pdf/document.tsx` (or, if the fallback was needed,
      `grep -q "flexGrow: 1 }} />"`), and `! grep -q "position: 'absolute'" src/lib/pdf/document.tsx`.
    - `grep -c "borderBottomColor: pdfColors.navy" src/lib/pdf/document.tsx` returns 4 — the four
      signature rules.
    - `grep -q "borderStyle: 'dashed'" src/lib/pdf/document.tsx` and `grep -q "pdfColors.stampBorder" src/lib/pdf/document.tsx`.
    - `grep -q "<LeaseticIcon" src/lib/pdf/document.tsx` and `grep -q "opacity={0.14}" src/lib/pdf/document.tsx`.
    - `grep -q "totalPages" src/lib/pdf/document.tsx` and `grep -q "fixed" src/lib/pdf/document.tsx` —
      the react-pdf page-number callback survived.
    - `! grep -q "{1} jours" src/lib/pdf/document.tsx` — the interpolation is a `.replace('{1}', ...)`
      call in code, not a literal.
    - `grep -q "String(inputs.validityDays)" src/lib/pdf/document.tsx` — DOC-06's actual value is used.
    - Rendered-text proof, FR fixture: the inventory contains `CONDITIONS`, `ACCEPTATION DU CLIENT`,
      `Fait à`, `Nom et qualité du signataire`, `Cachet de l’entreprise`, `SIREN 830 733 606`,
      `TVA FR06830733606` and `Page 1/1`. EN fixture: contains `CLIENT ACCEPTANCE`, `Place`,
      `Name and role of signatory`, `Company stamp`, `VAT FR06830733606`. Neither contains
      `Sale & leaseback` or `Évaluation de parc`.
    - DOC-06 proof: render the FR fixture with `validityDays: 15` and assert the extracted text
      contains `15 jours` and does NOT contain `30 jours`.
    - DOC-07 pinning proof: render two FR fixtures, one with `projectDesc: 'A'` and one with a
      ~400-character `projectDesc`; extract every `Tm` y-operand from each inflated page content
      stream and assert `Math.min(...ys)` is identical between the two, and that both render exactly
      one page. If the acceptance block were not pinned, the footer's baseline would move with the
      content and the minima would differ.
    - `npm run typecheck` exits 0; `npm run lint:check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm test -- src/lib/pdf/</automated>
  </verify>
  <done>The conditions paragraph interpolates the proposal's real validity period; the interests block is gone with a D-05 note; the acceptance block is pinned by `marginTop: 'auto'` and proven so by an identical footer baseline across a short and a long description; the legal footer carries both registration lines, the reference, the page number via react-pdf's callback, and the 14%-opacity mark.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the orphaned primitives and tokens (D-08, CI lint gate)</name>
  <files>src/lib/pdf/components/section-label.tsx, src/lib/pdf/components/key-value-row.tsx, src/lib/pdf/styles.ts</files>
  <read_first>
    - `src/lib/pdf/document.tsx` — the finished tree, to confirm what is genuinely unreferenced.
    - `src/lib/pdf/styles.ts` — the token set plan 43-01 left additive.
    - `src/lib/pdf/index.ts` — the public barrel, to confirm neither primitive is re-exported.
    - `.planning/phases/43-new-pdf-layout/43-CONTEXT.md` — D-08.
  </read_first>
  <action>
    Grep the whole repository (`src/`, `app/`, `tests/`, `__pdf-fixtures__/`, `scripts/`) for each of
    the following before removing it. Remove only what has zero remaining references:

    Delete `src/lib/pdf/components/section-label.tsx` and `src/lib/pdf/components/key-value-row.tsx`.
    Their consumers — the `PROJET` label (deleted in 43-05) and the computation-breakdown card
    (deleted in Task 1) — are gone. Remove their imports from `document.tsx`.

    In `styles.ts`, delete from `pdfFontSizes` the five legacy roles `footer`, `caption`, `body`,
    `title`, `loyer`, leaving exactly the ten design roles (D-10: "Today's five roles are replaced
    wholesale. Note 32pt disappears entirely"). Delete from `pdfColors` the tokens that lost their
    only consumer: `green` and `greenTint` (D-08 — the loyer hero is navy-outlined on white now),
    plus `ink`, `muted` and `border`, superseded by `navy`, `labelTeal` / `bodyBlue` and `hairline`.
    Keep `surface` (the `<Page>` background) and keep `pdfFontWeights` intact — the four registered
    weights are pinned by `tests/vendored-ui-integrity.test.ts` and must not be disturbed.

    If any grep finds a surviving reference, do not delete that item: record it in the SUMMARY with
    the file and line, and leave it. A silent deletion that breaks an unrelated consumer is worse
    than a documented leftover.
  </action>
  <acceptance_criteria>
    - `! test -e src/lib/pdf/components/section-label.tsx` and `! test -e src/lib/pdf/components/key-value-row.tsx`.
    - `! grep -rq "SectionLabel\|KeyValueRow" src/lib/pdf/ app/` (note `CardKeyValueRow` is a distinct
      identifier — verify the grep does not match it by using word boundaries).
    - `! grep -q "greenTint" src/lib/pdf/styles.ts` and `! grep -q "loyer: 32" src/lib/pdf/styles.ts`.
    - `grep -q "pdfFontWeights" src/lib/pdf/styles.ts` — the weight map is untouched.
    - `npm test -- tests/vendored-ui-integrity.test.ts` exits 0 — the four-weight registration guard
      is intact.
    - `npm run typecheck` exits 0 and `npm run lint:check` exits 0 — **this is the CI gate that
      unused imports and dead tokens would otherwise fail.**
    - `npm test -- src/lib/pdf/ tests/admin-09-grep-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm test -- src/lib/pdf/ tests/vendored-ui-integrity.test.ts tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <done>The two superseded primitives are deleted, `pdfFontSizes` holds exactly the ten design roles, the D-08 green tokens and the three superseded colour tokens are gone, the font-registration guard is untouched, and `eslint --max-warnings=0` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `computed` jsonb → hero and table | the financial figures a client acts on cross into the delivered document |
| design spec → legal copy | the conditions paragraph and the registration footer are legally-consequential text |
| flow layout → page count | an unpinned or overflowing block silently produces a second page the design does not support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06-01 | Information disclosure | commission leakage through the new hero/table nodes | mitigate | The table renders only `amountHT`, `durationMonths`, `coeff` (already a published figure on today's PDF), `loyerHT` and their product; no `commissionPct`, no `_pct` identifier, no derived commission. `no-commission.test.ts`'s four layers and the 20 `admin-09-grep-contracts` gates run as acceptance criteria |
| T-43-06-02 | Tampering | `Total des loyers HT` drifting on float rounding | mitigate | Rounded to the cent with an explicit `Math.round(x * 100) / 100` before `formatCurrency`; the value is asserted against the fixture's known product |
| T-43-06-03 | Repudiation | DOC-06 — a 15-day proposal displaying "30 jours" and contradicting its own `validUntil` | mitigate | `{1}` interpolates `inputs.validityDays`; a dedicated 15-day render asserts `15 jours` present and `30 jours` absent |
| T-43-06-04 | Denial of service | content overflow producing a second page the footer's `Page 1/1` contradicts | mitigate | Both the short- and long-description renders assert exactly one page; multi-page proposals are an explicit Deferred item, not a fix here |
| T-43-06-05 | Tampering | acceptance block not actually pinned, overlapping content | mitigate | `marginTop: 'auto'` (Yoga) rather than `position: 'absolute'`; the identical-footer-baseline assertion across a short and long description is the decisive proof |
| T-43-06-06 | Denial of service | deleting a token or primitive that still has a consumer | mitigate | Task 3 greps the whole tree before each deletion and records rather than forces any leftover; `typecheck` + `lint:check` + the full PDF suite gate the task |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No install. Gate: `git diff --stat package.json package-lock.json` empty at plan close |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint:check` exits 0
- `npm test -- src/lib/pdf/ tests/admin-09-grep-contracts.test.ts tests/vendored-ui-integrity.test.ts` exits 0
- **Expected transient failures, do not fix here:**
  - `__pdf-fixtures__/render-fixtures.test.ts` and `__pdf-fixtures__/commission-free-fixture.test.ts`
    — red since plan 43-01. D-16's single regeneration lands in plan 43-07. Do NOT run
    `npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE` here.
  - `__pdf-fixtures__/inter-typography.test.ts` — **expected to go red at this plan.** Its D-10 case
    asserts the FR and EN fixtures embed exactly four distinct Inter faces
    (`Inter-Regular` / `Medium` / `SemiBold` / `Bold`). The Claude Design layout uses only weights
    400 and 600, so the rewritten document embeds two. That is a correct consequence of the design,
    not a regression. Plan 43-07 owns the reconciliation. Record the observed face list in this
    plan's SUMMARY so 43-07 has the evidence.
</verification>

<success_criteria>
- The navy-outlined hero sits beside the five-row `CONDITIONS FINANCIÈRES` table with a bold total
- The total equals `monthlyRent × termMonths`, rounded to the cent
- The on-demand state renders the on-request string in the hero and em dashes for coefficient and total
- The conditions paragraph states the proposal's real `validityDays`
- The interests block no longer prints and its deletion is recorded as D-05 in source
- The acceptance block is pinned to the bottom, proven by an identical footer baseline across a short
  and a long description, and both render exactly one page
- The legal footer carries both registration lines, the reference, the page number and the
  14%-opacity mark
- Both superseded primitives and every orphaned token are gone; `eslint --max-warnings=0` passes
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-06-SUMMARY.md` when done.
</output>
