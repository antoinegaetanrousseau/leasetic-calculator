---
phase: 43-new-pdf-layout
plan: 13
type: execute
wave: 11
depends_on: [43-12]
files_modified: []
autonomous: false
gap_closure: true
requirements: [DOC-02]
must_haves:
  truths:
    - "The operator has confirmed, on ONE real finalized proposal, that a captured SIRET appears in the rendered PDF"
    - "The operator has confirmed the title no longer breaks mid-word in the language they finalized"
    - "The operator has confirmed the VOTRE CONTACT card reads as two distinct entities"
  artifacts:
    - path: ".planning/phases/43-new-pdf-layout/43-13-SUMMARY.md"
      provides: "the operator's verdict recorded verbatim, with no automated assertion substituted for it"
  key_links:
    - from: "a real finalized proposal"
      to: "the downloaded PDF's SOCIÉTÉ CLIENTE card"
      via: "the wizard finalize path, not a fixture render"
      pattern: "SIRET"
---

<objective>
Close the ONE `missing[]` bullet in `43-VERIFICATION.md` Gap 3 that no automated test can cover:

> "Operator confirmation on ONE real finalized proposal that a captured SIRET appears in the
> rendered PDF — the only path no automated test covers."

Every other bullet across the three gaps is closed by 43-09 through 43-12 with automated proof.
This one is different in kind: 43-12 proves the render path against fixtures, but no test exercises
the full chain of a human typing a SIRET into wizard step 1, finalizing, and opening the stored
PDF. The write paths are enforced (`finalize-wizard.ts:172` throws `LegacyDraftIncomplete`;
`submit.ts:77` refines the SIREN prefix) but the end-to-end capture-to-print path has never been
observed by a person.

Purpose: DOC-02 stays un-tickable on an assumption. 43-08-SUMMARY.md's key-decisions already record
that this phase declines to tick requirements on structural correctness alone — the D-15 human pass
is what caught both of the other two defects after two automated passes missed them.

Output: the operator's verdict recorded verbatim in `43-13-SUMMARY.md`.

**Scope boundary — what this checkpoint is NOT.** The `human_verification` entry in
`43-VERIFICATION.md`'s frontmatter (the field-by-field SOCIÉTÉ CLIENTE content audit against the
reference PNGs) was RESOLVED by the operator on 2026-09-09: *"Societe cliente card works."* Do NOT
re-ask it. This checkpoint asks only about a SIRET on a REAL finalized proposal, plus two quick
confirmations of the other two gap-closure fixes now that they are visible in the same document.

This plan modifies no source files and runs no automated gate. 43-12 already proved the suite, lint
gate and build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-new-pdf-layout/43-VERIFICATION.md
@.planning/phases/43-new-pdf-layout/43-08-SUMMARY.md
@.planning/phases/43-new-pdf-layout/43-12-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator confirmation — a captured SIRET prints on a real finalized proposal</name>
  <read_first>
    - .planning/phases/43-new-pdf-layout/43-VERIFICATION.md — Gap 3's final `missing[]` bullet, which is this checkpoint verbatim, and the `human_verification` block already marked RESOLVED (do not re-ask it)
    - .planning/phases/43-new-pdf-layout/43-12-SUMMARY.md — the regenerated baseline and the `.preview/*.pdf` byte sizes, so the agent can tell the operator what the fixtures already prove and what only a real proposal can
  </read_first>
  <what-built>
    Across plans 43-09 to 43-12 this gap closure shipped, with automated proof against fixtures:
    - `Font.registerHyphenationCallback((word) => [word])` in `src/lib/pdf/document.tsx`, so the
      title no longer breaks mid-word ("finan-cière" / "pro-posal"). `dehyphenate()` was deleted
      from `src/lib/pdf/layout.test.ts` and replaced with a guard that was demonstrated to fail
      when the callback is removed.
    - The VOTRE CONTACT card rebuilt to the 7-row structure Antoine specified verbatim: the partner
      name as headline 1, then `Partenaire` and `Téléphone` rows, then the advisor name as headline
      2, then `Fonction`, `Téléphone` and `Email`. `pdf.card.contact.salesRep` and
      `pdf.card.contact.advisorName` deleted; `pdf.card.contact.partner` ("Partenaire" / "Partner")
      added.
    - `partnerCo` sourced from `users.companyName` alone at both hydration sites, so an account with
      no company name now renders an em dash beside `Partenaire` instead of a person's name.
    - `clientSiret` added to the two happy-path fixtures with a positive render assertion; the
      byte-determinism baseline regenerated once.

    What the fixtures CANNOT prove, and why this checkpoint exists: no automated test walks the full
    chain of a human typing a SIRET into wizard step 1, finalizing, and opening the stored PDF.
  </what-built>
  <how-to-verify>
    Before pausing, the executing agent must:
    1. Confirm the dev server can be started (`npm run dev`) and give the operator the exact URL for
       the wizard's step 1, plus a reminder that a PARTNER login is required — `/clients` and the
       proposal wizard refuse admin accounts.
    2. Tell the operator which login to use, or ask which one they will use, and note that the
       partner's account must have a `companyName` if they want to see a real company under
       `Partenaire` (an account without one will correctly show an em dash — that is the fix, not a
       bug).
    3. Confirm no database migration has been run at any point in this gap closure.

    Then pause and ask the operator to do exactly this:

    1. Log in as a partner and start a new proposal.
    2. On step 1, enter a client with a real SIREN and its matching SIRET (the first 9 digits of the
       SIRET must equal the SIREN, or the form will reject it).
    3. Complete steps 2 and 3 and finalize the proposal.
    4. Download or open the generated PDF from the proposal detail page.
    5. Answer these three questions:
       - (a) **SIRET (the required item).** In the `SOCIÉTÉ CLIENTE` card, does the `SIRET` row show
         the 14-digit number you typed, rather than an em dash?
       - (b) **Title.** Does the title read cleanly, with no hyphen splitting a word across the line
         break? (French: "Proposition de location financière". English: "Equipment lease financing
         proposal".)
       - (c) **VOTRE CONTACT card.** Is it now unambiguous who is the partner and who is the
         Leasetic advisor? You should see your name as a headline, then a row labelled `Partenaire`
         with your company (or an em dash if your account has no company name), then a `Téléphone`
         row, then the advisor's name as a second headline, then `Fonction`, `Téléphone` and
         `Email`.

    Record the answers VERBATIM in `43-13-SUMMARY.md`. Do not paraphrase, do not substitute a test
    result for an answer, and do not mark the item confirmed on the strength of 43-12's fixture
    assertions — that substitution is precisely the pattern that let Gap 1 ship twice.
  </how-to-verify>
  <action>
    Present the verification steps above to the operator, wait for their answers, and record them
    verbatim in the SUMMARY under a "Operator verdict — recorded verbatim" heading.

    If (a) is NO — the SIRET row shows an em dash on a real finalized proposal — then Gap 3 IS a live
    defect after all, contrary to the verification report's read of the write paths. Do NOT attempt a
    fix in this plan. Record the operator's exact wording, the proposal reference, whether the
    proposal was created via the wizard finalize path or the `submit.ts` path, and stop. That is a
    new gap requiring its own plan.

    If (b) or (c) is NO, record the operator's exact wording and stop the same way. Both are covered
    by automated guards as of 43-09 and 43-10, so a NO means a guard is asserting the wrong thing —
    which is information worth more than a quick patch.

    If all three are YES, record that, and note in the SUMMARY that Gap 3's final bullet is closed
    and that Phase 44's backfill is no longer blocked by this phase's open defects.
  </action>
  <verify>
    <automated>MISSING — this item is human-only by construction. 43-VERIFICATION.md Gap 3's final missing[] bullet names it as "the only path no automated test covers". The fixture-level proof is already automated in 43-12 (src/lib/pdf/layout.test.ts, DOC-02 describe block); what remains is a real finalized proposal, which requires a partner login, a browser and a human.</automated>
    <human-check>The operator's verbatim answers to questions (a), (b) and (c) are recorded in 43-13-SUMMARY.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `43-13-SUMMARY.md` contains a heading "Operator verdict — recorded verbatim" followed by the operator's own words, quoted, for each of (a), (b) and (c)
    - The SUMMARY states which language the confirmed proposal was finalized in
    - The SUMMARY states whether the confirming account had a `companyName` set, so the `Partenaire` row's em dash or value is interpretable
    - No source file is modified by this plan: `git status --porcelain -- src app __pdf-fixtures__ scripts` produces no output
    - No database migration was run at any point
    - If any answer is NO, the SUMMARY records it as an OPEN gap with the operator's exact wording and does NOT mark DOC-02 complete
  </acceptance_criteria>
  <resume-signal>Answer (a), (b) and (c) — or type "approved" if all three are yes.</resume-signal>
  <done>The operator's verdict on a real finalized proposal is recorded verbatim, with no automated result substituted for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real client data → a real finalized proposal | This checkpoint asks the operator to type a real SIREN/SIRET and finalize. The resulting proposal and PDF persist in the production-adjacent environment. |
| operator verdict → requirement completion | A verdict is the sole evidence for DOC-02's end-to-end path. Paraphrasing or substituting a test result corrupts that evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-13-01 | Information disclosure | a real client's SIREN/SIRET entered for the test | accept | SIREN and SIRET are public French company registry identifiers, not personal or secret data. The operator may use his own companies' identifiers. The proposal is created by the operator in his own environment and is his to delete. |
| T-43-13-02 | Repudiation | the operator verdict | mitigate | The action requires the answers be recorded VERBATIM under a named heading, and forbids substituting an automated result. The failure branch is spelled out so a NO cannot be quietly reinterpreted as a YES. |
| T-43-13-03 | Tampering | production database | mitigate | This plan runs no migration and no `db:migrate`. It only starts the dev server and asks a human to use the app through its normal UI. |
| T-43-13-04 | Elevation of privilege | login used for the walk | mitigate | The instructions require a PARTNER login; the wizard and `/clients` refuse admin accounts. No credential is written into any planning artifact. |
| T-43-13-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. |
</threat_model>

<verification>
- The operator has answered (a), (b) and (c), and the answers are recorded verbatim.
- No source file, fixture or baseline changed.
- No database command was run.
</verification>

<success_criteria>
- Gap 3's final `missing[]` bullet is either closed by an operator YES, or re-opened as a new, precisely-worded gap by an operator NO.
- The verdict is recorded verbatim, not summarised.
- DOC-02 is ticked only if the operator confirmed it on a real finalized proposal.
</success_criteria>

<output>
Create `.planning/phases/43-new-pdf-layout/43-13-SUMMARY.md` when done.
</output>
