---
phase: 16-shell-refresh-contrast-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/RetractableSidebar.tsx
  - src/components/Topbar.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-04
requirements_addressed:
  - SHELL-01
  - SHELL-02
  - SHELL-04
tags:
  - shell
  - sidebar
  - topbar
  - icon-sizing
  - light-dark
  - verify-only

must_haves:
  truths:
    - "Sidebar nav icons render at 20×20 (lucide-react size={20}) in both expanded and collapsed mode, per D-12."
    - "Sidebar nav inactive background stays 'transparent' (not literal 'white') with a code comment explaining cross-theme correctness, per D-13."
    - "Sidebar collapsed-mode theme cycle button icons (Sun / Monitor / Moon) render at 16×16 (size={16}), per D-14."
    - "Sidebar expanded-mode <ThemeToggle> is untouched, per D-14."
    - "Sidebar collapsed-mode brand-row to first nav icon gap renders at 24px effective, per D-15 (verified visually; adjusted only if measurement fails)."
    - "Topbar renders the Figma 9:46 visual treatment unchanged (16.5px/600 ink page title, 9px/700 white-on-navy ADMIN pill, 64px height, sticky, --surface bg, 1px border-bottom --border), per D-16."
    - "Topbar verification comment added inline once confirmed: `// PHASE 16: verified visual match to Figma 9:46 on 2026-05-22`."
    - "Tri-state ThemeToggle (Light / System / Dark) and FR/EN LocaleToggle remain mounted in sidebar footer (verify-only, SHELL-02 + SHELL-04 — already shipped in Plan 11-04)."
    - "Light + dark pair: every sidebar/topbar surface uses --surface / --border / --ink / --muted / --gd tokens; no new dark-mode CSS introduced."
    - "ADMIN-09 9-gate grep-contract suite stays green throughout (no commission strings introduced)."
  artifacts:
    - path: "src/components/ui/RetractableSidebar.tsx"
      provides: "Sidebar with D-12 (size={20} nav icons) + D-13 (transparent inactive bg + comment) + D-14 (size={16} collapsed theme cycle icons)"
      contains: "size={20}"
    - path: "src/components/Topbar.tsx"
      provides: "Topbar visually verified for Figma 9:46 match; verification comment added"
      contains: "PHASE 16: verified"
  key_links:
    - from: "src/components/ui/RetractableSidebar.tsx"
      to: "lucide-react Icon size prop"
      via: "size={20} for nav icons, size={16} for collapsed theme cycle"
      pattern: "size=\\{(20|16)\\}"
---

<objective>
Apply the micro visual deltas to the refreshed v1.3 sidebar shell and verify the topbar's Figma `9:46` treatment. This plan covers SHELL-01 (sidebar 72/260 variants — already shipped in Plan 11-04; Phase 16 ships only D-12/D-13/D-14/D-15 micro touch-ups), SHELL-02 (tri-state theme toggle — verify-only since Plan 11-04 already shipped Light/System/Dark via cookie + no-flash script), SHELL-04 (FR/EN locale in sidebar footer — verify-only since Plan 11-04 already relocated `<LocaleToggle>`), and the topbar half of SHELL-05 (D-16 verify-only).

Purpose: Bring the existing sidebar to exact Figma `23:46` icon-size compliance and confirm the topbar already matches `9:46` so no further visual debt enters Phase 17/18.
Output: Two source files modified (RetractableSidebar code change for D-12/D-13/D-14, optional D-15 fix; Topbar inline verification comment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md
@.planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md

<interfaces>
<!-- D-12, D-13, D-14, D-15 micro-deltas from CONTEXT.md + UI-SPEC §RetractableSidebar micro-deltas. -->
<!-- Current code lines (from grep on RetractableSidebar.tsx 2026-05-22): -->

```ts
// Nav icon render path (line ~304 inside navItems.map):
<Icon size={18} strokeWidth={1.6} color={isActive ? 'var(--gd)' : 'var(--muted)'} />

// Nav itemStyle background (D-13):
background: isActive ? 'rgba(18, 150, 87, 0.10)' : 'transparent'

// Collapsed theme cycle button icons (lines ~358-360):
{theme === 'light' && <Sun size={17} strokeWidth={1.6} />}
{theme === 'system' && <Monitor size={17} strokeWidth={1.6} />}
{theme === 'dark' && <Moon size={17} strokeWidth={1.6} />}
```

<!-- D-16: Topbar verify-only. Expected zero functional change. -->
<!-- D-15: Collapsed brand-row gap should be 24px effective. Inspect; adjust ONLY if needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-12 / D-13 / D-14 / D-15 sidebar micro-deltas in RetractableSidebar.tsx</name>
  <files>src/components/ui/RetractableSidebar.tsx</files>
  <read_first>
    - src/components/ui/RetractableSidebar.tsx (full file — required to see current sizes at line ~304 and lines ~358-360, current background pattern, current collapsed-mode &lt;ul&gt; spacing)
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decisions D-12, D-13, D-14, D-15; specifics block at &lt;specifics&gt;)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (Component Contracts > RetractableSidebar micro-deltas)
    - src/components/ui/RetractableSidebar.test.tsx (existing test coverage — confirm no test asserts the previous `size={18}` or `size={17}` value; if any test does, plan a test update)
  </read_first>
  <action>
    Apply four targeted edits to `src/components/ui/RetractableSidebar.tsx`:

    1. **D-12 nav icon size 18→20**: Locate the `<Icon size={18} strokeWidth={1.6} color={isActive ? 'var(--gd)' : 'var(--muted)'} />` line inside the `navItems.map` render path (line ~304). Change `size={18}` to `size={20}`. Do NOT change `strokeWidth={1.6}` or the color expression. This single edit covers all 8 nav items (4 partner + 4 admin) since they all share this render path.

    2. **D-13 inactive nav background — add a clarifying inline comment, do NOT change behavior**: Locate the line containing `background: isActive ? 'rgba(18, 150, 87, 0.10)' : 'transparent'`. Leave the value `'transparent'` unchanged. ADD an inline comment immediately above or beside the line: `// D-13: transparent (not literal 'white') — cross-theme correct; sidebar bg is --surface so transparent renders equivalently in light, while explicit white would break dark mode.` Use a multiline `//` comment block if needed; place it directly above the `background:` property in the itemStyle object.

    3. **D-14 collapsed theme cycle icons 17→16**: Locate the three lines `{theme === 'light' && <Sun size={17} strokeWidth={1.6} />}`, `{theme === 'system' && <Monitor size={17} strokeWidth={1.6} />}`, `{theme === 'dark' && <Moon size={17} strokeWidth={1.6} />}`. Change `size={17}` to `size={16}` on all three. Do NOT touch the `<ThemeToggle current={theme} fullWidth />` line in the expanded-mode branch (D-14 explicitly preserves expanded-mode sizing).

    4. **D-15 brand-row to first nav icon gap (collapsed) — INSPECT FIRST**: Read the file to identify the spacing between the brand row and the first nav `<li>` in collapsed mode (look for the `<ul>` element rendering `navItems.map` and any margin/padding on it, or on a sibling spacer/eyebrow div above). If the rendered collapsed-mode gap is already 24px effective (e.g. via inherited padding/margin), do nothing and document the verification with an inline comment near the `<ul>`: `// D-15: collapsed-mode brand-row to first nav gap verified at 24px effective (2026-05-22)`. If the gap renders less (e.g. 16px or 0), add `marginTop: 24` on the `<ul>` in collapsed mode only (gated by the `collapsed` boolean conditional), with an inline comment `// D-15: marginTop 24 only in collapsed mode to match Figma 23:46 24px gap spacer`. Do NOT modify expanded-mode `<ul>` margin.

    Preserve all other code: the `useSyncExternalStore` collapse-state hook, the `cycleTheme` / `cycleLang` handlers, the `<LocaleToggle>` / `<ThemeToggle>` mounts at the expanded-footer block, the 18px collapsed-sidebar left/right padding, the 10px nav item top/bottom padding, and the inherited brand-row `marginTop: 24` on the eyebrow div (expanded mode). These are Phase 11 inherited values explicitly listed in UI-SPEC §Spacing > Exceptions as "not changed by Phase 16".
  </action>
  <verify>
    <automated>grep -c "size={20}" src/components/ui/RetractableSidebar.tsx; grep -c "size={16}" src/components/ui/RetractableSidebar.tsx; grep -ci "size={18}\|size={17}" src/components/ui/RetractableSidebar.tsx; npm run test -- --run src/components/ui/RetractableSidebar.test.tsx 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "size={20}" src/components/ui/RetractableSidebar.tsx` returns at least 1 (the nav icon render line now uses 20).
    - `grep -c "size={16}" src/components/ui/RetractableSidebar.tsx` returns at least 3 (the three Sun/Monitor/Moon icons in collapsed theme cycle).
    - `grep -c "size={18}" src/components/ui/RetractableSidebar.tsx` returns 0 (old nav-icon size removed; if any size={18} usage exists elsewhere unrelated to D-12, document it in the SUMMARY with rationale — but per UI-SPEC the nav icon line is the only `size={18}`).
    - `grep -c "size={17}" src/components/ui/RetractableSidebar.tsx` returns 0 (old collapsed theme cycle size removed).
    - The file contains the D-13 explanatory comment: `grep -c "D-13:" src/components/ui/RetractableSidebar.tsx` returns at least 1.
    - The file contains either a D-15 verification comment OR a D-15 fix comment: `grep -c "D-15:" src/components/ui/RetractableSidebar.tsx` returns at least 1.
    - The literal `'transparent'` background for inactive nav stays present: `grep -c "'transparent'" src/components/ui/RetractableSidebar.tsx` returns at least 1.
    - Expanded-mode `<ThemeToggle current={theme} fullWidth />` mount is preserved: `grep -c "ThemeToggle current={theme} fullWidth" src/components/ui/RetractableSidebar.tsx` returns 1.
    - Expanded-mode `<LocaleToggle current={lang} fullWidth />` mount is preserved: `grep -c "LocaleToggle current={lang} fullWidth" src/components/ui/RetractableSidebar.tsx` returns 1.
    - No commission strings introduced: `grep -ci "commission" src/components/ui/RetractableSidebar.tsx` returns 0.
    - Existing RetractableSidebar tests still pass: `npm run test -- --run src/components/ui/RetractableSidebar.test.tsx` exits 0.
    - The 9-gate grep-contract suite stays green: `npm run test -- --run tests/admin-09-grep-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>RetractableSidebar.tsx contains size={20} nav icons + size={16} collapsed theme cycle icons + D-13 comment + D-15 verification-or-fix comment; existing tests still pass; 9-gate suite green.</done>
</task>

<task type="auto">
  <name>Task 2: D-16 Topbar visual verification + inline comment</name>
  <files>src/components/Topbar.tsx</files>
  <read_first>
    - src/components/Topbar.tsx (full file — to confirm current visual treatment matches D-16 contract: 16.5px/600 ink page title with ellipsis at 60% max-width, 9px/700 navy/white ADMIN pill, 64px sticky height via var(--topbar-h), --surface bg, 1px border-bottom var(--border), UserMenu right-aligned via flex:1 spacer)
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decision D-16; CONTEXT specifies "If planner finds a visual delta during inspection, treat as planner discretion and document in the plan.")
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (Component Contracts > Topbar verify-only subsection — full visual contract enumerated)
  </read_first>
  <action>
    Read `src/components/Topbar.tsx` end-to-end and check each visual rule against the D-16 contract enumerated in UI-SPEC §Component Contracts > Topbar verify-only:

    1. Page title element has inline style with `fontSize: '16.5px'`, `fontWeight: 600`, `color: 'var(--ink)'`, ellipsis truncation (`overflow: 'hidden'`, `whiteSpace: 'nowrap'`, `textOverflow: 'ellipsis'`), `maxWidth: '60%'`.
    2. ADMIN pill (if present in source) has `fontSize: '9px'`, `fontWeight: 700`, `background: 'var(--navy)'`, `color: '#ffffff'`.
    3. Container has `height: 'var(--topbar-h)'`, `background: 'var(--surface)'`, `borderBottom: '1px solid var(--border)'`, sticky positioning with `top: 0` and `zIndex: 100`.
    4. UserMenu is right-aligned via a `flex: 1` spacer element between the title block and UserMenu.

    **If ALL FOUR rules match the source**, add a single-line inline comment immediately below the file's top-level imports (before the `export default function Topbar` or `export function Topbar`): `// PHASE 16: verified visual match to Figma 9:46 on 2026-05-22 (D-16). Zero functional change.` and STOP. No other code change.

    **If ANY rule does NOT match**, do NOT add the verification comment. Instead, apply the minimum patch to bring that rule to the contract value (per D-16 planner discretion), document the delta in the eventual 16-02-SUMMARY.md (which task scope = "Topbar minor visual correction per D-16 discretion: previous value `X`, new value `Y`, rationale: matches Figma 9:46"), then add the verification comment with the correction noted: `// PHASE 16: corrected and verified to match Figma 9:46 on 2026-05-22 (D-16). Delta: <one-line description>.`

    Do NOT introduce new dependencies. Do NOT change UserMenu's internal behavior. Do NOT remove the existing ADMIN pill if present.
  </action>
  <verify>
    <automated>grep -c "PHASE 16: verified\|PHASE 16: corrected" src/components/Topbar.tsx; npm run test -- --run 'src/components/Topbar' 2>&amp;1 | tail -5 || echo "no Topbar tests"; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Topbar.tsx` contains a PHASE 16 verification comment: `grep -c "PHASE 16: verified\|PHASE 16: corrected" src/components/Topbar.tsx` returns at least 1.
    - The visual treatment matches D-16 contract: the file contains `'16.5px'`, `'9px'` (if ADMIN pill present), `'var(--topbar-h)'`, `'var(--surface)'`, `'var(--ink)'`, `'var(--navy)'` (if ADMIN pill present) substrings (verify via `grep -c` on each).
    - No commission strings introduced: `grep -ci "commission" src/components/Topbar.tsx` returns 0.
    - Project type-check passes after the change: `npx tsc --noEmit` exits 0.
    - `npm run build` succeeds (no build-time regression from the comment or any optional D-16 micro-correction).
  </acceptance_criteria>
  <done>Topbar.tsx carries a PHASE 16 verification comment; any minor D-16 discretion-fix is recorded; build passes; 9-gate suite green.</done>
</task>

</tasks>

<verification>
- `grep -c "size={20}" src/components/ui/RetractableSidebar.tsx` → ≥1
- `grep -c "size={16}" src/components/ui/RetractableSidebar.tsx` → ≥3
- `grep -c "size={18}\|size={17}" src/components/ui/RetractableSidebar.tsx` → 0
- `grep -c "PHASE 16: " src/components/Topbar.tsx` → ≥1
- `npm run test -- --run src/components/ui/RetractableSidebar.test.tsx` passes
- `npm run test -- --run tests/admin-09-grep-contracts.test.ts` passes (9-gate suite stays green)
- `npm run build` passes
- SHELL-02 verify-only: `grep -c "setTheme\|'system'" src/components/ThemeToggle.tsx` returns ≥1 (existing tri-state behavior preserved; no Phase 16 modification)
- SHELL-04 verify-only: `grep -c "LocaleToggle" src/components/ui/RetractableSidebar.tsx` returns ≥1 (existing footer mount preserved)
</verification>

<success_criteria>
- RetractableSidebar nav icons render at 20px (D-12); inactive bg stays transparent with clarifying comment (D-13); collapsed theme cycle icons render at 16px (D-14); collapsed brand-row gap verified at 24px effective (D-15).
- Topbar carries an inline PHASE 16 verification comment (D-16); zero or minimal source change per D-16 planner discretion.
- SHELL-02 (tri-state Light/System/Dark) and SHELL-04 (FR/EN locale in sidebar footer) verified intact from Plan 11-04 — no Phase 16 modification.
- ADMIN-09 9-gate grep-contract suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shell-refresh-contrast-gates/16-02-SUMMARY.md` recording: exact line numbers edited in RetractableSidebar.tsx, whether D-15 required a code fix or only a verification comment, the result of the D-16 Topbar inspection (pure verification vs. minor correction with delta description), and confirmation that the 9-gate suite stayed green.
</output>
