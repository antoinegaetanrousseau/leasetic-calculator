---
phase: 16-shell-refresh-contrast-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/PageHero.tsx
  - src/components/ui/PageHero.test.tsx
autonomous: true
requirements:
  - SHELL-03
requirements_addressed:
  - SHELL-03
tags:
  - shell
  - design-system
  - primitive
  - server-component
  - light-dark

must_haves:
  truths:
    - "PageHero is a pure-presentational server component (no 'use client'), per D-01."
    - "PageHero accepts exactly 5 props: title (required string), subtitle (optional string), eyebrow (optional string), actions (optional ReactNode), children (optional ReactNode, reserved/unused in Phase 16), per D-01."
    - "Title renders at 32px / weight 700 / line-height 1.2 / color var(--ink) / margin 0, per D-02."
    - "Subtitle renders at 14.5px / weight 400 / line-height 1.55 / color var(--muted) / marginTop 8, per D-02."
    - "Eyebrow renders at 11.8px / weight 700 / letter-spacing 0.06em / uppercase / color var(--gd) / marginBottom 8, per D-02."
    - "Outer container is flex with justify-content space-between, align-items flex-start, marginBottom 32, per D-03 and D-04."
    - "When actions is absent, no right-slot element renders; the left column expands naturally, per D-03."
    - "PageHero light/dark behavior is driven exclusively by --ink, --muted, --gd token cascade (no per-component dark CSS), per D-05."
    - "PageHero exposes NO className escape-hatch prop, per Claude's Discretion in CONTEXT.md."
    - "ADMIN-09 invariant: PageHero contains zero commission-related literals or data access; the Phase 14 9-gate grep-contract suite stays green."
  artifacts:
    - path: "src/components/ui/PageHero.tsx"
      provides: "PageHero server component with 5-prop interface (title, subtitle, eyebrow, actions, children)"
      exports: ["PageHero", "PageHeroProps"]
    - path: "src/components/ui/PageHero.test.tsx"
      provides: "Vitest coverage of 7 cases: title-only, +subtitle, +eyebrow, +actions, all-four, light snapshot, dark snapshot"
      min_lines: 80
  key_links:
    - from: "src/components/ui/PageHero.tsx"
      to: "var(--ink), var(--muted), var(--gd)"
      via: "inline style color tokens"
      pattern: "var\\(--(ink|muted|gd)\\)"
    - from: "src/components/ui/PageHero.test.tsx"
      to: "src/components/ui/PageHero.tsx"
      via: "import { PageHero } from './PageHero'"
      pattern: "import.*PageHero.*from.*['\"]\\./PageHero['\"]"
---

<objective>
Ship the `<PageHero>` primitive (D-01..D-05) at `src/components/ui/PageHero.tsx` with full Vitest coverage. This is the foundation for SHELL-03 (every authed page renders the hero pattern). Phase 17 and Phase 18 will adopt it on partner home, wizard steps, /partners, /partners/new, /coefficients, and /history; Phase 16 ships only the primitive itself (this plan) plus one reference adopter on admin home (Plan 16-04).

Purpose: Provide a single source of truth for hero typography, spacing, and light/dark behavior so downstream phases never re-decide hero visual rules.
Output: One new server component + one colocated Vitest file covering all prop combinations and both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md
@.planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md

<interfaces>
<!-- The exact PageHero prop shape locked by D-01 (CONTEXT.md) and UI-SPEC §Component Contracts. -->
<!-- The executor MUST implement this interface verbatim. -->

```ts
// src/components/ui/PageHero.tsx
export interface PageHeroProps {
  title: string;                   // Required. The greeting or page title (e.g. "Bonjour, Antoine 👋", "Administration").
  subtitle?: string;               // Optional. Muted description line below the title.
  eyebrow?: string;                // Optional. Uppercase label above the title (e.g. "ADMIN", "ÉTAPE 1 SUR 3").
  actions?: React.ReactNode;       // Optional. Right-aligned page-level CTA slot.
  children?: React.ReactNode;      // Reserved for future composition; not consumed in Phase 16.
}
```

<!-- Pattern reference: existing Phase 11 server-component primitives use inline style={{}} with token-based colors. -->
<!-- Analog file to mirror: src/components/ui/MetricTile.tsx (server component, inline styles, token colors). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement &lt;PageHero&gt; primitive at src/components/ui/PageHero.tsx</name>
  <files>src/components/ui/PageHero.tsx</files>
  <read_first>
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decisions D-01..D-05, D-07)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (Component Contracts > PageHero subsection)
    - src/components/ui/MetricTile.tsx (analog server-component primitive — mirror pattern: no 'use client', inline style={{}}, token-based colors via var(--*))
    - src/components/ui/AdminNavCard.tsx (analog server-component primitive — confirm export style: `export function ComponentName(...)` and named PropsExport)
    - app/globals.css (verify --ink, --muted, --gd tokens are present in :root and html[data-theme="dark"] blocks — read-only)
  </read_first>
  <behavior>
    - Renders only title when title is the only prop provided.
    - Renders title + subtitle (eyebrow and actions slots absent) when subtitle is provided.
    - Renders eyebrow above title when eyebrow is provided (uppercase, brand-green, mb 8).
    - Renders actions on the right side of the flex container when actions is provided.
    - When actions is undefined OR null OR an empty fragment, the right slot does NOT render (no empty div added to DOM).
    - title element is an &lt;h1&gt; (semantic hero heading).
    - subtitle element is a &lt;p&gt;.
    - eyebrow element is a &lt;div&gt; with role="presentation" attribute (or a &lt;p&gt; — match Phase 11 precedent if any; default to &lt;div&gt;).
    - All colors come from CSS custom properties (var(--ink), var(--muted), var(--gd)) — no hardcoded hex.
    - No 'use client' directive at the top of the file (server component, D-01).
  </behavior>
  <action>
    Create `src/components/ui/PageHero.tsx` as a SERVER COMPONENT (no 'use client'; D-01). Export the named `PageHeroProps` interface verbatim from the &lt;interfaces&gt; block above (5 props: title required string, subtitle optional string, eyebrow optional string, actions optional ReactNode, children optional ReactNode). Export `function PageHero({ title, subtitle, eyebrow, actions }: PageHeroProps)` with this layout (D-03, D-04):

    Outer element: `<div>` with inline style {display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 32}.

    Left column: `<div>` with inline style {display: 'flex', flexDirection: 'column'}. Inside this column, render conditionally:
      1. eyebrow (if provided): `<div>` with inline style {fontSize: '11.8px', fontWeight: 700, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--gd)', marginBottom: 8} containing the eyebrow text (D-02).
      2. title (always rendered): `<h1>` with inline style {fontSize: 32, fontWeight: 700, lineHeight: 1.2, color: 'var(--ink)', margin: 0} containing the title text (D-02).
      3. subtitle (if provided): `<p>` with inline style {fontSize: '14.5px', fontWeight: 400, lineHeight: 1.55, color: 'var(--muted)', marginTop: 8, marginBottom: 0} containing the subtitle text (D-02).

    Right slot: render `actions` only when truthy (use a short-circuit guard like `{actions && <div>{actions}</div>}` wrapping actions in a `<div>` so the flex layout treats it as a single child; the wrapper has no explicit styling besides what flex provides). When actions is falsy, do NOT render the wrapper div (D-03).

    Do NOT add a className prop. Do NOT consume `children` (D-01 reserves it; UI-SPEC §Component Contracts). Do NOT introduce any global CSS — inline style only (matches Phase 11 precedent per UI-SPEC §Established Patterns).

    Add a top-of-file JSDoc block citing the source: `/** PageHero — v1.3 hero primitive (Phase 16, D-01..D-05). Server component. Used on every authed page (one reference adopter in Phase 16: admin home). Phase 17/18 expand consumers. */`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "PageHero|TS[0-9]+" || echo "tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/PageHero.tsx` exists and exports both `PageHero` (function) and `PageHeroProps` (interface): `grep -c "export function PageHero\|export interface PageHeroProps" src/components/ui/PageHero.tsx` returns at least 2.
    - File does NOT begin with `'use client'`: `head -2 src/components/ui/PageHero.tsx | grep -c "use client"` returns 0.
    - File uses inline-style token colors: `grep -c "var(--ink)\|var(--muted)\|var(--gd)" src/components/ui/PageHero.tsx` returns at least 3.
    - File contains exactly the prop names from the interface (title, subtitle, eyebrow, actions, children): `grep -c "title:\|subtitle?:\|eyebrow?:\|actions?:\|children?:" src/components/ui/PageHero.tsx` returns at least 5.
    - File contains the exact pixel values from D-02: `grep -c "32\|'11.8px'\|'14.5px'\|0.06em\|1.55\|1.2" src/components/ui/PageHero.tsx` returns at least 5.
    - File contains `marginBottom: 32` (D-04 spacing): `grep -c "marginBottom: 32" src/components/ui/PageHero.tsx` returns at least 1.
    - No commission-related strings: `grep -ci "commission" src/components/ui/PageHero.tsx` returns 0.
    - `npx tsc --noEmit` exits 0 (no new TypeScript errors).
  </acceptance_criteria>
  <done>PageHero.tsx exists as a server component with the locked 5-prop interface and pixel-exact D-02 typography rules; tsc passes; no commission leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest coverage at src/components/ui/PageHero.test.tsx</name>
  <files>src/components/ui/PageHero.test.tsx</files>
  <read_first>
    - src/components/ui/PageHero.tsx (the just-created primitive; verify exports and behavior)
    - src/components/ui/MetricTile.test.tsx (analog test file — mirror the import style, afterEach(cleanup) pattern, describe/it nesting, getByRole/within usage)
    - src/components/ui/AdminNavCard.test.tsx (analog test — confirm snapshot-style tests vs. assertion-style; PageHero tests should be assertion-style for snapshots 6 and 7 too, no toMatchSnapshot file)
    - __tests__/setup-dom.ts (verify jsdom + @testing-library/jest-dom is loaded — referenced in vitest.config.ts setupFiles)
    - vitest.config.ts (confirm `include` glob covers `src/**/*.test.tsx`)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (Component Contracts > PageHero > Test file — 7 required cases)
  </read_first>
  <behavior>
    - Test 1 (title-only): renders &lt;h1&gt; with the title text; no &lt;p&gt; (subtitle) in the DOM; no element with uppercase letter-spacing 0.06em (no eyebrow); no right-slot wrapper.
    - Test 2 (title + subtitle): &lt;h1&gt; present with title; &lt;p&gt; present with subtitle text; eyebrow absent.
    - Test 3 (title + subtitle + eyebrow): eyebrow element present with text-transform uppercase and color var(--gd) in its inline style; eyebrow text matches input.
    - Test 4 (title + actions): right-slot wrapper present with the actions ReactNode; subtitle absent; eyebrow absent. Use a sentinel React node (e.g. `<button data-testid="hero-cta">Action</button>`) to assert the wrapper rendered the actions.
    - Test 5 (all four props): all elements present; assert relative DOM order eyebrow → title → subtitle within the left column.
    - Test 6 (light snapshot): render PageHero with all 4 props inside a `<div data-theme="light">` wrapper; assert via getByRole/getByText that the title, subtitle, eyebrow text are present and the actions sentinel is present. No real CSS resolution — the test asserts presence + computed inline-style attributes.
    - Test 7 (dark snapshot): render PageHero with all 4 props inside a `<div data-theme="dark">` wrapper; assert wrapper has attribute data-theme="dark" and the same presence assertions as Test 6.
    - cleanup() is called in afterEach (matches MetricTile.test.tsx pattern).
  </behavior>
  <action>
    Create `src/components/ui/PageHero.test.tsx` mirroring the import/test structure of `src/components/ui/MetricTile.test.tsx`. Import order: `import { afterEach, describe, expect, it } from 'vitest'; import { cleanup, render, within } from '@testing-library/react'; import { PageHero } from './PageHero';` then `afterEach(() => cleanup());` then a single `describe('PageHero', () => { ... })` block.

    Add the 7 test cases enumerated above. Naming convention: `AC-PH-01` through `AC-PH-07` (matches MetricTile's `AC-MT-NN` convention). Each test:
      - AC-PH-01: title only — `render(<PageHero title="Hello" />)`, assert `getByRole('heading', { level: 1 })` has text "Hello"; assert `container.querySelectorAll('p').length === 0`; assert no element has style containing `letter-spacing: 0.06em`.
      - AC-PH-02: title + subtitle — assert `<h1>` + `<p>` both present with respective text; no eyebrow.
      - AC-PH-03: title + subtitle + eyebrow — assert an element whose inline-style attribute matches `/text-transform:\s*uppercase/` AND `/color:\s*var\(--gd\)/` is present with the eyebrow text. Use `container.querySelector` with attribute matching, or iterate elements and check `getAttribute('style')`.
      - AC-PH-04: title + actions — `render(<PageHero title="X" actions={<button data-testid="hero-cta">Go</button>} />)`, assert `getByTestId('hero-cta')` is present; assert no subtitle &lt;p&gt;.
      - AC-PH-05: all 4 props — assert DOM order via `container.querySelectorAll('*')` traversal of the left column (eyebrow div before h1 before p).
      - AC-PH-06: light wrapper — render inside `<div data-theme="light">`, assert all 4 elements present (eyebrow, title, subtitle, actions sentinel). Title's style attribute matches `/color:\s*var\(--ink\)/`.
      - AC-PH-07: dark wrapper — render inside `<div data-theme="dark">`, assert wrapper has `data-theme="dark"` attribute and same 4 elements present. (Note: jsdom does not resolve CSS variables; this test asserts the token reference, not the resolved color — D-05 says cascade is the mechanism, the test proves the tokens are referenced.)

    Do NOT use `toMatchSnapshot()` (the project does not commit snapshot files — confirmed via the absence of `__snapshots__` directories in src/components/ui/). Use assertion-style tests for snapshots 6 and 7.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/ui/PageHero.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/PageHero.test.tsx` exists: `test -f src/components/ui/PageHero.test.tsx && echo OK`.
    - File imports from vitest + @testing-library/react: `grep -c "from 'vitest'\|from '@testing-library/react'" src/components/ui/PageHero.test.tsx` returns at least 2.
    - File contains exactly 7 `it(` blocks (AC-PH-01 through AC-PH-07): `grep -c "it(" src/components/ui/PageHero.test.tsx` returns 7.
    - File registers `afterEach(() => cleanup())`: `grep -c "afterEach.*cleanup" src/components/ui/PageHero.test.tsx` returns at least 1.
    - All 7 tests pass: `npm run test -- --run src/components/ui/PageHero.test.tsx` exits 0 with output containing "7 passed" (or equivalent vitest summary).
    - The 9-gate grep-contract suite is unaffected: `npm run test -- --run tests/admin-09-grep-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>PageHero.test.tsx exists with 7 passing tests covering all prop combinations + light/dark wrappers; admin-09 9-gate suite still green.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/components/ui/PageHero.test.tsx` passes (7 / 7).
- `npm run test -- --run tests/admin-09-grep-contracts.test.ts` passes (9-gate suite stays green).
- `npx tsc --noEmit` exits 0.
- No new dependencies added (no Storybook, no axe-core, no pa11y): `git diff package.json` shows no changes.
</verification>

<success_criteria>
- `src/components/ui/PageHero.tsx` and `src/components/ui/PageHero.test.tsx` committed.
- PageHero ships as a pure server-component primitive with the locked 5-prop API; light + dark behavior verified via token references; ADMIN-09 9-gate suite remains green.
- Plan 16-04 can import `PageHero` from `@/components/ui/PageHero` without any further setup.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shell-refresh-contrast-gates/16-01-SUMMARY.md` recording: file paths created, test pass count, any deviations from D-01..D-05, and the exact PageHeroProps interface as shipped.
</output>
