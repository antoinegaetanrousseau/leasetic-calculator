---
phase: 16-shell-refresh-contrast-gates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/accessibility/16-contrast-audit.md
autonomous: false
requirements:
  - CONTRAST-01
  - CONTRAST-02
requirements_addressed:
  - CONTRAST-01
  - CONTRAST-02
tags:
  - accessibility
  - contrast-audit
  - wcag-aa
  - documentation
  - sign-off

must_haves:
  truths:
    - "docs/accessibility/ directory exists (created in this plan — was absent before per UI-SPEC §Pre-Population Source Map row 'docs/accessibility/ directory does not exist')."
    - "docs/accessibility/16-contrast-audit.md exists as a markdown document with the structured audit table required by D-08."
    - "The audit table contains 5 rows for CONTRAST-01 mandatory composites (per D-09): diff-panel changed-row over --surface light, diff-panel changed-row over --paper light, diff-panel changed-row over --surface dark, .chip-invited light, .chip-invited dark."
    - "The audit table contains 2 rows for CONTRAST-02 (Phase 16 new surfaces, per D-10): PageHero eyebrow var(--gd) over var(--paper) light, PageHero eyebrow var(--gd) over var(--paper) dark."
    - "Each row records: composite description, FG token, BG token, effective FG hex (after alpha blending), effective BG hex, WCAG 2.1 AA ratio, mode (Light or Dark), tool used (WebAIM contrast checker), pass status (pass requires ratio ≥ 4.5:1 for normal text)."
    - "Every row's ratio is ≥ 4.5:1 (WCAG 2.1 AA pass for normal text); any row that fails escalates as a CONTRAST-01 or CONTRAST-02 blocker before plan close."
    - "The document ends with a sign-off line by Antoine Rousseau dated 2026-MM-DD with the literal phrase 'Signed off by Antoine Rousseau' — per D-11."
    - "CONTRAST-01 closure is load-bearing: any v1.3 downstream wave that touches --gold, .chip-invited, or the diff-panel inherits this signed-off audit as the WCAG AA proof."
    - "CONTRAST-02 closes vacuously for any additional gold/teal pair NOT introduced by Phase 16; if Phase 16 plans 16-01..16-04 introduce no new gold/teal pair beyond the PageHero eyebrow (already row 6/7), the document records this explicitly: 'No additional CONTRAST-02 composites introduced by Phase 16 plans 16-01..16-04 beyond the PageHero eyebrow.'"
    - "Alpha-blending math is documented for the diff-panel composites — example formula provided in UI-SPEC §Contrast Audit Contract: for rgba(224,133,48,0.10) over #ffffff, the blended value is approximately #fcf3ea."
    - "No code change in this plan — purely a documentation artifact."
    - "Manual measurement step requires a human-with-browser checkpoint to run WebAIM (https://webaim.org/resources/contrastchecker/) for each composite; the audit document is produced from those measurements."
  artifacts:
    - path: "docs/accessibility/16-contrast-audit.md"
      provides: "Manual WCAG 2.1 AA contrast audit for 7 composites (5 CONTRAST-01 + 2 CONTRAST-02), with Antoine sign-off line"
      contains: "Signed off by Antoine Rousseau"
      min_lines: 60
    - path: "docs/accessibility/"
      provides: "New top-level docs sub-directory for accessibility artifacts (created in this plan if absent)"
  key_links:
    - from: "docs/accessibility/16-contrast-audit.md"
      to: "Phase 14 CoefficientDiffPanel.tsx and StatusChip.chip-invited usage"
      via: "audit rows 1-5 reference these composites by name"
      pattern: "diff-panel|chip-invited"
    - from: "docs/accessibility/16-contrast-audit.md"
      to: "Plan 16-01 PageHero eyebrow"
      via: "audit rows 6-7 reference the PageHero eyebrow composite (var(--gd) over var(--paper))"
      pattern: "PageHero eyebrow|--gd.*--paper"
---

<objective>
Produce the signed-off WCAG 2.1 AA contrast audit document `docs/accessibility/16-contrast-audit.md` that formally closes CONTRAST-01 (the Phase 14 deferred Tier-2 contrast measurement carry-forward) and CONTRAST-02 (any new Phase 16 gold/teal surface). This document is the LOAD-BEARING artifact for the contrast gates — once signed off and committed, every downstream v1.3 wave that touches `--gold`, `.chip-invited`, or the diff-panel can merge with the WCAG AA proof in hand. Phase 16's automated contrast CI (axe-core, pa11y) is explicitly OUT OF SCOPE (v1.4+); this plan ships a manual markdown audit measured via WebAIM contrast checker.

Purpose: Resolve a v1.3 cross-cutting prerequisite (ROADMAP §v1.3 invariants §3 palette stability + signed-off contrast audit before any --gold-touching wave merges).
Output: One new markdown file in a newly-created `docs/accessibility/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md
@.planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md

<interfaces>
Required composites per UI-SPEC §Contrast Audit Contract:

CONTRAST-01 (D-09) — 5 mandatory rows:
  1. Diff-panel changed-row text — FG --ink weight 600 (#41423d) — BG rgba(224,133,48,0.10) over --surface (#ffffff) blended to #fff1e1 — Light mode.
  2. Diff-panel changed-row text — FG --ink weight 600 (#41423d) — BG rgba(224,133,48,0.10) over --paper (#f5f7fc) blended to #fdf1e4 — Light mode.
  3. Diff-panel changed-row text — FG --ink dark (#e6e9ef) — BG rgba(224,133,48,0.10) over --surface dark (#161e2d) blended to #1d2033 — Dark mode.
  4. .chip-invited StatusChip text — FG chip text color (planner reads StatusChip.tsx to confirm) — BG --gold tint (planner reads chip bg) — Light mode.
  5. .chip-invited StatusChip text — FG chip text color — BG --gold tint — Dark mode.

CONTRAST-02 (D-10) — 2 mandatory rows for Phase 16 new surfaces:
  6. PageHero eyebrow — FG --gd (#129657) — BG --paper (#f5f7fc) — Light mode.
  7. PageHero eyebrow — FG --gd (#129657) — BG --paper dark (#0c121c) — Dark mode.

Alpha blending example (from UI-SPEC §Contrast Audit Contract):
  rgba(224,133,48,0.10) over #ffffff:
    R: 255 × 0.90 + 224 × 0.10 = 252.1 → 252
    G: 255 × 0.90 + 133 × 0.10 = 242.8 → 243
    B: 255 × 0.90 +  48 × 0.10 = 234.3 → 234
    Blended: #fcf3ea (verify with DevTools computed value)

Pass threshold: ≥ 4.5:1 for normal text (WCAG 2.1 AA).

Tool: WebAIM Contrast Checker (https://webaim.org/resources/contrastchecker/) per D-11 recommendation.

Sign-off line (D-11):
  "Signed off by Antoine Rousseau on 2026-MM-DD (commit <sha>). Each composite above measured manually using WebAIM contrast checker. All values ≥4.5:1 WCAG AA in both light and dark modes."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/accessibility/ directory and draft 16-contrast-audit.md with all 7 composite rows pre-calculated</name>
  <files>docs/accessibility/16-contrast-audit.md</files>
  <read_first>
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decisions D-08, D-09, D-10, D-11 — full mandatory composite list and sign-off requirements)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (§Contrast Audit Contract — locked tabular shape, alpha-blending example, pass threshold, tool, sign-off line)
    - app/globals.css (read the :root and html[data-theme="dark"] blocks to confirm token values: --ink #41423d/#e6e9ef, --muted #6e7191/#8b93a8, --gd #129657 (stable), --gold #e08530 (stable), --paper #f5f7fc/#0c121c, --surface #ffffff/#161e2d)
    - src/components/ui/StatusChip.tsx (read the .chip-invited variant — confirm the actual chip text color and background tint values for audit rows 4 and 5)
    - src/components/admin/CoefficientDiffPanel.tsx OR src/components/coefficient/CoefficientDiffPanel.tsx (locate the diff-panel changed-row composite source — confirm the rgba(224,133,48,0.10) value and the --ink weight 600 text color)
    - app/(public)/layout.tsx (for context on the public footer's existing Mentions légales link pattern — not used in this audit but provides design-system continuity)
  </read_first>
  <action>
    Create the directory and the audit document.

    1. Ensure the directory `docs/accessibility/` exists. If not, create it (e.g. via Write tool placing the file at the target path will implicitly create the parent dir).

    2. Create `docs/accessibility/16-contrast-audit.md` with the following structure (the executor pre-fills all rows with the alpha-blended values from UI-SPEC §Contrast Audit Contract and from the token values in app/globals.css; the human checkpoint at Task 2 confirms each ratio via WebAIM):

    Document outline (in order):
      a. Title heading: "# Phase 16 — WCAG 2.1 AA Contrast Audit"
      b. Subtitle paragraph stating the scope: closes CONTRAST-01 (Phase 14 deferred Tier-2 carry-forward) and CONTRAST-02 (Phase 16 new gold/teal surfaces).
      c. Section "## Method": one paragraph documenting (i) tool used = WebAIM Contrast Checker, (ii) pass threshold = ratio ≥ 4.5:1 for normal text per WCAG 2.1 AA, (iii) alpha-blending formula with the worked example from UI-SPEC for rgba(224,133,48,0.10) over #ffffff yielding #fcf3ea (note: UI-SPEC's blended example is approximately #fff1e1 — the executor should use UI-SPEC's #fff1e1 as the authoritative blended hex for row 1; verify via DevTools computed value and document any deviation).
      d. Section "## CONTRAST-01 — Diff-panel + StatusChip composites" with a markdown table. Columns (in order): `# | Composite | FG token (hex) | BG token (hex) | Effective FG (hex) | Effective BG (hex) | Mode | Ratio | Tool | Pass`. Pre-fill 5 rows per the &lt;interfaces&gt; block above (rows 1-5). For rows 1-3, the effective BG hex is the alpha-blended composite (#fff1e1 for row 1 over --surface light; #fdf1e4 for row 2 over --paper light; #1d2033 for row 3 over --surface dark). For rows 4-5, the planner reads StatusChip.tsx to determine the actual chip text color and chip bg tint (typically `--gold` 10-20% tint on the surface). Leave the Ratio column as `TBD (WebAIM)` until Task 2 confirms with measured values; pre-fill the rest.
      e. Section "## CONTRAST-02 — Phase 16 new surfaces" with a second markdown table using the same columns. Pre-fill 2 rows (rows 6-7): PageHero eyebrow FG `--gd` (#129657) over BG `--paper` light (#f5f7fc) — row 6; PageHero eyebrow FG `--gd` (#129657) over BG `--paper` dark (#0c121c) — row 7. Leave Ratio = TBD.
      f. Section "## Additional CONTRAST-02 composites" with the literal sentence: "No additional CONTRAST-02 composites introduced by Phase 16 plans 16-01..16-04 beyond the PageHero eyebrow rows 6-7. Plans 16-02 (sidebar/topbar micro-deltas), 16-03 (footer extension), and 16-04 (admin home adopter) introduce no new --gold or --teal foreground-on-background pair." This satisfies the "vacuous close" rule from CONTEXT.md D-10.
      g. Section "## Sign-off" reserved for the Task 2 human-checkpoint output — leave a placeholder line: "Signed off by Antoine Rousseau on <YYYY-MM-DD> (commit &lt;sha&gt;). Each composite above measured manually using WebAIM contrast checker. All values ≥4.5:1 WCAG AA in both light and dark modes." with `<YYYY-MM-DD>` and `<sha>` as visible placeholders.

    3. The document MUST NOT contain any commission-related strings. The ADMIN-09 9-gate grep-contract suite must remain green.

    4. The document MUST NOT introduce any new dependency, npm package, or CI workflow. It is a static markdown file.
  </action>
  <verify>
    <automated>test -d docs/accessibility &amp;&amp; test -f docs/accessibility/16-contrast-audit.md &amp;&amp; grep -c "CONTRAST-01\|CONTRAST-02" docs/accessibility/16-contrast-audit.md &amp;&amp; grep -c "WebAIM" docs/accessibility/16-contrast-audit.md &amp;&amp; grep -c "Signed off by Antoine Rousseau" docs/accessibility/16-contrast-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - The directory `docs/accessibility/` exists: `test -d docs/accessibility &amp;&amp; echo OK`.
    - The file `docs/accessibility/16-contrast-audit.md` exists: `test -f docs/accessibility/16-contrast-audit.md &amp;&amp; echo OK`.
    - The file references both requirement IDs: `grep -c "CONTRAST-01\|CONTRAST-02" docs/accessibility/16-contrast-audit.md` returns at least 2.
    - The file documents the tool: `grep -c "WebAIM" docs/accessibility/16-contrast-audit.md` returns at least 1.
    - The file contains the sign-off placeholder line (filled in Task 2): `grep -c "Signed off by Antoine Rousseau" docs/accessibility/16-contrast-audit.md` returns at least 1.
    - The file contains 5 CONTRAST-01 composite descriptions: `grep -ci "diff-panel\|chip-invited" docs/accessibility/16-contrast-audit.md` returns at least 5.
    - The file contains 2 CONTRAST-02 composite descriptions: `grep -ci "PageHero eyebrow" docs/accessibility/16-contrast-audit.md` returns at least 2.
    - The file contains the alpha-blending formula: `grep -c "alpha\|blend\|0.10\|rgba(224" docs/accessibility/16-contrast-audit.md` returns at least 1.
    - The file contains the vacuous-close note for additional CONTRAST-02 composites: `grep -c "No additional CONTRAST-02 composites" docs/accessibility/16-contrast-audit.md` returns at least 1.
    - No commission strings introduced: `grep -ci "commission" docs/accessibility/16-contrast-audit.md` returns 0.
    - 9-gate grep-contract suite stays green: `npm run test -- --run tests/admin-09-grep-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>docs/accessibility/16-contrast-audit.md exists with 7 pre-filled composite rows (5 CONTRAST-01 + 2 CONTRAST-02), tool/method documented, vacuous-close note recorded, sign-off placeholder reserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Antoine measures each composite via WebAIM and signs off (human-verify checkpoint)</name>
  <what-built>
    The audit document `docs/accessibility/16-contrast-audit.md` has been drafted with all 7 composites enumerated, FG/BG tokens identified, and effective hex values pre-computed via the documented alpha-blending formula. The Ratio column is `TBD (WebAIM)` for every row, and the Sign-off section is a placeholder.
  </what-built>
  <how-to-verify>
    For each of the 7 rows in `docs/accessibility/16-contrast-audit.md`:

    1. Open WebAIM Contrast Checker at https://webaim.org/resources/contrastchecker/ in a browser.
    2. Paste the row's "Effective FG (hex)" value into the Foreground Color field.
    3. Paste the row's "Effective BG (hex)" value into the Background Color field.
    4. Read the WCAG 2.1 contrast ratio shown by WebAIM (e.g. "5.8:1") and the Normal-Text WCAG AA verdict (Pass / Fail).
    5. Update the row's Ratio cell from `TBD (WebAIM)` to the measured ratio (e.g. `5.8:1`), and the Pass cell to `✓` (if ≥ 4.5:1) or `✗` (if &lt; 4.5:1).
    6. For diff-panel rows 1-3, also verify the alpha-blended effective BG hex against Chrome DevTools computed-style value — if DevTools shows a different hex, update the audit row's Effective BG hex AND re-measure the ratio in WebAIM.
    7. For .chip-invited rows 4-5, open the running app (`npm run dev`), navigate to a page rendering an `<StatusChip variant="invited">` (e.g. the partners/accounts list), and confirm the rendered chip text color + bg tint match what was pre-filled. Adjust the audit row if reality differs.
    8. For PageHero eyebrow rows 6-7, render the admin home (now using PageHero from Plan 16-04 — if Plan 16-04 has not yet executed, render any test instance of PageHero with an eyebrow), and confirm the eyebrow text is var(--gd) #129657 on var(--paper) bg in both light and dark themes.

    After all 7 rows have measured ratios filled in:

    9. If ANY row has a ratio &lt; 4.5:1 (Pass = ✗), DO NOT sign off. Escalate to the developer: the composite fails WCAG AA and the failing composite must be remediated before this plan closes. A failing composite blocks any downstream v1.3 wave that touches the failing surface (per CONTRAST-01/CONTRAST-02 success criteria in ROADMAP §Phase 16).

    10. If ALL 7 rows pass (Pass = ✓), update the Sign-off section's placeholder to: `Signed off by Antoine Rousseau on YYYY-MM-DD (commit <sha>). Each composite above measured manually using WebAIM contrast checker. All values ≥4.5:1 WCAG AA in both light and dark modes.` — substituting `YYYY-MM-DD` with today's date and `<sha>` with the eventual commit SHA of the audit doc (or `pending` if signing off before commit).

    11. Save the file and continue.
  </how-to-verify>
  <resume-signal>Type "signed off" to continue, or describe any failing composite that needs remediation before the audit can close.</resume-signal>
</task>

</tasks>

<verification>
- `test -f docs/accessibility/16-contrast-audit.md` succeeds.
- `grep -c "Signed off by Antoine Rousseau" docs/accessibility/16-contrast-audit.md` returns at least 1, and the line is NOT the placeholder (date is filled).
- Every Ratio cell in the file is a numeric ratio (e.g. `5.8:1`), not the placeholder `TBD (WebAIM)`: `grep -c "TBD (WebAIM)" docs/accessibility/16-contrast-audit.md` returns 0.
- Every Pass cell is `✓` (no `✗` remaining): `grep -c "✗" docs/accessibility/16-contrast-audit.md` returns 0.
- `npm run test -- --run tests/admin-09-grep-contracts.test.ts` passes.
</verification>

<success_criteria>
- Manual WCAG 2.1 AA contrast audit committed at `docs/accessibility/16-contrast-audit.md` with 7 composites (5 CONTRAST-01 + 2 CONTRAST-02), all rows showing measured ratios ≥ 4.5:1.
- Antoine sign-off line filled with date and (optionally) commit SHA per D-11.
- CONTRAST-01 closed: Phase 14 Tier-2 deferred contrast measurement formally resolved; any downstream v1.3 wave touching --gold / .chip-invited / diff-panel is unblocked.
- CONTRAST-02 closed: Phase 16's only new gold/teal pair (PageHero eyebrow var(--gd) over var(--paper)) measured and passing in both light and dark.
- ADMIN-09 9-gate grep-contract suite remained green.
- No code changes, no dependencies, no CI workflows introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shell-refresh-contrast-gates/16-05-SUMMARY.md` recording: each of the 7 composites with its measured ratio, the date Antoine signed off, the commit SHA of the audit doc, confirmation of the vacuous-close note for any non-introduced gold/teal pair, and confirmation that the 9-gate suite remained green.
</output>
