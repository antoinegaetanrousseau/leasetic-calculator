---
phase: 44-backfill-migration
plan: 05
type: execute
wave: 4
depends_on: ["44-04"]
files_modified:
  - scripts/backfill-proposal-pdfs.ts
  - package.json
  - .gitignore
  - .github/workflows/backfill-proposal-pdfs.yml
  - docs/operations/backfill-proposal-pdfs.md
autonomous: false
requirements: [MIG-01, MIG-02]

user_setup:
  - service: github
    why: "The apply job needs the blob-store credentials the Vercel runtime normally supplies, and the required-reviewer gate must exist before the workflow is dispatched."
    env_vars:
      - name: STORAGE_DRIVER
        source: "GitHub -> Settings -> Environments -> production -> Environment secrets. Value: the driver the Vercel production deployment uses (`vercel`)."
      - name: BLOB_READ_WRITE_TOKEN
        source: "Vercel -> Storage -> the proposals Blob store -> Tokens (read-write). Paste into GitHub -> Settings -> Environments -> production -> Environment secrets."
    dashboard_config:
      - task: "Confirm the `production` GitHub Environment still has a required reviewer. The approval click on that Environment IS MIG-02's confirmation gate; without it the apply job runs unattended."
        location: "GitHub -> Settings -> Environments -> production -> Deployment protection rules -> Required reviewers"
      - task: "Confirm `DATABASE_URL_MAIN` is present on the `production` Environment (it already is - db-migrate.yml uses it). Do NOT copy it to any other Environment."
        location: "GitHub -> Settings -> Environments -> production -> Environment secrets"

must_haves:
  truths:
    - "An operator can dispatch one GitHub Action that dry-runs the backfill and publishes a downloadable report of how many PDFs would be re-rendered and which ones failed (MIG-01)"
    - "The apply job runs only after a required-reviewer approval on the production Environment, and consumes the exact artifact that approval was granted against (MIG-02, D-08)"
    - "The dispatch also requires a typed confirmation string before the dry run starts (MIG-02)"
    - "Running apply from a developer machine is refused by the existing database guard before any write, with no bypass env var added and no guard file modified (D-07)"
    - "The script exits 0 on success, 1 on crash, 2 on environment refusal, 3 on guard refusal and 4 when any row failed, so a partial success turns the Action run red (D-10, D-11)"
    - "No log line, report line or Action output contains a connection string or a token - only a derived hostname and bare filenames"
    - "An operator can follow a written runbook that states plainly that the write is irreversible and that no sidecar copy of the retired layout exists (D-01)"
  artifacts:
    - path: "scripts/backfill-proposal-pdfs.ts"
      provides: "the tsx entry point; a thin, guard-arming CLI shell over @/lib/backfill"
      min_lines: 90
    - path: ".github/workflows/backfill-proposal-pdfs.yml"
      provides: "workflow_dispatch, two jobs, artifact hand-off, Environment gate"
      min_lines: 100
    - path: "docs/operations/backfill-proposal-pdfs.md"
      provides: "operator runbook"
      min_lines: 70
    - path: "package.json"
      provides: "db:backfill:proposal-pdfs:dry-run and db:backfill:proposal-pdfs"
      contains: "db:backfill:proposal-pdfs"
  key_links:
    - from: "scripts/backfill-proposal-pdfs.ts"
      to: "scripts/_load-env.ts"
      via: "first import, arming assertSafeDatabaseTarget at module scope"
      pattern: "import './_load-env'"
    - from: ".github/workflows/backfill-proposal-pdfs.yml"
      to: "the production GitHub Environment"
      via: "environment: production on both jobs; needs: dry-run on the apply job"
      pattern: "environment: production"
    - from: ".github/workflows/backfill-proposal-pdfs.yml"
      to: ".backfill/"
      via: "actions/upload-artifact@v4 then actions/download-artifact@v4"
      pattern: "upload-artifact"
---

<objective>
Ship the operator surface: the `tsx` entry point, the two npm scripts, the gated GitHub Action that
is the only place apply may run, and the runbook.

Purpose: MIG-02 lands here. The database guard is satisfied by being ARMED and refusing every venue
except a fresh Action checkout - it does not authorise the Action, it simply skips there (D-07). The
explicit confirmation is a typed string on the dispatch plus a required-reviewer approval between the
two jobs, and that approval is what makes "the run I approved" and "the run that happened" the same
run by construction (D-08). D-01 removed the rollback, so those gates are the whole safety net.

Output: one script, two npm scripts, one gitignore line, one workflow, one runbook, and one operator
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-backfill-migration/44-CONTEXT.md
@.planning/phases/44-backfill-migration/44-PATTERNS.md
@.planning/phases/44-backfill-migration/44-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/backfill-proposal-pdfs.ts + npm scripts + .gitignore (D-07, D-10, D-11)</name>
  <files>scripts/backfill-proposal-pdfs.ts, package.json, .gitignore</files>
  <read_first>
    - scripts/reconcile-proposals.ts - the whole file. It is the template: `import './_load-env';` as
      the first statement above the docstring, the header docstring listing the npm scripts, flags and
      exit codes, the `DATABASE_URL` presence and `new URL()` parse checks that exit 2, the
      `url.hostname` extraction (never `url.host`, which carries the port - bug_011),
      `resolveNeonTarget`, the sha256 fingerprint over `hostname + '/' + databaseName`, the lazy
      `await import()` of the DB and the domain module AFTER env validation, and the `result.mode` /
      `result.aborted` branching.
    - scripts/purge-soft-deleted.ts lines 50-125 - the banner block, the required-env-var checks, the
      per-row `[fail] id=...` printing, the re-run hint, and the non-zero exit on any failure.
    - scripts/_load-env.ts lines 1-60 - why this import must be first, and what the SKIP rule does.
    - scripts/_db-branch-guard.ts lines 28-80 - the credential-disclosure rules every message must
      respect, and the SKIP-rule paragraph naming the three sanctioned production paths.
    - src/lib/storage/vercel-blob.ts line 40 and src/lib/storage/s3.ts lines 27-31 - the exact
      credential env-var names per driver: `BLOB_READ_WRITE_TOKEN`; `S3_ENDPOINT`, `S3_REGION`,
      `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`.
    - package.json lines 28-36 - the `db:reconcile:dry-run` / `db:reconcile` pair and the
      `-r ./scripts/_preload-mock-server-only.cjs` wrapper.
    - .gitignore around line 31 - the `.reconcile/` entry.
    - src/lib/backfill/index.ts - the barrel this script imports from.
  </read_first>
  <action>
    Create `scripts/backfill-proposal-pdfs.ts` with `#!/usr/bin/env tsx` and `import './_load-env';`
    as the FIRST statement, above the docstring, exactly as `reconcile-proposals.ts` does. That import
    arms `assertSafeDatabaseTarget()` at module scope, which classifies Neon `main` as
    `refuse-production` and exits, so a developer machine holding a `.env*` candidate cannot run apply
    at all. That IS MIG-02's local-database guard. Do NOT add a bypass env var. Do NOT modify
    `scripts/_db-branch-guard.ts` or `scripts/_load-env.ts` - a bypass built for a bulk irreversible
    blob mutation would outlive this phase.

    Header docstring, modelled on reconcile's: what the script does and which requirements it serves
    (MIG-01..05); the two npm scripts that invoke it; the flags `--dry-run` and `--allow-drift`; the
    disclaimer that it applies no database migration and never invokes the forbidden schema-sync
    command; and the exit-code table - `0` success, `1` crash, `2` environment refusal, `3` guard
    refusal (no approved report / fingerprint mismatch / unaccepted drift), `4` partial failure (the
    run completed but at least one row failed). State explicitly that `4` EXTENDS reconcile's 0/1/2/3
    vocabulary rather than overloading `1`, because a partial failure is neither a crash nor a
    refusal, and that its purpose is to turn the Action run red so a partial success cannot pass
    unnoticed (D-10). State that apply is expected to run only from
    `.github/workflows/backfill-proposal-pdfs.yml` (D-07).

    Body:
    - Parse `--dry-run` and `--allow-drift` from `process.argv`; mode defaults to apply, matching
      reconcile.
    - Print the banner block in purge's style, naming the phase and the mode.
    - `DATABASE_URL` missing -> `console.error` + `exit(2)`. `new URL()` throwing -> `exit(2)`. Take
      `url.hostname` and `url.pathname.replace(/^\//, '')` as the database name, and compute the
      fingerprint as `createHash('sha256').update(hostname + '/' + databaseName)` hex. Every log line
      may interpolate the derived `hostname` and a bare filename ONLY - never the URL, never the user,
      password or query string. Script output gets pasted into transcripts.
    - Call `resolveNeonTarget(hostname)` and log the target label plus hostname, with an emphasised
      `*** PRODUCTION WRITE ***` suffix in apply mode when `target.isProductionSeverity`.
    - IN APPLY MODE ONLY, validate the storage environment before anything else: `STORAGE_DRIVER` must
      be set; when it is `vercel`, `BLOB_READ_WRITE_TOKEN` must be set; when it is `s3`, all of
      `S3_ENDPOINT`, `S3_REGION`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY` must be set.
      On any missing one, print the missing variable NAMES only and `exit(2)`. Dry-run mode must NOT
      require any storage variable - it writes no blob, and keeping job 1's secret surface free of
      blob credentials is deliberate. Put that reasoning in a comment so nobody "simplifies" the check
      to run in both modes.
    - Lazy `await import('../src/lib/backfill')` AFTER all env validation, mirroring reconcile's
      lazy-import discipline, then call `runBackfill({ deps: createLiveBackfillDeps({ log: (line) =>
      console.log(line) }), mode, rootDir: process.cwd(), databaseFingerprint, allowDrift })`.
    - Dry-run result -> print the four report paths and `JSON.stringify(result.counts)`, then
      `exit(0)`. Exit 0 even when some rows failed to render: MIG-01 wants those surfaced in the
      report for the operator to read, and job 1 must succeed so the artifact uploads and the approval
      gate is reached. Put that reasoning in a comment.
    - Apply result with `aborted: true` -> print `Refused to apply: <reason>`, print
      `formatBackfillDrift` output when the reason is drift, then `exit(3)`.
    - Apply result with `aborted: false` -> print the counts, print one
      `[fail] id=<proposalId>: <reason>` line per entry in `failures` (the `detail` was already
      redacted upstream in plan 02), print the re-run hint in purge's wording, then `exit(4)` when
      `failures.length > 0`, else `exit(0)`.
    - `main().catch(...)` printing `FATAL:` and `exit(1)`, exactly as reconcile does.

    Add two npm scripts to `package.json`, immediately after the existing `db:backfill:partner-type`
    line so the `db:backfill:*` family stays together:
    `"db:backfill:proposal-pdfs:dry-run": "tsx -r ./scripts/_preload-mock-server-only.cjs scripts/backfill-proposal-pdfs.ts --dry-run"`
    and
    `"db:backfill:proposal-pdfs": "tsx -r ./scripts/_preload-mock-server-only.cjs scripts/backfill-proposal-pdfs.ts"`.
    The `-r` preload shim is mandatory, not cosmetic: the dependency chain reaches
    `src/lib/db/queries` and `src/lib/pdf`, both of which carry `server-only`.

    Add `.backfill/` to `.gitignore`, immediately below the existing `.reconcile/` line.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('./package.json').scripts; if(!s['db:backfill:proposal-pdfs']||!s['db:backfill:proposal-pdfs:dry-run'])process.exit(1)" && grep -q '^\.backfill/' .gitignore && npm run check:no-drizzle-push && npm run lint:check</automated>
  </verify>
  <acceptance_criteria>
    - `head -2 scripts/backfill-proposal-pdfs.ts` shows the `#!/usr/bin/env tsx` shebang followed by
      `import './_load-env';`.
    - `git diff --name-only scripts/` lists only `scripts/backfill-proposal-pdfs.ts` - neither
      `_db-branch-guard.ts` nor `_load-env.ts` was modified.
    - `grep -Ec "BYPASS|SKIP_GUARD|ALLOW_LOCAL|--force" scripts/backfill-proposal-pdfs.ts` prints `0` -
      no bypass flag was invented.
    - `grep -c "url\.host\b" scripts/backfill-proposal-pdfs.ts` prints `0`, and
      `grep -c "url\.hostname" scripts/backfill-proposal-pdfs.ts` is at least `1` (bug_011).
    - `grep -Ec '\$\{databaseUrl\}|\$\{process\.env\.DATABASE_URL\}' scripts/backfill-proposal-pdfs.ts`
      prints `0` - the connection string is never interpolated into output.
    - `node -e "const s=require('./package.json').scripts; process.exit(s['db:backfill:proposal-pdfs'].includes('_preload-mock-server-only.cjs') && s['db:backfill:proposal-pdfs:dry-run'].includes('--dry-run') ? 0 : 1)"` exits 0.
    - `grep -c '^\.backfill/' .gitignore` prints `1`.
    - `npm run check:no-drizzle-push` exits 0 and `git status --porcelain drizzle/` is empty.
    - `npx tsc --noEmit` and `npm run lint:check` both exit 0.
  </acceptance_criteria>
  <done>Both npm scripts exist and the entry point is a thin, guard-arming shell over `@/lib/backfill` that leaks nothing.</done>
</task>

<task type="auto">
  <name>Task 2: the gated two-job workflow + the operator runbook (D-01, D-07, D-08, D-09)</name>
  <files>.github/workflows/backfill-proposal-pdfs.yml, docs/operations/backfill-proposal-pdfs.md</files>
  <read_first>
    - .github/workflows/db-migrate.yml - the whole file. Copy: the "MANUAL TRIGGER ONLY" header block,
      the security note that `workflow_dispatch` inputs are operator-controlled and flow through
      `env:` before any shell evaluation, the typed `confirm` input, the `concurrency` group with
      `cancel-in-progress: false`, `permissions: { contents: read }`, `actions/checkout@v4` with
      `persist-credentials: false`, `actions/setup-node@v4` with `node-version: '22'` and
      `cache: 'npm'`, `npm ci`, the confirmation-phrase step reading `CONFIRM_INPUT` from `env:`, and
      the `environment:` + Environment-secret comment style.
    - docs/operations/reconciliation-import.md and docs/operations/purge.md - the runbook format,
      heading structure and tone this repo documents an operator script in.
    - docs/operations/neon-branch-routing.md - the locked rule that real branches are reached only
      through a gated workflow.
    - .planning/phases/44-backfill-migration/44-CONTEXT.md - D-01, D-02, D-03, D-05, D-07, D-08, D-09.
    - .planning/phases/44-backfill-migration/44-PATTERNS.md - the workflow section, including its note
      that NO analog for cross-job artifact hand-off exists anywhere in this repo.
  </read_first>
  <action>
    Create `.github/workflows/backfill-proposal-pdfs.yml`, named
    `Backfill Proposal PDFs (One-Time Migration)`.

    Header comment block in db-migrate.yml's style, recording: manual trigger only - no `on: push`,
    no `on: pull_request`, no `on: schedule`; this is the milestone's one irreversible step, and the
    apply job overwrites `proposals/{userId}/{proposalId}.pdf` in place with no versioning and no
    sidecar copy, so the retired layout ceases to exist (D-01); the database guard
    (`scripts/_db-branch-guard.ts`) SKIPs here because a fresh Action checkout has no `.env*`
    candidate file, which is the same sanctioned path `MIGRATE PROD` and CI's ephemeral-branch step
    already use, and the guard is neither modified nor bypassed for this workflow (D-07); and the
    security note that `workflow_dispatch` inputs are operator-controlled and pass through `env:`
    before any shell evaluation.

    `on: workflow_dispatch` with two inputs:
    - `confirm` - `required: true`, `type: string`, description
      `Type "BACKFILL PROD PDFS" exactly to confirm.` The ROADMAP's success criterion 2 asks for an
      explicit TYPED confirmation, so this exists alongside the Environment gate rather than instead
      of it.
    - `allow_drift` - `required: false`, `type: boolean`, `default: false`, description explaining it
      proceeds when the freshly planned set differs from the approved report (D-09) and must stay
      false unless the difference was reviewed.

    `concurrency: { group: backfill-proposal-pdfs, cancel-in-progress: false }` - never cancel a
    running bulk mutation and never let two dispatches race. `permissions: { contents: read }`.

    Job `dry-run`: `runs-on: ubuntu-24.04`, `timeout-minutes: 60`, `environment: production`. Steps:
    checkout with `persist-credentials: false`; a confirmation step reading
    `CONFIRM_INPUT: ${{ github.event.inputs.confirm }}` through `env:` that exits non-zero unless it
    is exactly `BACKFILL PROD PDFS` (never interpolate the input into the shell directly); setup-node
    22 with npm cache; `npm ci`; `npm run db:backfill:proposal-pdfs:dry-run` with
    `env: DATABASE_URL: ${{ secrets.DATABASE_URL_MAIN }}` and NO storage secrets - the dry run writes
    no blob and must not be handed blob credentials; then `actions/upload-artifact@v4` with
    `name: backfill-dry-run-report`, `path: .backfill/`, `if-no-files-found: error`,
    `retention-days: 30`.

    Job `apply`: `needs: dry-run`, `runs-on: ubuntu-24.04`, `timeout-minutes: 120`,
    `environment: production`. Steps: checkout with `persist-credentials: false`;
    `actions/download-artifact@v4` with `name: backfill-dry-run-report` and `path: .backfill`;
    setup-node 22 with npm cache; `npm ci`; then the apply step with `env:` carrying
    `DATABASE_URL: ${{ secrets.DATABASE_URL_MAIN }}`,
    `STORAGE_DRIVER: ${{ secrets.STORAGE_DRIVER }}`,
    `BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}` and
    `ALLOW_DRIFT: ${{ github.event.inputs.allow_drift }}`, and a shell body that branches on
    `[ "$ALLOW_DRIFT" = "true" ]` to run `npm run db:backfill:proposal-pdfs -- --allow-drift` and
    otherwise `npm run db:backfill:proposal-pdfs`. The input is never interpolated into the command
    line directly.

    Both jobs declare `environment: production` on purpose, and the file MUST carry a comment
    explaining why, because it otherwise reads as a mistake: `DATABASE_URL_MAIN` is an ENVIRONMENT
    secret on `production` (db-migrate.yml documents this), so a job without that environment cannot
    read it. Both alternatives were rejected - promoting the production connection string to a
    repository secret, or copying it into a second, unreviewed Environment, would each widen who can
    reach a production write credential. The consequence is that the operator approves twice: once to
    let the dry run READ production, and once - after reading the report - to let the apply job WRITE.
    The second approval is MIG-02's confirmation and the D-08 guarantee that the approved plan and the
    executed plan are the same run.

    Also comment that `actions/upload-artifact@v4` and `actions/download-artifact@v4` are new to this
    repo's CI vocabulary (no other workflow uses either) and that the artifact hand-off, rather than a
    re-plan from scratch, is what makes job 2 operate on the exact plan the reviewer saw.

    Create `docs/operations/backfill-proposal-pdfs.md`, following the structure of
    `docs/operations/reconciliation-import.md`. It must contain at minimum:
    - A prominent, unmissable statement that this migration is IRREVERSIBLE: the blob key is a pure
      function of `(userId, proposalId)`, `VercelBlobStorage.put()` passes `addRandomSuffix: false`,
      the store has no versioning, and no sidecar copy of the retired layout is taken (D-01).
    - Scope: `status IN ('active','deleted')`, soft-deleted rows included with no 30-day window filter
      and why (a partner can restore one inside the window and would otherwise resurrect a
      retired-layout document), drafts structurally excluded because they have no blob (D-03).
    - What changes and what does not: figures are the stored `computed` jsonb printed verbatim,
      nothing recomputes and `params_snapshot` is never read at render time (D-05, MIG-04); each
      document keeps its own committed `language` (MIG-03); the advisor block and the partner company
      telephone are live reads and MAY legitimately differ from the delivered document - by design,
      not a defect, and nothing detects or reports that delta (D-02); a row whose blob object is
      missing from storage is processed like any other and repaired as a side effect (D-04).
    - Step-by-step: dispatch the workflow, type `BACKFILL PROD PDFS`, approve the first gate, download
      the `backfill-dry-run-report` artifact, read `dry-run-latest.md`, then approve the second gate -
      or cancel the run instead of approving.
    - The exit-code table (0/1/2/3/4) and what to do for each, including that `4` means some rows
      failed and that a plain re-run retries exactly those rows because they remain unmarked in
      `audit_log` (D-10, MIG-05).
    - Drift refusals: read the `+` / `-` lines, re-dispatch to produce a fresh dry run and approve
      that instead; use `allow_drift` only after reviewing the difference (D-09).
    - A "do not run this locally" section: `import './_load-env'` arms the guard, which classifies
      Neon `main` as `refuse-production` and exits, and no bypass exists or is to be added (D-07).
    - A note that this workflow applies no database migration; Phase 44 creates none.
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/backfill-proposal-pdfs.yml','utf8')); console.log('yaml ok')" && test "$(grep -c 'environment: production' .github/workflows/backfill-proposal-pdfs.yml || true)" = "2" && grep -q 'needs: dry-run' .github/workflows/backfill-proposal-pdfs.yml && grep -q 'upload-artifact@v4' .github/workflows/backfill-proposal-pdfs.yml && grep -q 'download-artifact@v4' .github/workflows/backfill-proposal-pdfs.yml && test -f docs/operations/backfill-proposal-pdfs.md</automated>
  </verify>
  <acceptance_criteria>
    - The workflow parses as YAML: the `node -e "require('js-yaml').load(...)"` command above prints
      `yaml ok`. (If `js-yaml` is not resolvable from `node_modules`, substitute any locally available
      YAML parser - do NOT install a package to satisfy this check.)
    - `grep -c 'environment: production' .github/workflows/backfill-proposal-pdfs.yml` prints `2`.
    - `grep -c 'needs: dry-run' .github/workflows/backfill-proposal-pdfs.yml` prints `1`.
    - `grep -Ec 'on:\s*$|workflow_dispatch' .github/workflows/backfill-proposal-pdfs.yml` is at
      least `1`, and `grep -Ec '^\s*(push|pull_request|schedule):' .github/workflows/backfill-proposal-pdfs.yml`
      prints `0` - no automatic trigger exists.
    - `grep -c 'persist-credentials: false' .github/workflows/backfill-proposal-pdfs.yml` prints `2`.
    - `grep -c 'permissions:' .github/workflows/backfill-proposal-pdfs.yml` prints `1` and the block
      grants `contents: read`.
    - No `github.event.inputs` value appears inside a `run:` block: running
      `awk '/^\s*run: \|/,/^\s*- /' .github/workflows/backfill-proposal-pdfs.yml | grep -c 'github.event.inputs'`
      prints `0` - every input reaches the shell through an `env:` variable.
    - `grep -c 'BLOB_READ_WRITE_TOKEN' .github/workflows/backfill-proposal-pdfs.yml` prints `1` - the
      blob token is present on the apply job only, never on the dry-run job.
    - `docs/operations/backfill-proposal-pdfs.md` contains all of: `irreversible`, `addRandomSuffix`,
      `BACKFILL PROD PDFS`, `allow_drift`, `params_snapshot`, and an exit-code table listing `4`.
    - `git diff --name-only .github/` lists only the new workflow file - `db-migrate.yml` and `ci.yml`
      are untouched.
  </acceptance_criteria>
  <done>A single dispatch produces a reviewable dry-run artifact and, only after an approval, an apply job that consumes exactly that artifact - and an operator has a written procedure for both.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator dispatch, artifact review, and the approval that IS MIG-02's confirmation</name>
  <files>none - this task changes no file</files>
  <read_first>
    - docs/operations/backfill-proposal-pdfs.md - the runbook written in task 2; the operator follows
      it, and any step that does not match reality is a defect to report rather than to work around.
    - .planning/phases/44-backfill-migration/44-CONTEXT.md - D-01 (there is no rollback) and D-08.
  </read_first>
  <action>
    STOP and hand control to the operator. Do not dispatch the workflow yourself, do not approve any
    GitHub Environment deployment, and do not run `npm run db:backfill:proposal-pdfs` in any form -
    the apply path is irreversible (D-01) and its authorisation is a human approval by design (D-08).

    Present the `<what-built>` summary and the numbered `<how-to-verify>` procedure below to the
    operator verbatim, including the DECISION POINT at step 5, then wait for the resume signal. If the
    operator reports a mismatch between `docs/operations/backfill-proposal-pdfs.md` and what actually
    happened, treat that as a defect in task 2's runbook or workflow and fix it before re-presenting
    this checkpoint.
  </action>
  <what-built>
    A `workflow_dispatch` GitHub Action with two jobs. Job 1 renders every in-scope proposal PDF,
    writes zero blobs and zero database rows, and uploads a two-form report as the
    `backfill-dry-run-report` artifact. Job 2 downloads that exact artifact, refuses on drift, and
    otherwise re-renders every proposal and overwrites its stored PDF in place. Both jobs sit behind
    the `production` Environment's required-reviewer gate.

    Before this checkpoint, confirm the `user_setup` items are done: `STORAGE_DRIVER` and
    `BLOB_READ_WRITE_TOKEN` exist as Environment secrets on `production`, and `production` still has a
    required reviewer.
  </what-built>
  <how-to-verify>
    1. Push the branch so the workflow file exists on the ref you intend to dispatch, then open
       GitHub -> Actions -> "Backfill Proposal PDFs (One-Time Migration)" -> Run workflow.
    2. Type `BACKFILL PROD PDFS` in the `confirm` field. Leave `allow_drift` unchecked. Dispatch.
    3. Approve the FIRST pending deployment (this only lets the dry run READ production). Watch job 1
       finish green.
    4. Download the `backfill-dry-run-report` artifact and open `dry-run-latest.md`. Check:
       - the "Counts" table shows a plausible number of candidates;
       - the "Would fail to render" table lists what you expect (ideally nothing);
       - no financial figure, no client amount, no blob key, no user id and no connection string
         appears anywhere in the file;
       - the "What approving this run does" section states the overwrite is irreversible.
    5. DECISION POINT. Approving the SECOND pending deployment is MIG-02's confirmation and starts the
       irreversible overwrite. There is no rollback and no sidecar copy of the retired layout (D-01).
       If anything in the report looks wrong, cancel the run instead of approving - cancelling costs
       nothing, and a fresh dispatch produces a fresh report.
    6. If you approve: watch job 2. Green means every listed proposal was re-rendered and marked. Red
       with exit code 4 means some rows failed - read the `[fail] id=...` lines, then re-dispatch; a
       re-run picks up exactly the failed rows because they are the ones still unmarked in
       `audit_log`.
    7. Spot-check the result: open two or three proposals in the extranet - at least one French, one
       English, and one soft-deleted if you have one - and confirm each PDF now shows the Phase 43
       layout, its own language, and the same loyer, coefficient and amount it always showed.
  </how-to-verify>
  <acceptance_criteria>
    - The workflow appears in the GitHub Actions list and is dispatchable.
    - A dispatch with a wrong `confirm` string fails at the confirmation step before job 1 reaches the
      database.
    - Job 1 completes green and produces a downloadable `backfill-dry-run-report` artifact containing
      `dry-run-latest.md` and `dry-run-latest.json`.
    - The report's Markdown form contains no amount, loyer, coefficient, blob key, user id or URL.
    - Job 2 does not start until a second, separate approval is granted.
    - After a successful apply, a spot-checked FR proposal and a spot-checked EN proposal each render
      in the Phase 43 layout, in their own language, with unchanged financial figures.
    - The operator confirms in their reply whether they approved or cancelled, and reports any
      mismatch between the runbook and what actually happened.
  </acceptance_criteria>
  <verify>
    <automated>MISSING - this task's outcome exists only in the GitHub Actions UI and the production blob store; no local command can observe it. The automated proofs for the code paths it exercises are `npx vitest run src/lib/backfill` (plans 02-04) and this plan's task 1 and task 2 gates.</automated>
    <human-check>The operator confirms the dry-run report was reviewed and states whether they approved the apply job or cancelled the run, plus the result of the FR/EN/soft-deleted spot check if they approved.</human-check>
  </verify>
  <done>The operator has dispatched the workflow, read `dry-run-latest.md`, and recorded an explicit approve-or-cancel decision along with any runbook mismatch.</done>
  <resume-signal>Type "approved" once the dry-run report has been reviewed and (if you chose to proceed) the apply job has completed - or describe what you saw and stopped for.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> `workflow_dispatch` inputs | `confirm` and `allow_drift` are operator-controlled strings that reach a shell. |
| GitHub Actions runner -> production Neon + production blob store | The apply job holds a write-capable `DATABASE_URL` and a read-write blob token. |
| repo write access -> irreversible bulk mutation | Anyone who can dispatch a workflow can start this; only the Environment reviewer can complete it. |
| script/guard output -> transcripts and issues | Every printed line is expected to be pasted somewhere public-ish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-24 | Elevation of privilege | irreversible bulk overwrite triggered without review | mitigate | `environment: production` on the apply job with a required reviewer; `needs: dry-run` so the report exists before the gate; the typed `BACKFILL PROD PDFS` confirmation checked before job 1 touches the database; `concurrency` with `cancel-in-progress: false` so two dispatches cannot race. |
| T-44-25 | Tampering | command injection via `workflow_dispatch` input | mitigate | `confirm` and `allow_drift` reach the shell only through `env:` variables (`CONFIRM_INPUT`, `ALLOW_DRIFT`) and are compared with `[ ... ]` tests; no `${{ github.event.inputs.* }}` appears inside any `run:` block, pinned by an acceptance grep. Same position `db-migrate.yml` already documents. |
| T-44-26 | Information disclosure | credentials in Action logs or the report artifact | mitigate | The script logs a derived `url.hostname` and bare filenames only - never the URL, user, password or query string; the fingerprint is a sha256 over `hostname/database`; error `detail` was redacted upstream; no `set -x` and no `env` dump anywhere in the workflow. |
| T-44-27 | Information disclosure | blob credentials handed to a job that does not need them | mitigate | `STORAGE_DRIVER` and `BLOB_READ_WRITE_TOKEN` appear on the apply job only; the dry-run job receives `DATABASE_URL` alone, and the script's storage-env check runs in apply mode only so the omission is enforced rather than incidental. |
| T-44-28 | Elevation of privilege | production DB URL copied to a lower-protection GitHub Environment | mitigate | Both jobs use the existing `production` Environment; no new Environment is created and `DATABASE_URL_MAIN` is not duplicated. The cost is a second approval click, which is accepted and documented in the workflow. |
| T-44-29 | Tampering | weakening the DB guard to make apply runnable locally | mitigate | The script imports `./_load-env` and adds no bypass; an acceptance criterion asserts `scripts/_db-branch-guard.ts` and `scripts/_load-env.ts` are byte-unchanged, and that no `BYPASS`/`SKIP_GUARD`/`ALLOW_LOCAL`/`--force` token exists in the new script. |
| T-44-30 | Repudiation | a partial run reported as success | mitigate | Exit code `4` on any failed row turns the Action red; per-row `audit_log` markers make the exact migrated set reconstructable after the fact. |
| T-44-31 | Denial of service | a hung run holding a production connection | mitigate | `timeout-minutes: 60` on the dry-run job and `120` on the apply job; the serial loop from plan 04 bounds memory to one PDF buffer. |
| T-44-SC | Tampering | third-party GitHub Actions and npm/pip/cargo installs | mitigate | No package-manager install task exists in this plan - no npm dependency is added anywhere in Phase 44, so the Package Legitimacy Gate has nothing to audit. The only new third-party references are `actions/upload-artifact@v4` and `actions/download-artifact@v4`, both first-party GitHub-published actions on the same major-tag convention as the `actions/checkout@v4` and `actions/setup-node@v4` this repo already uses. If an executor finds itself reaching for `npm install`, stop and escalate rather than proceeding. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run lint:check` exits 0.
- `npx vitest run` passes (the full suite, since `package.json` changed).
- `npm run check:no-drizzle-push` exits 0 and `git status --porcelain drizzle/` is empty.
- `npm run check:no-vercel-imports` exits 0.
- `git diff --name-only` after the plan lists exactly the five files in `files_modified`.
- The operator checkpoint in task 3 is answered before the plan is considered complete.
</verification>

<success_criteria>
- `npm run db:backfill:proposal-pdfs:dry-run` and `npm run db:backfill:proposal-pdfs` both exist and
  both carry the `server-only` preload shim.
- The script's first statement is `import './_load-env';` and no guard file was modified.
- The workflow has exactly one `workflow_dispatch` trigger, two jobs, both gated on the `production`
  Environment, with the apply job depending on the dry-run job and consuming its artifact.
- No `workflow_dispatch` input reaches a shell except through an `env:` variable.
- The runbook states the write is irreversible and documents every exit code.
- The operator has dispatched the workflow, reviewed the dry-run report, and recorded their decision.
</success_criteria>

<output>
Create `.planning/phases/44-backfill-migration/44-05-SUMMARY.md` when done.
</output>
</content>
