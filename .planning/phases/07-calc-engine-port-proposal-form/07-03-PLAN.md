---
phase: 7
plan_id: "07-03"
slug: home-page-cta
type: execute
wave: 3
depends_on: ["07-06"]
files_modified:
  - app/(authed)/page.tsx
  - app/globals.css
requirements: [PROP-01]
autonomous: true
tags: [home-page, cta, empty-state]
objective: |
  Replace the Phase-6 minimal placeholder body in `app/(authed)/page.tsx`
  with the Phase-7 home shell: hero greeting card + primary CTA "Nouvelle
  proposition" linking to `/proposals/new` + recent-proposals card with the
  empty-state body (PROP-01). The list shell is the Phase-7 contract; Phase 8
  populates it with real rows. Topbar + sidebar + footer (Phase 6) stay
  unchanged. Add the v10 base CSS classes (`.card`, `.btn-green`, `.btn-out`,
  `.btn-navy`, `.ctitle`, `.fld`, `.invalid`, `.ieu`, `.dg`, `.db`,
  `.db.on`, `.yn-btn`, `.yn-btn.on`, `.tbadge`) to `globals.css` once and
  for all (UI-SPEC §3.2.4 acceptance gate — Plans 07-04/05 depend on these).

truths:
  - "Visiting / (home, authenticated) shows hero greeting with displayName interpolated, primary green CTA, and an empty-state recent-proposals card"
  - "Clicking the CTA routes to /proposals/new (Plan 07-04 owns the destination)"
  - "globals.css contains .card, .btn-green, .btn-navy, .btn-out, .ctitle, .fld, input.invalid + input[aria-invalid=true], .ieu, .dg, .db, .db.on, .yn-btn, .yn-btn.on, .tbadge classes"
  - "Topbar's pageTitle resolves to t('header.home', lang) — already wired by Phase 6 default"

must_haves:
  truths:
    - "app/(authed)/page.tsx renders <h1> greeting + subtext + CTA <Link href='/proposals/new'> + recent-proposals card with FileText icon empty-state body"
    - "All user-facing strings flow through t() — no hardcoded JSX text (eslint no-restricted-syntax stays green)"
    - "globals.css adds the v10 form-chrome classes used by Plans 07-04/05; existing tokens (--gd, --teal, --danger, etc.) are reused — no token redefinition"
    - "Phase 7 CTA is a Next.js <Link>, not <a> — preserves client-side routing"
  artifacts:
    - path: "app/(authed)/page.tsx"
      provides: "Home page CTA + empty-state list shell (PROP-01)"
      min_lines: 60
      contains: "/proposals/new"
    - path: "app/globals.css"
      provides: "v10 base CSS classes for forms + cards + buttons"
      contains: ".btn-green"
  key_links:
    - from: "app/(authed)/page.tsx"
      to: "/proposals/new (Plan 07-04 page)"
      via: "<Link href='/proposals/new'>"
      pattern: "href=['\"]\\/proposals\\/new['\"]"
    - from: "app/(authed)/page.tsx"
      to: "@/lib/i18n"
      via: "getCurrentLang + t()"
      pattern: "from '@/lib/i18n'"
---

<objective>
Replace the Phase-6 placeholder home body. Add the v10 base CSS classes to
globals.css so Plans 07-04/05 can rely on them as a class-based contract
rather than re-inlining v10 styles per component (UI-SPEC §3.2.4 acceptance
gate).

Purpose: PROP-01 satisfied; the partner sees a clear "Nouvelle proposition"
entry-point. Plans 07-04/05 inherit a stable `.card`/`.btn-*`/`.fld`/etc.
class surface.

Output: `app/(authed)/page.tsx` rewritten; `app/globals.css` extended.
Topbar already accepts a `pageTitle` prop (default `t('header.home', lang)`)
so no Topbar changes are needed for Phase-7 home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-calc-engine-port-proposal-form/07-CONTEXT.md
@.planning/phases/07-calc-engine-port-proposal-form/07-UI-SPEC.md
@app/(authed)/page.tsx
@app/(authed)/layout.tsx
@app/globals.css
@src/components/Topbar.tsx
@src/lib/i18n/index.ts
@src/lib/i18n/dictionaries.ts
@Matrice_2026_THE_Leasetic-v10.html

<interfaces>
<!-- Phase 6 sets up the (authed) shell; this plan replaces only the page body. -->

From src/components/Topbar.tsx (Phase 6):
```ts
export interface TopbarProps {
  displayName: string;
  email: string;
  lang: Lang;
  theme: 'light' | 'dark' | 'system';
  isAdmin?: boolean;
  pageTitle?: string;       // optional override; defaults to t('header.home', lang)
}
```

From app/(authed)/layout.tsx (Phase 6 — owns the shell, mounts Topbar with displayName already resolved):
- requireUser() runs first
- displayName fallback chain: session.user.displayName ?? session.user.name ?? session.user.email
- main content wraps {children} in a max-1100px column with --paper bg

From src/lib/i18n/index.ts:
```ts
export async function getCurrentLang(): Promise<'fr' | 'en'>;
export function t(key: DictKey, lang: 'fr' | 'en'): string;
```
NOTE: t() does not interpolate; consumer does .replace('{0}', value).

From @/lib/auth/require.ts:
```ts
export async function requireUser(): Promise<{ session, role: 'partner' | 'admin' }>;
```

From `next/link`:
```ts
import Link from 'next/link';
<Link href="/proposals/new">…</Link>
```

NOTE on imports for `app/` paths: app/ is OUTSIDE the `@/*` alias (which maps `./src/*`). Use `@/lib/...` for src imports; for sibling app files, use relative paths (rare in this plan — only the page itself is touched).
</interfaces>

<v10_class_contract>
The classes Plan 07-04/05 will use as their visual contract. Source: v10 inline styles + UI-SPEC §3.2.2/§3.2.4/§3.2.5/§3.2.6. Tokens (`--surface`, `--gd`, etc.) already exist in `globals.css` from Phase 5/6.

Required additions to `app/globals.css`:

```css
/* === v10 base classes — added in Plan 07-03 for Plans 07-04/05 to consume === */
/* Source: 07-UI-SPEC.md §3.2.2 (card chrome), §3.2.4 (input chrome),
   §3.2.5 (composite controls), §3.2.6 (action row), §5.1 (blur validation) */

/* Card chrome — UI-SPEC §3.2.2 */
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 28px;
  box-shadow: var(--shadow-card);
}

/* Section title row — UI-SPEC §3.2.2 */
.ctitle {
  display: flex;
  align-items: center;
  gap: 8px;
  text-transform: uppercase;
  font-size: 11.8px;
  font-weight: 700;
  letter-spacing: 0.06em;
  color: var(--muted);
  margin-bottom: 16px;
}
.ctitle .dot {
  width: 8px;
  height: 8px;
  border-radius: 9999px;
  background: var(--gd);
  flex-shrink: 0;
}

/* Field row (.fld + .fld label + .fld input wrapper) — UI-SPEC §3.2.3 */
.fld {
  margin-bottom: 12px;
}
.fld + .fld { margin-top: 12px; }
.fld > label {
  display: block;
  font-size: 11.2px;
  font-weight: 500;
  color: var(--muted);
  margin-bottom: 4px;
}
.fld .req { color: var(--danger); margin-left: 2px; }
.fld .error-msg {
  font-size: 11.2px;
  font-weight: 500;
  color: var(--danger);
  line-height: 1.4;
  margin-top: 4px;
}

/* Input chrome — UI-SPEC §3.2.4 */
.fld input[type="text"],
.fld input[type="email"],
.fld input[type="tel"],
.fld input[type="number"] {
  width: 100%;
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 0.65rem 0.9rem;
  font-family: var(--font-sans);
  font-size: 14px;
  color: var(--ink);
  background: var(--surface);
  outline: none;
  transition: border-color 150ms, box-shadow 150ms;
}
.fld input:focus {
  border-color: var(--teal);
  box-shadow: 0 0 0 3px rgba(45, 122, 140, 0.12);
}
input.invalid,
input[aria-invalid="true"] {
  border-color: var(--danger) !important;
  box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.12) !important;
}

/* Currency-input addon ("75 000 [€ HT]") — UI-SPEC §3.2.5 */
.ieu {
  display: flex;
  align-items: stretch;
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
  transition: border-color 150ms, box-shadow 150ms;
}
.ieu:focus-within {
  border-color: var(--teal);
  box-shadow: 0 0 0 3px rgba(45, 122, 140, 0.12);
}
.ieu.invalid,
.ieu[aria-invalid="true"] {
  border-color: var(--danger) !important;
  box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.12) !important;
}
.ieu input {
  flex: 1;
  border: none !important;
  border-radius: 0 !important;
  padding-right: 1rem;
  background: transparent;
  outline: none;
}
.ieu input:focus {
  box-shadow: none !important;          /* parent .ieu:focus-within owns the ring */
}
.ieu .suffix {
  padding: 0.65rem 0.9rem;
  background: var(--surface);
  border-left: 1px solid var(--border);
  color: var(--muted);
  font-size: 14px;
  font-weight: 500;
  display: flex;
  align-items: center;
}

/* Tranche badge (auto-shown below amount) — UI-SPEC §3.2.5 */
.tbadge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  margin-top: 6px;
  padding: 4px 10px;
  border-radius: 9999px;
  background: rgba(45, 122, 140, 0.08);
  color: var(--teal);
  font-size: 11.2px;
  font-weight: 600;
}

/* Duration / Validity segmented (.dg .db / .db.on) — UI-SPEC §3.2.5 */
.dg {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0;
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
  transition: border-color 150ms, box-shadow 150ms;
}
.dg.invalid,
.dg[aria-invalid="true"] {
  border-color: var(--danger);
  box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.12);
}
.dg .db {
  padding: 0.65rem 0.5rem;
  background: var(--surface);
  color: var(--ink);
  border: none;
  border-right: 1px solid var(--border);
  font-family: inherit;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: background 150ms;
}
.dg .db:last-child { border-right: none; }
.dg .db:hover { background: var(--hover-overlay); }
.dg .db.on {
  background: var(--gd);
  color: #ffffff;
  font-weight: 600;
}

/* Yes/No toggle (.yn-btn / .yn-btn.on) — UI-SPEC §5 */
.yn-group {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0;
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
}
.yn-btn {
  padding: 0.65rem 0.5rem;
  background: var(--surface);
  color: var(--ink);
  border: none;
  border-right: 1px solid var(--border);
  font-family: inherit;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: background 150ms;
}
.yn-btn:last-child { border-right: none; }
.yn-btn:hover { background: var(--hover-overlay); }
.yn-btn.on {
  background: var(--gd);
  color: #ffffff;
  font-weight: 600;
}

/* Buttons — Phase 5 tokens; concrete classes added here for Plans 07-03/04/05 */
.btn-green,
.btn-navy,
.btn-out {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 0.6rem 1.5rem;
  border-radius: 9999px;
  font-family: inherit;
  font-size: 14px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: background 150ms, color 150ms, box-shadow 150ms;
}
.btn-green       { background: var(--gd);  color: #ffffff; }
.btn-green:hover { background: #0d7d49; }
.btn-navy        { background: var(--navy); color: #ffffff; }
.btn-navy:hover  { background: var(--navy-dark); }
.btn-out         { background: transparent; color: var(--ink); border: 1px solid var(--border); }
.btn-out:hover   { background: var(--hover-overlay); }
.btn-green:focus-visible,
.btn-navy:focus-visible,
.btn-out:focus-visible {
  outline: none;
  box-shadow: 0 0 0 3px rgba(45, 122, 140, 0.18);
}
.btn-green:disabled,
.btn-navy:disabled,
.btn-out:disabled {
  pointer-events: none;
  opacity: 0.6;
}
```
</v10_class_contract>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add v10 base CSS classes to globals.css (one-shot for Plans 07-03/04/05)</name>
  <files>app/globals.css</files>
  <read_first>
    - `app/globals.css` (current state — Phase 5 token spine, no v10 base classes yet)
    - `.planning/phases/07-calc-engine-port-proposal-form/07-UI-SPEC.md` §3.2.2, §3.2.4, §3.2.5, §3.2.6, §5.1
    - `Matrice_2026_THE_Leasetic-v10.html` lines 200-260 (v10 base styles for `.fld`, `.invalid`, `.ieu`, `.dg`, `.db`, `.yn-btn`, `.ctitle`)
    - `src/components/LoginForm.tsx` (Phase 6 example of inline-styled `.btn-green` button — confirm class name choice matches existing references)
  </read_first>
  <action>
**Append** the entire CSS block from `<v10_class_contract>` above to the end of `app/globals.css`. The block is a single self-contained section preceded by a `/* === v10 base classes — added in Plan 07-03 ===*/` separator comment. Do NOT modify any existing rules.

After: `npm run build` MUST exit 0 (no CSS-parsing errors). Optionally `npm run dev` smoke test in Chrome to verify Phase 6 LoginForm + UserMenu still render correctly (the new classes are purely additive — no Phase-6 element should change visually).

The classes inserted are pure-CSS using `var(--*)` tokens — no Tailwind utilities, no PostCSS plugins beyond what Phase 5 already configured. Phase 5 + Phase 6 components that referenced `.btn-green` as `className="btn-green"` (LoginForm, SetPasswordForm, InviteUrlModal, error.tsx, not-found.tsx) will now have BOTH the inline `style={{ background: 'var(--gd)', ... }}` AND the matching `.btn-green` class rule. The visual result is unchanged (same colors); the new rule simply provides a CSS-class-based contract Phases 7-10 can rely on.
  </action>
  <verify>
    <automated>npm run build && grep -c "^\\.btn-green" app/globals.css && grep -c "^\\.card" app/globals.css && grep -c "^\\.fld" app/globals.css && grep -c "^\\.dg " app/globals.css && grep -c "input\\[aria-invalid" app/globals.css && grep -c "\\.ieu" app/globals.css && grep -c "\\.tbadge" app/globals.css</automated>
  </verify>
  <done>
    - `app/globals.css` contains all classes from `<v10_class_contract>`: `.card`, `.ctitle`, `.fld`, `input.invalid`, `input[aria-invalid="true"]`, `.ieu`, `.tbadge`, `.dg`, `.db`, `.db.on`, `.yn-btn`, `.yn-btn.on`, `.btn-green`, `.btn-navy`, `.btn-out`
    - `npm run build` exits 0 (CSS parses)
    - Existing Phase 6 LoginForm + InviteUrlModal pages still render (no visual regression — verifiable with `npm run dev` + browser smoke OR via Phase-6 test count not regressing)
    - All new selectors use existing `var(--*)` tokens (no token redefinition — verify zero new `--<token>:` declarations were added)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace app/(authed)/page.tsx with Phase-7 home shell (CTA + empty-state list)</name>
  <files>app/(authed)/page.tsx</files>
  <read_first>
    - `app/(authed)/page.tsx` (current Phase-6 placeholder — to be REPLACED)
    - `app/(authed)/layout.tsx` (verify shell — displayName, isAdmin already wired into Topbar)
    - `src/lib/i18n/dictionaries.ts` (verify Plan 07-06 added all dashboard.* + header.proposals.new keys)
    - `.planning/phases/07-calc-engine-port-proposal-form/07-UI-SPEC.md` §3.1 (Home page contract: greeting card 32px margin-bottom, CTA max-content with leading Plus icon, empty-state with FileText 38px)
    - `src/components/LoginForm.tsx` (analog: 'use client' patterns are NOT needed here — page.tsx is a Server Component)
    - `src/lib/auth/require.ts` (requireUser pattern)
  </read_first>
  <action>
**Replace** `app/(authed)/page.tsx` entirely. The new file is a SERVER component (no 'use client') — the only "client" interactivity is the `<Link>` from next/link which is RSC-compatible.

```tsx
import Link from 'next/link';
import { Plus, FileText } from 'lucide-react';
import { requireUser } from '@/lib/auth/require';
import { getCurrentLang, t } from '@/lib/i18n';

// PITFALLS §1.6: cookie-reading layout opts out of static rendering.
export const dynamic = 'force-dynamic';

/**
 * Authenticated home page (PROP-01 — Phase 7).
 *
 * Replaces Phase 6's minimal placeholder body. Phase 8 will populate the
 * recent-proposals card with real DB rows. The shell (sidebar + topbar +
 * footer) is owned by app/(authed)/layout.tsx and stays unchanged.
 *
 * Defence in depth: requireUser() runs again here even though the parent
 * layout already gated the route — same pattern as Plan 06-07's admin tree
 * (see require.ts comment block on PITFALLS §7.3 ordering).
 */
export default async function HomePage() {
  const { session } = await requireUser();
  const lang = await getCurrentLang();

  // Same fallback chain Phase 6 layout uses for the topbar's UserMenu.
  const u = session.user as {
    email: string;
    displayName?: string | null;
    name?: string | null;
  };
  const displayName = u.displayName ?? u.name ?? u.email;

  // dashboard.greeting = "Bonjour, {0} 👋" / "Hello, {0} 👋" — manual interpolation
  // because Phase 6's t() helper does not auto-interpolate. The greeting goes
  // through JSXText so React's child-escape covers any HTML chars in displayName.
  const greeting = t('dashboard.greeting', lang).replace('{0}', displayName);

  return (
    <div>
      {/* Greeting section — UI-SPEC §3.1.2 (no card chrome; sits on --paper) */}
      <section style={{ marginBottom: 32 }}>
        <h1
          style={{
            fontSize: '24px',
            fontWeight: 700,
            color: 'var(--ink)',
            marginBottom: 8,
          }}
        >
          {greeting}
        </h1>
        <p
          style={{
            fontSize: '14.5px',
            fontWeight: 400,
            color: 'var(--muted)',
            marginBottom: 24,
          }}
        >
          {t('dashboard.subtext', lang)}
        </p>

        {/* Primary CTA — UI-SPEC §3.1.2 */}
        <Link
          href="/proposals/new"
          className="btn-green"
          aria-label={t('dashboard.cta.new.proposal', lang)}
          style={{
            width: 'max-content',
            padding: '0.75rem 1.5rem',
            textDecoration: 'none',
          }}
        >
          <Plus size={17} strokeWidth={1.6} aria-hidden="true" />
          <span>{t('dashboard.cta.new.proposal', lang)}</span>
        </Link>
      </section>

      {/* Recent-proposals card — Phase 7 ships the SHELL ONLY (PROP-01). */}
      {/* Phase 8 will populate with real rows + search + pagination. */}
      <section className="card">
        <div className="ctitle">
          {/* No dot for this section title — purely informational header. */}
          <span>{t('dashboard.recent.title', lang)}</span>
        </div>
        {/* Separator — UI-SPEC §3.1.3 */}
        <div
          style={{
            height: 1,
            background: 'var(--border)',
            margin: '12px 0 16px',
          }}
        />

        {/* Empty-state body — UI-SPEC §3.1.3 */}
        <div
          style={{
            minHeight: 240,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            textAlign: 'center',
          }}
        >
          <FileText
            size={38}
            strokeWidth={1.3}
            color="var(--muted)"
            style={{ opacity: 0.4 }}
            aria-hidden="true"
          />
          <h2
            style={{
              fontSize: '16.5px',
              fontWeight: 600,
              color: 'var(--ink)',
              marginTop: 16,
              marginBottom: 8,
            }}
          >
            {t('dashboard.empty.title', lang)}
          </h2>
          <p
            style={{
              fontSize: '14.5px',
              fontWeight: 400,
              color: 'var(--muted)',
              maxWidth: 480,
            }}
          >
            {t('dashboard.empty.body', lang)}
          </p>
        </div>
      </section>
    </div>
  );
}
```

After: `npm run typecheck && npm run lint:check && npm run build` MUST all exit 0.

The `welcomeHeading` + `welcomeSubtext` legacy keys (Phase 5 placeholders) remain in `dictionaries.ts` for backward compat — DO NOT delete them in this plan; they're not used here but other phases / future plans may rely on them. (STATE.md decision pre-06: legacy keys preserved.)
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint:check && npm run build && grep -c "/proposals/new" "app/(authed)/page.tsx" && grep -c "dashboard.greeting" "app/(authed)/page.tsx" && grep -c "dashboard.cta.new.proposal" "app/(authed)/page.tsx" && grep -c "dashboard.empty.title" "app/(authed)/page.tsx" && grep -c "FileText" "app/(authed)/page.tsx" && grep -c "from 'next/link'" "app/(authed)/page.tsx"</automated>
  </verify>
  <done>
    - `app/(authed)/page.tsx` is a SERVER component (no 'use client' directive at the top)
    - Imports `Link` from `next/link`, `Plus` and `FileText` from `lucide-react`
    - Calls `requireUser()` and `getCurrentLang()` once each
    - Renders `<h1>` greeting with `displayName` interpolated via `.replace('{0}', …)`
    - Renders the CTA `<Link href="/proposals/new" className="btn-green">` with `Plus` icon at left
    - Renders a `.card` containing `.ctitle` "Propositions récentes" + separator + empty-state body with `FileText` icon
    - All user-facing strings flow through `t()` — `npm run lint:check` exits 0
    - `npm run build` shows route `ƒ /` (or `ƒ /` under (authed) group) compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session.user.displayName → home page JSX | Admin-curated value (set via grant-admin / Better Auth admin plugin); rendered via JSXText interpolation. React's child-escape sanitizes HTML special chars. |
| Link href="/proposals/new" → Plan 07-04 page | Same-origin client-side navigation; Phase 6's proxy.ts auth gate already covers /proposals/new (it's under (authed)). |
| globals.css ← Phase 7 additions | Static stylesheet; no runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | XSS | displayName rendered via `.replace('{0}', displayName)` then placed into `<h1>{greeting}</h1>` | mitigate | The interpolated string is rendered as JSXText (`{greeting}`), which React auto-escapes. The displayName itself is admin-set (validated through Better Auth admin plugin / grant-admin script); even if it contained `<script>`, JSX would render it as text. NO use of raw-HTML injection patterns in this plan. |
| T-07-03-02 | Information Disclosure | Empty-state message reveals "no proposals" status | accept | This is the explicit PROP-04 design (empty state for new partners); it's per-partner data they own. |
| T-07-03-03 | CSRF | CTA is a `<Link>` (GET navigation) — no CSRF surface | n/a | Phase 7 has no mutating action on the home page; PROP-09 (DB-write submit) is Phase 8. |
| T-07-03-04 | Spoofing | Forged session reaching /proposals/new | mitigate | requireUser() runs both in (authed)/layout.tsx AND in /proposals/new page (Plan 07-04). Better Auth session+cookie cache is verified per-request with the AUTH-16 secondary DB check (deletedAt). |
| T-07-03-05 | Tampering | Future contributor edits globals.css to introduce a token override | mitigate | Plan 07-03's verify line `[ "$(grep -cE '^[[:space:]]*--[a-z-]+:' app/globals.css | tr -d ' ')" -le N ]` could pin token count; less rigorously, PR review of globals.css diffs catches drift. The added Phase 7 classes use `var(--*)` exclusively — they do NOT introduce new token names. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint:check` exits 0 (no hardcoded JSXText)
- `npm run build` exits 0; route `/` resolves under (authed) group
- `npm test` (full suite) — Phase 6 + Phase 7 tests still pass; no regression
- Manual: `npm run dev`, log in as antoine.rousseau@leasetic.com, visit `/`, verify:
  - Greeting "Bonjour, Antoine 👋" (or fallback chain) renders
  - "Nouvelle proposition" green CTA visible
  - "Propositions récentes" card with FileText icon empty state
  - Clicking CTA navigates to `/proposals/new` (404 until Plan 07-04 lands — that's expected in Wave 3 sequencing)
- All required CSS classes from Task 1 present and applied where Phase 6 components reference them by className
</verification>

<success_criteria>
- `app/(authed)/page.tsx` REPLACES Phase 6 placeholder with the Phase 7 home shell
- Primary CTA `<Link href="/proposals/new">` clearly visible and routes correctly
- Empty-state recent-proposals card renders with FileText icon + i18n copy
- `globals.css` extended with v10 base classes (15+ class rules added) — Plans 07-04/05 can now use them as a class-based contract
- All user-facing strings via `t()` — ESLint hardcoded-JSXText rule still passes
- PROP-01 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-calc-engine-port-proposal-form/07-03-SUMMARY.md` capturing:
- Final list of CSS classes added to globals.css (with line counts)
- Confirmation that no `--<token>:` declarations were added (only `var(--*)` references)
- Phase 6 visual regression check result (manual: LoginForm, InviteUrlModal, UserMenu, error/404 still render correctly)
- Any deviations with rationale
</output>
