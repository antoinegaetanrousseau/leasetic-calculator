---
phase: 18-admin-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/(admin)/[adminSegment]/page.tsx
  - app/(admin)/[adminSegment]/page.test.tsx
  - app/(admin)/[adminSegment]/_components/RecentActivityRow.tsx
  - app/(admin)/[adminSegment]/_components/RecentActivityRow.test.tsx
  - src/components/ui/MetricTile.tsx
  - src/components/ui/MetricTile.test.tsx
autonomous: true
requirements: [ADMIN-10, THEME-02]
must_haves:
  truths:
    - "Admin Home renders PageHero with ADMIN eyebrow + Nouvelle proposition CTA (D-29 dark via cascade)"
    - "3 stat tiles render: Partenaires actifs (D-01 active count + sur N comptes sublabel), Propositions ce mois (D-02 cross-partner monthly count + Europe/Paris month label sublabel), Dernière modif. coeffs (D-03 relative-time value + date + author sublabel)"
    - "ALL 3 stat tile values render in --teal per D-04 (user-confirmed deviation from Figma gold for Dernière modif. coeffs)"
    - "3 AdminNavCards reuse Phase 14 component verbatim — Coefficients / Partenaires / Historique (Historique retained as card despite removal from sidebar D-27)"
    - "Recent activity card renders top 5 rows from getRecentAdminActivity (D-06), read-only (D-07), with Voir tout → link to /history"
    - "Empty state copy renders when zero activity (Aucune activité récente.)"
  artifacts:
    - path: app/(admin)/[adminSegment]/page.tsx
      provides: "Admin Home rewrite per Figma 41:46 + D-01..D-07 + D-29"
      contains: "PageHero"
    - path: app/(admin)/[adminSegment]/_components/RecentActivityRow.tsx
      provides: "Read-only activity row (avatar + sentence + relative timestamp; no row-click handler per D-07)"
      contains: "ActivityRow"
    - path: src/components/ui/MetricTile.tsx
      provides: "valueColor prop extension to allow --teal override for ADMIN stat tiles (D-04)"
      contains: "valueColor"
  key_links:
    - from: app/(admin)/[adminSegment]/page.tsx
      to: src/lib/db/queries/partner-aggregates.ts + proposal-aggregates.ts + admin-activity.ts
      via: "server-component await of getActivePartnerCount / getTotalPartnerAccountCount / getMonthlyProposalCountAll / getRecentAdminActivity / coefficient_history latest row"
      pattern: "getActivePartnerCount|getMonthlyProposalCountAll|getRecentAdminActivity"
    - from: app/(admin)/[adminSegment]/page.tsx
      to: src/components/ui/MetricTile.tsx
      via: "valueColor='var(--teal)' on all 3 instances per D-04"
      pattern: "valueColor"
---

<objective>
Rewrite the admin home page per Figma `41:46` + UI-SPEC §Admin Home contract: PageHero with ADMIN badge + Nouvelle proposition CTA, then a 3-up grid of stat tiles (all teal-valued per D-04), then a 3-up grid of AdminNavCards (existing Phase 14 component, unchanged), then a Recent activity card (top 5, read-only, Voir tout to /history).

Purpose: ADMIN-10 success criterion 1 + THEME-02 admin home half. Consumes Wave-1 helpers (Plan 01).
Output: Rewritten Admin Home page.tsx + new RecentActivityRow component + minimal MetricTile extension (valueColor prop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/18-admin-surfaces/18-01-PLAN.md

<interfaces>
<!-- From Plan 18-01 (Wave 1) -->
- `getActivePartnerCount(): Promise<number>` from src/lib/db/queries/partner-aggregates.ts (D-01)
- `getTotalPartnerAccountCount(): Promise<number>` from src/lib/db/queries/partner-aggregates.ts (D-01 sublabel)
- `getMonthlyProposalCountAll(): Promise<number>` from src/lib/db/queries/proposal-aggregates.ts (D-02)
- `getRecentAdminActivity({limit:5}): Promise<ActivityRow[]>` from src/lib/db/queries/admin-activity.ts (D-05)
- `ActivityRow { id, source, actorName, actorEmail?, sentenceKey, sentenceArgs, timestamp }` (D-05 shape)

<!-- From existing codebase -->
- `<PageHero>` from src/components/ui/PageHero.tsx (Phase 16) — accepts eyebrow, title, subtitle, actions slot
- `<MetricTile>` from src/components/ui/MetricTile.tsx (Phase 11 COMP-03) — accepts label, value, sublabel; Phase 18 ADDS valueColor prop (D-04 enables --teal override on all 3 instances)
- `<AdminNavCard>` from src/components/ui/AdminNavCard.tsx (Phase 14) — unchanged; 3 cards: Coefficients / Partenaires / Historique
- `requireAdmin()` from src/lib/auth/require.ts — server-component gate
- `coefficient_history` table: latest row read for D-03 (`il y a Xj` from createdAt + author name from JOIN users)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MetricTile with valueColor prop (D-04)</name>
  <files>src/components/ui/MetricTile.tsx, src/components/ui/MetricTile.test.tsx</files>
  <read_first>
    - src/components/ui/MetricTile.tsx (full file — current prop shape, default value color, inline styles)
    - src/components/ui/MetricTile.test.tsx (existing tests to extend)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Admin Home stat tile component choice (line 220-231 area) and §Typography Subsection B stat tile value row (line 134-136 — Phase 11 origin + D-04 override note)
  </read_first>
  <behavior>
    - Test 1: rendering `<MetricTile label="X" value="Y" />` without valueColor uses the Phase 11 default color (typically `--ink` or similar — verify from existing file)
    - Test 2: rendering `<MetricTile label="X" value="Y" valueColor="var(--teal)" />` applies `color:var(--teal)` to the value element
    - Test 3: existing Phase 11 + Phase 17 callers (Partner Home MetricTiles) continue to render with the default color (no regression — verify by grepping existing usages and confirming they don't pass valueColor)
  </behavior>
  <action>
    Per D-04 — extend `MetricTile` with an optional `valueColor` prop that overrides the value element's color when provided.

    1. Add prop to the type/interface: `valueColor?: string` (CSS color value — typically a `var(--token)` reference).
    2. In the value element's inline style, apply `style={{ color: valueColor ?? <existing default> }}` — preserve the existing default behavior when prop is absent.
    3. JSDoc comment on the prop: "Override the value text color. Phase 18 D-04 uses `var(--teal)` for all 3 admin home stat tiles (user-confirmed deviation from Figma gold for 'Dernière modif. coeffs')."
    4. Update tests with Tests 1-3.

    DO NOT introduce a `variant` prop or change the component's default behavior — minimal API extension only.
  </action>
  <verify>
    <automated>npm test -- src/components/ui/MetricTile.test.tsx</automated>
  </verify>
  <done>
    - `grep -n "valueColor" src/components/ui/MetricTile.tsx` returns at least 2 hits (interface + style)
    - 3 behavior tests pass; existing MetricTile tests + existing Partner Home consumer tests remain green
    - No call site outside Phase 18 needs to be modified (additive prop)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RecentActivityRow component (D-06/D-07)</name>
  <files>app/(admin)/[adminSegment]/_components/RecentActivityRow.tsx, app/(admin)/[adminSegment]/_components/RecentActivityRow.test.tsx</files>
  <read_first>
    - src/lib/db/queries/admin-activity.ts (from Plan 18-01 — ActivityRow type definition; sentenceKey + sentenceArgs convention)
    - src/lib/i18n/index.ts (verify `t(key, lang, args?: string[])` signature for interpolation with sentenceArgs)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Admin Home Recent activity card row layout (line 237-243 area)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-06 top 5 + Voir tout link; D-07 read-only no row links no hover)
  </read_first>
  <behavior>
    - Test 1: given `{ source: 'coefficient_history', actorName: 'Antoine R.', sentenceKey: 'admin.home.activity.sentence.coefficientModified', sentenceArgs: ['Antoine R.'], timestamp: <2 hours ago> }` → renders sentence "Antoine R. a modifié les coefficients" + "il y a 2h" relative timestamp + initials avatar "AR"
    - Test 2: render does NOT contain any clickable element (no `<a>`, no `<button>`, no `role="link"`, no `onClick`) — per D-07
    - Test 3: avatar initials are derived from actorName (first letter of each word, max 2 letters, uppercase). "Antoine R." → "AR"; "Emmanuel Lobet" → "EL"; "Solo" → "S".
    - Test 4: avatar background uses `var(--gd-text)` color and white text
    - Test 5: empty actorName falls back to actorEmail's first letter (e.g. "antoine@…" → "A"); if both missing, renders `?`
    - Test 6: relative timestamp uses `admin.home.activity.time.minutesAgo/.hoursAgo/.daysAgo` per UI-SPEC: <60min → `il y a X min`; <24h → `il y a Xh`; ≥24h → `il y a X jours`
  </behavior>
  <action>
    Per D-06 / D-07 / UI-SPEC §Admin Home Recent activity row layout.

    1. Create `app/(admin)/[adminSegment]/_components/RecentActivityRow.tsx`:
       - Client or server component: server-rendered is fine (no interactivity). Default to server component to keep bundle slim.
       - Props: `{ row: ActivityRow, lang: 'fr' | 'en' }` where ActivityRow is imported from `src/lib/db/queries/admin-activity.ts`.
       - Layout per UI-SPEC: `display:flex; alignItems:center; gap:12px; padding:8px 0`
       - Avatar (left, 32px circle): `width:32px; height:32px; borderRadius:9999px; background:var(--gd-text); color:#ffffff; display:flex; alignItems:center; justifyContent:center; fontSize:12.5px; fontWeight:600; flexShrink:0`. Renders derived initials.
       - Sentence (middle, flex:1): `fontSize:14.5px; fontWeight:400; color:var(--ink); lineHeight:1.55` — render `t(row.sentenceKey, lang, row.sentenceArgs)`.
       - Timestamp (right): `fontSize:12.5px; fontWeight:500; color:var(--muted); flexShrink:0` — relative time computed from `row.timestamp`.
       - NO onClick, NO hover effect, NO cursor:pointer per D-07.
       - Initials helper: `getInitials(name?: string, email?: string): string` — exported or colocated. First letters of first 2 words from name, uppercase. Fallback to email[0]. Fallback to `?`.
       - Relative time helper: `relativeTime(date: Date, lang: 'fr' | 'en'): string` — diff = (now - date) in ms. <60min → minutes; <24h → hours; ≥24h → days. Use the new i18n keys from Plan 18-01.

    2. Create `RecentActivityRow.test.tsx` with Tests 1-6. Mock the `t()` function or use the real dictionaries (preferred — verifies end-to-end). Use a fixed `now` via `vi.useFakeTimers().setSystemTime(...)` for deterministic relative-time tests.

    Reference: ActivityRow shape is from Plan 18-01 Task 2 (`src/lib/db/queries/admin-activity.ts`); do NOT redefine the type here — import it.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/_components/RecentActivityRow.test.tsx</automated>
  </verify>
  <done>
    - `grep -n "onClick\|cursor:.*pointer\|href" app/\(admin\)/\[adminSegment\]/_components/RecentActivityRow.tsx | grep -v '^#'` returns 0 hits in this file (D-07 read-only)
    - 6 behavior tests pass
    - Initials helper is exported or colocated and tested
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite Admin Home page.tsx per Figma 41:46 (D-01..D-07, D-29)</name>
  <files>app/(admin)/[adminSegment]/page.tsx, app/(admin)/[adminSegment]/page.test.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/page.tsx (current Phase 14 implementation — PageHero + 3 AdminNavCards. REWRITE; preserve requireAdmin() + dynamic='force-dynamic')
    - app/(admin)/[adminSegment]/page.test.tsx (existing tests — extend or rewrite to match new structure)
    - src/components/ui/PageHero.tsx (verify props for eyebrow/title/subtitle/actions slot)
    - src/components/ui/MetricTile.tsx (Plan 18-02 Task 1 — valueColor prop)
    - src/components/ui/AdminNavCard.tsx (verify exact props — Phase 14)
    - src/lib/db/queries/partner-aggregates.ts (Plan 18-01)
    - src/lib/db/queries/proposal-aggregates.ts (Plan 18-01 — getMonthlyProposalCountAll)
    - src/lib/db/queries/admin-activity.ts (Plan 18-01)
    - src/lib/db/queries/coefficient-history.ts (verify the helper to read the LATEST row — Phase 14; if not present, add a small `getLatestCoefficientHistoryRow()` helper to this file OR inline a Drizzle SELECT in page.tsx with JOIN users on createdBy)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Admin Home component contract (full section, lines 196-263)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-01..D-07 + D-29 dark cascade)
  </read_first>
  <behavior>
    - Test 1: page renders PageHero with eyebrow t('admin.home.eyebrow') = "ADMIN", title t('admin.home.title'), subtitle t('admin.home.subtitle'), and Nouvelle proposition CTA linking to /proposals/new/parametres
    - Test 2: page renders exactly 3 MetricTile instances in a 3-up grid; each receives valueColor='var(--teal)' per D-04
    - Test 3 (D-01): Partenaires actifs tile shows label, the number from getActivePartnerCount, and sublabel "sur {N} comptes" with N = getTotalPartnerAccountCount value
    - Test 4 (D-02): Propositions ce mois tile shows the number from getMonthlyProposalCountAll and the current Europe/Paris month label (e.g. "mai 2026") as sublabel
    - Test 5 (D-03): Dernière modif. coeffs tile shows "il y a Xj" relative-time value (from coefficient_history latest row.createdAt) and "{date} — {author}" sublabel
    - Test 6 (D-03 empty): if coefficient_history is empty, tile shows "—" value and empty sublabel
    - Test 7: page renders 3 AdminNavCards in a 3-up grid (Coefficients / Partenaires / Historique) using existing component verbatim — Historique RETAINED here per UI-SPEC §Admin Home layout note "History card RETAINED on Admin Home even though removed from sidebar (D-27)"
    - Test 8: page renders Recent activity card with header ACTIVITÉ RÉCENTE + Voir tout → link to /[adminSegment]/history
    - Test 9: Recent activity renders up to 5 RecentActivityRow children from getRecentAdminActivity({limit:5})
    - Test 10: empty Recent activity renders empty-state copy `Aucune activité récente.`
  </behavior>
  <action>
    Per UI-SPEC §Admin Home component contract + D-01..D-07 + D-29.

    1. Replace contents of `app/(admin)/[adminSegment]/page.tsx`:
       - Server component, `export const dynamic = 'force-dynamic'`
       - First line of body: `await requireAdmin()` (preserves Phase 6 D-18 layer-order — 404 first via the layout, role next)
       - Read lang from session/cookies (per existing pattern in this file)
       - Resolve `const adminSegment = process.env.ADMIN_URL_SEGMENT;` (or however the existing file reads it — preserve)
       - Parallel-fetch via `Promise.all([...])` of: getActivePartnerCount(), getTotalPartnerAccountCount(), getMonthlyProposalCountAll(), getRecentAdminActivity({limit:5}), and the latest coefficient_history row
       - Compute month label: `new Intl.DateTimeFormat(lang === 'fr' ? 'fr-FR' : 'en-US', { month: 'long', year: 'numeric', timeZone: 'Europe/Paris' }).format(new Date())`
       - Compute relative-days for D-03: `Math.floor((Date.now() - lastCoeffEdit.createdAt.getTime()) / (1000*60*60*24))` and i18n via `admin.home.stats.derniereModifCoeffs.value` (`il y a {0}j`)
       - Compute D-03 sublabel: `t('admin.home.stats.derniereModifCoeffs.sublabel', lang, [formattedDate, authorName])`

    2. Layout (per UI-SPEC line 200-205):
       a. `<PageHero eyebrow={t('admin.home.eyebrow', lang)} title={t('admin.home.title', lang)} subtitle={t('admin.home.subtitle', lang)} actions={<Link href="/proposals/new/parametres" className="btn-green">{t('dashboard.cta.new', lang)}</Link>} />`
       b. Stat-tile grid: a 3-up CSS grid (`gridTemplateColumns:repeat(3,1fr); gap:24; marginBottom:32`) with 3 `<MetricTile>` instances per Test 3-5 spec, all with `valueColor="var(--teal)"`
       c. AdminNavCard grid: a 3-up CSS grid with `gap:24; marginBottom:32` — 3 `<AdminNavCard>` with the 3 existing href targets (coefficients / partners / history) per Phase 14 contract (read existing usage). NOTE: Historique card stays here per UI-SPEC line 203-204 even though removed from sidebar D-27.
       d. Recent activity card: `.card` chrome containing header row (`.ctitle` `● ACTIVITÉ RÉCENTE` left + Voir tout link right), then either the empty-state `<p>` OR a stack of `<RecentActivityRow>` children, then footer Voir tout link at bottom-right.

    3. CRITICAL — empty states:
       - Coefficient_history empty → tile value = `—`, sublabel = `''`. Do NOT crash.
       - Recent activity empty → render single `<p>` (color `var(--muted)`, fontSize `14.5px`) containing `t('admin.home.activity.empty', lang)` inside the card body.

    4. Add page tests with Tests 1-10 above. Mock the 5 query functions with `vi.mock(...)` and assert rendered output.

    5. NO inline diff/SQL — all data comes from Wave-1 helpers; this is a presentation-only file.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/page.test.tsx</automated>
  </verify>
  <done>
    - `grep -c "valueColor=\"var(--teal)\"" app/\(admin\)/\[adminSegment\]/page.tsx` returns 3 (one per stat tile per D-04)
    - `grep -n "RecentActivityRow\|admin.home.activity.empty" app/\(admin\)/\[adminSegment\]/page.tsx` returns hits
    - `grep -n "AdminNavCard" app/\(admin\)/\[adminSegment\]/page.tsx` shows 3 AdminNavCard usages (Coefficients / Partenaires / Historique)
    - 10 behavior tests pass
    - No commission references introduced: ADMIN-09 9-gate suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | requireAdmin() gates at server (Phase 6 D-18 layer order preserved) |
| coefficient_history → page render | author name read from JOIN users — auto-escaped via React text rendering |
| getRecentAdminActivity → page render | sentence interpolation via t() with sentenceArgs — args are escaped by React text rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Information disclosure (commission via Admin Home) | page.tsx + stat tiles | mitigate | Stat tiles surface counts only; AdminNavCard #1 title "Coefficients & commission" is chrome/label per Phase 14 mitigation T-14-03-02. No commission VALUES exposed. ADMIN-09 9-gate suite verifies post-change |
| T-18-02-02 | XSS via author name in Recent activity sentence | RecentActivityRow + page.tsx | mitigate | React text rendering auto-escapes; sentenceArgs values pass through `t()` which returns plain strings; no raw-HTML injection sinks present. Avatar initials helper returns plain text |
| T-18-02-03 | Information disclosure (empty-state leak about partner count) | Recent activity empty state | accept | Showing "Aucune activité récente." reveals there is no recent admin activity — this is the desired UX and does not leak partner-side data |
| T-18-02-04 | DoS via large coefficient_history row | latest-row read | accept | Single row SELECT bounded LIMIT 1; coefficient_history is admin-only writeable and audit-trail bounded; row size constant |
</threat_model>

<verification>
- `npm test -- app/\(admin\)/\[adminSegment\]/page.test.tsx app/\(admin\)/\[adminSegment\]/_components/RecentActivityRow.test.tsx src/components/ui/MetricTile.test.tsx` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (no commission leakage)
- `npx tsc --noEmit` exits 0
- Manual visual check deferred to Plan 18-07 (12-checkpoint sweep, light + dark)
</verification>

<success_criteria>
- Admin Home page rewritten per Figma 41:46 with PageHero + 3 stat tiles (all teal D-04) + 3 AdminNavCards + Recent activity card
- All 3 stat tiles consume Wave-1 cross-partner helpers and render correct values + sublabels
- Recent activity card shows top 5 read-only rows + empty state + Voir tout link
- THEME-02 dark mode verified end-to-end via token cascade (no per-component dark CSS)
- ADMIN-09 9-gate suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-02-SUMMARY.md`
</output>
