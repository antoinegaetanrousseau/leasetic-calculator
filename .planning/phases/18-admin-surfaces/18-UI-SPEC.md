---
phase: 18
slug: admin-surfaces
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-24
reviewed_at: 2026-05-24
checker_revision_pass: 0
flags_outstanding:
  - "THEME-02 dark-mode for admin surfaces is DERIVED (no painted Figma dark frames). Closing-out plan must include explicit visual sign-off on 6 surfaces × 2 modes = 12 checkpoints."
---

# Phase 18 — UI Design Contract
## Admin Surfaces + Help Center (Aide)

> Visual and interaction contract for Phase 18. Generated by gsd-ui-researcher.
> Consumed by gsd-planner, gsd-executor, gsd-ui-checker, gsd-ui-auditor.

**Scope summary:** 6 surfaces — 4 admin (Admin Home rebuild `41:46`, Partners list `42:46`, Créer partenaire visual refresh `43:46`, Coefficients visual refresh `45:46`) + 2 Aide (landing `93:177`, starter article `93:2773`-pattern). All 6 surfaces ship both light and dark (THEME-02), dark derived via Phase 16 `html[data-theme="dark"]` token cascade. Requirements: ADMIN-10/11/12/13/14, THEME-02, plus a net-new HELP-01 to be filed during planning.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled Tailwind v4 utility classes + inline `style={{}}` (matches Phase 17) |
| Preset | not applicable |
| Component library | none (no Radix, no shadcn) |
| Icon library | lucide-react (strokeWidth 1.6 default; per-component overrides as needed) |
| Font | Plus Jakarta Sans — self-hosted via `next/font/local`, registered as `--font-plus-jakarta-sans` in `app/layout.tsx` |

Source: continuity with Phase 11/13/16/17. No new dependencies introduced.

---

## Phase Goal

Apply the Figma `9:46` design system to the 4 admin surfaces and ship the net-new Aide (Help Center) flow (landing + 1 starter article), preserving the ADMIN-09 D-12 commission-invisibility envelope (9-gate grep contract stays green), the Phase 16 palette stability invariant (no new color tokens), and the Phase 17 i18n verify-and-reuse discipline (`_EnHasAllFrKeys` parity, no duplicate keys). All 6 surfaces render in light AND dark via the token cascade — no painted dark Figma frames exist for admin so the closing-out verification must include manual visual sign-off.

---

## User-Confirmed Deviations from Figma

These deviations are LOCKED from CONTEXT.md D-01..D-30 (user confirmed during `/gsd-discuss-phase`). Planner must NOT revisit them.

| # | Surface | Figma shows | Phase 18 ships | Source |
|---|---------|-------------|----------------|--------|
| 1 | Admin Home stat tiles | `Dernière modif. coeffs` value rendered in `--gold` | ALL three stat values rendered in `--teal`. `--gold` reserved for warnings only (palette discipline). | D-04 |
| 2 | Partners list filter pills | 3 tabs: `Tous / Actifs / Désactivés` | 4 tabs: `Tous / Actifs / Invités / Désactivés` (adds Invités for partner-onboarding visibility) | D-09 |
| 3 | Admin Home topbar | Spelled `Acceuil` | Correct `Accueil` | Figma typo |
| 4 | Coefficients topbar | Spelled `Coéfficients` (acute accent) | Correct `Coefficients` (matches `/coefficients` route + standard French) | Figma typo |
| 5 | Coefficients warning banner | Always visible | Dismissable per-session (×) — dismissal persists in `sessionStorage` key `gsd.coefficients.warning.dismissed` | D-20 |
| 6 | CoefficientHistorySidebar | Click-to-diff handler | Read-only rows (diff modal stays on `/history` full page only) | D-21, D-22 |
| 7 | Aide landing content | Generic placeholder (Dashly X, lorem ipsum, generic categories) | Leasétic 3-card grid: `Commencer ici` (link) / `Créer une proposition` (`Bientôt disponible` muted card) / `Contact` (mailto) | D-25 |
| 8 | Aide article content | Lorem ipsum + Dashly X placeholder visuals | Real Leasétic wizard quick-start (~500-1000 words FR + EN, Phase 17 wizard screenshots) | D-26 |
| 9 | Admin sidebar nav | Current: `Accueil / Partenaires / Coefficients / Historique` | New: `Accueil / Nouvelle proposition / Propositions / Partenaires / Coefficients / Aide` (Historique removed → moved to Admin Home AdminNavCard + Recent activity `Voir tout` link) | D-27 |
| 10 | Admin surfaces dark mode | No painted Figma dark frames exist | Derived via Phase 16 `html[data-theme="dark"]` token cascade — closing-out plan must include 12 visual checkpoints (6 surfaces × 2 modes) | D-29 |

---

## Spacing Scale

4-multiple scale (locked from Phase 11; 12px added Phase 13). No new exceptions in Phase 18.

| Token | Value | Usage in Phase 18 |
|-------|-------|-------------------|
| xs | 4px | Chip padding (horizontal), icon-text gap inside pills, overflow-menu item padding |
| sm | 8px | Eyebrow → title gap in PageHero, stat-tile label → value gap, table cell vertical padding (compact rows) |
| md-sm | 12px | Form field bottom margin (Créer partenaire), warning banner internal padding, Aide card body row gap |
| md | 16px | `.card` inner spacing supplement, filter-pill row → table gap, hero subtitle → warning banner gap, action card internal padding |
| lg | 24px | `<PageHero>` → stat-tile grid gap, 3-up grid gap, Coefficients 2-col layout gap (editor card | history sidebar), Aide 3-card grid gap |
| xl | 32px | `<PageHero>` marginBottom (D-04 from Phase 16 locked across all 6 surfaces), stat-tile grid → AdminNavCards gap, AdminNavCards → Recent activity card gap |
| 2xl | 48px | Major section breaks (inherited) |
| 3xl | 64px | Page-level spacing (inherited) |

Exceptions (all inherited from earlier phases — Phase 18 does NOT change these):

- `.card` base padding: 28px (Phase 7/11 — below-4-grid exception, locked)
- `<MetricTile>` padding: 18px 20px (Phase 11 — below-4-grid exception, locked)
- Nav item padding 10px 12px (Phase 11 inherited)
- `<RecapSection>` marginBottom: 16px (Phase 13 locked)

New Phase 18 spacing values introduced:

| Usage | Value | Rule | Element |
|-------|-------|------|---------|
| Admin Home stat-tile 3-up grid gap | 24px | 4-multiple | `display:grid; grid-template-columns:repeat(3,1fr); gap:24px` on Admin Home (matches Phase 17 partner home) |
| Recent activity row vertical gap | 16px | 4-multiple | each `<RecentActivityRow>` `marginTop:16px` (first row no gap) |
| Recent activity card `Voir tout →` margin-top | 16px | 4-multiple | matches Phase 17 `Voir toutes →` spacer |
| Partners table row vertical padding | 16px | 4-multiple | each `<tr>` `padding:16px 20px` (20px horizontal matches MetricTile horizontal precedent) |
| Partners filter pill row → table gap | 16px | 4-multiple | `marginBottom:16px` below the FilterPillRow + SearchBar combo (matches Phase 17 `/proposals`) |
| Créer partenaire form card → action card gap | 16px | 4-multiple | `marginTop:16px` on the action card (separate card, D-15) |
| Coefficients warning banner internal padding | 12px 16px | 4-multiple | vertical 12px, horizontal 16px |
| Coefficients warning banner → editor card gap | 16px | 4-multiple | `marginBottom:16px` on the banner |
| Coefficients editor / history sidebar 2-col gap | 24px | 4-multiple | `display:flex; gap:24px` (matches Phase 17 step-3 2-col layout) |
| Aide landing 3-card grid gap | 24px | 4-multiple | `display:grid; grid-template-columns:repeat(3,1fr); gap:24px` |
| Aide article prose paragraph gap | 16px | 4-multiple | `<p>` default `marginBottom:16px` |
| Aide article H2 top margin | 32px | 4-multiple | section heading separation |
| Aide article screenshot block margin | 24px | 4-multiple | top + bottom around embedded wizard screenshots |

---

## Typography

Font: Plus Jakarta Sans at all sizes.

### Subsection A — Phase 18 design choices (new + modified)

Phase 18 introduces ONLY ONE net-new typographic size: the Aide article H1 (prose). All other type choices reuse Phase 11/13/16/17 sizes verbatim.

| Role | Size | Weight | Line Height | Color token | Usage |
|------|------|--------|-------------|-------------|-------|
| Aide article H1 | 28px | 700 | 1.25 | `--ink` | Top-of-article title for `/aide/commencer-ici` — **NET NEW** size |
| Aide article H2 | 20px | 600 | 1.3 | `--ink` | Section heading inside article — reuses existing size (PdfPreviewMock heading 18px is closest pre-existing; 20px is incremental and necessary for prose hierarchy — **NET NEW SIZE**) |
| Aide article body | 14.5px | 400 | 1.6 | `--ink` | Prose paragraphs — REUSES existing body size with a slightly looser line-height (1.6 vs 1.55) for long-form reading |

**Phase 18 net-new sizes: 2 (28px H1, 20px H2). Phase 18 net-new weights: 0 (700 + 600 already declared in Phase 11/16/17). Within checker limits (4 sizes max — Phase 18 declares 3 new design choices total: 2 prose sizes + 1 looser body line-height variant).**

Aide article H1 (28px/700/1.25) is intentionally smaller than PageHero hero title (32px/700/1.2) — Aide articles do NOT use PageHero; they have a custom prose chrome (see Aide Article component contract below).

### Subsection B — Pre-existing inherited sizes (verify-only, NOT changed by Phase 18)

All admin surface typography reuses Phase 17 sizes verbatim. Listed below for traceability — NOT subject to Phase 18 typography limits.

| Role | Size | Weight | Line Height | Color token | Origin | Phase 18 usage |
|------|------|--------|-------------|-------------|--------|----------------|
| PageHero hero title | 32px | 700 | 1.2 | `--ink` | Phase 16 | All 4 admin surfaces + Aide landing |
| PageHero eyebrow | 11.8px | 700 | 1.4 | `--gd-text` | Phase 16 | Admin Home (`ADMIN` badge) |
| PageHero subtitle / body | 14.5px | 400 | 1.55 | `--muted` | Phase 16 | All 4 admin surfaces + Aide landing |
| Card section title `.ctitle` | 11.8px | 700 | 1.4 | `--muted` | Phase 7 | Recent activity card header, form card sections, Aide cards |
| Stat tile label | 11.8px | 700 | 1.4 | `--muted` | Phase 11 (`<MetricTile>`) | Admin Home 3 stat tiles |
| Stat tile value | 24px | 600 | 1.3 | `--teal` (per D-04) | Phase 11 (`<MetricTile>`) | Admin Home 3 stat tiles — value color override to `--teal` for ALL |
| Stat tile sublabel | 12.5px | 500 | 1.4 | `--muted` | Phase 11 (`<MetricTile>`) | Admin Home 3 stat tiles |
| Recent activity sentence | 14.5px | 400 | 1.55 | `--ink` | Phase 17 body | Recent activity rows |
| Recent activity relative timestamp | 12.5px | 500 | 1.4 | `--muted` | Phase 17 metric sublabel | Recent activity rows |
| Table column header | 11.2px | 700 | 1.4 | `--muted` | Phase 17 label/micro (700 uppercase variant) | Partners table `<thead>` |
| Table row primary text (partner name) | 14.5px | 600 | — | `--ink` | Phase 17 ProposalRow client name | Partners table `PARTENAIRE` column |
| Table row email | 13px | 400 | — | `--muted` | Phase 17 ProposalRow date | Partners table `EMAIL` column |
| Table row date | 13px | 400 | — | `--muted` | Phase 17 ProposalRow date | Partners table `DATE CRÉATION` / `DERNIÈRE ACTIVITÉ` |
| Overflow menu item | 14px | 400 | — | `--ink` | Phase 11 input text | Partner row `⋯` dropdown items |
| Form field label | 11.2px | 500 | 1.4 | `--muted` | Phase 11 `.fld label` | Créer partenaire field labels |
| Form field input | 14px | 400 | — | `--ink` | Phase 11 `.fld input` | Créer partenaire inputs |
| Form field error | 11.2px | 500 | 1.4 | `--danger` | Phase 11 `.fld label` (variant) | Inline RHF error messages (D-16) |
| Warning banner body | 14px | 400 | 1.5 | `--ink` | Phase 11 input text | Coefficients warning banner copy |
| Warning banner close (`×`) | 16px | 500 | — | `--muted` | n/a (inherited button) | Banner dismissal control |
| Filter pill (active) | 13px | 600 | — | `--gd-text` | Phase 17 FilterPillRow | Partners filter — 4 tabs |
| Filter pill (inactive) | 13px | 500 | — | `--muted` | Phase 17 FilterPillRow | Partners filter — 4 tabs |
| StatusChip text | 11.8px | 600 | 1.4 | variant-specific | Phase 11/14 | Partners table `STATUT` column |
| AdminNavCard title | 14.5px | 600 | — | `--ink` | Phase 14 | Admin Home 3 AdminNavCards (Coefficients / Partners / History) |
| AdminNavCard description | 13px | 400 | — | `--muted` | Phase 14 | Admin Home AdminNavCards |
| Aide landing card title | 14.5px | 600 | — | `--ink` | Phase 17 row primary | 3-card grid titles |
| Aide landing card body | 13px | 400 | 1.5 | `--muted` | Phase 17 list row date | 3-card grid body copy |
| Aide landing `Bientôt disponible` badge | 11.8px | 600 | 1.4 | `--muted` on `var(--border)` tint | Phase 11/14 StatusChip family | Disabled middle card |

---

## Color

Palette is frozen for all of v1.3 (ROADMAP §v1.3 cross-cutting invariants §3, Phase 16 D-04). Phase 18 introduces NO new token declarations (D-30).

| Role | Token | Light value | Dark value | Usage in Phase 18 |
|------|-------|-------------|------------|-------------------|
| Dominant surface (60%) | `--paper` | `#f5f7fc` | `#0c121c` | All page canvas backgrounds (6 surfaces) |
| Secondary surface (30%) | `--surface` | `#ffffff` | `#161e2d` | Cards (stat tiles, AdminNavCards, Recent activity, Partners table, form card, action card, Coefficients editor, history sidebar, warning banner, Aide cards, Aide article container) |
| Accent — brand green (text mode) | `--gd-text` | `#0e7544` | `#129657` | PageHero eyebrow (`ADMIN` badge on Admin Home), active filter-pill text, `●`-section bullets in form card sections (Créer partenaire), Aide article inline links |
| Accent — brand green | `--gd` | `#129657` | `#129657` | `.ctitle .dot` bullet on every card section header, `.btn-green` primary CTAs (`Nouvelle proposition`, `Inviter partenaire`, `Envoyer l'invitation →`), `chip-active` tint source |
| Accent — brand teal | `--teal` | `#2d7a8c` | `#2d7a8c` | **ALL three stat-tile values** on Admin Home (D-04 deviation), `Voir tout →` / `Voir toutes →` link text, `← PARTENAIRES` breadcrumb hover, input focus rings on Créer partenaire, Aide `Commencer ici` card title hover state |
| Accent — gold | `--gold` | `#e08530` | `#e08530` | Coefficients warning banner background tint source (`rgba(224,133,48,0.10)`) and accent border-left (`4px solid var(--gold)`), `StatusChip variant="invited"` tint source (existing Phase 14) |
| Accent — gold (text mode) | `--gold-text` | `#8a4e13` | `#e08530` | Coefficients warning banner icon + accent text (NOT body — body stays `--ink`), `StatusChip variant="invited"` text |
| Text primary | `--ink` | `#41423d` | `#e6e9ef` | All primary text — table rows, form inputs, banner body, article prose, card titles |
| Text secondary | `--muted` | `#6e7191` | `#8b93a8` | Subtitles, labels, table headers, inactive filter pills, AdminNavCard descriptions, Aide card body, warning banner `×` icon, Aide `Bientôt disponible` badge |
| Border | `--border` | `#d9dbe9` | `#242c3e` | Card borders, table row dividers, filter-pill inactive border, form input borders, history sidebar dividers, warning banner border (other than left-accent stripe), Aide landing card borders |
| Destructive | `--danger` | `#dc2626` | `#dc2626` | Créer partenaire form validation errors (D-16): inline red text + red border on invalid inputs |

**Accent `--gd` reserved for** (Phase 18 NEW usages): `.btn-green` on `Inviter partenaire` (Partners list hero CTA) and `Envoyer l'invitation →` (Créer partenaire submit). All other `.btn-green` usages inherited from Phase 13/17.

**Accent `--gd-text` reserved for** (Phase 18 NEW usages): PageHero eyebrow `ADMIN` badge (Admin Home, mirrors Phase 17 wizard eyebrow `ÉTAPE N SUR 3` pattern), active filter-pill text (4 tabs on Partners list), `●` section bullets on `INFORMATIONS PERSONNELLES` / `INFORMATIONS SOCIÉTÉ` / `MESSAGE D'INVITATION` headers in Créer partenaire form card (already established Phase 14 pattern).

**Accent `--teal` reserved for** (Phase 18 NEW usages): **ALL THREE** stat-tile values on Admin Home (`Partenaires actifs`, `Propositions ce mois`, `Dernière modif. coeffs`) per D-04 deviation. `Voir tout →` link text on Recent activity card. `← PARTENAIRES` breadcrumb hover color on Créer partenaire hero.

**Accent `--gold` reserved for** (Phase 18 NEW usages): Coefficients warning banner — left accent stripe `4px solid var(--gold)` + faint background tint `rgba(224,133,48,0.10)` over `--surface` `#ffffff` → `#fdf3e9` (light) / over `#161e2d` → `#1f231d` (dark). NO other Phase 18 surface uses `--gold` (palette discipline; D-04 explicit).

**`StatusChip` variants used on Partners table** (already shipped Phase 11/14):
- `variant="active"` → `--gd-text` on `rgba(18,150,87,0.10)` tint (light/dark via cascade)
- `variant="invited"` → `--gold-text` on `rgba(224,133,48,0.10)` tint
- `variant="inactive"` → `--muted` on `rgba(110,113,145,0.10)` tint (or equivalent — verify against Phase 14 implementation)

Source: `app/globals.css` `:root` + `html[data-theme="dark"]` blocks verbatim; ROADMAP §v1.3 §3 palette stability; Phase 16 D-04 invariant; Phase 18 CONTEXT.md D-30.

---

## Component Contracts

### 1. Admin Home `/(admin)/[adminSegment]/page.tsx` (ADMIN-10)

**Directive:** server component, `requireAdmin()` + `export const dynamic = 'force-dynamic'`. REWRITES the existing Phase 14 layout (current page renders only `<PageHero>` + 3 AdminNavCards — see `app/(admin)/[adminSegment]/page.tsx`).

**Layout (top → bottom):**
1. `<PageHero>` — eyebrow `ADMIN` badge + title `Administration` + subtitle + `Nouvelle proposition` CTA on right (`actions` slot)
2. Stat-tile 3-up grid — `display:grid; grid-template-columns:repeat(3,1fr); gap:24px; marginBottom:32px`
3. AdminNavCard 3-up grid — `display:grid; grid-template-columns:repeat(3,1fr); gap:24px; marginBottom:32px` (REUSES Phase 14 `<AdminNavCard>` verbatim — 3 cards: Coefficients / Partners / **History** — history retained here as a card even though removed from sidebar per D-27)
4. Recent activity `.card` — section header + 5 rows + `Voir tout →` link

**`<PageHero>` call:**
```tsx
<PageHero
  eyebrow={t('admin.home.eyebrow', lang)}          // EXISTING key — "ADMIN"
  title={t('admin.home.title', lang)}              // EXISTING key — "Administration"
  subtitle={t('admin.home.subtitle', lang)}        // EXISTING key
  actions={
    <Link href="/proposals/new/parametres" className="btn-green">
      {t('dashboard.cta.new', lang)}               // EXISTING key — "Nouvelle proposition"
    </Link>
  }
/>
```

**Stat tile component choice (Claude's Discretion in CONTEXT.md):** REUSE existing `<MetricTile>` from Phase 11. Add a new variant prop value `variant="admin-teal"` (or pass an explicit `valueColor="var(--teal)"` override prop) so all three values render in `--teal` regardless of label. Planner picks the cleaner API. Alternative: create `<AdminStatTile.tsx>` if MetricTile prop-shape doesn't fit cleanly — planner decides after reading both designs.

**Stat tile instances:**

| Tile | Label key | Value source | Sublabel source |
|------|-----------|--------------|-----------------|
| Partenaires actifs | `admin.home.stats.partenairesActifs` (NEW) | `users.role='partner' AND users.status='active'` count, cross-partner (D-01) | `admin.home.stats.partenairesActifs.sublabel` (NEW) — `sur {0} comptes` (D-01) |
| Propositions ce mois | `admin.home.stats.propositionsCeMois` (NEW) | `getMonthlyProposalCountAll()` — Phase 17 helper variant without userId filter (D-02) | `admin.home.stats.propositionsCeMois.sublabel` (NEW) — e.g. `mai 2026` (Europe/Paris) |
| Dernière modif. coeffs | `admin.home.stats.derniereModifCoeffs` (NEW) | Relative time `il y a {0}j` from `coefficient_history.last().created_at` (D-03) | `admin.home.stats.derniereModifCoeffs.sublabel` (NEW) — `{0} — {1}` with absolute date + author name |

Value color: ALWAYS `var(--teal)` per D-04. Document explicitly in the MetricTile variant prop comment block.

**AdminNavCard grid:** REUSES Phase 14 `<AdminNavCard>` exactly (no chrome changes). 3 cards: Coefficients / Partners / **History**. History card RETAINED on Admin Home even though removed from sidebar (D-27) — this is the user's deliberate single entry point to `/history`.

**Recent activity card:**
- `.card` chrome
- Header row: `.ctitle` `● ACTIVITÉ RÉCENTE` left + `<Link href={/${adminSegment}/history}>Voir tout →</Link>` right (12.5px/500/`--teal`)
- 5 rows: each row = `<RecentActivityRow>` (NEW component; name choice deferred to planner per Claude's Discretion — candidates: `RecentActivityRow`, `ActivityFeedItem`)
- Each row layout: `display:flex; alignItems:center; gap:12px; padding:8px 0`
  - Avatar circle (left, 32px diameter) — initials-based (`background:var(--gd-text)` filled, white text `font-size:12.5px/600`) — planner picks initials vs gravatar vs literal `<img>` from user profile per Claude's Discretion
  - Sentence (middle, `flex:1`) — `fontSize:14.5px; fontWeight:400; color:var(--ink); lineHeight:1.55`
  - Relative timestamp (right) — `fontSize:12.5px; fontWeight:500; color:var(--muted)`
- Rows are READ-ONLY (D-07) — no hover affordance, no row-click handler, no cursor change
- Empty state: render `<p style={{color:'var(--muted)',fontSize:'14.5px'}}>{t('admin.home.activity.empty', lang)}</p>` (e.g. `Aucune activité récente.`)
- Footer: `<Link href={/${adminSegment}/history}>Voir tout →</Link>` at bottom-right, `marginTop:16px`, `fontSize:14px; fontWeight:500; color:var(--teal); textDecoration:none`

**Data shape (planner contract — informs server query):**
```ts
type ActivityRow = {
  id: string;
  source: 'coefficient_history' | 'partner_status' | 'invitation';
  actorName: string;        // human-readable name (initials derived client-side)
  actorEmail?: string;      // for avatar fallback if no name
  sentenceKey: string;      // i18n template key
  sentenceArgs: string[];   // interpolation args
  timestamp: Date;          // for relative formatting
};
```
Query: union of 3 SQL sources (D-05), each LIMIT 10, merge + sort DESC by timestamp, slice top 5.

**Known partial accepted (D-06):** `Voir tout →` lands on `/history` which currently shows coefficient-only events. Unifying that feed is out of scope for Phase 18.

**Dark mode:** inherits via token cascade — no per-component dark CSS. Verify checklist below.

---

### 2. Partners list `/(admin)/[adminSegment]/partners/page.tsx` (ADMIN-11, ADMIN-12)

**Directive:** server component, `requireAdmin()` + `export const dynamic = 'force-dynamic'`. REWRITES existing `AccountsList.tsx` chrome AND completes the D-14 rename: file rename `AccountsList.tsx → PartnersList.tsx`, symbol rename, test file rename, all imports updated, leftover `Accounts` strings/identifiers scrubbed. Acceptance: `grep -ri "Accounts" app/\(admin\)/ src/` returns 0 hits (excluding node_modules).

**Layout (top → bottom):**
1. `<PageHero>` — title `Partenaires` + subtitle + `Inviter partenaire` CTA on right
2. Row: `<SearchBar>` left + nothing right OR `<SearchBar>` left + `<FilterPillRow>` right (planner picks layout; recommend: `<FilterPillRow>` BELOW SearchBar to match the 4-tab visual prominence)
3. Recommended: `<FilterPillRow>` on its own row, full width left-aligned — `marginBottom:16px`
4. `.card` containing styled `<table>` with 6 columns
5. Cursor-pagination footer (Phase 17 `PaginationKey` primitive)

**`<PageHero>` call:**
```tsx
<PageHero
  title={t('admin.partners.page.title', lang)}        // REUSE existing 'admin.accounts.page.title' OR re-key to 'admin.partners.page.title' per D-14 rename
  subtitle={t('admin.partners.page.subtitle', lang)}  // REUSE 'admin.accounts.page.sub' OR re-key
  actions={
    <Link href={`/${adminSegment}/partners/new`} className="btn-green">
      {t('admin.partners.invite.cta', lang)}          // NEW key — "Inviter un partenaire" (replaces 'admin.accounts.create.btn' "Nouveau partenaire")
    </Link>
  }
/>
```

**D-14 rename note for planner:** The CONTEXT.md mandates a full rename of `Accounts` → `Partners`. This includes i18n keys. Planner must decide: (a) re-key all `admin.accounts.*` → `admin.partners.*` (cleaner, more work, breaks existing tests that reference the keys), OR (b) keep existing `admin.accounts.*` keys (less work, leaves the namespace mismatch). Recommendation: (a) full re-key, since it's part of the D-14 acceptance contract.

**`<SearchBar>`:** REUSE existing component verbatim. Placeholder `t('admin.partners.search.placeholder', lang)` — REUSE `admin.accounts.search.placeholder` value `Rechercher par email ou nom…`.

**`<FilterPillRow>` — 4 tabs (D-09 deviation from Figma 3 tabs):**

EXTEND or duplicate the Phase 17 `<FilterPillRow>` to support 4 tabs. Recommendation: parameterize via a `tabs: Array<{ key: string; label: string; href: string; active: boolean }>` prop and rename to `<FilterPillTabs>` (or keep `<FilterPillRow>` with extended props — planner picks).

**Tab definitions** (URL-driven state via `<Link>`):
| Tab key | Label (FR) | Label (EN) | href (admin segment example) | Active when |
|---------|------------|------------|------------------------------|-------------|
| `all` | Tous | All | `/admin/partners` (no `status` param) | `!status` |
| `active` | Actifs | Active | `/admin/partners?status=active` | `status === 'active'` |
| `invited` | Invités | Invited | `/admin/partners?status=invited` | `status === 'invited'` |
| `inactive` | Désactivés | Disabled | `/admin/partners?status=inactive` | `status === 'inactive'` |

**Active pill styling** (matches Phase 17 `.chip-active` tint):
```
background: rgba(18, 150, 87, 0.10)
color: var(--gd-text)
fontWeight: 600
```

**Inactive pill styling:**
```
background: transparent
color: var(--muted)
fontWeight: 500
```

Shared layout: `display:inline-flex; alignItems:center; padding:6px 14px; borderRadius:9999px; fontSize:13px; textDecoration:none; transition:background 150ms, color 150ms`.

**Pill gap:** 8px (`display:flex; gap:8px` on the parent).

**Dark-mode active state** — same CONTRAST-02 Row 11 borderline issue from Phase 17 (active pill ~3.9:1 over dark surface). Planner inherits the Phase 17 decision (Option 2: introduce `--active-pill` token for dark mode). If Phase 17 shipped Option 1 (accept as-is), Phase 18 matches that decision — DO NOT change the pattern within Phase 18 since it would create inconsistency between `/proposals` and `/partners`.

**Test IDs:** `data-testid="partner-filter-pill-{key}"` on each pill (4 pills).

**Styled `<table>` — 6 columns (Figma 42:46):**

| Column | Header (FR) | Header (EN) | Width | Cell content |
|--------|-------------|-------------|-------|--------------|
| 1 | PARTENAIRE | PARTNER | flex (1fr) | Partner name (14.5px/600/`--ink`) + email below (13px/400/`--muted`) stacked OR single-line per Figma — planner picks per Figma re-render. Recommend stacked for table compactness. |
| 2 | EMAIL | EMAIL | flex (1fr) | `users.email` (13px/400/`--muted`) — REDUNDANT if column 1 uses stacked layout; planner picks (a) stack name+email in col 1 and drop col 2 OR (b) keep both columns separate. Sticking to CONTEXT.md "6 cols" spec → keep both separate, single-line. |
| 3 | DATE CRÉATION | CREATED | 140px | Formatted `users.created_at` (13px/400/`--muted`) — e.g. `12 avr. 2026` |
| 4 | DERNIÈRE ACTIVITÉ | LAST ACTIVITY | 140px | `MAX(proposals.created_at WHERE user_id=partner.id AND status != 'deleted')` formatted (D-08). Fallback `—` if zero proposals. |
| 5 | STATUT | STATUS | 100px | `<StatusChip variant={users.status} />` — variants `active` / `invited` / `inactive` |
| 6 | ⋯ | ⋯ | 48px | `<PartnerRowActions partnerId={id} status={status} />` — overflow menu trigger |

**Table chrome:**
- `<thead>`: `background:transparent; borderBottom:1px solid var(--border); padding:12px 20px`. Column headers: `11.2px/700/--muted; textTransform:uppercase; letterSpacing:0.04em; textAlign:left` (Figma `42:46`).
- `<tbody> <tr>`: `padding:16px 20px; borderBottom:1px solid var(--border)`. Last row no border-bottom.
- Row hover: `background:var(--hover-overlay)` (existing token).
- Row click target: optional — clicking the row could navigate to `/proposals?user_id={partnerId}` (D-11 net-new scope). Planner picks: (a) make `<tr>` a `<Link>`-wrapped clickable row, OR (b) leave rows non-clickable and rely on the overflow menu's `Voir les propositions du partenaire` action. Recommend (b) to avoid confusion with the overflow menu.

**`<PartnerRowActions>` — NEW client component:**

File: `app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.tsx`

Trigger: `<button>` with `⋯` icon (lucide-react `MoreVertical`), 32×32, `borderRadius:6px`, `color:var(--muted)`, hover `background:var(--hover-overlay)`.

Menu items (conditional visibility per D-10):
| Action | Label (FR) | Label (EN) | Visible when | i18n key |
|--------|------------|------------|--------------|----------|
| Resend invitation | Renvoyer l'invitation | Resend invitation | `status === 'invited'` | `admin.partners.action.resendInvitation` (NEW; REUSE `admin.accounts.action.reissue` value) |
| Disable | Désactiver le compte | Disable account | `status === 'active'` | `admin.partners.action.disable` (NEW; REUSE `admin.accounts.action.disable` value, extend wording) |
| Re-enable | Réactiver le compte | Re-enable account | `status === 'inactive'` | `admin.partners.action.enable` (NEW; REUSE `admin.accounts.action.enable` value, extend wording) |
| View proposals | Voir les propositions du partenaire | View partner's proposals | always | `admin.partners.action.viewProposals` (NEW — net-new scope D-11) |

Menu visual (popover / dropdown, anchored bottom-right of the `⋯` trigger):
- Container: `background:var(--surface); border:1px solid var(--border); borderRadius:8px; padding:4px 0; minWidth:240px; boxShadow:0 4px 16px rgba(0,0,0,0.08)`
- Item: `display:flex; alignItems:center; gap:8px; padding:8px 12px; fontSize:14px; color:var(--ink); cursor:pointer; background:transparent`
- Item hover: `background:var(--hover-overlay)`
- Implementation: planner picks (a) native `<details><summary>` (no dependency), (b) custom React state + click-outside hook (existing pattern in codebase — verify), or (c) accept a Radix Popover **only if** the codebase has already adopted Radix. Phase 17 confirmed no Radix; therefore (a) or (b). Recommend (b).

**Empty states (D-13):**
- Zero partners total: render inside `.card` body: `Aucun partenaire pour le moment.` (14.5px/400/`--muted`) + inline `<Link href="/${adminSegment}/partners/new" className="btn-green">Inviter un partenaire</Link>` below.
- Filter-empty (zero matching): `Aucun partenaire ne correspond aux filtres.` + `<Link href="/${adminSegment}/partners">Effacer les filtres →</Link>` (12.5px/500/`--teal`) that resets all filters + search query.

**Cursor pagination (D-12):** REUSE Phase 17 `PaginationKey` cursor primitive. ~20 rows per page. Footer = `<LoadMoreButton>` or equivalent. Below table, `marginTop:16px`.

**Dark mode:** inherits via token cascade. Table row hover (`--hover-overlay`) must remain legible in dark — verify in closing-out.

---

### 3. Créer partenaire `/(admin)/[adminSegment]/partners/new/page.tsx` (ADMIN-13)

**Directive:** REPAINT only — behavior (server action `adminCreateInvitation` + `InviteUrlModal`) unchanged from Phase 14. Visual refresh: PageHero adoption + action-card separation (D-15) + inline error state (D-16) + dirty-form confirm (D-18). All existing RHF + Zod resolver wiring stays.

**Layout (top → bottom):**
1. `<PageHero>` — title `Créer un partenaire` + subtitle + `← PARTENAIRES` breadcrumb link (left, ABOVE the title) — implementation choice: (a) breadcrumb in PageHero's `eyebrow` slot styled differently, OR (b) breadcrumb rendered as a separate `<Link>` ABOVE PageHero. Recommend (b) — `<Link>` with `fontSize:12.5px; fontWeight:500; color:var(--muted); textDecoration:none; marginBottom:12px; display:inline-flex; alignItems:center; gap:4px` rendered before `<PageHero>`. Hover `color:var(--teal)`.
2. Form `.card` — 3 sections with `●` bullet pattern (matches Phase 14 / Phase 17 wizard step 1)
3. Action `.card` — SEPARATE card below the form card (D-15) — `marginTop:16px`

**Breadcrumb link content:**
```tsx
<Link href={`/${adminSegment}/partners`} style={{
  fontSize: 12.5, fontWeight: 500, color: 'var(--muted)',
  textDecoration: 'none', marginBottom: 12,
  display: 'inline-flex', alignItems: 'center', gap: 4
}}>
  ← {t('admin.partners.breadcrumb.label', lang)}  {/* "PARTENAIRES" / "PARTNERS" — NEW key */}
</Link>
```
On hover: `color:var(--teal)`. Letter-spacing `0.04em` to match table-header capital treatment.

**`<PageHero>` call:**
```tsx
<PageHero
  title={t('partners.new.title', lang)}        // EXISTING — "Créer un partenaire"
  subtitle={t('partners.new.subtitle', lang)}  // EXISTING — "Renseignez les informations…"
  // NO actions slot — the submit CTA lives in the SEPARATE action card (D-15)
/>
```

**Form card (3 sections):**

Each section uses the existing `●` bullet pattern via `.ctitle`:
```tsx
<div className="ctitle"><span className="dot"></span>{t('partners.new.section.personal', lang)}</div>
```
- Section 1: `● INFORMATIONS PERSONNELLES` (EXISTING key `partners.new.section.personal`) — fields: firstName, lastName, email
- Section 2: `● INFORMATIONS SOCIÉTÉ` (EXISTING key `partners.new.section.company`) — fields: companyName, siret (optional), phone
- Section 3: `● MESSAGE D'INVITATION` (EXISTING key `partners.new.section.message`) — field: message textarea (with character counter `{0}/1000`)

Field chrome reuses `.fld` class verbatim:
```
.fld label { fontSize: 11.2px; fontWeight: 500; color: var(--muted); display: block; marginBottom: 6px }
.fld input, .fld textarea { fontSize: 14px; fontWeight: 400; color: var(--ink); background: var(--paper); border: 1px solid var(--border); borderRadius: 8px; padding: 10px 12px; width: 100%; }
.fld input:focus { borderColor: var(--teal); outline: 2px solid rgba(45,122,140,0.20); outline-offset: -1px; }
```

**Inline error state (D-16) — RHF + Zod resolver pattern (already wired Phase 14):**
- On validation failure: input gets red border `borderColor:var(--danger)` AND a red `<p>` below the input with the error message (`fontSize:11.2px; fontWeight:500; color:var(--danger); marginTop:4px`)
- Aria: `aria-invalid="true"` on the input + `aria-describedby={errorId}` linking to the error `<p id={errorId}>`
- Existing keys reused: `admin.accounts.modal.error.email.invalid`, `admin.accounts.modal.error.email.exists`, `admin.accounts.modal.error.name.required` (rename to `admin.partners.form.error.*` per D-14 rename if planner chooses re-key path; otherwise reuse keys verbatim)
- Server-action errors (network failures, server-side validation): fall back to a toast notification using the existing Phase 14 SaveConfirmModal-style toast pattern. Toast copy: `partners.new.toast.error` (EXISTING key)

**Action card (separate, D-15):**
```tsx
<div className="card" style={{ marginTop: 16, display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
  <button type="button" className="btn-out" onClick={handleCancel}>
    {t('partners.new.cancel', lang)}                {/* EXISTING — "← Annuler" */}
  </button>
  <button type="submit" className="btn-green">
    {t('admin.partners.form.submit', lang)}         {/* NEW — "Envoyer l'invitation →" (replaces existing 'partners.new.submit' "Créer le partenaire") */}
  </button>
</div>
```

Card internal padding: 16px (matches Phase 17 wizard action card pattern).

**Dirty-form confirm (D-18):**
- On `Annuler` click: if `formState.isDirty`, open a confirm dialog using existing modal primitive (or `window.confirm` as a baseline if no modal primitive exists — planner verifies)
- Dialog copy: `admin.partners.form.cancel.confirm` (NEW key) — `Vous avez des changements non enregistrés. Continuer ?` (FR) / `You have unsaved changes. Continue?` (EN)
- Confirm action: navigate to `/${adminSegment}/partners`
- Cancel action: close dialog, stay on form

**Success affordance unchanged (D-17):** `InviteUrlModal` opens with copyable invite link. No additional toast. Existing component reused verbatim.

**Dark mode:** inherits via token cascade. Form input focus-ring (`rgba(45,122,140,0.20)`) is opacity-based — renders correctly in both modes.

---

### 4. Coefficients `/(admin)/[adminSegment]/coefficients/page.tsx` (ADMIN-14)

**Directive:** REPAINT — editor card untouched structurally; warning banner added; CoefficientHistorySidebar refreshed in place per D-21/22.

**Layout (top → bottom):**
1. `<PageHero>` — title `Coefficients` (correct spelling — Figma typo `Coéfficients` ignored) + subtitle
2. `<CoefficientWarningBanner>` — NEW component, dismissable per-session (D-19/20). Full-width, BETWEEN hero subtitle and the editor card. `marginBottom:16px` to editor card.
3. 2-column layout — `display:flex; gap:24px; alignItems:flex-start`
   - Left column (`flex:1; minWidth:0`): editor `.card` (existing CoefficientsEditor) — unchanged structurally
   - Right column (`width:360px; flexShrink:0`): `<CoefficientHistorySidebar>` (refreshed in place per D-21/22)

**`<PageHero>` call:**
```tsx
<PageHero
  title={t('admin.coefficients.page.title', lang)}  // EXISTING — "Coefficients & Commission" → planner may shorten to "Coefficients" per Figma; EXISTING key may stay
  subtitle={t('admin.coefficients.page.sub', lang)} // EXISTING
/>
```

**`<CoefficientWarningBanner>` — NEW component:**

File: `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx`

Directive: `'use client'` — sessionStorage read + dismissal toggle requires client.

Visual:
```
background: rgba(224, 133, 48, 0.10)   /* over --surface → light: #fdf3e9 / dark: #1f231d */
border: 1px solid var(--border)
borderLeft: 4px solid var(--gold)
borderRadius: 8px
padding: 12px 16px
display: flex; alignItems: flex-start; gap: 12px
marginBottom: 16px
```

Content layout:
- Left: lucide-react `AlertTriangle` icon, 18px, `color:var(--gold-text)`, `flexShrink:0`, `marginTop:2px` (visual alignment with first line)
- Middle (`flex:1`): body copy — `fontSize:14px; fontWeight:400; color:var(--ink); lineHeight:1.5` — copy: `Modifier ces valeurs change le calcul de toutes les futures propositions. Les PDF déjà générés restent inchangés.` (FR, locked from Figma D-19). EN equivalent in copywriting table below.
- Right: `<button>` `×` close — `width:24px; height:24px; display:inline-flex; alignItems:center; justifyContent:center; borderRadius:4px; border:none; background:transparent; color:var(--muted); cursor:pointer; fontSize:16px; lineHeight:1; flexShrink:0`. Hover `color:var(--ink); background:var(--hover-overlay)`. `aria-label={t('admin.coefficients.warning.dismiss.aria', lang)}` (NEW key — `Masquer l'avertissement` / `Dismiss warning`).

Dismissal logic:
- On mount: read `sessionStorage.getItem('gsd.coefficients.warning.dismissed')`. If `=== '1'`, render nothing.
- On `×` click: `sessionStorage.setItem('gsd.coefficients.warning.dismissed', '1')`, then setState to hide.
- On tab close → sessionStorage cleared → banner visible again on next visit (D-20).
- SSR safety: useEffect for sessionStorage read (cannot run during SSR). Initial render = banner visible (graceful default; flicker only on first paint for users who already dismissed within the session).
- Optional planner-decided enhancement: render the banner inside a `<Suspense>`-friendly client island; or render with `visibility:hidden` initially then reveal after the sessionStorage check to avoid flicker.

**`<CoefficientHistorySidebar>` refresh (D-21/22):**

File: `app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx` (already exists — modify in place, do NOT replace)

Changes:
- **Visual chrome refresh** — match Figma `45:46` history card: tighter row design, no expand affordance
- **Remove click-to-diff handler** — rows are now read-only (D-22). The diff modal (`CoefficientDiffPanel`) stays on `/history` full-page only.
- **Preserve** — 2-column layout (already), cursor pagination (already), `/history` link at the bottom (already)

Row layout (tightened):
```
padding: 12px 16px; borderBottom: 1px solid var(--border); cursor: default (was pointer);
```
Row content (3 elements, vertical stack):
- Top line: relative time (`fontSize:13px; fontWeight:600; color:var(--ink)`) + admin name (`fontSize:13px; fontWeight:400; color:var(--muted); marginLeft:8px`)
- Middle line: change summary (`fontSize:12.5px; fontWeight:400; color:var(--muted); lineHeight:1.4`) — e.g. `Coefficient 36 mois T2: 2.95 → 3.05`
- Optional note line (if note present): `fontSize:12.5px; fontStyle:italic; color:var(--muted); marginTop:4px`

NO `onClick` handler on the row. The `+ {0} autres` link / pagination control stays as-is.

`/history` link at bottom: `<Link href={/${adminSegment}/history}>{t('admin.coefficients.history.viewAll', lang)}</Link>` (NEW key — `Voir tout l'historique →` / `View full history →`).

**Test updates required (D-22):** Phase 14 tests asserting in-card diff opening behavior must be UPDATED or REMOVED. Planner adds this to the plan's test-modification list.

**Dark mode:** inherits via token cascade. Warning banner left-accent stripe (`var(--gold)`) is the same hex in light + dark — verify visual contrast over dark tint background in closing-out.

---

### 5. Aide landing `/(authed)/aide/page.tsx` (HELP-01 net-new)

**Directive:** server component (or static — no auth-derived data on this page). `requireUser()` — both partner and admin can read. `/aide` lives under `(authed)` route group so partner + admin auth both apply (D-28).

**Layout (top → bottom):**
1. `<PageHero>` — title `Centre d'aide` + subtitle `Tout pour démarrer et bien utiliser Leasétic`
2. 3-card grid — `display:grid; grid-template-columns:repeat(3,1fr); gap:24px; maxWidth:1040px; marginTop:32px`

**`<PageHero>` call:**
```tsx
<PageHero
  title={t('aide.landing.title', lang)}        // NEW — "Centre d'aide" / "Help Center"
  subtitle={t('aide.landing.subtitle', lang)}  // NEW
/>
```

**3 cards (each `.card`, `padding:24px`):**

| # | Title (FR) | Body (FR) | Action | Disabled? |
|---|------------|-----------|--------|-----------|
| 1 | Commencer ici | Premier guide pour créer une proposition en 3 étapes. | `<Link href="/aide/commencer-ici">Lire le guide →</Link>` (`fontSize:13px; fontWeight:500; color:var(--teal)`) | NO |
| 2 | Créer une proposition | Guide détaillé du tunnel de proposition étape par étape. | `<span>` rendering `Bientôt disponible` badge — `display:inline-flex; padding:4px 10px; borderRadius:9999px; background:rgba(110,113,145,0.10); color:var(--muted); fontSize:11.8px; fontWeight:600` | YES — muted card, opacity 0.7 on entire card OR slightly desaturated chrome (planner picks) |
| 3 | Contact | Une question ? Écrivez-nous, nous vous répondons sous 24h ouvrées. | `<a href="mailto:{SUPPORT_EMAIL}">{SUPPORT_EMAIL}</a>` (`fontSize:13px; fontWeight:500; color:var(--teal)`) | NO |

**SUPPORT_EMAIL placeholder:** `support@leasetic.fr` (D-25). Planner must CONFIRM this address with the user during planning before shipping. If user wants a different address, swap the value.

**Card chrome (each):**
- Top: lucide-react icon, 24px, `color:var(--gd-text)` for cards 1+3; `color:var(--muted)` for card 2 (disabled). Recommended icons:
  - Card 1 `Commencer ici` — `BookOpen` or `Sparkles`
  - Card 2 `Créer une proposition` — `FileText`
  - Card 3 `Contact` — `Mail`
- `marginTop:16px` below icon: title (14.5px/600/`--ink`)
- `marginTop:8px` below title: body (13px/400/`--muted`/lineHeight 1.5)
- `marginTop:16px` below body: action (link OR badge)

**i18n keys (NEW):**
- `aide.landing.title` / `aide.landing.subtitle`
- `aide.landing.card.commencerIci.title` / `.body` / `.cta`
- `aide.landing.card.creerProposition.title` / `.body` / `.badge` (badge value: `Bientôt disponible` / `Coming soon`)
- `aide.landing.card.contact.title` / `.body`

**Sidebar nav inclusion (D-23/27):**
Both partner + admin sidebar configs must include an `Aide` item with target `/aide`. The sidebar nav per-role config:
- **Admin sidebar:** `Accueil / Nouvelle proposition / Propositions / Partenaires / Coefficients / Aide` (REPLACES current `Accueil / Partenaires / Coefficients / Historique`). `Historique` is REMOVED from the sidebar; access only via Admin Home AdminNavCard + Recent activity `Voir tout` link.
- **Partner sidebar:** `Accueil / Nouvelle proposition / Propositions / Aide`
- Existing sidebar i18n key REUSE: `sidebar.nav.help` (`Aide` / `Help` — already in dictionaries, line 50/759).
- Add new sidebar i18n key: `sidebar.nav.proposals` (NEW — for `Propositions` partner-facing item if it doesn't already exist; verify against Phase 17 — `proposals.title` already exists as `Mes propositions`; sidebar label may differ).
- Sidebar icon for `Aide`: planner picks (lucide `HelpCircle` or `LifeBuoy` recommended).

**Dark mode:** inherits via token cascade. Disabled card opacity (0.7) renders correctly in both modes; verify in closing-out.

---

### 6. Aide article — `Commencer ici` `/(authed)/aide/commencer-ici/page.tsx` (HELP-01)

**Directive:** server component, `requireUser()`. Content storage = hardcoded TSX (D-24) — NO markdown, NO CMS. Article copy lives in the component via `t()` keys following Phase 17 D-21 verify-and-reuse discipline.

**Layout (top → bottom, single-column, `maxWidth:720px; margin:0 auto`):**
1. Breadcrumb `<Link href="/aide">← Centre d'aide</Link>` (`fontSize:12.5px; fontWeight:500; color:var(--muted)`, hover `--teal`)
2. Article container `.card` — `padding:48px 56px` (looser than standard card to give prose room to breathe — planner may adjust to standard 28px if too generous)
3. H1 title (28px/700/1.25/`--ink`) — `Commencer ici`
4. Lead paragraph (16px/400/1.6/`--muted`) — `marginTop:16px` — 1-2 sentence intro
5. Section: `Aperçu du tunnel` — H2 (20px/600/1.3/`--ink`, `marginTop:32px`) + paragraph + ordered list of 3 steps
6. Section: `Étape 1 — Paramètres` — H2 + paragraph + Phase 17 wizard step-1 screenshot (Next `<Image>`, full container width, `borderRadius:8px; border:1px solid var(--border); margin:24px 0`)
7. Section: `Étape 2 — Calcul` — H2 + paragraph + Phase 17 wizard step-2 screenshot
8. Section: `Étape 3 — Vérifier` — H2 + paragraph + Phase 17 wizard step-3 screenshot
9. Section: `Et après ?` — H2 + closing paragraph + CTA `<Link href="/proposals/new/parametres" className="btn-green">Créer ma première proposition</Link>` (REUSES existing `dashboard.cta.new`)

**Word count target:** 500-1000 words FR + matching EN translation (D-26). Planner agent drafts initial content; user reviews/edits before executor implements.

**Screenshots (3 images):**
- Source: Phase 17 wizard surfaces (PageHero + form card for step 1, hero card + Détail card for step 2, RecapSection columns + PdfPreviewMock for step 3)
- Format: PNG, 2x DPR for retina, ~1200px wide source, served via Next `<Image>` with `width={720} height={auto}` sizing
- Location: `public/aide/wizard-step-{1,2,3}.png`
- Alt text: i18n keys (e.g. `aide.commencer-ici.screenshot.step1.alt`)
- Captured: planner or executor takes screenshots from the deployed Phase 17 wizard surfaces (light mode) — captured at standard viewport (1280px wide browser, retina)

**Prose typography (Phase 18 net-new):**
- H1: 28px/700/1.25/`--ink`
- H2: 20px/600/1.3/`--ink` (`marginTop:32px; marginBottom:16px`)
- Body `<p>`: 14.5px/400/1.6/`--ink` (`marginBottom:16px`)
- Lead paragraph (first paragraph after H1): 16px/400/1.6/`--muted` — slightly larger + muted for visual hierarchy. **This is a 4th body size — planner: if this pushes the type-system over the 4-size limit, drop the 16px lead and use the standard 14.5px body verbatim.** Recommend: drop the lead; rely on H1 + first paragraph contrast for hierarchy.
- Inline `<a>` link: `color:var(--gd-text); textDecoration:underline; textUnderlineOffset:2px`
- `<ol>` and `<ul>`: `paddingLeft:24px; marginBottom:16px`; `<li>` `marginBottom:8px`

**i18n keys (NEW):**
- `aide.commencer-ici.title`
- `aide.commencer-ici.lead`
- `aide.commencer-ici.section.overview.title` / `.body`
- `aide.commencer-ici.section.step1.title` / `.body` / `.screenshot.alt`
- `aide.commencer-ici.section.step2.title` / `.body` / `.screenshot.alt`
- `aide.commencer-ici.section.step3.title` / `.body` / `.screenshot.alt`
- `aide.commencer-ici.section.next.title` / `.body`
- `aide.commencer-ici.cta.create` (or reuse `dashboard.cta.new`)
- `aide.commencer-ici.breadcrumb` — `← Centre d'aide` / `← Help Center`

**Dark mode:** prose typography is token-driven (`--ink`, `--muted`, `--gd-text`, `--border`). Screenshots are light-mode captures — they will appear "out of place" in dark mode. Two options for the planner:
1. Accept light-mode screenshots in both modes (lower effort; screenshots are functional reference, not decorative).
2. Capture and serve dark-mode screenshots conditionally based on theme (higher effort; better visual integration). Implementation requires a client-side theme detector + `<picture>` element with `prefers-color-scheme` media query OR a React-rendered `<Image>` that swaps `src` based on the active theme.

Recommend Option 1 for Phase 18 (defer Option 2 to HELP-02 if user reports the inconsistency).

---

## Copywriting Contract

Following Phase 17 D-21 verify-and-reuse discipline. **Existing keys** = already in `src/lib/i18n/dictionaries.ts`. **NEW** = net-new key. Reused-from column lists the existing key when REUSE is recommended.

| # | Element | FR copy | EN copy | i18n key | Status | Reused-from |
|---|---------|---------|---------|----------|--------|-------------|
| **Admin Home** | | | | | | |
| 1 | PageHero eyebrow | ADMIN | ADMIN | `admin.home.eyebrow` | EXISTING | — |
| 2 | PageHero title | Administration | Administration | `admin.home.title` | EXISTING | — |
| 3 | PageHero subtitle | Gérez les paramètres globaux et les comptes | Manage global settings and accounts | `admin.home.subtitle` | EXISTING | — |
| 4 | Hero CTA | Nouvelle proposition | New proposal | `dashboard.cta.new` | EXISTING | — |
| 5 | Stat tile 1 label | Partenaires actifs | Active partners | `admin.home.stats.partenairesActifs` | **NEW** | — |
| 6 | Stat tile 1 sublabel | sur {0} comptes | of {0} accounts | `admin.home.stats.partenairesActifs.sublabel` | **NEW** | — |
| 7 | Stat tile 2 label | Propositions ce mois | Proposals this month | `admin.home.stats.propositionsCeMois` | **NEW** | — |
| 8 | Stat tile 2 sublabel | {0} | {0} (e.g. "May 2026") | `admin.home.stats.propositionsCeMois.sublabel` | **NEW** | — |
| 9 | Stat tile 3 label | Dernière modif. coeffs | Last coeff. update | `admin.home.stats.derniereModifCoeffs` | **NEW** | — |
| 10 | Stat tile 3 value (relative time template) | il y a {0}j | {0}d ago | `admin.home.stats.derniereModifCoeffs.value` | **NEW** | — |
| 11 | Stat tile 3 sublabel | {0} — {1} (date + author) | {0} — {1} | `admin.home.stats.derniereModifCoeffs.sublabel` | **NEW** | — |
| 12 | AdminNavCard 1 title | Coefficients & commission | Coefficients & commission | `admin.nav.coefficients.title` | EXISTING | — |
| 13 | AdminNavCard 1 description | Éditez les paramètres globaux | Edit global settings | `admin.nav.coefficients.description` | EXISTING | — |
| 14 | AdminNavCard 2 title | Partenaires | Partners | `admin.nav.partners.title` | EXISTING | — |
| 15 | AdminNavCard 2 description | Gérez les comptes partenaires | Manage partner accounts | `admin.nav.partners.description` | EXISTING | — |
| 16 | AdminNavCard 3 title | Historique | History | `admin.nav.history.title` | EXISTING | — |
| 17 | AdminNavCard 3 description | Historique des coefficients | Coefficient history | `admin.nav.history.description` | EXISTING | — |
| 18 | AdminNavCard open label | Ouvrir → | Open → | `admin.nav.open` | EXISTING | — |
| 19 | Recent activity card header | ACTIVITÉ RÉCENTE | RECENT ACTIVITY | `admin.home.activity.title` | **NEW** | — |
| 20 | Recent activity empty state | Aucune activité récente. | No recent activity. | `admin.home.activity.empty` | **NEW** | — |
| 21 | Recent activity `Voir tout` link | Voir tout → | View all → | `admin.home.activity.viewAll` | **NEW** | similar to `dashboard.recent.viewAll` (`Voir toutes →`) but admin-scoped phrasing |
| 22 | Recent activity sentence — coeff modified | {0} a modifié les coefficients | {0} updated the coefficients | `admin.home.activity.sentence.coefficientModified` | **NEW** | — |
| 23 | Recent activity sentence — partner invited | {0} a invité {1} | {0} invited {1} | `admin.home.activity.sentence.partnerInvited` | **NEW** | — |
| 24 | Recent activity sentence — partner activated | {0} a activé le compte de {1} | {0} activated {1}'s account | `admin.home.activity.sentence.partnerActivated` | **NEW** | — |
| 25 | Recent activity sentence — partner deactivated | {0} a désactivé le compte de {1} | {0} deactivated {1}'s account | `admin.home.activity.sentence.partnerDeactivated` | **NEW** | — |
| 26 | Recent activity relative time — minutes | il y a {0} min | {0} min ago | `admin.home.activity.time.minutesAgo` | **NEW** | similar to `admin.accounts.last_login.minutes_ago` — REUSE OK |
| 27 | Recent activity relative time — hours | il y a {0}h | {0}h ago | `admin.home.activity.time.hoursAgo` | **NEW** | REUSE `admin.accounts.last_login.hours_ago` OK |
| 28 | Recent activity relative time — days | il y a {0} jours | {0} days ago | `admin.home.activity.time.daysAgo` | **NEW** | REUSE `admin.accounts.last_login.days_ago` OK |
| **Partners list** | | | | | | |
| 29 | PageHero title | Partenaires | Partners | `admin.partners.page.title` | **NEW** (re-key) | REUSE value from `admin.accounts.page.title` |
| 30 | PageHero subtitle | Gérez les comptes partenaires et leurs accès. | Manage partner accounts and their access. | `admin.partners.page.subtitle` | **NEW** (re-key) | REUSE value from `admin.accounts.page.sub` |
| 31 | Hero CTA | Inviter un partenaire | Invite a partner | `admin.partners.invite.cta` | **NEW** | REPLACES `admin.accounts.create.btn` (`Nouveau partenaire`) |
| 32 | Search placeholder | Rechercher par email ou nom… | Search by email or name… | `admin.partners.search.placeholder` | **NEW** (re-key) | REUSE value from `admin.accounts.search.placeholder` |
| 33 | Filter pill — Tous | Tous | All | `admin.partners.filter.all` | **NEW** | — |
| 34 | Filter pill — Actifs | Actifs | Active | `admin.partners.filter.active` | **NEW** | similar to `admin.accounts.status.active` (`Actif`) but pluralized for filter |
| 35 | Filter pill — Invités | Invités | Invited | `admin.partners.filter.invited` | **NEW** | REUSE `chip.invited` (`invité.e`) value variant; filter uses plural |
| 36 | Filter pill — Désactivés | Désactivés | Disabled | `admin.partners.filter.inactive` | **NEW** | REUSE `admin.accounts.status.disabled` value variant |
| 37 | Table col 1 header | PARTENAIRE | PARTNER | `admin.partners.col.partner` | **NEW** | — |
| 38 | Table col 2 header | EMAIL | EMAIL | `admin.partners.col.email` | **NEW** (re-key) | REUSE `admin.accounts.col.email` |
| 39 | Table col 3 header | DATE CRÉATION | CREATED | `admin.partners.col.created` | **NEW** (re-key) | REUSE `admin.accounts.col.created` |
| 40 | Table col 4 header | DERNIÈRE ACTIVITÉ | LAST ACTIVITY | `admin.partners.col.lastActivity` | **NEW** | REPLACES `admin.accounts.col.last_login` (D-08 semantic change: last activity = last proposal created, not last login) |
| 41 | Table col 5 header | STATUT | STATUS | `admin.partners.col.status` | **NEW** (re-key) | REUSE `admin.accounts.col.status` |
| 42 | Table — no last activity placeholder | — | — | n/a (literal `—` em-dash) | — | — |
| 43 | Overflow action — resend invite | Renvoyer l'invitation | Resend invitation | `admin.partners.action.resendInvitation` | **NEW** (re-key) | REUSE `admin.accounts.action.reissue` |
| 44 | Overflow action — disable | Désactiver le compte | Disable account | `admin.partners.action.disableAccount` | **NEW** | extends `admin.accounts.action.disable` (`Désactiver`) with " le compte" |
| 45 | Overflow action — re-enable | Réactiver le compte | Re-enable account | `admin.partners.action.enableAccount` | **NEW** | extends `admin.accounts.action.enable` |
| 46 | Overflow action — view proposals | Voir les propositions du partenaire | View partner's proposals | `admin.partners.action.viewProposals` | **NEW** | net-new scope (D-11) |
| 47 | Empty state — zero partners total | Aucun partenaire pour le moment. | No partners yet. | `admin.partners.empty.zero` | **NEW** | similar to `admin.accounts.empty.title` |
| 48 | Empty state — zero partners CTA | Inviter un partenaire | Invite a partner | (reuse #31 `admin.partners.invite.cta`) | reuse | — |
| 49 | Empty state — filter-empty body | Aucun partenaire ne correspond aux filtres. | No partners match the filters. | `admin.partners.empty.filter` | **NEW** | — |
| 50 | Empty state — filter-empty clear link | Effacer les filtres → | Clear filters → | `admin.partners.empty.clearFilters` | **NEW** | — |
| **Créer partenaire** | | | | | | |
| 51 | Breadcrumb | PARTENAIRES | PARTNERS | `admin.partners.breadcrumb.label` | **NEW** | — |
| 52 | PageHero title | Créer un partenaire | Create a partner | `partners.new.title` | EXISTING | — |
| 53 | PageHero subtitle | Renseignez les informations du partenaire pour générer un lien d'invitation unique | Enter partner information to generate a unique invite link | `partners.new.subtitle` | EXISTING | — |
| 54 | Form section 1 header | INFORMATIONS PERSONNELLES | PERSONAL INFORMATION | `partners.new.section.personal` | EXISTING | — |
| 55 | Form section 2 header | INFORMATIONS SOCIÉTÉ | COMPANY INFORMATION | `partners.new.section.company` | EXISTING | — |
| 56 | Form section 3 header | MESSAGE D'INVITATION | INVITATION MESSAGE | `partners.new.section.message` | EXISTING | — |
| 57 | Field labels (firstName, lastName, email, companyName, siret, phone, message) | (multiple) | (multiple) | `partners.new.field.*` | EXISTING | — |
| 58 | Cancel button | ← Annuler | ← Cancel | `partners.new.cancel` | EXISTING | — |
| 59 | Submit button | Envoyer l'invitation → | Send invitation → | `admin.partners.form.submit` | **NEW** | REPLACES `partners.new.submit` (`Créer le partenaire`) — wording aligns with D-15 action card pattern |
| 60 | Submit spinner | Envoi en cours… | Sending… | `admin.partners.form.submit.spinner` | **NEW** | REPLACES `partners.new.submit.spinner` (`Création en cours…`) |
| 61 | Dirty-form confirm | Vous avez des changements non enregistrés. Continuer ? | You have unsaved changes. Continue? | `admin.partners.form.cancel.confirm` | **NEW** | — |
| 62 | Form validation errors (inline) | (multiple — invalid email, name required, etc.) | (multiple) | `partners.new.*` + `admin.accounts.modal.error.*` | EXISTING | REUSE Phase 14 keys |
| 63 | Server-action error toast | Erreur lors de la création. Réessayez. | Creation failed. Try again. | `partners.new.toast.error` | EXISTING | — |
| 64 | Success toast (success affordance is `InviteUrlModal` — no toast per D-17) | — | — | — | — | no copy |
| **Coefficients** | | | | | | |
| 65 | PageHero title | Coefficients | Coefficients | `admin.coefficients.page.title` | EXISTING (current value `Coefficients & Commission` — planner may shorten to `Coefficients` per Figma; if shortened, update existing key in place — DOES NOT count as net-new) | — |
| 66 | PageHero subtitle | Paramètres financiers globaux — toute modification crée une nouvelle ligne d'historique. | Global financial parameters — every save creates a new history entry. | `admin.coefficients.page.sub` | EXISTING | — |
| 67 | Warning banner body | Modifier ces valeurs change le calcul de toutes les futures propositions. Les PDF déjà générés restent inchangés. | Changing these values affects calculations for all future proposals. Already-generated PDFs remain unchanged. | `admin.coefficients.warning.body` | **NEW** | locked from Figma D-19 |
| 68 | Warning banner dismiss aria-label | Masquer l'avertissement | Dismiss warning | `admin.coefficients.warning.dismiss.aria` | **NEW** | — |
| 69 | History sidebar `/history` link | Voir tout l'historique → | View full history → | `admin.coefficients.history.viewAll` | **NEW** | similar to existing `admin.coefficients.history.load_more` (`Charger plus`) — different semantic |
| **Aide landing** | | | | | | |
| 70 | PageHero title | Centre d'aide | Help Center | `aide.landing.title` | **NEW** | — |
| 71 | PageHero subtitle | Tout pour démarrer et bien utiliser Leasétic | Everything you need to get started with Leasétic | `aide.landing.subtitle` | **NEW** | — |
| 72 | Card 1 title | Commencer ici | Start here | `aide.landing.card.commencerIci.title` | **NEW** | — |
| 73 | Card 1 body | Premier guide pour créer une proposition en 3 étapes. | First guide to create a proposal in 3 steps. | `aide.landing.card.commencerIci.body` | **NEW** | — |
| 74 | Card 1 CTA | Lire le guide → | Read the guide → | `aide.landing.card.commencerIci.cta` | **NEW** | — |
| 75 | Card 2 title | Créer une proposition | Create a proposal | `aide.landing.card.creerProposition.title` | **NEW** | — |
| 76 | Card 2 body | Guide détaillé du tunnel de proposition étape par étape. | Detailed guide to the proposal flow step by step. | `aide.landing.card.creerProposition.body` | **NEW** | — |
| 77 | Card 2 badge (disabled) | Bientôt disponible | Coming soon | `aide.landing.card.creerProposition.badge` | **NEW** | — |
| 78 | Card 3 title | Contact | Contact | `aide.landing.card.contact.title` | **NEW** | — |
| 79 | Card 3 body | Une question ? Écrivez-nous, nous vous répondons sous 24h ouvrées. | A question? Email us — we reply within 24 business hours. | `aide.landing.card.contact.body` | **NEW** | — |
| 80 | Sidebar nav item — Aide | Aide | Help | `sidebar.nav.help` | EXISTING | — |
| **Aide article — Commencer ici** | | | | | | |
| 81 | Breadcrumb | ← Centre d'aide | ← Help Center | `aide.commencer-ici.breadcrumb` | **NEW** | — |
| 82 | Article H1 | Commencer ici | Start here | `aide.commencer-ici.title` | **NEW** | (could reuse #72 if planner prefers; keep separate for prose context) |
| 83 | Lead paragraph | (draft during planning, ~1-2 sentences) | (draft during planning) | `aide.commencer-ici.lead` | **NEW** | — |
| 84 | Section 1 — Aperçu H2 | Aperçu du tunnel | Flow overview | `aide.commencer-ici.section.overview.title` | **NEW** | — |
| 85 | Section 1 — Aperçu body | (draft during planning) | (draft during planning) | `aide.commencer-ici.section.overview.body` | **NEW** | — |
| 86 | Section 2 — Étape 1 H2 | Étape 1 — Paramètres | Step 1 — Parameters | `aide.commencer-ici.section.step1.title` | **NEW** | — |
| 87 | Section 2 — Étape 1 body | (draft during planning) | (draft during planning) | `aide.commencer-ici.section.step1.body` | **NEW** | — |
| 88 | Section 2 — Étape 1 screenshot alt | Capture d'écran de l'étape Paramètres | Screenshot of the Parameters step | `aide.commencer-ici.section.step1.screenshot.alt` | **NEW** | — |
| 89 | Section 3 — Étape 2 H2 | Étape 2 — Calcul | Step 2 — Calculation | `aide.commencer-ici.section.step2.title` | **NEW** | — |
| 90 | Section 3 — Étape 2 body | (draft during planning) | (draft during planning) | `aide.commencer-ici.section.step2.body` | **NEW** | — |
| 91 | Section 3 — Étape 2 screenshot alt | Capture d'écran de l'étape Calcul | Screenshot of the Calculation step | `aide.commencer-ici.section.step2.screenshot.alt` | **NEW** | — |
| 92 | Section 4 — Étape 3 H2 | Étape 3 — Vérifier | Step 3 — Review | `aide.commencer-ici.section.step3.title` | **NEW** | — |
| 93 | Section 4 — Étape 3 body | (draft during planning) | (draft during planning) | `aide.commencer-ici.section.step3.body` | **NEW** | — |
| 94 | Section 4 — Étape 3 screenshot alt | Capture d'écran de l'étape Vérifier | Screenshot of the Review step | `aide.commencer-ici.section.step3.screenshot.alt` | **NEW** | — |
| 95 | Section 5 — Et après H2 | Et après ? | What's next? | `aide.commencer-ici.section.next.title` | **NEW** | — |
| 96 | Section 5 — Et après body | (draft during planning) | (draft during planning) | `aide.commencer-ici.section.next.body` | **NEW** | — |
| 97 | CTA at end | Créer ma première proposition | Create my first proposal | `aide.commencer-ici.cta.create` | **NEW** | could REUSE `dashboard.cta.new` (`Nouvelle proposition`) — planner picks; recommend separate for prose context warmth |

**Net-new i18n key count:** approximately **70 keys** (counting both languages: 70 FR + 70 EN = 140 dictionary entries). Bulk of net-new keys live in:
- `admin.home.stats.*` (7 keys)
- `admin.home.activity.*` (10 keys)
- `admin.partners.*` (23 keys, ~half are re-keys of existing `admin.accounts.*` values per D-14 rename)
- `admin.partners.form.*` (3 keys)
- `admin.coefficients.warning.*` (2 keys) + `.history.viewAll` (1 key)
- `aide.landing.*` (10 keys)
- `aide.commencer-ici.*` (17 keys including screenshot alts)

**i18n discipline reminder (Phase 17 D-21):** before adding each key, planner must `grep -n "{key}" src/lib/i18n/dictionaries.ts` to verify the key does not already exist with the desired value. Many `admin.partners.*` keys SHOULD reuse the values from existing `admin.accounts.*` keys — the rename is mechanical, the copy stays the same. Maintain `_EnHasAllFrKeys` parity proof at compile time.

---

## Color Token Audit — Per Surface

Verifies that every visual element on each surface resolves to an existing token (no new tokens per D-30).

### Admin Home

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | `#f5f7fc` | `#0c121c` | inherited |
| Card chrome (stat tiles, AdminNavCards, Recent activity) | `--surface` / `--border` | `#ffffff` / `#d9dbe9` | `#161e2d` / `#242c3e` | inherited |
| Hero eyebrow `ADMIN` badge | `--gd-text` | `#0e7544` | `#129657` | inherited |
| Hero title | `--ink` | `#41423d` | `#e6e9ef` | inherited |
| Hero subtitle | `--muted` | `#6e7191` | `#8b93a8` | inherited |
| Hero CTA bg | `--gd` | `#129657` | `#129657` | stable across modes |
| Stat tile label | `--muted` | — | — | inherited |
| **Stat tile value (ALL 3)** | `--teal` | `#2d7a8c` | `#2d7a8c` | **D-04 deviation locked** |
| Stat tile sublabel | `--muted` | — | — | inherited |
| AdminNavCard chrome | inherited Phase 14 — no change | — | — | — |
| Recent activity row sentence | `--ink` | — | — | inherited |
| Recent activity row timestamp | `--muted` | — | — | inherited |
| Recent activity row avatar bg | `--gd-text` | `#0e7544` | `#129657` | stable |
| Recent activity row avatar text | white `#ffffff` | — | — | stable across modes |
| `Voir tout →` link | `--teal` | — | — | inherited |

### Partners list

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | — | — | inherited |
| Card chrome | `--surface` / `--border` | — | — | inherited |
| Hero title / subtitle | `--ink` / `--muted` | — | — | inherited |
| Hero CTA `Inviter un partenaire` | `--gd` | — | — | inherited |
| SearchBar chrome | inherited Phase 17 | — | — | — |
| Active filter pill text | `--gd-text` | `#0e7544` | `#129657` | dark contrast borderline — match Phase 17 decision (see CONTRAST-02 addendum) |
| Active filter pill bg | `rgba(18,150,87,0.10)` over `--surface` | `#e7f4ec` | `#162a31` | inherited Phase 17 |
| Inactive filter pill text | `--muted` | — | — | inherited |
| Inactive filter pill bg | transparent | — | — | inherited |
| Table `<thead>` text | `--muted` | — | — | inherited |
| Table row primary text | `--ink` | — | — | inherited |
| Table row secondary text | `--muted` | — | — | inherited |
| Table row border | `--border` | — | — | inherited |
| StatusChip `active` text | `--gd-text` on `rgba(18,150,87,0.10)` | — | — | inherited Phase 14 |
| StatusChip `invited` text | `--gold-text` on `rgba(224,133,48,0.10)` | `#8a4e13` on `#fdf3e9` | `#e08530` on `#1f231d` | inherited Phase 14 |
| StatusChip `inactive` text | `--muted` on `rgba(110,113,145,0.10)` (verify) | — | — | inherited Phase 14 |
| Overflow menu trigger (`⋯`) | `--muted` | — | — | hover → `--ink` |
| Overflow menu dropdown bg | `--surface` | — | — | inherited |
| Overflow menu item text | `--ink` | — | — | inherited |
| Empty state body text | `--muted` | — | — | inherited |
| Empty state `Effacer les filtres →` link | `--teal` | — | — | inherited |

### Créer partenaire

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | — | — | inherited |
| Breadcrumb text default | `--muted` | — | — | inherited |
| Breadcrumb text hover | `--teal` | — | — | inherited |
| Form card chrome | `--surface` / `--border` | — | — | inherited |
| Section `●` bullet | `--gd` (dot) + `--muted` (label) | — | — | inherited Phase 14 pattern |
| Field label | `--muted` | — | — | inherited |
| Field input border (default) | `--border` | — | — | inherited |
| Field input border (focus) | `--teal` + `rgba(45,122,140,0.20)` outline | — | — | inherited Phase 11 |
| Field input border (error, D-16) | `--danger` | `#dc2626` | `#dc2626` | stable across modes |
| Field error text | `--danger` | — | — | inherited |
| Action card chrome (separate, D-15) | `--surface` / `--border` | — | — | inherited |
| Cancel button (`← Annuler`) | `.btn-out` (existing) | — | — | inherited |
| Submit button bg | `--gd` | — | — | inherited |
| Submit button text | `#ffffff` | — | — | stable |

### Coefficients

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | — | — | inherited |
| Hero title / subtitle | `--ink` / `--muted` | — | — | inherited |
| Warning banner bg | `rgba(224,133,48,0.10)` over `--surface` | `#fdf3e9` | `#1f231d` | NEW USAGE of existing tint |
| Warning banner left accent stripe | `--gold` | `#e08530` | `#e08530` | stable across modes |
| Warning banner icon | `--gold-text` | `#8a4e13` | `#e08530` | accent icon, AA-safe over banner bg (verify in CONTRAST audit) |
| Warning banner body text | `--ink` | — | — | inherited |
| Warning banner `×` close (default) | `--muted` | — | — | inherited |
| Warning banner `×` close (hover) | `--ink` | — | — | inherited |
| Warning banner border (non-accent) | `--border` | — | — | inherited |
| Editor card chrome | inherited Phase 14 | — | — | — |
| History sidebar card chrome | `--surface` / `--border` | — | — | inherited |
| History sidebar row primary text (timestamp) | `--ink` | — | — | inherited |
| History sidebar row admin name | `--muted` | — | — | inherited |
| History sidebar row summary text | `--muted` | — | — | inherited |
| History sidebar row note (italic) | `--muted` | — | — | inherited |
| History sidebar `/history` link | `--teal` | — | — | inherited |

### Aide landing

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | — | — | inherited |
| Hero title / subtitle | `--ink` / `--muted` | — | — | inherited |
| Card chrome (3 cards) | `--surface` / `--border` | — | — | inherited |
| Card icons (active cards 1 + 3) | `--gd-text` | `#0e7544` | `#129657` | inherited |
| Card icon (disabled card 2) | `--muted` | — | — | inherited |
| Card title | `--ink` | — | — | inherited |
| Card body | `--muted` | — | — | inherited |
| Active card CTA / link | `--teal` | — | — | inherited |
| Disabled card overlay | `opacity:0.7` (no token change) | — | — | derived |
| `Bientôt disponible` badge text | `--muted` | — | — | inherited |
| `Bientôt disponible` badge bg | `rgba(110,113,145,0.10)` over `--surface` | derived light | derived dark | inherited tint pattern |
| Sidebar `Aide` nav item | inherited Phase 16 sidebar token cascade | — | — | — |

### Aide article — Commencer ici

| Element | Token | Light value | Dark value (cascade) | Notes |
|---------|-------|-------------|----------------------|-------|
| Page canvas | `--paper` | — | — | inherited |
| Breadcrumb text | `--muted` / hover `--teal` | — | — | inherited |
| Article container card chrome | `--surface` / `--border` | — | — | inherited |
| H1 | `--ink` | — | — | inherited |
| H2 | `--ink` | — | — | inherited |
| Body `<p>` | `--ink` | — | — | inherited |
| Inline `<a>` link | `--gd-text` | `#0e7544` | `#129657` | underline styling |
| `<ol>` / `<ul>` markers | `--muted` | — | — | derived from list-style |
| Screenshot `<Image>` border | `--border` | — | — | inherited |
| End-of-article CTA | `--gd` bg + white text | — | — | inherited `.btn-green` |

**No new color tokens introduced across all 6 surfaces.** D-30 palette stability invariant honored.

---

## Spacing / Layout Grid Notes — Per Surface

### Admin Home
- Page max-width: `maxWidth:1040px` (matches Phase 14 + Phase 17 existing pattern)
- PageHero → stat-tile grid: `32px` gap (Phase 16 D-04 locked)
- Stat-tile grid → AdminNavCard grid: `32px` gap
- AdminNavCard grid → Recent activity card: `32px` gap
- All grids: `gap:24px` between columns

### Partners list
- Page max-width: `maxWidth:1280px` (wider than 1040px to give the 6-column table room — planner verifies against Figma `42:46`; if Figma is narrower, switch to 1040px)
- PageHero → SearchBar: `24px` gap
- SearchBar → FilterPillRow (if stacked vertically): `12px` gap. If side-by-side: `16px` horizontal `gap`.
- FilterPillRow → table card: `16px` gap
- Table row vertical padding: `16px 20px` (rows breathe)
- Table → pagination footer: `16px` gap

### Créer partenaire
- Page max-width: `maxWidth:720px` (narrow column for form readability — matches Phase 17 wizard step 1)
- Breadcrumb link → PageHero: `12px` gap
- PageHero → form card: `32px` gap (Phase 16 D-04)
- Form card → action card: `16px` gap (D-15 separate card)
- Form section gaps (within form card): inherited from Phase 14 — verify `marginBottom:24px` between sections

### Coefficients
- Page max-width: `maxWidth:1280px` (2-column layout needs width)
- PageHero → warning banner: `24px` gap
- Warning banner → 2-column layout: `16px` gap
- 2-column layout gap: `24px` (matches Phase 17 wizard step-3 2-col)
- Right column fixed width: `360px; flexShrink:0`

### Aide landing
- Page max-width: `maxWidth:1040px` (matches Admin Home)
- PageHero → 3-card grid: `32px` gap (Phase 16 D-04)
- 3-card grid gap: `24px`

### Aide article — Commencer ici
- Page max-width: `maxWidth:720px` (prose readability)
- Breadcrumb → article card: `16px` gap
- Article card internal padding: `48px 56px` (LOOSER than standard `.card` 28px — gives prose breathing room; planner may revert to 28px if too generous)
- H1 → lead paragraph: `16px` gap
- Lead → first H2: `32px` gap
- H2 → following paragraph: `16px` gap
- Paragraph → next paragraph: `16px` gap (`<p>` `marginBottom`)
- Section → next H2: `32px` gap
- Screenshot block: `24px` top + bottom margin

---

## Light / Dark Mode Checklist (THEME-02)

Every Phase 18 surface ships both light and dark simultaneously (ROADMAP §v1.3 §1). All token pairs resolve via `html[data-theme="dark"]` cascade — no per-component dark CSS.

**Critical context (D-29):** Unlike Phase 17, **there are NO painted Figma dark frames for admin surfaces**. Dark variants are entirely DERIVED. Closing-out plan MUST include 12 manual visual sign-off checkpoints (6 surfaces × 2 modes).

| Surface | Mode | Key token pairs | Verification approach | Risk |
|---------|------|----------------|----------------------|------|
| 1 Admin Home | Light | `--paper` / `--surface` / `--ink` / `--muted` / `--teal` (stat values) / `--gd-text` (eyebrow) | Visual diff vs Figma `41:46` | LOW — Figma reference exists |
| 1 Admin Home | Dark | same tokens via cascade | Manual visual sign-off (no Figma) | **MEDIUM** — verify stat-value `--teal` (`#2d7a8c`) over dark surface `#161e2d` AA contrast; verify `ADMIN` eyebrow `#129657` over dark `#0c121c` page bg AA contrast |
| 2 Partners list | Light | `--surface` / `--border` (table) / `--gd-text` (active pill) / StatusChip variants | Visual diff vs Figma `42:46` | LOW — Figma reference exists |
| 2 Partners list | Dark | same tokens via cascade | Manual visual sign-off | **MEDIUM** — active filter pill dark (~3.9:1 — CONTRAST-02 Row 11 borderline, inherited from Phase 17 decision); table row borders `--border` `#242c3e` over `--surface` `#161e2d` (low contrast by design — verify legibility) |
| 3 Créer partenaire | Light | `--surface` / `--border` / `--gd-text` (section bullets) / `--gd` (submit) / `--danger` (errors) | Visual diff vs Figma `43:46` | LOW |
| 3 Créer partenaire | Dark | same tokens via cascade | Manual visual sign-off | **LOW-MEDIUM** — form input focus ring `rgba(45,122,140,0.20)` opacity-based, renders OK; verify error border `--danger` `#dc2626` over dark input bg `--paper` `#0c121c` legible |
| 4 Coefficients | Light | `--surface` / `--gold` (warning accent) / `--gold-text` (warning icon) | Visual diff vs Figma `45:46` | LOW |
| 4 Coefficients | Dark | same tokens via cascade | Manual visual sign-off | **MEDIUM** — warning banner: tint `rgba(224,133,48,0.10)` over dark `#161e2d` → `#1f231d` (very subtle distinction from surface); verify `--gold-text` `#e08530` icon over banner bg AA contrast; verify left-accent stripe `--gold` `#e08530` visible against banner bg |
| 5 Aide landing | Light | `--surface` / `--gd-text` (icons) / `--teal` (CTAs) | No Figma reference (generic placeholder Figma) | **MEDIUM** — first authored Figma-less surface |
| 5 Aide landing | Dark | same tokens via cascade | Manual visual sign-off | **MEDIUM** — disabled card opacity 0.7 in dark may render too muted; verify legibility |
| 6 Aide article | Light | `--surface` (article card) / `--ink` (prose) / `--gd-text` (links) / `--border` (screenshots) | No Figma reference (generic lorem-ipsum Figma) | **MEDIUM** — first prose surface |
| 6 Aide article | Dark | same tokens via cascade | Manual visual sign-off | **MEDIUM-HIGH** — screenshots are LIGHT-MODE captures embedded in dark page (D-26 acknowledged); accepted partial per Option 1 — defer dark screenshots to HELP-02 |

**Closing-out plan addition:** the 18-08 verification phase (analogous to 17-08) must include a structured "12-checkpoint sweep" — 6 surfaces × 2 modes — with screenshots captured at standard 1280px viewport, both modes toggled via the `data-theme` cascade. Each checkpoint signs off in writing before merge.

**Contrast addendum:** If any token pair surfaces a new contrast issue (below 4.5:1 AA), document in `docs/accessibility/18-contrast-addendum.md` and either (a) accept-with-deviation noting the inherited Phase 17/Phase 8 baseline, or (b) introduce an opacity-elevated token variant similar to the Phase 17 `--active-pill` option. NO new color tokens (D-30); only opacity adjustments are permitted within existing token families.

---

## ADMIN-09 Envelope Verification

Phase 18 surfaces are admin-facing. The ADMIN-09 D-12 commission-invisibility envelope MUST hold:

- **Admin Home** — stat tiles, AdminNavCards, Recent activity card. NONE of these expose commission VALUES. The card title `Coefficients & commission` (AdminNavCard #1) is chrome/label only, NOT a value (Phase 14 threat T-14-03-02 already mitigated). Recent activity sentences reference "modified coefficients" but DO NOT include the commission value itself.
- **Partners list** — table columns: PARTENAIRE / EMAIL / DATE CRÉATION / DERNIÈRE ACTIVITÉ / STATUT / ⋯. ZERO commission columns. Overflow menu actions are partner-account management, not commission-adjacent.
- **Créer partenaire** — invitation form fields: firstName, lastName, email, companyName, siret, phone, message. ZERO commission fields.
- **Coefficients** — this IS the admin coefficients-editing surface where commission is editable (allowed exception, ADMIN-09 D-12). Warning banner copy does NOT include the commission value. History sidebar row summaries CAN include commission values when commission was the field modified (e.g. `Commission: 1.00 → 1.50`). This is pre-existing Phase 14 behavior; the Phase 18 visual refresh does NOT add new commission exposure paths.
- **Aide landing** — generic help center, ZERO commission references.
- **Aide article (Commencer ici)** — wizard quick-start guide. The article body discusses STEPS 1/2/3 of the wizard. The Phase 17 wizard step 2 surfaces commission to the deal-owner partner (D-12 allowed exception). The Aide article SHOULD describe step 2 conceptually ("le résultat du calcul s'affiche") but MUST NOT include literal commission values or the partner-facing commission line `Commission apporteur (non visible client)` as embedded copy. Wizard SCREENSHOTS embedded in the article: planner must capture wizard screenshots from a clean test draft where commission value is GENERIC (e.g. 1.00%) and the screenshot does NOT zoom into the commission row. Alternatively, blur or crop the commission row out of the step-2 screenshot.

**Phase 14 9-gate `tests/admin-09-grep-contracts.test.ts`:** must stay green throughout. Add new gates ONLY if Phase 18 introduces new commission-adjacent file paths (none expected, but verify after the rename `AccountsList.tsx → PartnersList.tsx`).

**New files created by Phase 18:**
- `app/(admin)/[adminSegment]/page.tsx` — REWRITE (commission-free)
- `app/(admin)/[adminSegment]/partners/PartnersList.tsx` — RENAME from AccountsList.tsx (commission-free)
- `app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.tsx` — NEW (commission-free)
- `app/(admin)/[adminSegment]/partners/_components/PartnersFilterPillTabs.tsx` — NEW (commission-free)
- `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx` — NEW (commission-free; warning copy mentions calculation impact but NOT commission value)
- `app/(authed)/aide/page.tsx` — NEW (commission-free)
- `app/(authed)/aide/commencer-ici/page.tsx` — NEW (commission-free)
- `src/lib/db/queries/partner-aggregates.ts` (new file or extend `proposal-aggregates.ts`) — commission-free
- `src/lib/db/queries/admin-activity.ts` (or similar) — Recent activity 3-source union — commission-free (queries `coefficient_history`, `users.status`, `invitations` — none include commission VALUES in the SELECT projection; only IDs + timestamps + actor names)

All new files pass all 9 gates trivially. Planner: run `pnpm test admin-09-grep-contracts.test.ts` before merge.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No new npm packages introduced in Phase 18. All work reuses existing lucide-react icons (`MoreVertical`, `AlertTriangle`, `BookOpen`/`Sparkles`, `FileText`, `Mail`, `HelpCircle`/`LifeBuoy`), inline styles, and CSS custom properties from `app/globals.css`. Constraint "do NOT introduce new dependencies" satisfied.

---

## Files Modified / Created

| File | Action | Driven by |
|------|--------|-----------|
| `app/(admin)/[adminSegment]/page.tsx` | REWRITE | ADMIN-10 |
| `app/(admin)/[adminSegment]/partners/page.tsx` | MODIFY (call PartnersList instead of AccountsList; pass FilterPillTabs + cursor) | ADMIN-11/12 |
| `app/(admin)/[adminSegment]/partners/AccountsList.tsx` | RENAME → `PartnersList.tsx` + FULL SCRUB (D-14) | ADMIN-11/12, D-14 |
| `app/(admin)/[adminSegment]/partners/_components/PartnersFilterPillTabs.tsx` | CREATE | ADMIN-11, D-09 |
| `app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.tsx` | CREATE | ADMIN-12, D-10 |
| `app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.test.tsx` | CREATE | ADMIN-12 |
| `app/(authed)/proposals/page.tsx` | MODIFY (accept admin `user_id` query param, gate by admin role) | D-11 |
| `app/(admin)/[adminSegment]/partners/new/page.tsx` | MODIFY (PageHero, breadcrumb, action card separation) | ADMIN-13, D-15 |
| `app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx` | MODIFY (inline error state D-16, dirty-form confirm D-18, submit label change) | ADMIN-13, D-16/18 |
| `app/(admin)/[adminSegment]/coefficients/page.tsx` | MODIFY (PageHero, warning banner, 2-col layout) | ADMIN-14 |
| `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx` | CREATE | ADMIN-14, D-19/20 |
| `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.test.tsx` | CREATE | ADMIN-14 |
| `app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx` | MODIFY (chrome refresh, remove click-to-diff) | ADMIN-14, D-21/22 |
| `app/(authed)/aide/page.tsx` | CREATE | HELP-01, D-23/25 |
| `app/(authed)/aide/commencer-ici/page.tsx` | CREATE | HELP-01, D-26 |
| `public/aide/wizard-step-1.png` | CREATE (screenshot capture) | D-26 |
| `public/aide/wizard-step-2.png` | CREATE | D-26 |
| `public/aide/wizard-step-3.png` | CREATE | D-26 |
| `src/components/ui/SidebarNav.tsx` (or equivalent) | MODIFY (per-role nav config — D-27) | D-27 |
| `src/lib/db/queries/proposal-aggregates.ts` | MODIFY (add `getMonthlyProposalCountAll()`) | ADMIN-10, D-02 |
| `src/lib/db/queries/partner-aggregates.ts` | CREATE (or extend existing) — partner count + "active" filter | ADMIN-10, D-01 |
| `src/lib/db/queries/admin-activity.ts` | CREATE — Recent activity 3-source union (coefficient_history + partner status changes + invitations) | ADMIN-10, D-05 |
| `src/lib/i18n/dictionaries.ts` | MODIFY (~70 new keys × FR + EN) | D-21 (verify-and-reuse) |
| `docs/accessibility/18-contrast-addendum.md` | CREATE (if any new pairs surface issues) | THEME-02 |
| `tests/admin-09-grep-contracts.test.ts` | NO CHANGE (verify still green after `Partners`/`Accounts` rename) | ADMIN-09 |
| `src/components/ui/MetricTile.tsx` | NO CHANGE (planner adds `valueColor` prop OR `variant="admin-teal"` — minor extension only) | ADMIN-10 |
| `src/components/ui/AdminNavCard.tsx` | NO CHANGE | ADMIN-10 |
| `src/components/ui/PageHero.tsx` | NO CHANGE | (all 6 surfaces) |
| `src/components/ui/StatusChip.tsx` | NO CHANGE | ADMIN-11 |
| `src/components/proposals/SearchBar.tsx` | NO CHANGE | ADMIN-11 |
| `app/(authed)/proposals/_components/FilterPillRow.tsx` | NO CHANGE (Phase 17 component; Partners uses a separate 4-tab variant) | — |
| `app/globals.css` | NO CHANGE (no new tokens; palette stable D-30) | D-30 |

---

## Pre-Population Source Map

| Decision | Source |
|----------|--------|
| All 30 D-xx decisions (D-01..D-30) | `18-CONTEXT.md` `<decisions>` — treated as LOCKED |
| Color tokens and values | `app/globals.css` `:root` + `html[data-theme="dark"]` (inherited verbatim from Phase 16/17) |
| Spacing scale | Phase 11/13/16/17 precedent; Phase 18 new values derived from 4-multiple rule |
| Stat-tile `--teal` always | D-04 explicit (CONTEXT.md) |
| 4-tab filter (Tous/Actifs/Invités/Désactivés) | D-09 explicit |
| Coefficients warning banner copy | D-19 explicit (locked from Figma) |
| sessionStorage dismissal key `gsd.coefficients.warning.dismissed` | D-20 explicit |
| CoefficientHistorySidebar in-place refresh + click-to-diff removal | D-21/22 explicit |
| Aide 3-card placeholder layout | D-25 explicit |
| Aide hardcoded TSX storage strategy | D-24 explicit |
| Aide article content target (~500-1000 words FR + EN) | D-26 explicit (planner drafts; user reviews) |
| Sidebar nav per-role | D-27 explicit |
| Dark mode derivation (no painted Figma frames) | D-29 explicit |
| Existing i18n keys (admin.home.*, admin.nav.*, admin.coefficients.*, admin.accounts.*, partners.new.*, sidebar.nav.*, dashboard.cta.new) | `src/lib/i18n/dictionaries.ts` (read directly) |
| Phase 17 component primitives (PageHero, MetricTile, AdminNavCard, StatusChip, FilterPillRow, CoefficientHistorySidebar, PaginationKey, SearchBar) | `17-UI-SPEC.md` + Phase 11/13/14/16 history |
| Existing Admin Home structure (PageHero + 3 AdminNavCards) | `app/(admin)/[adminSegment]/page.tsx` (read directly) |
| Phase 17 wizard surfaces (for Aide screenshots) | Phase 17 deployed wizard surfaces — to be captured during planning/execute |
| Figma node references | `18-CONTEXT.md` `<canonical_refs>` |
| Screenshots reference paths | `/tmp/figma-phase18/{admin-home,partners-list,creer-partenaire,coefficients,aide,aide-subpage}.png` (ephemeral) |
| HELP-01 requirement net-new | `18-CONTEXT.md` `<domain>` — must file in REQUIREMENTS.md during planning |

---

## Open Questions

1. **`<MetricTile>` API extension vs new component:** planner choice between extending `MetricTile.tsx` with a `valueColor` prop (or `variant="admin-teal"`) vs creating a separate `<AdminStatTile.tsx>`. Recommendation: extend MetricTile with `valueColor` prop (single source of truth, minimal API surface).
2. **`<FilterPillRow>` parameterization vs duplication:** planner choice between extending Phase 17's `FilterPillRow` to support N tabs vs creating a new `<PartnersFilterPillTabs>` component. Recommendation: parameterize (DRY).
3. **D-14 `admin.accounts.*` i18n re-key:** planner choice between (a) full re-key to `admin.partners.*` (matches D-14 acceptance spirit, breaks existing tests that import old keys), OR (b) keep existing keys, only add new keys where copy genuinely differs. Recommendation: (a) full re-key for naming consistency.
4. **Partners table column layout — stacked name+email in col 1 (drop col 2) vs both columns separate:** planner picks based on Figma `42:46` re-render. CONTEXT.md says 6 columns → recommend keep both separate.
5. **Overflow menu implementation:** planner picks (a) native `<details><summary>`, (b) custom React state + click-outside hook. Recommendation: (b).
6. **Aide article container card padding:** 28px standard vs 48px 56px looser. Recommendation: start with 48px 56px, audit during checker review.
7. **Aide article 4th body size (16px lead):** if it pushes type-system over 4-size limit, drop the lead and use standard 14.5px body. Recommendation: drop the lead.
8. **Aide article dark-mode screenshots:** Option 1 (light screenshots in dark page, accepted partial) vs Option 2 (theme-aware screenshot swap). Recommendation: Option 1 for Phase 18; defer Option 2 to HELP-02.
9. **Coefficients PageHero title shortening:** keep existing `Coefficients & Commission` (existing key value) vs shorten to `Coefficients` (matches Figma + sidebar). Recommendation: shorten to `Coefficients` for consistency with sidebar/breadcrumb. Modify existing key value in place; does NOT count as net-new key.
10. **`SUPPORT_EMAIL` for Aide Contact card:** placeholder `support@leasetic.fr` — planner must CONFIRM with user during planning before shipping.

All 10 questions are minor implementation choices, not blocking decisions. The 30 D-xx decisions in CONTEXT.md already cover every macro-level decision. Planner can resolve all 10 during plan-phase or defer to executor where appropriate.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Generated: 2026-05-24 by gsd-ui-researcher*
*Phase: 18-admin-surfaces*
*All 30 decisions from 18-CONTEXT.md pre-populated as LOCKED. Mirrors Phase 17 UI-SPEC structure. Zero new user questions — CONTEXT.md answered every macro-level design contract question; 10 minor implementation choices deferred to planner as Open Questions.*
