---
phase: 18-admin-surfaces
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/(admin)/[adminSegment]/partners/new/page.tsx
  - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx
  - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx
autonomous: true
requirements: [ADMIN-13, THEME-02]
must_haves:
  truths:
    - "Créer partenaire renders breadcrumb (← PARTENAIRES) above PageHero, links to /${adminSegment}/partners"
    - "PageHero renders title 'Créer un partenaire' + subtitle (existing keys), NO actions slot — submit lives in separate action card per D-15"
    - "Form card contains 3 sections (INFORMATIONS PERSONNELLES / INFORMATIONS SOCIÉTÉ / MESSAGE D'INVITATION) with existing ● bullet pattern (Phase 14 unchanged)"
    - "Action card sits SEPARATELY below the form card (marginTop:16) containing left-aligned ← Annuler (btn-out) + right-aligned Envoyer l'invitation → (btn-green primary) — D-15"
    - "Invalid fields render red border (borderColor:var(--danger)) + inline red error text (color:var(--danger), fontSize:11.2px) below the input per D-16; aria-invalid + aria-describedby wiring intact"
    - "Annuler with dirty form (RHF formState.isDirty=true) opens confirm dialog with copy 'Vous avez des changements non enregistrés. Continuer ?' per D-18; Confirm navigates to /partners; Cancel keeps user on form"
    - "Behavior unchanged: adminCreateInvitation server action + InviteUrlModal success affordance preserved from Phase 14 (D-17)"
  artifacts:
    - path: app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx
      provides: "Visual repaint per Figma 43:46 + D-15/16/17/18; behavior unchanged"
      contains: "isDirty"
    - path: app/(admin)/[adminSegment]/partners/new/page.tsx
      provides: "PageHero adoption + breadcrumb above hero per UI-SPEC line 380"
      contains: "PageHero"
  key_links:
    - from: app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx
      to: src/lib/i18n/dictionaries.ts (Plan 18-01)
      via: "t('admin.partners.form.submit') / .form.submit.spinner / .form.cancel.confirm / .breadcrumb.label keys"
      pattern: "admin.partners.form|admin.partners.breadcrumb"
    - from: app/(admin)/[adminSegment]/partners/new/page.tsx
      to: app/(admin)/[adminSegment]/partners (breadcrumb target)
      via: "breadcrumb Link href"
      pattern: "/partners$"
---

<objective>
Repaint /partners/new per Figma `43:46` + UI-SPEC §Créer partenaire contract. Behavior unchanged from Phase 14 (RHF + Zod + adminCreateInvitation + InviteUrlModal). Visual changes per D-15 (action card separated from form card), D-16 (inline red error state with red border), D-18 (dirty-form confirm dialog on Annuler). Adopt the PageHero primitive with a breadcrumb link ABOVE the hero.

Purpose: ADMIN-13 success criterion 3; consumes Plan 18-01 i18n keys (admin.partners.form.* + admin.partners.breadcrumb.*).
Output: page.tsx layout update + CreatePartnerForm.tsx visual refresh + tests for the new error state + dirty-form confirm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/18-admin-surfaces/18-01-PLAN.md

<interfaces>
<!-- From existing codebase -->
- `<PageHero>` from src/components/ui/PageHero.tsx (Phase 16) — accepts eyebrow, title, subtitle, actions slot
- `app/(admin)/[adminSegment]/partners/new/page.tsx` (Phase 14) — wraps CreatePartnerForm + InviteUrlModal trigger
- `app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx` (Phase 14) — RHF + Zod + adminCreateInvitation server action
- `adminCreateInvitation` server action (Phase 14) — unchanged signature
- `<InviteUrlModal>` (Phase 14) — unchanged
- Existing `.fld` class for form fields (Phase 11)
- Existing `.btn-green` / `.btn-out` button classes

<!-- From Plan 18-01 -->
- New i18n keys: `admin.partners.form.submit`, `admin.partners.form.submit.spinner`, `admin.partners.form.cancel.confirm`, `admin.partners.breadcrumb.label`, `admin.coefficients.warning.dismiss.aria` (note: that one belongs to Plan 18-05)
- Reused existing keys: `partners.new.title`, `.subtitle`, `.section.personal/.company/.message`, `.cancel`, `.toast.error`, `partners.new.field.*`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreatePartnerForm visual refresh — action card separation (D-15), inline error state (D-16), dirty-form confirm (D-18)</name>
  <files>app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx, app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx (full file — current Phase 14 implementation; understand current RHF setup, Zod schema, server-action call, error handling)
    - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx (existing tests — extend to cover D-15/16/18 behaviors)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Créer partenaire — full contract (lines 375-450) with form card + action card + error state + dirty-form confirm
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-15 action card separation; D-16 red border + inline error; D-17 InviteUrlModal unchanged; D-18 dirty-form confirm)
    - src/lib/i18n/dictionaries.ts (verify Plan 18-01 keys are present: admin.partners.form.submit, .form.submit.spinner, .form.cancel.confirm)
  </read_first>
  <behavior>
    - Test 1 (D-15 action card structure): rendered HTML contains TWO sibling `.card` elements at the form root — one form card with 3 sections, one action card below containing Annuler (left) + submit button (right). Action card style includes `marginTop:16; display:flex; justifyContent:space-between`
    - Test 2 (D-15 submit label): submit button text is t('admin.partners.form.submit') = "Envoyer l'invitation →" (replaces Phase 14's "Créer le partenaire")
    - Test 3 (D-15 submit spinner): while submitting, button text changes to t('admin.partners.form.submit.spinner') = "Envoi en cours…"
    - Test 4 (D-16 red border on invalid): submit form with invalid email → email input has `style.borderColor === 'var(--danger)'` (or className indicating same) AND `aria-invalid="true"` AND a sibling `<p>` with the error message colored var(--danger)
    - Test 5 (D-16 error message i18n): RHF zodResolver error key renders via t() into the inline `<p>`; existing Phase 14 keys reused (e.g. admin.accounts.modal.error.email.invalid)
    - Test 6 (D-16 aria-describedby): the input's aria-describedby attribute references the error `<p>`'s id when in error state
    - Test 7 (D-18 clean form Annuler): clicking ← Annuler when formState.isDirty=false navigates immediately to /${adminSegment}/partners (no confirm dialog)
    - Test 8 (D-18 dirty form Annuler): typing into any field then clicking ← Annuler opens a confirm dialog with copy from t('admin.partners.form.cancel.confirm')
    - Test 9 (D-18 confirm Yes): clicking Continuer/OK in the dialog navigates to /${adminSegment}/partners
    - Test 10 (D-18 confirm No): clicking Annuler/Cancel in the dialog closes the dialog AND keeps the user on the form with no field reset
    - Test 11 (D-17 unchanged): on successful submit, InviteUrlModal opens with the invite link (server action stub returns mock URL); no toast surface
    - Test 12 (D-16 server-action error toast): server action throws → toast notification fires with t('partners.new.toast.error') (Phase 14 SaveConfirmModal-style toast pattern)
  </behavior>
  <action>
    Per D-15 / D-16 / D-17 / D-18 / UI-SPEC §Créer partenaire.

    1. Modify `CreatePartnerForm.tsx`:

       a. **Action card separation (D-15)** — restructure the JSX so the submit row is NOT inside the form card. The form `<form>` element wraps BOTH cards. Form card: 3 sections with field inputs. Action card: separate `.card` with `style={{ marginTop: 16, display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: 16 }}` containing left button (Annuler) + right submit button.

       b. **Submit button label change (D-15)** — change submit text to `t('admin.partners.form.submit', lang)` (was Phase 14's `partners.new.submit`). Submitting state text → `t('admin.partners.form.submit.spinner', lang)`. Class `btn-green`. type="submit". Disabled when `formState.isSubmitting`.

       c. **Annuler button** — left button text stays as `t('partners.new.cancel', lang)` (EXISTING `← Annuler`). type="button" so it doesn't trigger form submit. onClick = `handleCancel()` (see below). Class `btn-out`.

       d. **Inline error state (D-16)** — for each `<input>` and `<textarea>`, apply:
          - When `errors[fieldName]` is present:
            - input gets `style={{ borderColor: 'var(--danger)' }}` overlay (preserve existing focus styles)
            - input gets `aria-invalid="true"` and `aria-describedby={\`${fieldName}-error\`}`
            - Render a sibling `<p id={\`${fieldName}-error\`}>` below with `style={{ color: 'var(--danger)', fontSize: '11.2px', fontWeight: 500, marginTop: 4 }}` containing `t(errors[fieldName].message, lang)` (the Zod error keys map to existing i18n keys)
          - When no error: no border override, no aria attributes, no error `<p>`.
          - Use RHF's `register('fieldName')` + `formState.errors` standard pattern.

       e. **Dirty-form confirm (D-18)** — implement `handleCancel`:
          ```
          const handleCancel = () => {
            if (formState.isDirty) {
              const confirmed = window.confirm(t('admin.partners.form.cancel.confirm', lang));
              if (!confirmed) return;
            }
            router.push(`/${adminSegment}/partners`);
          };
          ```
          `window.confirm` is acceptable as the baseline per UI-SPEC line 443 ("or `window.confirm` as a baseline if no modal primitive exists"). If a project-wide ConfirmDialog primitive exists (verify with `grep -rln "ConfirmDialog\|ConfirmModal" src/components/`), prefer it for visual consistency.

       f. **Server-action error toast (D-16 last paragraph)** — on `adminCreateInvitation` throw, catch and trigger the existing toast pattern (Phase 14 SaveConfirmModal-style — verify with `grep -rln "useToast\|toast.*error" src/components/`). Toast copy = `t('partners.new.toast.error', lang)` (EXISTING). Behavior unchanged from Phase 14 — just verify the catch-throw path renders the toast.

       g. **Preserve everything else** — Zod schema unchanged, server action call unchanged, InviteUrlModal trigger unchanged (D-17 unchanged success affordance), 3-section structure unchanged (D-17 ● bullet pattern unchanged).

    2. Extend `CreatePartnerForm.test.tsx` with Tests 1-12. Mock the server action; mock `useRouter` for Test 9 navigation assertion; mock `window.confirm` for Tests 8-10; mock the toast hook for Test 12.

    CRITICAL: do NOT change the Zod schema, do NOT change the adminCreateInvitation signature, do NOT change InviteUrlModal — those are all Phase 14 contracts that ADMIN-13 leaves untouched per CONTEXT.md.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/partners/new/CreatePartnerForm.test.tsx</automated>
  </verify>
  <done>
    - 12 behavior tests pass
    - `grep -c "marginTop: 16\|marginTop:16" app/\(admin\)/\[adminSegment\]/partners/new/CreatePartnerForm.tsx` returns >= 1 (action card spacing per D-15)
    - `grep -n "formState.isDirty\|formState\\.isDirty" app/\(admin\)/\[adminSegment\]/partners/new/CreatePartnerForm.tsx` returns hit (D-18 wiring)
    - `grep -n "var(--danger)" app/\(admin\)/\[adminSegment\]/partners/new/CreatePartnerForm.tsx` returns >= 2 (border + error text)
    - `grep -n "admin.partners.form.submit\|admin.partners.form.cancel.confirm" app/\(admin\)/\[adminSegment\]/partners/new/CreatePartnerForm.tsx` returns hits
    - Existing Phase 14 Zod schema + server-action call + InviteUrlModal preserved (verify via grep that adminCreateInvitation call still exists with unchanged shape)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page.tsx — PageHero adoption + breadcrumb above hero</name>
  <files>app/(admin)/[adminSegment]/partners/new/page.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/partners/new/page.tsx (current Phase 14 implementation)
    - src/components/ui/PageHero.tsx (verify props)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Créer partenaire layout (lines 379-394 breadcrumb + 396-403 PageHero call)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-15 layout — breadcrumb above hero)
  </read_first>
  <behavior>
    - Test 1: page renders a breadcrumb `<Link>` with text "← PARTENAIRES" linking to /${adminSegment}/partners, rendered ABOVE the PageHero (use DOM order verification)
    - Test 2: page renders PageHero with title t('partners.new.title') and subtitle t('partners.new.subtitle'), NO actions slot per D-15
    - Test 3: breadcrumb default color = var(--muted); hover color = var(--teal) (verify via inline style or CSS class)
    - Test 4: page max-width is 720px per UI-SPEC line 901
  </behavior>
  <action>
    Modify `app/(admin)/[adminSegment]/partners/new/page.tsx`:

    1. Wrap the page contents in a `<main>` with `style={{ maxWidth: 720, margin: '0 auto', padding: '0 24px' }}` per UI-SPEC line 901.

    2. ABOVE the PageHero, render the breadcrumb per UI-SPEC line 386-393:
       ```
       <Link
         href={`/${adminSegment}/partners`}
         style={{
           fontSize: 12.5, fontWeight: 500, color: 'var(--muted)',
           textDecoration: 'none', marginBottom: 12,
           display: 'inline-flex', alignItems: 'center', gap: 4,
           letterSpacing: '0.04em',
         }}
       >
         {`← ${t('admin.partners.breadcrumb.label', lang)}`}
       </Link>
       ```
       Hover state: rely on existing global CSS rule for `<Link>` hover OR add an inline `:hover` via a wrapper className. Recommend a small `<style jsx>` block OR add a one-off class `.breadcrumb-link` to `app/globals.css` Phase 18 file ONLY if no token-only solution works. Inline hover via React onMouseEnter/Leave is acceptable as a baseline.

    3. PageHero call:
       ```
       <PageHero
         title={t('partners.new.title', lang)}
         subtitle={t('partners.new.subtitle', lang)}
         // NO actions slot — submit lives in the separate action card (D-15)
       />
       ```

    4. Below PageHero, render `<CreatePartnerForm adminSegment={adminSegment} lang={lang} />` (preserve existing prop wiring from Phase 14).

    5. Preserve `requireAdmin()` + any existing server-component prelude.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/partners/new/ && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `grep -n "admin.partners.breadcrumb.label\|PARTENAIRES" app/\(admin\)/\[adminSegment\]/partners/new/page.tsx` returns hits
    - `grep -n "maxWidth.*720\|maxWidth: 720" app/\(admin\)/\[adminSegment\]/partners/new/page.tsx` returns hit
    - PageHero rendered WITHOUT actions slot (verify via grep that `actions={` is NOT in this file)
    - TypeScript compile clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | requireAdmin() server-side gate preserved from Phase 14 |
| RHF form submit → adminCreateInvitation | Zod resolver validates inputs at boundary; server action re-validates |
| window.confirm → router.push | client-side cancel flow; no auth boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Information disclosure (server-action error leak) | catch-throw → toast | mitigate | Phase 14 SaveConfirmModal toast renders the i18n-keyed user-facing error message (`partners.new.toast.error`), NOT the raw error.message — Phase 5 PITFALLS §9.4 discipline preserved |
| T-18-04-02 | Tampering (invitation creation by partner) | adminCreateInvitation server action | mitigate | Server action enforces admin role via existing Phase 14 gate; Phase 18 changes the UI but NOT the auth contract — adminCreateInvitation continues to require admin session (verify by `grep -n "requireAdmin\|isAdmin" src/lib/actions/`) |
| T-18-04-03 | CSRF on form submit | RHF form action | mitigate | Phase 6 SameSite=Lax + __Secure- cookies + Better Auth session cookie — existing CSRF defenses unchanged |
| T-18-04-04 | XSS via field values in error message | inline error `<p>` | mitigate | Error messages come from t() with static keys; field values are NOT interpolated into error messages; React text rendering auto-escapes |
| T-18-04-05 | Repudiation (dirty-form data loss without consent) | handleCancel | mitigate | D-18 confirm dialog requires explicit user consent before navigating away from dirty form |
</threat_model>

<verification>
- `npm test -- app/\(admin\)/\[adminSegment\]/partners/new/` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (no commission leakage; form is commission-free per UI-SPEC §ADMIN-09 envelope verification)
- `npx tsc --noEmit` exits 0
- Manual visual sign-off deferred to Plan 18-07 (12-checkpoint sweep, light + dark)
</verification>

<success_criteria>
- Form card + action card structural separation per D-15
- Inline red border + error text per D-16; aria-invalid + aria-describedby wired
- Dirty-form confirm dialog per D-18 (clean form: immediate navigate; dirty form: confirm)
- InviteUrlModal success affordance unchanged per D-17
- PageHero + breadcrumb adopted per UI-SPEC §Créer partenaire
- ADMIN-13 behavior preserved (Zod schema + adminCreateInvitation + InviteUrlModal unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-04-SUMMARY.md`
</output>
