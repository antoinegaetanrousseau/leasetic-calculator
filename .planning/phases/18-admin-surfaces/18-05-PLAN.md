---
phase: 18-admin-surfaces
plan: 05
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/(admin)/[adminSegment]/coefficients/page.tsx
  - app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx
  - app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.test.tsx
  - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx
  - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx
autonomous: true
requirements: [ADMIN-14, THEME-02]
must_haves:
  truths:
    - "CoefficientWarningBanner renders between hero subtitle and 2-column layout, full-width, marginBottom:16"
    - "Banner styled per UI-SPEC line 481-488: bg rgba(224,133,48,0.10), 1px solid var(--border), 4px solid var(--gold) borderLeft, borderRadius:8, padding:12px 16px"
    - "Banner contains AlertTriangle icon (--gold-text), body copy from t('admin.coefficients.warning.body'), and × close button (aria-label from t('admin.coefficients.warning.dismiss.aria'))"
    - "Banner dismissable per-session: × click writes sessionStorage 'gsd.coefficients.warning.dismissed' = '1', then hides banner (D-20); tab close clears sessionStorage → banner returns next visit"
    - "CoefficientHistorySidebar refreshed IN PLACE (D-21): visual chrome tightened, row click-to-diff handler REMOVED (D-22); 2-column layout, cursor pagination, /history link PRESERVED"
    - "/history link at bottom of history sidebar uses new key admin.coefficients.history.viewAll"
    - "Phase 14 tests asserting in-card diff opening behavior UPDATED or REMOVED (D-22 test update list)"
  artifacts:
    - path: app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx
      provides: "Dismissable warning banner with sessionStorage persistence per D-19/D-20"
      contains: "gsd.coefficients.warning.dismissed"
    - path: app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx
      provides: "In-place refresh per D-21: chrome tightened + click-to-diff REMOVED per D-22"
      contains: "CoefficientHistorySidebar"
  key_links:
    - from: app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx
      to: window.sessionStorage
      via: "sessionStorage.getItem/.setItem('gsd.coefficients.warning.dismissed', '1') per D-20"
      pattern: "sessionStorage"
    - from: app/(admin)/[adminSegment]/coefficients/page.tsx
      to: CoefficientWarningBanner + CoefficientHistorySidebar
      via: "Render in 2-column layout per UI-SPEC line 458-463"
      pattern: "CoefficientWarningBanner|CoefficientHistorySidebar"
---

<objective>
Two visual changes to the /coefficients admin surface per Figma `45:46` + UI-SPEC §Coefficients contract:
1. NEW `CoefficientWarningBanner` component (full-width, between hero subtitle and 2-column layout) with dismissable-per-session UX (sessionStorage key per D-20) and locked copy per D-19.
2. IN-PLACE refresh of existing `CoefficientHistorySidebar` (D-21): tighten the row chrome to match Figma `45:46`, REMOVE the click-to-diff handler (D-22). Editor card untouched structurally.

Purpose: ADMIN-14 success criterion 4; consumes Plan 18-01 i18n keys (admin.coefficients.warning.* + .history.viewAll).
Output: 1 new banner component + 1 in-place history sidebar refresh + 1 page.tsx wiring + test updates per D-22.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/18-admin-surfaces/18-01-PLAN.md

<interfaces>
<!-- From existing codebase -->
- `app/(admin)/[adminSegment]/coefficients/page.tsx` (Phase 14) — server component rendering editor + history sidebar in 2-column layout
- `app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx` (Phase 14) — current component with click-to-diff handler; target of D-21 in-place refresh + D-22 click-to-diff removal
- `app/(admin)/[adminSegment]/coefficients/CoefficientDiffPanel.tsx` (Phase 14) — stays on /history full page only per D-22; not modified by Phase 18
- `<PageHero>` from src/components/ui/PageHero.tsx
- `lucide-react` `AlertTriangle` icon
- Existing `.card` class

<!-- From Plan 18-01 -->
- New i18n keys: admin.coefficients.warning.body, admin.coefficients.warning.dismiss.aria, admin.coefficients.history.viewAll
- Existing reused keys: admin.coefficients.page.title (planner may shorten value to "Coefficients" per Open Q9), admin.coefficients.page.sub

<!-- Existing test surface -->
- Phase 14 tests for CoefficientHistorySidebar's click-to-diff behavior — locate via `grep -rln "CoefficientHistorySidebar\|click-to-diff" app/ tests/` — these must be UPDATED or REMOVED per D-22
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoefficientWarningBanner — dismissable per-session (D-19/D-20)</name>
  <files>app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx, app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.test.tsx</files>
  <read_first>
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Coefficients — CoefficientWarningBanner full spec (lines 473-500)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-19 placement + locked copy; D-20 sessionStorage dismissal)
    - src/lib/i18n/dictionaries.ts (verify Plan 18-01 keys added: admin.coefficients.warning.body, .dismiss.aria)
  </read_first>
  <behavior>
    - Test 1 (first visit): given sessionStorage is empty (or doesn't have 'gsd.coefficients.warning.dismissed'), banner renders with the alert icon + body copy + × close button
    - Test 2 (already dismissed): given sessionStorage has 'gsd.coefficients.warning.dismissed' = '1' at mount, the banner does NOT render (returns null / 0 DOM nodes)
    - Test 3 (× dismiss): click the × close button → sessionStorage.setItem is called with key 'gsd.coefficients.warning.dismissed' and value '1' → banner hides (re-render returns null)
    - Test 4 (× dismiss is reversible per tab close): writing to sessionStorage (not localStorage) ensures persistence is per-session only; verify by mocking storage and asserting `sessionStorage.setItem` (NOT `localStorage.setItem`) is called
    - Test 5 (i18n copy): body text matches t('admin.coefficients.warning.body', 'fr') = "Modifier ces valeurs change le calcul de toutes les futures propositions. Les PDF déjà générés restent inchangés."
    - Test 6 (aria-label on dismiss): the × button has aria-label = t('admin.coefficients.warning.dismiss.aria', lang) = "Masquer l'avertissement"
    - Test 7 (SSR-safe): rendering on server (window undefined) does NOT throw; the sessionStorage check runs only in a useEffect or is guarded by typeof window check
    - Test 8 (visual chrome): rendered element has style.background = 'rgba(224, 133, 48, 0.1)' (or matching token), borderLeft = '4px solid var(--gold)', borderRadius = 8, padding = '12px 16px'
  </behavior>
  <action>
    Per D-19 / D-20 / UI-SPEC §Coefficients CoefficientWarningBanner.

    1. CREATE `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.tsx`:
       - `'use client'` directive at top (sessionStorage + onClick require client)
       - Props: `{ lang: 'fr' | 'en' }`
       - State: `const [hidden, setHidden] = useState(false);` — initial false (banner visible by default; SSR-safe). Then useEffect on mount reads sessionStorage and calls `setHidden(true)` if the key is `'1'`. This produces a brief flicker on first paint for already-dismissed users — accepted per UI-SPEC line 498-500 baseline.
       - Optional enhancement (UI-SPEC line 500): render with `visibility:hidden` initially then reveal after sessionStorage check to avoid flicker. Planner may pick either; baseline (flicker) is acceptable.
       - When `hidden === true`: `return null;`
       - JSX structure:
         ```
         <div style={{
           background: 'rgba(224, 133, 48, 0.10)',
           border: '1px solid var(--border)',
           borderLeft: '4px solid var(--gold)',
           borderRadius: 8,
           padding: '12px 16px',
           display: 'flex',
           alignItems: 'flex-start',
           gap: 12,
           marginBottom: 16,
         }}>
           <AlertTriangle size={18} style={{ color: 'var(--gold-text)', flexShrink: 0, marginTop: 2 }} />
           <div style={{ flex: 1, fontSize: 14, fontWeight: 400, color: 'var(--ink)', lineHeight: 1.5 }}>
             {t('admin.coefficients.warning.body', lang)}
           </div>
           <button
             type="button"
             onClick={handleDismiss}
             aria-label={t('admin.coefficients.warning.dismiss.aria', lang)}
             style={{
               width: 24, height: 24,
               display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
               borderRadius: 4, border: 'none', background: 'transparent',
               color: 'var(--muted)', cursor: 'pointer',
               fontSize: 16, lineHeight: 1, flexShrink: 0,
             }}
           >×</button>
         </div>
         ```
       - `handleDismiss = () => { sessionStorage.setItem('gsd.coefficients.warning.dismissed', '1'); setHidden(true); }`
       - SSR-safe guard inside the useEffect: `if (typeof window === 'undefined') return; if (sessionStorage.getItem('gsd.coefficients.warning.dismissed') === '1') setHidden(true);`

    2. CREATE `CoefficientWarningBanner.test.tsx` with Tests 1-8.
       - Use `vi.spyOn(window.sessionStorage, 'getItem'/.setItem)` for assertions
       - For Test 7 SSR safety: render via `renderToString` if available, OR set `delete (global as any).window` before render and assert no throw — adapt to Vitest setup
       - For Test 8 visual chrome: assert via `getComputedStyle` or check inline `style` attribute substrings
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/coefficients/_components/CoefficientWarningBanner.test.tsx</automated>
  </verify>
  <done>
    - 8 behavior tests pass
    - `grep -n "sessionStorage" app/\(admin\)/\[adminSegment\]/coefficients/_components/CoefficientWarningBanner.tsx` returns hits; localStorage does NOT appear
    - `grep -n "gsd.coefficients.warning.dismissed" app/\(admin\)/\[adminSegment\]/coefficients/_components/CoefficientWarningBanner.tsx` returns hit
    - `grep -n "AlertTriangle" app/\(admin\)/\[adminSegment\]/coefficients/_components/CoefficientWarningBanner.tsx` returns hit
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoefficientHistorySidebar in-place refresh (D-21) + click-to-diff removal (D-22)</name>
  <files>app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx, app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx (full file — current Phase 14 implementation with click-to-diff handler)
    - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx (existing tests — Phase 14 tests for click-to-diff MUST be updated/removed per D-22)
    - app/(admin)/[adminSegment]/coefficients/CoefficientDiffPanel.tsx (verify it stays on /history full page only; not modified)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Coefficients CoefficientHistorySidebar refresh (lines 502-522) + row layout (lines 511-518)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-21 in-place refresh — preserve 2-col + pagination + /history link; D-22 click-to-diff removal)
    - src/lib/i18n/dictionaries.ts (verify Plan 18-01 key admin.coefficients.history.viewAll added)
  </read_first>
  <behavior>
    - Test 1 (D-22 click-to-diff removed): clicking on a history row does NOT open the diff modal; row has no onClick handler, no role="button", no cursor:pointer
    - Test 2 (D-22 row cursor: default): rendered row's style.cursor === 'default' (not 'pointer')
    - Test 3 (D-21 row layout): each row contains the 3-element vertical stack — top line (relative time + admin name), middle line (change summary), optional note line (italic if present)
    - Test 4 (D-21 row chrome): row style = padding:'12px 16px'; borderBottom:'1px solid var(--border)'
    - Test 5 (D-21 cursor pagination preserved): the existing cursor pagination control still renders and remains functional (existing Phase 14 test behavior)
    - Test 6 (D-21 /history link preserved): bottom of the sidebar contains a `<Link>` with t('admin.coefficients.history.viewAll', 'fr') = "Voir tout l'historique →" linking to /[adminSegment]/history
    - Test 7 (D-22 Phase 14 in-card diff test removed/updated): the Phase 14 tests asserting in-card diff modal opening are REMOVED from this test file OR updated to assert the row no longer opens a diff modal
  </behavior>
  <action>
    Per D-21 / D-22 / UI-SPEC §Coefficients CoefficientHistorySidebar refresh.

    1. Modify `CoefficientHistorySidebar.tsx` IN PLACE (do NOT replace the file — preserve identity for downstream consumers):

       a. **Remove click-to-diff handler (D-22)**:
          - Delete the `onClick={() => openDiffModal(...)}` handler from each row element
          - Delete the `cursor: 'pointer'` style on the row (or change to `cursor: 'default'`)
          - Delete the modal state (`useState` for diff modal open state if it's owned by this component)
          - Delete the `<CoefficientDiffPanel>` render (if rendered inline)
          - If the diff modal is owned by the page parent (not this component), simply remove the prop callback that triggered it

       b. **Tighten row chrome (D-21)** — per UI-SPEC line 511-518:
          ```
          rowStyle: { padding: '12px 16px', borderBottom: '1px solid var(--border)', cursor: 'default' }
          topLine: { fontSize: 13, fontWeight: 600, color: 'var(--ink)' } (relative time) + { fontSize: 13, fontWeight: 400, color: 'var(--muted)', marginLeft: 8 } (admin name)
          middleLine: { fontSize: 12.5, fontWeight: 400, color: 'var(--muted)', lineHeight: 1.4 } (change summary)
          noteLine (optional): { fontSize: 12.5, fontStyle: 'italic', color: 'var(--muted)', marginTop: 4 }
          ```

       c. **Preserve** — 2-column layout (the parent page.tsx owns this; sidebar is just the right column), cursor pagination, `/history` link at the bottom. The pagination control + load-more button stay as-is.

       d. **Update /history link copy** — change to `t('admin.coefficients.history.viewAll', lang)` (new key from Plan 18-01). Link href = `/${adminSegment}/history`. Style: `fontSize:14; fontWeight:500; color:var(--teal); textDecoration:none`. Place at bottom of the sidebar card with `marginTop:16`.

    2. Update `CoefficientHistorySidebar.test.tsx` per D-22:
       - REMOVE or UPDATE any existing Phase 14 tests that assert click-to-diff behavior. If updating, change assertion to "row click does NOT open diff modal" (Test 1 above).
       - ADD Tests 1-6 from behavior block above.
       - Test 7 is a meta-assertion: confirm that the Phase 14 "in-card diff opening" test no longer exists OR has been transformed into Test 1's negative assertion.

    CRITICAL: this is an IN-PLACE refresh. The file path, exported symbol name, and prop shape (minus removed callbacks) stay the same. Downstream consumers (the page.tsx that renders this component) must continue to import and render it without modification.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/coefficients/CoefficientHistorySidebar.test.tsx</automated>
  </verify>
  <done>
    - 7 behavior tests pass (including the test-update for Phase 14 click-to-diff)
    - `grep -n "openDiffModal\|onClick.*diff\|cursor:.*pointer" app/\(admin\)/\[adminSegment\]/coefficients/CoefficientHistorySidebar.tsx | grep -v '^#'` returns 0 hits (D-22 click-to-diff fully removed)
    - `grep -n "admin.coefficients.history.viewAll" app/\(admin\)/\[adminSegment\]/coefficients/CoefficientHistorySidebar.tsx` returns hit
    - `grep -n "CoefficientHistorySidebar" app/\(admin\)/\[adminSegment\]/coefficients/page.tsx` still shows page renders the component (in-place refresh preserves import)
    - CoefficientDiffPanel.tsx remains intact and continues to be rendered by /history full page (verify with `grep -n "CoefficientDiffPanel" app/\(admin\)/\[adminSegment\]/history/`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire page.tsx — render banner between hero subtitle and 2-column layout</name>
  <files>app/(admin)/[adminSegment]/coefficients/page.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/coefficients/page.tsx (current Phase 14 implementation — full file)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Coefficients layout (lines 458-471)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (Open Q9: page title shortening recommendation — shorten t('admin.coefficients.page.title') value to "Coefficients")
    - src/lib/i18n/dictionaries.ts (Open Q9: planner may shorten existing key value in place; does NOT count as net-new per Plan 18-01 contract)
  </read_first>
  <behavior>
    - Test 1: page renders PageHero with title and subtitle (existing keys)
    - Test 2: page renders `<CoefficientWarningBanner lang={lang} />` BETWEEN the PageHero and the 2-column layout
    - Test 3: page renders 2-column layout: left column (flex:1, minWidth:0) = existing CoefficientsEditor; right column (width:360, flexShrink:0) = `<CoefficientHistorySidebar />`
    - Test 4: 2-column layout style: `display:flex; gap:24; alignItems:flex-start`
    - Test 5: page max-width 1280px per UI-SPEC line 908
  </behavior>
  <action>
    Modify `app/(admin)/[adminSegment]/coefficients/page.tsx`:

    1. Wrap contents in `<main style={{ maxWidth: 1280, margin: '0 auto', padding: '0 24px' }}>` per UI-SPEC line 908.

    2. Render order per UI-SPEC line 458-463:
       a. `<PageHero title={t('admin.coefficients.page.title', lang)} subtitle={t('admin.coefficients.page.sub', lang)} />`
       b. `<CoefficientWarningBanner lang={lang} />` (NEW, between hero and 2-col)
       c. 2-column layout:
          ```
          <div style={{ display: 'flex', gap: 24, alignItems: 'flex-start' }}>
            <div style={{ flex: 1, minWidth: 0 }}>
              <CoefficientsEditor {...existingProps} />
            </div>
            <div style={{ width: 360, flexShrink: 0 }}>
              <CoefficientHistorySidebar {...existingProps} />
            </div>
          </div>
          ```

    3. Preserve `requireAdmin()`, `export const dynamic = 'force-dynamic'`, and any other server-side prelude from Phase 14.

    4. Open Q9 decision — shorten `admin.coefficients.page.title` value:
       - Edit the existing key's value in `src/lib/i18n/dictionaries.ts` from "Coefficients & Commission" → "Coefficients" (FR) and equivalent EN. Per UI-SPEC line 1074 recommendation and Open Q9.
       - This is a value-only change of an EXISTING key, NOT a net-new key. The Plan 18-01 ~70-key count is unaffected.
       - Verify no other consumer of this key relies on the longer value (grep -rn "admin.coefficients.page.title" app/ src/).

    5. NO test file change required if existing tests are tolerant to the new wrapper structure; otherwise update minimally.
  </action>
  <verify>
    <automated>npm test -- app/\(admin\)/\[adminSegment\]/coefficients/ && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `grep -n "CoefficientWarningBanner" app/\(admin\)/\[adminSegment\]/coefficients/page.tsx` returns hit
    - `grep -c "display: 'flex'\|display:.flex" app/\(admin\)/\[adminSegment\]/coefficients/page.tsx` returns >= 1 (2-col layout)
    - Banner rendered between PageHero and 2-col layout (verify DOM order in a quick test)
    - Existing page tests pass (or are updated minimally to tolerate the new banner sibling)
    - TypeScript compile clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | requireAdmin() server-side gate preserved from Phase 14 |
| Client → sessionStorage | per-tab isolation; no cross-origin or cross-tab leak (sessionStorage is per-tab by design) |
| × button click → setHidden | client-only state change; no server roundtrip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Tampering (sessionStorage key abuse) | CoefficientWarningBanner | accept | sessionStorage key 'gsd.coefficients.warning.dismissed' is a per-tab persistence detail with no security impact — its only effect is hiding a warning banner. User can manipulate sessionStorage, but the warning is informational; the actual coefficient-edit gate is server-enforced |
| T-18-05-02 | Information disclosure (commission via warning copy) | CoefficientWarningBanner body | mitigate | Warning copy is generic ("Modifier ces valeurs change le calcul…") and does NOT include the commission VALUE — UI-SPEC §ADMIN-09 envelope verification confirms |
| T-18-05-03 | XSS via i18n value | banner body + history row summaries | mitigate | i18n values are static strings rendered as React text — auto-escaped; no raw-HTML injection sinks |
| T-18-05-04 | Information disclosure (commission via history row summary) | CoefficientHistorySidebar row summary | accept | Per UI-SPEC §ADMIN-09 envelope: history sidebar row summaries CAN include commission values when commission was the field modified (e.g. "Commission: 1.00 → 1.50"). This is pre-existing Phase 14 behavior, allowed because /coefficients is the admin coefficient-editing surface (ADMIN-09 D-12 allowed exception). Phase 18 refresh does NOT add new commission exposure paths |
| T-18-05-05 | SSR crash (sessionStorage on server) | CoefficientWarningBanner mount | mitigate | useEffect-guarded sessionStorage read + `typeof window` guard prevents server-side crash (Test 7 enforces) |
| T-18-05-06 | Repudiation (warning dismissed silently) | sessionStorage write | accept | No audit log entry on banner dismissal; warning is a UX nudge, not a legal disclosure — accept |
</threat_model>

<verification>
- `npm test -- app/\(admin\)/\[adminSegment\]/coefficients/` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (warning banner adds no commission-adjacent surface; history sidebar refresh preserves Phase 14 ADMIN-09 D-12 allowed exception)
- `grep -rn "openDiffModal\|onClick.*diff" app/\(admin\)/\[adminSegment\]/coefficients/CoefficientHistorySidebar.tsx | grep -v '^#'` returns 0 (D-22 click-to-diff removed)
- `npx tsc --noEmit` exits 0
- Manual visual sign-off deferred to Plan 18-07 (12-checkpoint sweep)
</verification>

<success_criteria>
- Warning banner ships per D-19 + D-20 (sessionStorage dismissable, locked copy, --gold accent)
- CoefficientHistorySidebar in-place refresh per D-21 (tighter chrome, /history link)
- Click-to-diff handler removed per D-22; CoefficientDiffPanel stays on /history full page only
- Phase 14 tests for in-card diff opening updated/removed per D-22
- Editor card untouched structurally
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-05-SUMMARY.md`
</output>
