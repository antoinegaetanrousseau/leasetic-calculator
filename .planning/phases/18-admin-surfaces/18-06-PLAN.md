---
phase: 18-admin-surfaces
plan: 06
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/(authed)/aide/page.tsx
  - app/(authed)/aide/page.test.tsx
  - app/(authed)/aide/commencer-ici/page.tsx
  - app/(authed)/aide/commencer-ici/page.test.tsx
  - public/aide/wizard-step-1.png
  - public/aide/wizard-step-2.png
  - public/aide/wizard-step-3.png
autonomous: false
requirements: [HELP-01, THEME-02]
must_haves:
  truths:
    - "/aide landing renders PageHero (Centre d'aide title + subtitle) + 3-card grid (D-25): Commencer ici (link to /aide/commencer-ici) / Créer une proposition (Bientôt disponible badge, disabled) / Contact (mailto:SUPPORT_EMAIL)"
    - "/aide route lives under (authed) group — both partner + admin can read per D-28; gating happens at session layer not the route"
    - "/aide/commencer-ici renders breadcrumb (← Centre d'aide) + article container card (maxWidth 720px) + H1/H2/body/CTA prose with Phase 17 wizard screenshots"
    - "Article content drafts from Plan 18-01 i18n keys (aide.commencer-ici.section.*); user reviews + edits before execute"
    - "Sidebar Aide nav item appears for both partner + admin roles per D-27 (provided by Plan 18-01)"
    - "3 wizard screenshots captured from Phase 17 surfaces and saved under public/aide/"
    - "SUPPORT_EMAIL value confirmed with user before commit (default placeholder: support@leasetic.fr per D-25 + UI-SPEC Open Q10)"
  artifacts:
    - path: app/(authed)/aide/page.tsx
      provides: "Aide landing 3-card placeholder grid per D-25"
      contains: "aide.landing.card.commencerIci"
    - path: app/(authed)/aide/commencer-ici/page.tsx
      provides: "Commencer ici starter article — hardcoded TSX prose per D-24/26"
      contains: "aide.commencer-ici.title"
    - path: public/aide/wizard-step-1.png
      provides: "Phase 17 wizard step 1 screenshot for article body"
      contains: "wizard-step-1"
  key_links:
    - from: app/(authed)/aide/page.tsx
      to: app/(authed)/aide/commencer-ici/page.tsx
      via: "Card 1 Lire le guide → href=/aide/commencer-ici"
      pattern: "aide/commencer-ici"
    - from: app/(authed)/aide/commencer-ici/page.tsx
      to: public/aide/wizard-step-{1,2,3}.png
      via: "Next <Image src='/aide/wizard-step-N.png' />"
      pattern: "wizard-step-"
---

<objective>
Ship the Aide / Help Center (HELP-01 net-new) per UI-SPEC §Aide landing + §Aide article contracts. Net-new (authed) route group:
- `/aide` — landing page with 3-card placeholder grid (D-25). Two real CTAs (Lire le guide → starter article; Contact mailto) and one disabled card (Créer une proposition: Bientôt disponible badge).
- `/aide/commencer-ici` — hardcoded TSX starter article (D-24/26) walking through wizard step 1 → 2 → 3 with screenshots from Phase 17 wizard surfaces.

Both pages live under `(authed)` route group so partner + admin auth both apply (D-28). Sidebar Aide nav item already provided by Plan 18-01 D-27 for both roles.

Purpose: HELP-01 net-new (filed in REQUIREMENTS.md during planning) + UI-SPEC §Files Modified.
Output: 2 net-new pages + 3 screenshot PNG assets + tests + a `checkpoint:human-verify` for SUPPORT_EMAIL confirmation + screenshot capture sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/18-admin-surfaces/18-01-PLAN.md

<interfaces>
<!-- From Plan 18-01 -->
- ~17 i18n keys for landing + article: aide.landing.title, .subtitle, .card.commencerIci.title/.body/.cta, .card.creerProposition.title/.body/.badge, .card.contact.title/.body, aide.commencer-ici.title/.lead/.breadcrumb/.cta.create, .section.overview/.step1/.step2/.step3/.next title/body, .step1/.step2/.step3.screenshot.alt
- Sidebar nav already exposes Aide for both partner + admin roles

<!-- From existing codebase -->
- `<PageHero>` from src/components/ui/PageHero.tsx
- `requireUser()` from src/lib/auth/require.ts (or equivalent — verify the (authed) layout already calls this; if so, no per-route auth call needed)
- lucide-react icons: BookOpen / Sparkles (Card 1), FileText (Card 2), Mail (Card 3)
- Next.js `<Image>` from 'next/image'
- Phase 17 wizard surfaces (deployed) — source for the 3 screenshots
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm SUPPORT_EMAIL for Aide Contact card</name>
  <decision>
    What email address should the Aide landing Card 3 ("Contact") use as its mailto target?
  </decision>
  <context>
    UI-SPEC Open Q10 + D-25 placeholder defaults to `support@leasetic.fr`. The user must confirm this address before shipping. If a different address is preferred (e.g. `antoine.rousseau@leasetic.com`, `contact@leasetic.fr`, or a shared inbox), the planner-suggested placeholder must be replaced before commit.
  </context>
  <options>
    <option id="option-a">
      <name>support@leasetic.fr</name>
      <pros>Generic shared inbox; matches the planner placeholder; no domain ambiguity</pros>
      <cons>Inbox must actually exist and be monitored — if it doesn't, partners get a bounce or silence</cons>
    </option>
    <option id="option-b">
      <name>antoine.rousseau@leasetic.com (sole-founder direct)</name>
      <pros>Guaranteed delivery to a monitored inbox today; matches the v1.1 admin seeding domain</pros>
      <cons>Doesn't scale beyond Antoine; mixes personal + product traffic</cons>
    </option>
    <option id="option-c">
      <name>Other (specify)</name>
      <pros>User-defined</pros>
      <cons>Requires user input</cons>
    </option>
  </options>
  <resume-signal>Reply with "option-a", "option-b", or "option-c: {email}". The executor will substitute the chosen address everywhere in Card 3 (mailto: href + visible body text).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /aide landing — 3-card placeholder grid (D-25)</name>
  <files>app/(authed)/aide/page.tsx, app/(authed)/aide/page.test.tsx</files>
  <read_first>
    - app/(authed)/layout.tsx (verify the (authed) route group sets up requireUser() so /aide doesn't need its own auth call — per D-28 both partner + admin can read)
    - src/components/ui/PageHero.tsx (props)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Aide landing — full contract (lines 530-579)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-23 sidebar inclusion; D-25 3-card layout; D-28 (authed) group placement)
    - src/lib/i18n/dictionaries.ts (verify Plan 18-01 keys present: aide.landing.title/.subtitle, aide.landing.card.commencerIci/.creerProposition/.contact)
  </read_first>
  <behavior>
    - Test 1: page renders PageHero with title t('aide.landing.title') = "Centre d'aide" and subtitle
    - Test 2: page renders exactly 3 cards in a `display:grid; grid-template-columns:repeat(3,1fr); gap:24` grid
    - Test 3 (Card 1): Commencer ici — title + body + `<Link>` Lire le guide → href=/aide/commencer-ici, with a lucide icon (BookOpen or Sparkles) colored var(--gd-text)
    - Test 4 (Card 2): Créer une proposition — title + body + Bientôt disponible badge (NOT a link, NOT clickable), card opacity 0.7 (or visually disabled chrome), icon colored var(--muted)
    - Test 5 (Card 3): Contact — title + body + `<a href="mailto:{SUPPORT_EMAIL}">` (value from Task 1 decision) — Mail lucide icon colored var(--gd-text)
    - Test 6: page max-width 1040px per UI-SPEC line 915
    - Test 7: Card 2 disabled state — no href, no onClick, badge renders Bientôt disponible
  </behavior>
  <action>
    Per D-25 / UI-SPEC §Aide landing.

    1. CREATE `app/(authed)/aide/page.tsx`:
       - Server component (no client state on the landing — pure render). Verify (authed) layout already calls requireUser(); if not, add a per-route requireUser() call.
       - Read lang from session/cookies (existing pattern).
       - Resolve SUPPORT_EMAIL constant at the top of the file from Task 1 decision: `const SUPPORT_EMAIL = '<value from Task 1>';`

    2. JSX layout (per UI-SPEC line 534-545):
       - `<main style={{ maxWidth: 1040, margin: '0 auto', padding: '0 24px' }}>`
       - `<PageHero title={t('aide.landing.title', lang)} subtitle={t('aide.landing.subtitle', lang)} />`
       - 3-card grid: `<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 24, marginTop: 32 }}>` containing 3 `.card` elements

    3. Card chrome (each) per UI-SPEC line 557-563:
       - `.card` with `padding:24`
       - Top: lucide icon, 24px, color per card (cards 1+3: var(--gd-text); card 2: var(--muted))
       - marginTop:16 → title (14.5px/600/var(--ink))
       - marginTop:8 → body (13px/400/var(--muted)/lineHeight 1.5)
       - marginTop:16 → action (link OR badge)

    4. Per-card spec:

       **Card 1 — Commencer ici:**
       - Icon: `<BookOpen size={24} color="var(--gd-text)" />`
       - Title: `t('aide.landing.card.commencerIci.title', lang)`
       - Body: `t('aide.landing.card.commencerIci.body', lang)`
       - Action: `<Link href="/aide/commencer-ici" style={{ fontSize: 13, fontWeight: 500, color: 'var(--teal)', textDecoration: 'none' }}>{t('aide.landing.card.commencerIci.cta', lang)}</Link>`

       **Card 2 — Créer une proposition (disabled):**
       - Icon: `<FileText size={24} color="var(--muted)" />`
       - Title: `t('aide.landing.card.creerProposition.title', lang)`
       - Body: `t('aide.landing.card.creerProposition.body', lang)`
       - Action: `<span style={{ display: 'inline-flex', padding: '4px 10px', borderRadius: 9999, background: 'rgba(110, 113, 145, 0.10)', color: 'var(--muted)', fontSize: 11.8, fontWeight: 600 }}>{t('aide.landing.card.creerProposition.badge', lang)}</span>`
       - Card chrome: outer `.card` style adds `opacity: 0.7` per UI-SPEC line 551

       **Card 3 — Contact:**
       - Icon: `<Mail size={24} color="var(--gd-text)" />`
       - Title: `t('aide.landing.card.contact.title', lang)`
       - Body: `t('aide.landing.card.contact.body', lang)`
       - Action: `<a href={\`mailto:${SUPPORT_EMAIL}\`} style={{ fontSize: 13, fontWeight: 500, color: 'var(--teal)', textDecoration: 'none' }}>{SUPPORT_EMAIL}</a>`

    5. CREATE `app/(authed)/aide/page.test.tsx` with Tests 1-7. Use the SUPPORT_EMAIL value from Task 1 in the assertion for Test 5.
  </action>
  <verify>
    <automated>npm test -- app/\(authed\)/aide/page.test.tsx</automated>
  </verify>
  <done>
    - 7 behavior tests pass
    - `grep -c "aide.landing.card" app/\(authed\)/aide/page.tsx` returns >= 6 (3 cards × at least 2 keys per card — title + body or cta/badge)
    - `grep -n "mailto:" app/\(authed\)/aide/page.tsx` returns hit
    - `grep -n "opacity.*0.7\|opacity: 0.7" app/\(authed\)/aide/page.tsx` returns hit (Card 2 disabled chrome)
    - No commission references introduced
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capture Phase 17 wizard screenshots for Aide article</name>
  <what-built>
    The Aide starter article (`/aide/commencer-ici`) embeds 3 screenshots showing wizard step 1, 2, and 3 from the Phase 17 partner surfaces. Per UI-SPEC line 600-605:
    - Format: PNG, 2x DPR for retina, ~1200px wide source
    - Saved to: public/aide/wizard-step-1.png, wizard-step-2.png, wizard-step-3.png
    - Captured at standard viewport (1280px wide browser, retina)
    - Light mode (per UI-SPEC line 626-630 — Option 1 baseline; dark-mode swap deferred to HELP-02)

    The screenshots should also satisfy the ADMIN-09 D-12 commission-invisibility check from UI-SPEC line 968: capture from a test draft where commission value is generic (1.00%) and the screenshot does NOT zoom into the commission row, OR blur/crop the commission row out of step-2.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`. Log in as a test partner.
    2. Create a test draft via `/proposals/new/parametres`. Use generic test values (e.g. client name "Test", amount 50000€, duration 36 mois) — make sure the commission value resolves to a clean number like 1.00%.
    3. For each step, navigate to the wizard URL, set browser viewport to 1280px wide (use DevTools device toolbar OR resize), and capture a screenshot at retina (2x DPR — macOS Cmd+Shift+4 area select OR `screencapture -R x,y,w,h -t png path.png`):
       - Step 1 `/proposals/new/parametres` → save as `public/aide/wizard-step-1.png`. Capture the PageHero + form card with the 3 sections visible.
       - Step 2 `/proposals/new/calcul?draft_id={your-test-draft}` → save as `public/aide/wizard-step-2.png`. Capture the loyer hero card + Tranche/Coefficient chip. **CRITICAL: do NOT zoom into the commission row.** Crop or blur the commission row if it falls within the framed area.
       - Step 3 `/proposals/new/verification?draft_id={your-test-draft}` → save as `public/aide/wizard-step-3.png`. Capture the 2-column layout (RecapSection + PdfPreviewMock).
    4. Verify each PNG is approximately 1200px wide × proportional height, under 500KB each.
    5. Spot-check each image: open in Preview / image viewer and confirm:
       - [ ] Step 1 PNG shows the PageHero + form card sections clearly
       - [ ] Step 2 PNG does NOT include the literal text "Commission apporteur" zoomed (per ADMIN-09 D-12 envelope, allowed exception is conceptual; UI-SPEC line 968 wants screenshots commission-free)
       - [ ] Step 3 PNG shows the 2-column layout
       - [ ] All 3 PNGs are light-mode (white/var(--paper) background)
    6. Once captured, place files at the 3 paths above and verify with `ls -la public/aide/wizard-step-*.png`.
  </how-to-verify>
  <resume-signal>Type "captured" when all 3 PNGs are in place at `public/aide/wizard-step-{1,2,3}.png` AND confirmed commission-free per the ADMIN-09 envelope check. If a step image inadvertently captured a commission value, recapture before signaling.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: /aide/commencer-ici article — hardcoded TSX prose with 3 screenshots (D-24/26)</name>
  <files>app/(authed)/aide/commencer-ici/page.tsx, app/(authed)/aide/commencer-ici/page.test.tsx</files>
  <read_first>
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Aide article — full contract (lines 583-630)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-24 hardcoded TSX storage; D-26 ~500-1000 words FR+EN walking through wizard steps; D-28 (authed) placement)
    - src/lib/i18n/dictionaries.ts (verify Plan 18-01 article keys present: aide.commencer-ici.*)
    - public/aide/wizard-step-{1,2,3}.png (from Task 3 — must exist before this task runs)
  </read_first>
  <behavior>
    - Test 1: page renders breadcrumb `<Link href="/aide">{ t('aide.commencer-ici.breadcrumb') }</Link>` (← Centre d'aide)
    - Test 2: page renders article container card with maxWidth 720px per UI-SPEC line 587
    - Test 3: page renders H1 with t('aide.commencer-ici.title')
    - Test 4: page renders 5 H2 section titles (Aperçu du tunnel / Étape 1 / Étape 2 / Étape 3 / Et après ?) from aide.commencer-ici.section.*.title keys
    - Test 5: page renders 3 Next `<Image>` elements pointing to /aide/wizard-step-1.png, /wizard-step-2.png, /wizard-step-3.png with alt text from aide.commencer-ici.section.step*.screenshot.alt keys
    - Test 6: page renders end-of-article CTA `<Link href="/proposals/new/parametres" className="btn-green">` with t('aide.commencer-ici.cta.create')
    - Test 7: prose typography — H1 fontSize 28, H2 fontSize 20, body paragraphs fontSize 14.5 (verify via inline styles)
    - Test 8: page max-width 720px per UI-SPEC line 920
  </behavior>
  <action>
    Per D-24 / D-26 / UI-SPEC §Aide article.

    1. CREATE `app/(authed)/aide/commencer-ici/page.tsx`:
       - Server component. (authed) layout owns auth.
       - Read lang from session.

    2. JSX layout (per UI-SPEC line 587-596, single-column maxWidth 720px):
       ```
       <main style={{ maxWidth: 720, margin: '0 auto', padding: '0 24px' }}>
         <Link
           href="/aide"
           style={{ fontSize: 12.5, fontWeight: 500, color: 'var(--muted)', textDecoration: 'none', display: 'inline-block', marginBottom: 16 }}
         >
           {t('aide.commencer-ici.breadcrumb', lang)}
         </Link>

         <article className="card" style={{ padding: '48px 56px' }}>
           <h1 style={{ fontSize: 28, fontWeight: 700, lineHeight: 1.25, color: 'var(--ink)', margin: 0 }}>
             {t('aide.commencer-ici.title', lang)}
           </h1>

           <p style={{ fontSize: 14.5, fontWeight: 400, lineHeight: 1.6, color: 'var(--ink)', marginTop: 16, marginBottom: 0 }}>
             {t('aide.commencer-ici.lead', lang)}
           </p>

           {/* Section: Aperçu du tunnel */}
           <h2 style={{ fontSize: 20, fontWeight: 600, lineHeight: 1.3, color: 'var(--ink)', marginTop: 32, marginBottom: 16 }}>
             {t('aide.commencer-ici.section.overview.title', lang)}
           </h2>
           <p style={{ fontSize: 14.5, fontWeight: 400, lineHeight: 1.6, color: 'var(--ink)', marginBottom: 16 }}>
             {t('aide.commencer-ici.section.overview.body', lang)}
           </p>

           {/* Section: Étape 1 — Paramètres */}
           <h2 style={{ ...h2Style }}>{t('aide.commencer-ici.section.step1.title', lang)}</h2>
           <p style={{ ...bodyStyle }}>{t('aide.commencer-ici.section.step1.body', lang)}</p>
           <Image
             src="/aide/wizard-step-1.png"
             alt={t('aide.commencer-ici.section.step1.screenshot.alt', lang)}
             width={720}
             height={400}
             style={{ width: '100%', height: 'auto', borderRadius: 8, border: '1px solid var(--border)', margin: '24px 0' }}
           />

           {/* Section: Étape 2 — Calcul (same pattern) */}
           {/* Section: Étape 3 — Vérifier (same pattern) */}

           {/* Section: Et après ? */}
           <h2 style={{ ...h2Style }}>{t('aide.commencer-ici.section.next.title', lang)}</h2>
           <p style={{ ...bodyStyle }}>{t('aide.commencer-ici.section.next.body', lang)}</p>

           <div style={{ marginTop: 24 }}>
             <Link href="/proposals/new/parametres" className="btn-green">
               {t('aide.commencer-ici.cta.create', lang)}
             </Link>
           </div>
         </article>
       </main>
       ```
       - Define `h2Style` and `bodyStyle` as local const objects to DRY the repeated styles.
       - The image `width` and `height` props on Next.js `<Image>` need real dimensions — set to 1200x800 (source 2x) and let the rendered `style={{ width: '100%', height: 'auto' }}` handle responsive sizing.
       - Per UI-SPEC line 611 + Open Q7 recommendation: drop the 16px lead override; use standard 14.5px body for the lead paragraph (avoids breaching the 4-size typography limit). The render code above already follows this.

    3. CREATE `commencer-ici/page.test.tsx` with Tests 1-8.

    4. ADMIN-09 envelope check (UI-SPEC line 968): article body discusses wizard step 2 conceptually ("le résultat du calcul s'affiche", "loyer mensuel" etc.) but does NOT include literal commission values or the partner-facing commission line "Commission apporteur (non visible client)" as embedded copy. The body i18n strings drafted in Plan 18-01 already comply. Verify post-draft via `grep -ni "commission" src/lib/i18n/dictionaries.ts | grep "aide.commencer-ici"` returns 0 hits.

    5. No commission references in this page or its i18n keys.
  </action>
  <verify>
    <automated>npm test -- app/\(authed\)/aide/commencer-ici/page.test.tsx</automated>
  </verify>
  <done>
    - 8 behavior tests pass
    - `grep -c "aide.commencer-ici.section" app/\(authed\)/aide/commencer-ici/page.tsx` returns >= 8 (4 section title keys + at least 4 body keys)
    - `grep -c "wizard-step-" app/\(authed\)/aide/commencer-ici/page.tsx` returns 3 (one Image per step)
    - `grep -ni "commission" src/lib/i18n/dictionaries.ts | grep "aide.commencer-ici"` returns 0
    - ADMIN-09 9-gate suite remains green
    - `ls public/aide/wizard-step-{1,2,3}.png` shows 3 files exist
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → (authed) layout | requireUser() — both partner + admin auth honored per D-28 |
| /aide → /aide/commencer-ici | both routes under (authed); no privileged data exposed |
| mailto: link → external mail client | client-side handoff; no server data exposed |
| public/aide/*.png assets | static public assets; no auth gate (per Next.js public/ convention) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Information disclosure (commission via screenshot) | public/aide/wizard-step-2.png | mitigate | Task 3 checkpoint explicitly requires the step-2 screenshot to omit / crop / blur the commission row; ADMIN-09 envelope check (UI-SPEC line 968) enforced at capture time + verified during 18-07 closing-out via byte inspection or visual review |
| T-18-06-02 | Information disclosure (commission via article copy) | aide.commencer-ici.* i18n values | mitigate | Plan 18-01 drafted article body avoids literal commission values; grep gate in Task 4 done-criteria confirms no "commission" substring in aide.commencer-ici.* keys |
| T-18-06-03 | XSS via article markup | hardcoded TSX prose | mitigate | All copy comes from t() returns rendered as React text — auto-escaped; no raw-HTML injection sinks; no user-supplied content interpolated |
| T-18-06-04 | Phishing via Aide article CTA | end-of-article CTA href | accept | CTA points to /proposals/new/parametres (internal trusted route); no external redirect; no open-redirect parameter |
| T-18-06-05 | Information disclosure (SUPPORT_EMAIL harvesting by bots) | mailto: link rendered as visible text | accept | The address is intentionally public per D-25; Antoine confirmed in Task 1 decision. Future mitigation if spam volume becomes problematic: obfuscate via JS-rendered email or a contact form |
| T-18-06-06 | Tampering (public asset replaced by attacker) | public/aide/wizard-step-*.png | accept | public/ assets are served by Next.js with the same trust as any static asset; deployment-time integrity is the gate (Vercel build artifacts immutable per deploy); no runtime mutation surface |
</threat_model>

<verification>
- `npm test -- app/\(authed\)/aide/` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (Aide pages + i18n add no commission-adjacent surface)
- `ls public/aide/wizard-step-{1,2,3}.png` shows 3 PNG files exist
- `grep -ni "commission" src/lib/i18n/dictionaries.ts | grep "aide\\."` returns 0 (no commission references in Aide content)
- SUPPORT_EMAIL value confirmed by user (Task 1 decision)
- Manual visual sign-off deferred to Plan 18-07 (Aide landing + article × 2 modes — 4 of the 12 checkpoints)
</verification>

<success_criteria>
- /aide landing renders 3-card grid per D-25
- /aide/commencer-ici renders the starter article per D-24/26 with 3 wizard screenshots
- SUPPORT_EMAIL confirmed and wired into Card 3
- Screenshots captured commission-free per ADMIN-09 envelope
- Sidebar Aide nav item visible for both partner + admin (delivered by Plan 18-01 D-27)
- HELP-01 requirement satisfied (landing + 1 starter article)
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-06-SUMMARY.md`
</output>
