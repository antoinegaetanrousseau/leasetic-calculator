---
phase: 31.1-app-shell-refresh
plan: 07
type: execute
wave: 4
depends_on: ["31.1-01", "31.1-03", "31.1-04", "31.1-05", "31.1-06"]
files_modified:
  - .planning/codebase/UI-CONVENTIONS.md
  - CLAUDE.md
autonomous: false
requirements: [SHELL-C1, SHELL-C2, SHELL-C3, SHELL-C4, SHELL-C5, SHELL-C6, SHELL-C7]
must_haves:
  truths:
    - "UIC-04 records the mechanism that actually ships — no Locked rule is left silently contradicted (D-02)."
    - "OPEN-A is closed at 16px through a named container token, and OPEN-C is closed as delivered (D-01)."
    - "UIC-03 carries a dark-mode addendum pointing at the sampled palette and its three open gaps (D-13)."
    - "All seven ROADMAP success criteria are confirmed against the running application by the operator."
  artifacts:
    - path: ".planning/codebase/UI-CONVENTIONS.md"
      provides: "Revised UIC-04, UIC-03 addendum, OPEN-A and OPEN-C closures"
      contains: "--radius-container"
  key_links:
    - from: ".planning/codebase/UI-CONVENTIONS.md UIC-04"
      to: "app/globals.css radius scale"
      via: "rule text naming the explicit per-step scale and the control tier"
      pattern: "radius-container"
---

<objective>
Bring the ratified-conventions record in line with what this phase shipped, and put the seven
ROADMAP success criteria in front of the operator against the running application.

Purpose: UIC-04 is **Locked** — a rule with a documented incident behind it — and this phase
supersedes its multiplier clause by legitimate assignment (OPEN-A parks the container-radius
decision here). Leaving a Locked rule contradicted by the code is worse than the original split:
the next phase's checker reads UI-CONVENTIONS.md first. Same for OPEN-A, OPEN-C and UIC-03's
silence on dark mode.

Output: a conventions record that a future phase can trust, and an operator sign-off on criteria
1-7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/UI-CONVENTIONS.md
@.planning/phases/31.1-app-shell-refresh/31.1-01-SUMMARY.md
@.planning/phases/31.1-app-shell-refresh/31.1-04-SUMMARY.md
@.planning/phases/31.1-app-shell-refresh/31.1-05-SUMMARY.md
@.planning/phases/31.1-app-shell-refresh/31.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise UIC-04 and close OPEN-A and OPEN-C</name>
  <files>.planning/codebase/UI-CONVENTIONS.md</files>
  <read_first>
    - `.planning/codebase/UI-CONVENTIONS.md` § UIC-04 and § Open Items (OPEN-A, OPEN-C) and
      § Sources
    - `app/globals.css` as it stands after Plans 31.1-01 and 31.1-04 (the shipped scale is the
      source of truth for the rule text — do not describe the plan, describe the code)
    - `.planning/phases/31.1-app-shell-refresh/31.1-01-SUMMARY.md` (the measured control-corner
      value recorded there)
    - `tests/radius-scale.test.ts` and `tests/container-radius.test.ts` (the checks the rule should
      name, so a future reader knows how to re-verify it)
  </read_first>
  <action>
    Rewrite UIC-04 so it describes two tiers instead of one knob, keeping its **Locked** status and
    its incident narrative:

    - The multiplier clause is superseded. State that `--radius-sm … --radius-4xl` are now
      declared as explicit standalone px values, that no step derives from `--radius` or from any
      other step, and that this is what makes the 1rem pill incident structurally impossible rather
      than merely avoided.
    - Name the **control tier** honestly: base-maia's `Button`, `Input`, `Select`, `Badge`,
      `Toggle`, `Tabs`, `Tooltip`, `InputGroup`, `ButtonGroup`, `Progress`, `Combobox` and the ReUI
      `Badge`/`Cascader` reach `--radius-4xl` through the `rounded-4xl` utility, and
      `calendar.tsx` reads `var(--radius-4xl)` as `--cell-radius`. Record the value they actually
      resolve to (from `31.1-01-SUMMARY.md`) and note that ROADMAP criterion 4's "8px" described
      Colibris's controls, not Leasétic's — the binding requirement was "keeps its current corner",
      which is what shipped. This correction matters: a future phase reading "controls are 8px"
      would make a wrong edit.
    - Name the **container tier**: `--radius-container` (16px, aliasing `--radius-container-sm`
      from the explicit 12/16/24/32/40 ladder). State that container surfaces read it and control
      primitives never do, that the two tiers share no variable, and list what reads it: `.card`,
      shadcn `Card`, `Dialog`, `AlertDialog`, `Popover`, and Phase 31's `PairReviewCard`.
    - Keep the `.card`-tracks-a-token clause verbatim in force; note only that the token changed
      from `--radius-2xl` to `--radius-container`.
    - Record the two named exceptions from Plan 31.1-04: `EmbeddedPdfPreview.tsx`'s
      `rounded-[12px]` (frames the PDF page surface governed by the print invariant) and
      control-attached menu surfaces (select content, dropdown menus, combobox, cascader) which
      stay on the control tier by design.
    - Name `tests/radius-scale.test.ts` and `tests/container-radius.test.ts` as the checks that
      enforce the rule, replacing "re-check the top of the scale against a real form" as the only
      verification instruction.
    - Add a line stating `--radius: 0.625rem` is still declared but no longer drives anything.

    Close **OPEN-A**: replace the open item with a closure record — resolved at 16px via
    `--radius-container` in Phase 31.1; both the 18px `.card` and the 24px literal converged; the
    literal is gone. Keep the historical table so the reasoning survives, marked as resolved.

    Close **OPEN-C**: the deferred app-shell directives (breadcrumbs in the header, convergence on
    the vendored block's structure, smaller lockup, fixed collapsed layout) were delivered by Phase
    31.1. Record what was **not** adopted from `app-shell-1` and why: its className-set sidebar
    widths (the latent bug `Shell.tsx` documents, live at `app-shell-1/components/app-shell.tsx:28`)
    and its `SidebarRailToggle` (`tabIndex={-1}`, hardcoded English). That is the durable half of
    the closure.

    Add `31.1-UI-SPEC.md`, `31.1-CONTEXT.md` and `app/globals.css` rows to the Sources table as
    appropriate. Do not touch OPEN-B — this phase does not resolve it.
  </action>
  <acceptance_criteria>
    - UIC-04's text contains `--radius-container`, names `--radius-4xl` as the control tier, and no
      longer instructs the reader not to change the `@theme inline` multipliers (they no longer
      exist).
    - OPEN-A appears under a resolved/closed heading with the 16px value and the closing phase.
    - OPEN-C appears closed, with the two explicitly-not-adopted `app-shell-1` mechanics recorded.
    - OPEN-B is unchanged.
    - No rule elsewhere in the file still asserts that `--radius` drives the scale.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "radius-container\|OPEN-A\|OPEN-C\|radius-4xl" .planning/codebase/UI-CONVENTIONS.md && ! grep -n "multiplier block" .planning/codebase/UI-CONVENTIONS.md</automated>
  </verify>
  <done>UIC-04 describes the shipped two-tier mechanism; OPEN-A and OPEN-C are closed with their reasoning preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add the UIC-03 dark-mode addendum and the shell-dimension record</name>
  <files>.planning/codebase/UI-CONVENTIONS.md</files>
  <read_first>
    - `.planning/codebase/UI-CONVENTIONS.md` § UIC-03 and § UIC-10
    - `.planning/phases/31.1-app-shell-refresh/31.1-05-SUMMARY.md` (the sampled values and the
      `DARK-GAP-01/02/03` follow-ups)
    - `.planning/phases/31.1-app-shell-refresh/31.1-06-SUMMARY.md` (the badge-fill decision)
    - `app/globals.css` `html[data-theme="dark"]` as shipped
  </read_first>
  <action>
    Append a short dark-mode addendum to UIC-03. It must say: the accent is unchanged and stays
    `#01cc72` in both themes — Colibris's `#009999` teal is **not** adopted anywhere, and
    `tests/dark-palette.test.ts` enforces that; four shell-surface roles (`--background`,
    `--card`/`--sidebar`, `--border`/`--sidebar-border`, `--muted-foreground`) carry values sampled
    from one operator screenshot of Colibris in dark mode with `~a shade` confidence; the canonical
    record is `31.1-UI-SPEC.md` § Dark Mode; and three surfaces remain **unsampled and deliberately
    unchanged** — dialog/popover elevation, input fill, sidebar hover/active. Record those three as
    open follow-ups with their closure path (a targeted second Colibris screenshot), in the same
    "record, do not fix" register the Open Items section already uses.

    Also record, in UIC-03 or a short new subsection, that the collapsed sidebar brand badge uses
    `bg-sidebar-accent` rather than the accent, because the mark asset is itself `#01CC72` — a
    green-on-green badge would render blank. This is the kind of finding a future phase would
    otherwise rediscover by shipping it.

    Add a short shell-dimensions note (sidebar 252px / 68px, header `--topbar-h` 52px, brand lockup
    120px ≈ 47% of sidebar width) stating these are shell dimensions tracked separately from
    UIC-01's spacing scale, exactly as the previous 260/72 values were, and that the sidebar widths
    must stay in `SidebarProvider`'s inline `style` because a className is outranked by the
    provider's own spread style.

    Do not restate UIC-01, UIC-02, UIC-05 … UIC-09; they are unchanged by this phase.
  </action>
  <acceptance_criteria>
    - UIC-03 contains the addendum with the four sampled roles, the three named gaps, and the
      accent-unchanged statement.
    - The badge-fill finding is recorded with its reason.
    - The shell-dimension note names 252/68/52/120 and the inline-`style` requirement.
    - No unchanged rule's text was rewritten.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "009999\|252px\|bg-sidebar-accent\|unsampled\|Unsampled" .planning/codebase/UI-CONVENTIONS.md</automated>
  </verify>
  <done>UIC-03 records the sampled dark palette and its honest gaps; the shell dimensions and the badge-fill finding are durable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase acceptance — the seven ROADMAP criteria against the running app</name>
  <files>CLAUDE.md (item 10 only — written solely on explicit operator approval)</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 31.1: App Shell Refresh" (the seven success criteria are the
      script for this checkpoint)
    - `.planning/phases/31.1-app-shell-refresh/31.1-01-SUMMARY.md` (the recorded control-corner
      value item 1 compares against)
    - `.planning/phases/31.1-app-shell-refresh/31.1-05-SUMMARY.md` (`DARK-GAP-01/02/03`)
    - `CLAUDE.md` lines 24-30 (the two statements this phase invalidated — item 10)
  </read_first>
  <action>
    Run `npm run dev`, walk the operator through the ten items below in order, and record their
    answers verbatim. Do not fix anything discovered here silently: a failure becomes a named gap
    in the SUMMARY. Prepare the proposed `CLAUDE.md` replacement text for item 10 and show it to
    the operator **before** writing it — apply the edit only on explicit approval.
  </action>
  <what-built>
    All seven criteria are implemented and under automated source/behaviour gates
    (`tests/radius-scale.test.ts`, `tests/container-radius.test.ts`, `tests/dark-palette.test.ts`,
    `src/lib/route-meta.test.ts`, `src/components/Topbar.test.tsx`,
    `src/components/ui/AppSidebar.test.tsx`). jsdom performs no layout, so the geometric and
    colour claims below can only be confirmed on the running application.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and sign in as an admin. Then:

    1. **Criterion 4 — the named regression gate.** Open `/parametres` (the `ParametresForm` the
       `app/globals.css:66-74` incident specifically named). In DevTools, inspect any `Input` and
       read its computed `border-radius`. It must equal the value recorded in
       `31.1-01-SUMMARY.md` — the same corner as before this phase. If any field renders as a
       stadium/pill that did not before, stop: the scale regressed.
    2. **Criterion 4/5 — containers.** On `/` and `/{adminSegment}/companies/review`, confirm the
       metric tiles, `.card` sections and the review cards all share one corner (16px), and that
       the two review dialogs match them when opened.
    3. **Criterion 1 — collapsed rail.** Collapse the sidebar from the header control. The rail
       must show the brand badge and the nav icons only — no chevron, no vertical stack. Confirm
       the badge is **visible** (green mark on a neutral badge, not a blank green square).
    4. **Criterion 1/D-09 — centring.** With the rail collapsed, measure in DevTools: the badge's
       horizontal centre and a nav icon's horizontal centre should both be ~34px from the sidebar's
       left edge (the 68px rail's centre). Report both numbers.
    5. **Criterion 2 — the control.** Tab to the header toggle with the keyboard and activate it.
       In FR the accessible name is "Basculer la barre latérale"; switch to EN and confirm "Toggle
       sidebar". Confirm no second control announces the same name.
    6. **Criterion 3 — breadcrumbs.** Visit `/`, `/proposals`, `/clients`, a client detail page,
       `/{adminSegment}/companies`, `/{adminSegment}/companies/review` and
       `/{adminSegment}/partners/new`. Each shows a trail in your language whose last segment is
       plain text (not a link) and whose parent segment navigates.
    7. **Criterion 6 — proportion.** Expanded, the sidebar is 252px and the lockup 120px.
    8. **Criterion 7 — dark mode.** Toggle to dark. Confirm: the page background and sidebar share
       one tone with the card one step lighter; the accent is still Leasétic green, nowhere teal;
       **reload the page and confirm there is no flash of light theme**; open a proposal's PDF
       preview and confirm the document surface is still **pure white**.
    9. **Dark-mode gaps — your decision.** Three surfaces were never sampled and ship unchanged
       (`DARK-GAP-01` dialog/popover elevation, `DARK-GAP-02` input fill, `DARK-GAP-03` sidebar
       hover/active). Open a dialog and a form in dark mode and hover a nav item. Either accept
       them as-is, or supply a second Colibris dark-mode screenshot so they can be sampled in a
       follow-up. **No value will be invented to close them.**
    10. **`CLAUDE.md` approval.** `CLAUDE.md`'s "Open items" section still says OPEN-A is deferred
        to this phase and that `--radius` is pinned and the `@theme inline` multipliers must not
        change. Both statements are now false. The executor will show you the proposed replacement
        text; approve it before it is written — project instructions are not edited without your
        say-so.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms items 1-8, reporting the two measured centres from item 4 and the measured
      Input `border-radius` from item 1.
    - Item 9 is answered with an explicit accept-or-supply decision, recorded in the SUMMARY.
    - Item 10 is either approved (and `CLAUDE.md` updated in this task) or declined (and recorded
      as an open follow-up).
    - Any failure is recorded as a named gap in the SUMMARY rather than being fixed silently.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the measurements from items 1 and 4 and your decisions on items 9 and 10, or describe what failed.</resume-signal>
  <verify>
    <automated>npm run test && npm run build</automated>
    <human-check>Operator confirms items 1-8 with measurements, answers item 9, and approves or declines item 10.</human-check>
  </verify>
  <done>All seven ROADMAP criteria confirmed on the running application, with the measured Input radius and the two collapsed-rail centres recorded, an explicit DARK-GAP decision, and an approved-or-declined CLAUDE.md update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning/convention documents → every future phase's planner and checker | A wrong rule here propagates into code that has not been written yet. |
| Agent-proposed edits → `CLAUDE.md` (project instructions) | Project instructions are operator-owned configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.1-07-01 | Tampering | `CLAUDE.md` edited autonomously by an agent | mitigate | The `CLAUDE.md` change is gated behind the operator checkpoint (item 10) and is never written before explicit approval. Only `.planning/codebase/UI-CONVENTIONS.md` is edited autonomously, and only in the sections this phase's decisions assign to it |
| T-31.1-07-02 | Repudiation | A future phase reading "controls are 8px" from the ROADMAP and re-pointing the control tier | mitigate | Task 1 records the measured value and explicitly corrects the ROADMAP's description, naming the test that enforces it |
| T-31.1-07-03 | Information Disclosure | Convention docs quoting a real admin URL segment as an example | mitigate | Use `{adminSegment}` as a placeholder in all documentation text; never paste the value of `ADMIN_URL_SEGMENT` |
| T-31.1-07-04 | Tampering | Dark-mode gaps quietly filled with invented values to make the record look complete | mitigate | Checkpoint item 9 forces an explicit operator decision; `tests/dark-palette.test.ts` assertion 5 fails if a gap token changes without a deliberate test edit |
| T-31.1-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install anywhere in Phase 31.1 |
</threat_model>

<verification>
- `npm run test`, `npm run build`, `npm run typecheck`, `npm run lint:check` all pass.
- `.planning/codebase/UI-CONVENTIONS.md` contains no rule contradicted by `app/globals.css`.
- The operator has answered all ten checkpoint items.
</verification>

<success_criteria>
- UIC-04 revised, OPEN-A and OPEN-C closed, UIC-03 carries the dark addendum.
- All seven ROADMAP criteria confirmed on the running app, with measurements recorded.
- The dark-mode gaps carry an explicit operator decision, not an invented value.
</success_criteria>

<output>
Create `.planning/phases/31.1-app-shell-refresh/31.1-07-SUMMARY.md` when done. Record the measured
Input `border-radius`, the two collapsed-rail centres, the operator's `DARK-GAP` decision, and
whether `CLAUDE.md` was approved and updated.
</output>
