---
phase: 31.1-app-shell-refresh
verified: 2026-09-02T23:25:00Z
status: human_needed
score: 6/7 must-haves fully verified (1 partial — token layer verified, live pass pending)
overrides_applied: 0
human_verification:
  - test: "Reload the app on a dark-theme cookie, visually confirm the shell surfaces (background, sidebar, card, border, muted text) read as Colibris-adjacent tones with zero flash of the wrong theme on first paint."
    expected: "Background/sidebar render at #161616, cards at #1e1e1e, borders at oklch(1 0 0 / 8%), muted text at oklch(0.65 0 0) — matches the six pinned tokens in app/globals.css / tests/dark-palette.test.ts, and no visible flash of light-mode chrome before the dark theme applies."
    why_human: "Requires a running browser session with visual inspection — token-file assertions (tests/dark-palette.test.ts) prove the CSS values exist and are pinned, but cannot prove they render correctly composited on real DOM. The prior attempt at this check (31.1-07 operator checkpoint) started and the browser session dropped mid-check; it was never completed."
  - test: "Open the PDF/print preview for a proposal while the app is in dark mode."
    expected: "The PDF surface renders pure white background / #1a2832 text, per app/globals.css:214-218's [data-pdf-surface] override — unaffected by the new dark-mode token values."
    why_human: "Same reason as above — this is a rendering/visual regression check (Phase 5/8 invariant) that the source-assertion test (tests/dark-palette.test.ts, assertion 1) can only confirm at the CSS-rule level, not at the rendered-output level. Explicitly flagged as not-performed in 31.1-07-SUMMARY.md."
---

# Phase 31.1: App Shell Refresh Verification Report

**Phase Goal:** The application shell converges on Colibris (`https://colibris-eosin.vercel.app`) — breadcrumbs replace the page title, the collapse control moves into the header, the brand lockup is proportioned like Colibris's, and the radius scale is declared step-by-step so container surfaces can be generous without dragging inputs back into the pill shape that got the large radius reverted the first time.
**Verified:** 2026-09-02T23:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

This is a goal-backward, source-grounded verification. Every claim below was checked directly
against the running repository (grep, `npm run typecheck`, `npm run test`, direct file reads) —
not read off SUMMARY.md prose. SUMMARY.md claims that could not be independently reproduced are
called out explicitly rather than accepted on trust.

## Goal Achievement

### Observable Truths (SHELL-C1 … SHELL-C7, minted from ROADMAP's seven success criteria)

| # | Truth (ROADMAP criterion) | Status | Evidence |
|---|---|---|---|
| SHELL-C1 | Collapsing the sidebar no longer stacks the mark above a chevron; the collapse control lives in the shell header at every width | ✓ VERIFIED | `grep -n "ChevronLeftIcon\|toggleSidebar\|<button" src/components/ui/AppSidebar.tsx` → **zero matches** (chevron fully deleted). `Topbar.tsx:50-52` renders `<SidebarTrigger aria-label={t('shell.topbar.toggleSidebar', lang)} />` unconditionally (no `md:hidden`), so it renders at every width. `Shell.tsx:112` mounts `<Topbar>` unconditionally inside `SidebarInset`, so this applies to every authenticated page. |
| SHELL-C2 | Collapse control remains focusable, FR/EN-labelled; no two controls announce the same accessible name | ✓ VERIFIED | `SidebarTrigger` carries an explicit `aria-label` from the FR/EN dictionary (`shell.topbar.toggleSidebar` present in `dictionaries.ts` both languages). `AppSidebar.tsx:265` confirms `<SidebarRail aria-hidden="true" />` — the primitive's own drag-toggle stays excluded from the accessibility tree, and the hand-rolled chevron (the only other control that used to compete) is deleted, not merely relabelled. `Topbar.test.tsx` (8 tests, passing) includes an explicit single-accessible-name assertion. `AppSidebar.test.tsx` migrated its collapse-toggling tests to `SidebarProvider`'s own Cmd/Ctrl+B shortcut, confirming no reliance on a deleted control. |
| SHELL-C3 | Every authenticated page renders a breadcrumb trail, derived from `getRouteMeta`, in the viewer's language, current page non-link | ✓ VERIFIED | `TopbarBreadcrumb.tsx` resolves `RouteMeta.breadcrumb` via `getRouteMeta(pathname, adminSegment)` (same route-matching chain, not a parallel table — confirmed no second exported pathname function exists in `route-meta.ts`). Renders through the shadcn `Breadcrumb` primitive; last segment is always `BreadcrumbPage` (non-link), earlier segments `BreadcrumbLink`. Mounted unconditionally in `Topbar.tsx`, which `Shell.tsx` renders on every authenticated page. `route-meta.test.ts` (23 assertions) and `Topbar.test.tsx` (8 tests, including FR/EN parity and non-link current-page checks) both pass. |
| SHELL-C4 | Container surfaces render at a named token, never a per-file literal; Input/Button/Select keep their current corner; scale is explicit per-step, not multiplied | ✓ VERIFIED (with the ROADMAP's own footnote correction applied) | `grep -n "calc(var(--radius)" app/globals.css` → zero matches. `--radius-sm…4xl` are bare `NNpx` declarations (6/8/10/14/18/22/26). `button.tsx`, `input.tsx`, `select.tsx`, `badge.tsx` all still read `rounded-4xl` (`--radius-4xl` = 26px) — byte-identical to pre-phase. The two radius tiers (`--radius-sm…4xl` vs `--radius-container*`) share **zero** variables — confirmed by reading both declaration blocks directly; no `--radius-container*` line references any `--radius-sm/md/lg/xl/2xl` name. This is what makes the documented UIC-04 pill incident structurally impossible, not merely avoided. `tests/radius-scale.test.ts` (7 assertions) passes and encodes exactly this gate. Per the ROADMAP's own planning-correction footnote, the binding half of this criterion is "keeps its current corner" (26px, not Colibris's 8px) — verified unchanged. |
| SHELL-C5 | Phase 30's `.card` and Phase 31's review-queue cards render at the same radius; no surface left on `rounded-[24px]` | ✓ VERIFIED | `.card` (`app/globals.css`) and `Card`/`Dialog`/`AlertDialog`/`Popover` (`card.tsx`/`dialog.tsx`/`alert-dialog.tsx`/`popover.tsx`) all read `rounded-container` (confirmed via grep on each file). `PairReviewCard.tsx`, `MergeDialog.tsx`, `KeepSeparateDialog.tsx` also read `rounded-container` (Merge/KeepSeparate dropped their overrides entirely, inheriting from the primitive). `grep -rn "rounded-\[24px\]" app src \| grep -v components/blocks/` → **zero matches**. `tests/container-radius.test.ts` (5 assertions) passes. |
| SHELL-C6 | Sidebar brand lockup occupies ~47% of sidebar width (~120px in 252px); sidebar width vars still set where `SidebarProvider`'s inline style can't be outranked | ✓ VERIFIED | `Shell.tsx:94-95` sets `'--sidebar-width': '252px'`, `'--sidebar-width-icon': '68px'` via inline `style` (not `className` — confirmed no `[--sidebar-width` arbitrary-property usage remains in the file). `AppSidebar.tsx:203` — expanded branch: `<BrandLogo width={120} .../>`. 120/252 = 47.6%, matching Colibris's measured ~47%. Collapsed rail: badge (`size-8 rounded-[9px] bg-sidebar-accent`) and nav icons (`group-data-[collapsible=icon]:mx-auto`) both centre within symmetric-padding boxes per D-09's corrected reasoning. |
| SHELL-C7 | Dark mode renders on a palette sampled from Colibris (background, elevated surface, border, muted text traceable to a measured value); print/PDF still forces white; no-flash still works | ⚠ PARTIAL — token layer VERIFIED, live rendering NOT verified | Token layer: `app/globals.css`'s `html[data-theme="dark"]` block declares `--background:#161616`, `--card:#1e1e1e`, `--sidebar:#161616`, `--border`/`--sidebar-border:oklch(1 0 0 / 8%)`, `--muted-foreground:oklch(0.65 0 0)` — each traced to a provenance comment. `tests/dark-palette.test.ts` (6 assertions, **read and confirmed to pin exactly `--popover`, `--input`, `--sidebar-accent` at their pre-phase values** — assertion 5 — as well as the six sampled values, the PDF-white override, the no-flash mechanism, and zero Colibris-teal). All 6 assertions pass. **What is NOT verified:** the live visual pass (does the dark palette actually read as Colibris-adjacent when rendered) and the PDF-white confirmation in a running dark-mode session. `31.1-07-SUMMARY.md` states explicitly this was attempted and abandoned mid-session ("the browser session dropped"). This verifier did not have a running dev server / browser session available either, and does not fabricate a pass for a check the phase's own operator explicitly could not complete. |

**Score:** 6/7 truths fully VERIFIED at the source level; 1/7 (SHELL-C7) is honestly PARTIAL —
the token layer is real and gated by a permanent test, but the rendering-level claim ("dark mode
renders on a palette sampled from Colibris") has never actually been observed running, by any
agent, human or automated. This caveat is carried forward from the phase's own SUMMARY rather
than smoothed into a PASS.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/globals.css` | Explicit radius scale + `--radius-container` ladder + `--topbar-h:52px` + dark tokens | ✓ VERIFIED | All values confirmed present and correct by direct read; `npm run build`-equivalent (`typecheck`) passes. |
| `tests/radius-scale.test.ts` | 7-assertion regression gate | ✓ VERIFIED | Exists, 7 tests, all passing (`npx vitest run` executed directly by this verifier). |
| `tests/container-radius.test.ts` | 5-assertion component-layer gate | ✓ VERIFIED | Exists, 5 tests, all passing. |
| `tests/dark-palette.test.ts` | 6-assertion dark-mode gate, pinning `--popover`/`--input`/`--sidebar-accent` | ✓ VERIFIED | Exists, 6 tests, all passing; assertion 5 confirmed by direct read to pin exactly the three named gap tokens. |
| `src/lib/route-meta.ts` | `RouteMeta.breadcrumb` extending the existing route-matching chain | ✓ VERIFIED | `route-meta.test.ts` (23 assertions) passing; no second exported route-matching function found. |
| `src/components/TopbarBreadcrumb.tsx` | Breadcrumb rendering island | ✓ VERIFIED | Exists, wired into `Topbar.tsx`, renders the shadcn `Breadcrumb` primitive. |
| `src/components/TopbarTitle.tsx` | Deleted | ✓ VERIFIED | File does not exist; zero references anywhere in `app/`/`src/`. |
| `src/components/ui/AppSidebar.tsx` | Chevron deleted, collapsed badge, 120px lockup, eyebrow styling | ✓ VERIFIED | All four confirmed by direct grep/read. |
| `src/components/ui/Shell.tsx` | 252px/68px via inline style | ✓ VERIFIED | Confirmed at lines 94-95. |
| `.planning/codebase/UI-CONVENTIONS.md` | UIC-04 revised, OPEN-A/OPEN-C closed, UIC-03 dark addendum | ✓ VERIFIED | Read directly — UIC-04 correctly describes the two-tier mechanism with the real 26px control value (not the ROADMAP's mistaken 8px); OPEN-A and OPEN-C both marked RESOLVED with historical reasoning preserved. |
| `CLAUDE.md` | Open Items section rewritten to match shipped mechanism | ✓ VERIFIED | Lines 26-32 confirmed: states `--radius` drives nothing, controls are 26px, containers are `--radius-container` (16px), OPEN-A closed. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `Shell.tsx` | `Topbar.tsx` | unconditional render inside `SidebarInset` | ✓ WIRED | `Shell.tsx:112` — every authenticated page renders this. |
| `Topbar.tsx` | `TopbarBreadcrumb.tsx` | direct render | ✓ WIRED | Confirmed in `Topbar.tsx`. |
| `TopbarBreadcrumb.tsx` | `getRouteMeta` | `usePathname()` → `getRouteMeta(pathname, adminSegment)` | ✓ WIRED | Confirmed in source; both consume the same single route-matching function. |
| `.card` / `Card` / `Dialog` / `AlertDialog` / `Popover` / `PairReviewCard` / `MergeDialog` / `KeepSeparateDialog` | `--radius-container` | `rounded-container` utility | ✓ WIRED | Confirmed by direct grep across all 8 call sites. |
| `input.tsx`/`button.tsx`/`select.tsx`/`badge.tsx` | `--radius-4xl` | `rounded-4xl` utility | ✓ WIRED (unchanged) | Confirmed byte-identical to pre-phase behavior. |
| `Topbar.tsx` | `shell.topbar.toggleSidebar` dictionary key | `t(key, lang)` → `SidebarTrigger aria-label` | ✓ WIRED | Confirmed present in both `fr`/`en` dictionaries and consumed at the call site. |

### Data-Flow Trace (Level 4)

Not applicable in the conventional sense — this phase is presentation-only shell chrome with no
database/API data flow. The one "data flow" worth tracing is pathname → breadcrumb label, which
was traced above (SHELL-C3) and is dictionary-keyed, not live-entity-keyed, by the phase's own
documented and justified design decision (`31.1-02-SUMMARY.md`: entity names in a header are an
unscoped information-disclosure surface, out of scope for a presentation-only phase).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Radius/container/dark-mode regression suites all pass | `npx vitest run tests/radius-scale.test.ts tests/container-radius.test.ts tests/dark-palette.test.ts` | 3 files, 18 tests, all passing | ✓ PASS |
| Full test suite has no regressions from this phase | `npm run test` | 133 files passed, 3 skipped; 1685 tests passed, 18 skipped, 0 failing | ✓ PASS |
| Typecheck clean | `npm run typecheck` | Zero errors | ✓ PASS |
| No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in phase-modified files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across globals.css, Topbar.tsx, TopbarBreadcrumb.tsx, AppSidebar.tsx, Shell.tsx, route-meta.ts, card/dialog/alert-dialog/popover.tsx, all three new test files | Zero matches | ✓ PASS |
| Live dark-mode visual render + PDF-white confirmation | *(requires running browser)* | Not run — no live dev server/browser session available to this verifier; phase's own SUMMARY records this was also not completed by the operator | ? SKIP → routed to Human Verification |

### Requirements Coverage

No `SHELL-C*` rows exist in `.planning/REQUIREMENTS.md` (confirmed: `grep -n "SHELL-C" .planning/REQUIREMENTS.md` → no matches). This is a known, pre-existing, repeatedly-flagged gap — Phase 31.1 was inserted into the roadmap after the milestone's requirements table was built, and its requirement IDs (SHELL-C1…C7) were never registered there. Every plan and this verification instead traces to the ROADMAP's seven success criteria directly, per the task's own instruction (`Requirements: TBD` in ROADMAP.md, `<phase_requirement_ids>NONE</phase_requirement_ids>` in this task). Not a phase-goal blocker — it is a documentation-table gap the phase correctly declined to paper over by inventing rows it didn't author.

### Anti-Patterns Found

None found in the files this phase modified. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers, no stub returns, no hardcoded-empty props flowing to rendered output. The dead `sidebar.collapse`/`sidebar.expand` dictionary keys are a deliberate, documented, harmless leftover (D-07's own explicit instruction: leave them rather than delete as a side effect) — not an anti-pattern, an intentional decision recorded in both the UI-SPEC and the SUMMARY.

### Human Verification Required

### 1. Dark-mode live visual pass

**Test:** Set the theme cookie to dark, reload the app, and visually inspect the shell (sidebar, topbar, cards, borders, muted text) against the Colibris dark-mode reference screenshot.
**Expected:** The rendered surfaces read as Colibris-adjacent tones — background/sidebar at a near-black `#161616`, cards one step lighter at `#1e1e1e`, borders subtle (`oklch(1 0 0 / 8%)`), muted text mid-grey — and there is no flash of light-mode chrome before the dark theme applies on first paint.
**Why human:** This is a rendering/visual check. `tests/dark-palette.test.ts` proves the CSS declarations exist with the right values; it cannot prove they composite correctly in a real browser. The phase's own operator checkpoint (31.1-07) attempted this and the browser session dropped mid-check — it was never completed by anyone, human or automated.

### 2. PDF/print dark-mode white-override confirmation

**Test:** With the app in dark mode, open a proposal's PDF/print preview.
**Expected:** The PDF surface renders pure white background with dark text (`#1a2832`), per the Phase 5/8 invariant at `app/globals.css:214-218` — unaffected by any of this phase's dark-token edits.
**Why human:** Same reasoning as above — a rendering-level regression check that the source-assertion test can only confirm exists as a CSS rule, not that it visually applies correctly. Explicitly flagged as not-performed in `31.1-07-SUMMARY.md`.

### Gaps Summary

No BLOCKER-level gaps. Six of seven success criteria (SHELL-C1 through SHELL-C6) are fully
VERIFIED against the actual codebase — chevron removal, single accessible name, breadcrumb
rendering and language-correctness, the two-tier radius mechanism with zero shared variables
between control and container tiers, container-radius convergence with zero remaining
`rounded-[24px]` literals, and the sidebar/lockup proportions — all confirmed by direct source
inspection and passing automated tests (18 phase-specific assertions + 1685 project-wide tests,
all green, run directly by this verifier, not read off a summary).

The one WARNING-level item is SHELL-C7 (dark mode): the token layer is real, sampled with
provenance, and permanently gated by `tests/dark-palette.test.ts` — including the specific pin
on `--popover`/`--input`/`--sidebar-accent` the orchestrator context called out as load-bearing.
What is genuinely missing is a live, rendered confirmation that the palette looks right and that
the PDF-white override still holds in a running dark-mode session. This was attempted once by the
phase's own operator and abandoned mid-session (browser dropped) — it is an honest, previously
disclosed gap, not a new finding, and this verification declines to convert that PARTIAL into a
PASS. `DARK-GAP-01/02/03` (dialog/popover elevation, input fill, sidebar hover/active) are a
separate, deliberately-accepted limitation — pinned by test, operator-approved, not a defect.

---

*Verified: 2026-09-02T23:25:00Z*
*Verifier: Claude (gsd-verifier)*
