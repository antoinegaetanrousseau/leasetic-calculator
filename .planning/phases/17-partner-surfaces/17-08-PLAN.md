---
phase: 17-partner-surfaces
plan: 08
type: execute
wave: 3
depends_on: [17-03, 17-04, 17-05, 17-06, 17-07]
files_modified:
  - docs/accessibility/16-contrast-audit.md
autonomous: false
requirements: [THEME-01, WIZ-05]
must_haves:
  truths:
    - "Every Phase 17 partner-side surface (Partner Home /, /proposals, /proposals?archived=1, wizard step 1/2/3) ships visually correct in BOTH light and dark"
    - "CONTRAST-02 audit appendix updated with 4 new rows (Tranche/Coefficient chip light + dark; Active filter pill light + dark) per UI-SPEC §Contrast Audit"
    - "Row 11 (dark-mode active filter pill ~3.9:1) is documented with the Option 1 accept-with-deviation rationale (matches existing .chip-active baseline)"
    - "ADMIN-09 9-gate grep-contract suite remains green (full test run); ADMIN-09 D-12 envelope preserved across all 5 surfaces"
    - "Antoine signs off the light+dark visual verification + the contrast audit append"
  artifacts:
    - path: docs/accessibility/16-contrast-audit.md
      provides: "4 net-new rows (8, 9, 10, 11) covering Phase 17 token pairs per CONTRAST-02 + dark-mode row-11 deviation note"
      contains: "Tranche/Coefficient pill chip"
  key_links:
    - from: docs/accessibility/16-contrast-audit.md
      to: app/globals.css (token values for measurement)
      via: "measured composite values for --teal on rgba(45,122,140,0.10) and --gd-text on rgba(18,150,87,0.10)"
      pattern: "teal|gd-text"
---

<objective>
Closing-out verification for Phase 17:
- **THEME-01:** Every partner-side surface delivered by Plans 03-07 ships both light and dark variants matching Figma `82:*` duplicate frames. Verified by manual checkpoint (no per-component dark CSS exists — all surfaces inherit from the token cascade established in Phase 16; this checkpoint confirms the inheritance works end-to-end without layout shift).
- **CONTRAST-02 addendum:** Append rows 8-11 to `docs/accessibility/16-contrast-audit.md` documenting the new Phase 17 token pairs (Tranche/Coefficient chip teal on tint + Active filter pill gd-text on tint, both light + dark). Row 11 has an accept-with-deviation note since dark-mode ~3.9:1 matches the existing `.chip-active` baseline (Phase 8) and no regression is introduced.
- **WIZ-05 final check:** Full test run including `tests/admin-09-grep-contracts.test.ts` to confirm the 9-gate suite stays green across all 5 surfaces.

Purpose: ROADMAP §Phase 17 success criterion 5 — "Every partner-side screen ships light and dark variants matching Figma `82:*` duplicate frames; ADMIN-09 D-12 envelope and the 9-gate grep-contract suite remain green throughout." This plan is the formal sign-off.
Output: 1 modified doc (contrast audit appendix) + 1 human-verify checkpoint + 1 automated full-test-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/17-partner-surfaces/17-UI-SPEC.md
@.planning/phases/17-03-SUMMARY.md
@.planning/phases/17-04-SUMMARY.md
@.planning/phases/17-05-SUMMARY.md
@.planning/phases/17-06-SUMMARY.md
@.planning/phases/17-07-SUMMARY.md

<interfaces>
<!-- From docs/accessibility/16-contrast-audit.md (Phase 16 signed off; Phase 17 appends rows 8-11) -->
- Existing table format with columns: # | Composite | FG hex | BG (blended) | Mode | Ratio | Pass?
- Phase 16 rows: 1-7 (diff panel, gold chip, gold-text variants)
- Phase 17 appends: 4 new rows per UI-SPEC §Contrast Audit Phase 17 Addendum

<!-- From app/globals.css (palette stable per ROADMAP §v1.3 §3) -->
- --teal: #2d7a8c (both light and dark)
- --gd-text: #0e7544 (light), #129657 (dark)
- --surface: #ffffff (light), #161e2d (dark)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append CONTRAST-02 rows 8-11 to 16-contrast-audit.md</name>
  <files>docs/accessibility/16-contrast-audit.md</files>
  <read_first>
    - docs/accessibility/16-contrast-audit.md (the existing Phase 16 audit doc — verify current row count, table format, sign-off section)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (§Contrast Audit — CONTRAST-02 Addendum — pre-computed values for rows 8/9/10/11)
    - app/globals.css (verify current --teal, --gd-text, --surface values match the audit assumptions)
  </read_first>
  <action>
    Per UI-SPEC §Contrast Audit Phase 17 Addendum (CONTRAST-02), append 4 rows to the contrast measurement table in `docs/accessibility/16-contrast-audit.md`:

    | # | Composite | FG hex | BG (blended) | Mode | Ratio | Pass? |
    |---|-----------|--------|--------------|------|-------|-------|
    | 8 | Tranche/Coefficient pill chip — `--teal` text on tint | `#2d7a8c` | `rgba(45,122,140,0.10)` over `--surface` `#ffffff` → ~`#eaf3f5` | Light | ~4.8:1 (estimated) | PASS |
    | 9 | Tranche/Coefficient pill chip — `--teal` text on tint | `#2d7a8c` | `rgba(45,122,140,0.10)` over `--surface` dark `#161e2d` → ~`#1a282c` | Dark | ~9.2:1 (estimated) | PASS |
    | 10 | Active filter pill — `--gd-text` text on tint | `#0e7544` | `rgba(18,150,87,0.10)` over `--surface` `#ffffff` → `#e7f4ec` | Light | 4.96:1 (calculated per UI-SPEC) | PASS |
    | 11 | Active filter pill — `--gd-text` dark = `#129657` on tint | `#129657` | `rgba(18,150,87,0.10)` over `--surface` dark `#161e2d` → ~`#162a31` | Dark | ~3.9:1 (calculated per UI-SPEC) | ACCEPT-WITH-DEVIATION |

    Row 11 deviation note: append immediately below the table:
    > **Row 11 deviation:** Dark-mode active filter pill at ~3.9:1 is below the WCAG 2.1 AA 4.5:1 threshold. This matches the existing `.chip-active` baseline (Phase 8 shipped, same token pair, same opacity) — Phase 17 introduces no regression. Per Phase 17 Plan 04 decision: Option 1 (accept-with-deviation, no token-level change) preserves the v1.3 palette stability invariant (ROADMAP §v1.3 §3). If a future audit requires AA compliance on this composite, a follow-up plan can introduce `--active-pill` at higher dark-mode opacity per UI-SPEC §CONTRAST-02 Addendum Option 2.

    Sign-off section update: add a Phase 17 attribution line at the bottom of the file referencing the Antoine sign-off captured by Task 2 (the human-verify checkpoint).

    Use `Edit` tool (not Write — file pre-exists). Preserve all existing rows 1-7 and all Phase 16 sign-off content.
  </action>
  <verify>
    <automated>grep -c "Tranche/Coefficient pill chip\|Active filter pill" docs/accessibility/16-contrast-audit.md | { read count; [ "$count" -ge 4 ]; }</automated>
  </verify>
  <done>
    - `grep -c "Tranche/Coefficient pill chip" docs/accessibility/16-contrast-audit.md` returns 2 (rows 8 + 9)
    - `grep -c "Active filter pill" docs/accessibility/16-contrast-audit.md` returns 2 (rows 10 + 11)
    - `grep -c "ACCEPT-WITH-DEVIATION\|accept-with-deviation" docs/accessibility/16-contrast-audit.md` returns >= 1 (row 11 deviation note present)
    - All existing Phase 16 rows 1-7 preserved (verify via diff inspection)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Antoine signs off light + dark visual verification across all 5 partner surfaces (THEME-01)</name>
  <what-built>
    Plans 03-07 delivered the 5 Phase 17 partner-side surfaces:
    - Partner Home `/` (Plan 03): PageHero + 3 MetricTiles + Propositions récentes
    - `/proposals` + `/proposals?archived=1` (Plan 04): PageHero + FilterPillRow + SearchBar + ProposalsList with empty-state switching
    - Wizard step 1 `/proposals/new/parametres` (Plan 05): PageHero adoption + Stepper sibling
    - Wizard step 2 `/proposals/new/calcul` (Plan 06): PageHero + loyer hero card + Tranche/Coefficient chip + Détail card + Paramètres saisis recap
    - Wizard step 3 `/proposals/new/verification` (Plan 07): PageHero + 2-column 1040px layout + validity selector inside CALCUL recap + PdfPreviewMock with real lcRef

    All surfaces inherit the dark mode via the `html[data-theme="dark"]` token cascade established in Phase 16 — no per-component dark CSS exists. This checkpoint confirms the inheritance works end-to-end without layout shift, broken borders, or unreadable text.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (from project root). Default port 3000.
    2. Log in as a test partner (use credentials from `scripts/seed-admins-launch.ts` setup or any existing partner account).
    3. For EACH of the 5 surfaces below, visit the URL TWICE — once with the OS in light mode, once with dark mode (or use the sidebar tri-state theme toggle: System → Light → Dark).
       - `/` (Partner Home) — verify against Figma frame `9:47` (light) and `82:1088` (dark)
       - `/proposals` (Actives default) — verify against Figma `9:47` page chrome (no dedicated frame; use Partner Home dark for chrome)
       - `/proposals?archived=1` (Archivées toggle) — verify the Archivées pill goes active, list switches to archived rows or empty-state copy renders
       - `/proposals/new/parametres` — verify against Figma `35:46` (light) and `82:171` (dark)
       - `/proposals/new/calcul?draft_id={any-draft}` — verify against Figma `39:46` (light) and `82:317` (dark). Create a draft via step 1 if needed.
       - `/proposals/new/verification?draft_id={any-draft}` — verify against Figma `40:46` (light) and `82:460` (dark). Verify the real `Réf. LC-2026-{nnn}` shows in PdfPreviewMock (NOT literal LC-2026-XXX). Verify the validity selector defaults to the current draft's value and clicking another value updates without redirect.
    4. For EACH surface, check:
       - [ ] Hero (PageHero) renders: eyebrow (where applicable), title, subtitle, optional CTA — no layout shift
       - [ ] Tokens applied: backgrounds match `--paper`/`--surface`; text uses `--ink`/`--muted`; brand colors match `--gd`/`--teal`/`--gold` per surface
       - [ ] Dark mode: NO unreadable text (white on white, dark on dark); NO broken borders or invisible chips
       - [ ] No console errors in browser DevTools
    5. SPECIFIC visual gates per surface:
       - Partner Home: 3 MetricTiles in a 3-up grid with correct icons + values; Propositions récentes card has up to 5 rows + Voir toutes link
       - /proposals: FilterPillRow with Actives + Archivées pills, active pill clearly distinct from inactive
       - Step 2: loyer value is large (36px) and teal-colored; Tranche/Coefficient chip top-right; Détail du calcul shows 5 rows with the commission row visible (Commission apporteur (non visible client)) to the deal-owner partner
       - Step 3: 2-column layout; validity selector renders 3 buttons (15j/30j/60j) with one active; PdfPreviewMock shows real LC reference (not XXX placeholder)
    6. If ANY surface fails, capture screenshot(s), note the failure mode, and run `/gsd-execute-phase 17 --gaps` to generate a gap-closure plan. Do NOT approve until all surfaces pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 surfaces × 2 modes render correctly per the checklist. If any surface fails, describe the failure mode (which surface, which mode, what went wrong) and the planner will trigger gap-closure mode.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full test suite gate — confirm ADMIN-09 9-gate stays green + no regression (WIZ-05 final check)</name>
  <files></files>
  <read_first>
    - tests/admin-09-grep-contracts.test.ts (verify the 9 gates expected to remain green; understand what substrings each gate flags)
    - package.json (verify the test scripts — typically `npm test` runs the full Vitest suite)
  </read_first>
  <action>
    Run the full test suite to confirm Phase 17 introduces no regression. Specifically:

    1. Run `npm test` (or the project's full-test command per package.json) and capture the output.
    2. Confirm `tests/admin-09-grep-contracts.test.ts` passes all 9 gates — the suite gates on commission_pct and Commission substrings across PDF, audit_log, server logs, and pre-finalize traces.
    3. Confirm all Phase 17 plan tests pass:
       - `src/lib/db/queries/proposals.test.ts` (Plan 01 createDraft + finalizeDraft changes)
       - `src/lib/db/queries/proposal-aggregates.test.ts` (Plan 03 aggregate counts)
       - `src/lib/api/proposals/list.test.ts` (Plan 01 archived filter)
       - `src/components/proposals/PdfPreviewMock.tsx` colocated tests (Plan 02 lcRef prop)
       - `app/(authed)/page.test.tsx` (Plan 03 Partner Home)
       - `app/(authed)/proposals/page.test.tsx` (Plan 04 /proposals route)
       - `app/(authed)/proposals/_components/FilterPillRow.test.tsx` (Plan 04)
       - `app/(authed)/proposals/new/_components/ValiditySelectorClient.test.tsx` (Plan 07)
       - Existing wizard step 1/2/3 tests (Plans 05-07 — must remain green)
    4. Confirm the i18n parity proof (`_EnHasAllFrKeys` at dictionaries.ts:1353-1357) compiles cleanly via `npx tsc --noEmit`.
    5. If any test fails, report which file + which test + the failure message; do NOT proceed to git commit until all green.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `npm test` exits 0 (full suite green)
    - `npx tsc --noEmit` exits 0 (TS + parity proof green)
    - `tests/admin-09-grep-contracts.test.ts` 9 gates green (verify via test output)
    - No legacy literal `LC-2026-XXX` substring remains in app/ or src/ source (`grep -r "LC-2026-XXX" app/ src/ | grep -v test | grep -v dictionaries.ts` returns 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → CI/local | mechanical gate; no untrusted input |
| Documentation → reviewer | static markdown; human reads and signs off |
| Browser visual verification → human | human-in-the-loop confirms token cascade renders correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08-01 | Information disclosure (commission leak across phase) | full Phase 17 surface set | mitigate | Task 3 runs the 9-gate ADMIN-09 suite which gates on commission substrings across PDF / audit_log / server logs / pre-finalize traces; failure blocks merge |
| T-17-08-02 | Information disclosure (contrast audit deviation) | row 11 dark-mode active filter pill | accept | Documented in Task 1 with rationale: matches existing `.chip-active` baseline, no regression, palette stability invariant takes precedence over a token-level mitigation in v1.3; future v1.4+ can introduce `--active-pill` per UI-SPEC Option 2 |
| T-17-08-03 | Repudiation (visual sign-off without record) | Antoine human-verify | mitigate | Task 1 appends a Phase 17 attribution line to the contrast audit doc referencing the sign-off; SUMMARY.md created at end of plan records the date + the approved/rejected decision per the resume-signal |
</threat_model>

<verification>
- `npm test && npx tsc --noEmit` exits 0
- Antoine's "approved" signal received per Task 2 checkpoint (or gap-closure mode triggered if rejected)
- `docs/accessibility/16-contrast-audit.md` contains 4 new rows + row 11 deviation note
- `grep -c "Phase 17" docs/accessibility/16-contrast-audit.md` returns >= 1 (attribution line)
</verification>

<success_criteria>
- All 5 Phase 17 partner-side surfaces verified light + dark by Antoine (THEME-01)
- CONTRAST-02 addendum committed with 4 new rows + row 11 deviation rationale
- ADMIN-09 9-gate suite remains green across the phase (WIZ-05)
- Full test suite + TypeScript compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-partner-surfaces/17-08-SUMMARY.md` capturing:
- Antoine's sign-off date + outcome
- Any deviations or follow-ups noted during visual verification
- Final test counts (before/after Phase 17)
- Phase 17 closure summary referencing the 12 requirements satisfied
</output>
