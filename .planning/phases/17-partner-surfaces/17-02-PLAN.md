---
phase: 17-partner-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/dictionaries.ts
  - src/components/proposals/PdfPreviewMock.tsx
  - app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx
autonomous: true
requirements: [WIZ-06, PHOME-01, PHOME-03, PROPS-01, PROPS-02, WIZ-01, WIZ-02, WIZ-03]
must_haves:
  truths:
    - "All ~30 Phase 17 dictionary keys (FR + EN) per UI-SPEC Copywriting Contract are present and resolve via t(key, lang)"
    - "PdfPreviewMock accepts a lcRef: string prop and renders it inline (no longer reads wizard.step3.pdf.ref.line)"
    - "Compile-time _EnHasAllFrKeys parity proof remains satisfied"
  artifacts:
    - path: src/lib/i18n/dictionaries.ts
      provides: "All Phase 17 i18n keys (dashboard.*, proposals.*, wizard.step1/2/3.*, proposal.validity.*) in FR + EN blocks"
      contains: "dashboard.metricTile.thisMonth"
    - path: src/components/proposals/PdfPreviewMock.tsx
      provides: "lcRef prop on PdfPreviewMockProps; inline ref-line construction"
      contains: "lcRef:"
  key_links:
    - from: src/components/proposals/PdfPreviewMock.tsx
      to: caller (Wizard step 3 verification page)
      via: "new required prop lcRef threaded from draft.lcRef"
      pattern: "lcRef"
    - from: src/lib/i18n/dictionaries.ts
      to: type _EnHasAllFrKeys
      via: "compile-time parity proof catches missing EN counterparts"
      pattern: "_EnHasAllFrKeys"
---

<objective>
Add the ~30 net-new i18n keys per UI-SPEC Copywriting Contract (D-21) and update PdfPreviewMock to take a real `lcRef: string` prop (D-17). This unblocks every downstream Wave-2 plan (Partner Home, /proposals, all 3 wizard steps) — they call `t(...)` against these keys. PdfPreviewMock changes its prop API independently of where lcRef comes from (Plan 07 wires the real value).

Purpose: All consumer screens (Plans 03-07) need both the dictionary entries and the updated PdfPreviewMock signature. Centralizing these in Wave 1 means Wave 2 can run in parallel without contention.
Output: Updated dictionaries.ts (FR + EN parity), updated PdfPreviewMock.tsx with new prop, updated PdfPreviewMock.test.tsx with prop-passing fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/17-partner-surfaces/17-UI-SPEC.md
@.planning/phases/17-partner-surfaces/17-PATTERNS.md

<interfaces>
<!-- From src/lib/i18n/dictionaries.ts (existing) -->
- `export type Lang = 'fr' | 'en'`
- `export type DictKey = keyof typeof dictionaries.fr`
- `export function t(key: DictKey, lang: Lang): string` — returns FR fallback when EN key missing
- Compile-time guard at lines 1353-1357: `type _EnHasAllFrKeys = { [K in DictKey]: K extends keyof typeof dictionaries.en ? true : never }` — fails compile on drift
- FR block: lines 19-1086 (dashboard.* around line 296, wizard.step3.* around line 574)
- EN block: lines 1089-1340 (mirror structure)

<!-- From src/components/proposals/PdfPreviewMock.tsx (existing line 24-49) -->
Current props: `{ loyerDisplay: string; validityDays: 15 | 30 | 60; lang: Lang }`
Current ref line at line ~46: `t('wizard.step3.pdf.ref.line', lang).replace('{0}', String(validityDays))`
Phase 17 D-17 change: add `lcRef: string` to props; replace dictionary interpolation with inline construction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 17 i18n keys to FR + EN dictionaries (D-21)</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts (full file — verify which keys from UI-SPEC Copywriting Contract already exist; do NOT duplicate)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (Copywriting Contract section — full key/copy table with FR + EN values; ~30 keys total)
    - .planning/phases/17-partner-surfaces/17-PATTERNS.md (`src/lib/i18n/dictionaries.ts` section — existing keys to verify against, parity proof lines 1353-1357)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-21 — key inventory estimate ~25 keys, planner adds slightly more per UI-SPEC contract)
  </read_first>
  <action>
    Add ALL net-new keys listed in 17-UI-SPEC.md `## Copywriting Contract` table (rows marked **NEW**) to BOTH the FR block (around line 296 for `dashboard.*`, around line 574 for `wizard.*`, around line 700 for `proposals.*`) AND the EN block (mirror structure around line 964, 1226, etc.). The exact key inventory:

    `dashboard.home.subtitle`, `dashboard.cta.new` (verify against existing `dashboard.cta.new.proposal` at line ~298 — REUSE the existing key if present, do NOT create a duplicate; if executor renames, update Plan 03 references too), `dashboard.metricTile.thisMonth`, `dashboard.metricTile.total`, `dashboard.metricTile.drafts`, `dashboard.recent.title` (verify line ~299 — REUSE if present), `dashboard.recent.empty`, `dashboard.recent.viewAll`,
    `proposals.title`, `proposals.subtitle`, `proposals.filter.actives`, `proposals.filter.archived`, `proposals.empty.actives`, `proposals.empty.archived`,
    `wizard.step1.eyebrow`, `wizard.step1.title` (verify existing — REUSE), `wizard.step1.subtitle`,
    `wizard.step2.eyebrow`, `wizard.step2.title`, `wizard.step2.subtitle`, `wizard.step2.hero.sublabel`, `wizard.step2.detail.title`, `wizard.step2.detail.montantHT`, `wizard.step2.detail.commission`, `wizard.step2.detail.commissionNote`, `wizard.step2.detail.coefficient`, `wizard.step2.detail.duree`, `wizard.step2.detail.loyer`, `wizard.step2.recap.title`,
    `wizard.step3.eyebrow`, `wizard.step3.title` (verify — REUSE), `wizard.step3.subtitle` (verify — REUSE),
    `proposal.validity.label`, `proposal.validity.ariaLabel`, `proposal.validity.days15`, `proposal.validity.days30`, `proposal.validity.days60`

    For EACH key, use the FR / EN copy from the UI-SPEC Copywriting Contract table verbatim. Do NOT improvise wording.

    Verify-and-reuse policy: before adding ANY key, search the existing file for the key name; if found, leave it untouched and document the reuse in the SUMMARY (do NOT duplicate, which is a compile-time error). Per UI-SPEC: `dashboard.greeting`, `dashboard.cta.new.proposal`, `dashboard.recent.title`, `dashboard.empty.title/body`, `wizard.step1.title`, `wizard.step3.title`, `wizard.step3.subtitle`, `wizard.step3.pdf.title`, `wizard.step3.pdf.loyer.label` are candidates for reuse.

    The key `wizard.step3.pdf.ref.line` at line ~612 may stay in the dictionary (orphaned post-Plan 07) — do NOT delete in this plan; removal is a Plan 07 follow-up decision.

    The compile-time `_EnHasAllFrKeys` proof at lines 1353-1357 catches any FR key without an EN counterpart — if `npm run build` or `npx tsc --noEmit` fails on this proof, fix by adding the missing EN entry.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "_EnHasAllFrKeys\|_EnParityProof" | { read count; [ "$count" = "0" ]; }</automated>
  </verify>
  <done>
    - `grep -c "dashboard.metricTile.thisMonth" src/lib/i18n/dictionaries.ts` returns >= 2 (FR + EN occurrences)
    - `grep -c "proposals.filter.archived" src/lib/i18n/dictionaries.ts` returns >= 2
    - `grep -c "wizard.step2.detail.commission" src/lib/i18n/dictionaries.ts` returns >= 2
    - `grep -c "proposal.validity.label" src/lib/i18n/dictionaries.ts` returns >= 2
    - `npx tsc --noEmit` exits 0 (parity proof satisfied)
    - All UI-SPEC Copywriting Contract NEW keys present in both FR + EN blocks
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lcRef prop to PdfPreviewMock and inline ref-line construction (D-17)</name>
  <files>src/components/proposals/PdfPreviewMock.tsx, app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx</files>
  <read_first>
    - src/components/proposals/PdfPreviewMock.tsx (full file 1-95 — current PdfPreviewMockProps interface at line 24, ref line render at line ~46-49)
    - app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx (existing test fixtures — every render call needs the new prop)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (`<PdfPreviewMock>` MODIFIED section + Step 3 right column)
    - .planning/phases/17-partner-surfaces/17-PATTERNS.md (`src/components/proposals/PdfPreviewMock.tsx MODIFY` section — exact before/after code excerpts)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-03, D-17 — ref line format `Réf. {lcRef} · {validityDays} jours de validité`)
    - tests/admin-09-grep-contracts.test.ts (verify lcRef is not flagged as a commission substring — should be clean by construction)
  </read_first>
  <behavior>
    - Test 1: render PdfPreviewMock with lcRef="LC-2026-042", validityDays=30, lang="fr" → DOM contains "Réf. LC-2026-042" AND "30 jours de validité"
    - Test 2: render with lcRef="LC-2026-007", validityDays=15, lang="en" → DOM contains "Ref. LC-2026-007" AND "15 days validity"
    - Test 3: PdfPreviewMockProps interface contains lcRef: string (TypeScript compile-time check; ensures all callers must provide it)
    - Test 4: rendered output contains NO substring matching `LC-2026-XXX` literal placeholder (no leftover from Phase 13)
    - Test 5: rendered output contains NO commission-related substring (`commission_pct`, `Commission` as a value not as the existing draft label)
  </behavior>
  <action>
    Per 17-PATTERNS.md `src/components/proposals/PdfPreviewMock.tsx MODIFY` section + UI-SPEC `<PdfPreviewMock> MODIFIED`:

    1. Update `PdfPreviewMockProps` interface (line ~24-28): add `lcRef: string` as a REQUIRED prop. Place it between `validityDays` and `lang` for parameter ordering consistency with the JSX usage in step 3.

    2. Update the function destructure (line ~30): add `lcRef` to the destructured args.

    3. Replace the existing `refLine` construction (line ~46-49). Current line reads `t('wizard.step3.pdf.ref.line', lang).replace('{0}', String(validityDays))`. After:
       - When `lang === 'fr'`: `Réf. ${lcRef} · ${validityDays} jours de validité`
       - When `lang === 'en'`: `Ref. ${lcRef} · ${validityDays} days validity`
       - Inline construction is acceptable per UI-SPEC; do NOT introduce a new i18n key (the inline construction matches the Copywriting Contract). The retired key `wizard.step3.pdf.ref.line` stays in the dictionary but is no longer read by this component.

    4. ADMIN-09 invariant: lcRef format is `LC-2026-NNN` — contains no commission substring. The 9-gate suite at `tests/admin-09-grep-contracts.test.ts` gates on `commission_pct` and `Commission` (when adjacent to numeric values); the lcRef change passes trivially. Do NOT add a commission row to PdfPreviewMock.

    5. Update `app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx`: every existing `render(<PdfPreviewMock ... />)` call needs the new `lcRef` prop. Add the 5 behavior tests above. Use `lcRef="LC-2026-001"` as the default fixture.
  </action>
  <verify>
    <automated>npm test -- app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx</automated>
  </verify>
  <done>
    - src/components/proposals/PdfPreviewMock.tsx contains `lcRef: string` in PdfPreviewMockProps interface
    - `grep -c "lcRef" src/components/proposals/PdfPreviewMock.tsx` returns >= 3 (interface + destructure + usage)
    - `grep -c "wizard.step3.pdf.ref.line" src/components/proposals/PdfPreviewMock.tsx` returns 0 (key no longer read by this component)
    - All 5 behavior tests pass; existing PdfPreviewMock tests retargeted with lcRef fixture and remain green
    - `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dictionary → t(key, lang) | static lookup, no untrusted input; new keys do not expand attack surface |
| PdfPreviewMock → server component caller | lcRef prop required at type level (TS enforces); cannot be undefined at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information disclosure (commission via i18n) | new dictionary entries | mitigate | Audit every NEW key copy for commission substrings before commit; `wizard.step2.detail.commission` and `wizard.step2.detail.commissionNote` are label-only (per Phase 13 D-12 partner-facing relaxation contract); no commission_pct or commission value appears in copy strings |
| T-17-02-02 | Information disclosure (PDF preview leaks) | PdfPreviewMock lcRef prop | accept | lcRef is partner-visible by design (it is the canonical proposal identifier); displayed on step 3 to the deal-owner partner only; no cross-partner exposure (the wizard route is scoped to the partner's draft) |
| T-17-02-03 | Tampering (i18n key drift) | _EnHasAllFrKeys compile-time proof | mitigate | Existing parity proof at lines 1353-1357 fails compile on FR/EN drift; Task 1 verify step asserts `npx tsc --noEmit` returns 0 |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (parity proof satisfied; PdfPreviewMockProps interface tight)
- `npm test -- src/components/proposals/PdfPreviewMock.tsx app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (no commission leakage from new i18n copy)
- Audit: `grep -n "Commission" src/lib/i18n/dictionaries.ts` shows ONLY label keys (`wizard.step2.detail.commission` value `"Commission apporteur"` + `commissionNote` value `"(non visible client)"`); no commission_pct or numeric commission values in dictionary
</verification>

<success_criteria>
- All ~30 NEW i18n keys per UI-SPEC Copywriting Contract present in both FR and EN blocks; parity proof green
- PdfPreviewMock has new lcRef required prop; inline ref-line construction (FR / EN)
- All existing PdfPreviewMock test render calls updated with lcRef fixture; new behavior tests pass
- ADMIN-09 9-gate suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/17-partner-surfaces/17-02-SUMMARY.md`
</output>
