---
phase: 17-partner-surfaces
plan: 03
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - app/(authed)/page.tsx
  - app/(authed)/page.test.tsx
  - src/lib/db/queries/proposal-aggregates.ts
  - src/lib/db/queries/proposal-aggregates.test.ts
autonomous: true
requirements: [PHOME-01, PHOME-02, PHOME-03]
must_haves:
  truths:
    - "Partner Home renders <PageHero> with greeting + Nouvelle proposition CTA"
    - "3 MetricTile values render scoped to the partner: Ce mois-ci (Europe/Paris month), Total, Brouillons"
    - "Propositions récentes card shows up to 5 most-recent proposals with StatusChip; empty state copy renders when 0"
    - "Voir toutes → link routes to /proposals (no params)"
    - "RecentlyDeletedToggle and full ProposalsList are no longer mounted on Partner Home"
  artifacts:
    - path: app/(authed)/page.tsx
      provides: "Rebuilt Partner Home: PageHero + 3 MetricTiles + Propositions récentes preview card"
      contains: "PageHero"
    - path: src/lib/db/queries/proposal-aggregates.ts
      provides: "Server-side aggregate counts scoped to userId: countThisMonth, countTotal, countDrafts"
      contains: "countThisMonth"
  key_links:
    - from: app/(authed)/page.tsx
      to: src/lib/db/queries/proposal-aggregates.ts
      via: "synchronous SSR aggregate call before render"
      pattern: "countThisMonth|countTotal|countDrafts"
    - from: app/(authed)/page.tsx
      to: /proposals (Voir toutes link) + /proposals/new/parametres (Nouvelle proposition CTA)
      via: "Next.js <Link href>"
      pattern: "href=\"/proposals"
---

<objective>
Rewrite Partner Home `/` to the Figma `9:47` design (PHOME-01/02/03 + D-05..D-09 + D-19): replace the v1.1 inline hero + full ProposalsList + RecentlyDeletedToggle with `<PageHero>` + 3 `<MetricTile>` grid + Propositions récentes preview card (5 abbreviated rows + Voir toutes link). The full list moves to /proposals (Plan 04). Aggregate counts are computed server-side per D-05 inclusion rules + D-06 Europe/Paris timezone math.

Purpose: PHOME-01..03 partner home rebuild; eliminates the v1.1 list/toggle so /proposals can own that surface (clean separation).
Output: Rewritten page.tsx + new proposal-aggregates.ts query helper + Vitest coverage. RecentlyDeletedToggle file stays on disk per <deferred> in 17-CONTEXT (file removal is out of scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/17-partner-surfaces/17-PATTERNS.md
@.planning/phases/17-partner-surfaces/17-UI-SPEC.md
@.planning/phases/17-02-SUMMARY.md

<interfaces>
<!-- From src/components/ui/PageHero.tsx (Phase 16 — reuse verbatim) -->
export interface PageHeroProps {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  actions?: ReactNode;
  children?: ReactNode;  // reserved
}
// Renders with marginBottom: 32 baked in (D-04 from Phase 16)

<!-- From src/components/ui/MetricTile.tsx (Phase 11 — reuse verbatim) -->
export interface MetricTileProps {
  variant: 'month' | 'total' | 'drafts' | 'navy' | 'ink';
  label: string;
  value: string | number;
  sublabel?: string;
}

<!-- From src/components/ui/StatusChip.tsx (Phase 11/14 — reuse verbatim) -->
5 variants: 'active' | 'draft' | 'expired' | 'deleted' | 'invited'

<!-- From src/lib/api/proposals/list.ts (used for the 5-row preview) -->
buildListResponse({ userId, limit: 5 }) returns ListResponse with rows[] including row.displayStatus

<!-- From src/lib/db/queries/proposals.ts -->
deriveDisplayStatus(row): 'active' | 'draft' | 'expired' | 'deleted'

<!-- Current Partner Home consumers retiring on Partner Home (NO file deletion in this plan) -->
- src/components/proposals/RecentlyDeletedToggle.tsx — file stays, mount removed
- src/components/proposals/ProposalsList.tsx — file stays, mount removed (moves to /proposals via Plan 04)
- src/components/proposals/SearchBar.tsx — file stays, mount removed (moves to /proposals via Plan 04)

<!-- Carry-forward: <DeleteJustToast> stays mounted -->
- src/components/proposals/DeleteJustToast.tsx — still fires on ?deleted_just=1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create proposal-aggregates.ts with 3 userId-scoped count queries (D-05, D-06)</name>
  <files>src/lib/db/queries/proposal-aggregates.ts, src/lib/db/queries/proposal-aggregates.test.ts</files>
  <read_first>
    - src/lib/db/queries/proposals.ts (listProposalsByUser at line 152-197 — copy the userId scope + isNull(deletedAt) + inArray(status) shape using `count()` instead of `select()`)
    - src/lib/db/queries/proposals.ts (deriveDisplayStatus at line 551-568 — for understanding the expired-derivation contract; NOT used in this plan's queries since D-05 includes 'expired' via the status set + the timezone branch handles "current month")
    - src/db/schema.ts (proposals table — confirm columns: user_id, status, deleted_at, created_at)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-05 inclusion rules; D-06 Europe/Paris timezone math approach: app-side compute monthStartUtc, pass as gte filter)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (MetricTile instances table — variant + label key + value source mapping)
  </read_first>
  <behavior>
    - Test 1: countThisMonth(userId) returns COUNT of proposals WHERE user_id=$userId AND status IN ('active','draft','expired') AND deleted_at IS NULL AND created_at >= monthStartUtc (Europe/Paris)
    - Test 2: countTotal(userId) returns COUNT of proposals WHERE user_id=$userId AND status IN ('active','draft','expired') AND deleted_at IS NULL (no created_at filter)
    - Test 3: countDrafts(userId) returns COUNT of proposals WHERE user_id=$userId AND status='draft' AND deleted_at IS NULL
    - Test 4: soft-deleted proposals (deleted_at IS NOT NULL) are EXCLUDED from all 3 counts
    - Test 5: cross-user IDOR — count helpers scope strictly by userId; userA cannot see userB's counts (negative test)
    - Test 6: DST transition — when monthStartUtc is computed across a DST boundary (use a fixed Date.now() mock for March/October 2026), counts match expected rows
  </behavior>
  <action>
    Create `src/lib/db/queries/proposal-aggregates.ts` per D-05 + D-06 (17-CONTEXT decisions block).

    Export three functions:
    - `countThisMonth(userId: string): Promise<number>`
    - `countTotal(userId: string): Promise<number>`
    - `countDrafts(userId: string): Promise<number>`

    Use the Drizzle query pattern from `listProposalsByUser` (`src/lib/db/queries/proposals.ts:152-197`): same `eq(schema.proposals.userId, ...)`, `isNull(schema.proposals.deletedAt)`, `inArray(schema.proposals.status, ...)` composition — but with `db().select({ count: count() }).from(...)` instead of selecting rows. Use `count` from `drizzle-orm` or sql\`count(*)\` per the project's existing convention (verify against any prior aggregate in the codebase if present).

    For `countThisMonth`, compute `monthStartUtc` app-side per D-06:
    - Get current time `now = new Date()`
    - Format in `Europe/Paris` timezone using `Intl.DateTimeFormat('fr-FR', { timeZone: 'Europe/Paris', year: 'numeric', month: 'numeric' })`
    - Take year + month, construct the first-day-of-month at 00:00 Europe/Paris
    - Convert back to UTC Date (use the Zoned-Time approach: build a string `YYYY-MM-01T00:00:00` and interpret it via the well-known Date constructor with timezone-offset arithmetic — OR install no new dependencies; if a date-fns helper is already in package.json, use it, otherwise hand-roll)
    - Pass `gte(schema.proposals.createdAt, monthStartUtc)` as the additional filter
    - Add a top-of-file comment citing D-06 and explaining the timezone approach + DST survival

    D-05 inclusion rule status set: `inArray(schema.proposals.status, ['active', 'draft'])` — note that `'expired'` is NOT a stored status (`deriveDisplayStatus` derives it from `pdfGeneratedAt + validityDays < now()`); per D-05 the count is inclusive but the SQL must filter by stored status. For the SSR aggregate, an expired proposal still has `status='active'` in DB. Accept this as the simplest mapping; the count includes active+draft and EXCLUDES deleted — expired rows naturally roll into the active count.

    CRITICAL SECURITY: every query MUST include `eq(schema.proposals.userId, userId)`. Test 5 enforces this.

    Tests file `proposal-aggregates.test.ts` colocates with the helper. Mock the database via the project's existing test utilities (check `src/lib/db/queries/proposals.test.ts` for the established pattern — likely vi.mock of `@/lib/db`).
  </action>
  <verify>
    <automated>npm test -- src/lib/db/queries/proposal-aggregates.test.ts</automated>
  </verify>
  <done>
    - src/lib/db/queries/proposal-aggregates.ts exports countThisMonth, countTotal, countDrafts
    - All 6 behavior tests pass
    - `grep -n "userId\b" src/lib/db/queries/proposal-aggregates.ts` shows every query scopes by userId (cross-user IDOR mitigated)
    - File has top-of-file comment citing D-05 + D-06 rationale
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Partner Home page.tsx (PHOME-01/02/03)</name>
  <files>app/(authed)/page.tsx, app/(authed)/page.test.tsx</files>
  <read_first>
    - app/(authed)/page.tsx (current file — preserve `requireUser()` + `displayName` chain + `<DeleteJustToast>` mount; remove RecentlyDeletedToggle + full ProposalsList + SearchBar mounts)
    - app/(admin)/[adminSegment]/page.tsx (the Phase 16 PageHero adopter — copy the import block + `dynamic` declaration + PageHero call pattern from lines 42-47)
    - src/components/ui/PageHero.tsx (Phase 16 primitive — props interface verbatim)
    - src/components/ui/MetricTile.tsx (Phase 11 primitive — props interface, variants)
    - src/components/ui/StatusChip.tsx (Phase 11/14 — 5 variants for the recent-rows preview)
    - src/lib/db/queries/proposal-aggregates.ts (Task 1 output — three aggregate helpers)
    - src/lib/api/proposals/list.ts (buildListResponse — for the 5-row recent preview; pass limit: 5)
    - src/components/proposals/ProposalRow.tsx (for the abbreviated row layout — copy StatusChip + client name + lcRef + loyer rendering pattern; do NOT mount the full component, build a slimmer inline render to match UI-SPEC §Propositions récentes card)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (`Partner Home /` section — layout top-to-bottom, MetricTile instances table, Propositions récentes card spec)
    - .planning/phases/17-partner-surfaces/17-PATTERNS.md (`app/(authed)/page.tsx REWRITE` section — full code excerpts: imports, top-of-file boilerplate, auth+displayName chain, PageHero call, MetricTile 3-up grid, Propositions récentes card pattern)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-08 default Voir toutes link target = /proposals no params; D-09 row content shape; D-19 PageHero + Stepper composition rule)
  </read_first>
  <behavior>
    - Test 1: page renders <PageHero> with title containing displayName (mocked session.user.displayName = "Jane") and the Nouvelle proposition CTA <Link href="/proposals/new/parametres">
    - Test 2: page renders 3 MetricTile elements with the values from countThisMonth / countTotal / countDrafts (mocked aggregates)
    - Test 3: when partner has >=5 proposals, page renders exactly 5 abbreviated row Links to /proposals/{id}
    - Test 4: when partner has 0 proposals, empty-state copy renders inside the Propositions récentes card with i18n key `dashboard.recent.empty`
    - Test 5: Voir toutes link href is exactly `/proposals` (no params — per D-08)
    - Test 6: RecentlyDeletedToggle and SearchBar are NOT mounted on this page (grep import + JSX absence)
    - Test 7: DeleteJustToast IS still mounted (carry-forward — fires on ?deleted_just=1)
    - Test 8: requireUser is called BEFORE buildListResponse and the aggregates (defense-in-depth ordering)
  </behavior>
  <action>
    Rewrite `app/(authed)/page.tsx` to the layout in 17-UI-SPEC.md `## Partner Home /` section + 17-PATTERNS.md `### app/(authed)/page.tsx REWRITE` section.

    1. Imports (per 17-PATTERNS.md imports pattern):
       - Add: `Link from 'next/link'`, `Plus from 'lucide-react'`, `PageHero from '@/components/ui/PageHero'`, `MetricTile from '@/components/ui/MetricTile'`, `StatusChip from '@/components/ui/StatusChip'`, `{ countThisMonth, countTotal, countDrafts } from '@/lib/db/queries/proposal-aggregates'`, `{ buildListResponse } from '@/lib/api/proposals/list'`
       - Keep: `requireUser`, `getCurrentLang`, `t`, `DeleteJustToast`, `Metadata` type
       - Remove: `RecentlyDeletedToggle`, `SearchBar`, `ProposalsList` imports
       - Keep `export const dynamic = 'force-dynamic'` and `export const metadata: Metadata = { title: 'Accueil — Leasétic Matrice' }`

    2. Page function: keep the existing `requireUser()` + `getCurrentLang()` + `displayName` chain (carry-forward from current page lines 38-48). Then in parallel (use `await Promise.all([...])` for the 4 async calls — 3 aggregates + buildListResponse({limit:5})):
       - `const [countThisMonthVal, countTotalVal, countDraftsVal, recentList] = await Promise.all([countThisMonth(userId), countTotal(userId), countDrafts(userId), buildListResponse({ userId, limit: 5 })])`

    3. PageHero call (per UI-SPEC `<PageHero>` call sub-section):
       - title: `t('dashboard.greeting', lang).replace('{0}', displayName)` (reuse existing `dashboard.greeting` key from line ~296)
       - subtitle: `t('dashboard.home.subtitle', lang)` (NEW key from Plan 02)
       - actions: `<Link href="/proposals/new/parametres" className="btn-green" ...>` with `<Plus size={17} strokeWidth={1.6} />` icon + `<span>{t('dashboard.cta.new', lang)}</span>` (verify the existing `dashboard.cta.new.proposal` key — if Plan 02 reused it, use that key; if Plan 02 added a fresh `dashboard.cta.new` key, use that)

    4. MetricTile 3-up grid (per UI-SPEC §Layout step 2):
       - Wrapper: `<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 24, marginBottom: 32 }}>`
       - 3 MetricTile instances per the UI-SPEC table (variant=month/total/drafts; labels from `dashboard.metricTile.thisMonth/.total/.drafts` keys; values from the 3 aggregate counts as strings)

    5. Propositions récentes card (per UI-SPEC §Layout step 3 + 17-PATTERNS.md `Recent proposals card pattern`):
       - `.card` wrapper with `.ctitle` header `● PROPOSITIONS RÉCENTES` (use existing `dashboard.recent.title` key — reused from Plan 02 verify step)
       - Map `recentList.rows.slice(0, 5)` to abbreviated row Links to `/proposals/${id}` (use `.list-row` class)
       - Each row: `<StatusChip variant={row.displayStatus} label={t('chip.' + row.displayStatus, lang)} />`, client name (14.5px/600/--ink), lcRef (13px/500 monospace), loyer formatted right-aligned (14.5px/600)
       - If `recentList.rows.length === 0`: render empty state `<p style={{color:'var(--muted)', fontSize:'14.5px'}}>{t('dashboard.recent.empty', lang)}</p>` per UI-SPEC §Empty states
       - Bottom-right `Voir toutes →` `<Link href="/proposals">` styled `fontSize:14, fontWeight:500, color:var(--teal), textDecoration:none`, marginTop:16

    6. Keep `<DeleteJustToast />` mount (carry-forward).

    7. Create `app/(authed)/page.test.tsx` adapting the page-level test scaffolding from `app/(authed)/proposals/new/verification/page.test.tsx:36-80` (vi.hoisted + vi.mock pattern). Mock `requireUser`, `getCurrentLang`, the 3 aggregate helpers, and `buildListResponse`. Implement all 8 behavior tests.
  </action>
  <verify>
    <automated>npm test -- "app/(authed)/page.test.tsx"</automated>
  </verify>
  <done>
    - app/(authed)/page.tsx contains exactly: PageHero, 3 MetricTile, Propositions récentes card, DeleteJustToast (verified via grep)
    - `grep -c "RecentlyDeletedToggle\|SearchBar\|ProposalsList" "app/(authed)/page.tsx"` returns 0 (mount removed)
    - `grep -c "MetricTile\|PageHero" "app/(authed)/page.tsx"` returns >= 4 (1 PageHero + 3 MetricTile)
    - `grep -c "DeleteJustToast" "app/(authed)/page.tsx"` returns >= 1 (carry-forward preserved)
    - `grep -c "href=\"/proposals\"" "app/(authed)/page.tsx"` returns >= 1 (Voir toutes link)
    - `grep -c "href=\"/proposals/new/parametres\"" "app/(authed)/page.tsx"` returns >= 1 (Nouvelle proposition CTA)
    - All 8 behavior tests pass
    - `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | session-derived userId (trusted via requireUser); no untrusted query params consumed |
| Server component → proposal-aggregates | userId from session.user.id; never overridable from request |
| Server component → buildListResponse | limit=5 hardcoded for recent preview; no user-controlled limit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information disclosure (cross-user count leak) | proposal-aggregates queries | mitigate | All 3 helpers take userId and apply `eq(schema.proposals.userId, userId)`; Test 5 in Task 1 enforces; userId derived from session via requireUser, not from request params |
| T-17-03-02 | Information disclosure (cross-user recent list) | buildListResponse({userId, limit:5}) | mitigate | userId scoped per Plan 01 contract; ProposalRowDto projection strips commission |
| T-17-03-03 | Information disclosure (commission via abbreviated row) | recent-row inline render | mitigate | Abbreviated row renders ONLY StatusChip + client name + lcRef + loyer; commission fields not in the projected DTO; ADMIN-09 9-gate suite verifies |
| T-17-03-04 | Tampering (Voir toutes URL injection) | Link href | mitigate | href is the literal string `/proposals` (no interpolation); browser cannot redirect to attacker-controlled path |
| T-17-03-05 | DoS (unbounded aggregate scan) | countTotal across all-time | accept | Per-partner expected volume <1000 rows in v1.3; existing `(user_id)` index covers the WHERE clause; v1.4+ may add a materialized aggregate if measured |
| T-17-03-06 | Information disclosure (DST-related off-by-one) | countThisMonth Europe/Paris boundary | mitigate | App-side compute of monthStartUtc using Intl.DateTimeFormat + explicit timezone; Test 6 covers DST boundaries; code comment documents the approach |
</threat_model>

<verification>
- `npm test -- src/lib/db/queries/proposal-aggregates.test.ts "app/(authed)/page.test.tsx"` exits 0
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
- Visual: `npm run dev` → visit `/` → see Bonjour greeting + Nouvelle proposition CTA + 3 metric tiles + Propositions récentes card with up to 5 rows + Voir toutes → link (Plan 08 verifies)
</verification>

<success_criteria>
- Partner Home renders PageHero + 3 MetricTiles + Propositions récentes preview (≤5 rows or empty state) — matches Figma `9:47`
- Aggregate counts scoped to userId; Europe/Paris timezone correct for Ce mois-ci
- RecentlyDeletedToggle and full ProposalsList no longer mounted (moved to /proposals via Plan 04)
- ADMIN-09 9-gate suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/17-partner-surfaces/17-03-SUMMARY.md`
</output>
