---
phase: 19-new-capabilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.mjs
  - src/lib/xlsx/render.ts
  - src/lib/xlsx/types.ts
  - src/lib/xlsx/index.ts
  - src/lib/xlsx/render.test.ts
  - src/lib/api/proposals/list.ts
  - src/lib/i18n/dictionaries.ts
  - app/(authed)/proposals/_actions/exportProposals.ts
  - app/(authed)/proposals/_actions/exportProposals.test.ts
  - app/(authed)/proposals/_components/ExportButton.tsx
  - app/(authed)/proposals/_components/ExportButton.test.tsx
  - app/(authed)/proposals/page.tsx
  - tests/admin-09-grep-contracts.test.ts
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
tags:
  - xlsx
  - export
  - partner
  - admin-09
must_haves:
  truths:
    - "A partner on /proposals sees a secondary 'Exporter en XLSX' button in the PageHero actions slot, next to the primary 'Nouvelle proposition' CTA"
    - "Clicking the button while resultCount > 0 triggers a server-rendered .xlsx download named propositions-YYYY-MM-DD.xlsx"
    - "The downloaded file contains exactly one worksheet named 'Propositions' with all 10 columns (Référence, Client, Projet, Montant HT, Durée, Loyer mensuel, Coefficient, Statut, Date de création, Date d'expiration) in the partner's locale"
    - "The XLSX export respects the current URL filter (archived + q): exporting from /proposals?archived=1 returns archived rows; exporting from /proposals?q=Dupont returns matching rows"
    - "The button is disabled with tooltip 'Aucune proposition à exporter' (FR) / 'No proposals to export' (EN) when resultCount === 0"
    - "While the export is in flight the button label swaps to 'Génération…' (FR) / 'Generating…' (EN) and shows an inline Loader2 spinner in --teal"
    - "On success a sonner toast 'Export prêt' / 'Export ready' fires; on failure 'Échec de l'export, réessayez' / 'Export failed, try again' fires; button always re-enables in finally"
    - "exceljs is importable ONLY from src/lib/xlsx/** — ESLint blocks imports from anywhere else (no-restricted-imports) per D-02"
    - "Every file under src/lib/xlsx/** has 'import \\'server-only\\'' as line 1 (after optional directive)"
    - "The XLSX byte-inspection ADMIN-09 gate (gate 10) in tests/admin-09-grep-contracts.test.ts is GREEN: a fixture proposal with commission_pct=25 generated through generateProposalsXlsx contains zero 'commission' (case-insensitive) substring in any worksheet name, header row, or cell"
    - "The 9-gate ADMIN-09 baseline (gates 1-9) remains GREEN throughout (monotonic extension to 10 gates total)"
    - "No drizzle/migrations/* file is created or modified; no drizzle/meta/_journal.json change"
    - "No new audit_log event type is added (D-04 — export is a read operation)"
    - "Both the Partner /proposals route and the ExportButton ship light + dark variants automatically via the data-theme token cascade (no per-component dark CSS added)"
  artifacts:
    - path: "src/lib/xlsx/render.ts"
      provides: "generateProposalsXlsx({ rows, locale }) -> Promise<Buffer> adapter wrapping exceljs"
      exports: ["generateProposalsXlsx"]
      contains: "import 'server-only'"
      min_lines: 80
    - path: "src/lib/xlsx/types.ts"
      provides: "XlsxExportRow and GenerateProposalsXlsxArgs interfaces"
      exports: ["XlsxExportRow", "GenerateProposalsXlsxArgs"]
    - path: "src/lib/xlsx/index.ts"
      provides: "barrel re-export of generateProposalsXlsx + types (per CONVENTIONS.md barrel discipline)"
      exports: ["generateProposalsXlsx", "XlsxExportRow", "GenerateProposalsXlsxArgs"]
    - path: "src/lib/xlsx/render.test.ts"
      provides: "Vitest coverage: header generation, 10-column shape, locale formatting, commission absence"
    - path: "app/(authed)/proposals/_actions/exportProposals.ts"
      provides: "Server Action exportProposalsAction returning binary Response with Content-Disposition attachment"
      exports: ["exportProposalsAction"]
      contains: "'use server'"
    - path: "app/(authed)/proposals/_actions/exportProposals.test.ts"
      provides: "Vitest coverage: auth gate ordering, filter+search scope, empty-result behavior"
    - path: "app/(authed)/proposals/_components/ExportButton.tsx"
      provides: "Client component with default/disabled/loading state machine + sonner toast wiring"
      exports: ["ExportButton"]
      contains: "'use client'"
    - path: "app/(authed)/proposals/_components/ExportButton.test.tsx"
      provides: "Vitest coverage: disabled-when-zero, loading transition, toast firing"
    - path: "tests/admin-09-grep-contracts.test.ts"
      provides: "Gate 10 added: XLSX parser-based commission scan per D-05; suite total reaches 10 gates"
      contains: "Gate 10"
  key_links:
    - from: "app/(authed)/proposals/page.tsx"
      to: "app/(authed)/proposals/_components/ExportButton.tsx"
      via: "ExportButton mounted in <PageHero actions={...}> alongside existing Nouvelle proposition link"
      pattern: "ExportButton.*resultCount.*archived.*q.*lang"
    - from: "app/(authed)/proposals/_components/ExportButton.tsx"
      to: "app/(authed)/proposals/_actions/exportProposals.ts"
      via: "client onClick handler awaits exportProposalsAction({ archived, q })"
      pattern: "exportProposalsAction\\("
    - from: "app/(authed)/proposals/_actions/exportProposals.ts"
      to: "src/lib/api/proposals/list.ts"
      via: "buildExportQuery({ userId, archived, q, limit: MAX_EXPORT_ROWS }) replaces cursor-paginated buildListResponse for the export path"
      pattern: "buildExportQuery"
    - from: "app/(authed)/proposals/_actions/exportProposals.ts"
      to: "src/lib/xlsx/render.ts"
      via: "generateProposalsXlsx({ rows, locale }) generates the binary buffer returned in Response body"
      pattern: "generateProposalsXlsx"
    - from: "eslint.config.mjs"
      to: "src/lib/xlsx/**"
      via: "no-restricted-imports paths entry for 'exceljs' + allow-list block exempting files matching 'src/lib/xlsx/**/*.{ts,tsx}' (mirrors @react-pdf/renderer pattern at lines 77-81 + 130-141)"
      pattern: "exceljs.*src/lib/xlsx"
---

<objective>
Ship per-partner XLSX export from /proposals (EXPORT-01, EXPORT-02) per D-01..D-10. Delivers:

1. A new server-only `src/lib/xlsx/**` adapter wrapping `exceljs` (D-01, D-02) — Buffer in, Buffer out, locale-aware headers + cell formats per the UI-SPEC §XLSX Cell Formatting table, freeze header row, 10 fixed columns with locked widths, worksheet name `Propositions`.
2. `exceljs` ESLint guard mirroring `@react-pdf/renderer` → `src/lib/pdf/**` (per D-02 and eslint.config.mjs:77-81, 130-141).
3. A Server Action `exportProposalsAction` returning binary `Response` with `Content-Disposition: attachment` (D-03). Filename `propositions-YYYY-MM-DD.xlsx` (D-04). No `audit_log` row (D-04 — lifecycle-only convention holds).
4. A client `<ExportButton>` mounted in the existing PageHero `actions` slot on `/proposals` (D-06) with default / disabled-when-zero / loading state machine (D-07, D-10) and sonner toasts.
5. Unbounded export query path (`buildExportQuery`) in `src/lib/api/proposals/list.ts` — same scoping as the SSR `buildListResponse` BUT without cursor + with `MAX_EXPORT_ROWS = 10000` (D-08). Reads `archived` and `q` URL state at action time.
6. ~10 new i18n keys × FR+EN in `src/lib/i18n/dictionaries.ts` per UI-SPEC §Copy and §i18n Keys Summary (verify-and-reuse first per Phase 17 D-21).
7. ADMIN-09 grep-contract suite extended monotonically: gate 10 (XLSX parser-based commission scan per D-05) added to `tests/admin-09-grep-contracts.test.ts`. Suite reaches 10 gates total; gates 1-9 stay green.

Purpose: Partners get a take-away artifact of their proposal pipeline that opens cleanly in Excel/LibreOffice/Google Sheets, with locale-correct currency and dates, and zero commission leakage proven by parser-based test instead of byte heuristics.

Output: 13 net-new files + 4 modified files. Both light and dark inherited via Phase 16 token cascade; no per-component dark CSS. No schema migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 19 source-of-truth artifacts
@.planning/phases/19-new-capabilities/19-CONTEXT.md
@.planning/phases/19-new-capabilities/19-UI-SPEC.md
@.planning/phases/19-new-capabilities/19-PATTERNS.md

# Carry-forward context
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md

# Codebase docs
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

# Exact analogs the executor needs (PATTERNS.md identifies these as canonical)
@src/lib/pdf/render.ts
@app/(authed)/proposals/new/_actions/saveAsDraft.action.ts
@app/(authed)/proposals/new/verification/FinalizeButton.tsx
@app/(authed)/proposals/page.tsx
@app/(authed)/proposals/_components/FilterPillRow.tsx
@src/lib/api/proposals/list.ts
@src/lib/i18n/dictionaries.ts
@src/lib/i18n/format.ts
@src/lib/auth/require.ts
@eslint.config.mjs
@tests/admin-09-grep-contracts.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install exceljs + create src/lib/xlsx adapter (render + types + barrel + ESLint guard)</name>
  <files>
    package.json,
    package-lock.json,
    eslint.config.mjs,
    src/lib/xlsx/types.ts,
    src/lib/xlsx/render.ts,
    src/lib/xlsx/index.ts,
    src/lib/xlsx/render.test.ts
  </files>
  <read_first>
    src/lib/pdf/render.ts (exact analog — server-only line 1, module JSDoc, async Buffer-returning adapter, pure-Node imports),
    src/lib/pdf/index.ts (barrel re-export pattern),
    src/lib/api/proposals/list.ts (DTO-shape and naming conventions for *Args / *Row types),
    eslint.config.mjs lines 1-200 (no-restricted-imports rule shape at 77-81; allow-list block at 130-141 for src/lib/pdf/**),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §XLSX Cell Formatting (locked column widths, cell types, worksheet name, freeze header row),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-01, D-02, D-05, D-09 + §Claude's Discretion,
    .planning/phases/19-new-capabilities/19-PATTERNS.md §src/lib/xlsx/render.ts + §src/lib/xlsx/types.ts + §eslint.config.mjs sections,
    .planning/codebase/CONVENTIONS.md §Naming / §Module Design / §Comments (module-level JSDoc with D-NN references)
  </read_first>
  <behavior>
    - Test 1: importing generateProposalsXlsx from '@/lib/xlsx' returns an async function (smoke import)
    - Test 2: generateProposalsXlsx({ rows: [], locale: 'fr' }) returns a Buffer that parses back as a valid ExcelJS workbook with exactly 1 worksheet named 'Propositions'
    - Test 3: header row 1 contains exactly 10 cells in this order and FR text: ['Référence', 'Client', 'Projet', 'Montant HT', 'Durée', 'Loyer mensuel', 'Coefficient', 'Statut', 'Date de création', 'Date d\\'expiration']
    - Test 4: header row 1 in EN locale contains the EN equivalents from UI-SPEC §Copy column-header table
    - Test 5: worksheet.views[0] equals { state: 'frozen', ySplit: 1 } (header row frozen per D-09 Claude discretion)
    - Test 6: column widths match the UI-SPEC §XLSX Cell Formatting locked widths (Référence:18, Client:30, Projet:30, Montant HT:16, Durée:12, Loyer mensuel:16, Coefficient:14, Statut:14, Date de création:18, Date d'expiration:18)
    - Test 7: given a single XlsxExportRow with amountHt: 12000, monthlyRent: 333.33, durationMonths: 36, the resulting cells in row 2 (col D, col F) carry numeric values with Excel cell format string '# ##0.00 €' and durée carries the string '36 mois'
    - Test 8: given expiresAt: null in a row, the corresponding cell value is null/empty (not the string 'null')
    - Test 9: ADMIN-09 commission scrub — given an XlsxExportRow with NO commission-related fields (the type cannot carry one), iterating workbook.worksheets and walking every header/cell with /commission/i matches zero results (foreshadow of gate 10)
  </behavior>
  <action>
    Run `npm install --save exceljs` to add the package to `dependencies` (NOT devDependencies — runs at request time per CONTEXT.md §Operational); confirm package-lock.json updates and `npm ls exceljs` reports a single resolved version. Do not pin minor unless conflict appears; check `npm view exceljs versions` only if install errors.

    Create `src/lib/xlsx/types.ts` (pure type module — no 'server-only' directive, no runtime imports) declaring two exported interfaces per PATTERNS.md §src/lib/xlsx/types.ts: (a) `XlsxExportRow` with fields `lcRef: string`, `clientName: string`, `projectName: string`, `amountHt: number`, `durationMonths: number`, `monthlyRent: number`, `coefficient: string` (percent string like "3.69%" per UI-SPEC D-09 locked decision — NOT Excel native percent), `status: string` (i18n-resolved status label), `createdAt: Date | null`, `expiresAt: Date | null`; (b) `GenerateProposalsXlsxArgs` with `rows: XlsxExportRow[]`, `locale: 'fr' | 'en'`. Critical: NO `commissionPct`, NO `commission`, NO `commission_amount` field exists on this type — the type IS the ADMIN-09 boundary (D-05).

    Create `src/lib/xlsx/render.ts` with `import 'server-only';` on line 1 (mirrors src/lib/pdf/render.ts:1). Module JSDoc per CONVENTIONS.md §Comments format and PATTERNS.md template: references Phase 19 Plan 01, D-01 (exceljs), D-02 (adapter-isolation), D-03 (consumed by exportProposalsAction returning binary Response), D-04 (no audit_log), D-05 (commission absence guarantee), D-09 (locale-aware headers + cell formats). Import `ExcelJS from 'exceljs'` (this single import line is what the ESLint allow-list block must permit). Export async `generateProposalsXlsx({ rows, locale }: GenerateProposalsXlsxArgs): Promise<Buffer>` that: (1) creates `new ExcelJS.Workbook()`; (2) sets `workbook.creator = 'Leasétic Matrice'` and `workbook.created = new Date()`; (3) adds one worksheet named exactly `'Propositions'` (UI-SPEC §XLSX Cell Formatting — locked, single-locale); (4) sets `worksheet.views = [{ state: 'frozen', ySplit: 1 }]`; (5) sets `worksheet.columns = [...]` with the 10 column definitions in order (Référence/Client/Projet/Montant HT/Durée/Loyer mensuel/Coefficient/Statut/Date de création/Date d'expiration) — each column carries a `key` (camelCase matching XlsxExportRow), `header` resolved via the new i18n keys `proposals.export.column.*` resolved from `dictionaries.ts` using `t(key, locale)`, and `width` per the locked UI-SPEC widths table; (6) iterates `rows` and pushes each as `worksheet.addRow({...})`; for the `amountHt` and `monthlyRent` columns set the column-level `numFmt = '# ##0.00 €'` (per UI-SPEC §XLSX Cell Formatting Excel-native currency, locale-independent — Excel handles separators); for `createdAt` and `expiresAt` use Excel Date cell type (`worksheet.getCell(rowIdx, colIdx).numFmt = locale === 'fr' ? 'DD/MM/YYYY' : 'MM/DD/YYYY'`); the `durationMonths` cell goes in as the string `${row.durationMonths} mois` (per UI-SPEC); `coefficient` goes in as the raw `row.coefficient` percent string (e.g. "3.69%"); `status` is the pre-resolved i18n label string passed in by the caller (the adapter does not call `t()` for status because the StatusChip-equivalent label may be conditional in the caller); (7) `const buf = await workbook.xlsx.writeBuffer()`; `return Buffer.from(buf)`.

    The header label resolution uses `t('proposals.export.column.reference', locale)`, `t('proposals.export.column.client', locale)`, etc. — these keys are added in Task 2 to dictionaries.ts. For Task 1 the render.ts can either (a) call `t()` (then Task 1 cannot ship green tests until Task 2's keys exist) OR (b) accept an optional `columnHeaderResolver: (key: string) => string` arg with default `(k) => t(k, args.locale)`. Choose (a) — implement Tasks 1 and 2 with a tight feedback loop, run vitest after Task 2 adds the keys. Tests in render.test.ts that assert exact header text rely on the keys being present; commit Tasks 1+2 together if vitest gates require it.

    Create `src/lib/xlsx/index.ts` barrel re-exporting `generateProposalsXlsx` from `./render` and the two types from `./types` (per CONVENTIONS.md §Imports barrel discipline — Phase 19 consumers MUST import from `@/lib/xlsx`, never `@/lib/xlsx/render`).

    Create `src/lib/xlsx/render.test.ts` covering the 9 behaviors in <behavior>. Mock `'server-only'` as no-op via `vi.mock('server-only', () => ({}))` (CONVENTIONS pattern from Phase 6). Use `await import('exceljs')` for parse-back assertions to confirm the buffer round-trips through a fresh ExcelJS.Workbook.xlsx.load(buffer) call. Place a /commission/i regex assertion that walks every sheet, every row, every cell, and the sheet name — this is the local foreshadow of gate 10 (which lives in tests/admin-09-grep-contracts.test.ts and is added in Task 3).

    Modify `eslint.config.mjs` per PATTERNS.md §eslint.config.mjs (MODIFY) instructions and the existing `@react-pdf/renderer` precedent at lines 77-81 + 130-141:
    - (a) Add a new entry to the `no-restricted-imports.paths` array (same array that already lists `@vercel/blob`, `@vercel/postgres`, `@neondatabase/serverless`, `postgres`, `@react-pdf/renderer`, `@aws-sdk/*`): `{ name: 'exceljs', message: '`exceljs` may only be imported from src/lib/xlsx/. XLSX generation goes through src/lib/xlsx/render.ts (generateProposalsXlsx). (D-02)' }`. Place adjacent to the existing `@react-pdf/renderer` entry for readability.
    - (b) After the existing `files: ['src/lib/pdf/**/*.{ts,tsx}']` allow-list block (lines 130-141), append an analogous allow-list block: `{ files: ['src/lib/xlsx/**/*.{ts,tsx}'], rules: { 'no-restricted-imports': 'off', 'no-restricted-syntax': 'off' } }`. Justification matches pdf: the adapter directory legitimately imports exceljs; no-restricted-syntax JSXText is irrelevant here (no JSX). Do NOT touch the `ignores` block on the first rule object (lines 36-42) — `src/lib/xlsx/**` should NOT be added there (we want the rest of the lint rules to apply, just not the import restriction).

    Implementation order constraint: install exceljs → write types.ts → write render.ts → write barrel → run render.test.ts (will fail on header-text assertions until dictionary keys exist — that's expected; pass in Task 2). Modify eslint.config.mjs LAST in this task — verify `npm run lint` passes before considering the task done (the allow-list block must be in place before render.ts's `import ExcelJS from 'exceljs'` linter-passes).

    No fenced code blocks in this action. Code shapes are pinned by the analogs in <read_first> (src/lib/pdf/render.ts) and the explicit identifier names in this paragraph (generateProposalsXlsx, XlsxExportRow, GenerateProposalsXlsxArgs, MAX_EXPORT_ROWS, freeze ySplit:1, numFmt '# ##0.00 €', worksheet name 'Propositions'). Per CONVENTIONS.md §Function Design use options-object args (`{ rows, locale }`) not positional. Per CONVENTIONS.md §Comments include the D-NN references in the module JSDoc. Per CONVENTIONS.md §Imports use the `@/lib/i18n/dictionaries` alias when calling `t()`.
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run src/lib/xlsx/render.test.ts --reporter=verbose
    </automated>
    grep -n "exceljs" eslint.config.mjs | grep -v "^#" must show at least 2 hits (the restricted-imports entry + the allow-list block files glob);
    grep -rn "from 'exceljs'" src/ | grep -v node_modules must return ONLY paths under src/lib/xlsx/;
    head -1 src/lib/xlsx/render.ts must equal `import 'server-only';`;
    npm ls exceljs returns a single resolved version under dependencies (not devDependencies)
  </verify>
  <done>
    src/lib/xlsx/{types.ts, render.ts, index.ts, render.test.ts} exist; eslint.config.mjs has both the no-restricted-imports entry for `exceljs` and the allow-list block for src/lib/xlsx/**; package.json has `exceljs` under `dependencies`; `npm run typecheck` passes; `npm run lint` passes (zero warnings — `--max-warnings=0` CI gate); `npx vitest run src/lib/xlsx/render.test.ts` reports all 9 tests passing (or 8 if 1 header-text test is intentionally deferred to post-Task-2 commit); the XlsxExportRow type has NO commission-bearing field (verified by grep `grep -n "commission" src/lib/xlsx/ | grep -v '^#'` returning zero hits).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add export i18n keys + buildExportQuery unbounded path + Server Action returning binary Response</name>
  <files>
    src/lib/i18n/dictionaries.ts,
    src/lib/api/proposals/list.ts,
    app/(authed)/proposals/_actions/exportProposals.ts,
    app/(authed)/proposals/_actions/exportProposals.test.ts
  </files>
  <read_first>
    src/lib/i18n/dictionaries.ts (existing FR + EN key structure; _EnHasAllFrKeys compile-time parity proof location — must stay green per CONTEXT.md §invariants),
    src/lib/api/proposals/list.ts (existing buildListResponse + searchProposals + listProposalsByUser exports; existing scoping/IDOR discipline at the userId filter step; existing DisplayStatus mapping via deriveDisplayStatus from src/lib/db/queries/proposals),
    src/lib/db/queries/proposals.ts (deriveDisplayStatus signature and DisplayStatus union — needed to map status to i18n label),
    src/lib/i18n/format.ts (formatCurrency, formatDate, formatNumber signatures — but for XLSX the adapter uses native Excel numFmt instead, so format helpers are used only in cell-fallback paths and tests),
    src/lib/auth/require.ts (requireUser return shape: { session, user } — Task body asserts `await requireUser()` is the FIRST await per PITFALLS §7.3),
    app/(authed)/proposals/new/_actions/saveAsDraft.action.ts (closest Server Action analog — directive on line 1, requireUser ordering, error envelope),
    app/(authed)/proposals/new/verification/FinalizeButton.test.tsx (test pattern for Server Actions that touch db/render — vi.mock patterns for '@/lib/auth/require' and '@/lib/db'),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §Copy + §i18n Keys Summary (locked FR + EN text for all 22 export keys; status cell value table for the 4 displayStatus values),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-03, D-04, D-07, D-08, D-09 + §specifics §Export Server Action signature shape,
    .planning/phases/19-new-capabilities/19-PATTERNS.md §app/(authed)/proposals/_actions/exportProposals.ts + §src/lib/api/proposals/list.ts (MODIFY) sections,
    .planning/phases/17-partner-surfaces/17-CONTEXT.md §D-21 (verify-and-reuse i18n discipline — check before adding any key)
  </read_first>
  <behavior>
    - Test 1: exportProposalsAction({ archived: false, q: '' }) called by a user with at least one active proposal returns a Response with status 200, Content-Type 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', and a Content-Disposition header matching /^attachment; filename="propositions-\\d{4}-\\d{2}-\\d{2}\\.xlsx"$/
    - Test 2: exportProposalsAction({ archived: true }) calls buildExportQuery with { archived: true } in the args (verified by spy); the same for q
    - Test 3: AUTH-FIRST — vi.mock orders assert that requireUser() resolves before db() or buildExportQuery is called (mirrors CONVENTIONS.md auth-first ordering invariant test pattern from src/lib/auth/actions.test.ts:84-108)
    - Test 4: when requireUser() throws (unauthenticated), exportProposalsAction throws / does not return a 200 Response; buildExportQuery is NEVER called
    - Test 5: buildExportQuery({ userId: 'u1', archived: false, q: '', limit: 10000 }) returns an array of XlsxExportRow with no commission-bearing keys (asserted by Object.keys(row).every(k => !/commission/i.test(k)) for every row)
    - Test 6: buildExportQuery respects the archived filter — given a fixture DB with one active and one expired proposal for userId 'u1', archived:false returns the active row only; archived:true returns the expired row only (mirrors Phase 17 D-12 archived semantics)
    - Test 7: buildExportQuery enforces userId IDOR — given a row owned by userId 'u2', a call with userId:'u1' does NOT return that row (mirrors buildListResponse IDOR discipline)
    - Test 8: buildExportQuery uses MAX_EXPORT_ROWS = 10000 as the default limit (verified by spy on the underlying listProposalsByUser/searchProposals limit arg)
    - Test 9: dictionaries.ts FR branch contains all 15 keys listed in UI-SPEC §i18n Keys Summary under proposals.export.* (5 UI keys + 10 column-header keys); the existing _EnHasAllFrKeys compile-time proof passes (tsc clean) confirming EN parity
    - Test 10: per CONVENTIONS.md §Error Handling, exportProposalsAction wraps the buildExportQuery + generateProposalsXlsx call in try/catch; on failure it throws a bounded Error('ExportFailed') — the client component translates this to the i18n key proposals.export.toast.error
  </behavior>
  <action>
    Modify `src/lib/i18n/dictionaries.ts` to add the new keys per UI-SPEC §Copy and §i18n Keys Summary (locked FR + EN text). Verify-and-reuse first per Phase 17 D-21: grep the existing dictionary for `proposals.export.`, `proposals.column.`, `column.reference`, `column.client`, etc. — confirm zero hits before adding new keys (per UI-SPEC §i18n Keys Summary §Reuse confirmed: `chip.active`/`chip.draft`/`chip.expired`/`chip.deleted` already exist and are reused for status cell values; no other reusable keys). Add 15 net-new keys in the `fr` branch + 15 mirroring keys in the `en` branch (the _EnHasAllFrKeys compile-time proof will reject if parity is broken, so add to both branches in the same edit). Keys to add (per UI-SPEC):
    - `proposals.export.cta` — FR "Exporter en XLSX" / EN "Export to XLSX"
    - `proposals.export.loading` — FR "Génération…" / EN "Generating…"
    - `proposals.export.disabled.tooltip` — FR "Aucune proposition à exporter" / EN "No proposals to export"
    - `proposals.export.toast.success` — FR "Export prêt" / EN "Export ready"
    - `proposals.export.toast.error` — FR "Échec de l'export, réessayez" / EN "Export failed, try again"
    - `proposals.export.column.reference` — FR "Référence" / EN "Reference"
    - `proposals.export.column.client` — FR "Client" / EN "Client"
    - `proposals.export.column.project` — FR "Projet" / EN "Project"
    - `proposals.export.column.amountHt` — FR "Montant HT" / EN "Amount (excl. VAT)"
    - `proposals.export.column.duration` — FR "Durée" / EN "Duration"
    - `proposals.export.column.monthlyRent` — FR "Loyer mensuel" / EN "Monthly rent"
    - `proposals.export.column.coefficient` — FR "Coefficient" / EN "Coefficient"
    - `proposals.export.column.status` — FR "Statut" / EN "Status"
    - `proposals.export.column.createdAt` — FR "Date de création" / EN "Created on"
    - `proposals.export.column.expiresAt` — FR "Date d'expiration" / EN "Expires on"
    Place new keys grouped under a `proposals.export.*` namespace block in both branches, alphabetically adjacent to existing `proposals.*` keys for readability. After editing, `npm run typecheck` MUST stay green (the _EnHasAllFrKeys parity proof fires at tsc time).

    Modify `src/lib/api/proposals/list.ts` per PATTERNS.md §src/lib/api/proposals/list.ts (MODIFY) section. Add:
    - An exported `MAX_EXPORT_ROWS` constant: `export const MAX_EXPORT_ROWS = 10000;` (per D-08 and UI-SPEC §XLSX Cell Formatting end notes — sane upper bound, real partners have <100 proposals, 100x safety buffer).
    - An exported async function `buildExportQuery({ userId, q, archived, limit }: { userId: string; q?: string; archived?: boolean; limit?: number }): Promise<XlsxExportRow[]>`. Implementation mirrors `buildListResponse` (Phase 8 + Phase 17 D-13) BUT:
      - No cursor / no `cursorEncoded` (unbounded, single shot)
      - `limit` defaults to `MAX_EXPORT_ROWS`
      - Same userId-scoped IDOR discipline (T-19-01-01: userId comes from `requireUser()` session in the caller, never from request params)
      - Same `archived` semantics as Phase 17 D-12 (status='expired' OR deletedAt in 30-day window)
      - Same `q` ILIKE behavior on client name + reference
      - Returns an array projected to `XlsxExportRow` shape, applying `deriveDisplayStatus(row)` to compute the status, then resolving the status label via the existing `chip.${displayStatus}` i18n keys for the caller's locale (the locale param is NOT in `buildExportQuery`'s signature — instead the caller `exportProposalsAction` calls `buildExportQuery` for raw data, then maps each row to a localized XlsxExportRow in the action body OR the caller passes a `lang` param to `buildExportQuery`; pick the option that keeps the function pure of i18n by having the action body do the projection — cleaner per CONVENTIONS.md §Function Design and §Purity discipline).
      - CRITICAL — commission scrub: the raw `proposals` rows from the DB carry `paramsSnapshot` jsonb that contains `commission_pct`. The projection to `XlsxExportRow` MUST NOT include `paramsSnapshot` or `commission_pct` (D-05 — the type IS the boundary). Mirror the existing buildListResponse projection at list.ts:143-148.

    Decide projection ownership during execution: option A — `buildExportQuery` returns raw rows of a `ProposalForExport` type (lcRef + clientName + projectName + amountHt + durationMonths + monthlyRent + coefficient + displayStatus + createdAt + expiresAt + id), and `exportProposalsAction` maps to `XlsxExportRow` by resolving `status` via `t(\`chip.${displayStatus}\`, locale)`. Option B is acceptable but A is cleaner (pure query helper, no i18n coupling). Document the choice in `buildExportQuery`'s JSDoc.

    Create `app/(authed)/proposals/_actions/exportProposals.ts` per PATTERNS.md §app/(authed)/proposals/_actions/exportProposals.ts:
    - Line 1: `'use server';` (no other directive — Server Actions cannot also be 'use client'; no `import 'server-only'` because Server Actions are inherently server-only by Next.js framework guarantee).
    - Module JSDoc per CONVENTIONS.md §Comments with D-03 (binary Response shape), D-04 (no audit_log), D-08 (unbounded query, archived + q from action input), D-09 (locale from session.user.locale fallback 'fr').
    - Imports: `requireUser` from `@/lib/auth/require`; `buildExportQuery, MAX_EXPORT_ROWS` from `@/lib/api/proposals/list`; `generateProposalsXlsx` from `@/lib/xlsx`; `getCurrentLang` from `@/lib/i18n` (resolves locale from cookies for server contexts).
    - Export async `exportProposalsAction({ archived, q }: { archived?: boolean; q?: string }): Promise<Response>` that: (a) `const { session } = await requireUser();` — FIRST await per PITFALLS §7.3; (b) `const lang = await getCurrentLang();` — locale resolution (Phase 6 pattern); (c) `const rawRows = await buildExportQuery({ userId: session.user.id, archived, q, limit: MAX_EXPORT_ROWS });`; (d) project `rawRows` to `XlsxExportRow[]` resolving the status cell via `t(\`chip.${row.displayStatus}\`, lang)` (FR: Actif/Brouillon/Expiré/Supprimé; EN: Active/Draft/Expired/Deleted — these come from the existing `chip.active`/`chip.draft`/`chip.expired`/`chip.deleted` keys per UI-SPEC §Reuse confirmed); (e) `const buf = await generateProposalsXlsx({ rows: localizedRows, locale: lang });`; (f) build a filename `propositions-${todayIso()}.xlsx` where `todayIso()` returns ISO date in YYYY-MM-DD (use `new Date().toISOString().slice(0,10)` — D-04 locked format `propositions-2026-05-25.xlsx`); (g) return `new Response(buf, { status: 200, headers: { 'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', 'Content-Disposition': \`attachment; filename="${filename}"\` } })`. Wrap (b)-(f) in try/catch; on error console.error('[exportProposalsAction]', err) (CONVENTIONS.md §Logging) then `throw new Error('ExportFailed')` (bounded code — CONVENTIONS.md §Error Handling).

    Server Action binary Response sanity check: Next.js 16 Server Actions can return a `Response` object directly; the client invokes the action like a normal async function and the browser handles the Response's Content-Disposition via the framework's RSC payload handling. If a Next 16 surface limitation forces a different shape (e.g. requires returning `{ body: base64Buffer, headers }` and the client component reconstructs the Blob + triggers `<a href={URL.createObjectURL(blob)} download>` click), verify via context7 docs (`mcp__context7__resolve-library-id` → `next` → `mcp__context7__get-library-docs` querying "server action binary response"). Pick the simpler path that works; document the choice in the action's JSDoc.

    Create `app/(authed)/proposals/_actions/exportProposals.test.ts` covering the 10 behaviors above. Mock `@/lib/auth/require` (return `{ session: { user: { id: 'u1' } } }`), `@/lib/api/proposals/list` (vi.fn returning a fixture XlsxExportRow array), `@/lib/xlsx` (vi.fn returning Buffer.from('fake xlsx bytes')), `@/lib/i18n` (vi.fn returning 'fr'), `server-only` (no-op per Phase 6 Vitest pattern). The auth-ordering test uses a shared `callSequence: string[]` per CONVENTIONS.md test pattern documented at src/lib/auth/actions.test.ts:84-108.
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run app/\(authed\)/proposals/_actions/exportProposals.test.ts src/lib/xlsx/render.test.ts --reporter=verbose
    </automated>
    grep -c "proposals.export." src/lib/i18n/dictionaries.ts | grep -v '^#' must be >= 30 (15 keys × 2 branches);
    grep -n "MAX_EXPORT_ROWS" src/lib/api/proposals/list.ts must show the export and at least one use;
    grep -n "'use server'" app/\(authed\)/proposals/_actions/exportProposals.ts | grep -v '^#' must show line 1;
    grep -c "commission" app/\(authed\)/proposals/_actions/exportProposals.ts | grep -v '^#' must equal 0 (no commission identifiers in the action body — the type IS the boundary)
  </verify>
  <done>
    The 15 new export keys exist in both fr and en branches of dictionaries.ts and `npm run typecheck` (which fires the _EnHasAllFrKeys compile-time proof) stays green; `buildExportQuery` + `MAX_EXPORT_ROWS` are exported from `src/lib/api/proposals/list.ts`; `exportProposalsAction` exists with 'use server' directive on line 1, requireUser-first ordering, and returns a Response with the locked Content-Disposition filename pattern `propositions-YYYY-MM-DD.xlsx`; vitest runs green for both the action test file and the render test file from Task 1 (any header-text assertions that were deferred in Task 1 now pass because the i18n keys exist); `npm run lint` passes; the action body has zero `commission` identifiers (verified by grep gate above).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount ExportButton in /proposals PageHero actions slot + add ADMIN-09 gate 10 (parser-based commission scan)</name>
  <files>
    app/(authed)/proposals/_components/ExportButton.tsx,
    app/(authed)/proposals/_components/ExportButton.test.tsx,
    app/(authed)/proposals/page.tsx,
    tests/admin-09-grep-contracts.test.ts
  </files>
  <read_first>
    app/(authed)/proposals/page.tsx (current PageHero usage at lines ~107-128 — actions slot already wraps `Nouvelle proposition` link; understand initial.rows.length availability for resultCount prop),
    app/(authed)/proposals/new/verification/FinalizeButton.tsx (exact analog for client component with 3-state machine + Loader2 spinner + sonner toast; PATTERNS.md identifies this as the closest analog),
    app/(authed)/proposals/new/verification/FinalizeButton.test.tsx (client-component test pattern with React Testing Library, vi.mock for Server Actions, getByRole assertions, fireEvent.click + await waitFor),
    app/(authed)/proposals/_components/FilterPillRow.tsx (URL-state pattern: how the page reads ?archived= and ?q= from searchParams — ExportButton needs the same values),
    app/layout.tsx line 56 (sonner Toaster mount — confirms toast.success / toast.error are available),
    tests/admin-09-grep-contracts.test.ts (existing 9 gates 1-9; assertNoCommissionLeakage helper if defined; mock patterns vi.mock('@/lib/auth/require', ...) at line ~68-70; renderToString + createElement pattern for component-render gates),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §Surface 1 + §ExportButton State Machine + §Copy + §Accessibility Notes (locked: .btn-out class, Loader2 size 14 strokeWidth 2 color --teal, animate-spin Tailwind class, aria-disabled, title attribute for tooltip, sonner toast keys),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-05 (gate 10 mechanism — parser-based, not raw-bytes-only) + §specifics §EXPORT-02 gate code shape,
    .planning/phases/19-new-capabilities/19-PATTERNS.md §app/(authed)/proposals/page.tsx + §app/(authed)/proposals/_components/ExportButton.tsx + §tests/admin-09-grep-contracts.test.ts (MODIFY)
  </read_first>
  <behavior>
    - Test 1: ExportButton rendered with resultCount=5, archived=false, q='', lang='fr' shows label "Exporter en XLSX" + has class "btn-out" + is NOT disabled + has NO title attribute
    - Test 2: ExportButton with resultCount=0 is disabled (disabled attr + aria-disabled=true) + label "Exporter en XLSX" + title="Aucune proposition à exporter"
    - Test 3: ExportButton with resultCount=0 and lang='en' has title="No proposals to export"
    - Test 4: clicking an enabled ExportButton triggers exportProposalsAction({ archived: false, q: '' }) once; during the in-flight period the button becomes disabled, label swaps to "Génération…", a Loader2 SVG with class "animate-spin" and inline style color var(--teal) appears
    - Test 5: on resolved Response with status 200, sonner toast.success is called with the string from t('proposals.export.toast.success', 'fr') ("Export prêt"); button returns to default state
    - Test 6: on Server Action throw, sonner toast.error is called with t('proposals.export.toast.error', 'fr') ("Échec de l'export, réessayez"); button returns to default state (re-enabled in finally)
    - Test 7: clicking a button already in loading state is a no-op (guard at top of handler — `if (isLoading) return`)
    - Test 8: /proposals page.tsx imports ExportButton and renders it inside the PageHero `actions` prop, wrapped in a flex container with the existing `Nouvelle proposition` link as a sibling
    - Test 9 (ADMIN-09 gate 10): inside tests/admin-09-grep-contracts.test.ts, a new `describe('Gate 10: XLSX export — commission leakage scan (EXPORT-02)')` block generates an XLSX via `generateProposalsXlsx` with a fixture row, opens it with `new ExcelJS.Workbook().xlsx.load(buf)`, iterates `wb.worksheets`, asserts `sheet.name` does not match `/commission/i`, iterates every cell with `sheet.eachRow(row => row.eachCell(cell => expect(String(cell.value ?? '')).not.toMatch(/commission/i)))`, and asserts the raw buffer bytes also do not contain the literal substring 'commission' (case-insensitive) — the latter is a redundancy gate (D-05 prefers parser-based, but raw-byte check catches XML-attribute regressions)
    - Test 10 (ADMIN-09 gates 1-9 monotonic): the existing 9 gates remain green; the suite total reaches 10 gates after this task ships; `npx vitest run tests/admin-09-grep-contracts.test.ts` exits 0
  </behavior>
  <action>
    Create `app/(authed)/proposals/_components/ExportButton.tsx` per PATTERNS.md §app/(authed)/proposals/_components/ExportButton.tsx and UI-SPEC §Surface 1 §ExportButton State Machine:
    - Line 1: `'use client';`
    - Module JSDoc per CONVENTIONS.md with D-06 (PageHero actions placement), D-07 (loading state + Loader2 spinner + sonner toast), D-10 (disabled when resultCount === 0).
    - Imports: `useState` from 'react'; `Loader2` from 'lucide-react'; `toast` from 'sonner'; `t, type Lang` from `@/lib/i18n/dictionaries`; `exportProposalsAction` from '../_actions/exportProposals'.
    - Props interface `ExportButtonProps` per CONVENTIONS.md §React (PascalCase, props next to component): `{ resultCount: number; archived: boolean; q: string; lang: Lang }`.
    - Named export `function ExportButton({ resultCount, archived, q, lang }: ExportButtonProps)` implementing the 3-state machine from UI-SPEC §ExportButton State Machine table:
      - `const [isLoading, setIsLoading] = useState(false);`
      - `const isDisabled = resultCount === 0 || isLoading;`
      - `async function handleExport() { if (isLoading) return; setIsLoading(true); try { await exportProposalsAction({ archived, q }); toast.success(t('proposals.export.toast.success', lang)); } catch { toast.error(t('proposals.export.toast.error', lang)); } finally { setIsLoading(false); } }`
      - Render: `<button type="button" className="btn-out" onClick={handleExport} disabled={isDisabled} aria-disabled={isDisabled} title={resultCount === 0 ? t('proposals.export.disabled.tooltip', lang) : undefined}>...</button>` where the children are: `{isLoading && <Loader2 size={14} strokeWidth={2} className="animate-spin" style={{ color: 'var(--teal)' }} aria-hidden={true} />}` followed by `<span>{isLoading ? t('proposals.export.loading', lang) : t('proposals.export.cta', lang)}</span>`. Spinner + label live inside the button (matches FinalizeButton.tsx pattern). Critically: use the existing `.btn-out` CSS class from app/globals.css (do NOT inline border/background) — UI-SPEC §Color locks this as the secondary outline variant.
    - Important difference from FinalizeButton: in `finally` always call `setIsLoading(false)` (FinalizeButton intentionally keeps `isSubmitting=true` during redirect window; export has no redirect — browser handles the file download via Response headers and the action returns).

    Create `app/(authed)/proposals/_components/ExportButton.test.tsx` covering behaviors 1-7. Use `@testing-library/react` (render, screen, fireEvent, waitFor). Mock `sonner` (`vi.mock('sonner', () => ({ toast: { success: vi.fn(), error: vi.fn() } }))`), mock the action import (`vi.mock('../_actions/exportProposals', () => ({ exportProposalsAction: vi.fn(async () => new Response('xlsx', { status: 200 })) }))`), and assert via `getByRole('button')` + `expect(button).toBeDisabled()` / `toHaveAccessibleName(/Exporter/)` etc. For the loading-state test use a deferred promise so the in-flight state is observable before resolution.

    Modify `app/(authed)/proposals/page.tsx` per PATTERNS.md §app/(authed)/proposals/page.tsx (MODIFY):
    - Add `import { ExportButton } from './_components/ExportButton';` to the imports block.
    - Extend the existing `actions={...}` prop of `<PageHero>` (currently wraps only the `Nouvelle proposition` Link) to wrap a flex container containing both the existing Link AND a new `<ExportButton resultCount={initial.rows.length} archived={archived} q={q} lang={lang} />`. Use an inline style: `<div style={{ display: 'inline-flex', gap: 8, alignItems: 'center' }}>` (matches PATTERNS.md template + UI-SPEC §Surface 1 §Placement: gap 8px between primary and secondary).
    - Source of `resultCount`: pass `initial.rows.length` — this is the SSR-resolved page count. Note from PATTERNS.md §app/(authed)/proposals/page.tsx: `initial.rows.length` is the cursor-page count, not the absolute total. For the D-10 disabled-state heuristic, page-count semantics are sufficient — if the page has results, export will too (the inverse: if the page is empty AND there's no cursor, total is 0). Document this in an inline comment so a future reviewer doesn't add an unnecessary count query. (Optional refinement: if `initial.rows.length === 0 && initial.nextCursor === null` confidently signals zero — that's already the page-empty-state SSR contract; reuse it directly as the disabled signal.)
    - Sources of `archived` and `q`: pass the same values already extracted from searchParams in the existing page body. If the existing extractions are not exported variables, pull them out into local `const archived = ...; const q = ...;` before the PageHero JSX.
    - Critical: do NOT remove or restructure the existing `Nouvelle proposition` Link; it remains the primary CTA (`.btn-green`). The export button is the secondary (`.btn-out`) per UI-SPEC §Color §Export CTA button variant.

    Modify `tests/admin-09-grep-contracts.test.ts` to add gate 10 per PATTERNS.md §tests/admin-09-grep-contracts.test.ts (MODIFY) and CONTEXT.md §specifics §EXPORT-02 gate code shape:
    - Add a top-level `describe('Gate 10: XLSX export — commission leakage scan (EXPORT-02)', () => { ... })` block AFTER the existing gates 1-9 (do NOT renumber or modify the existing gates — monotonic extension per ROADMAP §v1.3 Cross-Cutting Invariants #2).
    - Inside the describe block, write one `it('generateProposalsXlsx output contains zero commission strings in any cell, header, or sheet name (parser-based + raw-byte redundancy)')` test that: (a) imports `generateProposalsXlsx` from `'../src/lib/xlsx'` (use relative path — tests/ does NOT have the @/* alias per CONVENTIONS.md §Imports note about tests at tests/admin-09-grep-contracts.test.ts:64); (b) builds a fixture `XlsxExportRow` (single row with lcRef='LC-2026-001', clientName='Test Client', projectName='Project A', amountHt=10000, durationMonths=36, monthlyRent=300, coefficient='3.69%', status='Actif', createdAt=new Date('2026-05-01T00:00:00Z'), expiresAt=null) — the type ITSELF has no commission field, but the test goal is to assert the GENERATED file has no commission string regardless of input shape; (c) `const buf = await generateProposalsXlsx({ rows: [fixture], locale: 'fr' });`; (d) parser-based scan: `const ExcelJS = await import('exceljs');` then `const wb = new ExcelJS.Workbook(); await wb.xlsx.load(buf);` then for-of-loop over `wb.worksheets` asserting `sheet.name` not /commission/i, then `sheet.eachRow(row => row.eachCell(cell => expect(String(cell.value ?? '')).not.toMatch(/commission/i)))`; (e) raw-byte redundancy: `expect(buf.toString('utf-8')).not.toMatch(/commission/i)` (CAVEAT — XLSX is zip-compressed, so the raw `Buffer.toString('utf-8')` will look like garbled bytes; to be meaningful the raw-byte assertion should decompress first OR be skipped — D-05 explicitly says parser-based is the rigor; raw-bytes is the redundancy layer that catches REGEX literals in shared XML strings table. If the raw-byte check produces false positives from garbled bytes, use the existing `assertNoCommissionLeakage(text, label)` helper if it exists in the file, otherwise wrap the raw-byte check in a comment noting limitation and keep parser-based as the primary assertion). Practical resolution: if `assertNoCommissionLeakage` already exists in tests/admin-09-grep-contracts.test.ts (it should, per CONTEXT.md §specifics), reuse it with a meaningful label. Otherwise add ONLY the parser-based assertions and skip raw bytes (parser is sufficient per D-05).
    - The mock setup at the top of the file may need extending if the new gate touches modules not already mocked. The xlsx render adapter is server-only; the test must mock `'server-only'` as no-op (likely already done globally for the suite).

    Verify the suite passes by running `npx vitest run tests/admin-09-grep-contracts.test.ts --reporter=verbose` and confirming the output lists 10 gates total (gates 1-9 unchanged + new gate 10).
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run tests/admin-09-grep-contracts.test.ts app/\(authed\)/proposals/_components/ExportButton.test.tsx app/\(authed\)/proposals/_actions/exportProposals.test.ts src/lib/xlsx/render.test.ts --reporter=verbose
    </automated>
    grep -cE "^\s*describe\('Gate [0-9]+:" tests/admin-09-grep-contracts.test.ts | grep -v '^#' must equal 10 (gates 1-9 + new gate 10);
    grep -n "ExportButton" app/\(authed\)/proposals/page.tsx | grep -v '^#' must show at least 2 hits (import + JSX usage inside PageHero actions);
    grep -n "from 'sonner'" app/\(authed\)/proposals/_components/ExportButton.tsx must show the toast import;
    grep -n "className=\"btn-out\"" app/\(authed\)/proposals/_components/ExportButton.tsx | grep -v '^#' must show 1 hit (UI-SPEC §Color locks .btn-out as the secondary variant — NOT .btn-green which is reserved for primary CTA)
  </verify>
  <done>
    ExportButton.tsx exists with 'use client' line 1, 3-state machine, sonner toast wiring, Loader2 spinner with --teal color, and the 5 UI-SPEC i18n keys; ExportButton.test.tsx covers the 7 client-component behaviors; /proposals page.tsx mounts ExportButton in the PageHero actions slot alongside `Nouvelle proposition` (primary + secondary hierarchy preserved); tests/admin-09-grep-contracts.test.ts has gate 10 (parser-based commission scan); `npx vitest run tests/admin-09-grep-contracts.test.ts` reports 10 describe blocks all green; `npm run typecheck` + `npm run lint` pass; the 9-gate baseline remains green (monotonic extension verified by grep count of describe blocks reaching 10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (`exportProposalsAction`) | Untrusted `archived?: boolean` + `q?: string` input from the URL state crosses here; `userId` is derived server-side from `requireUser()` session, NEVER from request params (T-19-01-01) |
| Server Action → DB (`buildExportQuery`) | Same scoping discipline as `buildListResponse`; ILIKE on `q` MUST go through Drizzle parameter binding, not string concatenation |
| Server Action → exceljs adapter (`generateProposalsXlsx`) | Row projection MUST strip `paramsSnapshot` (which carries `commission_pct`) before reaching the adapter — the `XlsxExportRow` type IS the ADMIN-09 boundary (D-05) |
| Server Action → browser (binary Response) | Filename in `Content-Disposition` MUST be sanitized — `propositions-${todayIso()}.xlsx` is hardcoded prefix + ISO date, no user input; safe by construction (D-04) |
| ESLint config → app code | `exceljs` import restriction (D-02) prevents `commission_pct` leakage paths through accidental ad-hoc XLSX generation outside the audited adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering / IDOR | `buildExportQuery` userId arg | mitigate | userId sourced from `requireUser()` session in `exportProposalsAction`, NEVER from request params or URL state; mirrors `buildListResponse` Phase 8 IDOR discipline; test 7 in Task 2 asserts cross-user data not returned |
| T-19-01-02 | Information Disclosure | XLSX cells / headers / sheet name | mitigate | `XlsxExportRow` type has no commission-bearing field; row projection in `exportProposalsAction` MUST omit `paramsSnapshot` and `commission_pct`; gate 10 in `tests/admin-09-grep-contracts.test.ts` (parser-based ExcelJS scan, D-05) asserts no /commission/i in any cell/header/sheet name; runs in CI on every commit |
| T-19-01-03 | Denial of Service | unbounded SQL query in `buildExportQuery` | mitigate | `MAX_EXPORT_ROWS = 10000` cap defends against pathological cases (real partners have <100 proposals, 100x buffer); enforced at the helper level, not just trusted from caller (D-08) |
| T-19-01-04 | Information Disclosure | SQL injection via `q` ILIKE param | mitigate | `q` passed through existing Drizzle `ilike()` operator (parameter-bound); same path as Phase 17 `buildListResponse`; CONVENTIONS.md §Validation locks Drizzle as the canonical SQL builder |
| T-19-01-05 | Information Disclosure | `Content-Disposition` filename injection | accept | Filename is hardcoded prefix `propositions-` + ISO date from `new Date()` server-side, no user input; safe by construction (D-04) |
| T-19-01-06 | Tampering | accidental `exceljs` import outside audited directory | mitigate | ESLint `no-restricted-imports` blocks `exceljs` outside `src/lib/xlsx/**` (D-02, mirrors `@react-pdf/renderer` pattern at eslint.config.mjs:77-81, 130-141); fails lint at build time |
| T-19-01-07 | Spoofing | Server Action invocation without auth | mitigate | `requireUser()` is the FIRST await in `exportProposalsAction` per PITFALLS §7.3; test 3/4 in Task 2 assert this ordering via shared callSequence pattern (mirrors src/lib/auth/actions.test.ts:84-108) |
| T-19-01-08 | Repudiation | no audit_log entry for exports | accept | D-04 — export is a read operation on already-persisted data; Phase 12 audit_log lifecycle-only convention holds; if compliance need surfaces later, revisit per CONTEXT.md §Deferred Ideas |
| T-19-01-09 | Information Disclosure | console.error in error path | accept | CONVENTIONS.md §Logging: server-side log line `[exportProposalsAction]` carries `err.message` to Vercel/OVH log capture only; user-facing toast returns the bounded i18n string `proposals.export.toast.error`; no enumerated detail to the browser |
| T-19-01-10 | Tampering | dirty `archived` / `q` query params (e.g. injection via deeplink) | mitigate | Server Action parameters typed `archived?: boolean; q?: string`; Next.js Server Actions deserialize these through the framework's RSC payload (not raw URL); `q` is passed to Drizzle `ilike` parameter binding (T-19-01-04) |
| T-19-01-SC | Tampering | npm install `exceljs` supply-chain risk | mitigate | exceljs is MIT-licensed, 3.5M weekly downloads, 11 years of releases, no native deps, no postinstall scripts (verify via `npm view exceljs`); justified in CONTEXT.md §decisions D-01; preferable to `xlsx`/SheetJS (which left npm registry in 2024 and requires a CDN URL). No `[ASSUMED]`/`[SUS]` checkpoint needed — exceljs is well-established. Slopcheck not required (well-known package). If `npm install` produces deprecation warnings or audit findings during execution, surface them in the SUMMARY.
</threat_model>

<verification>
After all 3 tasks complete, run the full Phase 19 Plan 01 verification gate (no shortcut). All must pass before Task complete:

```bash
cd /Users/antoinerousseau/Developer/leasetic-calculator
npm run typecheck                                             # _EnHasAllFrKeys parity proof + strict TS
npm run lint                                                  # no-restricted-imports exceljs guard + JSX text rule
npx vitest run                                                # full suite — confirms 9 baseline gates + new gate 10 + new feature tests all green
node scripts/check-no-vercel-only-imports.sh 2>/dev/null || bash scripts/check-no-vercel-only-imports.sh   # OVH portability defense-in-depth
npm ls exceljs                                                # confirm exceljs resolves to a single version under dependencies
```

Additional grep-level invariants (must pass):
- `grep -rn "from 'exceljs'" src/ app/ tests/ | grep -v node_modules | grep -v '^#'` returns ONLY paths under `src/lib/xlsx/` (other modules have NO direct exceljs import)
- `grep -c "describe('Gate [0-9]" tests/admin-09-grep-contracts.test.ts | grep -v '^#'` equals 10
- `grep -n "import 'server-only'" src/lib/xlsx/render.ts` shows line 1
- `grep -n "MAX_EXPORT_ROWS" src/lib/api/proposals/list.ts | grep -v '^#'` shows the const export
- `grep -rn "commission" src/lib/xlsx/ | grep -v '^#'` returns ZERO hits (commission identifier absent from the entire adapter directory)

Manual sanity check (not a gate, but recommended): start `npm run dev`, log in as a partner with at least one proposal, navigate to `/proposals`, click `Exporter en XLSX`, confirm the file downloads with the expected filename and opens cleanly in LibreOffice/Excel with locale-correct headers + currency formatting. Toggle to `?archived=1` and verify the export reflects the archived rows. Set search `?q=...` and verify the export honors it. Repeat in EN locale to confirm header translation.
</verification>

<success_criteria>
- EXPORT-01 grounded: partner can trigger XLSX export from /proposals; downloaded file is `propositions-YYYY-MM-DD.xlsx` containing 10 fixed columns per UI-SPEC; respects current URL filter (archived + q).
- EXPORT-02 grounded: `tests/admin-09-grep-contracts.test.ts` ships gate 10 (parser-based ExcelJS commission scan per D-05); suite total is 10 gates; gates 1-9 remain green (monotonic extension).
- OVH portability preserved: `exceljs` is importable only from `src/lib/xlsx/**` (ESLint guard mirrors `@react-pdf/renderer` pattern); every file under that directory starts with `import 'server-only'`.
- ADMIN-09 envelope intact: `XlsxExportRow` type has no commission field; the entire `src/lib/xlsx/` directory contains zero `commission` identifiers; the Server Action body contains zero `commission` identifiers; the 10-gate suite proves no commission strings reach the generated XLSX file.
- Light + dark inherited: the `.btn-out` secondary variant + `--teal` spinner color use Phase 16 tokens; no per-component dark CSS added; the partner /proposals route renders correctly in both modes via the data-theme cascade.
- i18n parity preserved: 15 new export keys × 2 branches added to dictionaries.ts; `_EnHasAllFrKeys` compile-time proof stays green (`npm run typecheck` passes).
- No schema migrations: zero changes to `drizzle/migrations/*` or `drizzle/meta/_journal.json`.
- No audit_log expansion: `proposal.export` is NOT a new event type (D-04 — Phase 12 lifecycle-only convention holds).
- Sidebar locked: no 7th sidebar item is added (Phase 18 D-27 lock preserved — Phase 19 Plan 01 is partner-side only, does not touch admin nav).
</success_criteria>

<output>
Create `.planning/phases/19-new-capabilities/19-01-SUMMARY.md` when done. Include:
- Tasks shipped + file inventory (new + modified)
- Gate 10 acceptance evidence (vitest output snippet showing 10 describe blocks)
- Any deviations from UI-SPEC (likely: confirmation that Next 16 Server Action binary Response API works as expected, OR documented alternative shape)
- exceljs version installed + npm audit summary if any warnings surfaced
- Confirmation that the projection ownership decision (option A vs B in Task 2) was chosen and its rationale
- Decisions log entries for any Claude's Discretion items locked during execution (XLSX cell formatting edge cases, projection ownership, raw-byte vs parser-only gate scope)
</output>
