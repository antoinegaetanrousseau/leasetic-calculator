---
phase: 19-new-capabilities
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/lib/db/queries/lc-references.ts
  - src/lib/db/queries/lc-references.test.ts
  - src/lib/i18n/dictionaries.ts
  - src/components/ui/AdminNavCard.tsx
  - app/(admin)/[adminSegment]/lc-references/page.tsx
  - app/(admin)/[adminSegment]/lc-references/page.test.tsx
  - app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx
  - app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.test.tsx
  - app/(admin)/[adminSegment]/page.tsx
  - tests/admin-09-grep-contracts.test.ts
autonomous: true
requirements:
  - LCDASH-01
  - LCDASH-02
tags:
  - lc-references
  - admin
  - dashboard
  - admin-09
must_haves:
  truths:
    - "An admin navigates to /[adminSegment]/lc-references and sees a single styled-table card listing every issued lc_ref across all partners (regardless of proposal status — drafts, active, expired, soft-deleted within 30-day window are all included)"
    - "Each row shows 6 columns: Référence (monospace LC-YYYY-NNN), Partenaire (user.name), Client (inputs.clientName, truncated 1-line), Montant HT (formatted EUR right-aligned), Statut (StatusChip with all 4 displayStatus variants used), Créée le (short date)"
    - "Rows are read-only — no <Link> wrapper, no onClick, no cursor:pointer (D-13; ADMIN-FUT-01 deferred to v1.4+)"
    - "Default sort is created_at DESC (most recent first per D-15)"
    - "Search field at the top of the table card matches `lc_ref ILIKE %q% OR users.name ILIKE %q% OR inputs->>'clientName' ILIKE %q%` per D-14; URL state via ?q="
    - "Cursor pagination shows ~20 rows per page with a Suivant link styled as --teal text link; no page-number display (cursor convention from Phase 17 17-04 + Phase 18 D-12)"
    - "First-run empty state (no issued lc_refs anywhere): heading 'Aucune référence LC pour le moment.' (FR) / 'No LC references yet.' (EN) + body subtitle"
    - "Filter/search-empty state: heading 'Aucune référence ne correspond à votre recherche.' + 'Effacer la recherche →' link clearing ?q="
    - "Admin Home (/[adminSegment]/page.tsx) now renders 4 AdminNavCards (Coefficients / Partenaires / Historique / Références LC) instead of 3; grid is responsive: 1-col mobile / 2-col tablet / 4-col desktop per UI-SPEC §Surface 3"
    - "The new 4th AdminNavCard links to /[adminSegment]/lc-references with title 'Références LC', description per UI-SPEC, lucide-react Hash icon, accent --teal (RGB 45,122,140)"
    - "ADMIN-09 grep-contract suite extends to 12 gates total: gate 11 (LC dashboard active+draft+expired+deleted row render scan) + gate 12 (LC dashboard search-result + empty-state render scan) per CONTEXT.md D-22-Claude-discretion (≥2 LCDASH gates for redundancy across query paths); gates 1-10 stay green (monotonic extension)"
    - "Sidebar nav remains locked at 6 items (Phase 18 D-27 preserved); LC dashboard is reachable ONLY via the 4th AdminNavCard on Admin Home (D-18)"
    - "No schema migration: zero changes to drizzle/migrations/* or drizzle/meta/_journal.json (lc_ref column already exists from Phase 12; partial unique index already in place)"
    - "Both the LC dashboard route and the extended Admin Home ship light + dark variants automatically via the data-theme token cascade"
  artifacts:
    - path: "src/lib/db/queries/lc-references.ts"
      provides: "listLcReferences({ q, cursorEncoded, limit }) cross-partner LC query helper with cursor pagination"
      exports: ["listLcReferences", "LcReferenceRow", "ListLcReferencesParams", "encodeLcRefCursor", "decodeLcRefCursor"]
      contains: "import 'server-only'"
    - path: "src/lib/db/queries/lc-references.test.ts"
      provides: "Vitest coverage: cross-partner scope (no userId filter), search ILIKE on 3 fields, cursor pagination (created_at, id) tuple, deletedAt visibility within 30-day window, displayStatus derivation per row"
    - path: "app/(admin)/[adminSegment]/lc-references/page.tsx"
      provides: "Admin SSR route — requireAdmin defense + dynamic='force-dynamic' + PageHero + LcReferencesList"
      contains: "requireAdmin"
    - path: "app/(admin)/[adminSegment]/lc-references/page.test.tsx"
      provides: "Server-component test: requireAdmin first await, searchParams plumbing, query helper invocation"
    - path: "app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx"
      provides: "Styled-table component mirroring PartnersList.tsx chrome — 6 columns, no overflow menu, read-only rows, search input, cursor pagination, empty states"
      exports: ["LcReferencesList", "LcReferencesListProps"]
    - path: "app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.test.tsx"
      provides: "Render test: 6 columns + status chip + monospace ref + empty states + commission absence"
    - path: "src/components/ui/AdminNavCard.tsx"
      provides: "Extended Variant union to include 'lc-references' (or generic accent prop) per UI-SPEC §Color"
      exports: ["AdminNavCard", "AdminNavCardProps"]
    - path: "app/(admin)/[adminSegment]/page.tsx"
      provides: "Admin Home extended to 4 AdminNavCards in a responsive grid (sm:grid-cols-2 xl:grid-cols-4)"
    - path: "tests/admin-09-grep-contracts.test.ts"
      provides: "Gates 11 + 12 added (LCDASH render + search/empty render scans per LCDASH-02); suite total reaches 12 gates"
      contains: "Gate 11"
  key_links:
    - from: "app/(admin)/[adminSegment]/lc-references/page.tsx"
      to: "src/lib/db/queries/lc-references.ts"
      via: "listLcReferences({ q, cursorEncoded }) returns { rows, hasMore, nextCursor } consumed by LcReferencesList props"
      pattern: "listLcReferences\\("
    - from: "app/(admin)/[adminSegment]/lc-references/page.tsx"
      to: "app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx"
      via: "LcReferencesList rows + nextCursor + lang + adminSegment + currentQ props"
      pattern: "LcReferencesList"
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "app/(admin)/[adminSegment]/lc-references/page.tsx"
      via: "AdminNavCard href={`/${adminSegment}/lc-references`} (4th card after Coefficients / Partenaires / Historique)"
      pattern: "/lc-references"
    - from: "app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx"
      to: "src/components/ui/StatusChip.tsx"
      via: "<StatusChip variant={displayStatus} label={t(`chip.${displayStatus}`, lang)} /> rendered in column 5 for each row (active/draft/expired/deleted)"
      pattern: "StatusChip"
---

<objective>
Ship the centralized cross-partner LC reference dashboard (LCDASH-01, LCDASH-02) per D-11..D-19. Delivers:

1. A new DB query helper `src/lib/db/queries/lc-references.ts` exporting `listLcReferences({ q, cursorEncoded, limit })` — cross-partner (no userId filter, gated upstream by requireAdmin), search ILIKE on `lc_ref` OR `users.name` OR `inputs->>'clientName'` (D-14), default sort `created_at DESC, id DESC` (D-15), cursor pagination via `(createdAt, id)` composite tuple at ~20 rows/page (D-16), includes drafts + soft-deleted within 30-day purge window (D-11, D-12), excludes paramsSnapshot/commission_pct from the returned row shape (ADMIN-09).
2. A new admin SSR route at `app/(admin)/[adminSegment]/lc-references/page.tsx` — `requireAdmin()` defense-in-depth + `dynamic='force-dynamic'` (D-19), composes PageHero (ADMIN eyebrow + locked title/subtitle per UI-SPEC) + LcReferencesList.
3. A new client/server-rendered table component `LcReferencesList.tsx` mirroring `PartnersList.tsx` chrome exactly (TH_BASE_STYLE / TD_BASE_STYLE constants per UI-SPEC §Table Chrome) — 6 columns (Référence/Partenaire/Client/Montant HT/Statut/Créée le), monospace `Référence` cell font, StatusChip in column 5 with all 4 displayStatus variants, read-only rows (no Link wrapper, no overflow menu per D-13), cursor "Suivant" link, empty states for first-run and search-empty (D-17).
4. AdminNavCard variant extension to support a 4th card on Admin Home — extend the variant union with `'lc-references'` (RGB 45,122,140 / `var(--teal)`, same accent as `partners`) per UI-SPEC §Color.
5. Admin Home extension from 3 → 4 AdminNavCards (D-18). Grid changes from `repeat(3, 1fr)` to responsive Tailwind classes `grid-cols-1 sm:grid-cols-2 xl:grid-cols-4` (UI-SPEC §Surface 3). 4th card: title "Références LC", description per UI-SPEC, lucide-react `Hash` icon, href `/${adminSegment}/lc-references`. Sidebar stays locked at 6 items (Phase 18 D-27 preserved — discovery via AdminNavCard, NOT via sidebar 7th item).
6. ~12 new i18n keys × FR+EN in `src/lib/i18n/dictionaries.ts` per UI-SPEC §i18n Keys Summary (verify-and-reuse first per Phase 17 D-21; reuse `admin.home.eyebrow`, `admin.nav.open`, `chip.active`/`.draft`/`.expired`/`.deleted`).
7. ADMIN-09 grep-contract suite extended monotonically: gates 11 + 12 (LC dashboard render scans across active/draft/expired/deleted rows + search-result + empty states per LCDASH-02 + CONTEXT.md §Claude's Discretion recommendation of 2 LCDASH gates for redundancy). Suite total reaches 12 gates; gates 1-10 (including Plan 01's gate 10) stay green.

Purpose: An admin investigating "what was on LC-2026-042?" can find any issued reference across all partners with one search box and one click; the dashboard's read-only design avoids accidentally granting admins access to a partner-scoped /proposals/[id] route (cross-partner admin read view = ADMIN-FUT-01, deferred to v1.4+).

Output: 8 net-new files + 3 modified files (AdminNavCard variant extension, Admin Home grid + 4th card, dictionaries, admin-09-grep-contracts). Both light and dark inherited via Phase 16 token cascade. No schema migrations (lc_ref column + partial unique index already exist from Phase 12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 19 source-of-truth artifacts
@.planning/phases/19-new-capabilities/19-CONTEXT.md
@.planning/phases/19-new-capabilities/19-UI-SPEC.md
@.planning/phases/19-new-capabilities/19-PATTERNS.md
@.planning/phases/19-new-capabilities/19-01-SUMMARY.md

# Carry-forward context — Phase 18 PartnersList chrome is the exact analog
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md

# Codebase docs
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

# Exact analogs the executor needs (PATTERNS.md identifies these as canonical)
@app/(admin)/[adminSegment]/partners/PartnersList.tsx
@app/(admin)/[adminSegment]/partners/page.tsx
@app/(admin)/[adminSegment]/page.tsx
@src/components/ui/AdminNavCard.tsx
@src/components/ui/PageHero.tsx
@src/components/ui/StatusChip.tsx
@src/lib/db/queries/proposal-aggregates.ts
@src/lib/db/queries/proposals.ts
@src/lib/api/proposals/list.ts
@src/lib/auth/require.ts
@src/lib/i18n/dictionaries.ts
@src/lib/i18n/format.ts
@src/db/schema.ts
@tests/admin-09-grep-contracts.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listLcReferences cross-partner DB query helper with cursor pagination + i18n keys</name>
  <files>
    src/lib/db/queries/lc-references.ts,
    src/lib/db/queries/lc-references.test.ts,
    src/lib/i18n/dictionaries.ts
  </files>
  <read_first>
    src/lib/db/queries/proposal-aggregates.ts (exact analog per PATTERNS.md — 'server-only' line 1, drizzle imports, module JSDoc with D-NN refs, getMonthlyProposalCountAll cross-partner pattern that drops userId filter),
    src/lib/db/queries/proposals.ts (deriveDisplayStatus signature + DisplayStatus union: 'active' | 'draft' | 'expired' | 'deleted'; createDraft lc_ref allocation behavior from Phase 17 D-03),
    src/lib/api/proposals/list.ts (existing buildListResponse — cursor encoding/decoding pattern via base64({createdAt, id}) tuple; mapping pattern that excludes paramsSnapshot/commission_pct),
    src/db/schema.ts (proposals table shape: id uuid, userId uuid, lcRef text nullable + partial unique index proposals_user_id_lc_ref_uq WHERE lc_ref IS NOT NULL, status enum draft|active|deleted, deletedAt timestamp nullable, finalizedAt, validityDays, inputs jsonb with clientName, paramsSnapshot jsonb with commission_pct, createdAt; users table: id, name, role, status),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §i18n Keys Summary §LC dashboard surface (~12 keys to add — admin.lcReferences.title/subtitle/search.placeholder/col.reference/col.partner/col.client/col.amountHt/col.status/col.createdAt/empty.firstRun/empty.firstRun.body/empty.search/empty.search.clear + admin.nav.lcReferences.title/.description = 14 keys total; UI-SPEC has the locked FR + EN text for each),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-11 (drafts included), D-12 (soft-deleted within 30 days included), D-14 (search 3 fields), D-15 (sort), D-16 (cursor pagination) + §specifics §LCDASH SQL outline,
    .planning/phases/19-new-capabilities/19-PATTERNS.md §src/lib/db/queries/lc-references.ts,
    .planning/phases/17-partner-surfaces/17-CONTEXT.md §D-21 (verify-and-reuse i18n discipline)
  </read_first>
  <behavior>
    - Test 1: listLcReferences({}) returns rows from ALL users (no userId filter); fixture DB with 3 users having 2/3/1 proposals each (with lc_ref allocated) returns 6 rows
    - Test 2: each returned row's shape is `LcReferenceRow` with keys exactly: id, lcRef, partnerName, clientName, amountHt, displayStatus, createdAt — and NO commission* or paramsSnapshot* key (commission scrub per ADMIN-09)
    - Test 3: rows are sorted by createdAt DESC, id DESC (D-15) — given fixtures with explicit timestamps, the returned order matches the expected DESC sort
    - Test 4: drafts ARE included (D-11) — fixture with status='draft' and lc_ref allocated appears in results with displayStatus 'draft' (verified via deriveDisplayStatus per row)
    - Test 5: soft-deleted within 30-day window ARE included (D-12) — fixture with deletedAt = NOW() - 5 days appears with displayStatus 'deleted'; a fixture with deletedAt = NOW() - 40 days is EXCLUDED (eventual consistency with Phase 10 purge cron 30-day window)
    - Test 6: search q='LC-2026' matches by lc_ref; q='Memento' matches by users.name; q='Dupont' matches by inputs->>'clientName' (D-14 — 3 ILIKE conditions OR'd)
    - Test 7: search q='' (or undefined) returns ALL rows (no filter applied)
    - Test 8: cursor pagination — listLcReferences({ limit: 2 }) on 5-row fixture returns { rows: 2 rows, hasMore: true, nextCursor: base64-encoded (createdAt,id) tuple of row[1] }; a follow-up call with cursorEncoded=nextCursor returns the next 2 rows
    - Test 9: rows with lc_ref IS NULL are NEVER returned (only issued references appear — D-11 literal wording "every issued LC reference"; isNotNull filter)
    - Test 10: encodeLcRefCursor + decodeLcRefCursor round-trip — given { createdAt: Date, id: string }, encode then decode returns the same values; decode of an invalid string returns null (not throws) for graceful fallback
    - Test 11: dictionaries.ts FR branch contains all 14 net-new keys from UI-SPEC §i18n Keys Summary §LC dashboard; npm run typecheck stays green (_EnHasAllFrKeys parity proof)
  </behavior>
  <action>
    Create `src/lib/db/queries/lc-references.ts` per PATTERNS.md §src/lib/db/queries/lc-references.ts:
    - Line 1: `import 'server-only';`
    - Imports from `drizzle-orm`: `and, isNotNull, or, ilike, desc, lt, sql, eq` (sql for jsonb path `inputs->>'clientName'` ILIKE; lt for the cursor tuple comparison; eq for the users JOIN ON clause).
    - Import `{ db, schema }` from `@/lib/db`.
    - Import `deriveDisplayStatus, type DisplayStatus` from `@/lib/db/queries/proposals` (Phase 12 helper — handles the 'expired' derivation from `finalizedAt + validityDays < now()` plus the status enum + deletedAt mapping).
    - Module JSDoc per CONVENTIONS.md §Comments with Phase 19 Plan 02 reference + D-11/D-12/D-13/D-14/D-15/D-16 callouts + T-19-02-01 (no userId scoping — admin-only consumer; upstream gate is requireAdmin in the SSR route + admin layout; this helper intentionally drops per-partner userId filtering, mirrors `getMonthlyProposalCountAll` cross-partner pattern from Phase 18) + ADMIN-09 invariant (paramsSnapshot bearing commission_pct MUST NOT appear in LcReferenceRow).
    - Export interfaces:
      - `LcReferenceRow` with keys: `id: string; lcRef: string; partnerName: string; clientName: string; amountHt: number; displayStatus: DisplayStatus; createdAt: Date`. Critically: NO commissionPct, NO paramsSnapshot, NO any other commission-bearing field.
      - `ListLcReferencesParams` with optional `q?: string; cursorEncoded?: string | null; limit?: number`.
      - `LcReferenceCursor` with `createdAt: Date; id: string` (internal cursor shape, may be inline).
    - Export `encodeLcRefCursor(cursor: { createdAt: Date; id: string }): string` returning `Buffer.from(JSON.stringify({ createdAt: cursor.createdAt.toISOString(), id: cursor.id })).toString('base64')` (mirror Phase 8 / Phase 17 cursor convention — read the existing implementation in src/lib/api/proposals/list.ts and copy the same encoding shape to ensure cursor format consistency across the codebase). If a shared encoder already lives in `@/lib/api/proposals/list.ts` (e.g. `encodeCursor`/`decodeCursor`), import and reuse instead of duplicating — verify by grep during execution per CONVENTIONS.md §Module Design barrel discipline.
    - Export `decodeLcRefCursor(encoded: string | null | undefined): { createdAt: Date; id: string } | null` — base64 decode + JSON.parse + Date hydration; on any error return null (graceful fallback — tests 10 asserts this contract).
    - Export async `listLcReferences(params: ListLcReferencesParams): Promise<{ rows: LcReferenceRow[]; hasMore: boolean; nextCursor: string | null }>` that:
      - Lazy-acquires the db singleton: `const dbi = db();` (CONVENTIONS.md §Module Design lazy singleton pattern — mirrors `src/lib/db/queries/proposal-aggregates.ts:133`).
      - Computes `const limit = params.limit ?? 20;` (D-16 page size).
      - Decodes cursor: `const cursor = decodeLcRefCursor(params.cursorEncoded ?? null);`.
      - Computes search predicates: trim params.q; if non-empty, build an `or(ilike(schema.proposals.lcRef, \`%${q}%\`), ilike(schema.users.name, \`%${q}%\`), sql\`${schema.proposals.inputs}->>'clientName' ILIKE ${'%' + q + '%'}\`)` clause. Use parameter-bound sql\`\` for the jsonb path; verify Drizzle's sql template literal interpolation syntax during execution (the existing buildListResponse may already do something like this for the partner-side q on inputs.clientName — read list.ts to confirm the exact syntax).
      - Computes cursor predicate: if cursor present, `lt((schema.proposals.createdAt, schema.proposals.id), (cursor.createdAt, cursor.id))` — Drizzle's tuple comparison may require an inline sql\`\` clause: `sql\`(${schema.proposals.createdAt}, ${schema.proposals.id}) < (${cursor.createdAt.toISOString()}, ${cursor.id})\``. Verify against the existing buildListResponse cursor implementation in list.ts — copy the exact tuple-comparison shape used there (no novel SQL invention; Phase 8 + Phase 17 + Phase 18 already established this primitive).
      - Computes deletedAt visibility: include rows where `deletedAt IS NULL` OR `deletedAt >= NOW() - INTERVAL '30 days'` (D-12 — soft-deleted within 30-day purge window per Phase 10). Use `or(isNull(schema.proposals.deletedAt), gte(schema.proposals.deletedAt, sql\`NOW() - INTERVAL '30 days'\`))` — confirm Phase 17 D-12 archived semantics implementation in src/lib/api/proposals/list.ts uses the same SQL pattern; reuse the same expression for consistency.
      - Always requires `isNotNull(schema.proposals.lcRef)` per D-11 ("every issued LC reference"; rows without an allocated lc_ref are not relevant to the dashboard).
      - Composes the conditions with `and(...)`.
      - Executes a Drizzle select with INNER JOIN: `select { id, lcRef, partnerName: users.name, clientNameRaw: sql<string>\`${schema.proposals.inputs}->>'clientName'\`, amountHtRaw: sql<string>\`${schema.proposals.inputs}->>'amountHt'\`, status, deletedAt, finalizedAt, validityDays, createdAt } from proposals INNER JOIN users on users.id = proposals.userId WHERE <conditions> ORDER BY createdAt DESC, id DESC LIMIT (limit + 1)`. The `+1` is the standard hasMore detection trick.
      - Projects raw rows to `LcReferenceRow` shape: `partnerName` from users.name, `clientName` from clientNameRaw, `amountHt` from `Number(amountHtRaw)`, `displayStatus` from `deriveDisplayStatus(row)` (Phase 12 helper handles deletedAt + status + finalizedAt+validityDays expiry). CRITICAL: the projection MUST NOT include paramsSnapshot or any commission_pct field (ADMIN-09 boundary; mirror buildListResponse's projection scrub at list.ts:143-148).
      - Computes `hasMore = projectedRows.length > limit`; trims to `rows = projectedRows.slice(0, limit)`; if hasMore, `nextCursor = encodeLcRefCursor({ createdAt: rows[rows.length-1].createdAt, id: rows[rows.length-1].id })`; otherwise `nextCursor = null`.
      - Returns `{ rows, hasMore, nextCursor }`.

    Create `src/lib/db/queries/lc-references.test.ts` covering the 10 behaviors above. Mock `'server-only'` as no-op. Mock the `db()` singleton with a Drizzle-compatible fake OR use the existing `__resetDbForTests` pattern from CONVENTIONS.md if a real DB fixture is established (read proposal-aggregates.test.ts for the existing test pattern; if proposal-aggregates uses an in-memory PGlite fixture or a Vitest-mocked Drizzle query builder, copy the same approach). The cursor round-trip test (10) can be a pure unit test bypassing the DB.

    Modify `src/lib/i18n/dictionaries.ts` to add the 14 net-new keys per UI-SPEC §i18n Keys Summary §LC dashboard surface (locked FR + EN text). Verify-and-reuse first per Phase 17 D-21 — grep existing dictionary for any `admin.lcReferences.`, `admin.nav.lcReferences.`, `admin.references.`, `lc.references.` keys (expect zero hits — these are net-new). Reuse confirmed (do NOT re-add): `admin.home.eyebrow`, `admin.nav.open`, `chip.active`, `chip.draft`, `chip.expired`, `chip.deleted`. Keys to add (per UI-SPEC):
    - `admin.lcReferences.title` — FR "Références LC" / EN "LC References"
    - `admin.lcReferences.subtitle` — FR "Toutes les références LC émises, tous partenaires confondus." / EN "All LC references issued across all partners."
    - `admin.lcReferences.search.placeholder` — FR "Rechercher par référence, partenaire ou client…" / EN "Search by reference, partner or client…"
    - `admin.lcReferences.col.reference` — FR "Référence" / EN "Reference"
    - `admin.lcReferences.col.partner` — FR "Partenaire" / EN "Partner"
    - `admin.lcReferences.col.client` — FR "Client" / EN "Client"
    - `admin.lcReferences.col.amountHt` — FR "Montant HT" / EN "Amount HT"
    - `admin.lcReferences.col.status` — FR "Statut" / EN "Status"
    - `admin.lcReferences.col.createdAt` — FR "Créée le" / EN "Created"
    - `admin.lcReferences.empty.firstRun` — FR "Aucune référence LC pour le moment." / EN "No LC references yet."
    - `admin.lcReferences.empty.firstRun.body` — FR "Les références apparaîtront ici dès qu'un partenaire créera une première proposition." / EN "References will appear here once a partner creates their first proposal."
    - `admin.lcReferences.empty.search` — FR "Aucune référence ne correspond à votre recherche." / EN "No references match your search."
    - `admin.lcReferences.empty.search.clear` — FR "Effacer la recherche →" / EN "Clear search →"
    - `admin.nav.lcReferences.title` — FR "Références LC" / EN "LC References"
    - `admin.nav.lcReferences.description` — FR "Voir toutes les références LC émises par les partenaires." / EN "View all LC references issued by partners."
    Note: that is 14 keys — 13 admin.lcReferences.* + 2 admin.nav.lcReferences.* = 15 keys total. Recount during execution; UI-SPEC §i18n Keys Summary lists the canonical set. Add to both fr and en branches in the same edit (the _EnHasAllFrKeys compile-time proof will fail at tsc if parity is broken).

    Implementation order: dictionaries first (or in parallel — they don't depend on the query helper); then `lc-references.ts`; then the test file. After both source files exist, `npx vitest run src/lib/db/queries/lc-references.test.ts` should pass.
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run src/lib/db/queries/lc-references.test.ts --reporter=verbose
    </automated>
    grep -c "admin.lcReferences." src/lib/i18n/dictionaries.ts | grep -v '^#' must be >= 26 (13 keys × 2 branches);
    grep -c "admin.nav.lcReferences." src/lib/i18n/dictionaries.ts | grep -v '^#' must be >= 4 (2 keys × 2 branches);
    head -1 src/lib/db/queries/lc-references.ts must equal `import 'server-only';`;
    grep -c "commission" src/lib/db/queries/lc-references.ts | grep -v '^#' must equal 0;
    grep -n "isNotNull" src/lib/db/queries/lc-references.ts | grep -v '^#' must show at least one use (D-11 lc_ref IS NOT NULL filter)
  </verify>
  <done>
    src/lib/db/queries/lc-references.{ts, test.ts} exist with listLcReferences exported; LcReferenceRow type has NO commission field (grep gate above); the 14-15 new admin.lcReferences.* and admin.nav.lcReferences.* keys exist in both fr and en branches of dictionaries.ts; `npm run typecheck` (firing _EnHasAllFrKeys) stays green; `npx vitest run src/lib/db/queries/lc-references.test.ts` reports all 11 tests passing; `npm run lint` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LcReferencesList styled-table component + admin SSR route + AdminNavCard variant extension</name>
  <files>
    src/components/ui/AdminNavCard.tsx,
    app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx,
    app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.test.tsx,
    app/(admin)/[adminSegment]/lc-references/page.tsx,
    app/(admin)/[adminSegment]/lc-references/page.test.tsx
  </files>
  <read_first>
    app/(admin)/[adminSegment]/partners/PartnersList.tsx (exact analog per PATTERNS.md — 277 lines; TH_BASE_STYLE + TD_BASE_STYLE constants at lines 41-59; props interface at 28-39; empty-state pattern at 96-147; table chrome at 149-275; cursor pagination Link at 257-274),
    app/(admin)/[adminSegment]/partners/page.tsx (admin SSR route analog; requireAdmin first await; dynamic='force-dynamic'; PageHero composition; PartnersList prop wiring),
    app/(admin)/[adminSegment]/page.tsx (current Admin Home — 3 AdminNavCards in `repeat(3, 1fr)` grid at lines ~160-193 per PATTERNS.md; lucide imports at line ~3; this task does NOT modify Admin Home — that's Task 3),
    src/components/ui/AdminNavCard.tsx (current variant union at lines 16-32; ACCENT_BY_VARIANT record at 28-32; openLabel prop; icon prop typed as LucideIcon),
    src/components/ui/PageHero.tsx (eyebrow/title/subtitle/actions props; no actions used here since LC dashboard is read-only — D-13),
    src/components/ui/StatusChip.tsx (existing variant union: active/draft/expired/deleted/invited; LC dashboard uses only the first 4 — invited is partner-status only),
    src/lib/auth/require.ts (requireAdmin signature + AUTH-15 defense-in-depth note),
    src/lib/i18n/format.ts (formatCurrency, formatDate signatures — Task 2 uses these for Montant HT and Créée le columns),
    src/lib/db/queries/lc-references.ts (Task 1 output — LcReferenceRow type + listLcReferences signature),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §Surface 2 (entire section — PageHero copy, search field, table chrome, 6 column definitions, StatusChip mapping, pagination, empty states) + §Surface 3 §4th Card Spec (icon Hash, accent RGB 45,122,140, openLabel reuse, href pattern),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-13 (read-only rows), D-17 (columns), D-18 (4th AdminNavCard), D-19 (admin SSR route shape),
    .planning/phases/19-new-capabilities/19-PATTERNS.md §LcReferencesList + §page.tsx + §AdminNavCard sections (each has the analog file + module JSDoc template + props interface + key differences from analog)
  </read_first>
  <behavior>
    - Test 1: LcReferencesList rendered with 3 LcReferenceRow fixtures + lang='fr' + adminSegment='admin-secret' + nextCursor='abc123' renders a <table> with exactly 1 thead row + 3 tbody rows + 6 columns
    - Test 2: column headers match the locked FR text from UI-SPEC §Surface 2 §Column Definitions (Référence / Partenaire / Client / Montant HT / Statut / Créée le)
    - Test 3: column 1 (Référence) cell uses monospace font (`fontFamily: "ui-monospace, 'Courier New', monospace"`) at fontSize 13 fontWeight 500 color var(--ink) per UI-SPEC §Surface 2 §Column Definitions
    - Test 4: column 4 (Montant HT) is right-aligned (`textAlign: 'right'`) and uses `formatCurrency(value, lang)` formatting (FR: "12 000 €", EN: "€12,000")
    - Test 5: column 5 (Statut) renders <StatusChip variant={displayStatus} label={t(`chip.${displayStatus}`, lang)} /> per UI-SPEC §Surface 2 §StatusChip Mapping — fixture with displayStatus='active' renders chip-active; displayStatus='draft' renders chip-draft; displayStatus='expired' renders chip-expired; displayStatus='deleted' renders chip-deleted
    - Test 6: column 6 (Créée le) uses `formatDate(createdAt, lang)`
    - Test 7: rows do NOT have an onClick handler, do NOT wrap content in a <Link>, do NOT have cursor:pointer in any style (D-13 read-only — assertion: querySelector for tr does not have role="link", `tr.style.cursor !== 'pointer'`, no anchor descendants in tbody rows except in the pagination row)
    - Test 8: when rows.length === 0 and currentQ is undefined/'', renders the first-run empty state — heading with t('admin.lcReferences.empty.firstRun', 'fr') and body with t('admin.lcReferences.empty.firstRun.body', 'fr')
    - Test 9: when rows.length === 0 and currentQ='Dupont' is provided, renders the search-empty state — heading with t('admin.lcReferences.empty.search', 'fr') + clear link "Effacer la recherche →" pointing to `/${adminSegment}/lc-references` (no q param)
    - Test 10: when nextCursor is non-null, renders a "Suivant" link with href `/${adminSegment}/lc-references?cursor=...` (URL-encoded), styled with color var(--teal); when nextCursor is null, no Suivant link is rendered
    - Test 11: AdminNavCard with variant='lc-references' renders with accent color RGB 45,122,140 (the same teal token as the partners variant); the icon prop accepts the lucide-react Hash component; href prop renders as <Link href="/admin-secret/lc-references"> for example adminSegment='admin-secret'
    - Test 12: admin SSR route page.tsx — vi.mock requireAdmin to throw if not admin → page rendering throws; vi.mock requireAdmin to resolve → page renders PageHero with eyebrow='ADMIN', title='Références LC', subtitle (locked FR text), no actions prop; LcReferencesList is rendered with rows from listLcReferences
    - Test 13: admin SSR route honors searchParams — `searchParams: { q: 'Memento', cursor: 'abc' }` passes through to `listLcReferences({ q: 'Memento', cursorEncoded: 'abc' })`
    - Test 14: admin SSR route has `export const dynamic = 'force-dynamic'` (D-19)
  </behavior>
  <action>
    Modify `src/components/ui/AdminNavCard.tsx` per PATTERNS.md §src/components/ui/AdminNavCard.tsx (MODIFY) and UI-SPEC §Surface 3 §Color §LC dashboard 4th AdminNavCard accent:
    - Extend the existing `Variant` type union from `'coefficients' | 'partners' | 'history'` to `'coefficients' | 'partners' | 'history' | 'lc-references'`.
    - Extend the `ACCENT_BY_VARIANT` record at lines 28-32 to include the new variant: `'lc-references': { rgb: '45, 122, 140', token: 'var(--teal)' }` (same teal as `partners` per UI-SPEC — `Hash` icon represents reference registry, teal is the closest semantic match). Position the new entry at the end of the record for diff readability.
    - Do NOT refactor to a generic `accent` prop — UI-SPEC §Color notes this as an alternative but the variant-union extension is the minimal-change path; defer the generic refactor unless there's a reason to touch the file otherwise (CONVENTIONS.md §Module Design: minimal surface area).
    - All existing tests for AdminNavCard must continue to pass (regression discipline). If existing tests assert the variant union exhaustively (e.g. switch with no-default), the new variant may need to be added to those test cases.

    Create `app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx` per PATTERNS.md §LcReferencesList.tsx (which is the longest pattern entry with module JSDoc + TH/TD constants + props + empty state + table chrome + pagination — all verbatim from PartnersList.tsx except the differences listed below):
    - This is a SERVER component (NOT a client component) — there's no 'use client' directive. Server components can compose <Link> + <StatusChip> + inline styles without interactivity. Mirrors PartnersList.tsx which is also a server component.
    - Module JSDoc per CONVENTIONS.md §Comments with Phase 19 Plan 02 + D-17 (6 columns) + D-13 (read-only rows) + D-16 (cursor pagination) + ADMIN-09 invariant note (LcReferenceRow shape has no commission_pct field; deriveDisplayStatus already bounds the rendered union).
    - Imports: `React` from 'react'; `Link` from 'next/link'; `t, type Lang` from `@/lib/i18n/dictionaries`; `formatDate, formatCurrency` from `@/lib/i18n/format`; `StatusChip` from `@/components/ui/StatusChip`; `type LcReferenceRow` from `@/lib/db/queries/lc-references`.
    - Copy the `TH_BASE_STYLE` and `TD_BASE_STYLE` constants verbatim from `PartnersList.tsx` lines 41-59 (UI-SPEC §Surface 2 §Table Chrome locks them as identical — do NOT extract to a shared module in this plan; defer that refactor to a follow-up if more admin tables emerge).
    - Define and export the props interface `LcReferencesListProps` per PATTERNS.md: `{ rows: LcReferenceRow[]; nextCursor: string | null; lang: Lang; adminSegment: string; currentQ?: string }`.
    - Export `function LcReferencesList({ rows, nextCursor, lang, adminSegment, currentQ }: LcReferencesListProps)` implementing:
      - Empty states FIRST (early return) per PATTERNS.md template:
        - If `rows.length === 0 && currentQ && currentQ.length > 0`: return the search-empty state per UI-SPEC §Surface 2 §Empty States §Filter/search-empty — `<section className="card" style={{ textAlign: 'center', padding: '40px 24px' }}>` with body `t('admin.lcReferences.empty.search', lang)` and an inline clear-link to `/${adminSegment}/lc-references` (no q param) styled `color: var(--teal); font-weight: 500`. Use `<Link>` (next/link) for the clear-link.
        - Else if `rows.length === 0`: return the first-run empty state — heading `t('admin.lcReferences.empty.firstRun', lang)` + body `t('admin.lcReferences.empty.firstRun.body', lang)` styled per UI-SPEC §Empty States §First-run.
      - Else render the table inside `<section className="card" style={{ padding: 0, overflow: 'hidden' }}>` per PATTERNS.md template:
        - `<table style={{ width: '100%', borderCollapse: 'collapse', tableLayout: 'auto' }}>` with `<thead>` containing one `<tr>` of 6 `<th scope="col">` cells using `{ ...TH_BASE_STYLE, width: <colWidth> }` per the locked widths from UI-SPEC §Surface 2 §Column Definitions: Référence 160px, Partenaire auto, Client auto (with truncation styles on td), Montant HT 140px (`textAlign: 'right'` on both th and td), Statut 120px, Créée le 120px. Header text via `t('admin.lcReferences.col.<key>', lang)`.
        - `<tbody>` mapping `rows` with `key={row.id}`; each row is a `<tr>` (NOT wrapped in a Link, NOT with onClick — D-13). Each `<td>` uses `{ ...TD_BASE_STYLE, borderBottom: lastRow ? 'none' : '1px solid var(--border)' }` (lastRow detection via `idx === rows.length - 1`) — mirrors PartnersList rendering at lines 220-275.
        - Column 1 cell (Référence): override TD style with `fontFamily: "ui-monospace, 'Courier New', monospace"`, `fontSize: 13`, `fontWeight: 500`, `color: 'var(--ink)'` per UI-SPEC §Surface 2 §Column Definitions row 1.
        - Column 2 cell (Partenaire): font 13px weight 500 color var(--ink); inline override.
        - Column 3 cell (Client): font 13px weight 400 color var(--muted); inline override with truncation `textOverflow: 'ellipsis', whiteSpace: 'nowrap', overflow: 'hidden', maxWidth: 220` per UI-SPEC §Surface 2 §Column Definitions row 3.
        - Column 4 cell (Montant HT): `textAlign: 'right'`, font 13px weight 600 color var(--ink); value via `formatCurrency(row.amountHt, lang)`.
        - Column 5 cell (Statut): `<StatusChip variant={row.displayStatus} label={t(\`chip.${row.displayStatus}\`, lang)} />`. The StatusChip variant union from src/components/ui/StatusChip.tsx already includes active/draft/expired/deleted; if the variant naming differs (e.g. StatusChip variant is `'archived'` not `'deleted'`), verify against the actual component during execution and adjust the mapping table per UI-SPEC §Surface 2 §StatusChip Mapping (the UI-SPEC table is the authoritative mapping).
        - Column 6 cell (Créée le): font 13px weight 400 color var(--muted); value via `formatDate(row.createdAt, lang)`.
      - Pagination row AFTER the table, INSIDE the same .card section: `{nextCursor && (<div style={{ display: 'flex', justifyContent: 'flex-end', padding: '12px 20px', borderTop: '1px solid var(--border)' }}><Link href={\`/${adminSegment}/lc-references?cursor=${encodeURIComponent(nextCursor)}${currentQ ? \`&q=${encodeURIComponent(currentQ)}\` : ''}\`} style={{ color: 'var(--teal)', fontWeight: 500, fontSize: 14, textDecoration: 'none' }}>{t('proposal.list.load.more', lang)}</Link></div>)}` — verify the `proposal.list.load.more` key exists in dictionaries.ts (Phase 8 / Phase 17 already established cursor pagination i18n; if the existing key is differently named, use whatever PartnersList uses — read PartnersList.tsx lines 257-274 to confirm the exact i18n key for the "Suivant" / "Load more" link and reuse it for consistency).

    Create `app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.test.tsx` covering behaviors 1-10. Render with @testing-library/react. Mock 'server-only' as no-op.

    Create `app/(admin)/[adminSegment]/lc-references/page.tsx` per PATTERNS.md §app/(admin)/[adminSegment]/lc-references/page.tsx:
    - Imports per PATTERNS.md template: `Metadata` from 'next'; `requireAdmin` from `@/lib/auth/require`; `getCurrentLang, t` from `@/lib/i18n`; `PageHero` from `@/components/ui/PageHero`; `LcReferencesList` from `./_components/LcReferencesList`; `listLcReferences` from `@/lib/db/queries/lc-references`.
    - Export `const dynamic = 'force-dynamic';` per D-19.
    - Export `const metadata: Metadata = { title: 'Références LC — Leasétic Matrice', robots: { index: false, follow: false } };` — title in FR (the metadata title is server-rendered before locale resolution; matches Phase 18 admin pages convention). The `robots: { index: false, follow: false }` matches the admin URL secrecy invariant (no SEO indexing).
    - Props interface: `interface PageProps { params: Promise<{ adminSegment: string }>; searchParams: Promise<{ q?: string; cursor?: string }> }` (Next.js 16 async params + searchParams pattern).
    - Export default async function `LcReferencesPage({ params, searchParams }: PageProps)` implementing:
      - `const { adminSegment } = await params;` — Next.js 16 PITFALL §1.1 async params unwrap.
      - `await requireAdmin();` — AUTH-15 defense-in-depth (the (admin) layout already gates, but per CONVENTIONS.md §Error Handling auth-first invariant every authed page calls requireUser/requireAdmin independently).
      - `const lang = await getCurrentLang();`
      - `const sp = await searchParams; const q = sp.q ?? ''; const cursor = sp.cursor ?? null;`
      - `const { rows, nextCursor } = await listLcReferences({ q: q || undefined, cursorEncoded: cursor });` (pass undefined when q is empty so the query helper short-circuits the search predicate).
      - Return JSX: a wrapper div containing `<PageHero eyebrow={t('admin.home.eyebrow', lang)} title={t('admin.lcReferences.title', lang)} subtitle={t('admin.lcReferences.subtitle', lang)} />` (no `actions` prop — D-13 read-only surface, no page-level CTA per UI-SPEC §Surface 2 §PageHero) followed by `<LcReferencesList rows={rows} nextCursor={nextCursor} lang={lang} adminSegment={adminSegment} currentQ={q} />`.
      - Search input UI: the UI-SPEC §Surface 2 §Search Field locks "URL-driven via ?q= query param (same pattern as /proposals?q=)" with the placeholder text key `admin.lcReferences.search.placeholder`. Implementation decision during execution: either (a) the search input is rendered INSIDE LcReferencesList as part of the card, OR (b) the search input is rendered in page.tsx as a sibling to LcReferencesList. Read Phase 18 PartnersList for the established pattern (likely a SearchBar sibling component). Reuse `src/components/proposals/SearchBar.tsx` or its admin equivalent if one exists; otherwise create a minimal `<form method="get" action={\`/${adminSegment}/lc-references\`}><input name="q" defaultValue={currentQ} placeholder={t('admin.lcReferences.search.placeholder', lang)} /></form>` inside the LcReferencesList card. UI-SPEC says "form-submit — URL-driven SSR re-render preferred (no client JS required for basic search, consistent with /proposals pattern)" — go with the form-submit approach; debounce is deferred.

    Create `app/(admin)/[adminSegment]/lc-references/page.test.tsx` covering behaviors 12-14. Mock `@/lib/auth/require`, `@/lib/i18n`, `@/lib/db/queries/lc-references`, 'server-only'. Use the pattern from `app/(admin)/[adminSegment]/page.test.tsx` (PATTERNS.md identifies it as the closest analog).
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run "app/(admin)/[adminSegment]/lc-references" src/components/ui/AdminNavCard.test.tsx --reporter=verbose
    </automated>
    grep -n "lc-references" src/components/ui/AdminNavCard.tsx | grep -v '^#' must show at least 2 hits (Variant union extension + ACCENT_BY_VARIANT entry);
    grep -n "requireAdmin" app/\(admin\)/\[adminSegment\]/lc-references/page.tsx | grep -v '^#' must show at least 1 hit;
    grep -n "export const dynamic = 'force-dynamic'" app/\(admin\)/\[adminSegment\]/lc-references/page.tsx | grep -v '^#' must show 1 hit;
    grep -c "commission" app/\(admin\)/\[adminSegment\]/lc-references/ | grep -v '^#' must equal 0;
    grep -n "ui-monospace" app/\(admin\)/\[adminSegment\]/lc-references/_components/LcReferencesList.tsx | grep -v '^#' must show 1 hit (monospace Référence column per UI-SPEC);
    grep -n "StatusChip" app/\(admin\)/\[adminSegment\]/lc-references/_components/LcReferencesList.tsx | grep -v '^#' must show at least 1 import + 1 usage hit
  </verify>
  <done>
    AdminNavCard.tsx has the new 'lc-references' variant; LcReferencesList.tsx renders the locked 6-column table with monospace Référence + StatusChip + cursor pagination + empty states; page.tsx is an admin SSR route with requireAdmin defense + dynamic='force-dynamic' + PageHero (no actions) + LcReferencesList; page.test.tsx + LcReferencesList.test.tsx cover behaviors 1-14; `npm run typecheck` + `npm run lint` pass; all vitest runs green; commission identifier absent from the entire lc-references directory.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Admin Home to 4 AdminNavCards (responsive grid 3→4) + add ADMIN-09 gates 11 + 12 (LC dashboard render scans)</name>
  <files>
    app/(admin)/[adminSegment]/page.tsx,
    tests/admin-09-grep-contracts.test.ts
  </files>
  <read_first>
    app/(admin)/[adminSegment]/page.tsx (current Admin Home — lucide imports at line ~3 already include Sliders, Users, History, Plus; AdminNavCard grid at lines ~160-193 currently uses `repeat(3, 1fr)` per PATTERNS.md),
    src/components/ui/AdminNavCard.tsx (Task 2 output — variant='lc-references' now available with --teal accent),
    src/lib/i18n/dictionaries.ts (Task 1 output — admin.nav.lcReferences.title + .description + admin.nav.open keys all present),
    tests/admin-09-grep-contracts.test.ts (existing gates 1-9 + gate 10 from Plan 01; vi.mock patterns; assertNoCommissionLeakage helper if present; renderToString + createElement pattern; the existing structure for adding new describe blocks WITHOUT renumbering or modifying prior gates — monotonic extension invariant from ROADMAP v1.3 invariants #2),
    src/lib/db/queries/lc-references.ts (Task 1 output — LcReferenceRow type shape; needed for the gate 11/12 fixture rows),
    .planning/phases/19-new-capabilities/19-UI-SPEC.md §Surface 3 (responsive grid Tailwind classes, 4th card spec including icon Hash + href pattern + accent --teal + reuse of admin.nav.open openLabel key) + §Surface 2 (entire LcReferencesList structure that gate 11/12 will render-scan),
    .planning/phases/19-new-capabilities/19-CONTEXT.md §decisions D-18 + §Claude's Discretion (LCDASH-02 exact gate count recommendation: 2 LCDASH gates — list render + search-result render — for redundancy across query paths),
    .planning/phases/19-new-capabilities/19-PATTERNS.md §app/(admin)/[adminSegment]/page.tsx (MODIFY) + §tests/admin-09-grep-contracts.test.ts (MODIFY) — the latter has the gate 11 code shape and the lc-references vi.mock setup
  </read_first>
  <behavior>
    - Test 1: Admin Home page.tsx renders 4 AdminNavCard children (Coefficients / Partenaires / Historique / Références LC) — count via getAllByRole('link', { name: /...?/ }) or via direct DOM assertion on the AdminNavCard component count
    - Test 2: the grid wrapper has Tailwind classes `grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4` (1-col mobile / 2-col tablet / 4-col desktop per UI-SPEC §Surface 3)
    - Test 3: the 4th card has variant='lc-references', title=t('admin.nav.lcReferences.title', lang), description=t('admin.nav.lcReferences.description', lang), href=`/${adminSegment}/lc-references`, icon={Hash}
    - Test 4 (Gate 11): generating an LcReferencesList HTML render via renderToString with 4 fixture rows (one each of displayStatus='active', 'draft', 'expired', 'deleted') produces an HTML string containing zero /commission/i matches in any character position (active + draft + expired + deleted render paths all scrubbed)
    - Test 5 (Gate 12): LcReferencesList rendered with rows=[] + currentQ='Dupont' (search-empty path) produces zero /commission/i HTML; LcReferencesList rendered with rows=[] + no currentQ (first-run empty) produces zero /commission/i HTML
    - Test 6 (Gate suite monotonic): the existing 10 gates remain green (gates 1-9 baseline + gate 10 from Plan 01); suite total reaches 12 gates after Task 3 ships
    - Test 7: sidebar count UNCHANGED (Phase 18 D-27 lock preserved) — Admin Home does NOT add any sidebar nav item; the LC dashboard is reachable ONLY via the 4th AdminNavCard
  </behavior>
  <action>
    Modify `app/(admin)/[adminSegment]/page.tsx` per PATTERNS.md §app/(admin)/[adminSegment]/page.tsx (MODIFY):
    - Extend the lucide-react import at line ~3 to include `Hash` (the existing imports are `Sliders, Users, History, Plus`; add `Hash` alphabetized or end-of-list per the file's existing convention). UI-SPEC §Surface 3 §4th Card Spec §Icon rationale locks `Hash` over `Receipt`.
    - Change the grid wrapper at lines ~162-165: from the current inline-style `style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 24, marginBottom: 32 }}` to the responsive Tailwind classes per UI-SPEC §Surface 3 §Grid Layout: `<div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4" style={{ gap: 24, marginBottom: 32 }}>`. Keep the inline-style gap + marginBottom (Tailwind v4 has gap utilities but the existing file uses inline style for gap — minimal diff for the responsive change).
    - Add a 4th `<AdminNavCard>` component invocation AFTER the existing `history` card (the existing 3 cards are `coefficients`, `partners`, `history` — verify their order during execution by reading the file): `<AdminNavCard variant="lc-references" title={t('admin.nav.lcReferences.title', lang)} description={t('admin.nav.lcReferences.description', lang)} href={\`/${adminSegment}/lc-references\`} icon={Hash} openLabel={t('admin.nav.open', lang)} />`. Note: `admin.nav.open` is REUSED (per UI-SPEC §i18n Keys Summary §Reuse confirmed) — do not introduce a separate openLabel for the new card.
    - Critical: do NOT modify any other section of Admin Home (PageHero, stats tiles, Recent activity card all stay unchanged — those are Phase 18 deliverables).
    - Critical: do NOT touch the sidebar nav config (Phase 18 D-27 sidebar lock holds — no 7th item is added; the LC dashboard is reachable ONLY via this AdminNavCard).

    Modify `tests/admin-09-grep-contracts.test.ts` to add gates 11 + 12 per PATTERNS.md §tests/admin-09-grep-contracts.test.ts (MODIFY) and CONTEXT.md §Claude's Discretion (2 LCDASH gates recommended for redundancy across the active/draft/expired/deleted + search/empty query paths):
    - Append to the top-of-file mocks if needed: `vi.mock('@/lib/db/queries/lc-references', () => ({ listLcReferences: vi.fn(async () => ({ rows: [...], hasMore: false, nextCursor: null })) }));` — the inline fixture rows mirror PATTERNS.md §tests sample with 1 active row for the basic gate 11 render. Note: the gate doesn't NEED to call `listLcReferences` — it can render LcReferencesList directly with fixture props (the component takes rows as a prop). So the mock for listLcReferences is only needed if the gate renders the SSR page; rendering the LcReferencesList component directly is simpler and sufficient per D-05's parser-based-rigor principle applied to LCDASH-02 (render-based rigor — assert on the actual JSX output, not just on the query helper).
    - Add `describe('Gate 11: LC dashboard — commission leakage scan (LCDASH-02)', () => { ... })` AFTER gate 10. Inside, one `it('renders ZERO commission strings across all 4 displayStatus variants (active, draft, expired, deleted)')` test that imports `LcReferencesList` from `'../app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList'` (relative path — tests/ does not use the @/* alias per CONVENTIONS.md), constructs 4 fixture rows of type LcReferenceRow (one for each displayStatus), calls `renderToString(createElement(LcReferencesList, { rows: [4 fixtures], nextCursor: null, lang: 'fr', adminSegment: 'admin-secret' }))` (using react-dom/server), and asserts the resulting HTML string does not match `/commission/i`. Use the existing `assertNoCommissionLeakage(html, label)` helper if present in the file; otherwise inline the assertion. The fixtures must be deterministic — hardcoded LcReferenceRow values, no Date.now(), no random IDs (deterministic-fixture invariant for grep-contract suites).
    - Add `describe('Gate 12: LC dashboard — commission leakage scan on empty + search paths (LCDASH-02)', () => { ... })` AFTER gate 11. Inside, two `it()` cases: (a) `renders ZERO commission strings on search-empty path` — renders LcReferencesList with rows=[] + currentQ='Dupont' + adminSegment='admin-secret' + lang='fr' and asserts the HTML has no /commission/i match; (b) `renders ZERO commission strings on first-run empty path` — renders LcReferencesList with rows=[] + currentQ=undefined + adminSegment='admin-secret' + lang='fr' and asserts the HTML has no /commission/i match.
    - After this task, `grep -cE "^\s*describe\('Gate [0-9]+:" tests/admin-09-grep-contracts.test.ts | grep -v '^#'` must equal 12 (gates 1-9 + gate 10 from Plan 01 + new gates 11 + 12). Run `npx vitest run tests/admin-09-grep-contracts.test.ts --reporter=verbose` to confirm 12 describe blocks all green.

    Verify the visual sanity of the Admin Home grid layout during execution: run `npm run dev`, navigate to `/${ADMIN_URL_SEGMENT}/` (the admin home), confirm 4 cards display in a 4-column row on desktop, that the 4th card has the Hash icon + teal accent + "Références LC" title + "Voir toutes les références LC émises par les partenaires." description, and that clicking the card navigates to `/${ADMIN_URL_SEGMENT}/lc-references` showing the LC dashboard from Task 2. Verify in dark mode (theme toggle in sidebar footer) that the 4th card inherits dark via the data-theme cascade (no visual regression). This manual check is NOT a CI gate — it's the recommended sanity step before commit.
  </action>
  <verify>
    <automated>
      cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint && npx vitest run tests/admin-09-grep-contracts.test.ts "app/(admin)/[adminSegment]/page.test.tsx" --reporter=verbose
    </automated>
    grep -cE "^\s*describe\('Gate [0-9]+:" tests/admin-09-grep-contracts.test.ts | grep -v '^#' must equal 12 (gates 1-9 + gate 10 + gates 11 + 12);
    grep -n "Hash" app/\(admin\)/\[adminSegment\]/page.tsx | grep -v '^#' must show at least 2 hits (import + usage in 4th AdminNavCard);
    grep -n "xl:grid-cols-4" app/\(admin\)/\[adminSegment\]/page.tsx | grep -v '^#' must show 1 hit (responsive grid extension per UI-SPEC);
    grep -n "lc-references" app/\(admin\)/\[adminSegment\]/page.tsx | grep -v '^#' must show 1 hit (the href);
    grep -c "AdminNavCard" app/\(admin\)/\[adminSegment\]/page.tsx | grep -v '^#' must show >= 4 hits (4 card invocations + import)
  </verify>
  <done>
    Admin Home renders 4 AdminNavCards in a responsive Tailwind grid (1-col mobile / 2-col tablet / 4-col desktop); the 4th card variant='lc-references' with Hash icon, teal accent, locked title/description, href to `/${adminSegment}/lc-references`; tests/admin-09-grep-contracts.test.ts has gates 11 + 12 added (LCDASH render scan + search/empty render scan); `grep -cE "^\s*describe\('Gate [0-9]+:" tests/admin-09-grep-contracts.test.ts` equals 12; `npx vitest run tests/admin-09-grep-contracts.test.ts` reports 12 describe blocks all green; `npm run typecheck` + `npm run lint` pass; sidebar nav unchanged (Phase 18 D-27 preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → admin SSR route (`/[adminSegment]/lc-references`) | Untrusted `?q=` + `?cursor=` from URL state crosses here; the route is doubly-gated: (a) the `(admin)` layout already checks `ADMIN_URL_SEGMENT` match + admin role; (b) `requireAdmin()` is called independently at the page level per AUTH-15 defense-in-depth (T-19-02-04) |
| admin SSR route → `listLcReferences` | Cross-partner query — NO userId filter at this layer (T-19-02-01); the upstream gate IS the authorization boundary; helper documents this intentional drop in its JSDoc |
| `listLcReferences` → DB (Drizzle) | `q` ILIKE on 3 fields (lc_ref + users.name + inputs->>'clientName') MUST go through Drizzle parameter binding; cursor decoded from base64 → JSON → Date hydration with try/catch returning null on malformed input (no exception leaks to the request) |
| `LcReferencesList` row projection | The query helper's projection MUST strip `paramsSnapshot` (which carries `commission_pct`) before the rendered surface — `LcReferenceRow` type has no commission field; component receives only the bounded type, gates 11 + 12 verify the rendered HTML has zero /commission/i match across all 4 displayStatus variants + both empty states |
| Admin URL secrecy | `robots: { index: false, follow: false }` on the page metadata; admin segment is env-driven; `notFound()` fires before any role reveal per Phase 6 D-18 layer-order invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Authorization / Privilege Escalation | `listLcReferences` cross-partner read | mitigate | Upstream gate is `requireAdmin()` in page.tsx + the `(admin)` layout's segment+role check; helper's JSDoc documents the intentional userId-drop. A partner-role caller hitting `/[adminSegment]/lc-references` directly would hit `notFound()` from the layout before reaching the page (URL secrecy + AUTH-14); a partner-role caller hitting `listLcReferences` from a custom Server Action would need to bypass `requireAdmin()` — out of scope (existing pattern: every authed action calls requireUser/requireAdmin as the FIRST await per PITFALLS §7.3) |
| T-19-02-02 | Information Disclosure | LC dashboard row content (`commission_pct` leakage) | mitigate | `LcReferenceRow` type has no commission field; query helper's projection scrubs `paramsSnapshot` before the rendered surface; gate 11 (LCDASH-02) renders LcReferencesList with 4 fixture rows (one per displayStatus) and asserts zero /commission/i in HTML; gate 12 (LCDASH-02) repeats for empty + search-empty paths. Runs in CI on every commit. |
| T-19-02-03 | Information Disclosure | SQL injection via `q` ILIKE param | mitigate | `q` passed through Drizzle `ilike()` operator (parameter-bound) for lc_ref + users.name; for the jsonb `inputs->>'clientName'` path, the value is bound via Drizzle's `sql` template parameter interpolation (not string concatenation) — verify the exact syntax during execution against existing buildListResponse usage |
| T-19-02-04 | Authentication bypass | direct hit on `/[adminSegment]/lc-references` URL | mitigate | (a) `(admin)` layout segment+role check fires first (URL secrecy); (b) page.tsx calls `requireAdmin()` as the first await independently (AUTH-15 defense-in-depth); non-admin hitting URL → `notFound()` 404, never 403 (CONVENTIONS.md §Error Handling) |
| T-19-02-05 | Information Disclosure | LC dashboard search exposure of partner relationships | accept | Admins legitimately need cross-partner visibility for LC reference investigation (the entire point of the dashboard); this is the authorized use case per LCDASH-01. Partner-to-partner exposure is NOT a concern — partners never reach this surface |
| T-19-02-06 | Tampering | malicious `?cursor=` value (e.g. SQL injection via base64-decoded JSON) | mitigate | `decodeLcRefCursor` does try/catch + JSON.parse + Date hydration; on any error returns null (graceful fallback — test 10 of Task 1 asserts); cursor values feed Drizzle parameter-bound comparisons, not string concatenation |
| T-19-02-07 | Denial of Service | unbounded scan from search box | mitigate | LIMIT 21 per cursor page (~20 + 1 for hasMore detection); no unbounded query path exists; admin must page through results explicitly. If admin spams the search field with high-fanout queries, the per-request LIMIT bounds DB load |
| T-19-02-08 | Information Disclosure | LC dashboard exposure to search-engine crawlers | mitigate | `robots: { index: false, follow: false }` in the page metadata; admin URL segment is env-driven (not a published path); no public sitemap entry |
| T-19-02-09 | Information Disclosure | soft-deleted proposal data leak past 30-day window | accept | D-12 — soft-deleted rows visible within 30-day purge window only; after window, the Phase 10 purge cron hard-deletes the row (eventual consistency). This is the same admin visibility window partners get on `/proposals?archived=1`. If an admin needs to investigate hard-deleted references, that data is gone by design |
| T-19-02-10 | Tampering | accidental sidebar 7th item addition breaking Phase 18 D-27 lock | mitigate | Task 3 explicitly states "do NOT touch the sidebar nav config"; the LC dashboard is reachable ONLY via the 4th AdminNavCard on Admin Home (D-18). If a developer later adds a 7th sidebar item, that's a new decision requiring a phase plan update |
| T-19-02-SC | Tampering | npm package supply-chain — no new packages added by Plan 02 | accept | Plan 02 adds NO new npm dependencies (lucide-react `Hash` icon already in lucide-react 0.469.0 which is already in package.json). No `[ASSUMED]`/`[SUS]` checkpoint needed |
</threat_model>

<verification>
After all 3 tasks complete, run the full Phase 19 Plan 02 verification gate. All must pass before commit:

```bash
cd /Users/antoinerousseau/Developer/leasetic-calculator
npm run typecheck                                             # _EnHasAllFrKeys parity proof + strict TS
npm run lint                                                  # no-restricted-imports + JSXText i18n rule
npx vitest run                                                # FULL suite — confirms gates 1-9 baseline + gate 10 from Plan 01 + new gates 11 + 12 + new feature tests all green
node scripts/check-no-vercel-only-imports.sh 2>/dev/null || bash scripts/check-no-vercel-only-imports.sh
```

Additional grep-level invariants (must pass):
- `grep -cE "^\s*describe\('Gate [0-9]+:" tests/admin-09-grep-contracts.test.ts | grep -v '^#'` equals 12
- `grep -rn "commission" app/(admin)/[adminSegment]/lc-references/ src/lib/db/queries/lc-references.ts | grep -v '^#'` returns ZERO hits
- `grep -n "import 'server-only'" src/lib/db/queries/lc-references.ts` shows line 1
- `grep -n "lc-references" src/components/ui/AdminNavCard.tsx | grep -v '^#'` shows at least 2 hits (variant union + ACCENT_BY_VARIANT)
- `grep -n "isNotNull" src/lib/db/queries/lc-references.ts | grep -v '^#'` shows the D-11 lc_ref IS NOT NULL filter
- `grep -cE "AdminNavCard" app/\(admin\)/\[adminSegment\]/page.tsx | grep -v '^#'` shows >= 5 hits (1 import + 4 card invocations)
- `grep -c "drizzle/migrations" $(git diff --name-only HEAD~5 HEAD 2>/dev/null)` returns 0 — confirms zero migration files were touched
- Sidebar nav grep: `grep -nE "lc-references" src/components/ui/Shell.tsx src/components/ui/SidebarNav.tsx 2>/dev/null | grep -v '^#'` returns ZERO hits (sidebar lock preserved — discovery only via AdminNavCard)

Manual sanity check (recommended, not a gate): start `npm run dev`, log in as admin, confirm 4 AdminNavCards on Admin Home in 4-column row (desktop); click "Références LC" → lands on `/[adminSegment]/lc-references` showing the styled table; verify column 1 is monospace; search `?q=Memento` and confirm filter works; click "Suivant" cursor link and confirm pagination; clear search via "Effacer la recherche →" empty-state link; verify a draft row renders with `chip-draft` (gold), an active row with `chip-active`, an expired row with `chip-expired`, a soft-deleted-within-30d row with `chip-deleted`. Toggle to dark mode and confirm visual parity (no broken contrast).
</verification>

<success_criteria>
- LCDASH-01 grounded: admin at `/[adminSegment]/lc-references` sees a cursor-paginated, searchable list of every issued lc_ref across all partners with the locked 6 columns (Référence/Partenaire/Client/Montant HT/Statut/Créée le); search matches by lc_ref OR partner.name OR inputs.clientName per D-14; sort is created_at DESC per D-15; drafts + soft-deleted-within-30-days included per D-11/D-12.
- LCDASH-02 grounded: `tests/admin-09-grep-contracts.test.ts` ships gates 11 + 12 (LC dashboard render scan across 4 displayStatus + search-empty + first-run-empty paths per CONTEXT.md §Claude's Discretion 2-gate recommendation); suite total reaches 12 gates; gates 1-10 (including Plan 01 gate 10) remain green (monotonic extension).
- Sidebar lock preserved: Phase 18 D-27 6-item sidebar unchanged; LC dashboard reachable only via 4th AdminNavCard on Admin Home (D-18).
- Read-only invariant: LC dashboard rows have no Link wrapper, no onClick, no cursor:pointer style (D-13; ADMIN-FUT-01 deferred to v1.4+).
- Palette stability preserved: 4th AdminNavCard reuses --teal (RGB 45,122,140) — same accent as the existing `partners` variant; no new color token introduced.
- Light + dark inherited: all Phase 19 Plan 02 surfaces inherit dark via the data-theme cascade; zero per-component dark CSS added.
- i18n parity preserved: ~14 new keys × 2 branches added to dictionaries.ts; _EnHasAllFrKeys compile-time proof stays green; `chip.active`/`.draft`/`.expired`/`.deleted` + `admin.home.eyebrow` + `admin.nav.open` reused per UI-SPEC §Reuse confirmed.
- No schema migration: zero changes to `drizzle/migrations/*` or `drizzle/meta/_journal.json` (lc_ref column + partial unique index already exist from Phase 12).
- No audit_log expansion: viewing the dashboard is a read operation; no new event type added (Phase 12 lifecycle-only convention holds).
- Cross-partner authorization gate: `requireAdmin()` is the FIRST await in the SSR page route per AUTH-15; the query helper's JSDoc documents intentional userId-drop with explicit cross-reference to the upstream gate.
- ADMIN-09 invariant intact: LcReferenceRow type has no commission field; the entire lc-references directory (app + src) contains zero `commission` identifiers; gates 11 + 12 prove no commission strings reach the rendered HTML across all 4 displayStatus variants and both empty states.
</success_criteria>

<output>
Create `.planning/phases/19-new-capabilities/19-02-SUMMARY.md` when done. Include:
- Tasks shipped + file inventory (new + modified)
- Gate 11 + 12 acceptance evidence (vitest output snippet showing 12 describe blocks)
- Confirmation that the search field implementation was decided (inside LcReferencesList vs sibling form-submit in page.tsx) and rationale
- Confirmation of cursor encoding reuse decision (shared with src/lib/api/proposals/list.ts vs duplicated) per Task 1 action
- Any deviations from UI-SPEC (e.g. if StatusChip variant naming differs from the UI-SPEC mapping table and the mapping had to adapt)
- Confirmation that no sidebar item was added (Phase 18 D-27 preserved)
- Decisions log entries for any Claude's Discretion items locked during execution (e.g. LCDASH-02 exact gate count if it differed from 2)
</output>
