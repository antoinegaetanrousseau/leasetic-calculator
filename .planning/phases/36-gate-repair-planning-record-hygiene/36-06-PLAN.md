---
phase: 36-gate-repair-planning-record-hygiene
plan: 06
type: execute
wave: 4
depends_on: ["36-05"]
files_modified:
  - .planning/phases/29-migration-safety-net/29-VERIFICATION.md
  - .planning/phases/29-migration-safety-net/29-SECURITY.md
  - .planning/phases/29-migration-safety-net/29-VALIDATION.md
autonomous: true
requirements: [CLOSE-05]

must_haves:
  truths:
    - "`29-VERIFICATION.md` § Known Weak Link records the probe outcome and no longer describes the empirical claim as unattempted"
    - "T-29-06's disposition in `29-SECURITY.md` is revisited in light of the observed result, with the revisit dated and attributed"
    - "`.planning/phases/29-migration-safety-net/29-VALIDATION.md` exists as a real file"
    - "29-VALIDATION.md explains why Phase 29's Nyquist dimensions are not derivable rather than hand-writing a retroactive one"
    - "29-VALIDATION.md states that Phase 29 closed on 5/5 verified must-haves, and that this is a measurement gap rather than a coverage gap"
  artifacts:
    - path: ".planning/phases/29-migration-safety-net/29-VALIDATION.md"
      provides: "Phase 29 Nyquist coverage record, written to clear the audit's file-existence finding"
      contains: "nyquist_compliant"
      min_lines: 60
    - path: ".planning/phases/29-migration-safety-net/29-VERIFICATION.md"
      provides: "Known Weak Link section updated with the Phase 36 probe outcome"
      contains: "Phase 36"
    - path: ".planning/phases/29-migration-safety-net/29-SECURITY.md"
      provides: "T-29-06 revisited against observed evidence"
      contains: "2026-09-05"
  key_links:
    - from: ".planning/phases/29-migration-safety-net/29-VERIFICATION.md"
      to: ".planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md"
      via: "the update cites the transcript rather than restating its numbers from memory"
      pattern: "36-PROBE-TRANSCRIPT\\.md"
    - from: ".planning/phases/29-migration-safety-net/29-VALIDATION.md"
      to: ".planning/phases/29-migration-safety-net/29-VERIFICATION.md"
      via: "the 5/5 must-have score is cited, not asserted"
      pattern: "29-VERIFICATION\\.md"
---

<objective>
Write the probe outcome into Phase 29's own artifacts and give Phase 29 the coverage record
it never had (CLOSE-05, part 3 of 3).

Purpose: CLOSE-05 has two halves. The empirical half is settled by plan 36-05's run, but a
result that lives only in a Phase 36 transcript does not close a Phase 29 gap — the v1.6
audit reads Phase 29's directory. The measurement half is `29-VALIDATION.md`: the audit's
`nyquist.missing_phases` finding is a file-existence check, so a note filed anywhere else
will not clear it.

Output: an updated Known Weak Link section, a revisited T-29-06 disposition, and a new
`29-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md
</context>

<constraints>
- **Do NOT hand-write a retroactive Nyquist validation for Phase 29** (D-36-04). Phase 29 ran
  with `workflow.research: false` and produced no `29-RESEARCH.md`; `VALIDATION.md` derives
  its dimensions from `RESEARCH.md`. The file records *why* the dimensions are not derivable.
  Inventing sampling dimensions after the fact would be a fabricated record.
- **Write in the house honest-failure register.** State the weaker claim that IS supported,
  state precisely what is NOT proven, name the upgrade path, and classify severity
  explicitly. `29-VERIFICATION.md:81-108`, `29-SECURITY.md:63` and `30-VALIDATION.md:130-137`
  are the models. A note written as an excuse fails this plan.
- **Amend, do not rewrite.** Phase 29's artifacts are a historical record of what was known
  in August 2026. Every edit is additive and dated; nothing that was true then is deleted.
- **Do not touch Phase 40's work.** Formally closing v1.6, re-running its audit and archiving
  phases 28-35 are CLOSE-06 / CLOSE-07. Do not edit `.planning/v1.6-MILESTONE-AUDIT.md`,
  `.planning/ROADMAP.md`'s milestone status or `MILESTONES.md`.
- **Never run `npm run db:migrate` or any migration command.** Do not re-run the probe — plan
  36-05 owns the single run.
- **Do not edit `eslint.config.mjs`.**
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Update 29-VERIFICATION.md § Known Weak Link with the probe outcome</name>
  <files>.planning/phases/29-migration-safety-net/29-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/29-migration-safety-net/29-VERIFICATION.md` lines 79-110 — the whole
      `## Known Weak Link — INFRA-05 Evidentiary Basis (assessed honestly, per instructions)`
      section: the three-step timeline of why ISOLATION-PROBE-29 never completed, the
      "What IS independently confirmed" list, the "What is genuinely NOT proven" list with
      its options (a) and (b), and the closing `WARNING-level residual gap, not a BLOCKER`
      classification. This is the register to match.
    - `.planning/phases/29-migration-safety-net/29-VERIFICATION.md` line 28 — the
      `**Score:** 5/5 truths verified (truth 4 verified on the narrower,
      architecturally-inferred basis described below, ...)` line. Task 3 cites this; note
      whether the probe outcome changes the parenthetical.
    - `.planning/phases/29-migration-safety-net/29-VERIFICATION.md` lines 110-125 — the
      `## Human Verification Required` and `## Gaps Summary` sections, both of which describe
      the probe as a "future, optional action" and must be reconciled with the fact that it
      has now been run.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md` — the
      verdict, the two counts, the sentinel label, and the "what this does and does not
      prove" section. This is the only source for the outcome; do not restate numbers from
      memory.
  </read_first>
  <action>
    Amend `29-VERIFICATION.md` additively. Do not delete the August account — it is the
    record of what was known then.

    Edit 1 — append a new subsection at the END of the `## Known Weak Link — INFRA-05
    Evidentiary Basis` section, before the next `##` heading, titled
    `### Update 2026-09-05 — SQL-level sentinel probe run (Phase 36, CLOSE-05 / D-36-03)`.
    It must carry:
      (a) Why the original blocker did not apply: the app-level probe failed because the
          `development` branch is a copy-on-write fork frozen at 2026-05-27 and its Better
          Auth credential hashes predate every rotation since. A SQL-level probe never
          touches Better Auth, so the login blocker is bypassed rather than solved.
      (b) The probe's shape in one sentence: a uniquely-labelled sentinel row written to
          `schema_meta` on `development` with a raw client, a single `SELECT count(*)`
          existence check on `main`, and deletion of the sentinel in the same run.
      (c) The observed verdict and both counts, copied from the transcript, with an explicit
          citation of
          `.planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md`.
      (d) What this now moves in the section above. If ISOLATED: the item listed under
          "What is genuinely NOT proven" — "that a row written against the `development`
          branch's compute endpoint is actually unreachable from the production Vercel
          deployment's runtime" — is now partially observed, and the update must say
          *partially* and say why: the probe observes endpoint-to-endpoint invisibility at
          one instant for one table through the pooled endpoints; it does not audit Neon's
          storage layer, and it does not exercise the Vercel runtime itself beyond the `main`
          endpoint that runtime shares. If ISOLATION VIOLATED: state it as a BLOCKER and stop
          — do not soften it, and flag it in the plan summary for immediate operator
          attention. If INCONCLUSIVE: state that the architectural-inference basis stands
          unchanged, which is the fallback CLOSE-05 explicitly permits.
      (e) A re-classification of the residual severity in the same explicit form the section
          already uses — name it (for example `residual gap` with a level) rather than
          leaving the August `WARNING-level residual gap, not a BLOCKER` line to be read as
          still current.

    Edit 2 — the `## Human Verification Required` section currently says the probe is a
    "future, optional action". Append a dated clause to that paragraph recording that it was
    performed on 2026-09-05 under Phase 36 CLOSE-05, and pointing at the new subsection. Do
    not rewrite the original sentence.

    Edit 3 — the `## Gaps Summary` section's INFRA-05 bullet describes "the empirical
    write-isolation proof" as "the one open item". Append a dated clause recording its
    current state per the probe. Keep the original text.

    Edit 4 — line 28's `**Score:** 5/5 truths verified` line. Do NOT change the score. Append
    a bracketed dated note recording that truth 4's narrower architecturally-inferred basis
    was revisited on 2026-09-05 and pointing at the new subsection, so a reader who stops at
    the score line is not left with the August framing.

    Every added clause carries the date `2026-09-05` and the attribution
    `Phase 36 / CLOSE-05`.
  </action>
  <verify>
    <automated>grep -c 'Update 2026-09-05 — SQL-level sentinel probe run' .planning/phases/29-migration-safety-net/29-VERIFICATION.md</automated>
    <automated>grep -c '36-PROBE-TRANSCRIPT.md' .planning/phases/29-migration-safety-net/29-VERIFICATION.md</automated>
    <automated>grep -c 'Known Weak Link — INFRA-05 Evidentiary Basis' .planning/phases/29-migration-safety-net/29-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Update 2026-09-05 — SQL-level sentinel probe run' <file>` returns `1`.
    - `grep -c '36-PROBE-TRANSCRIPT.md' <file>` returns ≥ 1.
    - The new subsection contains exactly one of `ISOLATED`, `ISOLATION VIOLATED`,
      `INCONCLUSIVE`, and that value equals the verdict in `36-PROBE-TRANSCRIPT.md`.
    - The new subsection contains the literals `schema_meta`, `SELECT count(*)` and `D-36-03`.
    - The August text is intact: `grep -c 'ISOLATION-PROBE-29' <file>` returns the same value
      as at HEAD, and `grep -c 'Invalid password' <file>` returns the same value as at HEAD.
    - `grep -c '\*\*Score:\*\* 5/5 truths verified' <file>` still returns `1` — the score was
      not changed.
    - `grep -c '2026-09-05' <file>` returns ≥ 4 — all four edits are dated.
    - `git diff --numstat <file>` shows zero deleted lines other than the four amended lines
      (i.e. deletions ≤ 4).
    - `git status --porcelain` shows no file outside `.planning/phases/29-migration-safety-net/`
      and Phase 36's own directory as modified.
  </acceptance_criteria>
  <done>Phase 29's Known Weak Link section records the probe outcome with its exact scope and residual, cites the transcript, and leaves the August account intact and clearly dated.</done>
</task>

<task type="auto">
  <name>Task 2: Revisit T-29-06's disposition in 29-SECURITY.md against the observed result</name>
  <files>.planning/phases/29-migration-safety-net/29-SECURITY.md</files>
  <read_first>
    - `.planning/phases/29-migration-safety-net/29-SECURITY.md` line 32 — the T-29-06 register
      row, currently `~~mitigate~~ → **accept** (re-classified 2026-08-31)`, `**CLOSED**
      (recorded below)`, with its rationale naming the blocked probe.
    - `.planning/phases/29-migration-safety-net/29-SECURITY.md` lines 41-52 — the `## Open`
      section, which states "None." and retains the superseded remedy for the audit trail,
      including the sentence "That path remains open to anyone who later restores a working
      `development`-branch login, and doing so would upgrade T-29-06 from accepted-risk to
      empirically-closed."
    - `.planning/phases/29-migration-safety-net/29-SECURITY.md` lines 56-63 — the
      `## Accepted Risks Log` table and specifically the T-29-06 row (line 63), which already
      names its own upgrade path: "**Cheap upgrade path:** restore a `development`-branch
      login (`grant-admin.ts` issues an invitation URL) and run the probe, which would move
      this from accepted to empirically closed."
    - `.planning/phases/29-migration-safety-net/29-SECURITY.md` lines 86-96 — the `## Summary`
      block (`**Closed:** 10/10 | **Open:** 0/10 | **Accepted risks:** 4`) and its explicit
      statement that T-29-06 "was **not** closed on evidence".
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md` — the
      verdict.
    - `.planning/phases/29-migration-safety-net/29-VERIFICATION.md` — the subsection added by
      Task 1, to cite rather than duplicate.
  </read_first>
  <action>
    D-36-03 requires T-29-06 to be *revisited in light of the observed result*, not silently
    flipped. Make the revisit explicit, dated and attributed, and preserve the 2026-08-31
    acceptance as history.

    Edit 1 — append a new section at the end of the file titled
    `## T-29-06 Revisit — 2026-09-05 (Phase 36, CLOSE-05 / D-36-03)`. It must state:
      (a) That the SQL-level sentinel probe was run on 2026-09-05, citing
          `.planning/phases/36-gate-repair-planning-record-hygiene/36-PROBE-TRANSCRIPT.md`
          and the new `### Update 2026-09-05` subsection in `29-VERIFICATION.md`.
      (b) That this is a SECOND upgrade path, distinct from the one the Accepted Risks Log
          named: the log's path required restoring a `development`-branch login via
          `grant-admin.ts`; the SQL-level probe sidesteps Better Auth entirely and is
          therefore cheaper and was not blocked.
      (c) The resulting disposition, chosen against the verdict and stated as a decision, not
          an inference:
            - ISOLATED → the disposition moves from `accept` to **mitigate — empirically
              closed**, and the new residual is stated precisely (one instant, one table,
              endpoint-to-endpoint through the pooled hosts; no storage-layer audit; no
              exercise of the Vercel runtime beyond the shared `main` endpoint).
            - ISOLATION VIOLATED → the disposition becomes **OPEN — BLOCKER**, and the entry
              must say so without softening, naming the sentinel label from the transcript.
            - INCONCLUSIVE → the disposition remains `accept`, unchanged, and the entry
              records that the second upgrade path was attempted and did not settle it.
      (d) Who decided and when: `Antoine (plan owner), 2026-09-05`.

    Edit 2 — the T-29-06 row at line 32. Append to the end of that cell a dated pointer, e.g.
    ` — revisited 2026-09-05, see § T-29-06 Revisit below.` Do not rewrite the existing cell
    text and do not change the `~~mitigate~~ → **accept** (re-classified 2026-08-31)`
    notation; the August disposition stays visible as history.

    Edit 3 — the Accepted Risks Log row for T-29-06 (line 63). Append a dated clause to the
    `**Cheap upgrade path:**` sentence recording that a second, cheaper path existed (a
    SQL-level probe that never touches Better Auth), that it was taken on 2026-09-05, and
    what it yielded. Do not delete the original rationale.

    Edit 4 — the `## Summary` block. If and only if the verdict is ISOLATED, update the
    accepted-risk count and the sentence stating T-29-06 "was **not** closed on evidence" —
    amend it additively with a dated clause rather than rewriting it, so the August framing
    stays legible. If the verdict is INCONCLUSIVE, change nothing in the Summary except to
    add a dated pointer to the revisit section. If ISOLATION VIOLATED, the Summary's
    `**Open:** 0/10` is now false and must be corrected with a dated note.

    Do not renumber threats, do not add new threat IDs, and do not touch any other threat row.
  </action>
  <verify>
    <automated>grep -c 'T-29-06 Revisit — 2026-09-05' .planning/phases/29-migration-safety-net/29-SECURITY.md</automated>
    <automated>grep -c '36-PROBE-TRANSCRIPT.md' .planning/phases/29-migration-safety-net/29-SECURITY.md</automated>
    <automated>grep -c 'T-29-0[1-9]\|T-29-SC' .planning/phases/29-migration-safety-net/29-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## T-29-06 Revisit — 2026-09-05' <file>` returns `1`.
    - The revisit section names one disposition from the set
      {`mitigate — empirically closed`, `OPEN — BLOCKER`, `accept`} consistent with the
      transcript's verdict, and contains the attribution `Antoine (plan owner)` and the date
      `2026-09-05`.
    - The revisit section contains the literal `grant-admin.ts` (naming the superseded first
      upgrade path) and the literal `36-PROBE-TRANSCRIPT.md`.
    - The August history survives: `grep -c 're-classified 2026-08-31' <file>` returns the
      same value as at HEAD, and `grep -c 'Invalid password' <file>` returns the same value
      as at HEAD.
    - The T-29-06 register row at line 32 now contains `revisited 2026-09-05`.
    - The Accepted Risks Log T-29-06 row now contains `2026-09-05`.
    - No threat ID was added or removed: the sorted unique list from
      `grep -o 'T-29-[0-9A-Z]*' <file> | sort -u` is identical to HEAD's.
    - `git diff --numstat <file>` shows deletions ≤ 4 — the edits are additive.
  </acceptance_criteria>
  <done>T-29-06 carries an explicit, dated, attributed revisit naming the second upgrade path and the resulting disposition, with the 2026-08-31 acceptance preserved as history.</done>
</task>

<task type="auto">
  <name>Task 3: Create 29-VALIDATION.md recording why Phase 29's Nyquist dimensions are not derivable</name>
  <files>.planning/phases/29-migration-safety-net/29-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/30-company-contact-registry/30-VALIDATION.md` — the only
      `*VALIDATION*.md` in the repo and itself a reconstructed one, which is exactly Phase
      29's situation. Read it in full (157 lines). Note specifically: the frontmatter block
      at lines 1-9 (`phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`,
      `created`), the reconstruction preamble at lines 10-16, the supporting-gates table at
      lines 27-41, the "Two near-misses that were NOT gaps" section at lines 130-137 (the
      house voice for a measurement-gap argument), and the Validation Sign-Off block plus
      scope note at lines 145-157.
    - `.planning/phases/29-migration-safety-net/29-VERIFICATION.md` line 28 — the
      `**Score:** 5/5 truths verified` line, and lines 1-30 for the five must-have truths
      themselves. This is the evidence for the "closed on 5/5 verified must-haves" claim.
    - `.planning/phases/29-migration-safety-net/` — the directory listing. There is no
      `29-RESEARCH.md`; there are `29-01-PLAN.md`, `29-01-SUMMARY.md`, `29-02-PLAN.md`,
      `29-02-SUMMARY.md`, `29-REVIEW.md`, `29-REVIEW-FIX.md`, `29-SECURITY.md`, `29-UAT.md`,
      `29-VERIFICATION.md`. Read both plans and both summaries — the gates each task actually
      verified against are the substance of this file.
    - `package.json` `scripts` block — verify each gate named in the table still exists
      today. `30-VALIDATION.md`'s nine gates are `typecheck`, `lint:check`,
      `check:migration-journal-sync`, `check:no-drizzle-push`, `check:db-smoke-filter`,
      `check:no-vercel-imports`, `check:no-v10-localstorage`, `check:seed-sql`,
      `check:local-db-branch`. Three of them (`check:migration-journal-sync`,
      `check:db-smoke-filter`, `check:local-db-branch`) were created BY Phase 29 — say so.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md` § D-36-04 and
      the "Claude's Discretion" bullet naming this file's path as the preferred home.
  </read_first>
  <action>
    Create `.planning/phases/29-migration-safety-net/29-VALIDATION.md`. It is a real file with
    a real body — the v1.6 audit's `nyquist.missing_phases` finding is a file-existence check
    and a note filed elsewhere will not clear it.

    **Frontmatter**, following `30-VALIDATION.md`'s keys exactly:
    `phase: 29`, `slug: migration-safety-net`, `status: approved`,
    `nyquist_compliant: not-derivable`, `wave_0_complete: true`, `created: 2026-09-05`.
    Use `not-derivable` rather than `false` (which reads as a failure the phase did not have)
    or `true` (which is unearned). Add a `reconstructed: true` key and a
    `reconstructed_by: Phase 36 / CLOSE-05 / D-36-04` key so the file's provenance is in its
    own frontmatter.

    **Reconstruction preamble**, in the shape of `30-VALIDATION.md` lines 10-16: state that
    this is reconstructed from artifacts (State B — no prior VALIDATION.md), name what was
    read (both plans, both summaries, `29-VERIFICATION.md`, `29-SECURITY.md`,
    `29-REVIEW.md`, `29-REVIEW-FIX.md`, `29-UAT.md`), and state the date.

    **The core section — `## Why the Nyquist dimensions are not derivable`.** This is D-36-04
    and it must be written so the v1.6 audit's `nyquist.missing_phases` finding stops
    recurring. Say, in the honest-failure register:
      - Phase 29 ran with `workflow.research: false` and produced no `29-RESEARCH.md`.
      - A GSD `VALIDATION.md` derives its sampling dimensions from `RESEARCH.md`. With no
        `RESEARCH.md`, the dimensions have no source.
      - Therefore this file does NOT hand-write a retroactive Nyquist validation. Doing so
        would fabricate dimensions that were never used to plan or execute the phase, and a
        fabricated record is worse than a missing one.
      - What the phase DID close on, stated as the substitute evidence: 5/5 verified
        must-have truths, citing `29-VERIFICATION.md` line 28 including its parenthetical
        that truth 4 was verified on the narrower architecturally-inferred basis. Note that
        truth 4's basis was revisited on 2026-09-05 by the sentinel probe and point at the
        `### Update 2026-09-05` subsection.
      - Classify it explicitly: **this is a measurement gap, not a coverage gap.** The
        distinction is the whole point — no requirement went unverified; what is missing is
        the record of the sampling frame, and that frame never existed to be recorded.

    **`## Supporting gates`** — a table in the shape of `30-VALIDATION.md` lines 27-41,
    listing each gate and its exact npm command, verified present in `package.json` at the
    time of writing. Mark the three gates Phase 29 itself created
    (`check:migration-journal-sync`, `check:db-smoke-filter`, `check:local-db-branch`) so the
    table doubles as a record of what the phase shipped.

    **`## Per-plan automated verification`** — for `29-01` and `29-02`, list what each plan's
    tasks were verified by, taken from the plan and summary files. Where a task had no
    automated verification, say so plainly rather than omitting it.

    **`## Manual-only`** — the items that were never automatable, including
    ISOLATION-PROBE-29 and its Phase 36 successor.

    **`## Validation Sign-Off`** — the checklist shape from `30-VALIDATION.md` lines 145-157,
    with each box answered honestly against Phase 29's actual state (`nyquist_compliant:
    not-derivable` must be reflected, not ticked as if compliant), an `**Approval:**` line
    dated 2026-09-05, and a `> Scope note:` recording that this artifact certifies automated
    requirement coverage and is a reconstruction, not a contemporaneous plan.

    Do not edit `29-VERIFICATION.md` or `29-SECURITY.md` in this task — Tasks 1 and 2 own
    those. Do not edit `.planning/v1.6-MILESTONE-AUDIT.md`; re-running that audit is Phase
    40's job (CLOSE-06).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-migration-safety-net/29-VALIDATION.md &amp;&amp; wc -l .planning/phases/29-migration-safety-net/29-VALIDATION.md</automated>
    <automated>head -12 .planning/phases/29-migration-safety-net/29-VALIDATION.md</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/29-migration-safety-net/29-VALIDATION.md','utf8');if(!/^---\n[\s\S]*?\n---\n/.test(t)){console.error('frontmatter missing');process.exit(1)};console.log('frontmatter OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/29-migration-safety-net/29-VALIDATION.md` exists and is at least
      60 lines.
    - Its frontmatter parses as a YAML block delimited by `---` on the first line, and
      contains the keys `phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`,
      `created`, `reconstructed`, `reconstructed_by`.
    - `grep -c 'nyquist_compliant: not-derivable' <file>` returns `1`.
    - `grep -c '## Why the Nyquist dimensions are not derivable' <file>` returns `1`.
    - That section contains all four of: `workflow.research: false`, `29-RESEARCH.md`,
      `5/5`, and `measurement gap`.
    - `grep -c 'coverage gap' <file>` returns ≥ 1 — the distinction is stated, not implied.
    - `grep -c '29-VERIFICATION.md' <file>` returns ≥ 1 — the 5/5 score is cited, not asserted.
    - The supporting-gates table names all nine gates and each named npm script actually
      exists: for every backticked `npm run X` in the table,
      `node -e "process.exit(require('./package.json').scripts['X']?0:1)"` exits 0.
    - `grep -c 'check:migration-journal-sync\|check:db-smoke-filter\|check:local-db-branch' <file>`
      returns ≥ 3 and the file marks them as created by Phase 29.
    - `grep -c '## Validation Sign-Off' <file>` returns `1` and the block contains
      `**Approval:**` with the date `2026-09-05`.
    - `grep -c 'ISOLATION-PROBE-29' <file>` returns ≥ 1 — the manual-only item is recorded.
    - `git status --porcelain .planning/v1.6-MILESTONE-AUDIT.md .planning/ROADMAP.md` prints
      nothing — Phase 40's files were not touched.
  </acceptance_criteria>
  <done>Phase 29 has a real `29-VALIDATION.md` whose body explains non-derivability, cites the 5/5 must-have score, classifies the gap as measurement rather than coverage, and lists the nine gates it actually ran against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe transcript → Phase 29 security record | A disposition change on a security threat is being made from an observation; the observation's scope must not be overstated when it crosses into the register |
| Phase 36 → Phase 29 historical artifacts | Amending a closed phase's record risks rewriting what was known at the time, destroying the audit trail |
| Phase 36 → Phase 40's scope | The v1.6 audit files and milestone status belong to CLOSE-06 / CLOSE-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-28 | Repudiation | T-29-06 disposition change | mitigate | The revisit is a dated, attributed decision section (`Antoine (plan owner), 2026-09-05`) that cites the transcript and names which upgrade path was taken, rather than an unexplained flip in a table cell. The 2026-08-31 acceptance and its `~~mitigate~~ → accept` notation are preserved verbatim, asserted by grep against HEAD counts. |
| T-36-29 | Tampering | Phase 29 historical record | mitigate | Every edit is additive and dated; acceptance criteria cap deletions at 4 lines per file and pin the HEAD counts of `ISOLATION-PROBE-29`, `Invalid password` and `re-classified 2026-08-31`. The August account of what was known then cannot be erased under cover of an update. |
| T-36-30 | Spoofing | overstated security claim | mitigate | The ISOLATED branch of Tasks 1 and 2 is required to state the residual precisely — one instant, one table, endpoint-to-endpoint through the pooled hosts, no storage-layer audit, no exercise of the Vercel runtime — so a single observation cannot be recorded as a general isolation guarantee. The ISOLATION VIOLATED branch forbids softening. |
| T-36-31 | Tampering | fabricated validation record | mitigate | D-36-04 forbids hand-writing retroactive Nyquist dimensions. `nyquist_compliant: not-derivable` plus a body explaining why is asserted by grep; a `true` value would be unearned and a fabricated dimensions table would be worse than the missing file. `reconstructed: true` and `reconstructed_by` put the provenance in the frontmatter. |
| T-36-32 | Tampering | gate table drift | mitigate | Every `npm run X` named in the supporting-gates table must resolve to a real key in `package.json`, checked programmatically in the acceptance criteria, so the reconstruction cannot certify gates that no longer exist. |
| T-36-33 | Elevation of privilege | scope creep into Phase 40 | mitigate | `.planning/v1.6-MILESTONE-AUDIT.md`, `.planning/ROADMAP.md` and `MILESTONES.md` are explicitly out of scope and asserted unmodified, so the v1.6 close cannot be half-performed here and then re-performed differently in Phase 40. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | Nothing is installed and no source file is edited. `git status --porcelain package.json package-lock.json src/ app/ scripts/` must print nothing. |
</threat_model>

<verification>
1. `test -f .planning/phases/29-migration-safety-net/29-VALIDATION.md` succeeds and the file
   is ≥ 60 lines with parseable frontmatter.
2. `grep -c '### Update 2026-09-05' .planning/phases/29-migration-safety-net/29-VERIFICATION.md`
   returns 1.
3. `grep -c '## T-29-06 Revisit — 2026-09-05' .planning/phases/29-migration-safety-net/29-SECURITY.md`
   returns 1.
4. The verdict recorded in all three files is identical and matches
   `36-PROBE-TRANSCRIPT.md`.
5. `git status --porcelain package.json package-lock.json eslint.config.mjs src/ app/ scripts/ .planning/v1.6-MILESTONE-AUDIT.md .planning/ROADMAP.md`
   prints nothing.
6. No migration command was run and the probe was not re-run.
</verification>

<success_criteria>
- `29-VERIFICATION.md` § Known Weak Link carries a dated update recording the probe outcome
  with its exact scope, and cites the transcript.
- T-29-06 in `29-SECURITY.md` carries a dated, attributed revisit naming the second upgrade
  path and the resulting disposition.
- `.planning/phases/29-migration-safety-net/29-VALIDATION.md` exists, explains
  non-derivability rather than fabricating dimensions, and classifies the gap as measurement
  rather than coverage.
- Phase 29's August record survives intact and clearly dated.
- Phase 40's files are untouched.
</success_criteria>

<output>
Create `.planning/phases/36-gate-repair-planning-record-hygiene/36-06-SUMMARY.md` when done.
</output>
