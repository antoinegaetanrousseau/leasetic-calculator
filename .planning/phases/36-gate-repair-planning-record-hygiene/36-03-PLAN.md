---
phase: 36-gate-repair-planning-record-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/reui-blocks-audit.md
  - .planning/REQUIREMENTS.md
  - src/components/blocks/
  - tests/container-radius.test.ts
  - tests/server-action-error-contracts.test.ts
autonomous: true
requirements: [HOUSE-04]

must_haves:
  truths:
    - "`docs/design/reui-blocks-audit.md` opens with a dated decision record stating the blocks were deleted on 2026-09-05, by whose decision, and how to reinstall"
    - "The decision record states the measured figures — 25 block directories, 152 files, 1.1M — not the audit body's stale 18/104/816K"
    - "`src/components/blocks/` no longer exists"
    - "`src/components/reui/` is untouched and all 13 of its entries still exist"
    - "`npm run typecheck`, `npm run lint:check`, `npm test` and `npm run build` all still pass after a 152-file drop"
    - "`.planning/REQUIREMENTS.md` no longer says the deletion is out of scope for this milestone, and HOUSE-04 carries the measured figures"
  artifacts:
    - path: "docs/design/reui-blocks-audit.md"
      provides: "dated delete decision record superseding 'Nothing was deleted.'"
      contains: "Deleted 2026-09-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "§ Out of Scope amendment + corrected HOUSE-04 figures"
      contains: "Amended 2026-09-05"
    - path: "tests/container-radius.test.ts"
      provides: "EXCLUDED_DIRS with the dead blocks entry removed"
    - path: "tests/server-action-error-contracts.test.ts"
      provides: "collectSourceFiles skip condition with the blocks half removed and the reui half intact"
      contains: "components/reui"
  key_links:
    - from: "docs/design/reui-blocks-audit.md"
      to: "the deleted tree"
      via: "the reinstall command that makes deletion reversible"
      pattern: "npx shadcn@latest add @reui/"
    - from: "tests/server-action-error-contracts.test.ts"
      to: "src/components/reui/"
      via: "the surviving half of the skip condition"
      pattern: "full\\.includes\\('components/reui'\\)"
---

<objective>
Record the delete decision in `docs/design/reui-blocks-audit.md`, then delete all 25 dead
vendored ReUI block directories at `src/components/blocks/` and clean up the two dead test
references (HOUSE-04, D-36-02).

Purpose: 152 files and 1.1M of vendored blocks that nothing imports sit in the tree, get
scanned by every directory-walking test, and were left in place on 2026-08-31 under a
provisional "Delete nothing yet". D-36-02 supersedes that: reinstall is one command, so
there is no cost to deleting and no value in keeping one "just in case" beyond the audit
record itself.

Output: a dated decision record at the head of the audit doc, an absent `src/components/blocks/`,
two cleaned test files, and a four-gate battery proving the drop moved nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md
@CLAUDE.md
</context>

<constraints>
- **Record before deleting.** Task 1 (the decision record) must land before Task 2 (the
  deletion). `docs/design/reui-blocks-audit.md` is the whole reason deletion is safe; if the
  record is not written first, the mapping is lost the moment the tree goes.
- **`src/components/reui/` is NOT touched.** Its 13 entries (`alert.tsx`, `badge.tsx`,
  `cascader/`, `data-grid/`, `filters/`, `filters.tsx`, `frame.tsx`, `gantt/`,
  `icon-stack.tsx`, `kanban.tsx`, `phone-input.tsx`, `stepper.tsx`, `timeline.tsx`) are live
  primitives wired into shipped surfaces. `src/components/ui/` is not touched either. Only
  `src/components/blocks/` is deleted.
- **Do not edit `eslint.config.mjs`.** Its `ignores` arrays contain `'src/components/blocks/**'`
  at two sites (lines 37 and 139). Those entries become inert globs matching nothing once the
  tree is gone — harmless to ESLint, and removing them is out of scope for Phase 36 because
  this phase does not edit that file. Task 1's decision record must state this explicitly so
  the residual is on the record rather than discovered later.
- **Use the measured figures, not the audit body's.** `36-PATTERNS.md` corrects
  `36-CONTEXT.md`: the tree is **25 directories / 152 files / 1.1M** as of 2026-09-05, because
  Phase 34 plan 34-03 vendored seven more (`solution-crm-1`…`solution-crm-6`, `solution-users-2`)
  after the 2026-08-31 audit was written. The 18/104/816K in the audit body is the historical
  count and stays as-is inside the body.
- **Two requirement-record statements are superseded and BOTH must be amended here, not just
  noted.** `.planning/ROADMAP.md` success criterion 4 was already amended during planning. The
  matching sentence in `.planning/REQUIREMENTS.md` § Out of Scope still reads "**Deleting the
  vendored ReUI blocks** — HOUSE-04 makes the call. If the call is 'delete', the deletion is
  its own work, not this milestone's." — which this plan contradicts by performing the
  deletion. D-36-02 (2026-09-05) is the later and authoritative decision. Task 3 amends that
  sentence in place and corrects HOUSE-04's stale figures. Operator decision, 2026-09-05:
  amend here rather than defer to Phase 40.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Prepend the dated delete decision record to the ReUI blocks audit</name>
  <files>docs/design/reui-blocks-audit.md</files>
  <read_first>
    - `docs/design/reui-blocks-audit.md` lines 1-40 — the `#` title, the existing header
      blockquote (lines 3-9) carrying the 2026-08-31 date and the `18 vendored blocks /
      816K / 104 files` figures, the sentence `Nothing was deleted.` on line 8, the
      `## Reinstalling is one command` section with `npx shadcn@latest add @reui/<block-name>`,
      and the `(Antoine, 2026-08-31)` inline attribution on line 36 — the house attribution form.
    - `docs/design/reui-blocks-audit.md` in full — the mapping, the two structural constraints
      (Frame vs Card), the mis-picks, and the "What is installed underneath" section that
      names the live `reui/` layer. All of this is KEPT.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md` §7 — the
      no-analog note and the required content of the record.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md` § D-36-02.
  </read_first>
  <action>
    First measure, so the record states facts observed at execution time rather than copied
    from this plan. Run:
    `ls -1 src/components/blocks | wc -l`, `find src/components/blocks -type f | wc -l`,
    `du -sh src/components/blocks`, and the zero-importer sweep
    `grep -rn "components/blocks" --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.cjs" --include="*.json" . --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git | grep -v "^./src/components/blocks/" | grep -v "^./.planning/"`.
    Expected on 2026-09-05: 25, 152, 1.1M, and exactly four hits — `eslint.config.mjs:37`,
    `eslint.config.mjs:139`, `tests/container-radius.test.ts:72`,
    `tests/server-action-error-contracts.test.ts:45`, none of which is an import. **If the
    sweep returns any hit that is an actual `import` or `from` statement, STOP** — a block
    became live since the audit and the deletion is no longer safe; report and do not proceed.

    Then edit `docs/design/reui-blocks-audit.md`. Insert a blockquote decision record
    immediately after the `# ReUI Pro blocks — audit` title line and BEFORE the existing
    `> Audited 2026-08-31 ...` blockquote, using the same `>` form for visual continuity.
    This is a new precedent — no dated decision-record prepend exists anywhere under `docs/`
    — so keep it to the house shapes already in this file: a `>` block plus the
    `(Antoine, YYYY-MM-DD)` inline attribution.

    The record must carry, in this order:
      1. A heading line reading `Decision: DELETED 2026-09-05` with the attribution
         `(Antoine, 2026-09-05 — D-36-02, Phase 36 HOUSE-04)`.
      2. The measured scope actually deleted, using the numbers observed in the step above:
         **25 block directories, 152 files, 1.1M** — with an explicit note that the
         `18 blocks / 104 files / 816K` figures in the body below are the 2026-08-31 count,
         taken before Phase 34 plan 34-03 vendored `solution-crm-1`…`solution-crm-6` and
         `solution-users-2`. The body figures are left as the historical record; the record
         above them is the current truth.
      3. The rationale in one sentence: reinstall is one command, so there is no cost to
         deleting and no value in keeping any block "just in case" beyond this document.
      4. The reinstall command verbatim: `npx shadcn@latest add @reui/<block-name>`.
      5. An explicit statement of what was NOT deleted: `src/components/reui/` and
         `src/components/ui/` are live and untouched.
      6. An explicit statement of the known residual: the two `'src/components/blocks/**'`
         entries in `eslint.config.mjs`'s `ignores` arrays (lines 37 and 139) are left in
         place because Phase 36 does not edit that file; they are inert globs matching
         nothing and are recorded here so a future maintainer removes them deliberately
         rather than discovering them as mystery cruft.
      7. A pointer that the mapping, the two structural constraints (Frame vs Card) and the
         mis-picks below are exactly what makes this deletion reversible.

    Finally, supersede the now-false sentence `Nothing was deleted.` in the existing
    2026-08-31 blockquote. Do NOT delete it — amend it in place to read as historical, e.g.
    by appending ` (true as of 2026-08-31; superseded by the decision record above.)`. The
    original audit's account of its own state must survive.

    Change nothing else in this file. The mapping table, the structural constraints, the
    mis-picks and the closing `auth-1` / `wizard-1` recommendation all stay verbatim.
  </action>
  <verify>
    <automated>grep -c 'DELETED 2026-09-05' docs/design/reui-blocks-audit.md</automated>
    <automated>grep -c 'npx shadcn@latest add @reui/' docs/design/reui-blocks-audit.md</automated>
    <automated>grep -n 'Nothing was deleted' docs/design/reui-blocks-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Decision: DELETED 2026-09-05' docs/design/reui-blocks-audit.md` returns `1`.
    - `grep -c '(Antoine, 2026-09-05' docs/design/reui-blocks-audit.md` returns `1`.
    - The record contains the literals `25`, `152` and `1.1M`, and the literal `D-36-02`.
    - The record contains the literals `solution-crm-1` and `solution-users-2` (the seven
      blocks the audit body predates).
    - The record contains `src/components/reui/` and the word `untouched`.
    - The record contains `eslint.config.mjs` and the literal `src/components/blocks/**`
      (the recorded residual).
    - `grep -c 'npx shadcn@latest add @reui/<block-name>' docs/design/reui-blocks-audit.md`
      returns ≥ 2 (once in the record, once in the pre-existing section).
    - `grep -c 'Nothing was deleted' docs/design/reui-blocks-audit.md` returns `1` AND that
      line now also contains `superseded`.
    - The decision-record blockquote appears BEFORE the line matching `^> Audited 2026-08-31`:
      `awk '/Decision: DELETED 2026-09-05/{a=NR} /^> Audited 2026-08-31/{b=NR} END{exit !(a<b)}'`
      exits 0.
    - `src/components/blocks/` still exists at this point — the record lands before the
      deletion: `test -d src/components/blocks`.
    - `git status --porcelain eslint.config.mjs` prints nothing.
  </acceptance_criteria>
  <done>The audit doc opens with a dated, attributed delete decision carrying the measured figures, the reinstall command, the untouched-`reui/` statement and the recorded eslint residual — and the tree is still present, so nothing has been lost.</done>
</task>

<task type="auto">
  <name>Task 2: Delete src/components/blocks/ and clean the two dead test references</name>
  <files>src/components/blocks/, tests/container-radius.test.ts, tests/server-action-error-contracts.test.ts</files>
  <read_first>
    - `tests/container-radius.test.ts` lines 60-80 — the comment block
      `// ── Assertion 1 allow-list — the literal sweep ──` and the line
      `const EXCLUDED_DIRS = ['src/components/blocks'];` at line 72. This is an exclusion
      path for a directory walk, NOT an import.
    - `tests/server-action-error-contracts.test.ts` lines 35-55 — `collectSourceFiles` and
      the line
      `if (full.includes('components/reui') || full.includes('components/blocks')) continue;`
      at line 45, plus the two-line comment above it explaining why vendored code is skipped.
      Only the `components/blocks` half is removed; `components/reui` stays.
    - `src/components/reui/` — list it before and after so the 13 entries can be shown
      untouched.
    - `docs/design/reui-blocks-audit.md` — the decision record written in Task 1; the
      deletion is only safe because it exists.
  </read_first>
  <action>
    Order matters: confirm Task 1's record is committed, then delete, then fix the tests.

    Step 1 — delete the tree with `git rm -r src/components/blocks` (use `git rm`, not `rm`,
    so the removal is staged as a tracked deletion and shows in the diff as 152 deletions
    rather than as untracked churn). Do NOT touch `src/components/reui/` or
    `src/components/ui/`.

    Step 2 — `tests/container-radius.test.ts` line 72. The `EXCLUDED_DIRS` array now names a
    directory that does not exist. Remove the `'src/components/blocks'` element. If that
    leaves the array empty, keep the constant declared as an empty array rather than deleting
    it — the directory walk reads it and other exclusions may be added later — and update the
    comment block above it (lines 68-71) so it no longer claims to exclude "vendored ReUI
    reference blocks", replacing that rationale with a one-line note that the vendored blocks
    were deleted 2026-09-05 (Phase 36, HOUSE-04) and pointing at
    `docs/design/reui-blocks-audit.md`. An empty array must not trip ESLint's
    `no-unused-vars` — if the constant becomes genuinely unreferenced, delete it and its
    comment block together.

    Step 3 — `tests/server-action-error-contracts.test.ts` line 45. Remove only the
    ` || full.includes('components/blocks')` half of the condition, leaving
    `if (full.includes('components/reui')) continue;` intact. Keep the two-line comment above
    it (it still explains why `reui/` is skipped) and append a short clause noting the
    `blocks` half was removed on 2026-09-05 when the vendored blocks were deleted.

    Step 4 — run the full gate battery in order and record each exit code:
    `npm run typecheck`, `npm run lint:check`, `npm test`, `npm run build`. D-36-02 is
    explicit that a drop of 152 files must not move any of the four. Capture the test count
    before and after (`npm test` output tail) and record it in the summary; a change in test
    count means the deletion changed what the directory-walking tests scan, which must be
    explained rather than accepted.

    Do NOT edit `eslint.config.mjs` even though its two `'src/components/blocks/**'` ignores
    entries are now inert. That residual is recorded in Task 1's decision record on purpose.
  </action>
  <verify>
    <automated>test ! -d src/components/blocks &amp;&amp; echo "blocks deleted OK"</automated>
    <automated>ls -1 src/components/reui | wc -l</automated>
    <automated>grep -rn "components/blocks" tests/ | wc -l</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint:check</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src/components/blocks` succeeds — the directory is gone.
    - `git status --porcelain -- src/components/blocks | grep -c '^D '` returns `152` (the
      measured file count from Task 1), confirming a staged tracked deletion, not an
      untracked `rm`. Scope the status query to that path — plans 36-01 and 36-02 share this
      working tree in the same wave and will have their own uncommitted changes.
    - `ls -1 src/components/reui | wc -l` returns `13`, and
      `git status --porcelain src/components/reui src/components/ui` prints nothing.
    - `grep -rn "components/blocks" tests/` returns no match.
    - `grep -c "full.includes('components/reui')" tests/server-action-error-contracts.test.ts`
      returns `1` — the live half of the skip condition survived.
    - `grep -c "components/blocks" tests/server-action-error-contracts.test.ts` returns `0`.
    - `npm run typecheck` exits 0.
    - `npm run lint:check` exits 0 with no output (CI runs `eslint . --max-warnings=0`, so an
      unused `EXCLUDED_DIRS` constant would fail here even though `tsc` passes).
    - `npm test` exits 0 and reports the same number of passing tests as the pre-deletion
      baseline captured at the start of step 4; any delta is explained in the summary.
    - `npm run build` exits 0.
    - `git status --porcelain -- eslint.config.mjs package.json package-lock.json` prints
      nothing.
  </acceptance_criteria>
  <done>`src/components/blocks/` is gone as a staged tracked deletion, `src/components/reui/` and `src/components/ui/` are byte-identical, the two dead test references are cleaned, and all four gates pass with an unchanged test count.</done>
</task>

<task type="auto">
  <name>Task 3: Amend .planning/REQUIREMENTS.md so its own record stops contradicting the deletion</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 117-119 — HOUSE-04's body, currently
      `- [ ] **HOUSE-04**: The 18 dead vendored ReUI blocks (816K, zero imports) carry a recorded`
      / `  keep-or-delete decision with its rationale, superseding the provisional "Delete nothing yet"`
      / `  of 2026-08-31.` Both figures are the 2026-08-31 audit count.
    - `.planning/REQUIREMENTS.md` lines 145-146 — the § Out of Scope bullet
      `- **Deleting the vendored ReUI blocks** — HOUSE-04 makes the call. If the call is "delete", the`
      / `  deletion is its own work, not this milestone's.`
    - `.planning/ROADMAP.md` § Phase 36 success criterion 4 — already amended during planning
      with a dated `*(Amended 2026-09-05 by D-36-02: ...)*` parenthetical. Mirror that wording
      and that form so the two records read consistently.
    - `docs/design/reui-blocks-audit.md` — the decision record written in Task 1, which is what
      both amendments point at.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md` § D-36-02.
  </read_first>
  <action>
    Two edits in `.planning/REQUIREMENTS.md`, both additive and dated. Run this task AFTER the
    deletion has landed so the amendment describes a fact rather than an intention.

    Edit 1 — the § Out of Scope bullet at lines 145-146. Keep the original two lines verbatim
    (they are the record of the v1.8 scoping decision as written on 2026-09-05 at roadmap time)
    and append a dated amendment clause in the same italic-parenthetical form the ROADMAP now
    uses: `*(Amended 2026-09-05 by D-36-02: the call is delete, and the deletion was performed
    in Phase 36 plan 36-03 rather than deferred — see `docs/design/reui-blocks-audit.md` and
    `.planning/ROADMAP.md` § Phase 36 criterion 4.)*`. Do not delete the original sentence; a
    reader must be able to see that the scope was widened deliberately and when.

    Edit 2 — HOUSE-04's body at lines 117-119. Correct the stale figures: `The 18 dead
    vendored ReUI blocks (816K, zero imports)` becomes the measured
    `25 dead vendored ReUI block directories (152 files, 1.1M, zero imports)`, with a short
    parenthetical recording that the 18 blocks / 816K figures came from the 2026-08-31 audit,
    written before Phase 34 plan 34-03 vendored `solution-crm-1`…`solution-crm-6` and
    `solution-users-2`. Leave the `- [ ]` checkbox alone — marking HOUSE-04 complete is
    execute-phase's job, not this task's.

    Do NOT touch any other requirement, do not touch the traceability tables at the foot of
    the file, and do not touch `.planning/ROADMAP.md` (already amended) or
    `.planning/v1.6-MILESTONE-AUDIT.md` (Phase 40 / CLOSE-06 owns it).
  </action>
  <verify>
    <automated>grep -c 'Amended 2026-09-05 by D-36-02' .planning/REQUIREMENTS.md</automated>
    <automated>! grep -q 'The 18 dead vendored ReUI blocks (816K' .planning/REQUIREMENTS.md &amp;&amp; echo 'HOUSE-04 figures corrected OK'</automated>
    <automated>test "$(grep -c 'deletion is its own work, not this milestone' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; echo 'original scoping sentence preserved OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Amended 2026-09-05 by D-36-02' .planning/REQUIREMENTS.md` returns `1`.
    - The original § Out of Scope sentence survives:
      `grep -c "deletion is its own work, not this milestone" .planning/REQUIREMENTS.md`
      returns `1`.
    - HOUSE-04 no longer carries the stale figures:
      `grep -c '18 dead vendored ReUI blocks' .planning/REQUIREMENTS.md` returns `0`, and the
      HOUSE-04 body contains the literals `25`, `152 files` and `1.1M`.
    - HOUSE-04 records the provenance of the old numbers: its body contains
      `2026-08-31` and `solution-crm-1`.
    - The checkbox was not flipped: `grep -c '^- \[ \] \*\*HOUSE-04\*\*' .planning/REQUIREMENTS.md`
      returns `1`.
    - No other requirement changed: `git diff -U0 -- .planning/REQUIREMENTS.md` contains no
      `+`/`-` line matching `HOUSE-0[123]|CLOSE-|OPS-|GAP-`.
    - The traceability tables are untouched: `git diff -U0 -- .planning/REQUIREMENTS.md`
      contains no `+`/`-` line starting with `|`.
    - `git status --porcelain -- .planning/ROADMAP.md .planning/v1.6-MILESTONE-AUDIT.md`
      prints nothing.
  </acceptance_criteria>
  <done>`.planning/REQUIREMENTS.md` § Out of Scope carries a dated amendment pointing at the performed deletion, HOUSE-04 states the measured 25 / 152 / 1.1M with the provenance of the old figures, and both original sentences survive as history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored tree → shipped bundle | `src/components/blocks/` vs `src/components/reui/` is the dead/live boundary; a deletion that crosses it removes code that shipped surfaces import |
| repo → audit document | The audit is the only artefact that makes the deletion reversible; losing or contradicting it turns a reversible cleanup into permanent loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | Denial of service | `src/components/reui/` and `src/components/ui/` | mitigate | Deletion is scoped to `src/components/blocks/` by an explicit `git rm -r` of that single path. Acceptance requires `ls -1 src/components/reui \| wc -l` to return `13` and `git status --porcelain src/components/reui src/components/ui` to be empty, so a mis-scoped delete of the live primitives fails the task rather than shipping. |
| T-36-11 | Denial of service | shipped surfaces importing a block | mitigate | Task 1 re-runs the zero-importer sweep at execution time and halts if any hit is an actual `import`/`from`. The four-gate battery (`typecheck`, `lint:check`, `test`, `build`) is then the second net — a broken import fails `build`. |
| T-36-12 | Repudiation | irreversible deletion | mitigate | The record lands before the tree goes (Task 1 acceptance asserts `test -d src/components/blocks` still passes), and the record carries the exact reinstall command `npx shadcn@latest add @reui/<block-name>`, so the deletion is one command from reversal. `git rm` also keeps the content recoverable from history. |
| T-36-13 | Tampering | stale figures in the record | mitigate | The record must state the figures measured at execution time (25/152/1.1M) and must explicitly flag the audit body's 18/104/816K as the 2026-08-31 count, naming the seven Phase 34 blocks that account for the difference. A record with wrong numbers reads as false to a future auditor and is caught by the literal-string acceptance criteria. |
| T-36-14 | Tampering | ESLint scope | mitigate | `eslint.config.mjs` is not edited; acceptance asserts it is byte-identical to HEAD. The two now-inert `'src/components/blocks/**'` ignores entries are recorded as a known residual in the audit doc rather than silently removed, so no ESLint scope change hides inside a deletion commit. |
| T-36-14a | Repudiation | scope widened without a record | mitigate | `.planning/REQUIREMENTS.md` § Out of Scope explicitly said the deletion was not this milestone's work. Task 3 amends it in place with a dated `*(Amended 2026-09-05 by D-36-02: ...)*` clause and preserves the original sentence, so a future auditor sees that the scope was widened deliberately, when, and on whose authority — rather than finding a milestone that quietly did what its own record forbade. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | Nothing is installed. `npm run build` and `npm test` use the existing lockfile; `package.json` and `package-lock.json` must be byte-identical to HEAD (asserted). No `npx shadcn@latest add` is executed in this plan — the command is only recorded as documentation. |
</threat_model>

<verification>
1. `test ! -d src/components/blocks` succeeds.
2. `ls -1 src/components/reui | wc -l` returns 13.
3. `grep -rn "components/blocks" src/ tests/ docs/ --include="*.ts" --include="*.tsx"` returns no match.
4. `npm run typecheck && npm run lint:check && npm test && npm run build` — all four exit 0.
5. `grep -c 'Decision: DELETED 2026-09-05' docs/design/reui-blocks-audit.md` returns 1.
6. `git status --porcelain -- eslint.config.mjs package.json package-lock.json` prints
   nothing (path-scoped — sibling wave-1 plans share this tree).
7. `grep -c 'Amended 2026-09-05 by D-36-02' .planning/REQUIREMENTS.md` returns 1 and
   `grep -c '18 dead vendored ReUI blocks' .planning/REQUIREMENTS.md` returns 0.
</verification>

<success_criteria>
- The audit doc carries a dated, attributed delete decision with the measured figures, the
  reinstall command, the untouched-`reui/` statement and the recorded eslint residual.
- `src/components/blocks/` is deleted as a staged tracked removal of 152 files.
- `src/components/reui/` and `src/components/ui/` are untouched.
- The two dead test references are cleaned and the `components/reui` skip survives.
- All four CI gates pass and the test count is unchanged.
- `.planning/REQUIREMENTS.md` § Out of Scope carries the dated D-36-02 amendment and HOUSE-04
  states the measured 25 / 152 / 1.1M figures, so the milestone record no longer contradicts
  what this plan performed.
</success_criteria>

<output>
Create `.planning/phases/36-gate-repair-planning-record-hygiene/36-03-SUMMARY.md` when done.
</output>
