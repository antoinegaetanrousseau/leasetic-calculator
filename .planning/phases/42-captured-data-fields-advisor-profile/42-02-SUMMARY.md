---
phase: 42-captured-data-fields-advisor-profile
plan: 02
subsystem: database
tags: [drizzle, postgres, neon, migration, schema, better-auth]

# Dependency graph
requires:
  - phase: 42-captured-data-fields-advisor-profile
    provides: "Plan 01's REQUIREMENTS/ROADMAP reconciliation and FR/EN dictionary keys this phase's downstream plans consume"
provides:
  - "users.telephone and users.company_telephone nullable text columns"
  - "leasetic_advisor singleton table (fixed-id row 00000000-0000-0000-0000-000000000001) seeded and applied to production"
  - "Migration 0011_phase42_captured_data committed, journal-synced, AND applied to the production Neon branch — the structural gate Plan 42-04 reads before registering Better Auth additionalFields"
affects: [42-04, 42-05, 42-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Blocking operator checkpoint as a separate plan between schema/migration authoring and Better Auth additionalFields registration, so the ordering that avoids the documented 'APIError: Failed to get session' outage (RESEARCH R2 Pitfall 1) is structural, not a comment someone can miss"
    - "Fixed-id singleton table (leasetic_advisor) updated in place by UPDATE, not append-only history — diverges deliberately from the globalParams pattern per D-08/D-09"

key-files:
  created:
    - drizzle/0011_phase42_captured_data.sql
    - drizzle/meta/0011_snapshot.json
  modified:
    - src/db/schema.ts
    - drizzle/meta/_journal.json
    - src/lib/pdf/no-commission.test.ts

key-decisions:
  - "Migration 0011 applied to production via the MIGRATE PROD GitHub Action (workflow_dispatch, branch main, confirm phrase MIGRATE PROD) — run 34241648389, both jobs (dry run + apply) succeeded, environment gate approved by Antoine ~2026-09-08 15:04 UTC"
  - "No local db:migrate or drizzle-kit push was ever run — .env.local resolves to the production Neon branch in this repo, so migration application is exclusively a GitHub Actions / environment-gated operation"

requirements-completed: []  # Intentionally empty — this plan lands storage primitives only; PROF-01/PROF-03/FIELD-02 close when the surfaces that read/write these columns ship (Plans 42-04/05/06), per operator instruction not to tick requirements prematurely in this phase.

# Metrics
duration: ~2h41m (Tasks 1-2: ~4min; remainder was elapsed wall-clock time waiting on the operator's MIGRATE PROD run and environment-gate approval)
completed: 2026-09-08
---

# Phase 42 Plan 02: Users Telephone Columns + leasetic_advisor Singleton + Production Migration Summary

**Declared `users.telephone`/`users.company_telephone` and the `leasetic_advisor` singleton table in Drizzle, generated and committed migration `0011_phase42_captured_data`, and got an operator to apply it to the production Neon branch via the gated `MIGRATE PROD` GitHub Action (run 34241648389, both jobs green) before any Better Auth `additionalFields` registration could deploy.**

## Performance

- **Duration:** ~2h41m total elapsed (Tasks 1-2 completed in ~4 minutes of active work; the remainder is wall-clock time spent waiting on the operator to run and approve the `MIGRATE PROD` workflow, which is expected for a `checkpoint:human-action` task)
- **Started:** 2026-09-08T12:29:48Z (Task 1 commit)
- **Completed:** 2026-09-08T15:10:34Z (this summary)
- **Tasks:** 3 (2 auto + 1 checkpoint:human-action)
- **Files modified:** 5 (`src/db/schema.ts`, `drizzle/0011_phase42_captured_data.sql`, `drizzle/meta/_journal.json`, `drizzle/meta/0011_snapshot.json`, `src/lib/pdf/no-commission.test.ts`)

## Accomplishments

- `src/db/schema.ts` now declares two nullable `users` columns — `companyTelephone` (`company_telephone`, admin-write-only via the partner form) and `telephone` (`telephone`, partner-writable via `/parametres` and admin-seedable) — plus the `leasetic_advisor` singleton table (`id` fixed-literal `uuid` primary key, no `.defaultRandom()`; nullable `name`/`fonction`/`telephone`/`email`; `updatedAt`/`updatedBy` audit pair), with a module comment recording why it is a fixed-id-UPDATE singleton and not an append-only history table (D-08/D-09). `src/lib/auth/index.ts` was left untouched, as required.
- Migration `drizzle/0011_phase42_captured_data.sql` was generated by `drizzle-kit generate` (no DB connection), renamed and journal-synced (`_journal.json` idx 11, tag `0011_phase42_captured_data`), carrying a hand-written header documenting the generator-output-vs-hand-added-seed split, plus an idempotent `INSERT ... ON CONFLICT ("id") DO NOTHING` seeding the advisor row at `00000000-0000-0000-0000-000000000001`.
- **Migration 0011 is now applied to the production Neon branch.** The operator ran the `MIGRATE PROD` GitHub Action (`.github/workflows/db-migrate.yml`, `workflow_dispatch`, `branch: main`, confirm phrase `MIGRATE PROD`), approved the `production` environment's required-reviewer gate at ~2026-09-08 15:04 UTC, and both jobs completed successfully.

## Migration Apply Evidence (unblocking Plans 42-04/05/06)

**This section is the gate Plan 42-04's first task reads before registering Better Auth `additionalFields`.**

- **Workflow run:** `34241648389`
- **Run URL:** https://github.com/antoinegaetanrousseau/leasetic-calculator/actions/runs/34241648389
- **Workflow file:** `.github/workflows/db-migrate.yml` ("DB Migrate (Branch-Scoped)")
- **Trigger:** `workflow_dispatch`, `branch: main`, confirm phrase `MIGRATE PROD`
- **Environment gate:** `production` GitHub Environment, required-reviewer approval by Antoine at ~2026-09-08 15:04 UTC
- **Jobs — both completed `success`:**
  - `Dry run — list pending migrations` — log confirms `Production migration confirmation OK.`
  - `Apply migrations to main` — log confirms:
    - `Found 12 migration file(s)` including `- 0011_phase42_captured_data.sql (2358 bytes)`
    - `Applying migrations to ep-icy-boat-alx5o1tz-pooler.c-3.eu-central-1.aws.neon.tech ...`
    - `Migrations applied successfully.`
- **Conclusion:** `users.telephone`, `users.company_telephone` and the `leasetic_advisor` table (with its seeded singleton row) exist on the production Neon `main` branch as of this run. It is now safe for Plan 42-04 to register these columns as Better Auth `additionalFields` — the outage precondition described in RESEARCH R2 Pitfall 1 (registering `additionalFields` before the columns exist, causing `APIError: Failed to get session` on every authed page) no longer applies.

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare the two users columns and the leasetic_advisor singleton table** - `f034174` (feat)
2. **Task 2: Generate migration 0011, rename it, seed the advisor row, sync the journal** - `4e7bf25` (feat)
3. **Task 2 Rule-3 auto-fix: allowlist migration 0011 for ADMIN-09's commission guard** - `7f4ff42` (fix)
4. **Task 3: checkpoint:human-action** - no commit (GitHub Actions run `34241648389`, operator-executed; see Migration Apply Evidence above)

**Plan metadata:** this summary's commit (see below)

## Files Created/Modified

- `src/db/schema.ts` - Two new nullable `users` columns (`telephone`, `companyTelephone`) and the `leaseticAdvisor` singleton table
- `drizzle/0011_phase42_captured_data.sql` - Generated DDL for the two columns and the advisor table, plus the hand-added idempotent seed INSERT
- `drizzle/meta/_journal.json` - New entry, idx 11, tag `0011_phase42_captured_data`
- `drizzle/meta/0011_snapshot.json` - Drizzle-kit generated schema snapshot for migration 0011
- `src/lib/pdf/no-commission.test.ts` - Migration 0011 added to the `KNOWN_MIGRATIONS` allowlist (Rule 3 auto-fix, see below)

## Decisions Made

- Migration application happened exclusively through the gated `MIGRATE PROD` GitHub Action — no local `db:migrate` or `drizzle-kit push` was run at any point, consistent with `.env.local` resolving to the production Neon branch in this repo and the `check:no-drizzle-push` CI guard.
- `requirements-completed` is intentionally left empty in this summary's frontmatter, and no requirement IDs were marked complete in `.planning/REQUIREMENTS.md`, per explicit operator instruction: this plan lands storage primitives only, and PROF-01/PROF-03/FIELD-02 should close when the surfaces that actually read/write these columns ship (Plans 42-04/05/06), not when the underlying columns merely exist.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added migration 0011 to the ADMIN-09 KNOWN_MIGRATIONS allowlist**
- **Found during:** Task 2 (migration generation)
- **Issue:** `src/lib/pdf/no-commission.test.ts` runs a Drizzle-journal guard (ADMIN-09's commission-invisibility test) that fails CI on any migration file not explicitly reviewed and allow-listed. The newly generated `0011_phase42_captured_data.sql` was not yet in the allowlist, which would have failed the test suite.
- **Fix:** Reviewed migration 0011's DDL (users.telephone / users.company_telephone / leasetic_advisor — no commission-related column) and added its tag to `KNOWN_MIGRATIONS`.
- **Files modified:** `src/lib/pdf/no-commission.test.ts`
- **Verification:** Full suite green — 2582 passed, 0 failed.
- **Committed in:** `7f4ff42`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to keep CI green; no scope creep — the fix only allow-lists a migration this plan itself generated.

## Issues Encountered

None beyond the expected wait for the `checkpoint:human-action` task — the operator ran and approved the `MIGRATE PROD` workflow (run `34241648389`) and both its jobs (dry run + apply) completed successfully, as recorded in the Migration Apply Evidence section above.

## User Setup Required

None beyond the completed checkpoint. The one external action this plan required — applying migration 0011 to the production Neon branch via the `MIGRATE PROD` GitHub Action — is done and verified green (run `34241648389`).

## Next Phase Readiness

- `users.telephone`, `users.company_telephone` and `leasetic_advisor` (with its seeded singleton row) exist on the production Neon `main` branch, confirmed by the `MIGRATE PROD` apply job's log ("Migrations applied successfully.", naming `0011_phase42_captured_data.sql`).
- **Plan 42-04 is unblocked**: it may now register these three columns as Better Auth `additionalFields` without reproducing the RESEARCH R2 Pitfall 1 outage, because the columns already exist on production.
- **Plans 42-05 and 42-06** (which read/write `leasetic_advisor` and the telephone columns) are also unblocked by this migration landing on production.
- `src/lib/auth/index.ts` remains untouched, exactly as this plan required — registration is Plan 42-04's sole responsibility.

---
*Phase: 42-captured-data-fields-advisor-profile*
*Completed: 2026-09-08*

## Self-Check: PASSED

All 5 claimed files found on disk; all 3 task commit hashes (f034174, 4e7bf25, 7f4ff42) found in git history. The `checkpoint:human-action` task (Task 3) has no commit by design — its evidence is the GitHub Actions run recorded above, which is not a local git artifact to self-check against `git log`.
