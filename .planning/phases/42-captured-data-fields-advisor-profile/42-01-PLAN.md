---
phase: 42-captured-data-fields-advisor-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - src/lib/i18n/dictionaries.ts
  - src/lib/i18n/dictionaries.test.ts
autonomous: true
requirements: [FIELD-02, PROF-01, PROF-02, PROF-03]

must_haves:
  truths:
    - "REQUIREMENTS.md FIELD-02 no longer claims the partner company telephone is a wizard field or a finalization blocker"
    - "REQUIREMENTS.md PROF-01 no longer claims a partner can set their fonction"
    - "REQUIREMENTS.md PROF-02 names telephone as the single missing-field condition"
    - "REQUIREMENTS.md PROF-03 splits the partner block (creating user) from the advisor block (admin setting)"
    - "REQUIREMENTS.md DOC-03 describes the partner-first / advisor-second card order and the translated fonction"
    - "ROADMAP.md Phase 42 success criteria 1-4 carry the same four corrections"
    - "Every FR/EN copy string this phase's four UI surfaces need exists in both dictionaries"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended FIELD-02, PROF-01, PROF-02, PROF-03, DOC-03"
      contains: "Restated 2026-09-08 by Phase 42"
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 42 success criteria 1-4 plus a Phase 43 planning note"
      contains: "Criteria 1-4 reconciled 2026-09-08"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "All new FR/EN keys for the SIRET field, /parametres telephone+fonction, admin partner telephone, admin advisor page, finalize dialog, legacy-draft toast, and the PDF fonction labels"
      contains: "admin.advisor.hero.title"
  key_links:
    - from: "src/lib/i18n/dictionaries.ts"
      to: "src/lib/i18n/dictionaries.test.ts"
      via: "FR/EN parity assertions over the new key set"
      pattern: "wizard.finalize.dialog.missingPhone.title"
---

<objective>
Reconcile the two upstream planning documents this phase's discussion contradicted (D-15, D-20,
D-21, D-22, D-23, D-24), and land the complete FR/EN copy deck every other Phase 42 plan consumes.

Purpose: CONTEXT.md's locked decisions invalidate four requirement statements and four ROADMAP
success criteria as literally written. Leaving them stale means the verifier grades this phase
against claims the operator explicitly retracted. Landing all i18n keys in one plan also gives
`src/lib/i18n/dictionaries.ts` a single owner, so the six downstream plans never race on it.

Output: amended `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, plus every new dictionary
key with an FR and an EN value and a parity test that names them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-captured-data-fields-advisor-profile/42-CONTEXT.md
@.planning/phases/42-captured-data-fields-advisor-profile/42-UI-SPEC.md
@.planning/phases/41-typography-migration/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend REQUIREMENTS.md FIELD-02, PROF-01, PROF-02, PROF-03 and DOC-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the four bullets under `### Fields` / `### Profile` and `DOC-03` under `### Document`
    - `.planning/phases/42-captured-data-fields-advisor-profile/42-CONTEXT.md` — D-06..D-16 for the substance, D-15/D-20/D-21/D-22/D-24 for the exact retractions
    - `.planning/phases/41-typography-migration/41-CONTEXT.md` — D-02, the precedent for amending upstream docs in place (note how Phase 41's amendment is annotated inline with a dated authority line rather than silently rewritten)
  </read_first>
  <action>
Edit the five requirement bullets in place. Keep the existing `- [ ] **ID**:` bullet shape and the
existing inline-italic `*(...)*` annotation convention already used by DOC-06 and DOC-09.

**FIELD-02** — replace the body with: the partner company's telephone is held on the partner
account (admin-set) and carried onto every proposal's immutable `inputs` at draft creation.
Append the annotation: `*(Restated 2026-09-08 by Phase 42 D-15 / D-11 / D-13: it is not a wizard
field, and it never blocks finalization — the column ships nullable and an absent value renders
as an em dash under DOC-11.)*`

**PROF-01** — replace the body with: a partner can set their own telephone on their account and
sees their fonction, name and email on the settings page; fonction is read-only. Append:
`*(Restated 2026-09-08 by Phase 42 D-20 / D-14: "can set their fonction" is dropped — fonction is
`users.partnerType`, admin-assigned and client-immutable by `input: false`.)*`

**PROF-02** — replace the body with: a partner whose account is missing a telephone is stopped at
proposal finalization with a message naming the telephone field and linking to `/parametres`.
Append: `*(Restated 2026-09-08 by Phase 42 D-21 / D-14: narrowed from "missing fonction or
telephone" — `partner_type` is NOT NULL so fonction can never be the missing thing. The "fixed
once, never re-prompted per proposal" clause is dropped as a behaviour to build: it follows
automatically from a check that reads the account.)*`

**PROF-03** — replace the body with a split statement: a finalized proposal's **partner** block is
sourced from the authenticated creating user's account rather than retyped per proposal; its
**advisor** block is sourced from a single admin-editable Leasetic advisor setting (name,
fonction, telephone, email) read live at render time and never snapshotted into `inputs`. Append:
`*(Restated 2026-09-08 by Phase 42 D-22 / D-06 / D-07 / D-08 / D-09: the advisor is a
Leasetic-side person who supports the partner's relationship with the end client, not the creating
user.)*`

**DOC-03** — replace the body so the partner company, commercial and telephone are the headline
contact and the Leasetic advisor's name, fonction, telephone and email sit beneath as the
supporting contact; state that the advisor's fonction renders from the FR/EN label pair matching
the proposal's committed `language`. Append: `*(Amended 2026-09-08 by Phase 42 D-10 / D-16 via
D-24: this deliberately deviates from `Quote-FR-A.dc.html:67-74`, which makes `advisorName` the
11pt/600 headline. The deviation is intended — Phase 43 must not treat it as a spec violation. The
two blocks are re-ordered, not relabelled.)*`

Do not touch FIELD-01, FIELD-03, the MIG-* block, the Traceability table, or the Out of Scope
section. Do not change any checkbox state.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); const need=['Restated 2026-09-08 by Phase 42 D-15','Restated 2026-09-08 by Phase 42 D-20','Restated 2026-09-08 by Phase 42 D-21','Restated 2026-09-08 by Phase 42 D-22','Amended 2026-09-08 by Phase 42 D-10']; const missing=need.filter(n=>!s.includes(n)); const stale=['must supply the partner company','set their fonction and telephone','missing fonction or telephone','come from the authenticated creating user'].filter(n=>s.includes(n)); if(missing.length||stale.length){console.error('missing:',missing,'stale:',stale);process.exit(1);} console.log('REQUIREMENTS amendments OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains all five dated annotation strings asserted by the verify command.
    - None of these retracted phrases survives anywhere in the file: `must supply the partner company`, `set their fonction and telephone`, `missing fonction or telephone`, `come from the authenticated creating user`.
    - `FIELD-01`, `FIELD-03` and every `MIG-*` bullet are byte-identical to their pre-task text (`git diff .planning/REQUIREMENTS.md` shows changes only inside the FIELD-02, PROF-01, PROF-02, PROF-03 and DOC-03 bullets).
    - No checkbox `- [ ]` / `- [x]` state changes.
  </acceptance_criteria>
  <done>REQUIREMENTS.md states the post-discussion truth for all five requirements, each carrying a dated authority annotation naming its D-NN.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ROADMAP.md Phase 42 success criteria 1-4 and add the Phase 43 planning note</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the `### Phase 42: Captured Data — Fields & Advisor Profile` entry (its `**Goal:**` line and the four `**Success Criteria**` items), and the `### Phase 43: New PDF Layout` entry immediately below it
    - `.planning/ROADMAP.md` — the Phase 41 entry's `> **Criteria 1 and 3 reconciled 2026-09-08**` blockquote, which is the formatting precedent for the reconciliation note this task adds
    - `.planning/phases/42-captured-data-fields-advisor-profile/42-CONTEXT.md` — D-23 and D-24
  </read_first>
  <action>
Rewrite the four Phase 42 success criteria to match Task 1's amended requirements (D-23):

1. The partner must supply the client's SIRET — validated as 14 digits, and whose first 9 digits
   equal the proposal's SIREN — before the proposal can be finalized; it persists in that
   proposal's immutable `inputs`. The partner company's telephone is admin-set on the partner
   account and session-hydrated into the same `inputs`; it is not a wizard field and never blocks
   finalization.
2. A partner can set their own téléphone on `/parametres` and sees their fonction there as
   read-only text derived from the admin-assigned `partnerType`, alongside the name and email
   already shown.
3. A partner whose account is missing a téléphone is stopped at proposal finalization by a dialog
   naming the téléphone field and linking to `/parametres`.
4. A newly finalized proposal's partner block is sourced from the authenticated creating user's
   account; the advisor block is sourced from a single admin-editable Leasetic advisor setting,
   read live at render time and never snapshotted into `inputs`.

Also update the Phase 42 `**Goal:**` line: it currently says the advisor identity is "sourced from
the creating user's account", which D-06 invalidates. Restate the goal so the advisor identity is
sourced from a single admin-editable Leasetic advisor setting.

Immediately beneath the four criteria, add a blockquote note in the exact shape of the Phase 41
entry's existing one, opening `> **Criteria 1-4 reconciled 2026-09-08** (Phase 42 discussion —
`42-CONTEXT.md` D-23, executed by Plan 42-01).` and stating in one sentence per criterion what was
retracted, with `Authority: .planning/phases/42-captured-data-fields-advisor-profile/42-CONTEXT.md`.

Finally, under the Phase 43 entry, add a `**Planning note:**` paragraph (the entry already has one
about `@react-pdf/renderer` SVG support — add a second, do not overwrite it) recording D-24: Phase
43's `DOC-03` now carries D-10's partner-first card restructure and D-16's PDF-only fonction
translation, and Phase 42 landed the `pdf.partnerType.*` FR/EN label pairs Phase 43 renders.

Leave `**Plans:** TBD` for Phase 42 alone — the planner's roadmap-update step owns that line.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); const need=['Criteria 1-4 reconciled 2026-09-08','pdf.partnerType']; const stale=['the partner company’s telephone before the proposal can be finalized','view and set their own fonction','missing fonction or téléphone','from the authenticated creating user']; const m=need.filter(n=>!s.includes(n)); const st=stale.filter(n=>s.includes(n)); if(m.length||st.length){console.error('missing:',m,'stale:',st);process.exit(1);} console.log('ROADMAP amendments OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 42 entry contains a blockquote starting `> **Criteria 1-4 reconciled 2026-09-08**` that cites `42-CONTEXT.md` D-23.
    - The Phase 42 `**Goal:**` line no longer contains the substring `creating user`.
    - None of these phrases survives in `.planning/ROADMAP.md`: `the partner company's telephone before the proposal can be finalized`, `view and set their own fonction`, `missing fonction or téléphone`, `from the authenticated creating user`.
    - The Phase 43 entry retains its pre-existing `@react-pdf/renderer` planning note AND gains a second planning note containing the literal `pdf.partnerType`.
    - No other phase entry in the file is modified (`git diff` is confined to the Phase 42 and Phase 43 entries).
  </acceptance_criteria>
  <done>ROADMAP Phase 42's goal and four criteria describe the same behaviour the amended REQUIREMENTS bullets do, and Phase 43 carries D-24's inheritance note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land the complete FR/EN copy deck in dictionaries.ts</name>
  <files>src/lib/i18n/dictionaries.ts, src/lib/i18n/dictionaries.test.ts</files>
  <read_first>
    - `src/lib/i18n/dictionaries.ts` — the `form.client.siren*` FR block and its EN counterpart (the key-pair shape to copy), the `parametres.identity.*` block, the `partners.new.field.*` block, the `wizard.finalize.*` / `wizard.toast.finalize.error` block, and the `error.field.*` block
    - `src/lib/i18n/dictionaries.test.ts` — the existing parity suite, especially the "has at least N keys per language" count assertion that must be raised
    - `.planning/phases/42-captured-data-fields-advisor-profile/42-UI-SPEC.md` — § Copywriting Contract, the authoritative FR/EN strings; copy them verbatim
  </read_first>
  <behavior>
    - Every new FR key has an EN sibling and vice versa (the existing parity tests cover this once the keys land).
    - `dictionaries.fr['error.field.siret.mismatch']` is `Le SIRET doit commencer par les 9 chiffres du SIREN.` and the EN is `The SIRET must start with the SIREN's 9 digits.`
    - `dictionaries.fr['partners.new.field.phone']` is `Téléphone (société)` and EN is `Company phone` (relabel of an existing key, not a new one).
    - `dictionaries.en['pdf.partnerType.Commercial']` is `Sales Representative`; `pdf.partnerType.Agent` is `Agent` in both; `pdf.partnerType.Partenaire` is `Partenaire` (FR) / `Partner` (EN).
    - `dictionaries.fr['admin.advisor.hero.title']` is `Conseiller Leasetic` and EN is `Leasetic advisor` — spelled `Leasetic`, never `Leasétic`.
  </behavior>
  <action>
Add every key below to BOTH `dictionaries.fr` and `dictionaries.en`, placed beside its existing
namespace siblings (SIRET keys beside `form.client.siren*`; `parametres.identity.*` beside the
existing identity keys; `partners.new.field.*` beside the existing partner-form keys; the
`wizard.finalize.*` keys beside the existing finalize keys; `admin.advisor.*` as a new contiguous
block beside the other `admin.*` namespaces; `pdf.partnerType.*` as a new contiguous block).
Copy the FR/EN strings verbatim from `42-UI-SPEC.md` § Copywriting Contract.

New keys — SIRET (D-01, D-02):
`form.client.siret`, `form.client.siret.placeholder`, `error.field.siret.mismatch`.

New keys — `/parametres` (D-14, D-20):
`parametres.identity.telephone.label`, `parametres.identity.telephone.placeholder`,
`parametres.identity.fonction.label`, `parametres.identity.fonction.readonly.notice`.

New keys — admin partner form (D-19):
`partners.new.field.telephone`, `partners.new.field.telephone.placeholder`.

Relabel (not new) — `partners.new.field.phone`: FR `Téléphone (société)`, EN `Company phone`
(D-13; the form now carries two phone fields and the old bare `Téléphone` is ambiguous).

New keys — admin advisor page (D-08), all 13 from UI-SPEC item 4:
`admin.advisor.hero.title`, `admin.advisor.hero.subtitle`, `admin.advisor.section.identity`,
`admin.advisor.field.name`, `admin.advisor.field.fonction`,
`admin.advisor.field.fonction.placeholder`, `admin.advisor.field.telephone`,
`admin.advisor.field.email`, `admin.advisor.action.cancel`, `admin.advisor.action.save`,
`admin.advisor.action.save.spinner`, `admin.advisor.toast.saved`, `admin.advisor.toast.error`.

New keys — finalize dialog (D-18) and legacy-draft toast (D-05):
`wizard.finalize.dialog.missingPhone.title`, `wizard.finalize.dialog.missingPhone.body`,
`wizard.finalize.dialog.missingPhone.cta`, `wizard.finalize.dialog.missingPhone.dismiss`,
`wizard.finalize.toast.legacyMissingSiret`.

New keys — PDF fonction labels (D-16, rendered by Phase 43, not by this phase):
`pdf.partnerType.Agent` (FR `Agent` / EN `Agent`), `pdf.partnerType.Commercial`
(FR `Commercial` / EN `Sales Representative`), `pdf.partnerType.Partenaire`
(FR `Partenaire` / EN `Partner`). Add a comment above the block stating these are PDF-only per
D-16 and that the admin and `/parametres` surfaces deliberately keep rendering the raw
`partnerType` string (the inconsistency is a recorded Deferred item, not drift).

Do NOT add `error.field.siret.invalid`, `error.field.phone.invalid` or `error.field.required` —
all three already exist and are reused verbatim.

In `dictionaries.test.ts`: raise the minimum-key-count assertion to match the new total, and add a
`describe('Phase 42 captured-data keys')` block asserting the five exact FR/EN string pairs listed
in `<behavior>`, using bracket access (dot-notation keys are not valid identifiers).
  </action>
  <verify>
    <automated>npx vitest run src/lib/i18n/dictionaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/i18n/dictionaries.test.ts` exits 0, including the raised key-count assertion and the new `Phase 42 captured-data keys` describe block.
    - The FR/EN parity tests ("every FR key exists in EN" / "every EN key exists in FR") pass, proving no new key landed in only one language.
    - `node -e "const{dictionaries}=require('ts-node')" ` is NOT used; verification is the vitest command above.
    - `npm run lint:check` exits 0.
    - No occurrence of the string `Leasétic` or `LEASÉTIC` is introduced (the advisor hero copy uses `Leasetic`).
    - Both `src/lib/i18n/dictionaries.ts` and `src/lib/i18n/dictionaries.test.ts` are staged in the same commit (an untracked test file is silently skipped by CI).
  </acceptance_criteria>
  <done>All 27 new keys plus the one relabel exist in both languages, and the parity suite plus the five verbatim-string assertions are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → rendered UI | Dictionary strings are interpolated into partner-visible pages and (via Phase 43) into the PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-A | Information Disclosure | `src/lib/i18n/dictionaries.ts` new copy | mitigate | No new string names a commission, rate, coefficient or any derived economic value (ADMIN-09). The `pdf.partnerType.*` labels are business-classification words only. `npm test` re-runs `tests/admin-09-grep-contracts.test.ts` unchanged. |
| T-42-01-B | Tampering | `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` | accept | Planning documents are not executable and are reviewed in the same commit; the risk is documentation drift, not a runtime attack surface. Both edits carry dated authority annotations naming their D-NN so a reviewer can trace them. |
| T-42-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing — `42-RESEARCH.md` § Package Legitimacy Audit records that the phase adds zero packages. No install task exists to gate. |
</threat_model>

<verification>
- `npx vitest run src/lib/i18n/dictionaries.test.ts` green.
- `npm run lint:check` green (`eslint --max-warnings=0`).
- `npm run typecheck` green.
- The two node one-liners in Tasks 1 and 2 both print their OK line.
</verification>

<success_criteria>
- REQUIREMENTS.md FIELD-02, PROF-01, PROF-02, PROF-03 and DOC-03 each state the post-discussion behaviour and cite the D-NN that changed them.
- ROADMAP.md Phase 42's goal and success criteria 1-4 agree with those requirement bullets; Phase 43 carries D-24's note.
- Every FR/EN string the other eight Phase 42 plans reference resolves through `t()` in both languages.
- No downstream plan needs to edit `src/lib/i18n/dictionaries.ts`.
</success_criteria>

<output>
Create `.planning/phases/42-captured-data-fields-advisor-profile/42-01-SUMMARY.md` when done.
</output>
