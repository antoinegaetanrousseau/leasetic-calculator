---
phase: 37-crm-stack-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(authed)/proposals/[id]/page.tsx
  - app/(authed)/proposals/[id]/page.test.tsx
  - tests/admin-09-grep-contracts.test.ts
autonomous: true
requirements: [GAP-01]

must_haves:
  truths:
    - "D-37-01: an admin opening /proposals/{id} for a proposal owned by any partner sees the detail page instead of a 404"
    - "D-37-01: a partner or sales user opening /proposals/{id} for a proposal they do not own still gets notFound() — the response is indistinguishable from a nonexistent id"
    - "D-37-01: the admin branch is decided from the requireUser() server-derived role only; no request param, header or client prop participates"
    - "D-37-02: tests/admin-09-grep-contracts.test.ts carries a gate over /proposals/[id] and the whole suite is green"
    - "D-37-02: the new gate is proven non-vacuous — it fails when the rendered HTML contains commission_pct"
  artifacts:
    - path: "app/(authed)/proposals/[id]/page.tsx"
      provides: "role-aware ownership guard"
      contains: "role === 'admin'"
    - path: "app/(authed)/proposals/[id]/page.test.tsx"
      provides: "admin bypass + non-admin denial regression tests"
      contains: "role: 'admin'"
    - path: "tests/admin-09-grep-contracts.test.ts"
      provides: "20th ADMIN-09 gate over /proposals/[id]"
      contains: "proposals/[id]"
  key_links:
    - from: "app/(authed)/proposals/[id]/page.tsx"
      to: "src/lib/auth/require.ts requireUser()"
      via: "const { session, role } = await requireUser()"
      pattern: "const \\{ session, role \\} = await requireUser\\(\\)"
    - from: "tests/admin-09-grep-contracts.test.ts"
      to: "app/(authed)/proposals/[id]/page.tsx"
      via: "renderToString(createElement(ProposalDetailPage, ...)) + assertNoCommissionLeakage"
      pattern: "assertNoCommissionLeakage"
---

<objective>
Close GAP-01. `app/(authed)/proposals/[id]/page.tsx` currently returns `notFound()` for every
caller who does not own the proposal, which makes the admin oversight click-through
(company → relationship → proposal) a dead end. Per **D-37-01**, add an unconditional
server-derived `role === 'admin'` bypass to that one guard. Per **D-37-02**, pin the surface with a
20th gate in `tests/admin-09-grep-contracts.test.ts` — commission is structurally absent from this
page today, and the gate makes that absence enforced rather than incidental now that the page has
become admin-reachable.

Per **D-37-06** no UI-SPEC governs this plan: the page renders byte-identically, only its
reachability changes.

Purpose: an admin can finish the oversight click-through that Phase 30 documented as broken and
assigned to "Phase 33/34", which neither phase picked up.
Output: a role-aware guard, regression tests on both arms of it, and a non-vacuous ADMIN-09 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-crm-stack-closure/37-CONTEXT.md
@.planning/phases/37-crm-stack-closure/37-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-derived admin bypass on the proposal detail guard (D-37-01)</name>

  <files>
    app/(authed)/proposals/[id]/page.tsx
    app/(authed)/proposals/[id]/page.test.tsx
  </files>

  <read_first>
    - `app/(authed)/proposals/[id]/page.tsx` — the file being modified. The destructure is at
      line 40 (`const { session } = await requireUser();`) and the flat guard at lines 45-49
      (`if (!proposal || proposal.userId !== session.user.id) { notFound(); }`) with the D-18
      obscurity comment above it.
    - `app/(authed)/proposals/[id]/page.test.tsx` — the file being modified. Note the
      `vi.hoisted` mock trio (`requireUserMock`, `getCurrentLangMock`, `getProposalByIdMock`),
      the `beforeEach` default at line 95 (`requireUserMock.mockResolvedValue({ session: { user:
      { id: 'user-1', email: 'u@e.com' } } })` — it supplies no `role`), and the existing
      describe block `'/proposals/[id] page.tsx — Plan 14-06 D-28 StatusChip in header'`.
    - `app/(authed)/proposals/page.tsx` lines 69-97 — Phase 18 D-11's `adminUserIdOverride`.
      Copy the POSTURE (`const { session, role } = await requireUser();` plus the "role MUST come
      from the session, NEVER from request params" discipline comment). Do NOT copy the
      `?user_id=` query-param mechanism — D-37-01 rejects it explicitly.
    - `src/lib/auth/require.ts` lines 42-92 — `RequireUserResult` already returns `role: Role`
      (`'partner' | 'admin' | 'sales'`), re-read from the DB per request (AUTH-16) and
      fail-closed to `'partner'` on any unrecognized value. No signature change is needed.
    - `app/(authed)/layout.tsx` lines 17, 21-28, 42 — the same `role === 'admin'` comparison
      shape used elsewhere in the `(authed)` tree.
  </read_first>

  <action>
    Change the destructure at `app/(authed)/proposals/[id]/page.tsx` line 40 from
    `const { session } = await requireUser();` to `const { session, role } = await requireUser();`.

    Widen the guard so `notFound()` still fires when the proposal is absent, and fires on
    non-ownership only when the caller is not an admin. Concretely: keep `!proposal` as an
    independent short-circuit, and gate the ownership arm behind `role !== 'admin'`. Express the
    admin arm as a strict equality comparison against the literal `'admin'` — never a truthiness
    check, never `role != 'partner'`, so the `sales` role is denied like any partner.

    Replace the existing D-18 comment block with one that keeps its original claim and adds the
    new one. It must state, in this order: (a) D-18 obscurity — not-found and not-owned both
    return 404 for non-admin callers, unchanged; (b) D-37-01 / GAP-01 — an admin bypasses the
    ownership arm, matching the precedent Phase 18 D-11 already set for
    `app/(authed)/proposals/page.tsx`'s `?user_id=` list override; (c) T-37-01-01 — `role` MUST
    come from `requireUser()` (server-derived, per-request DB read), NEVER from `params`,
    `searchParams`, headers or a client prop.

    Do NOT touch `src/components/proposals/ProposalRow.tsx`. Do NOT add a query parameter, a
    prop, or any relationship join. The render path (lines ~187-306) is unchanged.

    Then extend `app/(authed)/proposals/[id]/page.test.tsx`:
    - Add `role: 'partner'` to the `beforeEach` default `requireUserMock.mockResolvedValue(...)`
      so the default caller is explicit rather than `undefined`.
    - Add a new describe block titled for GAP-01 / D-37-01 with four cases:
      1. admin + proposal owned by a different user id → the page renders (no `notFound()`
         thrown); assert a field from the render path is present, e.g. the fixture's client
         company name.
      2. partner + proposal owned by a different user id → `notFound()` path taken.
      3. sales + proposal owned by a different user id → `notFound()` path taken (the bypass is
         admin-only).
      4. admin + `getProposalById` resolves `null` → `notFound()` path taken (absence is not
         bypassable).
      Assert the `notFound()` cases by whatever mechanism the file already uses for control-flow
      assertions; if none exists, mock `next/navigation`'s `notFound` with a `vi.fn()` that
      throws a sentinel and assert the sentinel, keeping the existing tests green.
  </action>

  <verify>
    <automated>npx vitest run "app/(authed)/proposals/[id]/page.test.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `app/(authed)/proposals/[id]/page.tsx` contains the exact substring
      `const { session, role } = await requireUser();`
    - `app/(authed)/proposals/[id]/page.tsx` contains `role === 'admin'`
    - `grep -n "searchParams\|_callerRole\|headers()" "app/(authed)/proposals/[id]/page.tsx"`
      returns no line inside or above the guard block — the admin branch reads no request-derived
      value.
    - `grep -c "notFound()" "app/(authed)/proposals/[id]/page.tsx"` is unchanged from before the
      edit (the guard is widened, not duplicated).
    - `git diff --stat -- src/components/proposals/ProposalRow.tsx` is empty.
    - `npx vitest run "app/(authed)/proposals/[id]/page.test.tsx"` exits 0 with the 4 new GAP-01
      cases plus every pre-existing case in that file passing.
    - Behaviour: with `requireUserMock` resolving `{ session: { user: { id: 'user-1' } }, role:
      'admin' }` and `getProposalByIdMock` resolving a proposal whose `userId` is `'user-2'`, the
      rendered output contains the fixture's client company string. With `role: 'partner'` or
      `role: 'sales'` and the same proposal, `notFound` is called.
  </acceptance_criteria>

  <done>
    An admin reaches any partner's proposal detail page; partner and sales callers get the same
    404 they got before; the ownership decision is made from a per-request DB-read role.
  </done>
</task>

<task type="auto">
  <name>Task 2: 20th ADMIN-09 gate over /proposals/[id], proven non-vacuous (D-37-02)</name>

  <files>
    tests/admin-09-grep-contracts.test.ts
  </files>

  <read_first>
    - `tests/admin-09-grep-contracts.test.ts` — the file being modified. Read in full. Note
      specifically: the harness comment at lines 63-77 explaining that `renderToString()` runs
      server components with no live DB so every helper must be stubbed; the shared assertion
      helper `assertNoCommissionLeakage` and its two regexes `COMMISSION_PCT_RX`
      (`/\bcommission_pct\b/i`) and `PCT_SUFFIX_RX` (`/_pct\b/`) at lines 159-169; Gate 11
      (`describe('Gate 11: LcReferencesList ...')`, lines 466-493) as the closest-shaped
      existing gate; the D-30 exception block at lines 394-412; and the TWO existing
      `vi.mock('@/lib/auth/require', ...)` factories at lines 68-70 and 531-533, both of which
      mock `requireAdmin` ONLY.
    - `app/(authed)/proposals/[id]/page.tsx` — the surface being gated, as modified by Task 1.
      It is an `export default async function`, reads `params: Promise<{ id: string }>`, calls
      `requireUser()` and `getProposalById(id)`, and its render path reads only `proposal.inputs`
      and `proposal.computed`.
    - `app/(authed)/proposals/[id]/page.test.tsx` — the working mock recipe for this page
      (`@/lib/auth/require`, `@/lib/i18n`, `@/lib/db/queries` via `vi.importActual` spread, and
      the four heavy-component stubs `EmbeddedPdfPreview`, `DeleteButtonClient`,
      `RestoreButtonClient`, `CopyRefButton`). Reuse this recipe rather than inventing one.
    - `.planning/phases/37-crm-stack-closure/37-PATTERNS.md` § `tests/admin-09-grep-contracts.test.ts`
      — the hoisting caution and the "confirm it actually fails" instruction.
  </read_first>

  <action>
    Add a new top-level `describe` block after Gate 12 (around line 526) named for a 20th gate
    over `/proposals/[id]`, following the Gate 10-12 convention of appended top-level describes.

    Import the page with a default import, matching the existing `AdminHomePage` style at line
    123: `import ProposalDetailPage from '../app/(authed)/proposals/[id]/page';`.

    Resolve the mock-hoisting hazard FIRST. This file already registers
    `vi.mock('@/lib/auth/require', ...)` twice, both supplying only `requireAdmin`; the new gate
    needs `requireUser` resolving `{ session: { user: { id: 'admin-1' } }, role: 'admin' }`.
    Consolidate to a SINGLE `vi.mock('@/lib/auth/require', ...)` factory that exports both
    `requireAdmin` and `requireUser`, and delete the now-redundant duplicate. Confirm every
    pre-existing gate still passes after the consolidation — that is the proof the merge was
    correct, and it must be reported in the SUMMARY.

    Add the additional mocks the page needs and this file does not yet have: `@/lib/db/queries`
    (spread `vi.importActual`, override `getProposalById`), `@/lib/i18n` (spread the real
    dictionaries, override `getCurrentLang`), and the four heavy-component stubs listed in
    `read_first`. Every one of these is a NEW mocking surface for this file — no existing gate
    touches the `(authed)` tree.

    Build the fixture proposal to exercise the MAXIMUM render surface, in the spirit of the
    lines 63-77 harness note: populate every `inputs` key the page reads (`partnerCo`,
    `partnerName`, `clientCo`, `clientName`, `clientRole`, `clientTel`, `clientEmail`,
    `clientSiren`, `slb`, `evalParc`, `projectDesc`, `partnerRef`, `amountHT`, `durationMonths`,
    `validityDays`) and every `computed` key (`state`, `trancheKey`, `coeff`, `loyerHT`). Set the
    fixture's `userId` to a value different from the mocked session id so the gate renders the
    page through the new D-37-01 admin path, not the owner path.

    Call `assertNoCommissionLeakage(html, '/proposals/[id] detail page (admin bypass path)')` —
    reuse the shared helper unmodified; do NOT define a second assertion helper.

    Make the gate NON-VACUOUS. `37-CONTEXT.md` names the recurring v1.8 defect shape as "a guard
    that does not guard — a check computed independently of the thing it checks", and an absence
    assertion over an empty string passes for the wrong reason. Add two controls in the same
    describe block:
    - **Positive control (committed):** before the absence assertion, assert the rendered `html`
      is non-empty AND contains at least two distinct fixture markers from the render path (e.g.
      the fixture's `clientCo` string and its `partnerRef` string). If the render silently
      degrades to an empty or error shell, this fails first and the gate cannot pass vacuously.
    - **Negative control (committed):** a separate `it()` asserting
      `expect(() => assertNoCommissionLeakage('<td>commission_pct</td>', 'negative control')).toThrow()`
      and likewise for a string containing `taux_pct`, proving both regexes fire.
    Additionally, during implementation only, temporarily splice `commission_pct` into the string
    passed to the real gate and confirm the gate FAILS; then revert that splice. Record the
    observed failure message in the SUMMARY. That temporary mutation is NOT committed.

    Add a comment on the describe block recording the D-37-02 rationale: the envelope needed no
    adjustment (the page renders zero `commission` matches and never reads `params_snapshot`),
    and this gate exists as a regression guard because the surface has just become
    admin-reachable. Do NOT add `/proposals/[id]` to the D-30 exception list at lines 394-412 —
    it is a non-exempt gate.
  </action>

  <verify>
    <automated>npx vitest run tests/admin-09-grep-contracts.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `tests/admin-09-grep-contracts.test.ts` contains the string `proposals/[id]` in a
      `describe(...)` title.
    - `grep -c "vi.mock('@/lib/auth/require'" tests/admin-09-grep-contracts.test.ts` returns
      exactly `1`.
    - `grep -c "function assertNoCommissionLeakage" tests/admin-09-grep-contracts.test.ts`
      returns exactly `1` (helper reused, not redefined).
    - `grep -n "commission_pct" tests/admin-09-grep-contracts.test.ts` shows a match inside the
      negative-control `it()` and inside no other new code.
    - `npx vitest run tests/admin-09-grep-contracts.test.ts` exits 0 with every pre-existing gate
      plus the new gate and its negative control passing.
    - Behaviour (non-vacuity, demonstrated during implementation and reported in the SUMMARY):
      splicing `commission_pct` into the HTML the new gate asserts on makes
      `npx vitest run tests/admin-09-grep-contracts.test.ts` exit non-zero with a
      `D-29 strict: /proposals/[id] ...` failure message. The splice is reverted before commit;
      `grep -c "commission_pct" tests/admin-09-grep-contracts.test.ts` is back to its
      negative-control-only count.
    - `.planning/phases/37-crm-stack-closure/37-01-SUMMARY.md` records the observed failure
      message from the mutation check verbatim.
  </acceptance_criteria>

  <done>
    A 20th ADMIN-09 gate renders the real admin-reachable `/proposals/[id]` page over a
    maximum-surface fixture, asserts zero commission tokens, and is demonstrably capable of
    failing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full gate run against the Phase 36 baseline</name>

  <files>
    (no source changes — verification only)
  </files>

  <read_first>
    - `.planning/phases/37-crm-stack-closure/37-CONTEXT.md` § Integration Points — the baseline
      this phase must hold is 2320 tests passing, 61 skipped, and CI runs `lint:check`,
      `typecheck`, `test` and `build`.
    - `package.json` scripts — `lint:check` is `eslint . --max-warnings=0`, `typecheck` is
      `tsc --noEmit`, `test` is `vitest run`, `build` is `next build`.
  </read_first>

  <action>
    Run all four CI gates in order and record their literal output numbers in the SUMMARY:
    `npm run lint:check`, `npm run typecheck`, `npm test`, `npm run build`.

    `lint:check` uses `--max-warnings=0`; an unused import or variable left behind by Task 1's
    destructure change passes `tsc` and `vitest` but fails CI. The phase depends on Phase 36
    precisely so that a clean `lint:check` is available as proof.

    Do NOT run `npm run db:migrate`, `drizzle-kit push`, or any database write — this project's
    `.env.local` points at the production Neon branch and no schema work is in scope.

    If the test total is not `previous_total + new_cases_added`, stop and reconcile before
    reporting: an unexplained drop means a pre-existing test was silently disabled by the mock
    consolidation in Task 2.
  </action>

  <verify>
    <automated>npm run lint:check &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint:check` exits 0 and prints no warnings.
    - `npm run typecheck` exits 0.
    - `npm test` exits 0; the passing count is at least 2320 and the skipped count is 61
      (unchanged). The SUMMARY states the exact passed/skipped numbers and accounts for the
      delta against 2320 by naming the cases added in Tasks 1 and 2.
    - `npm run build` exits 0.
    - `git diff --name-only` lists only the three files in this plan's `files_modified`.
  </acceptance_criteria>

  <done>
    All four CI gates green, test count grown only by this plan's additions, no file outside
    `files_modified` touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/proposals/[id]` server component | An authenticated caller supplies the proposal `id` path segment. Everything else about the caller must be derived server-side. |
| `users` table → authorization decision | `requireUser()` re-reads `users.role` per request; the cookie-cached session role is not trusted. |
| `proposals` row → rendered HTML | The row carries `inputs`, `computed` and `params_snapshot`; only the first two reach the render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Elevation of Privilege | the widened guard in `app/(authed)/proposals/[id]/page.tsx` | mitigate | The admin arm reads `role` from `requireUser()` only (`src/lib/auth/require.ts:114-124` — per-request DB read of `users.role`, fail-closed to `'partner'` on any unrecognized value). No `params`, `searchParams`, header or client prop participates. Enforced by the Task 1 acceptance criterion that greps the guard region for request-derived values, and by the Phase 18 D-11 discipline comment carried onto the new branch. |
| T-37-01-02 | Information Disclosure | non-admin caller requesting another partner's proposal id | mitigate | The ownership arm is unchanged for every non-admin. `role !== 'admin'` short-circuits to the same `notFound()` — a 404 indistinguishable from a nonexistent id. Pinned by Task 1 cases 2 (partner) and 3 (sales). |
| T-37-01-03 | Elevation of Privilege | the `sales` role riding the admin bypass | mitigate | Strict `role === 'admin'` against the `Role` union `'partner' \| 'admin' \| 'sales'`; no truthiness or negated-partner test. Pinned by Task 1 case 3. |
| T-37-01-04 | Information Disclosure | absence-vs-non-ownership distinguishability | mitigate | `!proposal` remains an independent short-circuit ahead of the role branch, so an admin requesting a nonexistent id gets `notFound()` on the same path as before. Pinned by Task 1 case 4. |
| T-37-01-05 | Information Disclosure | commission reaching an admin-reachable partner-facing surface | mitigate | The page is structurally commission-free (verified: zero case-insensitive `commission` matches, zero `params_snapshot` reads across all 456 lines). D-37-02's 20th gate in `tests/admin-09-grep-contracts.test.ts` converts that from incidental to enforced, and Task 2's positive and negative controls prove the gate is not vacuous. |
| T-37-01-06 | Information Disclosure | D-18 URL-secrecy posture | accept | D-18 governs the admin **tree** returning 404 rather than 403 to non-admins; it does not govern proposal ids. This change alters nothing D-18 promises: no non-admin response shape changes, and the admin already had list-level reach over any partner's proposals via Phase 18 D-11's `?user_id=` override on `app/(authed)/proposals/page.tsx:84`. This closes an inconsistency between the list and detail surfaces rather than widening reach. |
| T-37-01-07 | Tampering | partner user id leaking into a URL | mitigate | The rejected `?user_id=` mirroring would have put a partner's user id into an href built by `src/components/proposals/ProposalRow.tsx:157`, a component shared by the partner list and the admin relationship page. D-37-01 rejects it; Task 1's acceptance criterion asserts `git diff --stat` on that file is empty. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. No `package.json` dependency change is in `files_modified`; a non-empty `git diff` on `package.json`/`package-lock.json` is a Task 3 acceptance failure. |
</threat_model>

<verification>
- `npx vitest run "app/(authed)/proposals/[id]/page.test.tsx"` exits 0 with 4 new GAP-01 cases.
- `npx vitest run tests/admin-09-grep-contracts.test.ts` exits 0 with the 20th gate and its
  negative control.
- The mutation check (splice `commission_pct`, observe failure, revert) is performed and its
  failure message recorded in the SUMMARY.
- `npm run lint:check && npm run typecheck && npm test && npm run build` all exit 0.
- `git diff --name-only` lists exactly the three files in `files_modified`.
</verification>

<success_criteria>
GAP-01 is closed: an admin reaches `/proposals/{id}` for any partner's proposal, every non-admin
still gets the same 404 as for a nonexistent id, the role is server-derived, and the
commission-invisibility envelope over that now-admin-reachable surface is enforced by a gate that
has been demonstrated to fail on commission-bearing content.
</success_criteria>

<output>
Create `.planning/phases/37-crm-stack-closure/37-01-SUMMARY.md` when done.
It MUST record: the exact final guard expression; the observed failure message from the Task 2
mutation check; the pre- and post-consolidation gate counts in `tests/admin-09-grep-contracts.test.ts`;
and the four gate outputs with literal passed/skipped numbers.
</output>
