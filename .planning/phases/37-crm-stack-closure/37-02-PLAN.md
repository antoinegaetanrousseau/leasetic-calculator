---
phase: 37-crm-stack-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(authed)/page.tsx
  - src/lib/momentum/badges.ts
  - src/lib/momentum/badges.test.ts
autonomous: true
requirements: [GAP-03]

must_haves:
  truths:
    - "D-37-05 / IN-01: the momentum card call site in app/(authed)/page.tsx gates on `momentum` alone — the redundant `!isAdmin` is gone"
    - "D-37-05 / IN-02: BADGE_THRESHOLDS is immutable at both levels — assigning to BADGE_THRESHOLDS.wins or to BADGE_THRESHOLDS.wins.gold throws in strict mode"
    - "D-37-05: the partner home page renders byte-identical HTML before and after both fixes, for an admin caller, a partner with momentum, and a partner with empty momentum"
    - "D-37-06: no UI-SPEC governs this plan — its own acceptance criterion is that rendering does not change"
  artifacts:
    - path: "app/(authed)/page.tsx"
      provides: "single-source momentum gate"
      contains: "{momentum && <MomentumCard"
    - path: "src/lib/momentum/badges.ts"
      provides: "deep-frozen threshold constant"
      contains: "Object.freeze"
    - path: "src/lib/momentum/badges.test.ts"
      provides: "immutability assertions at both nesting levels"
      contains: "Object.isFrozen"
  key_links:
    - from: "app/(authed)/page.tsx"
      to: "app/(authed)/_components/MomentumCard.tsx"
      via: "{momentum && <MomentumCard ... />}"
      pattern: "\\{momentum && <MomentumCard"
    - from: "src/lib/momentum/badges.ts deriveBadgeProgress"
      to: "BADGE_THRESHOLDS"
      via: "BADGE_THRESHOLDS[axis][tier] lookup at line 134"
      pattern: "BADGE_THRESHOLDS\\[axis\\]\\[tier\\]"
---

<objective>
Close GAP-03 by resolving Phase 35's two INFO findings, per **D-37-05**:

- **IN-01** — `app/(authed)/page.tsx:158` renders `{!isAdmin && momentum && <MomentumCard ... />}`.
  `momentum` is already provably `null` whenever `isAdmin` is true (`momentumData` at lines 93-97
  is `isAdmin ? null : Promise.all([...])`, and the `momentum` IIFE at lines 105-118 returns `null`
  when `momentumData` is falsy). Two places encode the same gate — the "guard computed
  independently of the thing it checks" shape this milestone keeps finding.
- **IN-02** — `BADGE_THRESHOLDS` at `src/lib/momentum/badges.ts:23-27` is exported mutable.

Acceptance for both is that the momentum card renders **identically** before and after. These are
hygiene fixes; any change in output means the fix was wrong. Per **D-37-06** no UI-SPEC governs
this plan for exactly that reason.

Purpose: remove the duplicated gate and the mutable shared constant without moving a single byte
of rendered output.
Output: a single-source momentum gate, a deep-frozen threshold constant, and a recorded
byte-for-byte render diff proving nothing changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-crm-stack-closure/37-CONTEXT.md
@.planning/phases/37-crm-stack-closure/37-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the render baseline, then remove the redundant `!isAdmin` (IN-01)</name>

  <files>
    app/(authed)/page.tsx
  </files>

  <read_first>
    - `app/(authed)/page.tsx` — the file being modified. Read lines 40-170 in one pass. The three
      `isAdmin` sites are line 62 (`const isAdmin = role === 'admin';`), line 93
      (`isAdmin ? null : Promise.all([...])`) and line 158 (the redundant one). Removing line
      158's usage leaves `isAdmin` still consumed at line 93, so no unused-variable lint error
      can result — confirm that by reading, not by assuming.
    - `app/(authed)/page.test.tsx` — the existing harness. Read the `vi.hoisted` mock block, the
      `beforeEach` defaults ("safe momentum defaults so every pre-existing test..."), the
      `makeMomentumRow` factory, the describe block
      `'Partner Home / — momentum card (GAME-01..05, D-15/D-17)'` and its first case
      `'Admin: no momentum query fires and no momentum node renders'`. This file is the working
      recipe for rendering `HomePage()` under mocks — reuse it for the baseline capture rather
      than building a new harness.
    - `.planning/phases/35-sales-motivation/35-REVIEW.md` § IN-01 — the finding and its verbatim
      prescribed fix.
  </read_first>

  <action>
    **Capture the baseline BEFORE editing any source.** Create a throwaway (uncommitted) vitest
    file under `app/(authed)/` — name it so the repo's test glob picks it up, e.g.
    `app/(authed)/__scratch-render-baseline.test.tsx` — that copies the mock setup from
    `app/(authed)/page.test.tsx` and renders `HomePage()` for three fixtures, writing each
    `container.innerHTML` to a separate file under
    `/private/tmp/claude-501/-Users-antoinerousseau-Claude-Code/cf1fe483-89a8-48ec-ac29-01deaf077ccc/scratchpad/`:
      - `before-admin.html` — `requireUser` resolving `role: 'admin'`
      - `before-partner-momentum.html` — `role: 'partner'` with the three momentum queries
        resolving non-empty data via `makeMomentumRow`
      - `before-partner-empty.html` — `role: 'partner'` with the momentum queries resolving empty
        results
    Run it once and confirm all three files are non-empty and mutually different (if
    `before-admin.html` equals `before-partner-momentum.html`, the harness is not exercising the
    branch and the whole comparison would be vacuous — fix the fixtures before proceeding).

    **Then apply the fix.** At `app/(authed)/page.tsx:158`, change the JSX gate from
    `{!isAdmin && momentum && <MomentumCard ... />}` to `{momentum && <MomentumCard ... />}`.
    Change only the gate expression — every prop passed to `MomentumCard` (`lang`, `streakWeeks`,
    `movements`, `badgeProgress`, `trackedSinceLabel`) stays exactly as it is. Leave line 62 and
    line 93 untouched: `isAdmin` remains the single source of the gate, at the data layer.

    Add a one-line comment above the gate recording GAP-03 / IN-01: `momentum` is `null` for every
    admin because `momentumData` is `isAdmin ? null : ...`, so the role gate lives at the query,
    not at the render.

    **Then re-capture and diff.** Re-run the scratch test writing to `after-admin.html`,
    `after-partner-momentum.html`, `after-partner-empty.html`, and run `diff` on each pair.
    All three diffs MUST be empty. If any is not, the premise is wrong — stop, do not force the
    change, and report the divergence rather than adjusting the fixture to hide it.

    Delete the scratch test file before finishing. Keep the six HTML captures in the scratchpad
    and cite the diff results in the SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run "app/(authed)/page.test.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "!isAdmin" "app/(authed)/page.tsx"` returns `0`.
    - `app/(authed)/page.tsx` contains the exact substring `{momentum && <MomentumCard`.
    - `grep -c "isAdmin" "app/(authed)/page.tsx"` returns `2` (the declaration at line 62 and the
      `momentumData` gate at line 93).
    - CLI: `diff before-admin.html after-admin.html`, `diff before-partner-momentum.html
      after-partner-momentum.html` and `diff before-partner-empty.html after-partner-empty.html`
      each exit 0 with no output. The SUMMARY quotes all three exit codes.
    - CLI: `before-admin.html` and `before-partner-momentum.html` are NOT identical — proof the
      baseline harness actually exercised both branches and the diffs above are not vacuous.
    - `npx vitest run "app/(authed)/page.test.tsx"` exits 0, including the case
      `'Admin: no momentum query fires and no momentum node renders'` and the case
      `'Placement (D-17, A-1): the momentum card renders after the relance rows and before the
      recent-proposals rows'`.
    - `ls app/\(authed\)/ | grep -c scratch` returns `0` — the throwaway test file is deleted.
    - `npm run lint:check` exits 0 with no warnings (no unused `isAdmin`).
  </acceptance_criteria>

  <done>
    The momentum card is gated on `momentum` alone, and three byte-for-byte HTML diffs across
    admin / partner-with-data / partner-empty prove the rendered output is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deep-freeze BADGE_THRESHOLDS at both nesting levels (IN-02)</name>

  <files>
    src/lib/momentum/badges.ts
    src/lib/momentum/badges.test.ts
  </files>

  <read_first>
    - `src/lib/momentum/badges.ts` — the file being modified. Lines 16-27 carry the JSDoc block
      ("An operator-adjustable STARTING POINT ... changing one is a constant edit here, never a
      migration (D-03)") and the declaration
      `export const BADGE_THRESHOLDS: Record<BadgeAxisId, Record<BadgeTierId, number>> = { clients:
      {...}, wins: {...}, consistency: {...} }`. Line 134 is the only internal consumer:
      `const threshold = BADGE_THRESHOLDS[axis][tier];`.
    - `src/lib/registry/labels.ts` lines 33 and 53 — the project's frozen-exported-constant
      analog: `export const HEADCOUNT_BAND_LABELS: Readonly<Record<string, string>> =
      Object.freeze({...})` and `NAF_SECTION_LABELS` in the same shape. Note these are
      single-level records, so a shallow freeze suffices there — `BADGE_THRESHOLDS` is two levels
      deep and is NOT the same case.
    - `src/lib/momentum/badges.test.ts` — the file being modified. Line 4 imports
      `BADGE_THRESHOLDS`; lines 139-148 assert the exact UI-SPEC numbers via three property reads
      plus a whole-object `toEqual`. `toEqual` is structural value equality and is unaffected by
      freezing — this existing case must keep passing UNCHANGED.
    - `.planning/phases/35-sales-motivation/35-REVIEW.md` § IN-02 — the finding and its verbatim
      prescribed fix, which already calls for freezing at each level.
  </read_first>

  <action>
    In `src/lib/momentum/badges.ts`, freeze `BADGE_THRESHOLDS` at BOTH levels. A single outer
    `Object.freeze` leaves `BADGE_THRESHOLDS.wins` mutable and would not close the finding.
    Apply `Object.freeze` to the outer object AND to each of the three inner objects (`clients`,
    `wins`, `consistency`).

    Widen the type annotation to make the immutability visible in the type, not only at runtime:
    `Readonly<Record<BadgeAxisId, Readonly<Record<BadgeTierId, number>>>>`. This matches the
    `Readonly<Record<...>>` convention in `src/lib/registry/labels.ts`.

    Change no numeric value: `clients` stays `{ bronze: 3, silver: 10, gold: 25 }`, `wins` stays
    `{ bronze: 1, silver: 5, gold: 15 }`, `consistency` stays
    `{ bronze: 2, silver: 6, gold: 12 }`. Keep the existing JSDoc block and append one sentence
    recording GAP-03 / IN-02: the constant is frozen at both levels so an accidental mutation
    throws in strict mode instead of silently drifting; changing a threshold is still a constant
    edit here, never a migration (D-03).

    Leave line 134's `BADGE_THRESHOLDS[axis][tier]` read untouched — freezing does not affect
    reads.

    In `src/lib/momentum/badges.test.ts`, extend the existing `describe('BADGE_THRESHOLDS', ...)`
    block. Do NOT modify the existing `'matches the exact UI-SPEC numbers'` case. Add:
    - a case asserting `Object.isFrozen(BADGE_THRESHOLDS)` is `true` AND
      `Object.isFrozen(BADGE_THRESHOLDS.clients)`, `Object.isFrozen(BADGE_THRESHOLDS.wins)` and
      `Object.isFrozen(BADGE_THRESHOLDS.consistency)` are each `true` — the second-level
      assertions are the point of the fix and must be present individually, not as a loop that
      could pass over an empty list.
    - a case asserting that a top-level reassignment throws in strict mode (ES module code is
      strict): `expect(() => { (BADGE_THRESHOLDS as unknown as Record<string, unknown>).wins = {};
      }).toThrow()`.
    - a case asserting that a nested mutation throws:
      `expect(() => { (BADGE_THRESHOLDS.wins as unknown as Record<string, number>).gold = 999;
      }).toThrow()`. This is the case that would FAIL against a shallow-only freeze — it is the
      proof the fix went deep enough.
    - a case asserting `deriveBadgeProgress` returns the same result before and after an attempted
      (throwing) mutation, i.e. reads are unaffected.
  </action>

  <verify>
    <automated>npx vitest run src/lib/momentum/badges.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Object.freeze" src/lib/momentum/badges.ts` returns `4` (one outer plus three
      inner).
    - `src/lib/momentum/badges.ts` contains the exact substring
      `Readonly<Record<BadgeAxisId, Readonly<Record<BadgeTierId, number>>>>`.
    - `grep -v '^ \*' src/lib/momentum/badges.ts | grep -c "bronze: 3"` returns `1` and the same
      for `bronze: 1` and `bronze: 2` — no threshold value moved.
    - `src/lib/momentum/badges.test.ts` contains `Object.isFrozen(BADGE_THRESHOLDS.wins)`.
    - `npx vitest run src/lib/momentum/badges.test.ts` exits 0, with the pre-existing
      `'matches the exact UI-SPEC numbers'` case unmodified and passing.
    - Behaviour (depth proof, reported in the SUMMARY): with only the OUTER `Object.freeze`
      applied, the nested-mutation case fails; with the inner freezes added, it passes. Perform
      this check during implementation and quote the failing assertion message.
    - `git diff -- src/lib/momentum/badges.test.ts` shows additions only inside the existing
      `describe('BADGE_THRESHOLDS', ...)` block — no existing assertion is edited or deleted.
  </acceptance_criteria>

  <done>
    `BADGE_THRESHOLDS` is immutable at both levels, the type says so, every threshold value is
    unchanged, and a test that a shallow freeze would fail now passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full gate run and rendering-identity attestation</name>

  <files>
    (no source changes — verification only)
  </files>

  <read_first>
    - `.planning/phases/37-crm-stack-closure/37-CONTEXT.md` § Integration Points — the baseline to
      hold is 2320 tests passing, 61 skipped.
    - `package.json` scripts — `lint:check` is `eslint . --max-warnings=0`.
  </read_first>

  <action>
    Run all four CI gates and record their literal outputs: `npm run lint:check`,
    `npm run typecheck`, `npm test`, `npm run build`.

    `lint:check` is the gate that matters most here: `--max-warnings=0` means an orphaned
    `isAdmin`, an unused import, or a stale type alias left by either fix passes `tsc` and
    `vitest` but fails CI.

    Do NOT run `npm run db:migrate` or any database write — `.env.local` points at the production
    Neon branch and no schema work is in scope.

    Write the rendering-identity attestation into the SUMMARY: the three `diff` exit codes from
    Task 1, the statement that no `MomentumCard` prop changed, and the statement that no
    `BADGE_THRESHOLDS` value changed. D-37-05's acceptance is byte-identical rendering; the
    SUMMARY is where that is claimed with evidence, not asserted as a hope.
  </action>

  <verify>
    <automated>npm run lint:check &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint:check` exits 0 with zero warnings.
    - `npm run typecheck` exits 0.
    - `npm test` exits 0; passing count is at least 2320 and skipped is 61. The SUMMARY names the
      cases added in Task 2 to account for the delta.
    - `npm run build` exits 0.
    - `git diff --name-only` lists only `app/(authed)/page.tsx`, `src/lib/momentum/badges.ts` and
      `src/lib/momentum/badges.test.ts`.
    - `.planning/phases/37-crm-stack-closure/37-02-SUMMARY.md` contains the three Task 1 `diff`
      exit codes and an explicit sentence stating the momentum card renders identically before and
      after.
  </acceptance_criteria>

  <done>
    All four CI gates green, only the three intended files changed, and the byte-identical
    rendering claim is recorded with its evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session role → momentum data fetch | `app/(authed)/page.tsx:62,93` decides whether the three owner-scoped momentum queries run at all. |
| momentum query results → rendered card | Only owner-scoped rows reach `MomentumCard`. |
| `BADGE_THRESHOLDS` module export → every consumer | A shared mutable object crossing module boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Information Disclosure | removing `!isAdmin` from the render gate could surface a momentum card to an admin | mitigate | The remaining gate is the data-layer one at line 93 (`isAdmin ? null : Promise.all([...])`), which makes `momentum` unconditionally `null` for admins — the render gate was never the load-bearing check. Proven by the `before-admin.html` vs `after-admin.html` byte-diff in Task 1 and by the pre-existing case `'Admin: no momentum query fires and no momentum node renders'`. |
| T-37-02-02 | Information Disclosure | cross-partner momentum leakage | accept | Out of scope for this plan and unchanged by it: the three queries (`listWeeklyMovementsForOwner`, `listProgressWeekKeysForOwner`, `getBadgeCountsForOwner`) are owner-scoped by the session user id at line 93, and this plan modifies neither the queries nor their arguments. `git diff` on `src/lib/db/queries/momentum.ts` is empty. |
| T-37-02-03 | Tampering | a consumer mutating `BADGE_THRESHOLDS` at runtime, silently changing every partner's badge tiers for the life of the process | mitigate | Deep `Object.freeze` at both nesting levels plus a `Readonly<Record<..., Readonly<Record<...>>>>` annotation. Pinned by the Task 2 nested-mutation test, which a shallow-only freeze fails. |
| T-37-02-04 | Denial of Service | a frozen constant turning a previously silent write into a thrown TypeError in production | accept | The repo-wide grep found exactly two importers — `src/lib/momentum/badges.ts:134` (a read) and `src/lib/momentum/badges.test.ts:4` (reads plus a `toEqual`). No write site exists, so no path can start throwing. Task 2's acceptance criteria assert the existing `toEqual` case is unmodified and passing. |
| T-37-02-05 | Tampering | a "hygiene" fix silently changing rendered output | mitigate | Task 1 captures three pre-change HTML renders (admin, partner-with-data, partner-empty), re-captures post-change, and requires all three `diff`s to exit 0 — plus a non-vacuity check that the admin and partner captures differ from each other. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. A non-empty `git diff` on `package.json`/`package-lock.json` is a Task 3 acceptance failure. |
</threat_model>

<verification>
- Three byte-for-byte HTML `diff`s (admin, partner-with-data, partner-empty) exit 0, with the
  non-vacuity check that the admin and partner baselines differ.
- `npx vitest run "app/(authed)/page.test.tsx"` exits 0.
- `npx vitest run src/lib/momentum/badges.test.ts` exits 0, including the nested-mutation case.
- `npm run lint:check && npm run typecheck && npm test && npm run build` all exit 0.
- `git diff --name-only` lists exactly the three files in `files_modified`.
</verification>

<success_criteria>
GAP-03 is closed: Phase 35's IN-01 and IN-02 findings are both resolved, the momentum card renders
byte-identically before and after (evidenced by diffs, not asserted), `BADGE_THRESHOLDS` is frozen
at both levels, and `lint:check --max-warnings=0` stays clean.
</success_criteria>

<output>
Create `.planning/phases/37-crm-stack-closure/37-02-SUMMARY.md` when done.
It MUST record: the three Task 1 `diff` exit codes and the non-vacuity comparison; the failing
assertion message observed with an outer-only freeze; the final `BADGE_THRESHOLDS` declaration
line; and the four gate outputs with literal passed/skipped numbers.
</output>
