---
phase: 37-crm-stack-closure
plan: 05
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - .planning/phases/30-company-contact-registry/30-UAT.md
  - .planning/phases/33-pipeline/33-VERIFICATION.md
autonomous: false
requirements: [CLOSE-01, CLOSE-03]

must_haves:
  truths:
    - "D-37-04: all six walk items are performed in ONE operator session, after every code change in this phase has landed"
    - "D-37-04: UAT scenario 9 walks the real admin click-through from a relationship to a proposal detail page — not the dead end it recorded before plan 37-01"
    - "D-37-04: D-08's gate is confirmed against a PRODUCTION build (npm run build && npm run start), not next dev"
    - "D-37-04: the Space -> ArrowRight -> Space keyboard drag is confirmed as a WALK, not re-opened as a defect — WR-02 was fixed in 52d03e1 and PipelineBoard.test.tsx Test 9b already pins it"
    - "CLOSE-01: .planning/phases/30-company-contact-registry/30-UAT.md reaches pending: 0 with each of scenarios 2, 9, 10 and 12 individually recorded"
    - "CLOSE-03: .planning/phases/33-pipeline/33-VERIFICATION.md reaches status: passed"
    - "No plan step runs db:migrate or any migration command against the local environment"
  artifacts:
    - path: ".planning/phases/30-company-contact-registry/30-UAT.md"
      provides: "four walked scenarios and a Summary block reading pending: 0"
      contains: "pending: 0"
    - path: ".planning/phases/33-pipeline/33-VERIFICATION.md"
      provides: "passed status with the keyboard-drag and production-build items closed"
      contains: "status: passed"
  key_links:
    - from: "the operator's six recorded results"
      to: ".planning/phases/30-company-contact-registry/30-UAT.md scenarios 2, 9, 10, 12"
      via: "per-scenario result fields, one transcription per item"
      pattern: "result: pass"
    - from: "the operator's items 5 and 6"
      to: ".planning/phases/33-pipeline/33-VERIFICATION.md Addendum items 1 and 3"
      via: "a new dated addendum closing both"
      pattern: "status: passed"
---

<objective>
Close CLOSE-01 and CLOSE-03 with **one consolidated operator walk**, per **D-37-04**. Six items,
one session, last in the phase:

1. `30-UAT.md` scenario 2 — Clients nav per role
2. `30-UAT.md` scenario 9 — admin relationship detail, walking the click-through
3. `30-UAT.md` scenario 10 — sales-role parity and admin exclusion
4. `30-UAT.md` scenario 12 — no regression
5. The Space → ArrowRight → Space keyboard drag on the pipeline board (`33-VERIFICATION.md`
   Addendum item 3)
6. D-08's gate confirmed against a **production build**, not `next dev` (`33-VERIFICATION.md`
   Addendum item 1)

**Ordering is load-bearing, not a preference.** UAT scenario 9 is titled "Admin relationship detail
— and its known dead end". Walking it before plan 37-01 lands would re-record the dead end. This
plan is wave 2 and depends on both code plans for exactly that reason.

**Item 5 is a walk, not a defect.** WR-02 was fixed in `52d03e1`: the component's direct arrow path
stands down while `data-dragging` is true, so dnd-kit owns the arrows during a live drag and exactly
one write occurs. `PipelineBoard.test.tsx` Test 9b already pins it. Only the operator confirmation
is outstanding. Do not re-open it as a bug.

Purpose: the v1.6/v1.7 surfaces stop being "shipped but unwalked".
Output: `30-UAT.md` at `pending: 0` and `33-VERIFICATION.md` at `status: passed`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-crm-stack-closure/37-CONTEXT.md
@.planning/phases/37-crm-stack-closure/37-01-SUMMARY.md
@.planning/phases/37-crm-stack-closure/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the production build and assemble the walk pack</name>

  <files>
    (no file changes — environment preparation only)
  </files>

  <read_first>
    - `.planning/phases/37-crm-stack-closure/37-01-SUMMARY.md` — confirm the D-37-01 admin bypass
      actually landed. If it did not, STOP: scenario 9 would re-record the dead end.
    - `.planning/phases/37-crm-stack-closure/37-02-SUMMARY.md` — confirm the GAP-03 fixes landed
      and the rendering-identity diffs came back clean.
    - `scripts/check-local-db-branch.sh` — the INFRA-05 guard. It prints which Neon branch the
      local `DATABASE_URL` resolves to and fails if it is the production endpoint. It prints the
      hostname only, never the connection string.
    - `.planning/phases/33-pipeline/33-VERIFICATION.md` § `human_verification` and § Addendum
      2026-09-03 — the literal steps items 1 and 3 ask for, including the fixture company
      ("Pépinières Vaugelas", the siren-less legacy company) and the seed command.
    - `package.json` scripts — `build: next build`, `start: next start`,
      `db:seed:pipeline-fixtures`, `db:seed:fiche-fixtures`, `check:local-db-branch`,
      `grant:admin`.
  </read_first>

  <action>
    **Guard the database first.** Run `npm run check:local-db-branch` and record the branch
    hostname it prints. If it reports the production endpoint, STOP and escalate to the operator —
    do not seed, do not start a server. Every subsequent step in this plan writes data.

    Under no circumstances run `npm run db:migrate`, `npm run db:migrate:dry-run`,
    `drizzle-kit push`, or any other migration command. This project's `.env.local` points at a
    Neon branch that has historically been production; migrations are generated and committed
    locally and applied only through the `MIGRATE PROD` GitHub Action. No schema work is in this
    phase's scope, so no migration is needed.

    **Seed the fixtures** the walk depends on, against the confirmed non-production branch:
    `npm run db:seed:pipeline-fixtures` (item 5 needs a populated pipeline board; item 6 needs the
    siren-less legacy company) and `npm run db:seed:fiche-fixtures` (scenarios 9 and 10 need
    companies, relationships and contacts). Record each command's exit code and the row counts it
    reports.

    **Build and serve for real.** Run `npm run build`, then `npm run start`. Item 6 is explicitly a
    production-build check: `33-VERIFICATION.md` Addendum item 1 records that the prior confirmation
    was performed against `next dev` and therefore does not satisfy the requirement. Do NOT start
    `next dev` for any part of this walk. Note that running `npm run build` alongside a live
    `next dev` freezes `globals.css` recompiles — if a dev server is running, stop it first.

    **Confirm the server is reachable and login works** before handing over. If clicking login does
    nothing and the browser console shows a CORS error to a `*.vercel.app` origin, `APP_URL` /
    `NEXT_PUBLIC_APP_URL` in `.env.local` do not match the origin being served — fix that before the
    walk rather than during it.

    **Assemble the walk pack** and print it for the operator. It must contain, concretely:
    - the base URL the production server is listening on;
    - the exact URLs for each item: the partner home, `/clients`, `/pipeline`, `/proposals`, the
      admin companies list, one seeded company's admin detail page, the admin relationship detail
      page reached from that company's relationship row, and the admin `/partners` page (build the
      admin URLs using the configured `ADMIN_URL_SEGMENT`, and print the resolved segment);
    - the credentials or account identifiers for the three roles the walk needs — an admin, a
      partner, and a `sales` user (`partner_type` = `Commercial`). If a `sales` account does not
      exist in the seeded data, say so explicitly in the pack rather than letting the operator
      discover it mid-walk;
    - the seeded company names, including the siren-less legacy company item 6 needs.
  </action>

  <verify>
    <automated>npm run check:local-db-branch &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run check:local-db-branch` exits 0 and its printed branch hostname is recorded; the
      hostname is NOT the production endpoint.
    - CLI: `git log --oneline -n 20 | grep -c .` — plans 37-01 and 37-02 are both present in the
      working tree; `grep -c "role === 'admin'" "app/(authed)/proposals/[id]/page.tsx"` returns at
      least 1 and `grep -c '!isAdmin' "app/(authed)/page.tsx"` returns 0. If either fails, this
      task stops.
    - `npm run db:seed:pipeline-fixtures` and `npm run db:seed:fiche-fixtures` each exit 0, with
      their reported row counts recorded.
    - `npm run build` exits 0 and `npm run start` serves a page that returns HTTP 200 —
      demonstrated by a `curl -s -o /dev/null -w '%{http_code}'` against the login URL.
    - No command executed in this task matches `db:migrate` or `drizzle-kit push`. State this
      explicitly in the task output.
    - The walk pack printed for the operator names, for each of the six items, at least one
      concrete URL, and names the three role accounts (or states that one is missing).
  </acceptance_criteria>

  <done>
    A production build is serving against a confirmed non-production Neon branch with fixtures
    seeded, and the operator has a concrete pack of URLs, accounts and fixture names.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The consolidated six-item operator walk</name>

  <files>
    (no file changes — human observation; results are transcribed in Task 3)
  </files>

  <read_first>
    - `.planning/phases/30-company-contact-registry/30-UAT.md` — scenarios 2 (line 58), 9 (line
      145), 10 (line 160) and 12 (line 185), each with its `expected` block and
      `result: [pending]`. The `## Bookkeeping Correction` section (line 294) explains why these
      four are pending rather than passed: an over-broad find-and-replace once marked them `pass`
      without the operator confirming them. Do not repeat that.
    - `.planning/phases/33-pipeline/33-VERIFICATION.md` § Addendum 2026-09-03 — items 1 and 3, and
      the reasoning for why item 3 is a walk rather than a defect.
    - `.planning/phases/37-crm-stack-closure/37-01-SUMMARY.md` — what changed on
      `/proposals/[id]`, so the operator knows what scenario 9 should now do instead of 404.
    - Task 1's printed walk pack.
  </read_first>

  <action>
    Do not attempt to execute this task as an agent. Present the walk pack from Task 1 to the
    operator, hand them the six-item script in <how-to-verify> below, and BLOCK until they reply
    with six verdicts in the <resume-signal> format. Perform no browser automation, no simulated
    observation, and no inference of a result from a passing unit test — every one of the six items
    exists precisely because an automated check cannot stand in for it. Record the operator's own
    words; Task 3 transcribes them.
  </action>

  <what-built>
    Plan 37-01 gave `/proposals/[id]` a server-derived `role === 'admin'` bypass, so the admin
    oversight click-through from a company relationship to one of its proposals now opens the
    detail page instead of returning 404, and added a 20th ADMIN-09 grep gate over that surface.
    Plan 37-02 removed the redundant `!isAdmin` gate beside `momentum` on the partner home and
    deep-froze `BADGE_THRESHOLDS`, with byte-for-byte render diffs proving the momentum card is
    unchanged. Task 1 built the app with `next build` and is serving it with `next start` against a
    seeded non-production Neon branch.

    Everything that can be automated has been. What remains needs a human at a browser.
  </what-built>

  <how-to-verify>
    Work through all six items in ONE session, in this order. Record a verdict for EACH item
    individually — `pass`, or `fail` with what you saw. A single "all good" is not enough; Task 3
    transcribes six separate results into two documents.

    **Item 1 — UAT scenario 2: Clients nav is correct for each role.**
    1. Sign in as the **partner** account. Count the sidebar items.
       *Expect:* 5 items, with **Clients** sitting between Propositions and Aide.
    2. Click Clients. *Expect:* `/clients` opens (no 404) and the Clients entry highlights as
       active.
    3. Sign in as the **admin** account and stay in **Agent view**. Count the sidebar items.
       *Expect:* 4 items and **no Clients entry at all** — the link that always 404'd is gone.
    4. Switch to **Admin view**. *Expect:* the 7-item nav including Sociétés.
    Record: the item count seen in each of the three states, and whether Clients appeared.

    **Item 2 — UAT scenario 9: Admin relationship detail, and the click-through that used to dead-end.**
    1. As **admin**, open a seeded company's admin detail page and find its relationship row.
    2. Click "Voir →". *Expect:* the admin relationship detail opens, contacts shown **read-only**
       (no edit or delete controls), plus that relationship's proposals.
    3. Check the relationship-level data displays correctly: LC ref, amount, date, status, counts.
    4. **Now click through to a specific proposal from there.** *Expect — and this is the change:*
       the proposal **detail page opens**. It must NOT 404. Before plan 37-01 this was the
       documented dead end; scenario 9's whole point is now to walk the real click-through.
    5. On that proposal detail page, confirm no commission figure and no rate/percentage input is
       visible anywhere.
    Record: whether the detail page opened, and whether anything at relationship level was missing
    or wrong (that would be a new finding, not a known one).

    **Item 3 — UAT scenario 10: Sales-role parity, and admin exclusion.**
    1. Sign in as the **sales** account (`partner_type` = `Commercial`, role `sales`).
       *Expect:* the same sidebar as a partner, including Clients. Confirm you can open `/clients`,
       create a client, add a contact, and hold a relationship.
    2. In the admin partners list, find that user. *Expect:* they carry an "internal"
       classification.
    3. As **admin**, visit `/clients` directly. *Expect:* a **404** — not a 403, not a redirect.
       The clients tree is for relationship holders only.
    Record: what the sales user could do, the classification shown, and the exact admin `/clients`
    response.

    **Item 4 — UAT scenario 12: Nothing that worked before is different.**
    1. Open `/proposals` as a partner. *Expect:* the same search-box behaviour, the same columns
       and the same data as before Phase 30.
    2. Open the admin `/partners` page. *Expect:* unchanged look and behaviour.
    3. Confirm no commission or rate input appears anywhere on a partner-facing surface (the
       ADMIN-09 envelope).
    4. Because plan 37-02 touched the partner home, also open the partner home as a partner and
       confirm the momentum card looks exactly as it did — and open it as an admin and confirm no
       momentum card appears.
    Record: any difference at all, however small.

    **Item 5 — the pipeline board keyboard drag.**
    1. Open `/pipeline` as a partner with seeded pipeline data.
    2. Focus a card with the keyboard (Tab to it), press **Space** to pick it up, press
       **ArrowRight** to move it one lane, press **Space** to drop it.
    3. *Expect:* the card lands one lane to the right and **exactly one** stage change is recorded
       — not two. Confirm by opening that relationship's activity timeline and counting the
       stage-change events created by this drag.
    This is a confirmation walk, not a bug hunt: WR-02 was already fixed in `52d03e1` and
    `PipelineBoard.test.tsx` Test 9b pins the behaviour. You are recording that it holds in a real
    browser.
    Record: the number of stage-change events the timeline shows for that one drag.

    **Item 6 — D-08's gate, against the production build.**
    You are on `npm run start` output, not `next dev`. That distinction is the entire point of this
    item; the prior confirmation was performed against `next dev` and was recorded as insufficient.
    1. Open `/clients/[id]` for the **siren-less legacy company** (Pépinières Vaugelas, named in
       Task 1's walk pack).
    2. Press **"Marquer gagné"**.
    3. Enter a date and a reason. Submit.
    4. *Expect:* the dialog **stays open**, **keeps the typed date and reason**, and **reveals the
       SIREN banner and SIREN field** with the submit button relabelled.
    Record: whether the dialog stayed open, whether your typed values survived, and the exact
    relabelled button text.

    Then report all six verdicts.
  </how-to-verify>

  <verify>
    <human-check>The operator replies with six verdicts, one per item, each carrying the
    observation it rests on. Item 2 states whether the proposal detail page opened or 404'd; item 5
    states a stage-change event count; item 6 confirms the walk ran against `npm run start`.</human-check>
    <automated>MISSING — intentionally. All six items require a human at a browser: sidebar item
    counts per role, an admin click-through, a 404-vs-403 distinction, a visual regression pass, a
    keyboard drag producing exactly one write, and a dialog's behaviour under a production build.
    The automated coverage that does exist (PipelineBoard.test.tsx Test 9b,
    tests/server-action-error-contracts.test.ts, tests/admin-09-grep-contracts.test.ts) already
    passes and is what makes these confirmations rather than derivations.</automated>
  </verify>

  <acceptance_criteria>
    - Six separate verdicts are recorded, one per item, each with the observation the verdict rests
      on — not a blanket approval.
    - Item 2's record states explicitly whether the proposal detail page opened or 404'd. A 404
      here means plan 37-01 did not land as intended and this plan stops rather than recording a
      pass.
    - Item 5's record states a specific stage-change event count for the single drag.
    - Item 6's record states that the walk was performed against `npm run start` (production build)
      and quotes the relabelled submit button text.
    - Any observation that does not match `expected` is recorded verbatim as a finding, with the
      scenario it belongs to. It is NOT rounded up to a pass.
  </acceptance_criteria>

  <resume-signal>
    Reply with six lines — `1: pass|fail — <observation>` through `6: pass|fail — <observation>`.
    If everything matched, `1: pass — ...` six times is fine, but each line still needs its
    observation.
  </resume-signal>

  <done>
    Six individually-recorded operator verdicts, each backed by a stated observation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Transcribe the six results into 30-UAT.md and 33-VERIFICATION.md</name>

  <files>
    .planning/phases/30-company-contact-registry/30-UAT.md
    .planning/phases/33-pipeline/33-VERIFICATION.md
  </files>

  <read_first>
    - `.planning/phases/30-company-contact-registry/30-UAT.md` — the file being modified. Read it
      in full. Note the `## Current Test` block (lines 17-30) still holding scenario 2 with
      `awaiting: user response`; the per-scenario `result:` fields for 2, 9, 10 and 12; the
      `## Summary` block (line 195) reading `total: 13 / passed: 8 / issues: 3 / issues_fixed: 2`
      with **no `pending:` key at all**; the `## Gaps` section; and the `## Bookkeeping Correction`
      section explaining exactly how these four scenarios were once falsely marked `pass`.
    - `.planning/phases/33-pipeline/33-VERIFICATION.md` — the file being modified. Read the
      frontmatter (`status: human_needed`, the `human_verification` array) and the
      `## Addendum — 2026-09-03, after commit 52d03e1` section, which records item 1 as PARTIALLY
      CLOSED (walked against `next dev`), item 3 as STILL OPEN, and item 5 (migration 0009 on
      `main` / `preview`) as STILL OPEN **by design — a deliberate deferral to milestone close**.
    - `.planning/ROADMAP.md` § Phase 37 success criteria 2 and 3 — the exact end states these two
      documents must reach.
    - Task 2's six recorded verdicts.
  </read_first>

  <action>
    **Transcribe faithfully.** Each of the six verdicts goes to exactly one place. Do not use a
    find-and-replace across result fields — that is precisely the error the `## Bookkeeping
    Correction` section documents. Edit each scenario's `result:` individually and paste the
    operator's own observation into a `note:` beside it.

    **In `30-UAT.md`:**
    - Scenario 2: set `result:` from item 1's verdict; add a `note:` carrying the three sidebar
      item counts the operator reported.
    - Scenario 9: set `result:` from item 2's verdict. Rewrite the scenario's `expected` block so
      it no longer describes a dead end — the click-through now opens the proposal detail page —
      and add a line recording that the change came from Phase 37 / GAP-01 / D-37-01. Add a `note:`
      with the operator's observation.
    - Scenario 10: set `result:` from item 3's verdict; `note:` records the sales user's
      capabilities, the "internal" classification, and the exact admin `/clients` response.
    - Scenario 12: set `result:` from item 4's verdict; `note:` records the regression check
      including the partner-home momentum observation added because plan 37-02 touched that page.
    - Clear the stale `## Current Test` block (lines 17-30): it still holds scenario 2 with
      `awaiting: user response`. Replace it with a statement that no test is awaiting a response.
    - Rewrite the `## Summary` block so it carries an explicit `pending: 0` key alongside
      recomputed `total`, `passed`, `issues` and `issues_fixed` values. Recompute the counts from
      the per-scenario `result:` fields actually present in the file — do not adjust an existing
      number by hand. `total` must equal the number of `### N.` scenario headings.
    - Set the frontmatter `status` to `passed` (or `issues_found` if any of items 1-4 failed) and
      update `updated` to the walk's ISO-8601 timestamp.
    - Append a dated note recording that scenarios 2, 9, 10 and 12 were walked in Phase 37's
      consolidated session per D-37-04, that scenario 9 walked the click-through rather than the
      dead end, and — as a direct answer to the Bookkeeping Correction — that each result was
      transcribed individually from an operator observation.

    **In `33-VERIFICATION.md`:**
    - Append a new `## Addendum — <walk date>, Phase 37 consolidated walk` section.
    - Close **item 1** with item 6's verdict, stating explicitly that it was performed against
      `npm run build && npm run start` and NOT `next dev`, and quoting the relabelled submit button
      text. This supersedes the 2026-09-03 "PARTIALLY CLOSED" entry; say so.
    - Close **item 3** with item 5's verdict, stating the observed stage-change event count for the
      single Space → ArrowRight → Space drag, and noting this confirms rather than re-derives
      `PipelineBoard.test.tsx` Test 9b and the `52d03e1` WR-02 fix.
    - **Item 5 (migration 0009 on `main` / `preview`) must be handled explicitly, not dropped.** It
      is a deliberate deferral to milestone close and is not one of Phase 37's five success
      criteria. Record it in the addendum as an explicitly-deferred, non-blocking item owned by the
      v1.6 milestone close (Phase 40 / CLOSE-06), and state that `status: passed` refers to the
      criteria CLOSE-03 names — the keyboard drag and the production-build D-08 gate — with item 5
      outstanding by design. Do NOT create or edit any Phase 40 artifact, and do NOT run any
      migration command.
    - Set the frontmatter `status:` to `passed` (only if items 5 and 6 both passed; otherwise leave
      it `human_needed` and record why). Update `score` if the closure changes it, and mark the
      corresponding `human_verification` entries as superseded by this addendum rather than
      deleting them — the record of what was once open is part of the evidence.

    If ANY of the six items failed, do not force either document to a passing state. Record the
    failure verbatim in the relevant `## Gaps` / addendum section, leave the status honest, and
    surface the failure in `37-05-SUMMARY.md` as an operator decision point.
  </action>

  <verify>
    <automated>grep -q 'pending: 0' .planning/phases/30-company-contact-registry/30-UAT.md &amp;&amp; grep -q 'status: passed' .planning/phases/33-pipeline/33-VERIFICATION.md &amp;&amp; npm run lint:check &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'pending: 0' .planning/phases/30-company-contact-registry/30-UAT.md` returns at
      least 1, inside the `## Summary` block.
    - `grep -c 'result: \[pending\]' .planning/phases/30-company-contact-registry/30-UAT.md`
      returns `0`.
    - CLI consistency: the `total:` value in the Summary equals
      `grep -c '^### [0-9]' .planning/phases/30-company-contact-registry/30-UAT.md`, and
      `passed:` equals `grep -c '^result: pass' `-style count of per-scenario pass results. Report
      both computed numbers in the SUMMARY alongside the values written into the file.
    - `30-UAT.md` no longer contains `awaiting: user response`.
    - Each of scenarios 2, 9, 10 and 12 carries a `note:` containing an operator observation, and
      the four notes are mutually distinct — identical notes would signal the find-and-replace
      failure the Bookkeeping Correction warns about.
    - Scenario 9's `expected` block no longer contains the phrase `It 404s` or `dead end` as the
      described outcome, and references GAP-01 / D-37-01.
    - `.planning/phases/33-pipeline/33-VERIFICATION.md` frontmatter `status` is `passed`, and the
      file contains a new addendum heading naming the Phase 37 consolidated walk.
    - The new addendum contains the literal phrase `npm run start` and states the production-build
      distinction; it also contains an explicit paragraph on item 5 naming it as deferred to the
      v1.6 milestone close.
    - `git status --porcelain` shows only `30-UAT.md`, `33-VERIFICATION.md` and this plan's SUMMARY
      changed — no source file, no Phase 40 artifact, no `REQUIREMENTS.md`.
    - `npm run lint:check` and `npm test` both exit 0 (no code changed; a failure means something
      else broke and must be reported).
  </acceptance_criteria>

  <done>
    Six operator observations are individually transcribed, `30-UAT.md` reads `pending: 0` with
    self-consistent counts, and `33-VERIFICATION.md` reads `status: passed` with item 5's
    deferral stated rather than swept away.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → Neon database | Seeds and a running server write real rows; `.env.local` has historically pointed at production. |
| operator observation → written record | The integrity boundary CLOSE-01 exists to protect: `30-UAT.md`'s own Bookkeeping Correction documents a prior failure of exactly this boundary. |
| `next dev` → `next start` | CLOSE-03 item 6 is meaningless if walked against the wrong server. |
| browser session role → admin surfaces | The walk exercises admin, partner and sales roles against the surfaces plan 37-01 just widened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | seeding or walking against the production Neon branch | mitigate | Task 1 runs `npm run check:local-db-branch` (INFRA-05 guard, prints hostname only, fails on the production endpoint) and stops on a production result before any seed or server start. The branch hostname is recorded. |
| T-37-05-02 | Tampering | an accidental schema change against a live database | mitigate | The plan forbids `db:migrate`, `db:migrate:dry-run` and `drizzle-kit push` outright, in Task 1's action and in its acceptance criteria. No schema work is in phase scope. |
| T-37-05-03 | Repudiation | results recorded that no operator actually confirmed | mitigate | `30-UAT.md`'s `## Bookkeeping Correction` documents this exact prior failure (an over-broad find-and-replace). Task 3 forbids bulk replacement, requires per-scenario transcription with a distinct `note:` per scenario, and the acceptance criteria assert the four notes are mutually distinct. Task 2 requires six separate verdicts each backed by a stated observation. |
| T-37-05-04 | Spoofing | item 6 walked against `next dev` and recorded as a production-build confirmation | mitigate | Task 1 builds and serves with `npm run build && npm run start` and forbids starting `next dev`; the addendum must contain the literal phrase `npm run start` and state the distinction; the 2026-09-03 entry recording the earlier `next dev` walk is explicitly superseded rather than overwritten. |
| T-37-05-05 | Elevation of Privilege | the admin bypass from plan 37-01 verified only on the happy path | mitigate | Item 2 step 5 requires confirming no commission or rate input is visible on the newly admin-reachable proposal detail page; item 3 requires confirming an admin visiting `/clients` still receives a 404 rather than a 403 or redirect, so the walk exercises a denial as well as a grant. |
| T-37-05-06 | Information Disclosure | a real cross-tenant leak masked by a passing walk | accept | Cross-tenant isolation under a forged URL is `30-UAT.md` scenario 11, already `pass` from a prior confirmed session, and is not one of the four pending scenarios. It is not re-derived here; the walk records nothing about it. |
| T-37-05-07 | Repudiation | `33-VERIFICATION.md` reaching `status: passed` while a known item is quietly dropped | mitigate | Task 3 requires item 5 (migration 0009 on `main`/`preview`) to be stated explicitly in the addendum as an intentional deferral owned by the v1.6 milestone close, with `status: passed` scoped to the criteria CLOSE-03 actually names. |
| T-37-05-08 | Denial of Service | the walk expanding into fixes and swallowing the phase | mitigate | Item 5 is framed as a confirmation walk, not a bug hunt — WR-02 was fixed in `52d03e1` and Test 9b pins it. A failed item is recorded verbatim and escalated in the SUMMARY as an operator decision point; the plan forbids forcing either document to a passing state and asserts `git status --porcelain` lists no source file. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `git status --porcelain` must show no `package.json` or `package-lock.json` change. |
</threat_model>

<verification>
- `npm run check:local-db-branch` exited 0 against a non-production branch, and its hostname is
  recorded.
- No migration command was run at any point.
- The walk was performed against `npm run build && npm run start`, not `next dev`.
- Six individual operator verdicts exist, each with an observation.
- `30-UAT.md` reads `pending: 0`, contains no `result: [pending]`, and its Summary counts are
  self-consistent with its scenario headings.
- `33-VERIFICATION.md` reads `status: passed` with a new Phase 37 addendum closing items 1 and 3
  and explicitly deferring item 5.
- `git status --porcelain` lists only the two planning documents and this plan's SUMMARY.
</verification>

<success_criteria>
CLOSE-01 and CLOSE-03 are closed: all four of Phase 30's pending UAT scenarios and both of Phase
33's residual human items were walked in one operator session after every code change in this phase
landed, scenario 9 walked the real admin click-through instead of the dead end, D-08's gate was
confirmed against a production build, and both documents now record the outcome individually rather
than in aggregate.
</success_criteria>

<output>
Create `.planning/phases/37-crm-stack-closure/37-05-SUMMARY.md` when done.
It MUST record: the Neon branch hostname from the INFRA-05 guard; an explicit statement that no
migration command was run; the six operator verdicts verbatim; the computed vs. written Summary
counts for `30-UAT.md`; and — if any item failed — the failure and the operator decision point it
raises.
</output>
