---
phase: 06-auth-shell
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - src/lib/auth/require.ts
  - src/lib/auth/require.test.ts
  - src/lib/auth/actions.ts
  - src/lib/auth/actions.test.ts
  - proxy.ts
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - AUTH-11
  - AUTH-14
  - AUTH-15
  - AUTH-16

must_haves:
  truths:
    - "src/lib/auth/require.ts exports requireUser() (redirects to /login if no session OR if user.deletedAt is set) and requireAdmin() (calls notFound() if role !== 'admin')"
    - "requireUser() does an in-band session_version + deletedAt re-check against the DB (catches the 5-min cookieCache window)"
    - "src/lib/auth/actions.ts is a 'use server' module exporting disableUser, reEnableUser, createInvitation, createPasswordReset — all guarded by requireAdmin() before any data access"
    - "disableUser bumps session_version + sets deleted_at + calls Better Auth's session-revoke API for immediate session deletion"
    - "createInvitation and createPasswordReset invalidate any prior unused tokens for the user, generate a fresh token via generateToken(), insert a passwordResets row, return the plaintext URL"
    - "proxy.ts (NOT middleware.ts) at project root — exports a `proxy` function (not `middleware`); cookie-only check via getSessionCookie; redirects unauthenticated to /login?next=<path>; redirects authenticated /login visits to /; matcher excludes _next, fonts, favicon, /api/auth, /healthz"
    - "The proxy.ts file does NOT export `runtime` — Next.js 16 throws if a proxy file declares runtime"
  artifacts:
    - path: "src/lib/auth/require.ts"
      provides: "requireUser() / requireAdmin() server-side guards used by every (authed) and (admin) layout + every server action"
      exports: ["requireUser", "requireAdmin"]
    - path: "src/lib/auth/actions.ts"
      provides: "Server actions for admin-mediated user lifecycle (disable, re-enable, invite, reset)"
      exports: ["disableUser", "reEnableUser", "createInvitation", "createPasswordReset"]
    - path: "proxy.ts"
      provides: "Next.js 16 coarse auth gate — cookie-only redirect, no DB lookup"
      exports: ["proxy", "config"]
  key_links:
    - from: "src/lib/auth/require.ts"
      to: "src/lib/auth/index.ts (auth.api.getSession)"
      via: "imports auth from '@/lib/auth' and calls auth.api.getSession({ headers: await headers() })"
      pattern: "auth.api.getSession"
    - from: "src/lib/auth/require.ts"
      to: "src/db/schema.ts (users.sessionVersion + users.deletedAt)"
      via: "secondary in-band check via db().query.users.findFirst"
      pattern: "sessionVersion.*deletedAt"
    - from: "src/lib/auth/actions.ts"
      to: "auth.api session-revoke API"
      via: "primary revocation mechanism on disable"
      pattern: "revoke"
    - from: "proxy.ts"
      to: "better-auth/cookies (getSessionCookie)"
      via: "cookie presence check; no DB lookup"
      pattern: "getSessionCookie"
---

<objective>
Build the authorization layer that protects every authenticated and admin route. Three concerns:
1. `src/lib/auth/require.ts` — `requireUser()` and `requireAdmin()` guards that combine `auth.api.getSession()` with an in-band DB re-check of `sessionVersion` + `deletedAt` (the secondary AUTH-16 mechanism that catches the 5-min cookieCache window).
2. `src/lib/auth/actions.ts` — `'use server'` actions for admin-mediated user lifecycle: `disableUser`, `reEnableUser`, `createInvitation`, `createPasswordReset`. Each starts with `requireAdmin()` (defence-in-depth per AUTH-15 / PITFALLS §7.3).
3. `proxy.ts` at project root — Next.js 16 coarse auth gate (NOT `middleware.ts` per 06-RESEARCH.md §1). Cookie-only check; redirects unauthenticated visitors to `/login?next=<path>` and authenticated visitors away from `/login`.

Purpose: Plans 06-05/06/07 (public pages, authed shell, admin shell) all import these guards or invoke these actions. Without `proxy.ts` the redirect rules don't fire. Without `requireAdmin()` the env-driven admin URL has no role check (URL obscurity is NOT security per PITFALLS §7.1).

Output: 5 files (3 source + 2 test files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-shell/06-CONTEXT.md
@.planning/phases/06-auth-shell/06-RESEARCH.md
@.planning/phases/06-auth-shell/06-PATTERNS.md
@.planning/phases/06-auth-shell/06-03-SUMMARY.md
@src/lib/auth/index.ts
@src/lib/auth/tokens.ts
@src/lib/db/index.ts
@src/db/schema.ts
@src/lib/health.ts

<interfaces>
- 06-03's auth: `auth.api.getSession({ headers })` returns Promise<Session | null>. The session-revoke API method name should be confirmed against the installed Better Auth types (per RESEARCH §Assumptions A4 — likely `auth.api.revokeUserSessions` or `auth.api.revokeAllSessions`); use whatever the TypeScript types accept after `npm install`.
- 06-03's tokens: `generateToken()` returns `{ plaintext, hash }` — 32 random bytes URL-safe base64 + sha256 hex.
- 06-01's schema: `users` (text id, role, sessionVersion, deletedAt, email, displayName, createdBy, lastLoginAt), `passwordResets` (uuid id, userId text, kind, tokenHash unique, expiresAt, usedAt).

CRITICAL — research overrides:
- proxy.ts NOT middleware.ts (06-RESEARCH.md §1)
- export const config = { matcher: [...] } valid; export const runtime = ... INVALID (throws in Next.js 16 proxy files)
- proxy uses getSessionCookie(request) from better-auth/cookies (no DB lookup; PITFALLS §1.5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build requireUser() and requireAdmin() helpers + tests</name>
  <files>src/lib/auth/require.ts, src/lib/auth/require.test.ts</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §4 "Session Lifecycle and Revocation" (full TS spec)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "src/lib/auth/require.ts" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/health.ts (Phase 5 classifyError + guard pattern)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-18 (404 not 403), D-21 (redirect with next), D-23 (deleted_at = disabled)
  </read_first>
  <behavior>
    - Test 1: requireUser() with no session returns Promise that triggers redirect to '/login'
    - Test 2: requireUser() with session + active user (deletedAt null) returns { session, role: 'partner' }
    - Test 3: requireUser() with session but user.deletedAt set redirects to /api/auth/sign-out?redirect=/login
    - Test 4: requireUser() with session but user row missing in DB redirects to /api/auth/sign-out?redirect=/login
    - Test 5: requireAdmin() with role 'admin' returns { session }
    - Test 6: requireAdmin() with role 'partner' calls notFound() (NOT redirect, NOT 403)
    - Test 7: requireAdmin() with unknown role string still treated as non-admin and 404s
  </behavior>
  <action>
Create `src/lib/auth/require.ts` per 06-RESEARCH.md §4. The in-band session_version + deletedAt re-check stays even though primary revocation is the Better Auth session-revoke API call (the cookieCache TTL means a stale cookie can serve session data for up to 5 min after revocation; the DB re-check tightens that window to per-request).

File contents (use the Write tool):

```
import 'server-only';
import { headers } from 'next/headers';
import { redirect, notFound } from 'next/navigation';
import { eq } from 'drizzle-orm';
import { auth } from './index';
import { db, schema } from '@/lib/db';

export type Role = 'partner' | 'admin';

export interface RequireUserResult {
  session: NonNullable<Awaited<ReturnType<typeof auth.api.getSession>>>;
  role: Role;
}

export async function requireUser(): Promise<RequireUserResult> {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) {
    redirect('/login');
  }
  // Secondary AUTH-16 check: re-read role + sessionVersion + deletedAt.
  const user = await db().query.users.findFirst({
    where: eq(schema.users.id, session.user.id),
    columns: { sessionVersion: true, deletedAt: true, role: true },
  });
  if (!user || user.deletedAt !== null) {
    redirect('/api/auth/sign-out?redirect=/login');
  }
  return {
    session,
    role: (user.role === 'admin' ? 'admin' : 'partner') as Role,
  };
}

export async function requireAdmin(): Promise<{ session: RequireUserResult['session'] }> {
  const { session, role } = await requireUser();
  if (role !== 'admin') {
    notFound();
  }
  return { session };
}
```

Add a leading JSDoc block above the imports explaining: (a) PITFALLS §7.3 ordering rule (always require* first, then data access), (b) AUTH-16 secondary in-band check, (c) D-18 reason for `notFound()` not 403, (d) `import 'server-only'` so any Client Component import errors at build time.

Create `src/lib/auth/require.test.ts`. Mock `auth.api.getSession`, `db()`, `redirect`, `notFound`. Vitest mocks at module scope, then import after:

```
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('next/headers', () => ({
  headers: () => Promise.resolve(new Headers()),
}));
vi.mock('next/navigation', () => ({
  redirect: vi.fn((url) => { throw new Error('__REDIRECT__:' + url); }),
  notFound: vi.fn(() => { throw new Error('__NOT_FOUND__'); }),
}));
const mockGetSession = vi.fn();
vi.mock('./index', () => ({
  auth: { api: { getSession: (a) => mockGetSession(a) } },
}));
const mockFindFirst = vi.fn();
vi.mock('@/lib/db', () => ({
  db: () => ({ query: { users: { findFirst: (a) => mockFindFirst(a) } } }),
  schema: { users: { id: 'id-col' } },
}));

import { requireUser, requireAdmin } from './require';
import { redirect, notFound } from 'next/navigation';

beforeEach(() => { vi.clearAllMocks(); });

describe('requireUser', () => {
  it('redirects to /login when no session', async () => {
    mockGetSession.mockResolvedValue(null);
    await expect(requireUser()).rejects.toThrow('__REDIRECT__:/login');
    expect(redirect).toHaveBeenCalledWith('/login');
  });
  it('returns {session, role:partner} for active partner', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_1' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 1, deletedAt: null, role: 'partner' });
    const r = await requireUser();
    expect(r.role).toBe('partner');
  });
  it('returns role admin for active admin', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_a' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 1, deletedAt: null, role: 'admin' });
    const r = await requireUser();
    expect(r.role).toBe('admin');
  });
  it('forces sign-out when deletedAt is set', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_2' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 5, deletedAt: new Date(), role: 'partner' });
    await expect(requireUser()).rejects.toThrow('__REDIRECT__:/api/auth/sign-out?redirect=/login');
  });
  it('forces sign-out when DB user row missing', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_3' } });
    mockFindFirst.mockResolvedValue(undefined);
    await expect(requireUser()).rejects.toThrow('__REDIRECT__:/api/auth/sign-out?redirect=/login');
  });
});

describe('requireAdmin', () => {
  it('returns session for role admin', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_a' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 1, deletedAt: null, role: 'admin' });
    const r = await requireAdmin();
    expect(r.session.user.id).toBe('u_a');
  });
  it('calls notFound for partner role', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_p' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 1, deletedAt: null, role: 'partner' });
    await expect(requireAdmin()).rejects.toThrow('__NOT_FOUND__');
    expect(notFound).toHaveBeenCalled();
  });
  it('calls notFound defensively for unknown role string', async () => {
    mockGetSession.mockResolvedValue({ user: { id: 'u_x' } });
    mockFindFirst.mockResolvedValue({ sessionVersion: 1, deletedAt: null, role: 'guest' });
    await expect(requireAdmin()).rejects.toThrow('__NOT_FOUND__');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm run typecheck &amp;&amp; npx vitest run src/lib/auth/require.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - require.ts exports `requireUser`, `requireAdmin`, `Role` (type), `RequireUserResult` (interface)
    - First non-empty line of require.ts is `import 'server-only';` (or a comment block followed by it)
    - require.ts contains the literal substring `redirect('/login')` exactly once
    - require.ts contains the literal substring `notFound()` at least once
    - require.ts contains the literal substring `/api/auth/sign-out?redirect=/login` exactly once
    - All 8 test cases in require.test.ts pass under `npx vitest run`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>requireUser + requireAdmin guards landed with PITFALLS §7.3 ordering, AUTH-16 secondary check, D-18 404-not-403 behavior; 8 tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build src/lib/auth/actions.ts (admin user-lifecycle server actions) + tests</name>
  <files>src/lib/auth/actions.ts, src/lib/auth/actions.test.ts</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §3 "Admin-Creates-Account Flow" + §4 "Revocation Server Action"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "src/lib/auth/actions.ts" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/theme/actions.ts (existing 'use server' pattern)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-09 (24h TTL), D-11 (re-issuance invalidates prior), D-12 (token format), D-23 (disable = bump session_version + deleted_at)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-03-SUMMARY.md (confirms exact Better Auth session-revoke method name)
  </read_first>
  <behavior>
    - Test 1: disableUser(userId) calls requireAdmin() before touching DB; if requireAdmin throws (notFound/redirect), no DB writes occur
    - Test 2: disableUser(userId) updates users SET deleted_at = NOW(), session_version = session_version + 1 WHERE id = userId
    - Test 3: disableUser(userId) calls the Better Auth revoke-sessions API with { userId }
    - Test 4: reEnableUser(userId) updates users SET deleted_at = NULL WHERE id = userId (does NOT bump session_version per D-23)
    - Test 5: createInvitation(email, displayName) — when email is new — inserts a users row with role='partner' + lowercased email + a passwordResets row with kind='invite', and returns { url: 'https://.../invite/<plaintext>' }
    - Test 6: createInvitation deletes any prior unused passwordResets rows for that user before inserting (D-11)
    - Test 7: createInvitation with an active existing user (deletedAt null) throws "User already active — use reset instead"
    - Test 8: createPasswordReset(userId) inserts a passwordResets row with kind='reset' and returns the URL with /reset/<plaintext>; invalidates prior tokens for that user
  </behavior>
  <action>
Create `src/lib/auth/actions.ts`. The session-revoke API method name should be the one resolved in 06-03-SUMMARY.md after `npm install` — assume `auth.api.revokeUserSessions({ body: { userId } })` per RESEARCH §4 unless SUMMARY says otherwise. If the installed types reject this call, substitute the actual method name and document the deviation in this plan's SUMMARY.

```
'use server';

import { eq, and, sql } from 'drizzle-orm';
import { auth } from './index';
import { requireAdmin } from './require';
import { generateToken } from './tokens';
import { db, schema } from '@/lib/db';

const APP_URL = process.env.APP_URL ?? 'http://localhost:3000';
const TOKEN_TTL_MS = 24 * 60 * 60 * 1000; // D-09: 24-hour TTL

export interface InviteResult { url: string; }
export interface ResetResult { url: string; }

export async function disableUser(userId: string): Promise<void> {
  await requireAdmin();
  await db().update(schema.users).set({
    deletedAt: sql`NOW()`,
    sessionVersion: sql`${schema.users.sessionVersion} + 1`,
  }).where(eq(schema.users.id, userId));
  // Primary revocation: delete all DB sessions for this user (immediate effect).
  // Secondary defence: cookieCache TTL + requireUser()'s in-band deletedAt check.
  await auth.api.revokeUserSessions({ body: { userId } });
}

export async function reEnableUser(userId: string): Promise<void> {
  await requireAdmin();
  // Per D-23: clearing deleted_at re-enables; do NOT bump session_version (would
  // log-out the re-enabled user before they sign in again).
  await db().update(schema.users)
    .set({ deletedAt: null })
    .where(eq(schema.users.id, userId));
}

async function invalidatePriorTokens(userId: string): Promise<void> {
  await db().delete(schema.passwordResets)
    .where(eq(schema.passwordResets.userId, userId));
}

export async function createInvitation(email: string, displayName: string): Promise<InviteResult> {
  await requireAdmin();
  const lowered = email.toLowerCase();

  const existing = await db().query.users.findFirst({
    where: eq(schema.users.email, lowered),
    columns: { id: true, deletedAt: true, role: true },
  });

  let userId: string;

  if (existing) {
    if (existing.deletedAt === null) {
      // D-11: re-invitation is rejected for active users. Admin must use reset.
      throw new Error('User already active — use reset password instead.');
    }
    // User exists but was disabled: re-enable + new invitation.
    await db().update(schema.users)
      .set({ deletedAt: null, displayName })
      .where(eq(schema.users.id, existing.id));
    userId = existing.id;
  } else {
    // Generate Better Auth-compatible nanoid-style id (21 chars, base62-ish).
    // Better Auth's adapter accepts our pre-generated id; if not, omit and let
    // Drizzle's defaultRandom() pick one (note: schema currently doesn't default).
    const nanoidLike = generateToken().plaintext.slice(0, 21);
    userId = nanoidLike;
    await db().insert(schema.users).values({
      id: userId,
      email: lowered,
      name: displayName,
      displayName,
      role: 'partner',
      // password lives in accounts.password (Better Auth-managed); not set yet.
    });
  }

  await invalidatePriorTokens(userId);
  const { plaintext, hash } = generateToken();
  await db().insert(schema.passwordResets).values({
    userId,
    kind: 'invite',
    tokenHash: hash,
    expiresAt: new Date(Date.now() + TOKEN_TTL_MS),
  });

  return { url: `${APP_URL}/invite/${plaintext}` };
}

export async function createPasswordReset(userId: string): Promise<ResetResult> {
  await requireAdmin();
  await invalidatePriorTokens(userId);
  const { plaintext, hash } = generateToken();
  await db().insert(schema.passwordResets).values({
    userId,
    kind: 'reset',
    tokenHash: hash,
    expiresAt: new Date(Date.now() + TOKEN_TTL_MS),
  });
  return { url: `${APP_URL}/reset/${plaintext}` };
}
```

Add a JSDoc preamble explaining: (a) PITFALLS §7.3 ordering — every export starts with `requireAdmin()`, (b) D-11 invitation/reset re-issuance discipline, (c) what gets passed to Better Auth's session-revoke API, (d) why `password` field is NOT set on the users insert (Better Auth owns accounts.password).

NOTE on user creation pattern: 06-RESEARCH.md §3 documented two options (signUpEmail with throwaway password, or direct Drizzle insert bypassing Better Auth). This plan uses direct Drizzle insert — simpler, avoids the bogus-password churn. The token redemption flow in Plan 06-05 is responsible for inserting the matching `accounts` row with the argon2id hash when the partner sets their initial password.

NOTE on user ID generation: Better Auth uses nanoid internally; we generate a 21-char prefix from `generateToken().plaintext` (URL-safe base64) which is base62-compatible-enough for Better Auth's expectations. The actual nanoid library is a transitive dependency; if direct nanoid import is needed, fallback to `crypto.randomBytes(16).toString('base64url').slice(0, 21)`. Document the choice in SUMMARY.

Create `src/lib/auth/actions.test.ts`. Mock `requireAdmin`, `auth.api.revokeUserSessions`, `db()`, `generateToken`. Cover the 8 behaviors above. Example skeleton:

```
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('./require', () => ({ requireAdmin: vi.fn().mockResolvedValue({ session: { user: { id: 'admin-1' } } }) }));
const mockRevoke = vi.fn().mockResolvedValue(undefined);
vi.mock('./index', () => ({ auth: { api: { revokeUserSessions: (a) => mockRevoke(a) } } }));
vi.mock('./tokens', () => ({ generateToken: () => ({ plaintext: 'TESTTOKEN', hash: 'TESTHASH' }) }));

const dbCalls: { type: string; args: unknown }[] = [];
const fakeDb = {
  update: (t: unknown) => ({ set: (s: unknown) => ({ where: (w: unknown) => { dbCalls.push({type:'update', args:{t,s,w}}); return Promise.resolve(); } }) }),
  insert: (t: unknown) => ({ values: (v: unknown) => { dbCalls.push({type:'insert', args:{t,v}}); return Promise.resolve(); } }),
  delete: (t: unknown) => ({ where: (w: unknown) => { dbCalls.push({type:'delete', args:{t,w}}); return Promise.resolve(); } }),
  query: { users: { findFirst: vi.fn() } },
};
vi.mock('@/lib/db', () => ({
  db: () => fakeDb,
  schema: { users: { id:'id', email:'email', sessionVersion:'sv', deletedAt:'da', displayName:'dn' }, passwordResets: { userId:'uid', tokenHash:'th', kind:'k', expiresAt:'ea' } },
}));

import { disableUser, reEnableUser, createInvitation, createPasswordReset } from './actions';
import { requireAdmin } from './require';

beforeEach(() => { vi.clearAllMocks(); dbCalls.length = 0; fakeDb.query.users.findFirst.mockReset(); });

// ... (write the 8 tests verifying call ordering, payload contents, throw on already-active, URL shape)
```

The test must explicitly assert that `requireAdmin` is called BEFORE any `dbCalls` entry (use `expect(requireAdmin.mock.invocationCallOrder[0]).toBeLessThan(...)` against the first DB-call timestamp, OR simply track an array of invocation order via a local counter).

NOTE on `'use server'`: the module-level directive marks every export as a Server Action. Tests bypass this by importing the module directly in a Node context (Vitest treats it as a regular ESM module since `.test.ts` files are not Next.js-built; the `'use server'` is a no-op outside the Next.js bundler).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm run typecheck &amp;&amp; npx vitest run src/lib/auth/actions.test.ts &amp;&amp; head -1 src/lib/auth/actions.ts | grep -c "'use server'" &amp;&amp; grep -c "requireAdmin()" src/lib/auth/actions.ts | grep -qE "^[4-9]$" &amp;&amp; grep -c "session_version + 1" src/lib/auth/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - actions.ts starts with `'use server'` directive on the first non-comment line
    - Every exported function (disableUser, reEnableUser, createInvitation, createPasswordReset) calls `requireAdmin()` as the first await before any DB access — verified by source inspection AND by test assertions on call order (`requireAdmin` was invoked, AND no DB write occurred when the mocked requireAdmin throws)
    - disableUser sets `deletedAt: sql\`NOW()\`` and `sessionVersion: sql\`... + 1\`` and calls the Better Auth revoke API
    - createInvitation throws when the existing user has `deletedAt === null` (active — D-11)
    - createInvitation deletes prior tokens for that user before inserting a new one (verified by test asserting the delete call precedes the insert call)
    - Both URL-returning actions return `{ url }` of the form `${APP_URL}/(invite|reset)/<plaintext>`
    - All 8 test cases pass under `npx vitest run`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>4 admin server actions wired, all PITFALLS §7.3-correct (requireAdmin first), with 8 tests covering ordering, payload, idempotence, and the active-user re-invite rejection.</done>
</task>

<task type="auto">
  <name>Task 3: Create proxy.ts at project root (Next.js 16 coarse auth gate)</name>
  <files>proxy.ts</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §1 "Critical SDK Finding: proxy.ts replaces middleware.ts" (full TS spec)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "proxy.ts" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-21 (redirect with `?next=`), D-32 (matcher exclusions); per the research-supersedes-context note, D-32's `middleware.ts` references map to `proxy.ts` and the runtime opt-out is invalid
  </read_first>
  <action>
Create `proxy.ts` at the project root (same level as `app/`, `package.json`, `next.config.ts`). Per RESEARCH §1: file name is `proxy.ts`, exported function is `proxy`, NO `runtime` declaration (throws in Next.js 16).

```
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { getSessionCookie } from 'better-auth/cookies';

/**
 * Coarse auth gate (Next.js 16 proxy.ts convention; replaces deprecated middleware.ts).
 *
 * Per PITFALLS §1.5: cookie-only check, NO DB lookup. Role checks happen in layouts
 * (Plan 06-06 / 06-07's requireUser / requireAdmin). This file only enforces the
 * coarse "logged in vs not" boundary.
 *
 * Per D-21: unauthenticated visitors are redirected to /login?next=<encoded-path>
 * so post-login they land where they tried to go. Authenticated visitors hitting
 * /login are redirected to /.
 *
 * The matcher excludes:
 *  - /_next/static, /_next/image, /favicon.ico, /fonts/  — static assets
 *  - /api/auth/*                                          — Better Auth's internal endpoints
 *  - /healthz                                              — Phase 5 unauthenticated health check
 *
 * Public routes whitelisted INSIDE the proxy (not via the matcher):
 *  - /login                — entry point for unauthenticated users
 *  - /invite/<token>       — partner sets initial password (D-09)
 *  - /reset/<token>        — partner resets password (D-09)
 *
 * Per RESEARCH §1: do NOT add `export const config = { runtime: 'nodejs' }`.
 * Next.js 16 throws on runtime declarations in proxy files (Node is the default).
 */
export function proxy(request: NextRequest) {
  const sessionCookie = getSessionCookie(request);
  const { pathname } = request.nextUrl;

  // Authenticated users on /login → home
  if (sessionCookie && pathname === '/login') {
    return NextResponse.redirect(new URL('/', request.url));
  }

  // Unauthenticated users on protected routes → /login?next=<path>
  if (!sessionCookie && !isPublicPath(pathname)) {
    const next = encodeURIComponent(pathname);
    return NextResponse.redirect(new URL(`/login?next=${next}`, request.url));
  }

  return NextResponse.next();
}

function isPublicPath(pathname: string): boolean {
  return (
    pathname === '/login' ||
    pathname.startsWith('/invite/') ||
    pathname.startsWith('/reset/')
  );
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|fonts/|healthz|api/auth).*)',
  ],
};
```

The matcher uses a negative lookahead to exclude Next.js static assets (`_next/static`, `_next/image`), browser favicon, the self-hosted Plus Jakarta Sans fonts directory, the Phase 5 `/healthz` route, and Better Auth's catch-all endpoint (`api/auth/*`).

NOTE on `getSessionCookie` import: per RESEARCH §1, the function is exported from `better-auth/cookies`. If the installed `better-auth@1.6.9` types resolve this differently, adjust the import path and document in SUMMARY.

NOTE on cookie name: 06-RESEARCH.md §9 P11 mentions Better Auth's default cookie name is `better-auth.session_token` — `getSessionCookie(request)` defaults to that name with no extra arg needed. Plan 06-03 did NOT rename it (Claude's discretion left it as default), so no `cookieName` arg is required here.

VERIFY THE FILE LANDS AT PROJECT ROOT, NOT INSIDE `app/`. Next.js 16 looks for proxy.ts at the same level as next.config.ts. If placed inside `app/`, it is silently ignored.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ls proxy.ts &amp;&amp; ! ls app/proxy.ts 2>/dev/null &amp;&amp; grep -c "export function proxy" proxy.ts &amp;&amp; ! grep -q "export function middleware" proxy.ts &amp;&amp; ! grep -qE "runtime:\s*['\"]nodejs['\"]" proxy.ts &amp;&amp; grep -c "getSessionCookie" proxy.ts &amp;&amp; grep -c "?next=" proxy.ts &amp;&amp; grep -cE "_next/static\|_next/image\|favicon" proxy.ts &amp;&amp; grep -c "healthz" proxy.ts &amp;&amp; grep -c "api/auth" proxy.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/Users/antoinerousseau/Developer/leasetic-calculator/proxy.ts` (project root, NOT inside app/)
    - File exports `proxy` function and `config` constant
    - File does NOT export `middleware` (no legacy name)
    - File does NOT contain a `runtime` declaration anywhere (no `runtime: 'nodejs'` or `'edge'`)
    - File contains literal string `getSessionCookie` (the Better Auth helper)
    - File contains literal string `?next=` (the redirect parameter for D-21)
    - Matcher regex excludes `_next/static`, `_next/image`, `favicon.ico`, `fonts/`, `healthz`, `api/auth`
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
    - `npm run build` succeeds (Next.js 16 successfully recognizes the proxy.ts convention; no error about deprecated middleware.ts; no error about invalid runtime declaration)
  </acceptance_criteria>
  <done>proxy.ts at project root with cookie-only redirect logic; AUTH-05 + AUTH-06 satisfied at the coarse boundary; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → proxy.ts | Untrusted cookie + path. Proxy reads cookie value byte-for-byte; never trusts cookie payload to bypass redirects. |
| Server Action client invocation → actions.ts | Untrusted Form data; requireAdmin() must run before any data access. |
| In-band session_version check | Defence-in-depth bridging the cookieCache window after disable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Spoofing | proxy spoofed by request omitting cookie | accept | Coarse gate; layouts call requireUser() which does the real DB-backed check. PITFALLS §1.5 documents this layering. |
| T-06-04-02 | Elevation of Privilege | partner reaches admin layout if requireAdmin not called | mitigate | every admin route, layout, server action calls requireAdmin() (AUTH-15). require.ts uses notFound() (D-18 — 404 not 403, preserves URL secrecy). |
| T-06-04-03 | Tampering | server action invoked from non-admin client (forged origin) bypasses requireAdmin | mitigate | requireAdmin() reads server-side session — invocation source doesn't matter, only the session cookie does. Plus Better Auth's CSRF protections (Origin header check) reject cross-origin posts. |
| T-06-04-04 | Information Disclosure | requireUser() leaks deletedAt timing differences (length of error vs success path) | accept | Plan 06-05's LoginForm always shows the same generic error regardless of underlying reason (D-22). Side-channel timing attacks on deletedAt are not in scope. |
| T-06-04-05 | DoS | proxy DB lookup floods every request | mitigate | proxy.ts is cookie-only — NO DB lookup (PITFALLS §1.5). The DB hit happens in requireUser() in the layout, which only runs for routes that actually render. |
| T-06-04-06 | Tampering | createInvitation called for an already-active user re-invitating them and silently invalidating their session | mitigate | createInvitation throws when `deletedAt === null`; admin must use createPasswordReset for active users (D-11). |
| T-06-04-07 | Spoofing | token reuse: copy-paste old plaintext into URL after admin re-issued | mitigate | Re-issuance deletes prior `passwordResets` rows for that user (invalidatePriorTokens), so the old hash no longer matches in the DB lookup. The redemption flow in Plan 06-05 enforces single-use via `usedAt`. |
| T-06-04-08 | Information Disclosure | session-revoke method name mismatch silently no-ops | mitigate | This plan's task 2 verification step calls the API and asserts (via test mock) that it was called with the expected payload. If the installed Better Auth types reject the call, the typecheck step fails immediately. |
| T-06-04-09 | Repudiation | no audit log for admin disable / invitation actions | accept | Phase 6 deferred audit_log to Phase 9 (see CONTEXT.md "Audit-log writes during Phase 6"). Structured server logs via console.error/info are an acceptable interim per CONTEXT.md. |
</threat_model>

<verification>
At plan completion the following must all be true:

1. `cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck` exits 0
2. `npx vitest run src/lib/auth/require.test.ts src/lib/auth/actions.test.ts` shows ≥15 tests passing
3. `npm run lint:check` exits 0
4. `npm run build` exits 0 (Next.js 16 happy with proxy.ts at root)
5. `ls proxy.ts` succeeds; `ls app/proxy.ts` fails (correct location)
6. `! grep -q "export function middleware" proxy.ts` (no legacy name)
7. `! grep -qE "runtime:\\s*['\"]nodejs['\"]" proxy.ts` (no runtime declaration)
8. `head -1 src/lib/auth/actions.ts | grep -c "'use server'"` returns 1
9. `grep -c "requireAdmin()" src/lib/auth/actions.ts` returns 4 (one per exported action)
</verification>

<success_criteria>
- AUTH-05 + AUTH-06 satisfied via proxy.ts cookie-only redirect logic
- AUTH-11 + AUTH-16 satisfied: disableUser bumps session_version, calls revoke API, sets deletedAt; requireUser does in-band re-check
- AUTH-15 satisfied: every server action starts with requireAdmin()
- AUTH-14 partially satisfied: requireAdmin returns notFound (404) for non-admins; admin segment env-check lands in Plan 06-07's layout
- Plans 06-05/06/07 can now import requireUser/requireAdmin and the four admin actions; the proxy unconditionally redirects unauthenticated traffic so 06-05 only needs to handle the post-redirect form
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-shell/06-04-SUMMARY.md` covering: the actual session-revoke API method name observed (resolves RESEARCH §Assumptions A4), any deviations from the planned mock structure, p95 estimate of requireUser DB latency on local Postgres, and any cookie-name customizations applied (or not) in proxy.ts vs Better Auth defaults.
</output>
