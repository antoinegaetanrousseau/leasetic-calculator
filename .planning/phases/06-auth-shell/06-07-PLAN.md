---
phase: 06-auth-shell
plan: 07
type: execute
wave: 4
depends_on:
  - 06-04
  - 06-02
files_modified:
  - app/(admin)/[adminSegment]/layout.tsx
  - app/(admin)/[adminSegment]/page.tsx
  - src/components/InviteUrlModal.tsx
autonomous: true
requirements:
  - AUTH-07
  - AUTH-08
  - AUTH-10
  - AUTH-14
  - AUTH-15

must_haves:
  truths:
    - "Visiting /[adminSegment] where adminSegment === process.env.ADMIN_URL_SEGMENT AND user.role === 'admin' renders the admin shell (same Topbar + sidebar + main + footer as authed) with an ADMIN pill badge in the topbar"
    - "Visiting /[adminSegment] with a wrong segment value renders a 404 (NOT 403, NOT redirect) — preserves URL secrecy per D-18"
    - "Visiting /[adminSegment] with the correct segment but a partner role also returns 404 (requireAdmin's notFound() invocation)"
    - "InviteUrlModal is a Client Component with backdrop + centered panel, focus trap, Escape close, role=dialog aria-modal=true, copy-to-clipboard button with 2s confirmation, and is reusable for both invitation (kind='invite') and reset (kind='reset') URLs per UI-SPEC"
    - "InviteUrlModal source displays the URL once + the warning banner + the 'Validité 24 heures, usage unique.' footer note — never persists or transmits the URL anywhere else"
  artifacts:
    - path: "app/(admin)/[adminSegment]/layout.tsx"
      provides: "Hidden admin URL gate: env-segment check (notFound on mismatch) + requireAdmin (notFound on non-admin) + admin shell"
      min_lines: 30
    - path: "app/(admin)/[adminSegment]/page.tsx"
      provides: "Admin home placeholder; Phase 9 fills with coefficients + audit + accounts UIs"
      min_lines: 10
    - path: "src/components/InviteUrlModal.tsx"
      provides: "Reusable Client Component for displaying one-time invitation/reset URLs to admins (per D-10 / 06-UI-SPEC.md)"
      contains: "navigator.clipboard.writeText"
  key_links:
    - from: "app/(admin)/[adminSegment]/layout.tsx"
      to: "process.env.ADMIN_URL_SEGMENT"
      via: "params.adminSegment !== env → notFound() (D-18)"
      pattern: "process\\.env\\.ADMIN_URL_SEGMENT"
    - from: "app/(admin)/[adminSegment]/layout.tsx"
      to: "src/lib/auth/require.ts (requireAdmin)"
      via: "after segment check, requireAdmin() runs (404 not 403 on non-admin)"
      pattern: "await requireAdmin\\(\\)"
    - from: "src/components/InviteUrlModal.tsx"
      to: "navigator.clipboard.writeText"
      via: "copy-to-clipboard with 2-second 'Lien copié' confirmation"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Build the admin route group with two layers of defense (URL obscurity via env-driven segment + role check via requireAdmin) and the InviteUrlModal client primitive that future Phase 9 admin pages will use to surface one-time invitation/reset URLs (per D-10 / 06-UI-SPEC.md §Admin URL Display Modal).

Three concerns:
1. `app/(admin)/[adminSegment]/layout.tsx` — server component that first checks `params.adminSegment === process.env.ADMIN_URL_SEGMENT` (notFound on mismatch per D-18), then calls `requireAdmin()` (notFound on non-admin per D-18). Only when BOTH pass does it render the admin shell (same chrome as (authed)/layout but with `isAdmin={true}` propagated to Topbar so the ADMIN badge displays).
2. `app/(admin)/[adminSegment]/page.tsx` — minimal admin home; Phase 9 fills with the coefficients editor + accounts list + audit log table.
3. `src/components/InviteUrlModal.tsx` — Client Component implementing the "URL displayed once" modal pattern. Reusable for both kinds (invite + reset). Phase 9's admin partner-create form will mount this; Phase 6 ships the primitive with a smoke-test integration in the admin home.

Output: 3 files (2 in admin route group + 1 component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-shell/06-CONTEXT.md
@.planning/phases/06-auth-shell/06-RESEARCH.md
@.planning/phases/06-auth-shell/06-UI-SPEC.md
@.planning/phases/06-auth-shell/06-PATTERNS.md
@.planning/phases/06-auth-shell/06-04-SUMMARY.md
@.planning/phases/06-auth-shell/06-06-SUMMARY.md
@src/lib/auth/require.ts
@src/components/Topbar.tsx
@src/components/UserMenu.tsx

<interfaces>
- 06-04's require.ts: `requireAdmin()` calls `requireUser()` first (redirect to /login if no session, force-signout if disabled), then notFound() if role !== 'admin'.
- 06-06's Topbar: accepts `isAdmin?: boolean` prop; when true renders the ADMIN pill badge. The Topbar is reused as-is in the admin layout.
- 06-04's actions: `createInvitation`, `createPasswordReset` return `{ url }` — Phase 9 admin UI calls these and pipes the URL into InviteUrlModal.
- 06-02's i18n: `auth.modal.invite.title`, `auth.modal.reset.title`, `auth.modal.warning`, `auth.modal.button.copy`, `auth.modal.button.copied`, `auth.modal.button.close`, `auth.modal.footer.note`, `auth.toast.copy.success`, `shell.topbar.admin.badge`.

UI-SPEC contracts (06-UI-SPEC.md "Admin URL Display Modal"):
- Backdrop: position fixed inset 0, rgba(17,44,59,0.5), backdrop-filter blur(4px), z-index 200
- Panel: fixed centered, max-width 520, max-height 80vh internal scroll, --surface bg, border-radius 16, padding 28, shadow 0 20px 60px rgba(17,44,59,0.25)
- role="dialog" aria-modal="true" aria-labelledby="modal-title"
- Header: h2 (title) + X close button (Lucide X 20px, 32px hit target, hover bg --hover-overlay)
- Warning banner: rgba(224,133,48,0.08) gold-tint bg + 1px left border --gold + AlertTriangle 17px gold + warning copy
- URL display block: --paper bg, 1px var(--border), border-radius 12, padding 12 16, font-family monospace, font-size 12.5px, word-break break-all, user-select all
- Action row: flex gap 12px justify-content flex-end — .btn-green "Copier le lien" with Copy icon (becomes "Lien copié" + Check icon for 2s) + .btn-ghost "Fermer"
- Footer note: 10.5px --muted "Validité 24 heures, usage unique."
- Focus & accessibility: open → focus X close button; Escape closes; Tab cycles X → URL block (tabIndex=0) → Copy → Close → X; outside-click + close button both close; close → focus restores to triggerRef.current
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build (admin)/[adminSegment]/layout.tsx + page.tsx</name>
  <files>app/(admin)/[adminSegment]/layout.tsx, app/(admin)/[adminSegment]/page.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §7 "Hidden Admin URL — [adminSegment] Layout"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "app/(admin)/[adminSegment]/layout.tsx" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-17 (env var), D-18 (404 not 403 + independent requireAdmin), D-19 (admin discovery via env), D-20 (rotation = redeploy)
    - /Users/antoinerousseau/Developer/leasetic-calculator/app/(authed)/layout.tsx (Plan 06-06 — same shell pattern, this layout adds the segment + role gate)
  </read_first>
  <action>
Step 1: Create the route-group directory. Note shell-quoting for parens + brackets:

```
mkdir -p "/Users/antoinerousseau/Developer/leasetic-calculator/app/(admin)/[adminSegment]"
```

Step 2: Create `app/(admin)/[adminSegment]/layout.tsx`:

```
import { notFound } from 'next/navigation';
import { requireAdmin } from '@/lib/auth/require';
import { getCurrentLang, getCurrentTheme, t } from '@/lib/i18n';
import { Topbar } from '@/components/Topbar';

export const dynamic = 'force-dynamic';

interface AdminLayoutProps {
  params: Promise<{ adminSegment: string }>;
  children: React.ReactNode;
}

/**
 * Hidden admin URL gate.
 *
 * Two-layer defense per AUTH-14 / AUTH-15 / D-18:
 *
 *  Layer 1 (URL obscurity): if params.adminSegment !== process.env.ADMIN_URL_SEGMENT,
 *  call notFound() (renders the 404 page, NOT 403). This preserves the URL's
 *  secrecy — a guesser can't distinguish "admin URL exists, you're not admin"
 *  from "this URL doesn't exist at all".
 *
 *  Layer 2 (role check): requireAdmin() does the actual security gate. Even if
 *  a partner happens to know the correct ADMIN_URL_SEGMENT, requireAdmin calls
 *  notFound() for non-admin users (PITFALLS §7.1: URL obscurity is not security;
 *  the role check is).
 *
 *  Per AUTH-15: this layout is not the only gate. Every admin route handler and
 *  page (Phase 9 will add many) MUST also call requireAdmin() before any data
 *  access (defense in depth + mitigates layout-bypass attacks).
 */
export default async function AdminLayout({ params, children }: AdminLayoutProps) {
  // PITFALL §1.1: params is a Promise in Next.js 16
  const { adminSegment } = await params;

  const expected = process.env.ADMIN_URL_SEGMENT;
  if (!expected || adminSegment !== expected) {
    notFound();
  }

  const { session } = await requireAdmin();
  const lang = await getCurrentLang();
  const theme = await getCurrentTheme();

  const u = session.user as { email: string; displayName?: string | null; name?: string | null };
  const displayName = u.displayName ?? u.name ?? u.email;

  return (
    <div style={{
      display: 'grid',
      gridTemplateColumns: 'var(--shell-sidebar-w) 1fr',
      gridTemplateRows: 'var(--topbar-h) 1fr var(--footer-h)',
      minHeight: '100vh',
    }}>
      <aside style={{
        gridRow: '1 / 4', gridColumn: '1',
        background: 'var(--surface)', borderRight: '1px solid var(--border)',
        padding: '1.5rem 1rem',
        position: 'sticky', top: 0, height: '100vh',
      }}>
        <div style={{ fontWeight: 700, color: 'var(--navy)', fontSize: '22px' }}>Leasétic</div>
      </aside>

      <Topbar
        displayName={displayName}
        email={u.email}
        lang={lang}
        theme={theme}
        isAdmin={true}
      />

      <main style={{
        gridRow: '2', gridColumn: '2',
        background: 'var(--paper)',
        padding: '1.5rem 1.5rem 2rem',
        maxWidth: '1100px', width: '100%', margin: '0 auto',
      }}>
        {children}
      </main>

      <footer style={{
        gridRow: '3', gridColumn: '2',
        background: 'var(--paper)',
        borderTop: '1px solid var(--border)',
        height: 'var(--footer-h)',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        fontSize: '10.5px', color: 'var(--muted)',
      }}>
        {t('shell.footer.copyright', lang)}
      </footer>
    </div>
  );
}
```

Step 3: Create `app/(admin)/[adminSegment]/page.tsx`:

```
import { requireAdmin } from '@/lib/auth/require';
import { getCurrentLang, t } from '@/lib/i18n';

export const dynamic = 'force-dynamic';

/**
 * Admin home page.
 *
 * Per AUTH-15: every admin route/handler/page calls requireAdmin() independently
 * — defense in depth, do NOT rely on the parent layout alone.
 *
 * Phase 9 fills this page with the coefficients editor + accounts list + audit
 * log table. Phase 6 ships the placeholder so the route is reachable for
 * verification.
 */
export default async function AdminHomePage() {
  await requireAdmin();
  const lang = await getCurrentLang();

  return (
    <div>
      <h1 style={{ fontSize: '1.5rem', fontWeight: 700, color: 'var(--ink)', marginBottom: '0.5rem' }}>
        {t('shell.topbar.admin.badge', lang)}
      </h1>
      <p style={{ color: 'var(--muted)', fontSize: '14.5px' }}>
        {/* Placeholder copy — Phase 9 replaces with the coefficients + accounts UIs. */}
        {/* Reuses the welcomeSubtext key for now; Phase 9 will introduce admin.* keys. */}
        {t('welcomeSubtext', lang)}
      </p>
    </div>
  );
}
```

NOTE on env var: `ADMIN_URL_SEGMENT` was documented in `.env.example` by Plan 06-01. The dev developer must set it in `.env.local` before running locally; the verification command below tests the absent-env case (returns 404, which is correct — the gate fails closed when env is missing).

NOTE on directory creation: route group directories with parens AND dynamic segments with brackets BOTH need shell-quoting. Use double-quotes around the path: `"app/(admin)/[adminSegment]"`. After the directory exists, the layout.tsx + page.tsx files inside it are recognized by Next.js's file-based router.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ls "app/(admin)/[adminSegment]/layout.tsx" "app/(admin)/[adminSegment]/page.tsx" &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run build &amp;&amp; grep -c "process.env.ADMIN_URL_SEGMENT" "app/(admin)/[adminSegment]/layout.tsx" &amp;&amp; grep -c "notFound()" "app/(admin)/[adminSegment]/layout.tsx" &amp;&amp; grep -c "await requireAdmin()" "app/(admin)/[adminSegment]/layout.tsx" &amp;&amp; grep -c "await requireAdmin()" "app/(admin)/[adminSegment]/page.tsx" &amp;&amp; grep -c "isAdmin={true}" "app/(admin)/[adminSegment]/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the exact paths above
    - layout.tsx awaits `params` (PITFALL §1.1)
    - layout.tsx checks `params.adminSegment !== process.env.ADMIN_URL_SEGMENT` and calls `notFound()` on mismatch (BEFORE requireAdmin — segment check first per D-18 layered ordering)
    - layout.tsx ALSO calls `await requireAdmin()` after the segment check
    - page.tsx ALSO calls `await requireAdmin()` (defense in depth — AUTH-15)
    - layout.tsx passes `isAdmin={true}` to the Topbar (Phase 6 ADMIN badge displays)
    - The grid shell layout matches (authed)/layout.tsx exactly (sidebar + topbar + main + footer with the same CSS variables)
    - `npm run build` exits 0 (the dynamic segment route compiles cleanly)
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
  </acceptance_criteria>
  <done>Admin route tree gated by env-segment + role check; AUTH-14 + AUTH-15 satisfied; ADMIN badge visible in topbar.</done>
</task>

<task type="auto">
  <name>Task 2: Build src/components/InviteUrlModal.tsx (reusable URL display modal)</name>
  <files>src/components/InviteUrlModal.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-UI-SPEC.md "Admin URL Display Modal" (full layout + state inventory)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "src/components/InviteUrlModal.tsx" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/components/UserMenu.tsx (Plan 06-06 — same Escape + outside-click pattern)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-10 (URL displayed once, copy button, "won't be shown again" warning)
  </read_first>
  <action>
Create `src/components/InviteUrlModal.tsx`:

```
'use client';

import { useState, useEffect, useRef, useCallback } from 'react';
import { Copy, Check, X, AlertTriangle } from 'lucide-react';
import { toast } from 'sonner';
import { t, type Lang } from '@/lib/i18n';
import type { RedeemKind } from '@/lib/auth/redeem';

export interface InviteUrlModalProps {
  url: string;
  kind: RedeemKind;
  lang: Lang;
  /** Called when the modal closes (parent unmounts or sets state hidden). */
  onClose: () => void;
  /** Element to focus after the modal closes — typically the trigger button. */
  triggerRef?: React.RefObject<HTMLElement | null>;
}

/**
 * One-time URL display modal — used by Phase 9 admin partner-create form (and any
 * future admin-mediated reset trigger) to display the invitation/reset URL exactly
 * once with a copy button and "Ce lien ne sera plus affiché" warning (per D-10 /
 * 06-UI-SPEC.md §Admin URL Display Modal).
 *
 * Phase 6 ships the reusable primitive; Phase 9 wires the trigger.
 *
 * Accessibility floor (UI-SPEC §NON-NEGOTIABLE):
 *  - role="dialog" aria-modal="true" aria-labelledby
 *  - On open: focus moves to the X close button
 *  - On close: focus restores to triggerRef.current (when provided)
 *  - Escape closes
 *  - Trap focus inside (Tab cycles X → URL block → Copy → Close → X)
 *  - Backdrop click closes
 */
export function InviteUrlModal({ url, kind, lang, onClose, triggerRef }: InviteUrlModalProps) {
  const [copied, setCopied] = useState(false);
  const closeButtonRef = useRef<HTMLButtonElement>(null);
  const panelRef = useRef<HTMLDivElement>(null);

  const titleKey = kind === 'invite' ? 'auth.modal.invite.title' : 'auth.modal.reset.title';

  const handleClose = useCallback(() => {
    onClose();
    // Restore focus to the trigger after React unmounts the modal
    setTimeout(() => triggerRef?.current?.focus(), 0);
  }, [onClose, triggerRef]);

  const handleCopy = async () => {
    try {
      await navigator.clipboard.writeText(url);
      setCopied(true);
      toast.success(t('auth.toast.copy.success', lang));
      setTimeout(() => setCopied(false), 2000);
    } catch (e) {
      console.error('[InviteUrlModal] clipboard write failed:', e);
      toast.error(t('auth.error.generic', lang));
    }
  };

  // Focus management + key handling
  useEffect(() => {
    closeButtonRef.current?.focus();
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') {
        e.preventDefault();
        handleClose();
        return;
      }
      // Focus trap — keep Tab inside the panel
      if (e.key === 'Tab' && panelRef.current) {
        const focusable = panelRef.current.querySelectorAll<HTMLElement>(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );
        if (focusable.length === 0) return;
        const first = focusable[0]!;
        const last = focusable[focusable.length - 1]!;
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    };
    document.addEventListener('keydown', onKey);
    return () => document.removeEventListener('keydown', onKey);
  }, [handleClose]);

  return (
    <>
      {/* Backdrop — clicking closes */}
      <div
        aria-hidden="true"
        onClick={handleClose}
        style={{
          position: 'fixed', inset: 0,
          background: 'rgba(17,44,59,0.5)',
          backdropFilter: 'blur(4px)',
          zIndex: 200,
        }}
      />

      {/* Panel */}
      <div
        ref={panelRef}
        role="dialog"
        aria-modal="true"
        aria-labelledby="invite-modal-title"
        style={{
          position: 'fixed', top: '50%', left: '50%',
          transform: 'translate(-50%, -50%)',
          width: 'calc(100% - 32px)', maxWidth: 520, maxHeight: '80vh',
          background: 'var(--surface)', borderRadius: 16, padding: 28,
          boxShadow: '0 20px 60px rgba(17,44,59,0.25)',
          overflowY: 'auto', zIndex: 201,
          display: 'flex', flexDirection: 'column', gap: 16,
        }}
      >
        {/* Header row — title + X close button */}
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
          <h2 id="invite-modal-title" style={{ fontSize: '16.5px', fontWeight: 600, color: 'var(--ink)', margin: 0 }}>
            {t(titleKey, lang)}
          </h2>
          <button
            ref={closeButtonRef}
            type="button"
            onClick={handleClose}
            aria-label={t('auth.modal.button.close', lang)}
            style={{
              width: 32, height: 32, borderRadius: 8,
              background: 'transparent', border: 'none', cursor: 'pointer',
              color: 'var(--muted)',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}
            onMouseEnter={(e) => { (e.currentTarget as HTMLButtonElement).style.background = 'var(--hover-overlay)'; (e.currentTarget as HTMLButtonElement).style.color = 'var(--ink)'; }}
            onMouseLeave={(e) => { (e.currentTarget as HTMLButtonElement).style.background = 'transparent'; (e.currentTarget as HTMLButtonElement).style.color = 'var(--muted)'; }}
          >
            <X size={20} strokeWidth={1.6} />
          </button>
        </div>

        {/* Warning banner — gold tint, AlertTriangle icon */}
        <div style={{
          background: 'rgba(224,133,48,0.08)', borderLeft: '1px solid var(--gold)',
          borderRadius: 12, padding: '12px 16px',
          display: 'flex', alignItems: 'flex-start', gap: 12,
        }}>
          <AlertTriangle size={17} strokeWidth={1.6} style={{ color: 'var(--gold)', flexShrink: 0, marginTop: 2 }} aria-hidden="true" />
          <span style={{ fontSize: '14.5px', fontWeight: 500, color: 'var(--ink)' }}>
            {t('auth.modal.warning', lang)}
          </span>
        </div>

        {/* URL display block — monospace, user-select:all */}
        <div
          tabIndex={0}
          style={{
            background: 'var(--paper)', border: '1px solid var(--border)',
            borderRadius: 12, padding: '12px 16px',
            fontFamily: 'ui-monospace, SFMono-Regular, "SF Mono", Menlo, Monaco, monospace',
            fontSize: '12.5px', color: 'var(--ink)',
            wordBreak: 'break-all', userSelect: 'all',
          }}
        >
          {url}
        </div>

        {/* Action row — Copy + Close */}
        <div style={{ display: 'flex', gap: 12, justifyContent: 'flex-end' }}>
          <button
            type="button"
            onClick={handleCopy}
            className="btn-green"
            style={{
              display: 'flex', alignItems: 'center', gap: 8,
              borderRadius: 9999, padding: '0.6rem 1.5rem',
              fontWeight: 600, fontSize: 14, color: '#ffffff',
              background: 'var(--gd)', border: 'none', cursor: 'pointer',
            }}
          >
            {copied
              ? (<><Check size={17} strokeWidth={1.6} /> {t('auth.modal.button.copied', lang)}</>)
              : (<><Copy size={17} strokeWidth={1.6} /> {t('auth.modal.button.copy', lang)}</>)}
          </button>
          <button
            type="button"
            onClick={handleClose}
            className="btn-ghost"
            style={{
              borderRadius: 9999, padding: '0.6rem 1.5rem',
              fontWeight: 600, fontSize: 14, color: 'var(--ink)',
              background: 'transparent', border: 'none', cursor: 'pointer',
            }}
          >
            {t('auth.modal.button.close', lang)}
          </button>
        </div>

        {/* Footer note — 24h validity, single use */}
        <div style={{ fontSize: '10.5px', color: 'var(--muted)' }}>
          {t('auth.modal.footer.note', lang)}
        </div>
      </div>
    </>
  );
}
```

NOTE on portal: the backdrop + panel are siblings rendered inline; Next.js doesn't require ReactDOM.createPortal for this layout because z-index 200/201 already sits above the topbar (z 100). If a future caller mounts the modal inside a stacking-context-creating ancestor (e.g. position: relative with z-index), the portal pattern can be adopted then. For Phase 6 this works.

NOTE on `RedeemKind` import: Plan 06-05 created `src/lib/auth/redeem.ts` exporting `RedeemKind = 'invite' | 'reset'`. This file imports the type-only — fine because the redeem module is a `'use server'` module and importing only its type doesn't trigger server-action ambient bundling.

PHASE 9 INTEGRATION HINT (informational, not a Phase 6 task): Phase 9's admin partner-create form will be a Client Component that:
1. Renders an email + display_name + language select.
2. Submits via `useFormState(createInvitation, { url: null })` — the server action returns `{ url }`.
3. When `state.url` becomes non-null, mounts `<InviteUrlModal url={state.url} kind="invite" lang={lang} onClose={() => setState({url: null})} triggerRef={submitButtonRef} />`.
This integration is OUT OF SCOPE for Phase 6 — this plan only ships the InviteUrlModal primitive.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; head -1 src/components/InviteUrlModal.tsx | grep -c "'use client'" &amp;&amp; grep -c "navigator.clipboard.writeText" src/components/InviteUrlModal.tsx &amp;&amp; grep -c 'role="dialog"' src/components/InviteUrlModal.tsx &amp;&amp; grep -c 'aria-modal="true"' src/components/InviteUrlModal.tsx &amp;&amp; grep -c 'aria-labelledby="invite-modal-title"' src/components/InviteUrlModal.tsx &amp;&amp; grep -c "Escape" src/components/InviteUrlModal.tsx &amp;&amp; grep -c "auth.modal.warning" src/components/InviteUrlModal.tsx &amp;&amp; grep -c "auth.modal.footer.note" src/components/InviteUrlModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` directive
    - Component renders a backdrop (fixed inset-0, rgba navy 50%, backdrop-filter blur 4px) + a panel (centered, max-width 520, --surface bg, border-radius 16, padding 28)
    - Panel has `role="dialog"`, `aria-modal="true"`, `aria-labelledby="invite-modal-title"`
    - On mount, focus moves to the X close button
    - Escape key closes the modal (and restores focus to triggerRef.current if provided)
    - Backdrop click closes
    - Tab key cycles focus inside the panel (focus trap)
    - Copy button calls `navigator.clipboard.writeText(url)`, switches to "Lien copié" + Check icon for 2s, fires Sonner success toast
    - Renders the warning banner (gold tint + AlertTriangle + warning copy from `auth.modal.warning`)
    - Renders the URL in a monospace block with `user-select: all`
    - Renders the footer note from `auth.modal.footer.note`
    - All visible strings via `t()` (no hardcoded text)
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
  </acceptance_criteria>
  <done>InviteUrlModal primitive shipped with full UI-SPEC compliance + accessibility floor; ready for Phase 9 admin UI integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /[adminSegment]/* | Untrusted segment value. Two-layer defense: env-segment match + role check. |
| Layout → page handlers | Layout's role check does NOT cascade automatically; every page+handler must independently call requireAdmin (AUTH-15). |
| Browser → InviteUrlModal | The plaintext URL exists in the rendered HTML; visible to anyone with screen access while the modal is open. |
| Clipboard → external paste target | After copy, the plaintext URL leaves the app's control. Out-of-band sharing is the threat model assumption (admin shares via secure channel — D-10 warning). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | wrong segment guess returns 403/redirect (would confirm admin URL exists) | mitigate | layout.tsx calls notFound() not redirect/403 (D-18) |
| T-06-07-02 | Elevation of Privilege | partner reaches admin page if requireAdmin not called per-page | mitigate | layout.tsx + page.tsx each call requireAdmin independently (AUTH-15 / PITFALLS §7.1 defense in depth) |
| T-06-07-03 | Information Disclosure | ADMIN_URL_SEGMENT in error message / log / browser referrer | mitigate | layout never logs the env value; on mismatch only `notFound()` is called (which renders the not-found page; the segment value never appears in the response body). Phase 9's admin pages must apply the same Referrer-Policy discipline. |
| T-06-07-04 | Tampering | env var unset in deploy → all admin URLs 404 | accept | Plan 06-01's .env.example documents the var as required; CI doesn't validate env vars at build time (Phase 5 lock — no env-var smoke test in build). Operationally, missing env = 404 is the safe failure mode (URL-obscurity AND role-check both fail; no admin reach). |
| T-06-07-05 | Information Disclosure | InviteUrlModal URL visible to shoulder-surfer | accept | The warning banner explicitly tells admin to "share through a secure channel"; UI-SPEC says the URL is `user-select: all` (no obfuscation by design — admin needs to copy it). Out of scope to obscure further. |
| T-06-07-06 | Tampering | URL leaks via browser history because the URL was rendered in DOM | accept | The URL is a token-bearing path; once expired (24h) or used (single-use), the leak is harmless. Pre-expiry the threat is the same as token-in-clipboard. |
| T-06-07-07 | Information Disclosure | clipboard contents persist after admin's session ends | accept | OS-level concern. The 24h TTL bounds the damage window. Documented in CUT-08 runbook: "after creating an invitation, copy the URL, paste to your secure channel, then clear your clipboard." |
| T-06-07-08 | DoS | clipboard.writeText API rejected (browser policy / no clipboard access) | mitigate | try/catch wraps the writeText; on failure shows `auth.error.generic` toast; URL remains visible in the DOM for manual copy. |
</threat_model>

<verification>
At plan completion the following must all be true:

1. `cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run build` exits 0 (admin route group compiles cleanly)
2. `ls "app/(admin)/[adminSegment]/layout.tsx" "app/(admin)/[adminSegment]/page.tsx" src/components/InviteUrlModal.tsx` succeeds
3. `grep -c "process.env.ADMIN_URL_SEGMENT" "app/(admin)/[adminSegment]/layout.tsx"` = 1
4. `grep -c "notFound()" "app/(admin)/[adminSegment]/layout.tsx"` ≥ 1
5. `grep -c "await requireAdmin()" "app/(admin)/[adminSegment]/layout.tsx"` = 1 AND `grep -c "await requireAdmin()" "app/(admin)/[adminSegment]/page.tsx"` = 1 (defense in depth)
6. `head -1 src/components/InviteUrlModal.tsx | grep -c "'use client'"` returns 1
7. `grep -c 'aria-modal="true"' src/components/InviteUrlModal.tsx` = 1
8. `grep -c 'navigator.clipboard.writeText' src/components/InviteUrlModal.tsx` ≥ 1
9. `npm run typecheck && npm run lint:check` both exit 0
</verification>

<success_criteria>
- Admin route group built with two-layer defense: env-segment URL obscurity + role check, both failing closed via notFound() per D-18
- AUTH-14 satisfied: `process.env.ADMIN_URL_SEGMENT` is the single source of truth; mismatch returns 404
- AUTH-15 satisfied: layout AND page both independently call requireAdmin
- AUTH-07 / AUTH-08 / AUTH-10 (admin invite/reset URL surface) — the UI primitive (InviteUrlModal) is ready for Phase 9 to wire to the admin partner-create form; the underlying server actions (createInvitation, createPasswordReset) shipped in Plan 06-04
- ADMIN badge visible in topbar via Topbar's `isAdmin` prop (Plan 06-06's Topbar accepts the prop; this plan's layout passes `true`)
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-shell/06-07-SUMMARY.md` covering: any deviations from the planned focus-trap implementation (e.g. if the focusable-element selector misses any element), and a smoke-test note confirming the modal renders correctly when invoked from a quick-and-dirty test page (or, if no test page is wired, a note that Phase 9 is responsible for the integration smoke test).
</output>
