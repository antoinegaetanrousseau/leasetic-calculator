---
phase: 06-auth-shell
plan: 08
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - app/error.tsx
  - app/not-found.tsx
autonomous: true
requirements:
  - SHELL-12
  - SHELL-13

must_haves:
  truths:
    - "An unhandled exception in any (authed) or (admin) page renders app/error.tsx — a centered card with AlertTriangle icon, generic title 'Une erreur s'est produite. / Something went wrong.' (D-30: NO stack trace, NO error message, NO digest), and a 'Réessayer / Try again' button that calls the Next.js reset prop"
    - "Visiting an unmatched route renders app/not-found.tsx — a centered card with the 404 display number, 'Page introuvable / Page not found' heading, body copy, and 'Retour à l'accueil / Back to home' button linking to /"
    - "app/not-found.tsx is also the page rendered by every notFound() call from within layouts (Plan 06-07's admin segment mismatch + role check fall through to this page)"
    - "app/error.tsx is a 'use client' Component (Next.js convention); app/not-found.tsx is a Server Component that uses t() + getCurrentLang()"
    - "Both pages reuse the (public)/layout.tsx visual contract — centered, top-right LocaleToggle + ThemeToggle, --paper background, Leasétic logo above the card"
  artifacts:
    - path: "app/error.tsx"
      provides: "Generic error boundary for the entire app — caught at the root or nearest segment boundary"
      min_lines: 30
    - path: "app/not-found.tsx"
      provides: "404 page in FR + EN, used by Next.js for unmatched routes AND by every notFound() call site"
      min_lines: 25
  key_links:
    - from: "app/error.tsx"
      to: "Next.js reset prop"
      via: "Try-again button onClick={() => reset()}"
      pattern: "onClick=\\{.*reset"
    - from: "app/not-found.tsx"
      to: "src/lib/i18n/index.ts (getCurrentLang + t)"
      via: "Server Component reads cookie-based lang and translates every visible string"
      pattern: "getCurrentLang"
---

<objective>
Build the Phase 6 error boundary and 404 page per SHELL-12 + SHELL-13 + 06-UI-SPEC.md, with the layout chrome reused from the (public)/layout pattern (centered card on --paper, top-right toggles, Leasétic logo above).

Two concerns:
1. `app/error.tsx` — Next.js App Router convention. MUST be a Client Component (`'use client'`). Cannot use server-side `t()` with cookie-based lang because cookies are server-only — instead the bilingual fallback strings are read from the cookie directly via `document.cookie` parse on mount, OR (simpler) shows BOTH FR + EN inline (concatenated) per the UI-SPEC's bilingual error pattern. The actual approach used by 06-RESEARCH.md §16 is bilingual side-by-side. We follow that to avoid client-side cookie parsing complexity.
2. `app/not-found.tsx` — Server Component. Uses `getCurrentLang()` + `t()` to render the localized 404 page in the user's chosen language.

Both files inherit the visual chrome from the (public) layout pattern but are MOUNTED OUTSIDE that layout (Next.js conventions place error.tsx and not-found.tsx at the app root). Each page therefore re-implements the centered-card chrome inline.

Output: 2 files at the app/ root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-shell/06-CONTEXT.md
@.planning/phases/06-auth-shell/06-RESEARCH.md
@.planning/phases/06-auth-shell/06-UI-SPEC.md
@.planning/phases/06-auth-shell/06-PATTERNS.md
@.planning/phases/06-auth-shell/06-02-SUMMARY.md
@src/lib/i18n/index.ts
@src/lib/i18n/dictionaries.ts
@src/components/ThemeToggle.tsx
@src/components/LocaleToggle.tsx

<interfaces>
- 06-02 i18n keys (must already exist after Plan 06-02): `error.boundary.title`, `error.boundary.body`, `error.boundary.retry`, `error.404.display`, `error.404.title`, `error.404.body`, `error.404.button.home`
- Phase 5's `app/layout.tsx`: provides `<html>`, `<body>`, no-flash script, font CSS variable, Sonner Toaster. Both error.tsx and not-found.tsx render INSIDE that root layout — they don't include their own html/body.
- Phase 5's `getCurrentLang()` (server-only via cookies): not callable from a Client Component. error.tsx therefore uses bilingual side-by-side strings (per RESEARCH §16's accepted pattern + UI-SPEC §Error Boundary).

UI-SPEC contracts (06-UI-SPEC.md "Error Boundary Page" + "404 Page"):
- Both pages: --paper background, centered min-height: 100vh flex column, top-right LocaleToggle + ThemeToggle (absolute top: 24, right: 24), Leasétic logo (--navy, weight 700, 22px) above the card
- Error card: max-width 420, --surface bg, border-radius 16, padding 28, shadow var(--shadow-card)
- Error icon: Lucide AlertTriangle, size 38, strokeWidth 1.3, color --gold, opacity 0.6
- Error heading: 16.5px weight 600 --ink
- Error body: 14.5px --muted
- Error retry button: .btn-green, full-width
- 404 display number: 48px (text-5xl), weight 700, --navy, line-height 1.1
- 404 heading: 16.5px weight 600 --ink
- 404 body: 14.5px --muted
- 404 home button: .btn-green
- ESLint exemption (Plan 06-02 added): error.tsx is in the ignore list because it must work with bilingual side-by-side hardcoded strings (no server-side t() available). The not-found.tsx file is NOT exempted — it uses t().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build app/error.tsx (Client Component error boundary)</name>
  <files>app/error.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-UI-SPEC.md "Error Boundary Page" (full layout diagram + copy table)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §16 "Error Boundary and 404 Page"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "app/error.tsx" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-30 (no stack trace, no digest, no error message; localized FR/EN; retry button)
  </read_first>
  <action>
Create `app/error.tsx`. Per 06-RESEARCH.md §16 + Next.js App Router convention, this MUST be a Client Component, MUST accept `{error, reset}` props, MUST log the error server-side via console.error in a useEffect (not during render — would cause SSR issues).

Per D-30: NO stack trace, NO error.message, NO error.digest visible to the user. Per UI-SPEC: bilingual side-by-side strings (since server-side t() isn't available in a Client Component without a Locale context). Read the cookie client-side ONLY to pick FR or EN; if reading the cookie is unreliable, default to FR (the project's primary language).

```
'use client';

import { useEffect, useState } from 'react';
import { AlertTriangle } from 'lucide-react';
import { ThemeToggle } from '@/components/ThemeToggle';
import { LocaleToggle } from '@/components/LocaleToggle';

interface ErrorPageProps {
  error: Error & { digest?: string };
  reset: () => void;
}

/** Read the lt_lang cookie client-side. Returns 'fr' or 'en'; defaults to 'fr'. */
function readLangCookie(): 'fr' | 'en' {
  if (typeof document === 'undefined') return 'fr';
  const m = document.cookie.match(/(?:^|;\s*)lt_lang=([^;]+)/);
  return m && m[1] === 'en' ? 'en' : 'fr';
}

/** Read the lt_theme cookie client-side. Returns 'light' | 'dark' | 'system'; defaults to 'system'. */
function readThemeCookie(): 'light' | 'dark' | 'system' {
  if (typeof document === 'undefined') return 'system';
  const m = document.cookie.match(/(?:^|;\s*)lt_theme=([^;]+)/);
  if (m && (m[1] === 'light' || m[1] === 'dark' || m[1] === 'system')) return m[1];
  return 'system';
}

const STR = {
  fr: {
    title: "Une erreur s'est produite.",
    body: 'Si le problème persiste, contactez Leasétic.',
    retry: 'Réessayer',
  },
  en: {
    title: 'Something went wrong.',
    body: 'If the problem persists, contact Leasétic.',
    retry: 'Try again',
  },
} as const;

/**
 * Generic error boundary (SHELL-12 / D-30).
 *
 * Per Next.js App Router convention: must be a Client Component. The `error`
 * prop carries the runtime exception; the `reset` prop re-renders the segment.
 *
 * D-30: NO stack trace, NO error.message, NO error.digest displayed. Server
 * logs the actual error via console.error for forensics; the user sees only
 * the generic copy.
 *
 * Bilingual side-by-side strings: a Client Component cannot read cookies via
 * the server-only getCurrentLang(). We instead read the lt_lang cookie via
 * document.cookie on mount; until that resolves we render the FR strings
 * (project's primary language).
 */
export default function ErrorPage({ error, reset }: ErrorPageProps) {
  const [lang, setLang] = useState<'fr' | 'en'>('fr');
  const [theme, setTheme] = useState<'light' | 'dark' | 'system'>('system');

  useEffect(() => {
    setLang(readLangCookie());
    setTheme(readThemeCookie());
    // Server-side forensics — bounded redaction (Phase 5 classifyError discipline
    // applies but we don't classify here; we just log for the operator).
    // The user-visible copy is generic; the server log is detailed.
    // eslint-disable-next-line no-console
    console.error('[error.tsx]', error.message, error.digest ?? '');
  }, [error]);

  const s = STR[lang];

  return (
    <div style={{
      minHeight: '100vh', display: 'flex', flexDirection: 'column',
      justifyContent: 'center', alignItems: 'center',
      background: 'var(--paper)', padding: '24px 16px', position: 'relative',
    }}>
      <div style={{ position: 'absolute', top: 24, right: 24, display: 'flex', gap: 12, zIndex: 10 }}>
        <LocaleToggle current={lang} />
        <ThemeToggle current={theme} />
      </div>

      <div style={{ fontWeight: 700, color: 'var(--navy)', fontSize: 22, marginBottom: 16 }}>Leasétic</div>

      <div style={{
        width: '100%', maxWidth: 420, padding: 28,
        background: 'var(--surface)', borderRadius: 16,
        boxShadow: 'var(--shadow-card)',
        textAlign: 'center',
      }}>
        <AlertTriangle size={38} strokeWidth={1.3} style={{ color: 'var(--gold)', opacity: 0.6, marginBottom: 16 }} aria-hidden="true" />
        <h1 style={{ fontSize: '16.5px', fontWeight: 600, color: 'var(--ink)', marginBottom: 8 }}>{s.title}</h1>
        <p style={{ fontSize: '14.5px', color: 'var(--muted)', marginBottom: 24 }}>{s.body}</p>
        <button
          type="button"
          onClick={() => reset()}
          className="btn-green"
          style={{
            width: '100%', borderRadius: 9999, padding: '0.6rem 1.5rem',
            fontWeight: 600, fontSize: 14, color: '#ffffff',
            background: 'var(--gd)', border: 'none', cursor: 'pointer',
          }}
        >
          {s.retry}
        </button>
      </div>
    </div>
  );
}
```

NOTE on the ESLint hardcoded-JSX rule: Plan 06-02 added `app/error.tsx` to the rule's `ignores` list precisely because this file uses bilingual side-by-side hardcoded strings (D-30 + RESEARCH §16). The `STR` object is plain TS (not JSX) so the JSXText selector wouldn't match it anyway, but the explicit ignore documents the intent.

NOTE on `console.error`: a `// eslint-disable-next-line no-console` comment is included because the project may add a no-console rule in v1.2 — for now the comment is a no-op but signals intent.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; head -1 app/error.tsx | grep -c "'use client'" &amp;&amp; ! grep -q 'error.message' app/error.tsx | grep -v console &amp;&amp; ! grep -q 'error.digest' app/error.tsx | grep -v console &amp;&amp; grep -c "reset()" app/error.tsx &amp;&amp; grep -c 'AlertTriangle' app/error.tsx &amp;&amp; grep -c 'STR.fr' app/error.tsx &amp;&amp; grep -c 'STR.en' app/error.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` directive
    - Component accepts `{error, reset}` props with the standard Next.js error boundary types
    - On mount, console.error logs `error.message` and `error.digest` (server forensics) — but the rendered output never shows these values to the user (D-30)
    - Renders the bilingual STR object based on the `lt_lang` cookie (defaults to FR)
    - The retry button calls `reset()` (the Next.js-provided segment reset)
    - Layout follows the UI-SPEC: centered, --paper bg, top-right LocaleToggle + ThemeToggle, Leasétic logo above the card, AlertTriangle icon (38px, --gold, opacity 0.6), .btn-green retry button
    - `npm run build` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0 (the ignore list from Plan 06-02 covers this file)
  </acceptance_criteria>
  <done>app/error.tsx live with D-30 redaction, bilingual fallback, and UI-SPEC visual contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build app/not-found.tsx (Server Component 404)</name>
  <files>app/not-found.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-UI-SPEC.md "404 Page" (full layout diagram + copy)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §16 "404 page"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "app/not-found.tsx" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/i18n/index.ts (getCurrentLang, t)
  </read_first>
  <action>
Create `app/not-found.tsx`. UNLIKE error.tsx, this is a Server Component — it can use `getCurrentLang()` + `t()` because the page is rendered server-side (cookies available).

```
import Link from 'next/link';
import { ThemeToggle } from '@/components/ThemeToggle';
import { LocaleToggle } from '@/components/LocaleToggle';
import { getCurrentLang, getCurrentTheme, t } from '@/lib/i18n';

export const dynamic = 'force-dynamic';

/**
 * 404 page (SHELL-13 / D-31).
 *
 * Used by Next.js for any unmatched route AND by every server-side notFound()
 * call site (Plan 06-04's requireAdmin role-mismatch, Plan 06-07's admin segment
 * mismatch + role check). Per D-18, both paths land here — preserves URL secrecy
 * via 404 rather than 403.
 *
 * Server Component: getCurrentLang reads the lt_lang cookie server-side and
 * t() returns the localized string. Unlike error.tsx, no client-side cookie
 * parsing needed.
 */
export default async function NotFoundPage() {
  const lang = await getCurrentLang();
  const theme = await getCurrentTheme();

  return (
    <div style={{
      minHeight: '100vh', display: 'flex', flexDirection: 'column',
      justifyContent: 'center', alignItems: 'center',
      background: 'var(--paper)', padding: '24px 16px', position: 'relative',
    }}>
      <div style={{ position: 'absolute', top: 24, right: 24, display: 'flex', gap: 12, zIndex: 10 }}>
        <LocaleToggle current={lang} />
        <ThemeToggle current={theme} />
      </div>

      <div style={{ fontWeight: 700, color: 'var(--navy)', fontSize: 22, marginBottom: 16 }}>Leasétic</div>

      <div style={{
        width: '100%', maxWidth: 420, padding: 28,
        background: 'var(--surface)', borderRadius: 16,
        boxShadow: 'var(--shadow-card)',
        textAlign: 'center',
      }}>
        <span style={{
          display: 'block',
          fontSize: 48, fontWeight: 700, color: 'var(--navy)',
          lineHeight: 1.1, marginBottom: 16,
        }} aria-hidden="true">
          {t('error.404.display', lang)}
        </span>
        <h1 style={{ fontSize: '16.5px', fontWeight: 600, color: 'var(--ink)', marginBottom: 8 }}>
          {t('error.404.title', lang)}
        </h1>
        <p style={{ fontSize: '14.5px', color: 'var(--muted)', marginBottom: 24 }}>
          {t('error.404.body', lang)}
        </p>
        <Link
          href="/"
          className="btn-green"
          style={{
            display: 'inline-block', borderRadius: 9999,
            padding: '0.6rem 1.5rem', fontWeight: 600, fontSize: 14,
            color: '#ffffff', background: 'var(--gd)',
            border: 'none', textDecoration: 'none', cursor: 'pointer',
          }}
        >
          {t('error.404.button.home', lang)}
        </Link>
      </div>
    </div>
  );
}
```

NOTE on Link to `/`: when an unauthenticated user hits the 404 page (e.g. a bad URL), the home link sends them to `/` which the proxy.ts redirects to `/login?next=/` (preserving intent). When an authenticated user hits the 404, they go straight to `/`. Both flows land somewhere meaningful.

NOTE on dynamic = 'force-dynamic': even though not-found.tsx might be statically renderable in some Next.js setups, reading cookies via getCurrentLang requires the dynamic opt-out per PITFALLS §1.6.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ls app/not-found.tsx &amp;&amp; ! head -1 app/not-found.tsx | grep -q "'use client'" &amp;&amp; grep -c "getCurrentLang" app/not-found.tsx &amp;&amp; grep -c "t('error.404.title'" app/not-found.tsx &amp;&amp; grep -c "t('error.404.body'" app/not-found.tsx &amp;&amp; grep -c "t('error.404.button.home'" app/not-found.tsx &amp;&amp; grep -c "t('error.404.display'" app/not-found.tsx &amp;&amp; grep -c "force-dynamic" app/not-found.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT start with `'use client'` (Server Component)
    - File contains `export const dynamic = 'force-dynamic'`
    - File uses `getCurrentLang()` + `getCurrentTheme()` from `@/lib/i18n` (server-side cookie reads)
    - All visible strings via `t()` — `error.404.display`, `error.404.title`, `error.404.body`, `error.404.button.home`
    - The 404 display number renders at fontSize: 48 weight: 700 color: var(--navy)
    - The home link uses Next.js `<Link href="/">` with `.btn-green` styling
    - `npm run build` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0 (no hardcoded JSX literals — every string goes through t())
  </acceptance_criteria>
  <done>app/not-found.tsx live as a Server Component using server-side i18n; SHELL-13 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime exception → error.tsx | Untrusted error payload (could contain DB schema details, sensitive paths, stack info). |
| Server forensics console.error → host log aggregator | Vercel logs are operator-readable; not user-visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Information Disclosure | stack trace / error.message visible to end user | mitigate | error.tsx renders ONLY the bilingual generic STR copy (D-30). The error object is logged to server console (Vercel logs) but never injected into the rendered HTML. |
| T-06-08-02 | Information Disclosure | error.digest leaks via window.__NEXT_DATA__ to the client bundle | accept | Next.js's built-in error boundary serializes a digest hash for cross-server-client correlation; the digest itself is opaque. v1.1 doesn't display it; v1.2+ may add a "Reference: <digest>" line for support workflows. |
| T-06-08-03 | Spoofing | not-found.tsx accidentally rendered for an authenticated route the user CAN access | accept | Phase 9 must call requireAdmin BEFORE notFound for explicit role checks (covered by AUTH-15 discipline). The 404 page itself doesn't leak whether an admin URL exists. |
| T-06-08-04 | Information Disclosure | URL segment in the 404 page exposes the env value (because Next.js might pass the raw URL into rendering) | mitigate | not-found.tsx does NOT read params or pathname; renders only static localized copy. The home link is hardcoded to /. |
| T-06-08-05 | Tampering | reset() called in a malicious loop (e.g. by error-trace tooling) causing infinite re-renders | accept | Standard Next.js error boundary semantics; out of scope to defend against pathological dev tools. |
</threat_model>

<verification>
At plan completion the following must all be true:

1. `cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run build` exits 0 (Next.js wires error.tsx + not-found.tsx into the build)
2. `head -1 app/error.tsx | grep -c "'use client'"` returns 1
3. `! head -1 app/not-found.tsx | grep -q "'use client'"` (server component)
4. `grep -c "error.message" app/error.tsx` ≤ 1 (only inside console.error, not in JSX) — verified via narrower `! grep -E '<[^>]*error.message' app/error.tsx` (no JSX usage)
5. `grep -c "getCurrentLang" app/not-found.tsx` = 1
6. `grep -cE "t\\('error\\.404\\.(display|title|body|button.home)'" app/not-found.tsx` = 4 (each of the 4 keys used exactly once)
7. `npm run typecheck && npm run lint:check` both exit 0
</verification>

<success_criteria>
- SHELL-12 satisfied: any unhandled exception renders the generic error boundary with retry; no error details leak to the user (D-30)
- SHELL-13 satisfied: 404 page localized FR + EN, used by both unmatched routes and notFound() call sites
- The 404 page is the unified destination for: (a) wrong admin segment (D-18 → notFound), (b) non-admin reaching admin layout (requireAdmin → notFound), (c) random unmatched URL (Next.js → not-found.tsx). All three paths render the same UI.
- Plan 06-09 can rely on these pages already existing for the CLI smoke test (e.g. visiting an admin URL with a wrong segment lands on the bilingual 404)
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-shell/06-08-SUMMARY.md` covering: the bilingual side-by-side fallback approach for error.tsx (and a note about whether a future phase should refactor to a Locale context provider so error.tsx can use t()), and any layout adjustments needed to keep the centered card visually consistent across screen sizes.
</output>
