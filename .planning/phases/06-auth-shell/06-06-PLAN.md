---
phase: 06-auth-shell
plan: 06
type: execute
wave: 4
depends_on:
  - 06-04
  - 06-02
files_modified:
  - app/(authed)/layout.tsx
  - app/(authed)/page.tsx
  - app/page.tsx
  - src/components/Topbar.tsx
  - src/components/UserMenu.tsx
  - src/lib/theme/actions.ts
  - src/lib/i18n/actions.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-06
  - SHELL-01
  - SHELL-02
  - SHELL-04
  - SHELL-07
  - SHELL-08
  - SHELL-10
  - SHELL-14

must_haves:
  truths:
    - "An authenticated user visiting / sees the (authed) shell: sidebar with Leasétic wordmark, topbar with page-title slot + LocaleToggle + ThemeToggle + UserMenu, main content area, footer"
    - "The UserMenu shows the user's display_name + email; clicking the trigger opens the dropdown with a single Logout item; clicking Logout calls authClient.signOut and redirects to /login?logged_out=1"
    - "The Topbar receives an isAdmin boolean prop; when true, renders an ADMIN pill badge next to the page title"
    - "Toggling theme while authenticated writes BOTH the lt_theme cookie AND users.theme via DB update"
    - "Toggling locale while authenticated writes BOTH the lt_lang cookie AND users.language via DB update"
    - "Toggling theme/locale while NOT authenticated still writes the cookie (Phase 5 behavior preserved)"
    - "The legacy app/page.tsx is replaced by app/(authed)/page.tsx — visiting / returns the (authed) shell with a placeholder home content using the existing welcomeHeading + welcomeSubtext keys (Phase 7 fills this in)"
    - "Initial paint shows the correct theme without flash (SHELL-08; Phase 5 no-flash script preserved unchanged)"
  artifacts:
    - path: "app/(authed)/layout.tsx"
      provides: "requireUser-guarded shell layout with grid, sidebar, Topbar, footer"
      min_lines: 30
    - path: "app/(authed)/page.tsx"
      provides: "Authenticated home placeholder (Phase 7 fills this with proposal CTA)"
      min_lines: 10
    - path: "app/page.tsx"
      provides: "Old root home page DELETED — replaced by app/(authed)/page.tsx"
      min_lines: 0
    - path: "src/components/Topbar.tsx"
      provides: "Server Component composing Topbar with isAdmin badge slot"
      contains: "ADMIN"
    - path: "src/components/UserMenu.tsx"
      provides: "Client Component dropdown with display name + email + logout"
      contains: "authClient.signOut"
    - path: "src/lib/theme/actions.ts"
      provides: "EXTENDED setTheme — cookie + DB write when authenticated"
      contains: "users.theme"
    - path: "src/lib/i18n/actions.ts"
      provides: "EXTENDED setLang — cookie + DB write when authenticated"
      contains: "users.language"
  key_links:
    - from: "app/(authed)/layout.tsx"
      to: "src/lib/auth/require.ts (requireUser)"
      via: "first await — every (authed) page is gated"
      pattern: "await requireUser\\(\\)"
    - from: "src/components/UserMenu.tsx"
      to: "src/lib/auth/client.ts (authClient.signOut)"
      via: "logout button onClick → signOut + router.push('/login?logged_out=1')"
      pattern: "authClient\\.signOut"
    - from: "src/lib/theme/actions.ts"
      to: "src/db/schema.ts (users.theme)"
      via: "after cookie set, if session: db().update(users).set({theme})"
      pattern: "schema\\.users\\)\\s*\\.set\\(\\{\\s*theme"
---

<objective>
Build the authenticated app shell that wraps every (authed) route, plus extend the Phase 5 theme + locale server actions to persist to the DB when the user is authenticated. Replace the legacy `app/page.tsx` with a (authed)-grouped home page.

Five concerns:
1. `app/(authed)/layout.tsx` — calls `requireUser()` first, then renders the Phase 5 grid shell (sidebar + topbar + main + footer) with the Topbar composed from this plan's components.
2. `app/(authed)/page.tsx` — Phase 7 will fill in proposals + form; for now a minimal placeholder using existing dictionary keys (Phase 5 backward-compat: `welcomeHeading`, `welcomeSubtext`).
3. `src/components/Topbar.tsx` (Server Component) + `src/components/UserMenu.tsx` (Client Component dropdown) — compose the topbar with reusable Phase 5 LocaleToggle + ThemeToggle plus the new user menu and ADMIN badge slot.
4. Extend `src/lib/theme/actions.ts` and `src/lib/i18n/actions.ts` to additionally write to `users.theme` / `users.language` when authenticated (D-27).
5. Delete `app/page.tsx` — the (authed) route group's `page.tsx` now serves `/`.

Purpose: This is the visible Phase 6 shell. After this plan an authenticated partner can log in, land on `/`, see their name in the topbar, toggle theme/locale (persistent across logout/login), and click logout to return to /login.

Output: 7 file changes (5 created, 1 extended × 2, 1 deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-shell/06-CONTEXT.md
@.planning/phases/06-auth-shell/06-RESEARCH.md
@.planning/phases/06-auth-shell/06-UI-SPEC.md
@.planning/phases/06-auth-shell/06-PATTERNS.md
@.planning/phases/06-auth-shell/06-04-SUMMARY.md
@.planning/phases/06-auth-shell/06-02-SUMMARY.md
@app/page.tsx
@app/layout.tsx
@src/lib/theme/actions.ts
@src/lib/i18n/actions.ts
@src/components/ThemeToggle.tsx
@src/components/LocaleToggle.tsx

<interfaces>
- 06-04's require.ts: `requireUser()` returns `{ session, role: 'partner' | 'admin' }`. session.user has `id`, `email`, `name`, plus additionalFields `displayName`, `language`, `theme`, `role`.
- 06-04's actions: not directly used here; 06-07 owns admin actions.
- 06-03's auth/client.ts: `authClient.signOut()` returns Promise; default callbackURL is `/login`.
- 06-02's i18n: dictionary contains `shell.user.menu.aria`, `shell.user.menu.logout`, `shell.topbar.admin.badge`, `shell.locale.toggle.aria`, `shell.theme.toggle.aria`, `shell.footer.copyright`.
- Phase 5's `app/layout.tsx`: provides `<html>`, `<body>`, no-flash script, font CSS variable, Sonner Toaster. Do NOT modify.
- Phase 5's `src/lib/theme/actions.ts`: cookie write, ALLOWED_THEMES guard, revalidatePath. Pattern to extend.
- Phase 5's `src/lib/i18n/actions.ts`: identical shape for `lt_lang` cookie. Pattern to extend.

UI-SPEC contracts:
- Topbar: --topbar-h (64px) height, --surface bg, border-bottom 1px var(--border), sticky top z=100, padding 0 24px, gap 12px
- Sidebar (Phase 6): Leasétic wordmark only, weight 700, 22px, --navy, padding 1.5rem 1rem
- Footer: --footer-h (48px), `t('shell.footer.copyright', lang)` centered 10.5px --muted
- ADMIN pill badge: 9px uppercase, padding 2px 8px, --navy bg, white text, border-radius 9999px, mounted next to page title when isAdmin
- UserMenu trigger: pill button, padding 6px 12px, hover bg --hover-overlay, contains avatar circle (28px) + display name + ChevronDown
- UserMenu dropdown panel: position absolute top calc(100% + 4px) right 0, width 240, --surface bg, --border, border-radius 12px, padding 8px, shadow var(--shadow-card)
- Dropdown header: display name 14.5px weight 600 + email 11.2px --muted, padding 12px 16px, border-bottom
- Logout item: padding 10px 16px, border-radius 8px, hover bg --hover-overlay, LogOut icon 17px --muted

Phase 7 forward-compat: Topbar accepts an optional `pageTitle` prop so future pages can pass their own; default is `t('header.home', lang)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Topbar (Server Component) + UserMenu (Client Component)</name>
  <files>src/components/Topbar.tsx, src/components/UserMenu.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-UI-SPEC.md "Topbar Component Specification" + "UserMenu Component Specification"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "src/components/Topbar.tsx" + "src/components/UserMenu.tsx"
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/components/ThemeToggle.tsx (Phase 5 client component shape)
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/components/LocaleToggle.tsx
  </read_first>
  <action>
Per D-25 — the Topbar order is fixed: Leasétic logo (in sidebar, not topbar) · page-title slot · spacer · LocaleToggle · ThemeToggle · UserMenu (display-name + caret + dropdown with logout). The order below matches D-25 verbatim. The ADMIN badge sits next to the page-title slot when isAdmin (per UI-SPEC §Topbar Component Specification).

Create `src/components/Topbar.tsx` (Server Component):

```
import { LocaleToggle } from './LocaleToggle';
import { ThemeToggle } from './ThemeToggle';
import { UserMenu } from './UserMenu';
import { t, type Lang } from '@/lib/i18n';

export interface TopbarProps {
  displayName: string;
  email: string;
  lang: Lang;
  theme: 'light' | 'dark' | 'system';
  isAdmin?: boolean;
  pageTitle?: string;
}

export function Topbar({ displayName, email, lang, theme, isAdmin = false, pageTitle }: TopbarProps) {
  const title = pageTitle ?? t('header.home', lang);

  return (
    <header style={{
      gridRow: '1', gridColumn: '2',
      background: 'var(--surface)',
      borderBottom: '1px solid var(--border)',
      height: 'var(--topbar-h)',
      display: 'flex', alignItems: 'center', gap: 12,
      padding: '0 24px',
      position: 'sticky', top: 0, zIndex: 100,
    }}>
      <span style={{ fontSize: '16.5px', fontWeight: 600, color: 'var(--ink)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap', maxWidth: '60%' }}>
        {title}
      </span>
      {isAdmin && (
        <span style={{
          fontSize: '9px', fontWeight: 700, letterSpacing: '0.06em',
          padding: '2px 8px', borderRadius: 9999,
          background: 'var(--navy)', color: '#ffffff',
        }} aria-label={t('shell.topbar.admin.badge', lang)}>
          {t('shell.topbar.admin.badge', lang)}
        </span>
      )}
      <div style={{ flex: 1 }} />
      <LocaleToggle current={lang} />
      <ThemeToggle current={theme} />
      <UserMenu displayName={displayName} email={email} lang={lang} />
    </header>
  );
}
```

Create `src/components/UserMenu.tsx` (Client Component):

```
'use client';

import { useState, useEffect, useRef } from 'react';
import { useRouter } from 'next/navigation';
import { LogOut, ChevronDown } from 'lucide-react';
import { authClient } from '@/lib/auth/client';
import { t, type Lang } from '@/lib/i18n';

interface UserMenuProps {
  displayName: string;
  email: string;
  lang: Lang;
}

function initials(name: string): string {
  const trimmed = name.trim();
  if (!trimmed) return '?';
  const parts = trimmed.split(/\s+/);
  if (parts.length === 1) return parts[0]!.slice(0, 2).toUpperCase();
  return (parts[0]![0]! + parts[parts.length - 1]![0]!).toUpperCase();
}

export function UserMenu({ displayName, email, lang }: UserMenuProps) {
  const [open, setOpen] = useState(false);
  const containerRef = useRef<HTMLDivElement>(null);
  const triggerRef = useRef<HTMLButtonElement>(null);
  const router = useRouter();

  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') {
        setOpen(false);
        triggerRef.current?.focus();
      }
    };
    const onClick = (e: MouseEvent) => {
      if (containerRef.current && !containerRef.current.contains(e.target as Node)) {
        setOpen(false);
      }
    };
    document.addEventListener('keydown', onKey);
    document.addEventListener('mousedown', onClick);
    return () => {
      document.removeEventListener('keydown', onKey);
      document.removeEventListener('mousedown', onClick);
    };
  }, [open]);

  const handleLogout = async () => {
    // AUTH-18 / D-24: official client function only — never custom POST.
    await authClient.signOut();
    // The Sonner success toast is shown on /login via the ?logged_out=1 query param
    // pickup in LoginForm (Plan 06-05).
    router.push('/login?logged_out=1');
    router.refresh();
  };

  return (
    <div ref={containerRef} style={{ position: 'relative' }}>
      <button
        ref={triggerRef}
        type="button"
        aria-haspopup="menu"
        aria-expanded={open}
        aria-label={t('shell.user.menu.aria', lang)}
        onClick={() => setOpen(o => !o)}
        style={{
          display: 'flex', alignItems: 'center', gap: 8,
          padding: '6px 12px', borderRadius: 9999,
          background: open ? 'var(--hover-overlay)' : 'transparent',
          color: 'var(--ink)', border: 'none', cursor: 'pointer',
        }}
        onMouseEnter={(e) => { if (!open) (e.currentTarget as HTMLButtonElement).style.background = 'var(--hover-overlay)'; }}
        onMouseLeave={(e) => { if (!open) (e.currentTarget as HTMLButtonElement).style.background = 'transparent'; }}
      >
        <span style={{
          width: 28, height: 28, borderRadius: 9999,
          background: 'var(--gd)', color: '#ffffff',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontSize: 12, fontWeight: 600,
        }}>{initials(displayName)}</span>
        <span style={{
          fontSize: '14.5px', fontWeight: 500, color: 'var(--ink)',
          maxWidth: 160, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',
        }}>{displayName}</span>
        <ChevronDown size={14} strokeWidth={1.6} style={{ color: 'var(--muted)' }} />
      </button>

      {open && (
        <div role="menu" style={{
          position: 'absolute', top: 'calc(100% + 4px)', right: 0,
          width: 240, background: 'var(--surface)',
          border: '1px solid var(--border)', borderRadius: 12,
          boxShadow: 'var(--shadow-card)', padding: 8, zIndex: 110,
        }}>
          <div style={{ padding: '12px 16px', borderBottom: '1px solid var(--border)' }}>
            <div style={{ fontSize: '14.5px', fontWeight: 600, color: 'var(--ink)' }}>{displayName}</div>
            <div style={{ fontSize: '11.2px', color: 'var(--muted)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{email}</div>
          </div>
          <button
            role="menuitem"
            type="button"
            onClick={handleLogout}
            style={{
              display: 'flex', alignItems: 'center', gap: 12,
              width: '100%', padding: '10px 16px', borderRadius: 8,
              background: 'transparent', color: 'var(--ink)', border: 'none',
              fontSize: '14.5px', fontWeight: 500, cursor: 'pointer', textAlign: 'left',
            }}
            onMouseEnter={(e) => { (e.currentTarget as HTMLButtonElement).style.background = 'var(--hover-overlay)'; }}
            onMouseLeave={(e) => { (e.currentTarget as HTMLButtonElement).style.background = 'transparent'; }}
          >
            <LogOut size={17} strokeWidth={1.6} style={{ color: 'var(--muted)' }} />
            <span>{t('shell.user.menu.logout', lang)}</span>
          </button>
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; head -1 src/components/UserMenu.tsx | grep -c "'use client'" &amp;&amp; ! head -1 src/components/Topbar.tsx | grep -q "'use client'" &amp;&amp; grep -c "authClient.signOut" src/components/UserMenu.tsx &amp;&amp; grep -c "shell.user.menu.aria" src/components/UserMenu.tsx &amp;&amp; grep -c "shell.topbar.admin.badge" src/components/Topbar.tsx &amp;&amp; grep -c "var(--topbar-h)" src/components/Topbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Topbar.tsx is a Server Component (NO `'use client'` directive)
    - UserMenu.tsx is a Client Component (`'use client'` first non-comment line)
    - Topbar exports a `Topbar` function accepting `displayName`, `email`, `lang`, `theme`, optional `isAdmin`, optional `pageTitle`
    - When `isAdmin === true`, Topbar renders the ADMIN pill badge with --navy bg + white text + 9px uppercase
    - UserMenu renders an avatar circle with initials, display name (truncated 160px), ChevronDown caret, dropdown panel with display name + email header + Logout item
    - Logout calls `authClient.signOut()` then `router.push('/login?logged_out=1')` (NEVER a custom POST per D-24)
    - UserMenu closes on Escape (and refocuses trigger), outside-click, item activation
    - All visible strings go through `t()` (no hardcoded text)
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0 (no JSXText hardcoded literals)
  </acceptance_criteria>
  <done>Topbar (server) + UserMenu (client) wired with proper accessibility, logout path, ADMIN slot ready for Plan 06-07.</done>
</task>

<task type="auto">
  <name>Task 2: Extend theme + i18n server actions for DB persistence (D-27)</name>
  <files>src/lib/theme/actions.ts, src/lib/i18n/actions.ts</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/theme/actions.ts (Phase 5 — extend, do NOT replace cookie path)
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/i18n/actions.ts (Phase 5 — same)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §12 "Theme Persistence to DB"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-27 (cookie + DB write rules)
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/auth/index.ts (auth.api.getSession)
  </read_first>
  <action>
Extend `src/lib/theme/actions.ts` to add DB write after the existing cookie write (preserve everything before `revalidatePath`):

```
'use server';

import { cookies, headers } from 'next/headers';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { auth } from '@/lib/auth';
import { db, schema } from '@/lib/db';

const ALLOWED_THEMES = ['light', 'dark', 'system'] as const;
type Theme = typeof ALLOWED_THEMES[number];

export async function setTheme(theme: Theme) {
  // Runtime guard against hostile callers (TypeScript narrowing not enforced at the wire boundary).
  if (!ALLOWED_THEMES.includes(theme as Theme)) {
    return; // silently reject — no error surface
  }
  const c = await cookies();
  c.set('lt_theme', theme, {
    path: '/',
    sameSite: 'lax',
    httpOnly: false, // client-side no-flash script needs to read it
    secure: process.env.NODE_ENV === 'production',
    maxAge: 60 * 60 * 24 * 365, // 1 year
  });

  // D-27: when authenticated, also persist to users.theme so the choice
  // survives logout/login on the same or another device.
  try {
    const session = await auth.api.getSession({ headers: await headers() });
    if (session) {
      await db().update(schema.users)
        .set({ theme })
        .where(eq(schema.users.id, session.user.id));
    }
  } catch (e) {
    // Bounded redaction: log server-side, but never let DB write failure block
    // the cookie-only path (the user still gets the theme change in the UI).
    console.error('[setTheme] DB persistence failed:', e);
  }

  revalidatePath('/');
}
```

Extend `src/lib/i18n/actions.ts` mirror-for-mirror:

```
'use server';

import { cookies, headers } from 'next/headers';
import { revalidatePath } from 'next/cache';
import { eq } from 'drizzle-orm';
import { auth } from '@/lib/auth';
import { db, schema } from '@/lib/db';
import type { Lang } from './dictionaries';

const ALLOWED_LANGS = ['fr', 'en'] as const;

export async function setLang(lang: Lang) {
  if (!ALLOWED_LANGS.includes(lang as Lang)) {
    return;
  }
  const c = await cookies();
  c.set('lt_lang', lang, {
    path: '/',
    sameSite: 'lax',
    httpOnly: false,
    secure: process.env.NODE_ENV === 'production',
    maxAge: 60 * 60 * 24 * 365,
  });

  try {
    const session = await auth.api.getSession({ headers: await headers() });
    if (session) {
      await db().update(schema.users)
        .set({ language: lang })
        .where(eq(schema.users.id, session.user.id));
    }
  } catch (e) {
    console.error('[setLang] DB persistence failed:', e);
  }

  revalidatePath('/');
}
```

Both extensions:
- Preserve the original cookie-write path verbatim (Phase 5 behavior)
- Wrap the DB write in try/catch so a transient DB failure doesn't break the cookie + UI update
- Read session via `auth.api.getSession({ headers })` — same pattern as require.ts

Per CONTEXT.md "Login reconciliation (D-27)": on login, the user's stored DB theme/language should sync to cookie if cookie is empty. This reconciliation belongs in a `databaseHooks.session.create.after` (or equivalent Better Auth hook) NOT in this plan — defer to a follow-up if needed. For v1.1 launch, the simpler "cookie + DB on toggle" suffices: if a user logs in on a device where their cookie is set, that cookie wins until they next toggle.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; npm run typecheck &amp;&amp; head -1 src/lib/theme/actions.ts | grep -c "'use server'" &amp;&amp; head -1 src/lib/i18n/actions.ts | grep -c "'use server'" &amp;&amp; grep -c "auth.api.getSession" src/lib/theme/actions.ts &amp;&amp; grep -c "auth.api.getSession" src/lib/i18n/actions.ts &amp;&amp; grep -c "schema.users" src/lib/theme/actions.ts &amp;&amp; grep -c "schema.users" src/lib/i18n/actions.ts &amp;&amp; grep -c "lt_theme" src/lib/theme/actions.ts &amp;&amp; grep -c "lt_lang" src/lib/i18n/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files retain `'use server'` directive on first non-comment line
    - Both files retain the original Phase 5 cookie set (preserved verbatim)
    - Both files added: `auth.api.getSession({ headers: await headers() })` lookup
    - Both files added: `db().update(schema.users).set({...}).where(eq(schema.users.id, session.user.id))` when session present
    - Both files wrap the DB-write in try/catch (failure logs but doesn't break cookie path)
    - `revalidatePath('/')` still runs at the end of each action
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
  </acceptance_criteria>
  <done>Theme + locale toggles now persist to DB when authenticated; SHELL-04 + SHELL-07 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Build (authed) layout + page; delete app/page.tsx</name>
  <files>app/(authed)/layout.tsx, app/(authed)/page.tsx, app/page.tsx</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-UI-SPEC.md "Authenticated App Shell Layout" + "Topbar Component Specification"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "app/(authed)/layout.tsx" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/app/page.tsx (Phase 5 grid shell — pattern to inherit)
    - /Users/antoinerousseau/Developer/leasetic-calculator/app/layout.tsx (root — do NOT modify)
  </read_first>
  <action>
Step 1: Delete `app/page.tsx` (the Phase 5 home page placeholder). The (authed) route group's `page.tsx` will serve `/` going forward.

```
rm /Users/antoinerousseau/Developer/leasetic-calculator/app/page.tsx
```

Step 2: Create the (authed) route group directory and layout. Note: route group parens require shell-quoting.

```
mkdir -p "/Users/antoinerousseau/Developer/leasetic-calculator/app/(authed)"
```

Create `app/(authed)/layout.tsx`:

```
import { requireUser } from '@/lib/auth/require';
import { getCurrentLang, getCurrentTheme, t } from '@/lib/i18n';
import { Topbar } from '@/components/Topbar';

export const dynamic = 'force-dynamic';

export default async function AuthedLayout({ children }: { children: React.ReactNode }) {
  const { session, role } = await requireUser();
  const lang = await getCurrentLang();
  const theme = await getCurrentTheme();

  // Better Auth's session.user shape (from Plan 06-03's additionalFields):
  //   id, email, name, displayName, language, theme, role, sessionVersion, ...
  // Use displayName when present, fall back to email.
  const u = session.user as { email: string; displayName?: string | null; name?: string | null };
  const displayName = u.displayName ?? u.name ?? u.email;

  return (
    <div style={{
      display: 'grid',
      gridTemplateColumns: 'var(--shell-sidebar-w) 1fr',
      gridTemplateRows: 'var(--topbar-h) 1fr var(--footer-h)',
      minHeight: '100vh',
    }}>
      <aside style={{
        gridRow: '1 / 4', gridColumn: '1',
        background: 'var(--surface)', borderRight: '1px solid var(--border)',
        padding: '1.5rem 1rem',
        position: 'sticky', top: 0, height: '100vh',
      }}>
        <div style={{ fontWeight: 700, color: 'var(--navy)', fontSize: '22px' }}>Leasétic</div>
      </aside>

      <Topbar
        displayName={displayName}
        email={u.email}
        lang={lang}
        theme={theme}
        isAdmin={role === 'admin'}
      />

      <main style={{
        gridRow: '2', gridColumn: '2',
        background: 'var(--paper)',
        padding: '1.5rem 1.5rem 2rem',
        maxWidth: '1100px', width: '100%', margin: '0 auto',
      }}>
        {children}
      </main>

      <footer style={{
        gridRow: '3', gridColumn: '2',
        background: 'var(--paper)',
        borderTop: '1px solid var(--border)',
        height: 'var(--footer-h)',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        fontSize: '10.5px', color: 'var(--muted)',
      }}>
        {t('shell.footer.copyright', lang)}
      </footer>
    </div>
  );
}
```

Create `app/(authed)/page.tsx`:

```
import { getCurrentLang, t } from '@/lib/i18n';

export const dynamic = 'force-dynamic';

/**
 * Authenticated home page. Phase 7 will replace this body with the
 * "Create new proposal" CTA + recent proposals list (PROP-01 / PROP-02).
 *
 * For Phase 6, this is intentionally minimal — uses the existing welcomeHeading
 * + welcomeSubtext keys (legacy Phase 5 keys preserved by Plan 06-02 for this
 * exact backward-compat purpose).
 */
export default async function HomePage() {
  const lang = await getCurrentLang();
  return (
    <div>
      <h1 style={{ fontSize: '1.5rem', fontWeight: 700, color: 'var(--ink)', marginBottom: '0.5rem' }}>
        {t('welcomeHeading', lang)}
      </h1>
      <p style={{ color: 'var(--muted)', fontSize: '14.5px' }}>
        {t('welcomeSubtext', lang)}
      </p>
    </div>
  );
}
```

NOTE on route conflict: Next.js treats `app/page.tsx` and `app/(authed)/page.tsx` as competing for `/`. After deleting `app/page.tsx`, only `(authed)/page.tsx` exists, so the route group resolves cleanly. The `(authed)/layout.tsx` wraps `(authed)/page.tsx` because they're in the same group.

NOTE on root layout: `app/layout.tsx` (Phase 5) still wraps everything. The route group layout chain is: `app/layout.tsx` (html/body/font/Toaster/no-flash) → `app/(authed)/layout.tsx` (grid + Topbar + sidebar + footer + requireUser) → `app/(authed)/page.tsx` (content).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ! ls app/page.tsx 2>/dev/null &amp;&amp; ls "app/(authed)/layout.tsx" "app/(authed)/page.tsx" &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run build &amp;&amp; grep -c "requireUser" "app/(authed)/layout.tsx" &amp;&amp; grep -c "Topbar" "app/(authed)/layout.tsx" &amp;&amp; grep -c "isAdmin={role === 'admin'}" "app/(authed)/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` no longer exists
    - `app/(authed)/layout.tsx` exists with `export default async function AuthedLayout` calling `await requireUser()` as the first await
    - The layout renders the Phase 5 grid (3-row × 2-col) with the same CSS variables (`--shell-sidebar-w`, `--topbar-h`, `--footer-h`)
    - The `Topbar` is mounted with `displayName`, `email`, `lang`, `theme`, `isAdmin={role === 'admin'}`
    - `app/(authed)/page.tsx` exists and renders the welcome heading + subtext using the legacy Phase 5 i18n keys
    - `export const dynamic = 'force-dynamic'` is present on both the layout and page (PITFALLS §1.6 — every cookie-reading page opts out of static)
    - `npm run build` exits 0 (the route resolves to `(authed)/page.tsx` cleanly without the legacy `app/page.tsx` collision)
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
  </acceptance_criteria>
  <done>(authed) shell live; visiting / (authenticated) renders the new shell with topbar + user menu + sidebar + footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated request → (authed)/layout | `requireUser()` is the gate — it enforces both session existence AND in-band deletedAt re-check (Plan 06-04). |
| Client (UserMenu) → authClient.signOut | Same-origin only via Better Auth CSRF protections. |
| Server Action setTheme/setLang DB write → users.theme/language | `additionalFields.theme/language.input: true` is OK for these specific fields (D-27 — user-controllable preferences). Role + sessionVersion remain `input: false`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Elevation of Privilege | non-admin user reaching admin badge / admin pages | mitigate | Layout passes `isAdmin={role === 'admin'}` only — Topbar shows the badge but does NOT expose the admin URL. Plan 06-07's admin layout independently calls requireAdmin (defense in depth). |
| T-06-06-02 | Information Disclosure | display_name leakage in HTML source for unauthenticated users | accept | (authed)/layout requires session; unauthenticated requests redirect via proxy.ts before this layout renders. The HTML never ships to unauthenticated users. |
| T-06-06-03 | Tampering | malicious POST to /api/auth/sign-out from cross-origin | mitigate | UserMenu uses authClient.signOut (AUTH-18). Better Auth validates Origin. |
| T-06-06-04 | Information Disclosure | DB persistence failure leaking error details to user | mitigate | try/catch in setTheme/setLang logs server-side via console.error and silently succeeds at the user-facing level (cookie still set). User experience unchanged. |
| T-06-06-05 | Tampering | session.user spoofed locally | mitigate | requireUser does in-band DB read of users row by id; can't fake displayName via cookie alone (Better Auth signs the cookie). |
| T-06-06-06 | Information Disclosure | UserMenu exposes email in dropdown — visible if someone shoulder-surfs | accept | The user is logged in; their email is presumed visible to the user themselves. Phase 5 UI-SPEC explicitly puts email in the dropdown header. |
| T-06-06-07 | Spoofing | initials() returns predictable letter pair (could be used for username enumeration via Avatar) | accept | The avatar circle is rendered post-auth; unauthenticated users never see it. |
</threat_model>

<verification>
At plan completion the following must all be true:

1. `cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run build` exits 0 (full Next.js build with new route group + deleted page.tsx + extended actions)
2. `! ls app/page.tsx 2>/dev/null` (file deleted)
3. `ls "app/(authed)/layout.tsx" "app/(authed)/page.tsx"` succeeds
4. `grep -c 'await requireUser()' "app/(authed)/layout.tsx"` ≥ 1
5. `head -1 src/components/UserMenu.tsx | grep -c "'use client'"` returns 1
6. `! head -1 src/components/Topbar.tsx | grep -q "'use client'"` (Topbar is server)
7. `grep -c "auth.api.getSession" src/lib/theme/actions.ts` returns 1
8. `grep -c "auth.api.getSession" src/lib/i18n/actions.ts` returns 1
9. `npm run typecheck && npm run lint:check` both exit 0
</verification>

<success_criteria>
- AUTH-03 (logout) wired via UserMenu → authClient.signOut → /login?logged_out=1 → toast
- AUTH-06 (auth on /login → /) is satisfied by Plan 06-05's login/page.tsx; this plan completes the symmetry by ensuring `/` resolves to the (authed) home only when authenticated
- SHELL-01 (topbar shell), SHELL-02 (display name visible) materially satisfied via Topbar + UserMenu
- SHELL-04 (FR/EN persists), SHELL-07 (theme persists) wired via extended actions
- SHELL-08 (no flash on initial paint) preserved — Phase 5 root layout still owns the no-flash script unchanged
- SHELL-10 (toasts) consumed: logout flows through ?logged_out=1 → LoginForm picks up the param and fires `auth.toast.logout.success`
- SHELL-14 (mobile graceful) — the grid layout collapses on narrow viewports because grid-template-columns sets the sidebar to a fixed width and main to 1fr; mobile users get a horizontal scroll on the sidebar but no other breakage; v1.2 will add a real responsive breakpoint
- The legacy app/page.tsx is gone; / now serves (authed)/page.tsx with its requireUser gate
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-shell/06-06-SUMMARY.md` covering: any changes to the displayName fallback chain (e.g. if Better Auth's session.user shape requires alternate field access), and any DB-persistence regressions observed for unauthenticated theme/locale toggles.
</output>
