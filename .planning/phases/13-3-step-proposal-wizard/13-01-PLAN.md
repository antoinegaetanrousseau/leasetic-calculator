---
phase: 13-3-step-proposal-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(authed)/proposals/new/_components/WizardActionBar.tsx
  - app/(authed)/proposals/new/_components/WizardActionBar.test.tsx
  - app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.tsx
  - app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.test.tsx
  - app/(authed)/proposals/new/_components/PdfPreviewMock.tsx
  - app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx
  - app/(authed)/proposals/new/_components/RecapSection.tsx
  - app/(authed)/proposals/new/_components/RecapSection.test.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-01]

must_haves:
  truths:
    - "WizardActionBar renders a `.card` with optional `← Précédent` (omitted when currentStep===1) + `Enregistrer comme brouillon` ghost button + flex spacer + primary CTA (Link kind for steps 1/2, button kind for step 3)"
    - "WizardActionBar shows Loader2 spinner + spinnerLabel + aria-busy=true when primary.kind==='action' and primary.isSubmitting===true"
    - "PlusDeDetailsAccordion exposes role=button + aria-expanded + aria-controls trigger and animates height + opacity over 200ms ease-out; defaultOpen prop hydrates from draft.inputs._uiAccordionOpen and onToggle fires fire-and-forget server action"
    - "PdfPreviewMock renders BrandLogo (140px) + title + ref line with literal `LC-2026-XXX` + 3 placeholder bars (100%/92%/78%) + LOYER MENSUEL block in --gd + 2 trailing bars; root has role=img and aria-label per wizard.step3.pdf.preview.aria"
    - "RecapSection renders a `.card` with .ctitle/.dot header + optional right-aligned ← Modifier Link + label/value rows + optional sub-line under a row (used for commission '(non visible client)' parenthetical)"
    - "src/lib/i18n/dictionaries.ts FR + EN entries contain all wizard.* keys from UI-SPEC §6.1–§6.8 — compile-time _EnHasAllFrKeys check still passes"
    - "All 4 component test files exist and pass under Vitest with jsdom; each test imports its component from its colocated file"
  artifacts:
    - path: "app/(authed)/proposals/new/_components/WizardActionBar.tsx"
      provides: "Shared client component for the Save+Précédent+CTA action bar; consumed on all 3 wizard steps"
      exports: ["WizardActionBar", "WizardActionBarProps"]
    - path: "app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.tsx"
      provides: "Collapsible region wrapper for step-1 optional fields"
      exports: ["PlusDeDetailsAccordion", "PlusDeDetailsAccordionProps"]
    - path: "app/(authed)/proposals/new/_components/PdfPreviewMock.tsx"
      provides: "CSS-only mock of the eventual PDF for step-3 right column"
      exports: ["PdfPreviewMock", "PdfPreviewMockProps"]
    - path: "app/(authed)/proposals/new/_components/RecapSection.tsx"
      provides: "Section header + label/value rows + optional ← Modifier link; used on step 2 (1x) and step 3 (3x)"
      exports: ["RecapSection", "RecapSectionProps"]
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "~30 new wizard.* keys × FR + EN"
      contains: "wizard.step1.title"
  key_links:
    - from: "WizardActionBar"
      to: "sonner"
      via: "toast.success / toast.error inside save-draft handler"
      pattern: "import\\s*\\{\\s*toast\\s*\\}\\s*from\\s*['\"]sonner['\"]"
    - from: "PdfPreviewMock"
      to: "src/components/ui/BrandLogo"
      via: "import + width prop"
      pattern: "import\\s*\\{\\s*BrandLogo\\s*\\}"
    - from: "RecapSection"
      to: "app/globals.css"
      via: ".card + .ctitle + .dot class usage"
      pattern: "className=\"card\""
---

<objective>
Ship the 4 route-private wizard primitives + ~30 wizard.* i18n keys that all 3 wizard step routes (plans 13-03/04/05) consume.

Purpose: pull all reusable component contracts and copy into one wave-1 plan so steps 1/2/3 in Wave 2 can be planned/executed in parallel without inter-plan file conflicts. Every component spec is locked in 13-UI-SPEC.md §5; every dictionary string is locked in §6.

Output: 4 .tsx + 4 .test.tsx component files under `app/(authed)/proposals/new/_components/` + a 30-key extension to `src/lib/i18n/dictionaries.ts` (FR + EN both, compile-time parity proof gates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-3-step-proposal-wizard/13-CONTEXT.md
@.planning/phases/13-3-step-proposal-wizard/13-UI-SPEC.md
@.planning/phases/13-3-step-proposal-wizard/13-PATTERNS.md

<!-- Phase 11 shipped Stepper + BrandLogo + Shell. These four components depend only on Phase 11 primitives. -->
@.planning/phases/11-design-system-foundation-brand-assets/11-CONTEXT.md
</context>

<interfaces>
<!-- Locked component contracts — executor implements directly from these signatures. -->

From `13-UI-SPEC.md` §5.1 (WizardActionBar):
```ts
export interface WizardActionBarProps {
  currentStep: 1 | 2 | 3;
  draftId: string;
  onSaveDraft: () => Promise<void>;
  primary:
    | { kind: 'link'; href: string; label: string }
    | { kind: 'action'; onClick: () => void; label: string; spinnerLabel: string; isSubmitting: boolean };
  lang: Lang;
}
```

From `13-UI-SPEC.md` §5.2 (PlusDeDetailsAccordion):
```ts
export interface PlusDeDetailsAccordionProps {
  defaultOpen: boolean;
  onToggle: (open: boolean) => void;
  lang: Lang;
  children: ReactNode;
}
```

From `13-UI-SPEC.md` §5.3 (PdfPreviewMock):
```ts
export interface PdfPreviewMockProps {
  loyerDisplay: string;
  validityDays: 15 | 30 | 60;
  lang: Lang;
}
```

From `13-UI-SPEC.md` §5.4 (RecapSection):
```ts
export interface RecapSectionProps {
  sectionTitle: string;
  rows: Array<{ label: string; value: string | ReactNode }>;
  modifierLink?: { href: string; label: string };
  rowSublabels?: Record<number, string>;
}
```

Existing imports the executor will use (do NOT redeclare):
- `src/components/ui/BrandLogo.tsx` exports `BrandLogo` (Phase 11) — accepts `width?: number` prop
- `src/lib/i18n/dictionaries.ts` exports `t(key, lang)` and `type Lang = 'fr' | 'en'`; compile-time guard `type _EnHasAllFrKeys = ...` on lines 14-15
- `sonner` exports `{ toast }` — already installed (Phase 7)
- `lucide-react` exports `Plus`, `Loader2` — already installed; exact pin `0.469.0`
- `next/link` default export `Link`
- `react` exports `useState`, `useTransition`, `type ReactNode`
- `next/navigation` exports `useRouter`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ~30 wizard.* i18n keys (FR + EN) to dictionaries.ts</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts lines 1-60 (existing FR block opening + compile-time `_EnHasAllFrKeys` parity check at lines 14-15)
    - src/lib/i18n/dictionaries.ts existing `'sidebar.nav.*'` namespace (lines ~41-54) — pattern to mirror
    - 13-UI-SPEC.md §6.1 page titles + subtitles (6 keys)
    - 13-UI-SPEC.md §6.2 section bullet headers (7 keys: informations.client, details.projet, parametres.saisis, detail.calcul, client, projet, calcul)
    - 13-UI-SPEC.md §6.3 wizard.field.client.co.label + wizard.field.client.name.label override (2 keys)
    - 13-UI-SPEC.md §6.4 accordion trigger + 2 aria labels (3 keys)
    - 13-UI-SPEC.md §6.5 step-2 specific labels (10 keys: hero.label, hero.sub, chip.tranche, row.amount, row.commission, row.commission.sublabel, row.coefficient, row.duration, row.loyer.calculated, error.incomplete)
    - 13-UI-SPEC.md §6.6 step-3 specific labels (5 keys: modifier.link, pdf.title, pdf.ref.line, pdf.preview.aria, pdf.loyer.label)
    - 13-UI-SPEC.md §6.7 action bar button labels (7 keys: previous, previous.aria, save.draft, step1.continue, step2.continue, step3.confirm, step3.confirm.spinner)
    - 13-UI-SPEC.md §6.8 toast strings (4 keys: save.draft.success, finalize.success, finalize.error, validation.errors)
    - 13-PATTERNS.md §`src/lib/i18n/dictionaries.ts` for the exact namespace insertion pattern
  </read_first>
  <behavior>
    - Test 1: dictionaries.fr contains key 'wizard.step1.title' with value 'Paramètres du projet'
    - Test 2: dictionaries.en contains key 'wizard.step1.title' with value 'Project parameters'
    - Test 3: dictionaries.fr contains key 'wizard.toast.save.draft.success' with value 'Brouillon enregistré ✓'
    - Test 4: dictionaries.en contains key 'wizard.toast.save.draft.success' with value 'Draft saved ✓'
    - Test 5: every wizard.* key present in FR also present in EN (enforced by existing compile-time _EnHasAllFrKeys check — typecheck fails if drift)
    - Test 6: dictionaries.fr['wizard.step2.row.commission.sublabel'] === '(non visible client)' (D-12 commission parenthetical copy)
    - Test 7: dictionaries.fr['wizard.step3.pdf.ref.line'] contains literal 'LC-2026-XXX' (per D-15; never digits)
  </behavior>
  <action>Open `src/lib/i18n/dictionaries.ts`. Add a new `wizard.*` namespace block inside both the `fr` and `en` exported objects, mirroring the existing `sidebar.nav.*` block. Insert all 44 keys from 13-UI-SPEC.md §6.1–§6.8 (six §6.1 + seven §6.2 + two §6.3 + three §6.4 + ten §6.5 + five §6.6 + seven §6.7 + four §6.8). Use the EXACT FR + EN strings from §6.1–§6.8 (do not paraphrase). The compile-time `_EnHasAllFrKeys` type guard on lines 14-15 will catch any FR↔EN key drift on typecheck. Do NOT delete or modify existing keys. Do NOT add the legacy `form.client.co` override (per D-7-13 carry-over and UI-SPEC §6.3 recommendation: keep both keys for backward-compat). Order keys: §6.1 first, then §6.2, then §6.3 wizard.field.*, then §6.4 accordion, then §6.5 step2, then §6.6 step3, then §6.7 action, then §6.8 toast. Also add a `wizard.toast.draft.error` key in both FR/EN with values 'Erreur lors de l\\'enregistrement. Réessayez.' and 'Save failed. Try again.' to support the WizardActionBar onSaveDraft catch branch in Task 2. Run `npm run typecheck` to verify FR↔EN parity holds.</action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -q "error" && exit 1; grep -c "'wizard\\." src/lib/i18n/dictionaries.ts | awk '$1 < 80 { exit 1 } { exit 0 }'</automated>
  </verify>
  <done>~45 wizard.* keys exist in BOTH fr and en blocks; npm run typecheck exits 0 (FR↔EN parity holds); no existing keys removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WizardActionBar + PlusDeDetailsAccordion (client components)</name>
  <files>
    app/(authed)/proposals/new/_components/WizardActionBar.tsx,
    app/(authed)/proposals/new/_components/WizardActionBar.test.tsx,
    app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.tsx,
    app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.test.tsx
  </files>
  <read_first>
    - 13-UI-SPEC.md §5.1 (full WizardActionBar prop signature + layout + interaction states + accessibility) — pixel-locked
    - 13-UI-SPEC.md §5.2 (full PlusDeDetailsAccordion prop signature + layout + animation timing + ARIA)
    - 13-UI-SPEC.md §7.6, §7.7, §7.8 (interaction states for primary CTA, ghost button, ← Précédent text-link)
    - 13-UI-SPEC.md §7.4 (accordion trigger interaction states)
    - 13-UI-SPEC.md §11 (animation/transition inventory — 200ms ease-out for accordion, 1s linear infinite for spinner; @keyframes spin check before adding)
    - 13-PATTERNS.md §WizardActionBar (full pattern lift from `src/components/proposals/DeleteButtonClient.tsx` lines 1-43 — useTransition + try/catch + toast.success + router.push pattern)
    - 13-PATTERNS.md §PlusDeDetailsAccordion (pattern from `src/components/UserMenu.tsx` lines 23-92 — useState(open) + aria-expanded + aria-controls)
    - src/components/proposals/DeleteButtonClient.tsx lines 1-43 (analog — useState busy + try/catch/finally + toast.success + router.push)
    - src/components/UserMenu.tsx lines 23-92 (analog — useState(open) + aria-expanded + aria-controls)
    - src/components/ui/Stepper.test.tsx lines 1-33 (Vitest test pattern — afterEach(() => cleanup()), describe/it, screen.getBy*)
    - src/lib/i18n/dictionaries.ts — for `t(key, lang)` + `type Lang` imports
  </read_first>
  <behavior>
    WizardActionBar:
    - Test 1: renders Précédent link with aria-label "Étape précédente" when currentStep === 2
    - Test 2: omits Précédent link when currentStep === 1 (queryByLabelText returns null)
    - Test 3: renders primary CTA as Link element when primary.kind === 'link' (anchor with href)
    - Test 4: renders primary CTA as button when primary.kind === 'action'
    - Test 5: shows Loader2 SVG + spinnerLabel + aria-busy=true when primary.kind === 'action' && isSubmitting === true
    - Test 6: calls onSaveDraft when "Enregistrer comme brouillon" is clicked (mocked function called once)
    - Test 7: disables save button + primary CTA while save-action is pending (useTransition isPending)
    - Test 8: shows success toast `Brouillon enregistré ✓` after onSaveDraft resolves (mock sonner.toast.success)
    - Test 9: shows error toast `Erreur lors de l'enregistrement. Réessayez.` after onSaveDraft throws (mock toast.error)

    PlusDeDetailsAccordion:
    - Test 1: starts collapsed when defaultOpen === false (trigger has aria-expanded="false")
    - Test 2: starts expanded when defaultOpen === true (trigger has aria-expanded="true", children visible)
    - Test 3: toggles aria-expanded between true/false on click
    - Test 4: calls onToggle(true) when collapsing→expanding, onToggle(false) when expanding→collapsing
    - Test 5: trigger has role="button", aria-controls="plus-de-details-region", focusable via tab
    - Test 6: child content (passed via `children` prop) is in DOM regardless of open state (height:0 + opacity:0 hides, not removes — preserves RHF state per UI-SPEC §5.2)
    - Test 7: Plus icon has transform: rotate(45deg) inline style when open === true
  </behavior>
  <action>
  Create `app/(authed)/proposals/new/_components/WizardActionBar.tsx` ('use client'). Prop signature from `<interfaces>` block above. Layout per UI-SPEC §5.1: `<section className="card">` with `display: flex; align-items: center; gap: 12`. Conditional `← Précédent` Link (omitted when currentStep === 1) — href computed as `/proposals/new/${currentStep === 2 ? 'parametres' : 'calcul'}?draft_id=${draftId}`, aria-label from `t('wizard.action.previous.aria', lang)`, styled inline with `color: var(--muted), fontSize: 14.5, fontWeight: 500, textDecoration: 'none'`. Then `<button type="button" className="btn-out" onClick={handleSave} disabled={isPending}>` with text `t('wizard.action.save.draft', lang)`. Then `<div style={{ flex: 1 }} />` spacer. Then primary CTA: when `primary.kind === 'link'`, render `<Link href={primary.href} className="btn-green" style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>{primary.label}</Link>`; when `primary.kind === 'action'`, render `<button type="button" className="btn-green" onClick={primary.onClick} disabled={primary.isSubmitting} aria-busy={primary.isSubmitting} style={{ filter: primary.isSubmitting ? 'brightness(0.9)' : undefined }}>` containing `{primary.isSubmitting && <Loader2 size={16} className="animate-spin" aria-hidden="true" />}` + label morphs to `primary.spinnerLabel` when isSubmitting else `primary.label`. handleSave wraps `onSaveDraft()` in `useTransition` + `try { await onSaveDraft(); toast.success(t('wizard.toast.save.draft.success', lang)); } catch { toast.error(t('wizard.toast.draft.error', lang)); }`. Use `const router = useRouter();` only if needed — the server action handles the redirect, so router.push is NOT required here (server action `saveAsDraftAction` from plan 13-02 redirects to `/` itself).

  CRITICAL: Reference D-17 + D-18 + D-19 (action-bar composition) and D-24 (finalize spinner UX) in code comments. Use the `wizard.toast.draft.error` key added in Task 1 for the catch path.

  Create the colocated `WizardActionBar.test.tsx` with the 9 behaviour assertions above. Use `import { render, screen, fireEvent } from '@testing-library/react';` + `import { vi } from 'vitest';` + `vi.mock('sonner', () => ({ toast: { success: vi.fn(), error: vi.fn() } }))`. afterEach(() => cleanup()).

  Create `app/(authed)/proposals/new/_components/PlusDeDetailsAccordion.tsx` ('use client'). Pattern lifted directly from `13-PATTERNS.md` §PlusDeDetailsAccordion (the in-line useState(open) + aria-expanded + role="button" + animated height + Plus icon rotation 0°→45°). Children stay mounted at all times (height: 0 + overflow: hidden + opacity: 0 when closed — D-21 RHF state preservation). Animation: `transition: 'height 200ms ease-out, opacity 200ms ease-out'`. onToggle fires fire-and-forget (no await — pure state mirror). Trigger uses `t('wizard.accordion.trigger', lang)` for label + `t('wizard.accordion.aria.label.open', lang)` / `t('wizard.accordion.aria.label.close', lang)` for aria-label toggle.

  Create the colocated `PlusDeDetailsAccordion.test.tsx` with the 7 behavior assertions above. Pattern from `Stepper.test.tsx`.

  Reference D-06 (accordion contents preserved in `_uiAccordionOpen`), D-19 (action bar composition), D-24 (spinner UX) in code comments next to the relevant logic.
  </action>
  <verify>
    <automated>npm test -- --run app/\\(authed\\)/proposals/new/_components/WizardActionBar.test.tsx app/\\(authed\\)/proposals/new/_components/PlusDeDetailsAccordion.test.tsx</automated>
  </verify>
  <done>Both .tsx files exist; both .test.tsx files exist; all 16 tests pass; both components export the prop interface from `<interfaces>` block; no `any` types introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement PdfPreviewMock + RecapSection (presentational)</name>
  <files>
    app/(authed)/proposals/new/_components/PdfPreviewMock.tsx,
    app/(authed)/proposals/new/_components/PdfPreviewMock.test.tsx,
    app/(authed)/proposals/new/_components/RecapSection.tsx,
    app/(authed)/proposals/new/_components/RecapSection.test.tsx
  </files>
  <read_first>
    - 13-UI-SPEC.md §5.3 (full PdfPreviewMock layout — logo block 140px + title 18px/600/--navy + ref line 12.5px/--muted with literal `LC-2026-XXX` + 3 bars 100%/92%/78% + LOYER MENSUEL block + 2 trailing bars 85%/70%)
    - 13-UI-SPEC.md §5.4 (full RecapSection layout — .ctitle/.dot header + optional ← Modifier Link + 2-column flex rows + optional sub-line + final-row top-border for "DÉTAIL DU CALCUL")
    - 13-PATTERNS.md §PdfPreviewMock (skeleton from BrandLogo + AdminNavCard analogs)
    - 13-PATTERNS.md §RecapSection (skeleton from .ctitle/.dot pattern in `ProposalForm.tsx:213-219`)
    - src/components/ui/BrandLogo.tsx — export signature + width prop
    - src/components/proposal/ProposalForm.tsx lines 213-219 — `.ctitle` / `.dot` triad pattern
    - app/(authed)/proposals/[id]/page.tsx lines 51-60 — `<dt>` / `<dd>` row idiom analog
    - app/globals.css — `.card`, `.ctitle`, `.dot`, `.error-msg` utility class definitions (consume; do not redeclare)
  </read_first>
  <behavior>
    PdfPreviewMock:
    - Test 1: renders root with role="img" and aria-label from `wizard.step3.pdf.preview.aria`
    - Test 2: renders BrandLogo with width={140}
    - Test 3: renders title `Proposition de financement` (FR) when lang === 'fr'
    - Test 4: renders ref line containing literal substring 'LC-2026-XXX' (never digits — D-15)
    - Test 5: ref line interpolates `validityDays` prop (e.g. `30 jours de validité` when validityDays === 30)
    - Test 6: renders the LOYER MENSUEL label + value where the value contains `loyerDisplay` prop and has color: var(--gd)
    - Test 7: placeholder bars (3 + 2) are aria-hidden="true"

    RecapSection:
    - Test 1: renders `.card` section with `.ctitle` header containing the sectionTitle prop
    - Test 2: renders `.dot` glyph with `background: var(--gd)` style (uses existing `.ctitle .dot` rule; the `.dot` element exists in DOM)
    - Test 3: omits the ← Modifier link when modifierLink prop is undefined
    - Test 4: renders ← Modifier Link with href === modifierLink.href when modifierLink prop is provided
    - Test 5: renders one row per entry in rows[] with label + value
    - Test 6: row.value renders ReactNode if value is a ReactNode (e.g. <strong>...</strong>)
    - Test 7: renders a sub-line below a row's label when rowSublabels[rowIndex] is provided (used for commission '(non visible client)' on step 2)
  </behavior>
  <action>
  Create `app/(authed)/proposals/new/_components/PdfPreviewMock.tsx`. Pure-render server-or-client component (no state, no event handlers). Skeleton from `13-PATTERNS.md` §PdfPreviewMock. Prop signature from `<interfaces>` block. Root: `<div role="img" aria-label={t('wizard.step3.pdf.preview.aria', lang)} style={{ background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: 12, padding: 24, width: '100%', maxWidth: 360 }}>`. Inside: `<BrandLogo width={140} />` + 20px margin-bottom; then `<h3>` title 18px/600/--navy (text from `t('wizard.step3.pdf.title', lang)`) + 4px margin-bottom; then `<p>` ref line 12.5px/--muted with text `t('wizard.step3.pdf.ref.line', lang).replace('{0}', String(validityDays))` (the literal LC-2026-XXX comes from the i18n string itself, per UI-SPEC §6.6 — DO NOT hardcode in JSX); then 24px margin-bottom + a div containing 3 bars (8px height, var(--border) bg, border-radius 4, widths 100%/92%/78% with 8px gap, aria-hidden); then 28px margin-bottom + LOYER MENSUEL block (`.ctitle`-style label from `t('wizard.step3.pdf.loyer.label', lang)` + `{loyerDisplay}` value at 18px/700/--gd + optional sublabel + 2 trailing bars at 85%/70% widths).

  Reference D-15 (no real lc_ref pre-finalize) in a code comment next to the ref-line rendering.

  Create the colocated `PdfPreviewMock.test.tsx` with the 7 behavior assertions. Mock `BrandLogo` if needed (or render the real component — `BrandLogo` is a server component but for Vitest jsdom it should render to its SVG; verify by trial — if it breaks Vitest, mock it with `vi.mock('@/components/ui/BrandLogo', () => ({ BrandLogo: ({ width }: { width: number }) => <div data-testid="brand-logo" data-width={width} /> }))`).

  Create `app/(authed)/proposals/new/_components/RecapSection.tsx`. Pure-render component. Skeleton from `13-PATTERNS.md` §RecapSection. Prop signature from `<interfaces>` block. Root: `<section className="card" style={{ marginBottom: 16 }}>`. Header row: `<div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 16 }}>` with left side `<div className="ctitle"><span className="dot" style={{ background: 'var(--gd)' }} aria-hidden="true" /><span>{sectionTitle}</span></div>` (the .ctitle / .dot pattern is verbatim from ProposalForm.tsx:213-219) and right side `{modifierLink && <Link href={modifierLink.href} style={{ fontSize: 12.5, fontWeight: 500, color: 'var(--teal)', textDecoration: 'none' }}>← {modifierLink.label}</Link>}`. Rows: `{rows.map((row, idx) => (<div key={idx} style={{ display: 'flex', justifyContent: 'space-between', marginTop: idx === 0 ? 0 : 12 }}>` with `<div>` containing the label (11.2px/500/--muted) + optional `rowSublabels?.[idx]` rendered below the label as 11.2px/400/--muted/marginTop:2 + `<div style={{ fontSize: 14.5, color: 'var(--ink)', textAlign: 'right' }}>{row.value}</div>` for the right column.

  Reference D-12 (commission parenthetical is the only consumer of rowSublabels) in a code comment near the rowSublabels logic.

  Create the colocated `RecapSection.test.tsx` with the 7 behavior assertions. Pattern from `Stepper.test.tsx`.
  </action>
  <verify>
    <automated>npm test -- --run app/\\(authed\\)/proposals/new/_components/PdfPreviewMock.test.tsx app/\\(authed\\)/proposals/new/_components/RecapSection.test.tsx</automated>
  </verify>
  <done>Both .tsx files exist; both .test.tsx files exist; all 14 tests pass; PdfPreviewMock root has role="img"; RecapSection consumes existing `.card` + `.ctitle` + `.dot` classes (no new CSS).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DOM | These are presentational/client components only; no untrusted input crosses to the server in this plan. Mutation handlers (e.g., WizardActionBar's onSaveDraft, PlusDeDetailsAccordion's onToggle) only receive `()=>void` callbacks defined and authenticated by their server-side caller (plans 13-03/04/05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-S | Spoofing | WizardActionBar.tsx onSaveDraft callback | accept | Callback contract — onSaveDraft is provided by the server-rendered route page (plans 13-03/04/05) which has already run `requireUser()` (D-01); WizardActionBar itself runs no auth and is given no draftId beyond the prop. Spoofing risk lives at the server-action layer (plan 13-02), not here. |
| T-13-01-I | Information Disclosure (ADMIN-09) | All 4 components | accept | None of these components render commission. PdfPreviewMock renders ONLY loyerDisplay + validityDays + literal LC-2026-XXX (D-15); commission never crosses this plan's surface. RecapSection accepts arbitrary rows from its caller — caller (plan 13-04 step 2) is responsible for never passing commission into a row that ends up in the PDF render path. Verified by plan 13-06's golden-PDF-no-commission test. |
| T-13-01-T | Tampering | dictionaries.ts | mitigate | The compile-time `_EnHasAllFrKeys` parity proof (lines 14-15) catches any FR↔EN drift, preventing partial-localization runtime hazards. Build fails if a wizard.* FR key has no EN counterpart. |
| T-13-01-X | XSS via dictionary values | dictionaries.ts (FR/EN strings) | mitigate | All wizard.* values are static literals authored in this plan — no user-supplied content. React's default JSX escaping covers any interpolated placeholders (e.g., `{0}` replacements). No raw-HTML injection sinks used in any of the 4 components. |
</threat_model>

<verification>
- npm run typecheck exits 0 (FR↔EN parity for wizard.* keys holds via _EnHasAllFrKeys)
- npm run lint exits 0
- npm test -- --run app/\(authed\)/proposals/new/_components/ passes (37+ test assertions across 4 component test files)
- grep -v '^#' app/(authed)/proposals/new/_components/PdfPreviewMock.tsx | grep -c "LC-2026-XXX" returns 0 (literal `LC-2026-XXX` comes from i18n key `wizard.step3.pdf.ref.line`, NOT from JSX hardcode — D-15 enforcement)
- grep -v '^#' src/lib/i18n/dictionaries.ts | grep -c "LC-2026-XXX" returns 2 (FR + EN entries for `wizard.step3.pdf.ref.line`)
- grep -v '^#' app/(authed)/proposals/new/_components/*.tsx | grep -E "import.*from ['\"]@\\/components/ui/(Stepper|RetractableSidebar)['\"]" returns 0 (Phase 11 Stepper/RetractableSidebar are consumed by route pages in 13-03/04/05, NOT by these route-private components)
</verification>

<success_criteria>
- 4 component files + 4 test files exist under `app/(authed)/proposals/new/_components/`
- src/lib/i18n/dictionaries.ts gains ~45 new wizard.* keys × FR + EN (compile-time parity proof holds)
- All 37+ Vitest assertions pass
- WizardActionBar implements D-17 + D-18 + D-19 + D-24 (action bar composition + save-draft + spinner UX)
- PlusDeDetailsAccordion implements D-06 (5 optional fields container + 200ms ease-out animation)
- PdfPreviewMock implements D-15 (CSS mock, never real lc_ref, never real PDF blob)
- RecapSection supports the step-2 commission parenthetical (D-12 visual pathway — partner-facing only; PDF render path is separate and verified in plan 13-06)
- No new global CSS classes introduced (all surfaces consume existing `.card` / `.ctitle` / `.dot` / `.btn-green` / `.btn-out`)
- All 4 components export the prop interface from the `<interfaces>` block above (so plans 13-03/04/05 can import deterministically without scavenger-hunting types)
</success_criteria>

<output>
After completion, create `.planning/phases/13-3-step-proposal-wizard/13-01-SUMMARY.md` documenting:
- Final file list + line counts
- All 37+ Vitest assertions enumerated as a checklist
- The exact set of new wizard.* keys added (count FR + count EN)
- Confirmation that no new global CSS classes were added (grep result)
- Confirmation that PdfPreviewMock renders LC-2026-XXX from i18n, not from a JSX literal
- Any FR/EN copy that the developer might want to review (per UI-SPEC §16 open question #2 — wizard.field.client.co.label override)
</output>
