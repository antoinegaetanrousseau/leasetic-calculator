---
phase: 26-active-expired-row-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/proposals/RowActionsClient.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROWACT-01, ROWACT-03, ROWACT-04]
must_haves:
  truths:
    - "A reusable client component renders an Archive icon button for active/expired rows and a Restore icon button for deleted rows, selected by displayStatus; draft renders nothing."
    - "Clicking Archive POSTs to /api/proposals/{id}/delete, shows a sonner success toast with a 'Voir les archivees' action link to /proposals?archived=1, then calls router.refresh()."
    - "Clicking Restore POSTs to /api/proposals/{id}/restore, shows a success toast, then calls router.refresh() (no navigation away from the list)."
    - "New FR and EN archive keys are added in dictionaries.ts (both fr + en sections) and the _EnHasAllFrKeys parity proof still compiles."
  artifacts:
    - path: "src/components/proposals/RowActionsClient.tsx"
      provides: "Shared per-row Archive/Restore icon-button client component keyed off displayStatus"
      min_lines: 60
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "FR + EN keys for finalized-row archive action + toast"
      contains: "proposal.row.action.archive"
  key_links:
    - from: "src/components/proposals/RowActionsClient.tsx"
      to: "/api/proposals/{id}/delete"
      via: "fetch POST in the Archive handler"
      pattern: "proposals/.+/delete"
    - from: "src/components/proposals/RowActionsClient.tsx"
      to: "/api/proposals/{id}/restore"
      via: "fetch POST in the Restore handler"
      pattern: "proposals/.+/restore"
---

<objective>
Build the shared per-row action component and the i18n keys it needs, with zero wiring into the list yet (that is Plan 02). The component renders exactly one button depending on the row's server-derived `displayStatus`: Archive for `active`/`expired`, Restore for `deleted`, nothing for `draft`. It is a near-clone of `DraftActionsClient`'s Archive handler (minus Edit, minus Delete) plus `RestoreButtonClient`'s restore call, reusing the established `ICON_BTN` 30x30 ghost styling and lucide icons at size 14.

Purpose: ROWACT-01 (Archive on active/expired) + ROWACT-03 (finalized rows show only Archive — no Edit, no Delete) + D-02 (Restore in Archivees) delivered through one parameterized component, keeping ROWACT-04 intact (component takes only `proposalId` + `lang` + `displayStatus`; never touches commission/params_snapshot).
Output: `src/components/proposals/RowActionsClient.tsx` plus new FR/EN i18n keys in `dictionaries.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-active-expired-row-actions/26-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FR + EN i18n keys for the finalized-row Archive action and toast</name>
  <read_first>
    - src/lib/i18n/dictionaries.ts (SINGLE-file dictionary — it has an `fr` section and an `en` section, plus the `_EnHasAllFrKeys` compile-time parity proof near line 1837. NOTE: there is NO fr.ts / en.ts; all keys live here. Find the existing `proposal.draft.action.archive`, `proposal.draft.toast.archive.success/error`, `proposal.toast.delete.action.view.deleted`, `proposal.toast.restore.success/error`, and `proposal.detail.action.restore` entries to mirror tone, placement, and the dotted-flat-key shape.)
    - src/components/proposals/DeleteButtonClient.tsx (model for the toast-with-action label key `proposal.toast.delete.action.view.deleted` → onClick router.push)
    - src/lib/i18n/dictionaries.test.ts (the runtime parity test that backs the compile-time `_EnHasAllFrKeys` guard)
  </read_first>
  <action>
    Add these NEW keys to BOTH the FR section AND the EN section of src/lib/i18n/dictionaries.ts (identical key names, language-appropriate values — every FR key MUST have an EN counterpart or `_EnHasAllFrKeys` fails to compile):
    `proposal.row.action.archive` (FR "Archiver" / EN "Archive"),
    `proposal.row.toast.archive.success` (FR "Proposition archivee" / EN "Proposal archived"),
    `proposal.row.toast.archive.error` (FR "Echec de l'archivage" / EN "Archiving failed"),
    `proposal.row.toast.archive.action.viewArchived` (FR "Voir les archivees" / EN "View archived").
    Do NOT add Restore label keys unless needed — Restore reuses the existing `proposal.detail.action.restore` + `proposal.toast.restore.success` / `proposal.toast.restore.error` keys already present (verify while reading; if a row-specific aria-label reads better, add `proposal.row.action.restore` to BOTH sections and note it in the SUMMARY). Match the project's existing FR diacritics convention exactly as seen in adjacent keys (e.g. "archivees" vs "archivées" — copy whatever the neighbouring entries use). Keep keys dotted-flat; do NOT reorder or edit existing keys.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit 2>&1 | grep -i "EnHasAllFr\|EnParityProof\|dictionaries" || echo PARITY_PROOF_COMPILES</automated>
  </verify>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx vitest run src/lib/i18n/dictionaries.test.ts --reporter=dot 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "proposal.row.action.archive" src/lib/i18n/dictionaries.ts returns >= 2 (one FR section + one EN section occurrence).
    - Source: grep -c "proposal.row.toast.archive.action.viewArchived" src/lib/i18n/dictionaries.ts returns >= 2.
    - Build: npx tsc --noEmit reports no error on the `_EnHasAllFrKeys` / `_EnParityProof` types in dictionaries.ts (proves FR-subset-of-EN parity held).
    - Test: src/lib/i18n/dictionaries.test.ts passes.
  </acceptance_criteria>
  <done>Four new archive keys exist in both the FR and EN sections of dictionaries.ts; the _EnHasAllFrKeys parity proof typechecks clean and the parity test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create RowActionsClient — displayStatus-keyed Archive/Restore icon button</name>
  <read_first>
    - src/components/proposals/DraftActionsClient.tsx (THE template: `ICON_BTN` const, `handleArchive` fetch-POST then toast then router.refresh(), `e.stopPropagation()`, disabled-while-busy via useState, lucide `Archive` size 14, the wrapping div with onClick stopPropagation, `t(key, lang)` from '@/lib/i18n/dictionaries')
    - src/components/proposals/RestoreButtonClient.tsx (the restore fetch-POST to /api/proposals/{id}/restore, lucide `Undo2`, toast keys `proposal.toast.restore.success`/`error`; note it router.push-es to the detail page — we deliberately will NOT do that)
    - src/components/proposals/DeleteButtonClient.tsx (toast-with-action shape: second arg { action: { label, onClick }, duration: 6000 } — model for D-05's "Voir les archivees" toast)
    - src/lib/db/queries/proposals.ts (line 870: `export type DisplayStatus = 'draft' | 'active' | 'expired' | 'deleted'`; line 881 `deriveDisplayStatus` — the union this component switches on. Confirms softDeleteProposal WHERE status='active' and restoreProposal WHERE status='deleted'.)
    - src/lib/api/proposals/list.ts (ProposalRowDto.displayStatus: DisplayStatus — the prop source; the DTO is commission-stripped, paramsSnapshot never crosses)
  </read_first>
  <action>
    Create src/components/proposals/RowActionsClient.tsx as a 'use client' component. Props: { proposalId: string; lang: Lang; displayStatus: DisplayStatus } — import `Lang` and `t` from '@/lib/i18n/dictionaries', import `type DisplayStatus` from '@/lib/db/queries/proposals' (match how list.ts re-exports it; if importing from '@/lib/db/queries' barrel works, prefer that to match DraftActionsClient/RestoreButtonClient conventions). Copy the `ICON_BTN` style object from DraftActionsClient (30x30, border var(--border), color var(--muted), borderRadius 6, flexShrink 0, transition). Use a `busy` useState boolean. Render exactly one button keyed off displayStatus:
      - displayStatus === 'deleted' -> a single Restore icon button (lucide Undo2 size 14). onClick async: e.stopPropagation(); if busy return; setBusy(true); try fetch('/api/proposals/' + proposalId + '/restore', { method: 'POST' }); if !res.ok toast.error(t('proposal.toast.restore.error', lang)) and return; else toast.success(t('proposal.toast.restore.success', lang)) then router.refresh(); finally setBusy(false). Do NOT router.push to the detail page (stay in the Archivees list — this is the deliberate difference from RestoreButtonClient). aria-label + title = t('proposal.detail.action.restore', lang) (or 'proposal.row.action.restore' if Task 1 added it).
      - displayStatus === 'active' || displayStatus === 'expired' -> a single Archive icon button (lucide Archive size 14). onClick async: e.stopPropagation(); if busy return; setBusy(true); try fetch('/api/proposals/' + proposalId + '/delete', { method: 'POST' }) (reuse existing endpoint per D-01 — it calls softDeleteProposal; do NOT use /draft-archive or /draft-delete); if !res.ok toast.error(t('proposal.row.toast.archive.error', lang)) and return; else toast.success(t('proposal.row.toast.archive.success', lang), { action: { label: t('proposal.row.toast.archive.action.viewArchived', lang), onClick: () => router.push('/proposals?archived=1') }, duration: 6000 }) then router.refresh(); finally setBusy(false). NO window.confirm (D-04 instant). aria-label + title = t('proposal.row.action.archive', lang).
      - displayStatus === 'draft' -> return null (drafts use DraftActionsClient; guard against misuse).
    Disable the button + set opacity 0.5 while busy. Wrap the button in a div with style { display: 'flex', gap: 4, alignItems: 'center', justifyContent: 'flex-end' } and onClick={(e)=>e.stopPropagation()} exactly like DraftActionsClient so the action column never bubbles a click to the row body. Props are strictly proposalId + lang + displayStatus — NO commission / paramsSnapshot prop, no fenced code (ROWACT-04 / D-07).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit 2>&1 | grep -i "RowActionsClient" || echo TYPECHECK_CLEAN_FOR_FILE</automated>
  </verify>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx vitest run tests/admin-09-grep-contracts.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "/delete" src/components/proposals/RowActionsClient.tsx >= 1 AND grep -c "/restore" src/components/proposals/RowActionsClient.tsx >= 1 (Archive reuses the delete endpoint; Restore uses the restore endpoint).
    - Source: grep -c "viewArchived" src/components/proposals/RowActionsClient.tsx returns 1 (the "Voir les archivees" toast action wired to /proposals?archived=1).
    - Source: grep -ci "commission\|paramsSnapshot\|window.confirm" src/components/proposals/RowActionsClient.tsx returns 0.
    - Behavior: read the render branches — the component returns null when displayStatus === 'draft', Archive button for active/expired, Restore button for deleted.
    - Test: npx vitest run tests/admin-09-grep-contracts.test.ts passes with all gates green, unmodified (file not edited).
  </acceptance_criteria>
  <done>RowActionsClient renders Archive for active/expired, Restore for deleted, null for draft; Archive POSTs /delete with a "Voir les archivees" toast then refreshes; Restore POSTs /restore then refreshes; no commission surface; admin-09 suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (partner) → /api/proposals/{id}/{delete,restore} | Untrusted proposalId crosses here; the only state-changing surface this plan touches |
| server → client (ProposalRowDto) | Commission/params_snapshot must never cross; only displayStatus (bounded union) is consumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation/IDOR | RowActionsClient → /api/proposals/{id}/delete and /restore | accept (mitigated upstream) | VERIFIED in source: delete/restore route handlers derive userId from `requireUser()` session (NEVER request body); softDeleteProposal(id, userId) WHERE proposals.id=id AND proposals.userId=userId AND status='active'; restoreProposal(id, userId) WHERE userId=userId AND status='deleted'. A partner cannot archive/restore another partner's proposal by ID — non-owned IDs return affected=0 → 404. This plan introduces NO new endpoint and no bypass; residual risk LOW. |
| T-26-02 | Information Disclosure | RowActionsClient props | mitigate | Props are exactly proposalId + lang + displayStatus (bounded 4-string union). No commission/paramsSnapshot prop; grep-asserted 0 occurrences. ProposalRowDto (list.ts) whitelist never projects paramsSnapshot. ROWACT-04 envelope held. Severity LOW. |
| T-26-03 | Tampering (CSRF) | state-changing POST /delete and /restore | accept | Reuses the exact same-origin fetch-POST pattern as the existing draft archive / detail delete / detail restore; relies on the established SameSite=Lax + __Secure- cookie posture (STATE.md Phase 6 follow-up #2). No new CSRF surface — identical to in-production flows. Severity LOW. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No new packages; lucide-react + sonner already in package.json. No legitimacy checkpoint required. |
</threat_model>

<verification>
- npx tsc --noEmit clean (parity proof + new component typecheck).
- npx vitest run tests/admin-09-grep-contracts.test.ts — gate suite green, unmodified.
- npx vitest run src/lib/i18n/dictionaries.test.ts — FR/EN parity test green.
- Both FR and EN sections of dictionaries.ts carry the new proposal.row.* keys.
</verification>

<success_criteria>
- RowActionsClient exists, renders the correct single button per displayStatus, and is import-ready for Plan 02.
- No commission data path introduced; admin-09 suite passes.
- FR/EN parity proof compiles.
</success_criteria>

<output>
Create `.planning/phases/26-active-expired-row-actions/26-01-SUMMARY.md` when done.
</output>
