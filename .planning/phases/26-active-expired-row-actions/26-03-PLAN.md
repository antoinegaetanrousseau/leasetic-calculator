---
phase: 26-active-expired-row-actions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ROWACT-02, ROWACT-03, ROWACT-01]
must_haves:
  truths:
    - "REQUIREMENTS.md ROWACT-02 (per-row Delete) is marked descoped/superseded by the Archive-only decision (D-01), not silently dropped."
    - "REQUIREMENTS.md ROWACT-03 is rewritten to finalized rows show only Archive (no Edit, no Delete)."
    - "REQUIREMENTS.md captures the new Restore-in-Archivees capability (D-02) as an explicit requirement."
    - "ROADMAP.md Phase 26 goal + success criteria reflect Archive-only: criterion 2 (per-row Delete with confirmation) removed/rewritten, criterion 3 = finalized rows show only Archive, a Restore criterion added."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Descope-reconciled ROWACT entries (Archive-only) + Restore requirement"
      contains: "descoped"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 success criteria matching Archive-only scope"
      contains: "Archive"
  key_links: []
---

<objective>
Reconcile the stale roadmap/requirements with the Archive-only descope (D-01), exactly as Phase 25 reconciled its descoped teal rebrand. Documentation-only: no source code, no test changes. Accounts for ROWACT-02 by SUPERSEDING it (Archive-only collapses Archive + Delete into one operation), rewrites ROWACT-03 to Archive-only, and captures the genuinely-new Restore-in-Archivees capability (D-02) as a tracked requirement.

Purpose: keep traceability honest — every original ROWACT ID has an explicit disposition in REQUIREMENTS.md and ROADMAP.md, and the new scope (Archive + Restore, no Delete) is the documented source of truth before the milestone audit runs.
Output: edited REQUIREMENTS.md + ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-active-expired-row-actions/26-CONTEXT.md
@.planning/milestones/v1.4-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md to Archive-only + Restore (descope ROWACT-02)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the ROWACT-01..04 block at lines 19-22, the Out of Scope table lines 47-53, and the Traceability table lines 59-71)
    - .planning/phases/26-active-expired-row-actions/26-CONTEXT.md (D-01 descope rationale, D-02 Restore, D-03 rewritten ROWACT-03 — the authority)
    - .planning/milestones/v1.4-REQUIREMENTS.md (Phase 25 descope precedent: the `[~] **BRAND-01** *(descoped)*:` strike pattern at lines 53-55 + the "Coverage: 19/22 requirements active ... BRAND-01/02/03 descoped" tally at line 128 + the BRAND-01 traceability row "⊘ Descoped (was Phase 25)" at line 119 — mirror this exact wording style)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md to reflect the Archive-only descope (D-01), mirroring how v1.4-REQUIREMENTS.md handled BRAND-01/02/03:
      1. Add a short scope-reduction note (blockquote, "> ") at the top of the "### Proposal Row Actions (ROWACT)" section stating the phase was descoped during the 2026-05-30 discussion from Archive + Delete to Archive-only; the per-row Delete is dropped (D-01); a per-row Restore is added (D-02); cite 26-CONTEXT.md as the authority.
      2. ROWACT-02: do NOT remove the line. Rewrite it to the descoped form using the Phase 25 `[~] ... *(descoped)*` style: "- [~] **ROWACT-02** *(descoped 2026-05-30, D-01)*: ~~per-row Delete on finalized rows~~ — superseded by ROWACT-01 Archive. For a finalized proposal the only reversible backend op is soft-delete; Archive and Delete collapse to the same operation, so a single Archive button ships instead. Hard-delete remains forbidden by 10-year PDF retention." Keep the ROWACT-02 ID visible.
      3. ROWACT-03: rewrite to "- [ ] **ROWACT-03**: Active and expired rows expose ONLY Archive (no Edit, no Delete — finalized proposals are immutable and Delete is descoped per D-01); draft rows retain their existing Edit + Archive + Delete set unchanged."
      4. Add a NEW requirement immediately after ROWACT-04: "- [ ] **ROWACT-05**: From the Archivees view (/proposals?archived=1), a partner can RESTORE a soft-deleted proposal via a per-row Restore icon button, returning it to the Actives list in place (D-02)."
      5. ROWACT-01 and ROWACT-04: leave wording intact (still accurate).
      6. Out of Scope table: add a row — "| Per-row Delete on finalized rows | Descoped 2026-05-30 (D-01) — collapses into Archive given the single soft-delete state; revisitable as its own phase (separate archivedAt vs deletedAt product decision) |".
      7. Traceability table: change ROWACT-02's Status cell to "⊘ Descoped (D-01)"; add a row "| ROWACT-05 | Phase 26 | Mapped |"; update the Coverage block (was "v1.5 requirements: 6 total / Mapped: 6") to reflect ROWACT-05 added and ROWACT-02 descoped (e.g. "7 defined; 6 active + 1 descoped; mapped: 6 active + ROWACT-05"). Update the "Last updated" footer with 2026-05-30 + a one-line descope note.
    Do not touch the UIFIX or Future Requirements sections except the Coverage tally if it counts ROWACT.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && grep -ci "descoped" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "ROWACT-02" .planning/REQUIREMENTS.md >= 2 (still present in the requirement block AND traceability table, marked descoped — NOT removed).
    - Source: grep -ci "descoped" .planning/REQUIREMENTS.md >= 2 (the ROWACT-02 line + Out-of-Scope row + traceability cell).
    - Source: grep -c "ROWACT-05" .planning/REQUIREMENTS.md >= 2 (new Restore requirement + traceability row).
    - Source: grep -ci "only Archive" .planning/REQUIREMENTS.md >= 1 (ROWACT-03 rewritten to Archive-only).
  </acceptance_criteria>
  <done>REQUIREMENTS.md shows ROWACT-02 descoped (not dropped), ROWACT-03 rewritten to Archive-only, ROWACT-05 added for Restore, Out-of-Scope + Traceability + Coverage updated.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP.md Phase 26 goal + success criteria to Archive-only</name>
  <read_first>
    - .planning/ROADMAP.md (the "### Phase 26: Active/Expired Row Actions" block at lines 329-341: Goal, Requirements line, the 4 success criteria; AND the v1.5 phase-list bullet at line 90 "Archive + Delete icon buttons on non-draft proposal rows"; AND the footer "Last updated" line near 391)
    - .planning/ROADMAP.md (the Phase 25 descope blockquote at lines 306-311 — copy its "> **Scope reduced 2026-05-30 (discussion).**" tone). NOTE: Plan 03 has ALREADY added a similar blockquote + a Plans list under the Phase 26 block (lines ~342-359 inserted by the planner). Verify whether that block exists; if it does, do NOT duplicate it — edit the Goal/Requirements/criteria above it instead.
    - .planning/phases/26-active-expired-row-actions/26-CONTEXT.md (D-01/D-02/D-03/D-06 — the new scope)
  </read_first>
  <action>
    Edit .planning/ROADMAP.md "### Phase 26: Active/Expired Row Actions". (The planner already inserted a scope-reduction blockquote + a Plans list beneath criterion 4 — confirm it is present and correct; do not add a second one.)
      1. Rewrite the Goal line to: "**Goal:** Partners can ARCHIVE any active or expired proposal directly from the /proposals row (and RESTORE soft-deleted proposals from the Archivees view), using the same icon-button pattern already wired for draft rows — with the ADMIN-09 envelope held throughout." (Remove "or delete".)
      2. Update the Requirements line to: "**Requirements:** ROWACT-01, ROWACT-03, ROWACT-04, ROWACT-05 *(ROWACT-02 descoped)*".
      3. Rewrite the Success Criteria:
         - Criterion 1: keep (archiving an active/expired row moves it to Archivees without full-page navigation; row leaves Actives and appears in Archivees).
         - Criterion 2 (currently per-row Delete with confirmation): REPLACE with a Restore criterion — "From the Archivees view, clicking the per-row Restore icon button on a soft-deleted proposal returns it to the Actives list in place (no full-page navigation)."
         - Criterion 3: rewrite to "Draft rows continue to show Edit + Archive + Delete unchanged; active and expired rows show ONLY Archive (no Edit, no Delete)."
         - Criterion 4 (ProposalRowDto carries no commission; grep-contract suite green): keep verbatim.
         - Add a new criterion capturing D-06: "Clicking an active/expired row body still opens the proposal detail page; the Archive/Restore icon button acts without navigating (stopPropagation)."
      4. Update the v1.5 phase-list bullet at line 90 from "Archive + Delete icon buttons on non-draft proposal rows; ADMIN-09 envelope held (19-gate grep suite stays green)" to "Archive icon button on non-draft proposal rows + Restore in Archivees (Delete descoped); ADMIN-09 envelope held (19-gate grep suite stays green)".
      5. Update the ROADMAP footer "Last updated" line with 2026-05-30 noting the Phase 26 Archive-only descope.
    Do not edit any other phase block.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && sed -n '/### Phase 26:/,/### Phase 27:/p' .planning/ROADMAP.md | grep -ci "restore"</automated>
  </verify>
  <acceptance_criteria>
    - Source: within the Phase 26 block, the Goal line no longer offers "delete" — sed -n '/### Phase 26:/,/### Phase 27:/p' .planning/ROADMAP.md | grep -i "Goal:" shows Archive/Restore, not "or delete".
    - Source: the Phase 26 block contains "ROWACT-05" and "descoped" — grep on the extracted block both >= 1.
    - Source: a success criterion mentions Restore from the Archivees view — extracted block grep -ci "restore" >= 1.
    - Source: a success criterion captures body-click-opens-detail / stopPropagation (D-06) — extracted block grep -ci "stopPropagation\|detail page" >= 1.
    - Source: grep -c "Archive + Delete icon buttons" .planning/ROADMAP.md returns 0 (v1.5 bullet updated).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 26 goal + criteria describe Archive-only + Restore + body-click-opens-detail; the old Delete criterion is replaced; ROWACT-02 marked descoped; v1.5 bullet updated; no duplicated blockquote.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; edits .planning/*.md, no runtime surface, no code, no inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering (doc integrity) | REQUIREMENTS.md / ROADMAP.md | accept | Edits are planning docs only — no executable surface, no trust boundary crossed. The risk is mis-documenting scope; mitigated by acceptance-criteria greps asserting ROWACT-02 is marked descoped (not silently removed) and ROWACT-05 + Restore criteria are present. Severity LOW. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No packages, no code. |
</threat_model>

<verification>
- REQUIREMENTS.md: ROWACT-02 descoped (still present), ROWACT-03 Archive-only, ROWACT-05 Restore added, Out-of-Scope + Traceability + Coverage updated.
- ROADMAP.md Phase 26: goal/criteria reflect Archive + Restore + body-click-opens-detail; Delete criterion removed; v1.5 bullet updated; no duplicate blockquote.
</verification>

<success_criteria>
- Every original ROWACT ID has an explicit disposition; ROWACT-02 is descoped, not dropped.
- New Restore capability (D-02) is a tracked requirement + roadmap criterion.
- Docs match the Archive-only build that Plans 01-02 deliver.
</success_criteria>

<output>
Create `.planning/phases/26-active-expired-row-actions/26-03-SUMMARY.md` when done.
</output>
