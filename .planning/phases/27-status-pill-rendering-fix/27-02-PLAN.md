---
phase: 27-status-pill-rendering-fix
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: []
autonomous: false
requirements: [UIFIX-02, UIFIX-03]
must_haves:
  truths:
    - "The home 'Propositions récentes' chip displays its full label with no text clipping, no vertical misalignment, and no fixed-width artifact across desktop widths in BOTH light and dark mode (UIFIX-02 visual confirmation)."
    - "The /proposals table chip renders content-hugging on every row variant (draft / active / expired / archived) in BOTH light and dark mode, with no regression from the Phase 26 row-action slots (UIFIX-03 re-verification)."
    - "If UIFIX-03 re-verification reveals a real regression, the regression is fixed at the consumer grid track (ProposalRow/.list-row), not by changing chip colors."
  artifacts: []
  key_links:
    - from: "home /"
      to: "/proposals"
      via: "both surfaces render StatusChip trailing + content-hugging — visual parity"
      pattern: "StatusChip"
---

<objective>
Verify the status-pill rendering on BOTH partner surfaces in light AND dark mode,
closing the two ROADMAP Phase 27 success criteria. UIFIX-02 (home recent list)
was source-fixed in Plan 27-01 and needs visual confirmation that the chip shows
its full label with no clipping / misalignment / stretch. UIFIX-03 (`/proposals`
table) was fixed in Phase 25 (`max-content` track) and Phase 26 then added
`actionsSlot` / `draftActionsSlot` to the row — this is a RE-VERIFICATION on the
post-Phase-26 DOM to confirm the row-action slots did not disturb chip sizing on
any row variant.

This phase is sizing/layout only — chip colors, variants, chrome, and palette are
frozen (Phase 25 D-03 + this phase's out-of-scope). If UIFIX-03 re-verification
surfaces a genuine regression, fix it at the consumer grid track only; otherwise
this plan is verification-only.

Purpose: closes UIFIX-02 (visual) + UIFIX-03 (re-verification) — both ROADMAP
Phase 27 success criteria.
Output: human-verify confirmation; optional consumer-track fix ONLY if a real
regression is found (no chip color changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-status-pill-rendering-fix/27-CONTEXT.md
@.planning/phases/27-status-pill-rendering-fix/27-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UIFIX-02 (home recent list): the "Propositions récentes" chip was reordered to
    trailing and moved into a content-hugging grid track (Plan 27-01). UIFIX-03
    (/proposals): no code change expected — re-verifying the Phase 25 max-content
    chip track survived the Phase 26 row-action slots. Both surfaces must be
    correct in light AND dark mode.
  </what-built>
  <how-to-verify>
    1. Run the dev server (npm run dev) and log in as a partner that has at least
       one active proposal, ideally also a draft and an archived/expired one (so
       all chip variants are visible).
    2. HOME surface (UIFIX-02) — go to `/` and look at the "Propositions récentes"
       card:
       a. Column order reads clientCo → lcRef → amount → chip (chip on the RIGHT).
       b. The chip shows its full label with NO clipping — verify the longest
          label "Brouillon" (FR) renders complete; switch locale to EN and confirm
          the EN labels are also un-clipped.
       c. The chip HUGS its text — no wide empty pill, no fixed-width stretch.
       d. The chip is vertically centered with the row text (no misalignment).
       e. Resize the browser across a few desktop widths — chip stays hugging,
          text stays un-clipped (clientCo may truncate with ellipsis; that is
          expected and unchanged).
    3. /proposals surface (UIFIX-03) — go to `/proposals`:
       a. On active and expired rows, confirm the chip hugs its content and sits
          correctly alongside the new Archive icon button (Phase 26) — no
          stretch, no clipping, no overlap.
       b. Switch to drafts (`/proposals?drafts=1`) — confirm the draft chip hugs
          content next to the Edit/Archive/Delete buttons.
       c. Switch to Archivées (`/proposals?archived=1`) — confirm the archived/
          deleted chip hugs content next to the Restore button.
    4. DARK MODE — toggle the theme to Dark and repeat steps 2 and 3 on both
       surfaces. Confirm the chip sizing/alignment is identical in dark (colors
       are unchanged by design — only confirm no clipping/stretch/misalignment).
    5. Confirm the two surfaces now READ THE SAME (trailing, content-hugging chip).
  </how-to-verify>
  <resume-signal>
    Type "approved" if both surfaces show a content-hugging, full-label, aligned
    chip in light AND dark. Otherwise describe the exact defect (which surface,
    which row variant, which locale, which theme, clipping vs stretch vs
    misalignment) so a consumer-track fix can be scoped — do NOT request a chip
    color/variant change (out of scope).
  </resume-signal>
</task>

</tasks>

<verification>
- Human checkpoint confirms UIFIX-02 (home) full-label, content-hugging, aligned chip in light + dark.
- Human checkpoint confirms UIFIX-03 (/proposals) chip still content-hugging on draft/active/expired/archived rows in light + dark, no Phase-26 regression.
- If a regression fix was required, it touches only the consumer grid track (no chip color/variant/palette change); `npx tsc --noEmit` + `npm run lint:check` stay green.
</verification>

<success_criteria>
- ROADMAP Phase 27 criterion 1: home "Propositions récentes" chip shows full label, no clipping, no misalignment, no fixed-width artifact across desktop widths in light + dark.
- ROADMAP Phase 27 criterion 2: /proposals chip content-hugging like home, correct in light + dark, no regression on draft/archived rows.
</success_criteria>

<output>
Create `.planning/phases/27-status-pill-rendering-fix/27-02-SUMMARY.md` when done.
</output>
