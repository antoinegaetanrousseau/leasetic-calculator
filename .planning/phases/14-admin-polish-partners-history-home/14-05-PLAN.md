---
phase: 14-admin-polish-partners-history-home
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-04]
files_modified:
  - app/(admin)/[adminSegment]/history/page.tsx
  - app/(admin)/[adminSegment]/history/page.test.tsx
  - app/(admin)/[adminSegment]/history/CoefficientHistoryList.tsx
  - app/(admin)/[adminSegment]/history/CoefficientHistoryList.test.tsx
  - app/(admin)/[adminSegment]/history/CoefficientHistoryRow.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "Visiting /<seg>/history as admin renders the page with h1 `Historique des coefficients` and subtitle `Tous les changements de coefficients et commission`."
    - "Below the header, paginated rows from coefficient_history render in DESC order, 20 per page, via cursor-based pagination (?cursor=base64)."
    - "Each row shows the italic summary on the top line, meta (changed_at · changed_by) below, and a `Voir le détail →` trigger button on the right."
    - "Clicking the trigger expands the row to show the CoefficientDiffPanel in 'full' mode (2-col Avant|Après); the trigger label morphs to `↓ Masquer le détail`."
    - "Only ONE row can be expanded at a time (single-active per D-25): clicking row B closes row A automatically."
    - "Pressing Escape OR clicking the diff panel's `Fermer ×` button closes the expanded row; focus returns to the row's trigger button."
    - "When hasMore=true, a `Page suivante →` link to the next cursor renders at the bottom; when on a non-first page, a `← Page précédente` link renders too."
    - "Non-admin users hitting /<seg>/history are redirected by requireAdmin()."
  artifacts:
    - path: "app/(admin)/[adminSegment]/history/page.tsx"
      provides: "Server-component admin route with requireAdmin + dynamic='force-dynamic' + cursor URL extraction + listCoefficientHistory call (page size 20)"
      contains: "listCoefficientHistory"
    - path: "app/(admin)/[adminSegment]/history/CoefficientHistoryList.tsx"
      provides: "'use client' wrapper owning the single-active expandedRowId state per D-25"
      contains: "expandedRowId"
    - path: "app/(admin)/[adminSegment]/history/CoefficientHistoryRow.tsx"
      provides: "'use client' row component with trigger button + onToggle prop wired by parent list"
      contains: "aria-expanded"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "Remaining history.* keys: title, subtitle, row.viewDetail, row.hideDetail, pagination.next, pagination.previous, empty (~7 keys × FR + EN)"
      contains: "history.title"
  key_links:
    - from: "history/page.tsx"
      to: "src/lib/db/queries/coefficient-history.ts listCoefficientHistory + decode/encodeCoefficientHistoryCursor"
      via: "await listCoefficientHistory({ cursor: decoded, limit: 20 })"
      pattern: "listCoefficientHistory\\("
    - from: "CoefficientHistoryList.tsx (expand handler)"
      to: "app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx (Plan 14-04 artifact)"
      via: "<CoefficientDiffPanel mode=\"full\" row={expandedRow} lang={lang} onClose={() => setExpandedRowId(null)} />"
      pattern: "mode=\"full\""
    - from: "history/page.tsx → requireAdmin"
      to: "src/lib/auth/require.ts"
      via: "await requireAdmin() defence-in-depth at the top of the server component"
      pattern: "requireAdmin\\("
---

<objective>
Ship the standalone `/[adminSegment]/history` route per D-21..D-25 and UI-SPEC §5.3:

1. Build the server component at `app/(admin)/[adminSegment]/history/page.tsx` per PATTERNS.md §`history/page.tsx`:
   - Imports: `Metadata` from next, `Link` from next, `requireAdmin` from `@/lib/auth/require`, `getCurrentLang` + `t` from `@/lib/i18n`, `listCoefficientHistory` + `decodeCoefficientHistoryCursor` + `encodeCoefficientHistoryCursor` from `@/lib/db/queries/coefficient-history`, the local `<CoefficientHistoryList>`.
   - `export const dynamic = 'force-dynamic'`.
   - `export const metadata: Metadata = { title: 'Historique des coefficients — Leasétic Matrice', robots: { index: false, follow: false } }`.
   - Props: `{ params: Promise<{ adminSegment: string }>; searchParams: Promise<{ cursor?: string }> }`.
   - Body: `await params`, `await requireAdmin()`, `const lang = await getCurrentLang()`, `const sp = await searchParams`, decode `cursor` if present, call `await listCoefficientHistory({ cursor, limit: 20 })`, encode the `nextCursor` if present, render the page shell (h1 + subtitle hero) wrapping `<CoefficientHistoryList rows={result.rows} hasMore={result.hasMore} nextCursorEncoded={encoded} currentCursor={sp.cursor ?? null} adminSegment={adminSegment} lang={lang} />`.

2. Build `<CoefficientHistoryList>` (`'use client'`) per UI-SPEC §5.3:
   - Owns the single-active expandedRowId state: `const [expandedRowId, setExpandedRowId] = useState<string | null>(null);`.
   - Renders `<div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>` containing one `<CoefficientHistoryRow>` per row, plus the pagination footer.
   - Single-active toggle per D-25: `onToggle={(id) => setExpandedRowId(prev => prev === id ? null : id)}` passed to each row; each row receives `isExpanded={row.id === expandedRowId}`.
   - Pagination per UI-SPEC §5.3.6: `display: flex; justify-content: space-between;` row containing (left) the `← Page précédente` link IF `currentCursor !== null` (back to bare `/history` works as page 1; the planner may include explicit previous-cursor support — recommendation: skip explicit previous-cursor; render the left link only when on a non-first page, routed to bare `/history`), and (right) `Page suivante →` IF `hasMore && nextCursorEncoded`.
   - Empty state per UI-SPEC §5.3 Empty: when `rows.length === 0 && !currentCursor`, render the centered empty-state card with `history.empty` copy. (When rows.length === 0 but `currentCursor` exists, the user has navigated past the last page — render the same empty state but also show the `← Page précédente` link.)
   - Escape key listener: when expandedRowId !== null, `document.addEventListener('keydown', e => { if (e.key === 'Escape') setExpandedRowId(null) })` — installed in a `useEffect`.
   - Focus management per UI-SPEC §5.3.5: on expand, `requestAnimationFrame(() => closeButtonRef.current?.focus())`; on collapse via Escape or Fermer ×, `triggerRef.current?.focus()`. Triggers + closeButton refs are owned by the row (the close button lives inside `<CoefficientDiffPanel mode="full">` — Plan 14-04 already wires the onClose callback; this plan wires the focus-return).

3. Build `<CoefficientHistoryRow>` (`'use client'`) per UI-SPEC §5.3.3 + §5.3.4:
   - Props: `{ row: CoefficientHistoryListRow; isExpanded: boolean; onToggle: () => void; lang: Lang }`.
   - Layout: `<article aria-labelledby={\`history-row-\${row.id}-summary\`}>` containing a `.card` wrapper (28px padding) with `border-color: var(--teal)` when isExpanded. Top line: italic body/default summary with `id={\`history-row-\${row.id}-summary\`}`. Bottom line: `display: flex; justify-content: space-between;` with meta on the left (`changedAt · changedByDisplay`) and the trigger button on the right (`type="button"`; text `Voir le détail →` when collapsed, `↓ Masquer le détail` when expanded; `aria-expanded={isExpanded}`; `aria-controls={\`history-diff-\${row.id}\`}`).
   - When `isExpanded`: render `<div id={\`history-diff-\${row.id}\`} role="region" style={{ marginTop: 24 }}><CoefficientDiffPanel mode="full" row={row} lang={lang} onClose={onToggle} /></div>`.
   - Per UI-SPEC §5.3.3: the row card itself is NOT clickable — only the explicit `Voir le détail →` trigger button toggles expansion (keeps the rest of the row text selectable for copying).
   - Hover state on collapsed rows: `border-color: var(--teal)` (per UI-SPEC §4.6 chrome table — same as the trigger hover indicator). When expanded, the teal border is permanent. (CSS-only via `:hover` pseudo-class — implement inline via a small style block in the component OR with a CSS module; the existing project does NOT use CSS modules — use inline `:hover` is impossible; use inline `style` for the persistent expanded border + a `className` like `.history-row` with a hover rule in globals.css OR accept a less-polished hover state. Recommendation: accept a static border for hover — the trigger underline on hover IS the affordance; the border-color hover is a v1.3+ polish.)

4. Add the remaining ~7 `history.*` i18n keys per UI-SPEC §6.6 to `src/lib/i18n/dictionaries.ts` in BOTH FR and EN:
   - `history.title` (`Historique des coefficients` / `Coefficient history`)
   - `history.subtitle` (`Tous les changements de coefficients et commission` / `All coefficient and commission changes`)
   - `history.row.viewDetail` (`Voir le détail →` / `View details →`)
   - `history.row.hideDetail` (`↓ Masquer le détail` / `↓ Hide details`)
   - `history.diff.summary.label` (`Résumé` / `Summary`) — optional, used as an inline sub-label if the executor decides to add one above the summary line; otherwise the summary stands alone and this key is added but unused (acceptable for i18n parity).
   - `history.pagination.next` (`Page suivante →` / `Next page →`)
   - `history.pagination.previous` (`← Page précédente` / `← Previous page`)
   - `history.empty` (`Aucun changement de coefficient pour le moment.` / `No coefficient changes yet.`)
   The 4 `coefficients.history.*` + 6 `history.diff.*` keys already landed in Plan 14-04.

5. Vitest tests:
   - `page.test.tsx` (server-component harness): mock `listCoefficientHistory` to return 3 rows + hasMore=true + nextCursor; assert (a) `requireAdmin()` is invoked; (b) the cursor query param is decoded and passed to the query; (c) `<CoefficientHistoryList>` mounts with the expected props.
   - `CoefficientHistoryList.test.tsx`:
     - 3-row render produces 3 `<CoefficientHistoryRow>` mounts.
     - Clicking row 1's trigger expands row 1; clicking row 2's trigger collapses row 1 and expands row 2 (single-active per D-25).
     - Pressing Escape collapses the currently expanded row.
     - When hasMore=true, the next-page link renders with the encoded cursor URL.
     - When currentCursor=null + rows=[], the empty-state card renders.

Purpose: ROUTE-02 success criterion #5 (per CONTEXT.md scope inflation: standalone `/history` route ADDED, D-21..D-25 accepted). Completes the history surface area together with Plan 14-04's sidebar.

Output: A working `/<seg>/history` route an admin can paginate through, expand rows to see the full diff, and close via Escape or Fermer ×.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md
@.planning/phases/14-admin-polish-partners-history-home/14-04-SUMMARY.md

# Source files this plan consumes
@app/(admin)/[adminSegment]/partners/page.tsx
@app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx
@app/(authed)/page.tsx
@src/lib/db/queries/coefficient-history.ts
@src/lib/auth/require.ts
@src/lib/i18n/format.ts
@src/lib/i18n/dictionaries.ts

<interfaces>
<!-- Cursor encode/decode (src/lib/db/queries/coefficient-history.ts lines 119-147) — already shipped Phase 12 -->
export type CoefficientHistoryCursor = { changedAt: string; id: string };
export function encodeCoefficientHistoryCursor(cursor: CoefficientHistoryCursor): string;
export function decodeCoefficientHistoryCursor(encoded: string): CoefficientHistoryCursor | null;

<!-- listCoefficientHistory (same file, line 158) — already shipped Phase 12 -->
export async function listCoefficientHistory(
  args: { cursor?: CoefficientHistoryCursor; limit: number }
): Promise<{ rows: CoefficientHistoryListRow[]; hasMore: boolean; nextCursor: CoefficientHistoryCursor | null }>;

<!-- CoefficientHistoryListRow shape (same file, line 107) -->
export interface CoefficientHistoryListRow {
  id: string;
  changedAt: Date;
  changedById: string;
  changedByDisplay: string | null;
  beforeJson: GlobalParamsSnapshot | null;
  afterJson: GlobalParamsSnapshot;
  summary: string;
}

<!-- CoefficientDiffPanel (Plan 14-04 artifact) — consumed verbatim in 'full' mode -->
import { CoefficientDiffPanel } from './CoefficientDiffPanel';
// Props: { row: CoefficientHistoryListRow; mode: 'full' | 'condensed'; lang: Lang; onClose?: () => void }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build /history server page + CoefficientHistoryList client component with single-active expand + pagination</name>
  <files>app/(admin)/[adminSegment]/history/page.tsx, app/(admin)/[adminSegment]/history/page.test.tsx, app/(admin)/[adminSegment]/history/CoefficientHistoryList.tsx, app/(admin)/[adminSegment]/history/CoefficientHistoryList.test.tsx, app/(admin)/[adminSegment]/history/CoefficientHistoryRow.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.3 (full page layout + row expansion semantics + single-active per D-25 + pagination + empty state + ARIA + focus management) + §6.6 (history.* i18n keys not yet added)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`history/page.tsx` (server-component shell + cursor extraction pattern)
    - app/(authed)/page.tsx lines 25-65 (cursor URL pattern; the canonical Phase 8 analog)
    - app/(admin)/[adminSegment]/partners/page.tsx (admin route shell pattern — for `requireAdmin()` + `dynamic = 'force-dynamic'` + metadata)
    - app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx (Plan 14-04 artifact — read once to verify prop signature)
    - app/(admin)/[adminSegment]/coefficients/HistoryTable.tsx — the Phase 9 history table's expand-state pattern (`Set<string>` toggle at lines 34, 50-57; THIS plan uses single-id state instead per D-25, but the keyboard interaction patterns transfer)
    - src/lib/db/queries/coefficient-history.ts — verify the exact signatures of encode/decode/list helpers
    - .planning/phases/14-admin-polish-partners-history-home/14-04-SUMMARY.md — confirm CoefficientDiffPanel artifact exists + signature
  </read_first>
  <behavior>
    - Test 1 (RED first — page.test.tsx, auth gate): `requireAdmin` mock called; when it throws (Next.js redirect), page does NOT render the list.
    - Test 2 (RED first — page.test.tsx, cursor decoding): when `searchParams.cursor` is set to a valid encoded string, `decodeCoefficientHistoryCursor` is called with that string and the result is passed to `listCoefficientHistory`.
    - Test 3 (RED first — page.test.tsx, list mount): `<CoefficientHistoryList>` receives `rows`, `hasMore`, `nextCursorEncoded`, `currentCursor`, `adminSegment`, `lang` props.
    - Test 4 (RED first — CoefficientHistoryList.test.tsx, 3-row render): mocking 3 rows produces 3 `<CoefficientHistoryRow>` mounts.
    - Test 5 (RED first — single-active expand per D-25): clicking row 1's trigger expands row 1 (aria-expanded=true on row 1, false on row 2). Clicking row 2's trigger then expands row 2 AND collapses row 1 (aria-expanded=true on row 2, false on row 1). Verify via DOM queries.
    - Test 6 (RED first — Escape closes expanded row): with row 1 expanded, dispatching a keydown Escape event on `document` collapses row 1 (aria-expanded=false on both).
    - Test 7 (RED first — diff panel mount on expand): when row 1 is expanded, the rendered output contains `<CoefficientDiffPanel mode="full">` mounted inside row 1 (assert via mocked diff-panel testid).
    - Test 8 (RED first — pagination, hasMore=true): when `hasMore=true && nextCursorEncoded='ABC'`, the next-page link renders with `href={\`/${adminSegment}/history?cursor=ABC\`}`.
    - Test 9 (RED first — pagination, currentCursor !== null): when `currentCursor !== null`, the `← Page précédente` link renders pointing to bare `/<seg>/history`.
    - Test 10 (RED first — empty state): when `rows=[] && currentCursor===null`, the empty-state card renders with `history.empty` copy.
    - Test 11 (compile-time): `_EnHasAllFrKeys` typechecks after adding ~7 new keys.
  </behavior>
  <action>
    Two-step TDD pass producing 3 source files + 2 test files:

    (a) RED — write all 11 test cases first across `page.test.tsx` and `CoefficientHistoryList.test.tsx`. Use:
       - Server-component harness from `partners/new/page.test.tsx` (Plan 14-02 artifact) or `verification/page.test.tsx` for the page test.
       - Hoisted mocks for `listCoefficientHistory`, `decodeCoefficientHistoryCursor`, `encodeCoefficientHistoryCursor`, `requireAdmin`, `getCurrentLang`, `t`.
       - Mocked `<CoefficientDiffPanel>` to a placeholder `<div data-testid="diff-panel-full" data-mode={mode} data-row-id={row.id} />` so we assert prop passing without exercising the full panel (its own tests cover that in Plan 14-04).
       - Mock data fixtures: 3 `CoefficientHistoryListRow` objects with distinct ids + summaries; a fixture `nextCursorEncoded='Y2hhbmdlZEF0LWlk'` for the pagination tests.
       Run `npx vitest run app/(admin)/[adminSegment]/history` — all cases fail (the page + list files don't exist yet).

    (b) GREEN — implement the 3 source files + update dictionaries:

       (i) `page.tsx` per PATTERNS.md §`history/page.tsx`:
       - Imports per the §`history/page.tsx` block (Metadata, requireAdmin, getCurrentLang, t, listCoefficientHistory + cursor helpers, the local CoefficientHistoryList).
       - `export const dynamic = 'force-dynamic'`.
       - `export const metadata: Metadata = { title: 'Historique des coefficients — Leasétic Matrice', robots: { index: false, follow: false } }`.
       - Page props per PATTERNS.md `<interfaces>` extension (params + searchParams both Promises in Next.js 16).
       - Body: `await params`, `await requireAdmin()`, `const lang = await getCurrentLang()`, `const sp = await searchParams`, `const cursorEncoded = sp.cursor ?? null`, decode if present, call `listCoefficientHistory({ cursor: decoded ?? undefined, limit: 20 })`, encode `nextCursor` if present.
       - Render the page shell: `<div style={{ maxWidth: 1040, margin: '0 auto', padding: '32px 0' }}>` containing the h1 (`heading/hero` style: 32px/700) with `t('history.title', lang)`, the subtitle `<p>` (`body/large`: 16px/400/--muted, marginTop 8) with `t('history.subtitle', lang)`, then `<CoefficientHistoryList rows={result.rows} hasMore={result.hasMore} nextCursorEncoded={encoded} currentCursor={cursorEncoded} adminSegment={adminSegment} lang={lang} />` wrapped with `marginTop: 32`.

       (ii) `CoefficientHistoryList.tsx` (`'use client'`):
       - Imports: `useState`, `useEffect`, `useRef` from React; `Link` from next; `t`, `type Lang` from `@/lib/i18n/dictionaries`; the local `<CoefficientHistoryRow>`; type `CoefficientHistoryListRow` from `@/lib/db/queries/coefficient-history`.
       - Props: `{ rows: CoefficientHistoryListRow[]; hasMore: boolean; nextCursorEncoded: string | null; currentCursor: string | null; adminSegment: string; lang: Lang }`.
       - State: `const [expandedRowId, setExpandedRowId] = useState<string | null>(null)`.
       - Escape-key listener: `useEffect` that attaches `document.addEventListener('keydown', e => { if (e.key === 'Escape' && expandedRowId) setExpandedRowId(null) })`. Cleanup on unmount.
       - Render: empty-state branch (`rows.length === 0`) → centered `.card` with `t('history.empty', lang)`. Otherwise: flex column gap-16 with one `<CoefficientHistoryRow>` per row passing `isExpanded={row.id === expandedRowId} onToggle={() => setExpandedRowId(prev => prev === row.id ? null : row.id)} lang={lang}`.
       - Pagination row at the bottom: `<div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 24 }}>`. Left: `{currentCursor && (<Link href={\`/${adminSegment}/history\`}>{t('history.pagination.previous', lang)}</Link>)}`. Right: `{hasMore && nextCursorEncoded && (<Link href={\`/${adminSegment}/history?cursor=${nextCursorEncoded}\`}>{t('history.pagination.next', lang)}</Link>)}`. Both links styled `body/default-medium` / `--teal`.

       (iii) `CoefficientHistoryRow.tsx` (`'use client'`):
       - Imports: `useRef` from React; `formatDate` from `@/lib/i18n/format`; `t`, `type Lang` from `@/lib/i18n/dictionaries`; `<CoefficientDiffPanel>` from `./CoefficientDiffPanel`; type `CoefficientHistoryListRow` from `@/lib/db/queries/coefficient-history`.
       - Props: `{ row: CoefficientHistoryListRow; isExpanded: boolean; onToggle: () => void; lang: Lang }`.
       - Outer: `<article aria-labelledby={\`history-row-\${row.id}-summary\`} style={{...card chrome: bg --surface, 1px border, radius 16, padding 28, borderColor: isExpanded ? 'var(--teal)' : 'var(--border)' }}>`.
       - Top line: `<p id={\`history-row-\${row.id}-summary\`} style={{ fontStyle: 'italic' }}>{row.summary}</p>`.
       - Bottom line (8px top margin): `<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: 8 }}>` containing:
         - Left: `<div style={{ color: 'var(--muted)', fontSize: 12.5, fontWeight: 500 }}><span>{formatDate(row.changedAt, lang)}</span><span> · </span><span>{row.changedByDisplay ?? '—'}</span></div>`.
         - Right: `<button type="button" ref={triggerRef} onClick={onToggle} aria-expanded={isExpanded} aria-controls={\`history-diff-\${row.id}\`} style={{ color: 'var(--teal)', fontSize: 14.5, fontWeight: 500, background: 'none', border: 'none', cursor: 'pointer' }}>{isExpanded ? t('history.row.hideDetail', lang) : t('history.row.viewDetail', lang)}</button>`.
       - When expanded: `<div id={\`history-diff-\${row.id}\`} role="region" style={{ marginTop: 24 }}><CoefficientDiffPanel mode="full" row={row} lang={lang} onClose={onToggle} /></div>`.
       - Focus management: `useRef<HTMLButtonElement | null>(null)` for the trigger; use a `useEffect` that watches `isExpanded` and refocuses the trigger when transitioning from true → false. (Diff panel's close button gets focus on expand via its own internal effect from Plan 14-04 — verify in 14-04 SUMMARY.)

    (c) GREEN — add the ~7 new `history.*` keys to `src/lib/i18n/dictionaries.ts` per UI-SPEC §6.6 (the keys NOT yet added by Plan 14-04):
       - `history.title`, `history.subtitle`, `history.row.viewDetail`, `history.row.hideDetail`, `history.diff.summary.label`, `history.pagination.next`, `history.pagination.previous`, `history.empty`.
       Run `npm run build` — `_EnHasAllFrKeys` typecheck passes.

       Run `npx vitest run app/(admin)/[adminSegment]/history` — all 11 cases pass (GREEN). Commit: `feat(14-05): ship standalone /history route with cursor pagination + single-active expand`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/history &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -q 'listCoefficientHistory' app/(admin)/[adminSegment]/history/page.tsx" &amp;&amp; bash -c "grep -q 'decodeCoefficientHistoryCursor' app/(admin)/[adminSegment]/history/page.tsx" &amp;&amp; bash -c "grep -q 'requireAdmin' app/(admin)/[adminSegment]/history/page.tsx" &amp;&amp; bash -c "grep -q 'expandedRowId' app/(admin)/[adminSegment]/history/CoefficientHistoryList.tsx" &amp;&amp; bash -c "grep -q 'mode=\"full\"' app/(admin)/[adminSegment]/history/CoefficientHistoryRow.tsx" &amp;&amp; bash -c "grep -q 'history.title' src/lib/i18n/dictionaries.ts"</automated>
  </verify>
  <done>`/[adminSegment]/history` server-renders for admins; cursor pagination works (page size 20, ?cursor= URL param); `<CoefficientHistoryList>` enforces single-active expand per D-25; Escape closes the expanded row; `<CoefficientDiffPanel mode="full">` mounts on row expansion with the close-button onClose wired to the list state; pagination links render conditionally on hasMore + currentCursor; empty state renders for `[]` + bare URL; all Vitest cases pass; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /[adminSegment]/history (GET) | Untrusted: `requireAdmin()` AUTH-15 gate at the top of the server component. |
| Browser → ?cursor=<base64> URL param | Untrusted: the cursor string is decoded server-side; malformed cursors must return `null` (Phase 12 contract — verify in `decodeCoefficientHistoryCursor` behavior). |
| Server → listCoefficientHistory query | Read-only SELECT against `coefficient_history`; no DML side effects. |
| Server → diff panel render (admin-only) | Per D-30: commission values from before_json/after_json render here — admin-gated surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Spoofing/Authorization | `/history/page.tsx` | mitigate | `await requireAdmin()` at the top of the server component (AUTH-15 defence-in-depth). Vitest Test 1 covers the redirect-throws path. |
| T-14-05-02 | Tampering (malformed cursor) | `?cursor=` URL param | mitigate | `decodeCoefficientHistoryCursor` returns `null` for malformed input (Phase 12 contract). The page falls through to a no-cursor `listCoefficientHistory({ limit: 20 })` call. No SQL injection — cursor decode produces a typed `{ changedAt: string; id: string }` consumed by Drizzle query builder. |
| T-14-05-03 | Information disclosure (ADMIN-09 D-29 — row LIST HTML) | Rendered `/history` list HTML (collapsed rows) | mitigate | Collapsed-row HTML contains: italic summary (from `generateDiffSummary` — MAY include commission percentage transitions per D-30 admin-only exception), changed_at, changed_byDisplay, and the trigger button label. Per D-30 the surface is admin-only and commission visibility is permitted INSIDE this gated surface. Plan 14-06's grep gate verifies the partner-list HTML does NOT contain commission values (the strict D-29 surface). The /history list HTML is intentionally permissive per D-30. |
| T-14-05-04 | Information disclosure (ADMIN-09 D-30 — diff panel HTML) | Expanded-row diff panel | accept | Diff panel renders before_json + after_json which include commission_pct. D-30 explicitly logs this as the admin-only exception. The surface is gated by `requireAdmin()` upstream. |
| T-14-05-05 | Tampering (open redirect) | Pagination links | accept | Hard-coded inline `\`/${adminSegment}/history?cursor=${nextCursorEncoded}\`` — partner-controlled input cannot reach the href slot beyond the encoded cursor (which is server-generated by `encodeCoefficientHistoryCursor`). |
| T-14-05-06 | DoS (large page size) | `listCoefficientHistory({ limit: 20 })` | mitigate | Hard-coded limit at the call site; the client cannot override. Limit is well within Drizzle/Neon performance envelope (1000s of rows per second for this table shape). |
| T-14-05-07 | XSS (stored, via row.summary OR row.changedByDisplay) | Row card text | mitigate | Both fields originate from admin-side data: summary is `generateDiffSummary` output (numeric values + FR field labels — no user input); changedByDisplay is `users.displayName ?? users.name ?? users.email` (admin-controlled). React default escaping applies. ASVS L1 V5.3.3 satisfied. |
| T-14-05-08 | Repudiation | Coefficient history audit trail | accept | Phase 12 `coefficient_history` table is append-only (TRIGGER-enforced REJECT on UPDATE/DELETE per Phase 12 D-03). This plan only READS — no write-side surface area. |
| T-14-05-09 | Information disclosure (cursor leakage) | Encoded cursor in URL | accept | Cursors encode `{changedAt, id}` of a `coefficient_history` row. These are admin-only fields (id is a UUID; changedAt is a timestamp). Even if a cursor leaks (e.g. via browser history sharing), it's useless without admin auth — `requireAdmin()` blocks anonymous access. |
| T-14-05-10 | Confused-deputy (cursor injection across users) | listCoefficientHistory | accept | `coefficient_history` is global (NOT scoped by user_id) — every admin sees the same history. Cursors are not user-scoped; no confused-deputy risk. |

**ADMIN-09 invariant for this plan:**
- Diff panel: commission values MAY render (D-30 exception, admin-gated).
- Row list collapsed HTML: summaries MAY mention commission transitions (D-30 — same admin-only surface).
- Plan 14-06 grep gate covers the partner-list (D-29 strict, no commission).
</threat_model>

<verification>
- `npx vitest run app/(admin)/[adminSegment]/history` — all 11+ cases pass.
- `npm run build` — production build OK; `_EnHasAllFrKeys` typecheck passes with the new keys.
- `npm run lint` — clean.
- Manual smoke (documented in SUMMARY): visit `/<seg>/history` → confirm rows render, click `Voir le détail →` → diff panel opens with full mode; click another row's trigger → first row collapses, second expands (single-active); press Escape → expanded row collapses, focus returns to its trigger; click `Page suivante →` → URL updates to `?cursor=...`, next 20 rows render.
</verification>

<success_criteria>
- ROUTE-02 success criterion #5 (the standalone-route portion): `/[adminSegment]/history` exists, paginates coefficient_history at 20/page via cursor URL params, expands rows to show CoefficientDiffPanel in full mode, single-active per D-25, Escape + Fermer × close the expanded row.
- Server component admin route discipline: `requireAdmin()` + `dynamic = 'force-dynamic'` + admin metadata (robots: { index: false, follow: false }).
- Cursor encode/decode reuses Phase 12 helpers (`encodeCoefficientHistoryCursor` / `decodeCoefficientHistoryCursor`) — no new cursor library.
- Shared `<CoefficientDiffPanel>` from Plan 14-04 imported in 'full' mode — single component source of truth.
- ~7 new `history.*` i18n keys land verbatim per UI-SPEC §6.6 in both FR and EN (the diff-panel subset shipped with Plan 14-04).
- All Vitest cases pass; build + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-05-SUMMARY.md` documenting:
- Files created (page.tsx + .test.tsx, CoefficientHistoryList.tsx + .test.tsx, CoefficientHistoryRow.tsx)
- The single-active expandedRowId pattern (verified against D-25)
- Cursor flow: URL → decode → query → encode next → render link
- Focus management approach (refocus trigger on collapse — confirm UI-SPEC §5.3.5 was achievable)
- Pagination polish: implemented `← Page précédente` as bare /history link (per discretion); document if explicit previous-cursor encoding is needed
- Border-color hover state on collapsed rows (deferred polish per UI-SPEC §5.3 — document the trade-off)
- Test count delta
- The ~7 i18n keys added
- Any deviations from D-21..D-25 (none expected; flag if so)
</output>
