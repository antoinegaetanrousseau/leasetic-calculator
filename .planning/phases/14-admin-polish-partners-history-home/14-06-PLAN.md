---
phase: 14-admin-polish-partners-history-home
plan: 06
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-04, 14-05]
files_modified:
  - app/(admin)/[adminSegment]/partners/AccountsList.tsx
  - app/(admin)/[adminSegment]/partners/AccountsList.test.tsx
  - app/(admin)/[adminSegment]/partners/page.tsx
  - src/components/proposals/ProposalsList.tsx
  - src/components/proposals/ProposalRow.tsx
  - app/(authed)/proposals/[id]/page.tsx
  - app/(authed)/proposals/[id]/page.test.tsx
  - src/lib/db/queries/users.ts
  - tests/admin-09-grep-contracts.test.ts
autonomous: false

requirements: [ROUTE-02]

must_haves:
  truths:
    - "The partner list (AccountsList.tsx, now in partners/) renders a gold 'invité.e' StatusChip on rows whose user is in the listInvitedPartners() result set."
    - "The partner list 'Créer un partenaire' CTA is a <Link href=`/${adminSegment}/partners/new`> styled as .btn-green — NOT a <button> that opens a modal."
    - "CreatePartnerModal.tsx file stays in the repo per D-10 (shelf code) but its rendering/triggering is removed from AccountsList.tsx."
    - "ProposalsList rows render <StatusChip variant={deriveDisplayStatus(row)} /> instead of ad-hoc ValidityChip/DeletedChip pairing (per D-27)."
    - "The proposal detail page header renders <StatusChip variant={deriveDisplayStatus(proposal)} /> next to the title (per D-28)."
    - "An automated test (tests/admin-09-grep-contracts.test.ts) verifies the rendered HTML of: partner list, /partners/new form, admin home, and /history list rows contains ZERO commission-value strings (D-29 strict). The diff panel + coefficients editor are EXCLUDED from the gate (D-30 admin-only exception)."
    - "A color-contrast measurement task (human checkpoint) confirms the diff-panel changed-row composite (--ink weight 600 over rgba(224,133,48,0.10) on --surface) passes WCAG 2.1 AA in both light and dark themes."
  artifacts:
    - path: "app/(admin)/[adminSegment]/partners/AccountsList.tsx"
      provides: "Updated partner list with StatusChip 'invited' integration + CTA <Link> swap (modal CTA removed)"
      contains: "StatusChip"
    - path: "app/(admin)/[adminSegment]/partners/page.tsx"
      provides: "Server-component page now also calls listInvitedPartners() and passes invitedUserIds:Set<string> to AccountsList"
      contains: "listInvitedPartners"
    - path: "src/components/proposals/ProposalsList.tsx"
      provides: "Row-level StatusChip wiring via deriveDisplayStatus"
      contains: "deriveDisplayStatus"
    - path: "src/components/proposals/ProposalRow.tsx"
      provides: "Render slot uses <StatusChip /> instead of ad-hoc ValidityChip/DeletedChip composition"
      contains: "StatusChip"
    - path: "app/(authed)/proposals/[id]/page.tsx"
      provides: "Proposal detail header renders <StatusChip /> next to the title"
      contains: "StatusChip"
    - path: "tests/admin-09-grep-contracts.test.ts"
      provides: "Vitest grep-contract suite verifying zero commission-value leakage across Phase 14 non-exempt surfaces"
      contains: "ADMIN-09"
  key_links:
    - from: "app/(admin)/[adminSegment]/partners/page.tsx"
      to: "src/lib/db/queries/users.ts listInvitedPartners"
      via: "await listInvitedPartners() called server-side; result IDs passed as Set to AccountsList"
      pattern: "listInvitedPartners\\(\\)"
    - from: "src/components/proposals/ProposalRow.tsx"
      to: "src/lib/db/queries/proposals.ts deriveDisplayStatus"
      via: "deriveDisplayStatus(row) → variant prop on <StatusChip>"
      pattern: "deriveDisplayStatus"
    - from: "AccountsList.tsx CTA"
      to: "/${adminSegment}/partners/new (Plan 14-02 route)"
      via: "<Link href={…} className=\"btn-green\">"
      pattern: "/partners/new"
---

<objective>
Close ROUTE-02 success criteria #2 + #4 + the ADMIN-09 strict-invariant verification:

1. Partner list (`AccountsList.tsx`, now under `partners/` from Plan 14-01) per UI-SPEC §5.7 + D-26 + D-11:
   - **StatusChip 'invited' integration (D-26):** the partner-list server-side page (`partners/page.tsx`) calls `listInvitedPartners()` (Phase 12 helper) to obtain the set of partner user IDs in invited state. Wraps the ID list as a `Set<string>` and passes to `<AccountsList invitedUserIds={…} />` as a new prop. The list component replaces its current ad-hoc `<span className="chip chip-{active|disabled}">` rendering (Phase 9 line ~356) with `<StatusChip variant={invitedUserIds.has(user.id) ? 'invited' : user.isDisabled ? 'disabled' : 'active'} label={…} />` using i18n keys `chip.invited` (Plan 14-01 artifact) / `chip.disabled` / `chip.active` (existing Phase 9 keys).
   - **CTA swap (D-11):** the `<button onClick={() => setShowCreate(true)}>` (Phase 9 line ~276-290) becomes `<Link href={\`/${adminSegment}/partners/new\`} className="btn-green">` with the same `<UserPlus />` icon + label `t('admin.accounts.create.btn', lang)`. The button's createBtnRef is no longer needed.
   - **D-10 shelf-code discipline:** the `import { CreatePartnerModal }` line AND the `{showCreate && <CreatePartnerModal …/>}` render block STAY in `AccountsList.tsx` (shelf code per D-10). The `useState` for `showCreate` may stay or be removed — recommendation: REMOVE the state since nothing triggers it, but keep the import + an unconditional dead `null && <CreatePartnerModal {...staleProps} />` is NOT acceptable (lint/build would error). Practical resolution: delete the `useState showCreate` AND delete the modal render block — the import line stays so the file shows intent that the modal still exists for future use; lint warning for unused-import (if any) gets a single-line eslint-disable comment with explanatory note. Document the precise resolution in the SUMMARY.
   - Update `AccountsList.test.tsx` per D-12: assert (a) CTA is a `<Link>` with the correct href, NOT a button; (b) modal does NOT mount when CTA is clicked (Link navigation, not modal toggle); (c) standalone modal mount still works if explicitly instantiated (negative case keeps the modal code warm).

2. Proposal list + detail (D-27 + D-28) per UI-SPEC §5.8:
   - `src/components/proposals/ProposalRow.tsx`: REPLACE the existing `<ValidityChip>` / `<DeletedChip>` composition (lines 67-76) with `<StatusChip variant={deriveDisplayStatus(rowFull)} label={t(\`chip.${deriveDisplayStatus(rowFull)}\`, lang)} />`. Where `deriveDisplayStatus(row)` is the Phase 12 helper that returns one of `'draft' | 'active' | 'expired' | 'deleted'`. Note: the row needs to be the full `ProposalRow` shape for `deriveDisplayStatus` to work — verify the existing `ProposalRowDto` exposes the fields the helper needs (`status`, `deletedAt`, `createdAt`, `validityDays`, `lcRef`). If not, extend the DTO + the list query.
   - Per UI-SPEC §5.8, the `LanguageChip` STAYS next to the new StatusChip (no removal — they coexist).
   - `app/(authed)/proposals/[id]/page.tsx`: REPLACE the existing detail-page header chip block (lines ~129-145, currently ValidityChip OR DeletedChip + LanguageChip) with `<StatusChip variant={deriveDisplayStatus(proposal)} label={t(\`chip.${deriveDisplayStatus(proposal)}\`, lang)} /> + <LanguageChip … />` (same two chip slot, but StatusChip replaces the validity/deleted variants).
   - Existing FR/EN keys `chip.active`, `chip.draft`, `chip.expired`, `chip.deleted` are already wired (Phase 8/9/11). No new i18n keys required for the proposal-side rollout.
   - Update existing tests for `ProposalRow.tsx` + `proposals/[id]/page.tsx` to assert `<StatusChip>` mount per row + per detail header. Remove obsolete `<ValidityChip>` / `<DeletedChip>` assertions (those components remain on disk as Phase 8 code, NOT deleted — they're now unused, document in SUMMARY).

3. ADMIN-09 grep-contract test suite (`tests/admin-09-grep-contracts.test.ts` — new file):
   - Render each non-exempt Phase 14 surface to HTML (using `renderToString` from `react-dom/server` OR `@testing-library/react`'s `container.innerHTML`):
     - Partner list row HTML (mock 3 partners, one invited)
     - /partners/new form HTML (empty form mount)
     - Admin home HTML (3 AdminNavCards rendered)
     - /history list row HTML (collapsed rows only — diff panel + summaries are exempt per D-30)
     - **EXCEPTION:** the coefficients editor card + the diff panel (sidebar inline + /history full mode) + the sidebar summary cards + the /history row italic summaries are EXEMPT (D-30 admin-only). These surfaces MAY render commission values. The grep gate must EXCLUDE these zones.
   - For each non-exempt rendered HTML string, assert: `/\bcommission_pct\b/.test(html) === false` AND `/\b[0-9]+(\.[0-9]+)?\s*%\b/.test(html) === false` (the numeric-percentage pattern — but be careful: the partner list MAY show percentages in OTHER columns? Verify: Phase 9 partner list has no percentage columns. The /partners/new form has no percentages. Admin home has no percentages. So the strict numeric-percent gate is safe across these surfaces.).
   - Provide clear failure messages quoting the offending match (e.g. `expect(html).not.toMatch(/\\bcommission_pct\\b/) // Found 'commission_pct' in partner list HTML — D-29 strict violated`).
   - Per D-29: this suite is BLOCKING. Failure here means the Phase 14 ADMIN-09 invariant is violated.

4. Color-contrast measurement checkpoint (human-verify, blocking):
   - Per UI-SPEC §7.1: the diff-panel changed-row composite (`--ink` weight 600 over `rgba(224, 133, 48, 0.10)` over `--surface`) is a NEW combination that hasn't been measured. Measure in BOTH light mode (`--ink` = `#41423d` over the alpha-tint over `#ffffff` surface) AND dark mode (`--ink` = `#e6e9ef` over the same tint over `#161e2d` surface).
   - Use the WebAIM contrast checker OR `npx -y wcag-contrast` CLI. Required ratio: 4.5:1 (body text).
   - Document the measured ratios in the SUMMARY. If light or dark mode FAILS, the checker phase MUST flag a gap closure plan; planner does NOT preemptively soften the gold tint (the recommended ratio per UI-SPEC is "should pass 4.5:1 easily" — the measurement confirms).

Purpose: Closes ROUTE-02 #2 (modal CTA removed via behavior change), #4 (invited StatusChip on partner list + StatusChip on proposal lists + detail). Validates the ADMIN-09 D-29 strict invariant across all non-exempt Phase 14 surfaces. Confirms the new gold-pill diff-panel chrome meets WCAG 2.1 AA.

Output: A complete Phase 14 surface; verified absence of commission leakage; visual contrast assured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md
@.planning/phases/14-admin-polish-partners-history-home/14-01-SUMMARY.md
@.planning/phases/14-admin-polish-partners-history-home/14-02-SUMMARY.md
@.planning/phases/14-admin-polish-partners-history-home/14-04-SUMMARY.md
@.planning/phases/14-admin-polish-partners-history-home/14-05-SUMMARY.md

# Source files this plan modifies / reads
@app/(admin)/[adminSegment]/partners/page.tsx
@app/(admin)/[adminSegment]/partners/AccountsList.tsx
@app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx
@src/components/proposals/ProposalsList.tsx
@src/components/proposals/ProposalRow.tsx
@src/components/proposals/ValidityChip.tsx
@src/components/proposals/DeletedChip.tsx
@app/(authed)/proposals/[id]/page.tsx
@src/components/ui/StatusChip.tsx
@src/lib/db/queries/users.ts
@src/lib/db/queries/proposals.ts
@src/lib/i18n/dictionaries.ts

<interfaces>
<!-- listInvitedPartners (src/lib/db/queries/users.ts) — already shipped Phase 12 -->
export interface InvitedPartnerRow {
  id: string;
  email: string;
  displayName: string | null;
  // … other fields
}
export async function listInvitedPartners(): Promise<InvitedPartnerRow[]>;

<!-- deriveDisplayStatus (src/lib/db/queries/proposals.ts line 551) — already shipped Phase 12 -->
export type DisplayStatus = 'draft' | 'active' | 'expired' | 'deleted';
export function deriveDisplayStatus(row: ProposalRow): DisplayStatus;

<!-- StatusChip (Plan 14-01 artifact — variant union extended to include 'invited') -->
export interface StatusChipProps {
  variant: 'active' | 'draft' | 'expired' | 'disabled' | 'invited';
  label: string;
}

<!-- AccountsList.tsx props extension (Phase 14 — pass invitedUserIds Set) -->
export interface AccountsListProps {
  partners: PartnerWithCount[];           // existing
  lang: Lang;                              // existing
  adminSegment: string;                    // existing
  invitedUserIds: Set<string>;            // NEW Phase 14
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partner list — StatusChip 'invited' integration + CTA swap (button → Link)</name>
  <files>app/(admin)/[adminSegment]/partners/page.tsx, app/(admin)/[adminSegment]/partners/AccountsList.tsx, app/(admin)/[adminSegment]/partners/AccountsList.test.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/partners/page.tsx (the moved Phase 9 page — locate existing data-fetch flow)
    - app/(admin)/[adminSegment]/partners/AccountsList.tsx (full 518 lines — locate chip rendering at line ~354-371 + CTA button at line ~276-290 + existing showCreate state + CreatePartnerModal import + render block at line ~507-515)
    - app/(admin)/[adminSegment]/partners/AccountsList.test.tsx (existing test cases — extend, don't replace)
    - src/lib/db/queries/users.ts — `listInvitedPartners` signature (already in `<interfaces>`)
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.7 (visual contract) + §6.7 (chip.invited copy, already in dictionaries from Plan 14-01)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`AccountsList.tsx` (the StatusChip integration excerpt + the button-to-Link swap excerpt)
    - .planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md D-10, D-11, D-12, D-26
  </read_first>
  <behavior>
    - Test 1 (RED first — page test): partners/page.tsx calls `listInvitedPartners()` and passes the result IDs as a `Set<string>` to `<AccountsList invitedUserIds={…}>`.
    - Test 2 (RED first — AccountsList): given partners [A, B, C] and invitedUserIds = new Set(['A']), the rendered output shows `<StatusChip variant="invited">` on A's row, `<StatusChip variant="active">` on B + C (assuming B and C are not disabled).
    - Test 3 (RED first — AccountsList): given a disabled partner D (with invitedUserIds not containing D's id), the rendered output shows `<StatusChip variant="disabled">` on D's row.
    - Test 4 (RED first — AccountsList CTA): the "Créer un partenaire" CTA is a `<Link href={\`/${adminSegment}/partners/new\`}>`, NOT a `<button>`. Asserted via `container.querySelector('a.btn-green[href*="partners/new"]')` returning truthy AND `container.querySelector('button.btn-green')` returning falsy at the CTA position. (D-12 part a.)
    - Test 5 (RED first — AccountsList CTA): clicking the CTA does NOT mount the CreatePartnerModal in the DOM (since Link navigates, not opens modal). Verified via after-click DOM scan for the modal's root element. (D-12 part b.)
    - Test 6 (RED first — D-12 part c, modal still mountable standalone): rendering `<CreatePartnerModal {...props}>` directly (NOT through AccountsList) still works — the modal hasn't been deleted from the repo. Quick smoke render asserting the modal mounts with no exceptions.
    - Test 7 (existing tests preserved): all pre-Phase-14 AccountsList tests for the partner-list CRUD (create, reissue invite, password reset, enable/disable, delete) continue to pass.
  </behavior>
  <action>
    Two-step TDD:

    (a) RED — extend `AccountsList.test.tsx` with tests 1-6 above. Test 1 modifies the page test (or adds a new one if no page test exists). Test 6 is a small `describe('CreatePartnerModal — D-10 shelf code preservation')` block ensuring the modal can still be rendered standalone (negative case). Run `npx vitest run app/(admin)/[adminSegment]/partners` — new cases fail (the page doesn't pass invitedUserIds; AccountsList still uses ad-hoc chips + button CTA).

    (b) GREEN — three coordinated edits:

       (i) `app/(admin)/[adminSegment]/partners/page.tsx`: after the existing `listPartnersWithCounts()` call, add `const invitedRows = await listInvitedPartners(); const invitedUserIds = new Set(invitedRows.map(r => r.id));`. Pass `invitedUserIds={invitedUserIds}` to `<AccountsList>`. Add the import `import { listInvitedPartners } from '@/lib/db/queries';` (verify the export is on the barrel; if not, import directly from `@/lib/db/queries/users`).

       (ii) `app/(admin)/[adminSegment]/partners/AccountsList.tsx`:
         - Add `invitedUserIds: Set<string>` to the props interface; destructure in the function signature.
         - Add import `import { StatusChip } from '@/components/ui/StatusChip';`.
         - Add import `import Link from 'next/link';` if not already imported.
         - REPLACE the existing chip rendering at lines ~354-371 (the `<span className={\`chip ${isDisabled ? 'chip-disabled' : 'chip-active'}\`}>`) with the 3-branch ternary per UI-SPEC §5.7:
           ```
           {invitedUserIds.has(user.id)
             ? <StatusChip variant="invited" label={t('chip.invited', lang)} />
             : user.isDisabled
               ? <StatusChip variant="disabled" label={t('chip.disabled', lang)} />
               : <StatusChip variant="active" label={t('chip.active', lang)} />}
           ```
         - REPLACE the CTA button at lines ~276-290 with a `<Link>` per PATTERNS.md §`AccountsList.tsx`:
           ```
           <Link href={`/${adminSegment}/partners/new`} className="btn-green" style={{ display: 'inline-flex', alignItems: 'center', gap: 8, marginLeft: 'auto' }}>
             <UserPlus size={16} strokeWidth={1.6} aria-hidden="true" />
             {t('admin.accounts.create.btn', lang)}
           </Link>
           ```
           Remove `createBtnRef` declaration if it's no longer used elsewhere. Remove the `useState showCreate` declaration and its toggle handler. Remove the `<CreatePartnerModal>` render block.
         - **D-10 disposition:** keep the `import { CreatePartnerModal } from './CreatePartnerModal';` line at the top of the file as a deliberate shelf-code marker. Add a single-line comment above it: `// D-10: shelf-code — modal preserved for future UX iterations; not currently triggered.`. If ESLint flags unused-import, suppress with `// eslint-disable-next-line @typescript-eslint/no-unused-vars` on the import line. Document the chosen approach in the SUMMARY.

       (iii) Verify `i18n` keys exist:
         - `chip.invited` — shipped by Plan 14-01.
         - `chip.disabled` — pre-existing Phase 9.
         - `chip.active` — pre-existing Phase 8/9.
         - `admin.accounts.create.btn` — pre-existing Phase 9. NO new i18n keys in this task.

    Run `npx vitest run app/(admin)/[adminSegment]/partners` — all cases pass (GREEN). Commit: `feat(14-06): wire StatusChip invited + Link CTA on partner list`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/partners &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -q 'listInvitedPartners' app/(admin)/[adminSegment]/partners/page.tsx" &amp;&amp; bash -c "grep -q 'invitedUserIds' app/(admin)/[adminSegment]/partners/AccountsList.tsx" &amp;&amp; bash -c "grep -q 'href=.*partners/new' app/(admin)/[adminSegment]/partners/AccountsList.tsx" &amp;&amp; bash -c "grep -c 'StatusChip variant=\"invited\"\\|StatusChip variant=\"disabled\"\\|StatusChip variant=\"active\"' app/(admin)/[adminSegment]/partners/AccountsList.tsx | grep -qE '^[1-9]'" &amp;&amp; bash -c "test -f app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx"</automated>
  </verify>
  <done>Partner list renders the 3-variant StatusChip per row (invited gold / disabled red / active green); the "Créer un partenaire" CTA is a `<Link>` to /partners/new with the .btn-green chrome; the CreatePartnerModal file stays on disk; its import line stays in AccountsList.tsx as shelf code; its render block + showCreate state are removed; all Vitest cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProposalsList + proposal detail header — StatusChip integration (D-27 + D-28)</name>
  <files>src/components/proposals/ProposalsList.tsx, src/components/proposals/ProposalRow.tsx, src/components/proposals/ProposalRow.test.tsx (existing or new), app/(authed)/proposals/[id]/page.tsx, app/(authed)/proposals/[id]/page.test.tsx (existing or new)</files>
  <read_first>
    - src/components/proposals/ProposalRow.tsx (full file — locate ValidityChip + DeletedChip render at lines 67-76)
    - src/components/proposals/ProposalsList.tsx (full file — locate row iteration + any DTO shape transformation)
    - src/components/proposals/ValidityChip.tsx + DeletedChip.tsx (the components being replaced — read to understand the prop signatures they expected, to confirm StatusChip can subsume their behavior)
    - app/(authed)/proposals/[id]/page.tsx lines 105-145 (the page header chip block — locate ValidityChip + DeletedChip + LanguageChip arrangement)
    - src/lib/db/queries/proposals.ts line 551 (`deriveDisplayStatus` signature + implementation)
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.8 (visual contract) + §6.7 (existing chip.* keys consumed)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`ProposalsList.tsx + proposals/[id]/page.tsx` (the StatusChip swap excerpt)
  </read_first>
  <behavior>
    - Test 1 (RED first — ProposalRow): given a row with `status='active'` + non-null `createdAt` + `validityDays=30` + null `deletedAt`, the rendered output contains a `<StatusChip variant="active">` (NOT a `<ValidityChip>`).
    - Test 2 (RED first — ProposalRow): given a row with `deletedAt` non-null, the rendered output contains `<StatusChip variant="deleted">` (NOT a `<DeletedChip>`).
    - Test 3 (RED first — ProposalRow): given a row with `status='draft'`, the rendered output contains `<StatusChip variant="draft">`. (This is a NEW case enabled by Phase 12 draft proposals.)
    - Test 4 (RED first — ProposalRow): given a row past its validity window, the rendered output contains `<StatusChip variant="expired">`.
    - Test 5 (RED first — proposals/[id] page): the detail-page header renders `<StatusChip variant={deriveDisplayStatus(proposal)} />` alongside the existing `<LanguageChip>`.
    - Test 6 (existing tests preserved): all pre-Phase-14 ProposalRow + proposals/[id] tests for non-chip behavior (delete button, restore button, copy-ref, etc.) continue to pass.
  </behavior>
  <action>
    Two-step TDD:

    (a) RED — extend existing `ProposalRow.test.tsx` + `proposals/[id]/page.test.tsx` (or create if they don't exist; the test harness pattern follows `verification/page.test.tsx`). Add tests 1-5 above. Use `deriveDisplayStatus` import to seed test fixtures (e.g. for `expired`: a row with `createdAt = 60 days ago` + `validityDays = 30` → derivable as expired by the helper). Run `npx vitest run src/components/proposals app/(authed)/proposals` — new cases fail.

    (b) GREEN — two edits:

       (i) `src/components/proposals/ProposalRow.tsx`:
         - Add imports: `import { StatusChip } from '@/components/ui/StatusChip';` and `import { deriveDisplayStatus, type DisplayStatus } from '@/lib/db/queries/proposals';` (also import `type DictKey` from `@/lib/i18n/dictionaries` for the chip label).
         - REPLACE the chip-render block at lines ~67-76 (the existing ValidityChip / DeletedChip composition) with: 
           ```
           const displayStatus = deriveDisplayStatus(rowFull);  // see DTO note below
           <StatusChip variant={displayStatus} label={t(`chip.${displayStatus}` as DictKey, lang)} />
           ```
         - **DTO compatibility:** verify the existing `ProposalRowDto` (or whatever the prop type is named) exposes the fields `deriveDisplayStatus(row: ProposalRow)` needs (`status`, `deletedAt`, `createdAt`, `validityDays`). If the DTO drops any of these, EITHER extend the DTO (update the list query to include them) OR derive the displayStatus server-side and pass it as a new `displayStatus: DisplayStatus` prop on each row. Recommendation: extend the DTO — keeps the helper as the single source of truth. Document the choice in SUMMARY.
         - The existing `<ValidityChip>` + `<DeletedChip>` components stay on disk (Phase 8 code, harmless dead code). Remove ONLY the JSX usage from ProposalRow.tsx.
         - The existing tooltip on `<ValidityChip>` (e.g. "Expires in N days") is currently lost in the swap — UI-SPEC §5.8 specifies "No new copywriting" but doesn't address tooltips. **Planner ruling:** the validity-countdown tooltip is a Phase 8 affordance not surfaced in v1.2 design; dropping it during the StatusChip swap is acceptable. Document the trade-off in SUMMARY.

       (ii) `app/(authed)/proposals/[id]/page.tsx`:
         - Replace the existing chip block at lines ~129-145 (the `{isDeleted ? <DeletedChip /> : <ValidityChip />}` ternary) with `<StatusChip variant={deriveDisplayStatus(proposal)} label={t(\`chip.${deriveDisplayStatus(proposal)}\` as DictKey, lang)} />`. The `<LanguageChip>` immediately following STAYS — it coexists with the StatusChip per UI-SPEC §5.8.
         - Add the same imports as in ProposalRow.tsx (`StatusChip`, `deriveDisplayStatus`, `type DictKey`).

    (c) The Phase 8 / 9 / 11 chip.* keys (`chip.active`, `chip.draft`, `chip.expired`, `chip.deleted`) are pre-existing. Verify they exist in both FR + EN dictionaries with the values per UI-SPEC §6.7. NO new i18n keys in this task.

    Run `npx vitest run src/components/proposals app/(authed)/proposals` — all cases pass (GREEN). Commit: `feat(14-06): swap ProposalRow + proposal detail to StatusChip via deriveDisplayStatus`.
  </action>
  <verify>
    <automated>npx vitest run src/components/proposals app/(authed)/proposals/\[id\] &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -q 'deriveDisplayStatus' src/components/proposals/ProposalRow.tsx" &amp;&amp; bash -c "grep -q 'StatusChip' src/components/proposals/ProposalRow.tsx" &amp;&amp; bash -c "grep -q 'StatusChip' app/(authed)/proposals/\[id\]/page.tsx"</automated>
  </verify>
  <done>ProposalRow renders `<StatusChip>` per row driven by `deriveDisplayStatus(row)`; proposal detail header renders `<StatusChip>` next to `<LanguageChip>`; the dead `ValidityChip.tsx` + `DeletedChip.tsx` files stay on disk (Phase 8 code); all Vitest cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ADMIN-09 grep-contract test suite (D-29 strict verification)</name>
  <files>tests/admin-09-grep-contracts.test.ts</files>
  <read_first>
    - .planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md D-29 + D-30 (exact invariant text)
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §1 (the routes Phase 14 owns) + §4.4 (the chip-invited semantic boundary)
    - app/(admin)/[adminSegment]/partners/AccountsList.tsx (post-Task-1 form — to render in test)
    - app/(admin)/[adminSegment]/partners/new/page.tsx + CreatePartnerForm.tsx (Plan 14-02 output)
    - app/(admin)/[adminSegment]/page.tsx (Plan 14-03 admin home)
    - app/(admin)/[adminSegment]/history/page.tsx + CoefficientHistoryList.tsx (Plan 14-05 output — collapsed rows only; the diff panel is D-30 exempt)
    - Existing Phase 9 STRIDE test files in the repo (if any) — use as a model for grep-contract test harness
  </read_first>
  <behavior>
    - Test 1 (RED first if no scaffold; GREEN once implemented): rendered partner-list HTML (3 mock partners, none invited has commission %, since partners don't have commission fields) — `/\b[0-9]+(\.[0-9]+)?\s*%/` matches ZERO. `/\bcommission_pct\b/` matches ZERO.
    - Test 2: rendered /partners/new form HTML — same two patterns match ZERO.
    - Test 3: rendered admin home HTML — `/\bcommission_pct\b/` matches ZERO. The literal "commission" word DOES appear (card title "Coefficients & commission" — chrome label, NOT a value). Test asserts `/\bcommission_pct\b/` is the strict check; the bare word `commission` is exempt because it's a chrome label not a data leak.
    - Test 4: rendered /history list HTML (collapsed rows only — render with `expandedRowId = null`) — `/\bcommission_pct\b/` matches ZERO. The italic summary MAY include the string "Commission" (capital C — as in `Commission : 5.00% → 5.50%`) — this is the D-30 admin-only exception. Test design choice: the /history list rows ARE admin-only and per D-30 the summary CAN show commission transitions. **Planner ruling:** include the /history list HTML in the grep gate but make it a separate `describe` block; the assertion is that `commission_pct` (the field key) doesn't appear (it shouldn't, since the summary is a natural-language sentence not the field key). The bare numeric-% in the summary IS allowed (D-30); the gate must NOT count bare percentages in the summary as a violation. **Refined gate for /history:** assert `/\bcommission_pct\b/.test(html) === false` AND `/_pct\b/.test(html) === false` (catches any JSON-field-key leakage); but allow percentage values inside the summary text.
    - Test 5: the diff panel (full mode) rendered HTML — EXEMPT per D-30. Test does NOT include the diff panel surface; document the exemption in the test file comments.
    - Test 6: the coefficients editor card on /coefficients — EXEMPT (pre-existing Phase 9 ADMIN-01; renders commission_pct in the editor form fields). NOT included.
    - Test 7: the CoefficientHistorySidebar inline expansion (condensed diff panel) — EXEMPT per D-30. NOT included.
  </behavior>
  <action>
    Create `tests/admin-09-grep-contracts.test.ts` (a new top-level test file — adjust path if the project's existing convention is to colocate; the repo uses colocated `*.test.tsx` next to source files, but a cross-cutting STRIDE-style test fits better at a top-level `tests/` directory; verify if `tests/` exists or create it). The file uses `vitest` + `@testing-library/react` (renderToString or container.innerHTML) + the production-ready imports (NOT mocks) of:
    - `<AccountsList>` (passing mock partners + mock invitedUserIds; mock lang/adminSegment)
    - `<CreatePartnerForm>` (mounted in isolation; the page wrapper not needed for the form HTML scan)
    - Admin home server component (use the page-test harness pattern from Plan 14-03 to render to HTML)
    - `<CoefficientHistoryList>` with `expandedRowId = null` (collapsed rows only)

    For each rendered HTML output, run the gates:
    ```
    expect(html, 'D-29: partner list HTML must contain no percentage values').not.toMatch(/\b[0-9]+(\.[0-9]+)?\s*%/);
    expect(html, 'D-29: partner list HTML must not surface commission_pct field').not.toMatch(/\bcommission_pct\b/);
    expect(html, 'D-29: partner list HTML must not contain _pct suffix on any JSON key').not.toMatch(/_pct\b/);
    ```

    Refine the gates per the per-surface specifics in Behavior Test 4. Add explanatory comments in the test file documenting WHY each gate exists (cite D-29 strict + D-30 exception).

    Run `npx vitest run tests/admin-09-grep-contracts.test.ts` — all gates pass (the upstream plans have already shipped surfaces that comply). If any gate FAILS, this is a Phase 14 D-29 violation: log it, stop, and surface to the checker for revision planning.
  </action>
  <verify>
    <automated>npx vitest run tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <done>The grep-contract test file ships; all gates pass against the Phase 14 surfaces (partner list, /partners/new, admin home, /history list rows); the diff panel + editor + sidebar inline-expansion are documented as D-30 exempt zones; CI now blocks any regression that leaks commission values into non-exempt Phase 14 surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Color-contrast measurement checkpoint — diff-panel changed-row composite</name>
  <what-built>
    Plans 14-04 + 14-05 introduced a new visual composite that hasn't been measured: the diff-panel changed-row pill — `--ink` weight 600 over `rgba(224, 133, 48, 0.10)` (10% gold tint) over `--surface`. UI-SPEC §7.1 flags this as a NEW measurement required for WCAG 2.1 AA. Per Phase 14 D-29 strict + UI-checker §7.1 + §12.7, the measurement MUST be confirmed in both light and dark themes before Phase 14 ships.
  </what-built>
  <how-to-verify>
    1. **Spin up local dev:** `cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run dev`. Open http://localhost:3000.
    2. **Authenticate as admin:** log in with the admin account; navigate to `/<seg>/history` where `<seg>` is your local admin segment (read from `.env.local`).
    3. **Open a row with changes:** click `Voir le détail →` on a row whose summary mentions a value change (e.g. "Commission : 5.00% → 5.50%"). The diff panel opens in full mode with the changed-cell pill rendered in gold tint.
    4. **Measure light mode:** open Chrome DevTools → use the Lighthouse Accessibility audit OR install the "WCAG Color contrast checker" Chrome extension (or use https://webaim.org/resources/contrastchecker/). Identify a changed-cell value (the bolded text on the gold-tint background). Pick the rendered foreground (`--ink` = `#41423d` in light mode) and the COMPOSITED background (the 10% gold-tint over white `--surface`). Pick the contrast ratio reported. EXPECTED: ≥ 4.5:1 (body-text WCAG AA threshold).
    5. **Measure dark mode:** toggle the theme via the sidebar theme switch (data-theme="dark"). Repeat the measurement. Foreground is now `--ink` = `#e6e9ef`; background is the 10% gold-tint over `--surface` dark = `#161e2d`.
    6. **Record both ratios:** document in your reply. Acceptable: both ≥ 4.5:1. If either fails: report the failing ratio + the surface. Phase 14 cannot ship until this clears WCAG AA.
    7. **Optional CLI verification:** for higher confidence, install `wcag-contrast` (`npx -y wcag-contrast <fg-hex> <bg-hex>`) and compute the COMPOSITED bg color (10% gold #e08530 over #ffffff → roughly #fbf2e9 — verify with a pixel-picker tool like macOS Digital Color Meter). Report both methods' ratios if they diverge.
  </how-to-verify>
  <resume-signal>Type "approved: light=X.X, dark=Y.Y" with the measured ratios, OR describe the failure (which mode, which ratio).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /[adminSegment]/partners (server-rendered list) | `requireAdmin()` upstream; the StatusChip 'invited' integration adds a new derived display state but no new auth surface. |
| Browser → /[authed]/proposals/{,/[id]} (proposal list + detail) | Existing partner-auth gate (`authedLayout` requires `session.user.role === 'partner' OR session.user.role === 'admin'`); the StatusChip swap doesn't alter auth. |
| Browser → /[adminSegment]/partners/new | CTA-clicked Link navigation (no POST until form-submit); Plan 14-02's `<form>` server-action gate is the actual trust boundary. |
| Color-contrast measurement | NOT a trust boundary — visual UX check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Spoofing/Authorization | Partner list `listInvitedPartners()` call | mitigate | Helper inherits existing Phase 12 D-12 server-side guard (the query runs in the server component under `requireAdmin()`). No new auth surface. |
| T-14-06-02 | Information disclosure (ADMIN-09 D-29) | Partner list rendered HTML — invitedUserIds: Set<string> | mitigate | The Set contains only user IDs (UUIDs). No commission values surface. Grep gate in Task 3 verifies. |
| T-14-06-03 | Information disclosure (D-29 strict) | All Phase 14 non-exempt surfaces | mitigate | Task 3's grep-contract test suite is the explicit gate. Any future Phase 14 patch that leaks commission_pct into a non-exempt surface fails CI. |
| T-14-06-04 | Information disclosure (D-29 strict — proposal list) | ProposalsList row HTML using `<StatusChip>` | mitigate | The chip label is `chip.{draft|active|expired|deleted}` — none of these are commission values. The chip variant is derived by `deriveDisplayStatus(row)` which consumes `status`, `deletedAt`, `createdAt`, `validityDays` — no commission fields. The proposal-list surface has been ADMIN-09 strict since Phase 8/9; this plan preserves that. |
| T-14-06-05 | Information disclosure (D-29 strict — proposal detail header) | proposals/[id] page header chip | mitigate | Same as T-14-06-04; the StatusChip + LanguageChip render no commission values. |
| T-14-06-06 | Tampering | CTA Link href hard-coded | accept | `\`/${adminSegment}/partners/new\`` — adminSegment is server-resolved from the route param; partner-controlled input cannot reach the href. |
| T-14-06-07 | Repudiation | StatusChip displayStatus derivation | accept | `deriveDisplayStatus` is a pure function over server-side row state; no audit log changes. Phase 12 audit-log discipline preserved. |
| T-14-06-08 | DoS | listInvitedPartners() on every partner-list render | accept | The query is bounded by users.role='partner' AND not-deleted AND last_login_at IS NULL — Phase 12 D-12 contract. Indexed; trivial query cost. |
| T-14-06-09 | CSRF / cross-site | CTA Link navigation | accept | GET navigation, no POST. CSRF inapplicable. The eventual form POST inside `/partners/new` is protected by Next.js server-action CSRF + Better Auth SameSite cookies (covered by Plan 14-02 STRIDE). |
| T-14-06-10 | Accessibility (WCAG 2.1 AA) | Diff-panel changed-row composite | mitigate | Task 4 (checkpoint) measures the new gold-tint composite in both light + dark modes. If either fails the 4.5:1 ratio, gap closure plan blocks ship. |
| T-14-06-11 | XSS (stored, via deriveDisplayStatus return value used in i18n key concatenation) | `t(\`chip.${displayStatus}\`)` pattern | accept | `displayStatus` is constrained by the TypeScript `DisplayStatus` union — only 4 literal strings can flow. The `as DictKey` cast is safe because the 4 keys all exist in the dictionary (verified by `_EnHasAllFrKeys`). |

**ADMIN-09 D-29 strict verification (Task 3):** the grep gate enforces zero `commission_pct` references AND zero `_pct` JSON-key suffixes in:
- Partner list HTML
- /partners/new form HTML
- Admin home HTML
- /history list row HTML (collapsed rows only; expanded diff panel is D-30 exempt)

The diff panel (sidebar inline + /history full mode) + the coefficients editor are EXEMPT per D-30 (admin-only surfaces, intentionally render commission for admin audit).
</threat_model>

<verification>
- `npx vitest run app/(admin)/[adminSegment]/partners` — all partner-list cases pass (including the new StatusChip 'invited' + Link CTA cases).
- `npx vitest run src/components/proposals app/(authed)/proposals/[id]` — all proposal cases pass (StatusChip on rows + detail header).
- `npx vitest run tests/admin-09-grep-contracts.test.ts` — all D-29 grep gates pass.
- `npm run build` + `npm run lint` clean.
- Color-contrast checkpoint approved (light + dark ratios documented).
- Full suite: `npx vitest run` — total count up from 788 (pre-Phase-14) to ~830-850 (Phase 14 plans add ~40-50 new cases combined per CONTEXT.md prediction).
</verification>

<success_criteria>
- ROUTE-02 #2 (modal CTA removed via behavior change): the partner-list CTA is a `<Link>` to `/partners/new`; the modal code stays per D-10.
- ROUTE-02 #4 (StatusChip rollout): the partner list renders the `invited` variant; ProposalsList + proposal detail use `<StatusChip>` driven by `deriveDisplayStatus`.
- ADMIN-09 D-29 strict invariant verified by automated grep test (`tests/admin-09-grep-contracts.test.ts`) across 4 non-exempt surfaces; the suite is part of CI.
- D-30 admin-only exceptions documented in the grep test (diff panel + editor + sidebar inline expansion).
- WCAG 2.1 AA color contrast confirmed for the diff-panel changed-row composite in both light and dark modes (Task 4 checkpoint).
- The `CreatePartnerModal.tsx` file remains on disk per D-10; the `ValidityChip.tsx` + `DeletedChip.tsx` files remain on disk (now unused, harmless).
- All Vitest cases pass; build + lint clean.
- Phase 14 ROUTE-02 closed (success criterion #3 partner-home MetricTiles deferred to v1.3 per CONTEXT.md — that deferral is logged in PROJECT.md, not closed by this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-06-SUMMARY.md` documenting:
- AccountsList.tsx final shape (StatusChip 3-variant ternary + Link CTA + D-10 shelf-code resolution)
- ProposalRow DTO extension (if any was required to surface fields for deriveDisplayStatus)
- Trade-off note: ValidityChip tooltip was dropped during the swap (acceptable v1.2; v1.3 polish candidate)
- The ADMIN-09 grep test design (which patterns; which surfaces; which exempts)
- Color-contrast measurement results from Task 4: light-mode ratio + dark-mode ratio
- Full Phase 14 vitest count delta (start: 788; end: ~830-850)
- Final Phase 14 sign-off: all 5 ROUTE-02 success criteria status (#1 ✓, #2 ✓, #3 partner-home tiles DEFERRED to v1.3, #4 ✓, #5 ✓ with scope-inflated /history)
- Any D-26..D-30 deviations (none expected; flag if so)
</output>
