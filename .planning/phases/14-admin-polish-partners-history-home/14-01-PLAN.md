---
phase: 14-admin-polish-partners-history-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(admin)/[adminSegment]/accounts
  - app/(admin)/[adminSegment]/partners
  - next.config.ts
  - app/globals.css
  - src/components/ui/StatusChip.tsx
  - src/components/ui/StatusChip.test.tsx
  - src/components/ui/Shell.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "The directory app/(admin)/[adminSegment]/accounts/ no longer exists; its contents now live under app/(admin)/[adminSegment]/partners/."
    - "Hitting /<seg>/accounts in a browser issues a 308 redirect to /<seg>/partners (preserving any sub-path)."
    - "The CSS class .chip-invited exists in app/globals.css with gold tint over 12% bg."
    - "The StatusChip variant union accepts 'invited' and renders <span class=\"chip chip-invited\">."
    - "Shell.tsx sidebar links resolve partners → /<seg>/partners and history → /<seg>/history (no longer the Phase 13 hotfix fallbacks)."
    - "Both FR and EN dictionaries contain the chip.invited key with values 'invité.e' / 'Invited'."
  artifacts:
    - path: "app/(admin)/[adminSegment]/partners/page.tsx"
      provides: "Moved partner list server-component page (was accounts/page.tsx)"
    - path: "app/(admin)/[adminSegment]/partners/AccountsList.tsx"
      provides: "Moved partner list client component (file name unchanged per D-04)"
    - path: "app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx"
      provides: "Shelved modal — moves with the directory but no longer triggered (D-10)"
    - path: "app/(admin)/[adminSegment]/partners/timeAgo.ts"
      provides: "Utility — moves with the directory"
    - path: "next.config.ts"
      provides: "redirects() block routing /:adminSegment/accounts/:path* → /:adminSegment/partners/:path* (308)"
      contains: "redirects"
    - path: "app/globals.css"
      provides: ".chip-invited CSS class declaration after .chip-draft"
      contains: ".chip-invited"
    - path: "src/components/ui/StatusChip.tsx"
      provides: "Extended variant union including 'invited'"
      contains: "'invited'"
  key_links:
    - from: "next.config.ts redirects()"
      to: "/${adminSegment}/partners"
      via: "permanent: true (emits 308)"
      pattern: "permanent:\\s*true"
    - from: "src/components/ui/StatusChip.tsx"
      to: "app/globals.css .chip-invited"
      via: "templated className `chip chip-${variant}`"
      pattern: "chip chip-\\$\\{variant\\}"
    - from: "src/components/ui/Shell.tsx adminHrefs"
      to: "/${adminSegment}/partners and /${adminSegment}/history"
      via: "adminHrefs object literal"
      pattern: "partners:\\s*`/\\$\\{adminSegment\\}/partners`"
---

<objective>
Establish Phase 14's structural foundation that every downstream plan depends on:

1. Rename `app/(admin)/[adminSegment]/accounts/` → `partners/` per D-01, moving all sub-files in a single atomic operation (page.tsx, AccountsList.tsx, CreatePartnerModal.tsx, timeAgo.ts, colocated tests). The component file `AccountsList.tsx` keeps its current name per D-04 to minimize churn.
2. Add a 308 permanent redirect in `next.config.ts` so `/<seg>/accounts/*` URLs continue to resolve for at least the v1.2 milestone of warm-cache time per D-02. Pick the `redirects()` config approach (NOT the stub-page approach) to keep the legacy directory fully deleted.
3. Add the `.chip-invited` CSS utility class to `app/globals.css` (gold-12% tint, identical chrome to `.chip-draft` but semantically distinct per UI-SPEC §4.4) and extend the `StatusChip` variant union to include `'invited'` per UI-SPEC §4.5. Update `StatusChip.test.tsx` to add the `invited` variant case.
4. Revert today's temporary Shell.tsx sidebar hotfix (commit `6809b1f`) per D-03: change `partners` href from `/${adminSegment}/accounts` to `/${adminSegment}/partners`, change `history` href from `/${adminSegment}/coefficients` to `/${adminSegment}/history`. Drop the now-stale comment block flagging the patch.
5. Add the new `chip.invited` i18n key (`invité.e` / `Invited`) to both FR and EN dictionaries.

Purpose: This plan creates the renamed directory + redirect + StatusChip surface area that every Wave-2 plan reads from or extends. Without it Wave-2 plans (especially 14-06) would step on each other modifying StatusChip + globals.css concurrently.

Output: A renamed directory tree, an extended primitive surface, a working legacy-URL redirect, sidebar links that resolve to their natural destinations, and the foundational i18n key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md

# Source files this plan modifies (read once at execution start)
@app/(admin)/[adminSegment]/accounts/page.tsx
@app/(admin)/[adminSegment]/accounts/AccountsList.tsx
@app/(admin)/[adminSegment]/accounts/CreatePartnerModal.tsx
@app/(admin)/[adminSegment]/accounts/timeAgo.ts
@next.config.ts
@app/globals.css
@src/components/ui/StatusChip.tsx
@src/components/ui/StatusChip.test.tsx
@src/components/ui/Shell.tsx
@src/lib/i18n/dictionaries.ts

<interfaces>
<!-- StatusChip current contract (extracted from src/components/ui/StatusChip.tsx) -->
export interface StatusChipProps {
  variant: 'active' | 'draft' | 'expired' | 'disabled';
  label: string;
}
export function StatusChip({ variant, label }: StatusChipProps) {
  return <span className={`chip chip-${variant}`}>{label}</span>;
}

<!-- .chip-draft analog in app/globals.css (insertion point: immediately after .chip-draft closing brace, ~line 374) -->
.chip-draft {
  background: rgba(224, 133, 48, 0.12);
  color: var(--gold);
}

<!-- Shell.tsx adminHrefs current shape (hotfix to revert, lines ~50-62) -->
const adminHrefs =
  role === 'admin' && adminSegment
    ? {
        home: `/${adminSegment}`,
        coefficients: `/${adminSegment}/coefficients`,
        partners: `/${adminSegment}/accounts`,      // ← revert to `/${adminSegment}/partners`
        history: `/${adminSegment}/coefficients`,   // ← revert to `/${adminSegment}/history`
      }
    : null;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename accounts/ → partners/, add 308 redirect, drop temp Shell hotfix</name>
  <files>app/(admin)/[adminSegment]/accounts (deleted), app/(admin)/[adminSegment]/partners/page.tsx, app/(admin)/[adminSegment]/partners/AccountsList.tsx, app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx, app/(admin)/[adminSegment]/partners/timeAgo.ts, app/(admin)/[adminSegment]/partners/AccountsList.test.tsx (if it exists), app/(admin)/[adminSegment]/partners/CreatePartnerModal.test.tsx (if it exists), next.config.ts, src/components/ui/Shell.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/accounts/* — every file in the directory (page.tsx, AccountsList.tsx, CreatePartnerModal.tsx, timeAgo.ts, and any *.test.tsx)
    - next.config.ts (current: 12 lines, no redirects block)
    - src/components/ui/Shell.tsx (lines 50-62 for the adminHrefs block + lines 55-60 for the stale hotfix comment block)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`next.config.ts` (308 redirect minimal idiom)
  </read_first>
  <behavior>
    - Test 1 (Vitest): a unit test for any file that imports from `@/app/(admin)/[adminSegment]/accounts/...` MUST now fail to find that path. (We won't add a new test here — we rely on the next.config.ts redirect smoke + tsc to catch it.)
    - Test 2 (manual/curl post-deploy, not in this task): `curl -I https://<host>/<seg>/accounts` returns `HTTP/2 308` with `location: /<seg>/partners`. Add NO new automated test for this — Next.js's redirects() behavior is framework-tested.
    - Test 3 (tsc + lint): `npm run build` and `npm run lint` pass after the move + Shell.tsx revert.
    - Test 4 (grep): `grep -rn "(admin)/\[adminSegment\]/accounts" app/ src/ --include="*.tsx" --include="*.ts" | grep -v '^#' | grep -v "/partners/"` returns ZERO matches (no leftover references to the old path inside source).
  </behavior>
  <action>
    Three atomic operations, in order:

    (a) Directory move per D-01. Use `git mv` so history follows (e.g. `git mv app/(admin)/[adminSegment]/accounts app/(admin)/[adminSegment]/partners`). Do NOT change file contents during the move. Verify every file in `accounts/` (page.tsx, AccountsList.tsx, CreatePartnerModal.tsx, timeAgo.ts, and ANY colocated `*.test.tsx`) is now under `partners/`. After the move, scan with `grep -rn '(admin)/\[adminSegment\]/accounts' app/ src/` and confirm zero matches inside source files (string-literal references — i18n keys like `admin.accounts.*` are unchanged per D-04). The path `app/(admin)/[adminSegment]/accounts/` MUST no longer exist on disk.

    (b) Add the `redirects()` block to `next.config.ts` per D-02. Insert immediately after `generateBuildId`. Use exactly: source `'/:adminSegment/accounts/:path*'`, destination `'/:adminSegment/partners/:path*'`, `permanent: true` (Next.js emits 308 for permanent). Add a comment `// D-02: 308 permanent redirect for v1.1 bookmarks` on the line above the redirect object. Do NOT create a stub `app/(admin)/[adminSegment]/accounts/page.tsx` — the redirects() approach handles all sub-paths cleanly.

    (c) Revert the Shell.tsx hotfix per D-03. In `src/components/ui/Shell.tsx`, locate the `adminHrefs` const block (around lines 50-62). Change `partners: \`/${adminSegment}/accounts\`` → `partners: \`/${adminSegment}/partners\``. Change `history: \`/${adminSegment}/coefficients\`` → `history: \`/${adminSegment}/history\``. Drop the stale comment block at lines 55-60 that flags the patch (the "TODO Phase 14 revert" or similar — grep for any TODO/FIXME tied to commit 6809b1f and remove). Leave the rest of the file unchanged.

    Do NOT touch globals.css, StatusChip, or dictionaries in this task — those are Task 2.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -rn '(admin)/\[adminSegment\]/accounts' app/ src/ --include='*.tsx' --include='*.ts' | grep -v '/partners/' | grep -v '^#' | wc -l | grep -q '^0$'" &amp;&amp; bash -c "test ! -d 'app/(admin)/[adminSegment]/accounts'" &amp;&amp; bash -c "grep -q 'permanent: true' next.config.ts" &amp;&amp; bash -c "grep -q 'partners: \`/\${adminSegment}/partners\`' src/components/ui/Shell.tsx" &amp;&amp; bash -c "grep -q 'history: \`/\${adminSegment}/history\`' src/components/ui/Shell.tsx"</automated>
  </verify>
  <done>The directory `app/(admin)/[adminSegment]/accounts/` is gone; `partners/` contains all four moved files plus any colocated tests. `next.config.ts` has a `redirects()` block returning the permanent 308 mapping. `Shell.tsx` adminHrefs point at `/<seg>/partners` and `/<seg>/history`. Build, lint, and the grep guards pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add .chip-invited CSS, extend StatusChip variant union, add chip.invited i18n key</name>
  <files>app/globals.css, src/components/ui/StatusChip.tsx, src/components/ui/StatusChip.test.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/components/ui/StatusChip.tsx (full file — 21 lines)
    - src/components/ui/StatusChip.test.tsx (full file — existing 4-variant test cases)
    - app/globals.css lines 365-385 (the .chip-* block including .chip-draft at line 371-374)
    - src/lib/i18n/dictionaries.ts — locate the existing `chip.*` keys (e.g. `chip.active`, `chip.disabled`, `chip.draft`, `chip.expired`, `chip.deleted`) and the FR/EN section boundaries
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §4.4 (the exact CSS block) + §4.5 (the type union extension) + §6.7 (the i18n key copy)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`StatusChip.tsx` and §`app/globals.css`
  </read_first>
  <behavior>
    - Test 1 (StatusChip.test.tsx — RED first): add an `AC-SC-08` test case rendering `<StatusChip variant="invited" label="invité.e" />`. Assert: rendered output is `<span class="chip chip-invited">invité.e</span>`. This test MUST fail before the variant union is extended.
    - Test 2 (GREEN): extending the variant union in StatusChip.tsx + adding the `.chip-invited` CSS class makes Test 1 pass.
    - Test 3 (compile-time): `_EnHasAllFrKeys` (existing infrastructure in dictionaries.ts) MUST continue to typecheck after adding the new key to both FR and EN.
    - Test 4 (grep gate): `grep -c '\.chip-invited' app/globals.css` returns ≥1; `grep -c "'invited'" src/components/ui/StatusChip.tsx` returns ≥1.
  </behavior>
  <action>
    Three changes, in TDD order:

    (a) In `src/components/ui/StatusChip.test.tsx`, add a new test case (model on existing variant tests). The case is `AC-SC-08: <StatusChip variant="invited" label="invité.e" /> renders chip chip-invited with the label`. Render via `@testing-library/react`, query the rendered `<span>` (e.g. `container.querySelector('span.chip')` or `getByText('invité.e')`), assert `className === 'chip chip-invited'` AND `textContent === 'invité.e'`. Run `npx vitest src/components/ui/StatusChip.test.tsx` — the new case MUST fail (RED) because the variant union does not yet allow `'invited'`. Commit the failing test: `test(14-01): add failing test for StatusChip invited variant`.

    (b) Extend `src/components/ui/StatusChip.tsx` per UI-SPEC §4.5. Change the union from `'active' | 'draft' | 'expired' | 'disabled'` to `'active' | 'draft' | 'expired' | 'disabled' | 'invited'`. The component body templates `chip chip-${variant}` so no body change is needed. Update the JSDoc comment listing the variants (lines 3-9 of the existing file) to include `'invited' → gold tint (.chip-invited, added Phase 14)`. Also add the `.chip-invited` declaration to `app/globals.css` immediately AFTER the `.chip-draft` closing brace (around line 374-375). Use exactly the block from UI-SPEC §4.4: `background: rgba(224, 133, 48, 0.12); color: var(--gold);`. Run `npx vitest src/components/ui/StatusChip.test.tsx` — all test cases pass (GREEN). Commit: `feat(14-01): extend StatusChip with invited variant + .chip-invited CSS`.

    (c) Add the `chip.invited` i18n key to `src/lib/i18n/dictionaries.ts` in BOTH `fr` and `en` dictionaries. Per UI-SPEC §6.7: FR value `invité.e`, EN value `Invited` (capital I, no accent). Insert near the other `chip.*` keys (the existing block has `chip.active`, `chip.disabled`, `chip.draft`, `chip.expired`, `chip.deleted` — add `chip.invited` adjacent to them). Run `npm run build` — the compile-time `_EnHasAllFrKeys` proof must continue to typecheck. No other dictionary changes in this task; the ~30 other Phase 14 keys land in subsequent plans.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/StatusChip.test.tsx &amp;&amp; bash -c "grep -c '\.chip-invited' app/globals.css | grep -q '^1$'" &amp;&amp; bash -c "grep -q \"'invited'\" src/components/ui/StatusChip.tsx" &amp;&amp; bash -c "grep -q \"chip.invited.*invité.e\" src/lib/i18n/dictionaries.ts || grep -q \"'chip.invited': 'invité.e'\" src/lib/i18n/dictionaries.ts" &amp;&amp; bash -c "grep -q \"chip.invited.*Invited\" src/lib/i18n/dictionaries.ts || grep -q \"'chip.invited': 'Invited'\" src/lib/i18n/dictionaries.ts" &amp;&amp; npm run build</automated>
  </verify>
  <done>StatusChip accepts variant `'invited'` and renders the new class; `.chip-invited` exists in globals.css with gold-12% tint matching .chip-draft chrome; both FR and EN dictionaries contain `chip.invited`; the Vitest case AC-SC-08 passes; the project builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js route layer | A user hitting the legacy `/<seg>/accounts` URL crosses into the Next.js redirect handler. |
| (None new for the directory rename itself) | The rename does not change auth-gate placement — every page under `partners/` still calls `requireAdmin()` (it was already called inside `accounts/page.tsx` before the move; the file moves with the call). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | `next.config.ts` redirects() entry | mitigate | Path is hard-coded in next.config.ts — partner-controlled input cannot reach the source pattern (D-02). The destination is also hard-coded. ASVS L1 V13.2.1: open-redirect surface absent. |
| T-14-01-02 | Spoofing/Authorization | Moved partner-list route at `/<seg>/partners` | mitigate | The moved `page.tsx` retains its `requireAdmin()` call — defence-in-depth identical to pre-move. Vitest harness from Phase 9 covers the auth gate; no new test required because behavior is preserved. |
| T-14-01-03 | Information disclosure | Shell.tsx adminHrefs revert | accept | Sidebar links are visible only to admin users (`role === 'admin'` gate at Shell.tsx line ~50). Revealing the URL paths `/<seg>/partners` and `/<seg>/history` to a non-admin is impossible — the admin sidebar is not rendered. |
| T-14-01-04 | Information disclosure | `.chip-invited` class addition | accept | New CSS utility class; does not surface any data. Consumers (Plan 14-06) add the `invited` chip to admin-only surfaces. |
| T-14-01-05 | Denial of service | next.config.ts redirects() | accept | Single static redirect rule; framework-level matcher; no performance risk. |
| T-14-01-06 | Repudiation | Directory rename | accept | `git mv` preserves file history; reviewable in the PR diff. Audit log unaffected (no production behavior change). |

**ADMIN-09 invariant for this plan:** No commission values are introduced or surfaced by any change in this plan. The `chip-invited` CSS class is metadata for partner status only. Grep gate: `grep -rn 'commission' app/(admin)/[adminSegment]/partners/ | grep -v '\.test\.' | grep -v '^Binary' | wc -l` returns ZERO new commission references introduced by this plan (Phase 14 D-29 strict).
</threat_model>

<verification>
- `npm run build` succeeds (Next.js bundles the moved directory + the redirects() block).
- `npm run lint` succeeds.
- `npx vitest run src/components/ui/StatusChip.test.tsx` — all variant tests pass including new `AC-SC-08`.
- `npx vitest run` — full suite continues to pass (788 cases pre-Phase-14 baseline; this plan adds 1 case → expect 789).
- `grep -rn '(admin)/\[adminSegment\]/accounts' app/ src/ --include='*.tsx' --include='*.ts' | grep -v '/partners/' | grep -v '^#'` returns ZERO matches.
- `test ! -d 'app/(admin)/[adminSegment]/accounts'` (legacy directory gone).
- `grep -q 'permanent: true' next.config.ts` (redirect added).
- `grep -q 'chip.invited' src/lib/i18n/dictionaries.ts` (i18n key added).
</verification>

<success_criteria>
- Directory moved (D-01); `accounts/` no longer exists; `partners/` contains the 4 prior files (+ any colocated tests).
- `next.config.ts` emits 308 for `/<seg>/accounts/:path*` → `/<seg>/partners/:path*` (D-02).
- `Shell.tsx` adminHrefs point to `/<seg>/partners` and `/<seg>/history` (D-03 revert).
- `StatusChip` accepts `variant="invited"` and renders `<span class="chip chip-invited">{label}</span>` (UI-SPEC §4.5).
- `.chip-invited` class exists in globals.css with the exact contract from UI-SPEC §4.4.
- `chip.invited` i18n key present in both FR (`invité.e`) and EN (`Invited`) dictionaries (UI-SPEC §6.7).
- Vitest suite remains green; `_EnHasAllFrKeys` typecheck passes; build + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-01-SUMMARY.md` documenting:
- What moved (file list, byte-equivalence confirmation)
- The redirect rule shape (source/destination/308)
- StatusChip variant + .chip-invited CSS surface area
- Shell.tsx revert diff
- Test count delta (expect +1 from 788)
- Any deviations from D-01..D-04 (none expected; flag if so)
</output>
