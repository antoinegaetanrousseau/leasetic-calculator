---
phase: 14-admin-polish-partners-history-home
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - app/(admin)/[adminSegment]/page.tsx
  - app/(admin)/[adminSegment]/page.test.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "Visiting /<seg>/ as an admin renders an h1 with the admin.home.title text + a subtitle below."
    - "Below the title block, exactly 3 <AdminNavCard> instances render in a grid (Coefficients & commission, Partenaires, Historique)."
    - "The 3 cards link to /<seg>/coefficients, /<seg>/partners, /<seg>/history respectively (note: /<seg>/history does not exist yet — Plan 14-05 ships it; the LINK is wired here)."
    - "Each card uses the correct lucide-react icon: Sliders (coefficients), Users (partners), History (history)."
    - "The Phase 9 two-link layout (Settings2 + Users cards) is fully replaced — no leftover Phase 9 markup."
    - "Both FR and EN dictionaries contain admin.home.* (2 keys) + admin.nav.* (7 keys) per UI-SPEC §6.1 + §6.2."
  artifacts:
    - path: "app/(admin)/[adminSegment]/page.tsx"
      provides: "Admin home server component rendering 3 AdminNavCards (replaces Phase 9 2-link layout per D-13..D-16)"
      contains: "AdminNavCard"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "9 new keys: admin.home.title, admin.home.subtitle, admin.nav.{coefficients,partners,history}.{title,description}, admin.nav.open"
      contains: "admin.home.title"
  key_links:
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "src/components/ui/AdminNavCard.tsx"
      via: "<AdminNavCard variant={…} icon={…} title={…} description={…} href={…} openLabel={…} />"
      pattern: "<AdminNavCard"
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "lucide-react Sliders, Users, History icons"
      via: "import { Sliders, Users, History } from 'lucide-react'"
      pattern: "Sliders.*Users.*History"
---

<objective>
Replace the Phase 9 two-link admin home layout (Settings2 + Users cards) with the 3-card AdminNavCard grid per D-13..D-16 and UI-SPEC §5.5:

1. Modify `app/(admin)/[adminSegment]/page.tsx` to:
   - Keep the existing `requireAdmin()` defence-in-depth + `dynamic = 'force-dynamic'` + admin metadata (those are Phase 9 contract, unchanged).
   - REPLACE the imports `Settings2, Users` from lucide with `Sliders, Users, History` (Phase 14 D-14 icon swap).
   - Import `<AdminNavCard>` from `@/components/ui/AdminNavCard`.
   - REPLACE the entire 2-link `grid-template-columns: '1fr 1fr'` block (Phase 9 lines ~57-102) with a new 3-card grid: `display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; max-width: 1040px; marginTop: 32px;`. Responsive: `@media (max-width: 999px)` collapses to single column (per UI-SPEC §5.5 — implement via inline `style` for now; the existing Phase 11 chrome has no `.admin-grid` class).
   - Render 3 instances per the UI-SPEC §5.5 table:
     - #1 — variant `coefficients`, icon `Sliders`, title `t('admin.nav.coefficients.title', lang)`, description `t('admin.nav.coefficients.description', lang)`, href `/${adminSegment}/coefficients`, openLabel `t('admin.nav.open', lang)`.
     - #2 — variant `partners`, icon `Users`, title `admin.nav.partners.title`, description `admin.nav.partners.description`, href `/${adminSegment}/partners` (the renamed route from Plan 14-01).
     - #3 — variant `history`, icon `History`, title `admin.nav.history.title`, description `admin.nav.history.description`, href `/${adminSegment}/history` (the new route from Plan 14-05; the LINK is wired here regardless of whether the destination exists yet — Next.js will serve a 404 for `/history` until 14-05 ships, which is fine for parallel execution).
   - Per UI-SPEC §5.5 deprecation note: Phase 9's bespoke `.admin-nav-card` CSS in globals.css stays (dead code, harmless). Plan does NOT touch globals.css.

2. Add the 9 new i18n keys per UI-SPEC §6.1 + §6.2 to `src/lib/i18n/dictionaries.ts` in BOTH FR and EN (verbatim from the table).

3. Update or create `app/(admin)/[adminSegment]/page.test.tsx` to assert:
   - Exactly 3 `<AdminNavCard>` instances render.
   - Each has the correct `href` (3 distinct values).
   - Each has the correct `variant` prop (verified via querying the rendered DOM for the variant-specific icon-square color class, OR by mocking AdminNavCard and asserting the variant prop directly).
   - The page calls `requireAdmin()` defence-in-depth (mock-asserted).

Purpose: ROUTE-02 success criterion #3 (the admin-home portion — "renders 3 AdminNavCards"; partner home MetricTiles are DEFERRED to v1.3 per CONTEXT.md). Closes the design-contract gap between the Phase 9 stop-gap layout and the v1.2 Figma node `41:46` design.

Output: Admin home renders the 3 cards per Figma node 41:46; navigation to each of the 3 destinations works (modulo /history which arrives in Plan 14-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md

# Source files this plan modifies / consumes
@app/(admin)/[adminSegment]/page.tsx
@src/components/ui/AdminNavCard.tsx
@src/components/ui/AdminNavCard.test.tsx
@src/lib/i18n/dictionaries.ts
@src/lib/auth/require.ts

<interfaces>
<!-- AdminNavCard component (src/components/ui/AdminNavCard.tsx) -->
<!-- Consumer-side: import and pass these props verbatim. Phase 14 does NOT modify the component. -->
export interface AdminNavCardProps {
  variant: 'coefficients' | 'partners' | 'history';  // drives icon-square color per Phase 11
  icon: LucideIcon;
  title: string;
  description: string;
  href: string;
  openLabel: string;
}

<!-- lucide-react icons used by this plan (already installed) -->
import { Sliders, Users, History } from 'lucide-react';

<!-- Phase 9 page.tsx current shape (LINES 57-102, to be REPLACED) — extracted from the existing source -->
<!-- Phase 9 imports: import { Settings2, Users } from 'lucide-react'; -->
<!-- Phase 9 grid: `grid-template-columns: '1fr 1fr'` with 2 <Link>s. -->
<!-- Phase 14 replaces this entire block. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace admin-home layout with 3 AdminNavCards + add 9 i18n keys</name>
  <files>app/(admin)/[adminSegment]/page.tsx, app/(admin)/[adminSegment]/page.test.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - app/(admin)/[adminSegment]/page.tsx (full file — Phase 9 currently ~104 lines)
    - src/components/ui/AdminNavCard.tsx (the consumed component — verify the exact `AdminNavCardProps` interface)
    - src/components/ui/AdminNavCard.test.tsx (testing patterns for AdminNavCard variants — use as model for the page test)
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.5 (the full 3-card table) + §6.1 (admin.home.* keys) + §6.2 (admin.nav.* keys)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`admin home page.tsx` (the 3-card grid block to copy)
    - app/(authed)/proposals/new/verification/page.test.tsx — server-page test harness pattern
  </read_first>
  <behavior>
    - Test 1 (RED first — page.test.tsx): rendering the admin home server component asserts exactly 3 `<AdminNavCard>` mounts (via mocked component capturing props OR via DOM query for the AdminNavCard rendered shape — pick mock-based for cleaner assertion).
    - Test 2 (RED first): each card has the correct href: `/<adminSegment>/coefficients`, `/<adminSegment>/partners`, `/<adminSegment>/history`.
    - Test 3 (RED first): each card has the correct variant prop: `coefficients`, `partners`, `history`.
    - Test 4 (RED first): each card has the correct icon component: `Sliders`, `Users`, `History` (assert via the icon prop reference, since lucide icons are component references).
    - Test 5 (RED first): h1 text content matches the resolved `admin.home.title` value; subtitle text matches `admin.home.subtitle`.
    - Test 6 (compile-time): `_EnHasAllFrKeys` continues to typecheck after adding 9 new keys.
    - Test 7 (auth gate — should be already covered by existing test, retain): `requireAdmin()` is invoked at the top of the server component.
  </behavior>
  <action>
    Three-step TDD pass:

    (a) RED — add the 5 new test cases to `app/(admin)/[adminSegment]/page.test.tsx` (create the file if it doesn't exist; if Phase 9 has an existing test, EXTEND not REPLACE). Use the hoisted-mock harness from `verification/page.test.tsx`. Mock `requireAdmin` and `getCurrentLang` + `t`. Mock `<AdminNavCard>` via `vi.mock('@/components/ui/AdminNavCard', () => ({ AdminNavCard: vi.fn(({ variant, href, icon, title, description, openLabel }) => <div data-testid={\`navcard-${variant}\`} data-href={href} data-icon-name={icon.displayName ?? icon.name} />) }))`. Then assert the captured props match the spec. Run `npx vitest run app/(admin)/[adminSegment]/page.test.tsx` — cases MUST fail (page still has Phase 9 layout).

    (b) GREEN — modify `app/(admin)/[adminSegment]/page.tsx` per PATTERNS.md §`admin home page.tsx`:
       - Replace imports: `import { Sliders, Users, History } from 'lucide-react';` + `import { AdminNavCard } from '@/components/ui/AdminNavCard';` (remove `Settings2`).
       - Keep the existing function signature + `requireAdmin()` + `getCurrentLang()` + `params` destructure.
       - DELETE the Phase 9 2-link block (the existing `<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', ... }}>` containing the 2 `<Link>` cards).
       - INSERT the 3-card block per PATTERNS.md §`admin home page.tsx` (verbatim shape):
         ```
         <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 24, maxWidth: 1040, marginTop: 32 }}>
           <AdminNavCard variant="coefficients" title={t('admin.nav.coefficients.title', lang)} description={t('admin.nav.coefficients.description', lang)} href={`/${adminSegment}/coefficients`} icon={Sliders} openLabel={t('admin.nav.open', lang)} />
           <AdminNavCard variant="partners" title={t('admin.nav.partners.title', lang)} description={t('admin.nav.partners.description', lang)} href={`/${adminSegment}/partners`} icon={Users} openLabel={t('admin.nav.open', lang)} />
           <AdminNavCard variant="history" title={t('admin.nav.history.title', lang)} description={t('admin.nav.history.description', lang)} href={`/${adminSegment}/history`} icon={History} openLabel={t('admin.nav.open', lang)} />
         </div>
         ```
         (Insert no fenced code in the actual page — this is shown here in plan-prose for clarity; the executor writes it inline as JSX.)
       - Ensure the h1 + subtitle hero block above the grid uses i18n keys `admin.home.title` + `admin.home.subtitle`. If Phase 9's page already has an h1, change its text source to `t('admin.home.title', lang)`. Add the subtitle `<p style={{ fontSize: 16, color: 'var(--muted)', marginTop: 8 }}>{t('admin.home.subtitle', lang)}</p>` immediately below per UI-SPEC §5.5.
       - Per UI-SPEC §5.5 responsive note: do NOT add a media query if Phase 9 already handles it via outer container. If not, accept the responsive collapse as discretion — the inline style `gridTemplateColumns: 'repeat(3, 1fr)'` is fine; mobile-tracking is out of Phase 14 scope (desktop-primary per PROJECT.md constraints).

    (c) GREEN — add the 9 i18n keys to `src/lib/i18n/dictionaries.ts` in BOTH `fr` and `en` per UI-SPEC §6.1 + §6.2:
       - `admin.home.title` — `Administration` / `Administration`
       - `admin.home.subtitle` — `Gérez les paramètres globaux et les comptes` / `Manage global parameters and accounts`
       - `admin.nav.coefficients.title` — `Coefficients & commission` (same FR + EN)
       - `admin.nav.coefficients.description` — `Éditez les paramètres globaux` / `Edit global parameters`
       - `admin.nav.partners.title` — `Partenaires` / `Partners`
       - `admin.nav.partners.description` — `Gérez les comptes partenaires` / `Manage partner accounts`
       - `admin.nav.history.title` — `Historique` / `History`
       - `admin.nav.history.description` — `Historique des coefficients` / `Coefficient history`
       - `admin.nav.open` — `Ouvrir →` / `Open →`
       Run `npm run build` — `_EnHasAllFrKeys` typecheck passes.

    Run `npx vitest run app/(admin)/[adminSegment]/page.test.tsx` — all cases pass (GREEN). Commit: `feat(14-03): rebuild admin home with 3 AdminNavCards + 9 i18n keys`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/page.test.tsx &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -c '<AdminNavCard' app/(admin)/[adminSegment]/page.tsx | grep -q '^3$'" &amp;&amp; bash -c "grep -q 'Sliders, Users, History' app/(admin)/[adminSegment]/page.tsx" &amp;&amp; bash -c "grep -q 'admin.home.title' src/lib/i18n/dictionaries.ts" &amp;&amp; bash -c "grep -q 'admin.nav.open' src/lib/i18n/dictionaries.ts"</automated>
  </verify>
  <done>Admin home renders exactly 3 AdminNavCards with the spec'd variants/icons/hrefs/labels; Vitest cases pass; 9 new i18n keys exist in both dictionaries; `_EnHasAllFrKeys` typecheck passes; build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /[adminSegment]/ (admin home) | Untrusted: `requireAdmin()` AUTH-15 gate at the top of the server component (existing Phase 9 contract, preserved). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Spoofing/Authorization | Admin home page | mitigate | Existing `await requireAdmin()` call preserved at the top of the server component (Phase 9 contract). Vitest harness covers the gate (Test 7). |
| T-14-03-02 | Information disclosure (ADMIN-09 D-29) | Admin home rendered HTML | mitigate | Three nav cards render only titles + descriptions + icons — no commission values. Grep gate (covered by Plan 14-06): `grep -c 'commission\|commission_pct' rendered-admin-home.html` returns 0 (the only "commission" mention is the TITLE "Coefficients & commission" which is a literal label, not a value — Plan 14-06 grep gate must be tuned to count `commission_pct` and numeric percentage values, NOT the bare word `commission` in a card title). |
| T-14-03-03 | Tampering | href values in the 3 cards | accept | Hard-coded inline `\`/${adminSegment}/{coefficients,partners,history}\`` template literals — partner-controlled input cannot reach the href slot. |
| T-14-03-04 | Information disclosure | adminSegment leakage via href | accept | The adminSegment is intentionally surfaced in URLs within admin-only views (Phase 6 design). Non-admins never see the rendered nav cards because `requireAdmin()` blocks them upstream. |

**ADMIN-09 grep gate clarification:** the admin home renders the literal string "Coefficients & commission" as a card TITLE. The Phase 14 ADMIN-09 grep contract (D-29) targets commission VALUES (numeric percentages, the `commission_pct` field key, etc.) — NOT the literal word "commission" used in chrome labels. Plan 14-06's grep gate is calibrated accordingly (pattern: `commission_pct\|[0-9]+(\.[0-9]+)?\s*%` excluding `Coefficients & commission` chrome).
</threat_model>

<verification>
- `npx vitest run app/(admin)/[adminSegment]/page.test.tsx` — all 5+ new cases pass.
- `grep -c '<AdminNavCard' app/(admin)/[adminSegment]/page.tsx` returns exactly 3.
- `npm run build` succeeds; `_EnHasAllFrKeys` compile-time proof typechecks with the 9 new keys.
- `npm run lint` clean.
- Manual smoke (NOT automated): visit `/<seg>/` as admin → see 3 cards. Click each card → navigates to the spec'd href.
</verification>

<success_criteria>
- ROUTE-02 success criterion #3 (admin-home portion): 3 AdminNavCards render (D-13..D-16). Partner-home MetricTiles are deferred to v1.3 per CONTEXT.md — this plan does NOT touch `app/(authed)/page.tsx`.
- Icons match Figma node `41:46`: `Sliders` (coefficients), `Users` (partners), `History` (history).
- 9 i18n keys land verbatim per UI-SPEC §6.1 + §6.2 in both FR and EN; parity typecheck passes.
- Existing `requireAdmin()` defence-in-depth call preserved at the top of the server component.
- Phase 9's bespoke `.admin-nav-card` CSS in globals.css stays as dead code (per UI-SPEC §5.5 recommendation — zero-risk).
- All Vitest cases pass; build + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-03-SUMMARY.md` documenting:
- The 3-card grid implementation (inline-style approach chosen vs a new CSS class)
- Test mock strategy used for AdminNavCard variant assertions
- 9 i18n keys added (count + names)
- Note that `/history` href works without /history existing yet (404 expected until Plan 14-05 ships)
- Any deviations from D-13..D-16 (none expected; flag if so)
</output>
