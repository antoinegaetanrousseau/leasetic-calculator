---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - src/lib/i18n/dictionaries.ts
  - vitest.config.ts
  - package.json
  - __tests__/setup-dom.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "globals.css declares --brand-mark: #6DC388 in :root"
    - "globals.css declares --shell-sidebar-w-collapsed: 72px and --shell-sidebar-current-w: 260px in :root"
    - ".chip-expired in globals.css uses muted (rgba(110,113,145,0.12) + var(--muted)) NOT gold"
    - ".chip-draft exists in globals.css with rgba(224,133,48,0.12) + var(--gold)"
    - "brand-logo theme picker CSS rules exist (light hides .brand-logo-dark, dark hides .brand-logo-light)"
    - "dictionaries.ts has 11 new sidebar.* keys in BOTH fr and en namespaces"
    - "vitest.config.ts environment is 'jsdom' (not 'node')"
    - "package.json has @testing-library/react, @testing-library/jest-dom, jsdom in devDependencies"
    - "npm test passes (existing tests survive the env switch)"
    - "npm run typecheck passes 0"
    - "npm run lint:check passes 0"
  artifacts:
    - path: "app/globals.css"
      provides: "extended token spine + chip-draft + chip-expired rewrite + brand-logo picker"
      contains: "--brand-mark: #6DC388"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "11 new sidebar.* keys × 2 langs"
      contains: "'sidebar.nav.adminCoefficients'"
    - path: "vitest.config.ts"
      provides: "DOM-testing environment"
      contains: "environment: 'jsdom'"
    - path: "__tests__/setup-dom.ts"
      provides: "DOM-testing setup (registers @testing-library/jest-dom matchers)"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "app/globals.css :root --brand-mark"
      to: "public/logo-light.svg (Plan 11-02)"
      via: "intent documentation only (no runtime binding — SVG hard-codes #6DC388)"
      pattern: "var\\(--brand-mark\\)"
    - from: "vitest.config.ts environment=jsdom"
      to: "Plans 11-02..11-04 component .test.tsx files"
      via: "Vitest test runner uses jsdom for render() calls"
      pattern: "environment:\\s*'jsdom'"
    - from: "dictionaries.ts sidebar.* keys"
      to: "RetractableSidebar.tsx (Plan 11-04)"
      via: "t('sidebar.nav.home', lang) call sites"
      pattern: "sidebar\\.(nav|eyebrow|collapse|expand|lang|theme)"
---

<objective>
Foundation layer for Phase 11. Establish the token spine extensions, i18n keys, and DOM-testing infrastructure that every subsequent plan in this phase depends on. Three independent, low-context changes bundled because they share no implementation risk and unblock Waves 2-4.

Purpose: Plans 11-02..11-05 cannot proceed without (a) `--brand-mark` token + brand-logo CSS picker + chip class updates in `app/globals.css`, (b) 11 new sidebar.* i18n keys in `dictionaries.ts`, and (c) a `vitest jsdom` environment so D-12 ("Vitest unit tests per component asserting prop-driven DOM output") is reachable.

Output: 1 modified CSS file (globals.css), 1 modified i18n dictionary (dictionaries.ts), 1 modified Vitest config, 1 new test-setup file, and 3 new devDependencies in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-design-system-foundation-brand-assets/11-CONTEXT.md
@.planning/phases/11-design-system-foundation-brand-assets/11-UI-SPEC.md
@.planning/phases/11-design-system-foundation-brand-assets/11-PATTERNS.md

<interfaces>
<!-- Existing globals.css :root block (lines 7-37) declares: --navy, --green, --gd, --gl, --teal, --paper, --white, --ink, --muted, --border, --gold, --danger, --shell-sidebar-w (260px), --topbar-h (64px), --footer-h (48px), --shadow-card, --surface, --hover-overlay, --active-pill. The @theme block (lines 54-78) already aliases most as --color-*. -->

<!-- Existing globals.css chip block (lines 344-375 + 496-499) declares: .chip (base), .chip-active, .chip-expired (currently gold — TO REWRITE), .chip-deleted, .chip-language, .chip-disabled (red-danger). -->

<!-- Existing dictionaries.ts has ONLY ONE sidebar.* key (`sidebar.brand` at fr line 41 and en line 545). The file enforces FR↔EN parity via `_EnHasAllFrKeys` compile-time type in dictionaries.test.ts. Adding a key to FR without EN (or vice versa) will fail the parity test. -->

<!-- Existing vitest.config.ts (line 6): `environment: 'node'`. Existing tests all run under node (no React DOM). The single `.test.tsx` file is `src/lib/pdf/document.test.tsx` (Phase 8) — tests a pure async fn, NOT React render(). Confirmed it survives the env switch: jsdom is a superset, node-only tests still pass. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend app/globals.css with v1.2 tokens, brand-logo CSS picker, .chip-draft, and .chip-expired rewrite</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current full file — see lines 1-110 :root + @theme blocks, 340-410 chip block, 496-528 chip-disabled + seed-banner regions)
    - .planning/phases/11-design-system-foundation-brand-assets/11-UI-SPEC.md §4.4 (brand-mark token), §6.1 (brand-logo CSS picker), §6.6 (StatusChip CSS + chip-expired rewrite), §6.7 (--shell-sidebar-current-w grid-column token), §11.2 (chip-expired regression risk)
    - .planning/phases/11-design-system-foundation-brand-assets/11-CONTEXT.md D-10 (--brand-mark additive), D-09 (CSS picker rules)
    - .planning/phases/11-design-system-foundation-brand-assets/11-PATTERNS.md → "app/globals.css (MODIFY — extend token spine + chip classes + brand-logo picker)" section (6 specific changes listed)
  </read_first>
  <action>
    Make EXACTLY the following 5 edits to `app/globals.css`:

    1. **Append three new tokens to the `:root` block** (between current line 22 `--danger: #dc2626;` and line 23 `/* Layout shell tokens */`). Add these three lines (per D-10 and UI-SPEC §6.7 Option B):
       - `--brand-mark: #6DC388;` (additive — documents that SVG mark fill `#6DC388` is intentional; NOT used at runtime by any TS component)
       - `--shell-sidebar-w-collapsed: 72px;` (NEW collapsed-state width token per UI-SPEC §2 / §6.3)
       - `--shell-sidebar-current-w: 260px;` (runtime-mutated grid-column width; default 260px = expanded; RetractableSidebar mutates this on `<html>` on toggle in Plan 11-04)

    2. **Append the brand-mark alias to the `@theme` block** (after the last existing `--color-*` line, which is `--color-navy-dark`):
       - `--color-brand-mark: var(--brand-mark);` (exposes the token to Tailwind utility classes, e.g. `bg-brand-mark`, even though Phase 11 has no consumer for that utility — declared for downstream phases per UI-SPEC §4.4)

    3. **REWRITE the existing `.chip-expired` rule** (currently at lines 362-365, with gold tint) — replace with muted-tint per UI-SPEC §6.6 + §11.2:
       ```
       .chip-expired {
         background: rgba(110, 113, 145, 0.12);
         color: var(--muted);
       }
       ```
       This is a breaking visual change from v1.1 — Phase 8's ValidityChip.tsx uses this class and will visually shift from gold→muted-gray. Regression smoke is scheduled in Plan 11-05 task 4. Do NOT change ValidityChip.tsx in this task.

    4. **ADD a new `.chip-draft` rule** immediately AFTER the rewritten `.chip-expired` rule. Use the same shape that `.chip-expired` USED TO HAVE (gold/orange tint — now reassigned semantically to the new `draft` status):
       ```
       .chip-draft {
         background: rgba(224, 133, 48, 0.12);
         color: var(--gold);
       }
       ```

    5. **APPEND the brand-logo CSS picker rules** at the bottom of the file (just before the closing of the file, after line 528 `/* === end Phase 9 admin surface === */`). Add a new section header comment `/* === Phase 11 brand-logo theme picker (UI-SPEC §6.1, D-09) === */` and these two rules:
       ```
       html[data-theme="light"] .brand-logo-dark  { display: none; }
       html[data-theme="dark"]  .brand-logo-light { display: none; }
       ```

    Do NOT modify any other CSS rules. Do NOT introduce additional whitespace beyond what is needed to read each change cleanly. Do NOT touch the `@media print` block (Phase 8 invariant per UI-SPEC §10). Do NOT touch `.admin-nav-card` (Phase 9 chrome — Plan 11-03 chains it via `.admin-nav-card-v2`).
  </action>
  <verify>
    <automated>grep -c "^  --brand-mark: #6DC388;$" app/globals.css | grep -q "^1$" && grep -c "^  --shell-sidebar-w-collapsed: 72px;$" app/globals.css | grep -q "^1$" && grep -c "^  --shell-sidebar-current-w: 260px;$" app/globals.css | grep -q "^1$" && grep -c "^  --color-brand-mark: var(--brand-mark);$" app/globals.css | grep -q "^1$" && grep -A2 "^\.chip-expired {" app/globals.css | grep -q "rgba(110, 113, 145, 0.12)" && grep -A2 "^\.chip-draft {" app/globals.css | grep -q "rgba(224, 133, 48, 0.12)" && grep -c 'html\[data-theme="light"\] \.brand-logo-dark' app/globals.css | grep -q "^1$" && grep -c 'html\[data-theme="dark"\]  \.brand-logo-light' app/globals.css | grep -q "^1$" && ! grep -E "^\.chip-expired \{" app/globals.css -A2 | grep -q "var(--gold)"</automated>
  </verify>
  <acceptance_criteria>
    - **AC-CSS-01** (binds UI-SPEC §4.4): `grep "^  --brand-mark: #6DC388;$" app/globals.css` returns exactly one match inside the `:root` block.
    - **AC-CSS-02** (binds UI-SPEC §6.7 Option B): `grep "^  --shell-sidebar-w-collapsed: 72px;$" app/globals.css` returns exactly one match; `grep "^  --shell-sidebar-current-w: 260px;$" app/globals.css` returns exactly one match.
    - **AC-CSS-03** (binds UI-SPEC §4.4): `grep "^  --color-brand-mark: var(--brand-mark);$" app/globals.css` returns exactly one match inside the `@theme` block.
    - **AC-CSS-04** (binds UI-SPEC §6.6 + §11.2): The 3-line CSS block starting `.chip-expired {` contains the literal string `rgba(110, 113, 145, 0.12)` AND `var(--muted)` AND does NOT contain `var(--gold)`. Verified by `grep -A2 "^\.chip-expired {" app/globals.css`.
    - **AC-CSS-05** (binds UI-SPEC §6.6 + AC-SC-02): The 3-line CSS block starting `.chip-draft {` contains the literal string `rgba(224, 133, 48, 0.12)` AND `var(--gold)`. Verified by `grep -A2 "^\.chip-draft {" app/globals.css`.
    - **AC-CSS-06** (binds UI-SPEC §6.1 + AC-BL-01 / AC-BL-02): `grep -c 'html\[data-theme="light"\] \.brand-logo-dark' app/globals.css` returns 1; `grep -c 'html\[data-theme="dark"\]  \.brand-logo-light' app/globals.css` returns 1.
    - **AC-CSS-07** (regression check — no other CSS rules disturbed): `wc -l app/globals.css` returns ≤ 540 (was 528; max +12 lines for the 5 changes). `npm run build` succeeds with 0 errors and 0 warnings related to CSS.
  </acceptance_criteria>
  <done>
    `git diff app/globals.css` shows exactly 5 logical changes (3 :root token additions, 1 @theme alias, 1 chip-expired rewrite, 1 chip-draft addition, 1 brand-logo picker block). No other CSS rules touched. The 8 grep gates in `<verify>` pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 13 new sidebar.* i18n keys to dictionaries.ts (FR + EN parity)</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts (current full file — note FR namespace starts at line 19, EN namespace starts ~line 525; `sidebar.brand` already declared at fr line 41 and en line 545)
    - src/lib/i18n/dictionaries.test.ts (FR↔EN parity test — `_EnHasAllFrKeys` compile-time type; missing key in either side fails build/typecheck)
    - .planning/phases/11-design-system-foundation-brand-assets/11-UI-SPEC.md §7 Copywriting Contract table (16 rows total; 13 are i18n keys owned by Plan 11-04 RetractableSidebar)
    - .planning/phases/11-design-system-foundation-brand-assets/11-PATTERNS.md → "src/lib/i18n/dictionaries.ts (MODIFY — add 11 sidebar.* keys × 2 langs)" — exact key list with FR + EN values
  </read_first>
  <action>
    Insert exactly 13 NEW dot-notation string-literal keys into the FR namespace and the EN namespace of `dictionaries.ts`. Place them adjacent to the existing `'sidebar.brand': 'Leasétic',` line in each namespace (immediately after `'sidebar.brand': 'Leasétic',` in both FR and EN blocks).

    Use these exact 13 keys, each appearing exactly once in FR and once in EN:

    | Key | FR value | EN value |
    |---|---|---|
    | `sidebar.collapse` | `Réduire le menu` | `Collapse menu` |
    | `sidebar.expand` | `Déployer le menu` | `Expand menu` |
    | `sidebar.lang.cycle` | `Changer de langue` | `Change language` |
    | `sidebar.theme.cycle` | `Changer de thème` | `Change theme` |
    | `sidebar.eyebrow.navigation` | `NAVIGATION` | `NAVIGATION` |
    | `sidebar.nav.home` | `Accueil` | `Home` |
    | `sidebar.nav.proposalsNew` | `Nouvelle proposition` | `New proposal` |
    | `sidebar.nav.history` | `Historique` | `History` |
    | `sidebar.nav.help` | `Aide` | `Help` |
    | `sidebar.nav.adminHome` | `Tableau de bord` | `Dashboard` |
    | `sidebar.nav.adminCoefficients` | `Coefficients` | `Coefficients` |
    | `sidebar.nav.adminPartners` | `Partenaires` | `Partners` |
    | `sidebar.nav.adminHistory` | `Historique` | `History` |

    Discipline (per file header docstring lines 12-16):
    - All values are STRING LITERALS — no template literals, no interpolation.
    - Use single quotes (matches existing file convention).
    - Trailing comma after each entry (matches existing file convention).
    - Preserve U+2019 curly apostrophe convention for FR strings if needed (none of the 13 strings contain apostrophes — pure ASCII safe).

    Do NOT add a comment block describing these as "Phase 11" — just append cleanly after `'sidebar.brand'` (the existing block comment `// sidebar / topbar / footer` on line 40 already covers the section).

    Do NOT modify any existing keys. Do NOT change any value of `sidebar.brand` (must remain `'Leasétic'` in both langs).
  </action>
  <verify>
    <automated>grep -c "'sidebar\.collapse': 'Réduire le menu'" src/lib/i18n/dictionaries.ts | grep -q "^1$" && grep -c "'sidebar\.collapse': 'Collapse menu'" src/lib/i18n/dictionaries.ts | grep -q "^1$" && grep -c "'sidebar\.nav\.adminCoefficients': 'Coefficients'" src/lib/i18n/dictionaries.ts | grep -q "^2$" && grep -c "'sidebar\." src/lib/i18n/dictionaries.ts | grep -E "^(28|29|30)$" && npm run typecheck 2>&1 | tail -5 | grep -qE "(Done|0 errors|no errors)" || (npm run typecheck && echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - **AC-I18N-01** (binds UI-SPEC §7 chrome string table): All 13 new keys present in FR namespace. Verified by `grep -E "'sidebar\\.(collapse|expand|lang\\.cycle|theme\\.cycle|eyebrow\\.navigation|nav\\.(home|proposalsNew|history|help|adminHome|adminCoefficients|adminPartners|adminHistory))':" src/lib/i18n/dictionaries.ts | grep -c "Réduire\|Déployer\|Changer\|NAVIGATION\|Accueil\|Nouvelle\|Historique\|Aide\|Tableau\|Coefficients\|Partenaires"` returns ≥ 13.
    - **AC-I18N-02**: All 13 new keys present in EN namespace with EN values (`Collapse menu`, `Expand menu`, `Change language`, `Change theme`, `NAVIGATION`, `Home`, `New proposal`, `History`, `Help`, `Dashboard`, `Coefficients`, `Partners`, `History`). The string "Historique" appears 2× in FR namespace (sidebar.nav.history + sidebar.nav.adminHistory) and "History" appears 2× in EN namespace.
    - **AC-I18N-03** (binds parity test): `npm run test src/lib/i18n/dictionaries.test.ts` passes (FR↔EN parity holds — no key in one language missing from the other).
    - **AC-I18N-04** (binds discipline rule from file header line 14): No new key uses backticks/template literals — verified by `grep -c "sidebar\\.\\(nav\\|eyebrow\\|collapse\\|expand\\|lang\\|theme\\)" src/lib/i18n/dictionaries.ts` shows only single-quoted string literals (visual inspection of diff suffices; lint rule will fail otherwise).
    - **AC-I18N-05**: `npm run typecheck` passes 0 errors. The `_EnHasAllFrKeys` compile-time type check at the bottom of the file is satisfied.
    - **AC-I18N-06**: Total `'sidebar.` line count in the file is exactly 28 (14 keys × 2 languages — the existing `sidebar.brand` × 2 + the 13 new × 2 = 28). Verified by `grep -c "'sidebar\\." src/lib/i18n/dictionaries.ts` returning 28.
  </acceptance_criteria>
  <done>
    `git diff src/lib/i18n/dictionaries.ts` shows exactly 26 added lines (13 new keys × 2 namespaces). `npm run test src/lib/i18n/dictionaries.test.ts` passes. `npm run typecheck` returns 0 errors. The 5 grep gates in `<verify>` pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install @testing-library/react + jsdom and switch Vitest environment to jsdom</name>
  <files>package.json, vitest.config.ts, __tests__/setup-dom.ts</files>
  <read_first>
    - package.json (current file — note no @testing-library packages present; `vitest` is at `2.1.8`)
    - vitest.config.ts (current file — `environment: 'node'`, includes pattern covers `src/**/*.test.ts(x)`, `app/**/*.test.ts(x)`, `__pdf-fixtures__/**/*.test.ts`)
    - src/lib/i18n/format.test.ts (sample of existing node-environment test — confirms most existing tests are pure-function describe/it/expect and don't need DOM)
    - src/lib/pdf/document.test.tsx (the only existing .test.tsx — tests `renderToBuffer()` of a server-only React-PDF document; runs without DOM and must continue to pass under jsdom)
    - .planning/phases/11-design-system-foundation-brand-assets/11-CONTEXT.md D-12 + D-13 (D-12 mandates Vitest unit tests per component; D-13 forbids Storybook + Playwright but is silent on @testing-library/react — therefore @testing-library/react IS PERMITTED)
    - .planning/phases/11-design-system-foundation-brand-assets/11-PATTERNS.md → "Vitest test files (one per component)" section — "Critical gap — no DOM testing infrastructure yet"
  </read_first>
  <action>
    Three changes:

    1. **Install dev dependencies** via the project's package manager (npm — confirmed by `package-lock.json` presence). Run:
       ```
       npm install --save-dev @testing-library/react@16.1.0 @testing-library/jest-dom@6.6.3 jsdom@25.0.1
       ```
       Use exact-pin convention (no carets) per the project's locked package.json discipline (see STATE.md Decisions Log: "Exact version pins without carets"). After install, manually verify `package.json` `devDependencies` block lists the three new entries with exact versions (no `^`). If npm inserts carets, hand-edit `package.json` to remove them and re-run `npm install` to refresh `package-lock.json`.

       **Peer-dep handling:** `@testing-library/react@16.x` peers `react@^18 || ^19`. The project is on `react@19.0.0`. No `--legacy-peer-deps` needed. If npm flags a peer warning for `jsdom` vs an installed package, document the warning in the task SUMMARY but do not block.

    2. **Create `__tests__/setup-dom.ts`** at the project root (this is the new setup file; the directory does not currently exist — `mkdir -p __tests__` first). Contents:
       ```typescript
       // Phase 11 — DOM testing setup.
       // Registers @testing-library/jest-dom custom matchers (toBeInTheDocument, toHaveAttribute, etc.)
       // on Vitest's expect. Loaded by vitest.config.ts via `setupFiles`.
       import '@testing-library/jest-dom/vitest';
       ```
       Note the `/vitest` subpath — this is the Vitest-compatible matcher registration (per @testing-library/jest-dom@6.x docs). Do NOT import from `@testing-library/jest-dom` (Jest-default) — that registers against Jest's `expect`, not Vitest's.

    3. **Edit `vitest.config.ts`** to switch the environment and load the setup file:
       - Change line 6 `environment: 'node',` to `environment: 'jsdom',`
       - Add a new line inside the `test` config object: `setupFiles: ['./__tests__/setup-dom.ts'],`
       - Preserve the existing `include` array and `resolve` block verbatim.

       Final file shape (10 lines, 5 changes):
       ```typescript
       import { defineConfig } from 'vitest/config';
       import path from 'node:path';

       export default defineConfig({
         test: {
           environment: 'jsdom',
           globals: false,
           setupFiles: ['./__tests__/setup-dom.ts'],
           include: ['src/**/*.test.ts', 'src/**/*.test.tsx', 'app/**/*.test.ts', 'app/**/*.test.tsx', '__pdf-fixtures__/**/*.test.ts'],
         },
         resolve: {
           alias: { '@': path.resolve(__dirname, './src') },
         },
       });
       ```

    Compatibility verification: after these three changes, ALL existing tests must still pass — pure-function tests run identically under jsdom (jsdom is a superset of node for these purposes). The single existing `.test.tsx` (`src/lib/pdf/document.test.tsx`) tests `renderToBuffer()` which uses `react-pdf/renderer`'s own rendering (NOT React-DOM render); it is environment-agnostic and must continue to pass.

    Do NOT add `@testing-library/react` to `package.json` `dependencies` — only `devDependencies`. Do NOT add a `happy-dom` alternative (jsdom is selected per CONTEXT D-12 implementation guidance + PATTERNS critical-gap recommendation). Do NOT introduce a `__tests__` folder for component tests (test files in Plans 11-02..11-04 colocate as `*.test.tsx` next to their components per PATTERNS shared convention — `__tests__/setup-dom.ts` is ONLY the Vitest setup file, not a test location).
  </action>
  <verify>
    <automated>grep -c '"@testing-library/react":' package.json | grep -q "^1$" && grep -c '"@testing-library/jest-dom":' package.json | grep -q "^1$" && grep -c '"jsdom":' package.json | grep -q "^1$" && ! grep -E '"(@testing-library/react|@testing-library/jest-dom|jsdom)":\s*"\^' package.json && grep -c "environment: 'jsdom'" vitest.config.ts | grep -q "^1$" && ! grep -c "environment: 'node'" vitest.config.ts | grep -q "^1$" && grep -c "setupFiles: \['./__tests__/setup-dom.ts'\]" vitest.config.ts | grep -q "^1$" && test -f __tests__/setup-dom.ts && grep -q "@testing-library/jest-dom/vitest" __tests__/setup-dom.ts && npm test 2>&1 | tail -3 | grep -qE "passed"</automated>
  </verify>
  <acceptance_criteria>
    - **AC-TEST-01** (binds PATTERNS "Critical gap" + CONTEXT D-12): `package.json` `devDependencies` block contains `"@testing-library/react": "16.1.0"`, `"@testing-library/jest-dom": "6.6.3"`, `"jsdom": "25.0.1"` — all with exact pins, no carets.
    - **AC-TEST-02** (binds STATE.md Decisions Log "Exact version pins without carets"): No new dependency entry uses caret (`^`) or tilde (`~`) version range. Verified by `grep -E '"(@testing-library/react|@testing-library/jest-dom|jsdom)":\s*"\^' package.json` returning 0 matches.
    - **AC-TEST-03** (binds UI-SPEC §13 Dimension 6 "Registry Safety"): No new RUNTIME dependency in `dependencies` — only `devDependencies`. Verified by `jq '.dependencies | keys[]' package.json` not containing any of the three new packages.
    - **AC-TEST-04** (binds CONTEXT D-12): `vitest.config.ts` declares `environment: 'jsdom'` exactly once. No `environment: 'node'` line remains. Verified by `grep -c "environment: 'jsdom'" vitest.config.ts == 1` AND `grep -c "environment: 'node'" vitest.config.ts == 0`.
    - **AC-TEST-05**: `vitest.config.ts` declares `setupFiles: ['./__tests__/setup-dom.ts']`. Verified by exact-string grep.
    - **AC-TEST-06**: File `__tests__/setup-dom.ts` exists and contains the import `import '@testing-library/jest-dom/vitest';`. Verified by `test -f __tests__/setup-dom.ts && grep -c "@testing-library/jest-dom/vitest" __tests__/setup-dom.ts == 1`.
    - **AC-TEST-07** (binds CI green-bar from STATE.md "Tests: 399/399 passing"): `npm test` runs all existing tests under the new jsdom environment and returns exit code 0. Pre-existing tests must continue to pass. Output contains the string `passed` and does NOT contain `failed` for any test count > 0.
    - **AC-TEST-08** (binds PROJECT.md OVH-portability): No new dependency triggers `npm run check:no-vercel-imports`. Verified by running that script and getting exit code 0.
  </acceptance_criteria>
  <done>
    `git diff package.json` shows 3 new devDependency lines (exact pins). `git diff vitest.config.ts` shows 2 changes (environment + setupFiles). New file `__tests__/setup-dom.ts` exists with the single-line import. `npm test` exits 0 with all existing test suites passing. `npm run typecheck` exits 0. The 10 grep gates in `<verify>` pass.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the following composite verification:

1. `npm run typecheck` exits 0.
2. `npm run lint:check` exits 0.
3. `npm test` exits 0 (all existing tests pass under jsdom).
4. `npm run build` exits 0 (production build succeeds with new CSS tokens).
5. `npm run check:no-vercel-imports` exits 0 (no new Vercel-only imports introduced).
6. `git status` shows exactly 5 modified/new files: `app/globals.css`, `src/lib/i18n/dictionaries.ts`, `vitest.config.ts`, `package.json`, `package-lock.json` (auto-updated by npm install), plus 1 new file `__tests__/setup-dom.ts`.
7. `grep -c "^\.chip-" app/globals.css` returns ≥ 6 (was 5: chip-active/expired/deleted/language/disabled; now 6 with chip-draft added).
</verification>

<success_criteria>
- All 5 grep gates in Task 1 verify block pass (CSS spine extended + chip-expired rewritten + chip-draft added + brand-logo picker added).
- All 5 grep gates in Task 2 verify block pass (13 new sidebar.* keys × 2 langs).
- All 10 grep gates in Task 3 verify block pass (DOM-testing infra installed + env switched + setup file created).
- The phase-level verification above (typecheck + lint + test + build + no-vercel-imports) all exit 0.
- Plans 11-02, 11-03, 11-04, 11-05 can begin: they depend on `--brand-mark` token, `.chip-draft` class, brand-logo picker rules, sidebar.* i18n keys, and the jsdom Vitest environment shipped by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/11-design-system-foundation-brand-assets/11-01-SUMMARY.md` documenting:
- Final exact versions installed for @testing-library/react, @testing-library/jest-dom, jsdom.
- Any peer-dep warnings observed during `npm install` (and whether they were resolved).
- The exact line numbers in `app/globals.css` where each of the 5 changes landed (for downstream traceability).
- Confirmation that `src/lib/pdf/document.test.tsx` continues to pass under jsdom (the one pre-existing `.test.tsx`).
- Whether `package-lock.json` changes are committed (default: yes — locked discipline).
</output>
