---
phase: 24
slug: admin-dual-view-toggle
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-30
---

# Phase 24 — UI Design Contract: Admin Dual-View Toggle

> Visual and interaction contract for the `Admin | Agent` view toggle control in the sidebar footer.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> **Scope:** ONE new control only — `ViewToggle` in `RetractableSidebar.tsx` footer, plus
> its two visual states (expanded segmented / collapsed pill). No new pages. No new routes.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-rolled, matching existing codebase) |
| Preset | not applicable |
| Component library | none (inline styles + Tailwind v4 utility classes, same as existing toggles) |
| Icon library | lucide-react (already installed) |
| Font | Plus Jakarta Sans (next/font/local, already registered as `--font-sans`) |

> Source: `components.json` absent; stack confirmed as Next.js App Router + Tailwind v4 + hand-rolled
> component library. `ThemeToggle` and `LocaleToggle` are the direct style templates.

---

## Spacing Scale

Declared values — 8-point scale, multiples of 4 only:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, segment inner gap |
| sm | 8px | Gap between controls in collapsed footer stack |
| md | 16px | Footer divider margin-bottom |
| lg | 24px | Footer divider margin-top |
| xl | 32px | — |
| 2xl | 48px | — |
| 3xl | 64px | — |

Exceptions:

- Segment horizontal padding: `px-3` (12px) / `py-1.5` (6px) — matches `ThemeToggle` / `LocaleToggle` exactly.
- Container inner padding: `p-1` (4px) on the pill wrapper — matches existing toggles.
- Collapsed pill: `width: 36px; height: 28px` — matches the collapsed `LocaleToggle` pill geometry.

---

## Typography

Applies to segment labels only (the toggle has no heading or body text):

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14.5px | 400 | 1.55 | App base (inherited, not used in toggle) |
| Label — unselected segment | 11.5px | 600 | 1 | Segment text, matches `LocaleToggle` font-size/weight |
| Label — selected segment | 11.5px | 600 | 1 | Same size; selected state is color-only, not weight change |
| Collapsed pill | 11.5px | 600 | 1 | Shows current view initial ("A" for Admin, "G" for Agent) |

Letter-spacing: `0.04em`, `text-transform: uppercase` — matches `LocaleToggle` exactly.

> Source: `src/components/LocaleToggle.tsx` lines 33-39.

---

## Color

Token values (from `app/globals.css`):

| Token | Light value | Dark value |
|-------|------------|-----------|
| `--gd` | `#129657` | `#129657` (unchanged) |
| `--paper` | `#f5f7fc` | `#0c121c` |
| `--border` | `#d9dbe9` | `#242c3e` |
| `--ink` | `#41423d` | `#e6e9ef` |
| `--muted` | `#6e7191` | `#8b93a8` |
| `--surface` | `#ffffff` | `#161e2d` |

### Color contract for the `ViewToggle` control

| State | Background | Foreground | Token pair | WCAG 2.1 AA contrast | Result |
|-------|-----------|-----------|------------|----------------------|--------|
| Selected segment — light | `--gd` `#129657` | `#ffffff` | gd / white | 7.39:1 | ✅ AAA |
| Selected segment — dark | `--gd` `#129657` | `#ffffff` | gd / white | 7.39:1 | ✅ AAA |
| Unselected segment — light | transparent over `--paper` `#f5f7fc` | `--muted` `#6e7191` | paper / muted | 4.51:1 | ✅ AA |
| Unselected segment — dark | transparent over `--paper` `#0c121c` | `--muted` `#8b93a8` | paper-dark / muted-dark | 5.73:1 | ✅ AA |
| Control wrapper — light | `--paper` `#f5f7fc` | — | — | — | n/a |
| Control wrapper — dark | `--paper` `#0c121c` | — | — | — | n/a |
| Collapsed pill — light | `--paper` `#f5f7fc` | `--ink` `#41423d` | paper / ink | 8.59:1 | ✅ AAA |
| Collapsed pill — dark | `--paper` `#0c121c` | `--ink` `#e6e9ef` | paper-dark / ink-dark | 17.9:1 | ✅ AAA |

> Contrast ratios computed from W3C relative luminance formula. No new tokens introduced.
> All pairs reuse existing semantic tokens from `app/globals.css`.

### 60/30/10 surface allocation (sidebar context)

| Role | Token | Value (light) | Value (dark) |
|------|-------|--------------|-------------|
| Dominant (60%) — sidebar bg | `--surface` | `#ffffff` | `#161e2d` |
| Secondary (30%) — control wrapper | `--paper` | `#f5f7fc` | `#0c121c` |
| Accent (10%) — active segment bg | `--gd` | `#129657` | `#129657` |
| Destructive | `--danger` | `#dc2626` | `#dc2626` |

Accent (`--gd`) reserved for: active nav items (existing), active segment fill in `ViewToggle`, active segment fill in `ThemeToggle` and `LocaleToggle` (existing pattern, unchanged).

---

## Visual Contract: Expanded Sidebar (260px)

The `ViewToggle` control in **expanded** state is a two-segment segmented control rendered as a `div[role="radiogroup"]`, identical in structure to `LocaleToggle` and `ThemeToggle`.

### Wrapper

```
div[role="radiogroup"]
  className: "flex items-center rounded-full border p-1"
  style:
    background: var(--paper)
    borderColor: var(--border)
    width: 100%          ← fullWidth=true, matches LocaleToggle/ThemeToggle
```

### Segments

Two `button[role="radio"]` children, left-to-right: **Admin** then **Agent**.

```
button[role="radio"]
  className: "rounded-full px-3 py-1.5 uppercase"
  style:
    background: active ? var(--gd) : transparent
    color:      active ? #ffffff   : var(--muted)
    fontSize:   11.5px
    fontWeight: 600
    letterSpacing: 0.04em
    flex: 1
    textAlign: center
```

Segment labels:

| Segment | FR label | EN label | i18n key |
|---------|----------|----------|----------|
| Admin view | `Admin` | `Admin` | `sidebar.view.admin` |
| Agent view | `Agent` | `Agent` | `sidebar.view.agent` |

> Both labels are the same in FR and EN (proper names / roles, not translated).
> Keys follow the existing `sidebar.*` namespace.

### Ordering in the expanded footer stack

```
── divider (borderTop: 1px solid var(--border), margin: 16px 0 12px 0)
│
├── ViewToggle     ← NEW — topmost in footer controls, above LocaleToggle
├── LocaleToggle   (existing, fullWidth)
└── ThemeToggle    (existing, fullWidth)
```

Gap between controls: `gap: 8` (existing `<div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>` wrapper — add `ViewToggle` inside it, as the first child).

The divider markup is unchanged. `ViewToggle` is inserted as the first item in the existing `<div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>` container.

---

## Visual Contract: Collapsed Sidebar (72px)

In **collapsed** state the footer renders a vertical stack of pill/icon buttons (`display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'center'`).

The `ViewToggle` collapsed affordance is a single pill button that cycles the view on click and shows the current view's initial letter.

```
button[type="button"]
  onClick: cycleView (toggles Admin ↔ Agent, fires redirect per D-01)
  aria-label: t('sidebar.view.cycle', lang)  ← new i18n key
  style:
    width: 36px
    height: 28px
    borderRadius: 9999px (rounded-full)
    background: var(--paper)
    border: 1px solid var(--border)
    color: var(--ink)
    fontSize: 11.5px
    fontWeight: 600
    letterSpacing: 0.04em
    textTransform: uppercase
    cursor: pointer
  Content: currentView === 'admin' ? 'A' : 'G'
```

This matches the collapsed `LocaleToggle` pill geometry exactly (`width: 36, height: 28, borderRadius: 9999, background: var(--paper), border: 1px solid var(--border), color: var(--ink), fontSize: 11.5px, fontWeight: 600`).

### Ordering in the collapsed footer stack (top to bottom)

```
ViewToggle pill   ← NEW — topmost
LocaleToggle pill (existing: shows "fr" / "en")
ThemeToggle icon  (existing: shows Sun/Monitor/Moon)
```

The collapsed stack renders the `ViewToggle` as the first child in the existing `<div style={{ display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'center' }}>` block.

---

## Interaction Contract

### Role and ARIA

| Attribute | Expanded control | Collapsed pill |
|-----------|-----------------|----------------|
| `role` | `radiogroup` on wrapper | none (plain `button`) |
| `role="radio"` + `aria-checked` | on each segment button | n/a |
| `aria-label` on wrapper | `"Vue"` (FR) / `"View"` (EN) — use `sidebar.view.aria` key | n/a |
| `aria-label` on collapsed pill | `t('sidebar.view.cycle', lang)` | same |
| `aria-label` on individual segments | not required (text content is sufficient) | n/a |

### Keyboard interaction

| Key | Behavior |
|-----|----------|
| `Tab` | Moves focus to the segmented control wrapper (first focusable child) |
| `Arrow Left` / `Arrow Right` | Moves between segments (standard radiogroup pattern; implement with onKeyDown) |
| `Enter` / `Space` | Activates the focused segment |
| `Tab` (out) | Moves focus to next footer control (LocaleToggle) |

### Focus ring

Same pattern as existing footer controls: browser default `outline` is not suppressed. Use Tailwind `focus-visible:ring-2 focus-visible:ring-[var(--gd)] focus-visible:ring-offset-1` on each segment button, matching the existing ThemeToggle segment style (verify in `ThemeToggle.tsx` — if no explicit ring is applied there, do not add one to `ViewToggle`; stay consistent).

> `ThemeToggle` and `LocaleToggle` currently use `transition-colors` via `className` but do not
> add explicit focus rings (browser default). `ViewToggle` must match — no explicit ring beyond
> browser default unless the existing toggles already have one.

### Hover

```
background: var(--hover-overlay)   [light: rgba(17,44,59,0.04) / dark: rgba(255,255,255,0.05)]
```

Applied to unselected segment on hover. Selected segment retains `var(--gd)` on hover (no change).

### Transition

```
transition-colors   (Tailwind utility, 150ms ease — same as ThemeToggle's className)
```

### View-switch behavior (D-01, D-02 — execution, not UI)

These are interaction contracts the executor must honor:

1. On segment click: set `sessionStorage['leasetic.view']` to `'admin'` or `'agent'`, dispatch a custom event (mirror the `leasetic-sidebar-toggled` pattern from the collapse store), then `router.push()` to the new view's home (agent: `/`; admin: `adminHrefs.home`).
2. Default when `sessionStorage` key absent: `'admin'`.
3. On logout: clear `sessionStorage['leasetic.view']` (hook into the existing logout path).
4. Auto-reconcile: when inside an `(admin)` route group, the effective view is always `'admin'` regardless of the stored flag (D-02).

---

## Copywriting Contract

| Element | FR | EN | i18n key |
|---------|----|----|----------|
| Segment label — Admin | `Admin` | `Admin` | `sidebar.view.admin` |
| Segment label — Agent | `Agent` | `Agent` | `sidebar.view.agent` |
| Collapsed pill aria-label | `Changer de vue` | `Switch view` | `sidebar.view.cycle` |
| Radiogroup aria-label | `Vue` | `View` | `sidebar.view.aria` |

No empty state (the control is always present for admins).
No error state (view switching is client-only, no network call, no failure path).
No destructive action (switching view is reversible with one click; no confirmation needed).

> All 4 keys follow the existing `sidebar.*` namespace established in Phase 6.
> The `_EnHasAllFrKeys` compile-time parity proof will enforce EN completeness.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn registry used |
| third-party | none | not applicable |

No third-party component blocks are introduced in this phase. All implementation
reuses existing in-repo patterns (`ThemeToggle`, `LocaleToggle`, `useSyncExternalStore`).

---

## Constraints Carried from CONTEXT.md

| Constraint | Contract implication |
|------------|---------------------|
| C-01: view flag must survive sidebar remount | `sessionStorage` (not React state) is the authoritative store; see D-04 |
| C-02: `(authed)` layout currently passes `isAdmin={role==='admin'}` | Executor must make `isAdmin` view-flag-aware for admin users on non-admin routes |
| C-03: non-admin users never see the toggle | Gate the entire `ViewToggle` render on `isAdmin === true` (real role, not derived view) |
| C-04: toggle is nav-only, not authz | No middleware changes; no guard modifications |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
