# v1.2 — UX Polish + Proposal Wizard — REQUIREMENTS (ARCHIVED)

**Started:** 2026-05-11
**Shipped:** 2026-05-21
**Source:** `.planning/milestones/v1.2-CONTEXT.md` (Figma design session 2026-05-11)

---

## Final Status: 14/14 requirements satisfied (with documented adjustments)

---

## Database / schema

- [x] **DB-01** ✅ shipped Phase 12 (2026-05-12): System extends the `proposals.status` enum from `active | expired | deleted` to `draft | active | expired | deleted`. A new proposal starts in `draft` until the partner completes the 3-step wizard and confirms save. Drafts are only visible to their creator.
- [x] **DB-02** ✅ shipped Phase 12 (2026-05-12): System extends partner account status to include `invited` (alongside the existing actif/désactivé states). An `invited` account exists in the DB and has been emailed a one-time-URL invitation but has never logged in. Surfaces as a distinct chip in the admin Liste des partenaires (post-Phase-14 rename: now `/partners` list).
- [x] **DB-03** ✅ shipped Phase 12 (2026-05-12): System persists every coefficient change to a `coefficient_history` table — columns: `id`, `changed_at`, `changed_by_user_id` (FK to `users`), `before_json` (jsonb snapshot of prior `global_params`), `after_json`, `summary` (short human label). Rows are append-only; UPDATE/DELETE rejected at DB level via Postgres TRIGGER.

## Routing

- [x] **ROUTE-01** ✅ shipped Phase 13 (2026-05-12): System splits `/proposals/new` into a 3-step wizard at three new routes: `/proposals/new/parametres`, `/proposals/new/calcul`, `/proposals/new/verification`. State persists between steps via the `draft` proposal row (DB-01). A stepper widget at the top of each step shows current/done/pending state; partners can click back to any *completed* step but cannot jump ahead (D-20..D-23 Stepper completion-state semantics).
  - **D-12 RELAXATION:** Commission visible to deal-owner partner on step 2 + step 3 ● CALCUL recap surfaces (signed-off STRIDE addendum). All other ADMIN-09 invariants preserved (PDF render, audit_log, server logs, pre-finalize traces).
- [x] **ROUTE-02** ✅ shipped Phase 14 (2026-05-20): System exposes a dedicated route `/[adminSegment]/partners/new` that replaces v1.1's modal-based partner creation. Form contains three sections — personal info (prénom, nom, email), company info (société, SIRET optionnel, téléphone), and a customizable invitation message. Submitting issues the same one-time-URL invitation as v1.1.
  - **#2 ADJUSTMENT:** Modal "removal" satisfied via CTA behavior change (button → Link to /partners/new), not code deletion. `CreatePartnerModal.tsx` stays in repo as shelf code (D-10).
  - **#3 PARTIAL DEFERRAL:** Admin home 3 AdminNavCards shipped ✅; partner home 3 MetricTiles deferred to v1.3 (with dashboard restructure).

## Reusable components

- [x] **COMP-01** ✅ shipped Phase 11: `<Stepper>` component used on all 3 wizard routes. Props: `currentStep: 1 | 2 | 3`, `completedSteps: number[]`. Each step renders one of three visual states (active/pending/done with white check).
- [x] **COMP-02** ✅ shipped Phase 11: `<RetractableSidebar>` component used in `(authed)` and `(admin)` layouts. Two width states: 260px expanded / 72px collapsed. User preference persisted in localStorage.
- [x] **COMP-03** ✅ shipped Phase 11: `<MetricTile>` component (shipped; consumer-side deferred to v1.3 — see ROUTE-02 #3 above).
- [x] **COMP-04** ✅ shipped Phase 11 + Phase 14: `<AdminNavCard>` component used 3× on the admin home (Coefficients / Partenaires / Historique).
- [x] **COMP-05** ✅ shipped Phase 11 + extended Phase 14: `<StatusChip>` variants — `active` (green), `draft` (gold), `expired` (gray), `disabled` (red), `invited` (gold, added Phase 14-01 D-26). Used in: proposal list rows, proposal detail header, partner table rows.

## Brand assets

- [x] **ASSET-01** ✅ shipped Phase 11: `public/logo-light.svg` exists with mark color `#6DC388` (`--brand-mark`) and `--navy` wordmark.
- [x] **ASSET-02** ✅ shipped Phase 11: `public/logo-dark.svg` exists with same mark color + light-ink wordmark; CSS picker via `data-theme` attribute auto-selects.

## Public surfaces

- [x] **PUB-01** ✅ shipped Phase 15 (2026-05-21): Login page (`/login`) replaces its v1.1 plain-text "Leasétic" header with the official lockup SVG (ASSET-01) centered above the existing form card. Body bg `--paper`, form card `--surface`. Form structure unchanged. Top-right LocaleToggle + ThemeToggle preserved.
- [x] **PUB-02** ✅ shipped Phase 15 (2026-05-21): `/invite/[token]` and `/reset/[token]` inherit the same `(public)` shell pattern via the shared `app/(public)/layout.tsx`.

---

## Traceability — Final

| Requirement | Phase | Status |
|-------------|-------|--------|
| DB-01 | Phase 12 | ✅ shipped 2026-05-12 |
| DB-02 | Phase 12 | ✅ shipped 2026-05-12 |
| DB-03 | Phase 12 | ✅ shipped 2026-05-12 |
| ROUTE-01 | Phase 13 | ✅ shipped 2026-05-12 (with D-12 ADMIN-09 partial relaxation, signed-off STRIDE addendum) |
| ROUTE-02 | Phase 14 | ✅ shipped 2026-05-20 (with #3 partner-home MetricTiles deferred to v1.3) |
| COMP-01 | Phase 11 | ✅ shipped |
| COMP-02 | Phase 11 | ✅ shipped |
| COMP-03 | Phase 11 | ✅ shipped (consumer deferred to v1.3) |
| COMP-04 | Phase 11 | ✅ shipped (consumed in Phase 14) |
| COMP-05 | Phase 11 | ✅ shipped (extended in Phase 14 with `invited` variant) |
| ASSET-01 | Phase 11 | ✅ shipped |
| ASSET-02 | Phase 11 | ✅ shipped |
| PUB-01 | Phase 15 | ✅ shipped 2026-05-21 |
| PUB-02 | Phase 15 | ✅ shipped 2026-05-21 |

**Coverage:** 14/14 satisfied.

---

## Adjustments / outcomes worth flagging

1. **ROUTE-02 #2 (modal removal):** satisfied via behavior change, not code deletion. The modal `CreatePartnerModal.tsx` stays in the codebase as shelf code per Phase 14 D-10; the active CTA in the partner list became a `<Link>` to `/partners/new`. The user's intent (no longer trigger the modal from the list UX) is satisfied without losing the working modal flow.

2. **ROUTE-02 #3 (partner-home MetricTiles):** partial deferral to v1.3. Admin home 3 AdminNavCards shipped ✅; partner home 3 MetricTiles deferred together with the full dashboard restructure (hero + tiles + 5-row recent + /proposals route).

3. **Color-contrast measurement** for Phase 14 diff-panel changed-row composite: deferred to v1.3 color refresh per user signal during Phase 14 close. Hard prerequisite for any v1.3 plan touching `--gold` / `.chip-invited` / diff-panel composite.

4. **ADMIN-09 D-12 partner-facing commission relaxation** (Phase 13): first deliberate departure from "commission invisible to partner" since Phase 9. Signed-off STRIDE addendum at `docs/security/13-stride-addendum-admin09-step2.md`. Phase 14 added a 9-gate grep-contract suite in CI to mechanically enforce zero further relaxation on admin surfaces.

5. **Phase 12 ship gap** (drizzle migration journal missing): resolved 2026-05-21. Carry-forward Tier-2: add post-deploy DB-smoke CI step.

---

*Archived: 2026-05-21 via `/gsd-complete-milestone v1.2`*
