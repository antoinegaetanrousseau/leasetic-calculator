---
phase: 26-active-expired-row-actions
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/proposals/ProposalRow.tsx
  - src/components/proposals/ProposalsList.tsx
autonomous: false
requirements: [ROWACT-01, ROWACT-03, ROWACT-04]
must_haves:
  truths:
    - "Active and expired /proposals rows render the Archive icon button; clicking the body still opens the proposal detail page; clicking Archive archives without navigating."
    - "Deleted rows in the Archivees view render a Restore icon button that restores in place."
    - "Active/expired rows are a clickable div (role=button), not a full-row Link; the action button uses stopPropagation so it does not navigate."
    - "Draft rows keep Edit + Archive + Delete via the existing DraftActionsClient, unchanged."
  artifacts:
    - path: "src/components/proposals/ProposalRow.tsx"
      provides: "Finalized-row clickable-div + action-slot branch (generalized from draftMode)"
      contains: "actionsSlot"
    - path: "src/components/proposals/ProposalsList.tsx"
      provides: "Per-row mounting of RowActionsClient keyed off row.displayStatus"
      contains: "RowActionsClient"
  key_links:
    - from: "src/components/proposals/ProposalsList.tsx"
      to: "src/components/proposals/RowActionsClient.tsx"
      via: "JSX mount in the row map via actionsSlot"
      pattern: "RowActionsClient"
    - from: "src/components/proposals/ProposalRow.tsx"
      to: "/proposals/{id}"
      via: "router.push on clickable-div body click"
      pattern: "/proposals/"
---

<objective>
Wire RowActionsClient (built in Plan 01) into the list. Two edits: (1) ProposalRow must give finalized (active/expired/deleted) rows a clickable-div body (role="button") that opens the detail page on click, plus an action slot on the right — generalizing the existing `draftMode` branch rather than inventing a new mechanism (D-06). (2) ProposalsList must mount RowActionsClient for every non-draft row, keyed off `row.displayStatus` (D-03): active/expired -> Archive, deleted -> Restore. Draft rows keep DraftActionsClient untouched.

Purpose: completes ROWACT-01 (archive from the row, list reflows in place via router.refresh + the page's remountKey) and ROWACT-03 (finalized rows expose only Archive; no Edit/Delete) end-to-end. ROWACT-04 holds — only `displayStatus` (bounded union) is read; the DTO is unchanged.
Output: edited ProposalRow.tsx + ProposalsList.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-active-expired-row-actions/26-CONTEXT.md
@.planning/phases/26-active-expired-row-actions/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize ProposalRow finalized branch to a clickable div with an action slot</name>
  <read_first>
    - src/components/proposals/ProposalRow.tsx (current: finalized rows render a full-row `<Link href={'/proposals/' + row.id}>`; draftMode renders a clickable div role=button with onClick router.push('/proposals/new/parametres?draft_id=' + row.id) and a draftActionsSlot; deleted adds restoreSlot INSIDE the Link. The `columns` JSX, `chipLabelKey`, `ariaLabel`, and StatusChip render are shared. useRouter is already imported.)
    - src/components/proposals/ProposalsList.tsx (the caller — to keep prop names consistent with Task 2)
  </read_first>
  <action>
    Edit src/components/proposals/ProposalRow.tsx so finalized (non-draft) rows can render an action slot AND remain body-clickable to the detail page (D-06). Add a new optional prop `actionsSlot?: React.ReactNode` (the finalized-row analogue of `draftActionsSlot`). Change the default (non-draftMode) render path from the `<Link>` to a clickable `div` carrying className, `role="button"`, `tabIndex={0}`, `aria-label={ariaLabel}`, `onClick={() => router.push('/proposals/' + row.id)}`, and `onKeyDown={(e) => { if (e.key === 'Enter') router.push('/proposals/' + row.id); }}` — mirroring the existing draftMode div. Render `{columns}` then `{actionsSlot}` (covers active/expired Archive and deleted Restore). Keep the `deleted` className branch ('list-row is-deleted'); the deleted Restore button now arrives via `actionsSlot`, so `restoreSlot` is no longer the wiring path — keep the `restoreSlot` prop accepted for backward compat but stop rendering it in the finalized path (the caller in Task 2 stops passing it). Preserve the existing `draftMode` branch VERBATIM (drafts unchanged — still push to the wizard). className resolution stays: deleted -> 'list-row is-deleted', draftMode -> 'list-row is-draft', else 'list-row'. Do not change `columns`, `chipLabelKey`, `ariaLabel`, or StatusChip logic. Do NOT read or project commission/paramsSnapshot. No fenced code.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit 2>&1 | grep -i "ProposalRow" || echo TYPECHECK_CLEAN_FOR_FILE</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "actionsSlot" src/components/proposals/ProposalRow.tsx >= 2 (prop declared + rendered).
    - Source: the finalized (non-draftMode) path renders a div with role="button" and an onClick that router.push-es to '/proposals/' + row.id — read the branch to confirm (no longer a full-row `<Link>`).
    - Source: grep -c "draft_id=" src/components/proposals/ProposalRow.tsx >= 1 (draftMode branch unchanged, still pushes to the wizard).
    - Build: npx tsc --noEmit reports no ProposalRow error.
  </acceptance_criteria>
  <done>Finalized rows are clickable divs that open the detail page and accept an actionsSlot; draft rows unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RowActionsClient per row in ProposalsList, keyed off displayStatus</name>
  <read_first>
    - src/components/proposals/ProposalsList.tsx (current: reads q/deleted/drafts from searchParams; maps rows to ProposalRow passing deleted, draftMode, restoreSlot=RestoreButtonClient when deleted, draftActionsSlot=DraftActionsClient when draftMode)
    - src/components/proposals/RowActionsClient.tsx (built in Plan 01 — props proposalId + lang + displayStatus; renders null for draft, Archive for active/expired, Restore for deleted)
    - src/components/proposals/ProposalRow.tsx (the actionsSlot prop added in Task 1)
    - src/lib/api/proposals/list.ts (ProposalRowDto.displayStatus is the per-row signal)
  </read_first>
  <action>
    Edit src/components/proposals/ProposalsList.tsx. Add `import { RowActionsClient } from './RowActionsClient';`. In the rows.map, for NON-draft rows pass the new `actionsSlot` prop on ProposalRow: actionsSlot={ !draftMode ? <RowActionsClient proposalId={row.id} lang={lang} displayStatus={row.displayStatus} /> : null }. Because RowActionsClient itself returns the Archive button for active/expired and the Restore button for deleted (D-03 keyed off displayStatus), the view (deleted/archived searchParam) does NOT decide which button — it is purely row-driven, which naturally covers both the Archivees view (expired-by-time rows show Archive; soft-deleted rows show Restore) and the default Actives view. Keep the existing draftActionsSlot wiring (DraftActionsClient) for draftMode rows EXACTLY as-is. Remove the legacy `restoreSlot={ deleted ? <RestoreButtonClient ... /> : null }` wiring (set it to null or drop the prop) so the deleted view shows the compact icon-button Restore from RowActionsClient instead of the full-width btn-green RestoreButtonClient — note this swap in the SUMMARY. You may drop the now-unused RestoreButtonClient import if nothing else uses it. Do not change the empty-state blocks, LoadMoreButton, or the useState reflow logic. No fenced code.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit 2>&1 | grep -i "ProposalsList\|RowActionsClient" || echo TYPECHECK_CLEAN</automated>
  </verify>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx vitest run --reporter=dot 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "RowActionsClient" src/components/proposals/ProposalsList.tsx >= 2 (import + mount).
    - Source: grep -c "displayStatus={row.displayStatus}" src/components/proposals/ProposalsList.tsx returns 1 (per-row keying, D-03).
    - Source: grep -c "DraftActionsClient" src/components/proposals/ProposalsList.tsx >= 1 (draft wiring preserved).
    - Behavior: read the map — a draftMode row still receives draftActionsSlot=DraftActionsClient and actionsSlot null/absent (drafts keep Edit+Archive+Delete); a non-draft row receives actionsSlot=RowActionsClient.
    - Test: full suite green — npx vitest run passes including tests/admin-09-grep-contracts (gates unmodified) and the dictionaries parity test.
    - Build: npx tsc --noEmit clean.
  </acceptance_criteria>
  <done>Non-draft rows render RowActionsClient (Archive for active/expired, Restore for deleted) via actionsSlot; draft rows unchanged; full test suite + tsc green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Per-row Archive button on active/expired /proposals rows + per-row Restore in the Archivees view, with the row body still opening the detail page.</what-built>
  <how-to-verify>
    1. Run the dev server (npm run dev) and log in as a partner with at least one active proposal.
    2. Visit /proposals (Actives). Confirm each active/expired row shows an Archive icon button on the right and NO Edit and NO Delete button.
    3. Click the row body (not the icon) — it should open /proposals/{id} (detail page).
    4. Back on /proposals, click the Archive icon on an active row. Expect: instant (no confirm dialog), a success toast with a "Voir les archivees" action link, the row disappears from Actives in place (no full-page reload flash).
    5. Click "Voir les archivees" (or the Archivees pill -> /proposals?archived=1). Confirm the archived proposal appears, showing a Restore icon button.
    6. Click Restore. Expect a success toast and the row returns to Actives (list refreshes in place).
    7. Visit /proposals?drafts=1 and confirm draft rows STILL show Edit + Archive + Delete (unchanged).
    8. Toggle dark mode and re-check the icon buttons render correctly in both themes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (wrong button on a row, navigation on icon click, missing toast, draft regression, dark-mode glitch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (partner) → /api/proposals/{id}/{delete,restore} | proposalId crosses here via the mounted RowActionsClient |
| server → client (ProposalRowDto) | only displayStatus (bounded union) is read per row; no commission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation/IDOR | ProposalsList → RowActionsClient → existing endpoints | accept (mitigated upstream) | Wiring only; the archive/restore endpoints already scope by `requireUser()` session.user.id (verified in delete/restore route source — userId never from request body). No new endpoint, no client-supplied owner. Residual LOW. |
| T-26-05 | Information Disclosure | ProposalRow / ProposalsList render path | mitigate | Only row.displayStatus (4-string union) is read to choose the button; row.id + lang are the only other inputs. paramsSnapshot/commission never projected onto ProposalRowDto (list.ts toDto whitelist). admin-09 grep-contract suite stays green unmodified. ROWACT-04 envelope held. Severity LOW. |
| T-26-06 | Spoofing / wrong-action | displayStatus-keyed render | mitigate | Restore only renders for displayStatus==='deleted' (D-03); restoreProposal WHERE status='deleted' makes a wrong call a no-op 404. Archive only renders for active/expired; softDeleteProposal WHERE status='active' (expired-by-time rows keep DB status 'active') so archive succeeds for both. No destructive cross-state action possible. Severity LOW. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed. |
</threat_model>

<verification>
- npx tsc --noEmit clean.
- npx vitest run full suite green, including tests/admin-09-grep-contracts (gates unmodified) and the dictionaries _EnHasAllFrKeys parity test.
- Human checkpoint confirms the row-action UX (Archive/Restore/body-click/draft-unchanged) in light + dark.
</verification>

<success_criteria>
- ROWACT-01: archiving an active/expired row from /proposals moves it to Archivees with in-place reflow (no full-page navigation).
- ROWACT-03: finalized rows show only Archive (active/expired) or Restore (deleted); drafts keep Edit+Archive+Delete.
- ROWACT-04: grep-contract suite passes unmodified.
- D-02: Restore button present in the Archivees view.
- D-06: row body still opens the detail page.
</success_criteria>

<output>
Create `.planning/phases/26-active-expired-row-actions/26-02-SUMMARY.md` when done.
</output>
