---
phase: 27-status-pill-rendering-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(authed)/page.tsx
autonomous: true
requirements: [UIFIX-02]
must_haves:
  truths:
    - "The home 'Propositions récentes' status chip renders its full label (Actif / Brouillon / Expirée / Archivée) with no text clipping and no fixed-width stretch artifact."
    - "The home recent-list row column order is clientCo → lcRef → amount → chip (chip trailing, matching /proposals)."
    - "The chip hugs its content (content-track + non-stretching justify-self), not pinned to a fixed 90px track."
    - "The row's other inline styles (clientCo 1fr truncation, font sizes, paddings, amount tabular-nums right-align) are preserved."
  artifacts:
    - path: "app/(authed)/page.tsx"
      provides: "Home recent-list row with trailing content-hugging StatusChip"
      contains: "justify-self"
  key_links:
    - from: "app/(authed)/page.tsx"
      to: "src/components/ui/StatusChip.tsx"
      via: "StatusChip rendered as the LAST grid child in the recentRows.map row"
      pattern: "StatusChip"
---

<objective>
Fix the home "Propositions récentes" status-pill rendering (UIFIX-02). The home
recent-list row in `app/(authed)/page.tsx` currently renders `<StatusChip>` as
the FIRST child of an inline grid whose `gridTemplateColumns` is
`'90px 1fr auto auto'`, pinning the chip into a fixed `90px` track. Because a
grid item defaults to `justify-self: stretch`, the chip fills the 90px track —
producing the fixed-width / clipping artifact. The `.chip` base already hugs in
isolation (`display: inline-flex; padding: 4px 8px`, globals.css §`.chip`); the
defect lives entirely at this consumer's grid track (carried diagnosis from
Phase 25 D-03).

Two coupled edits, both inside the `recentRows.map(...)` row, per D-02:
1. Reorder the JSX children so the column order becomes
   clientCo → lcRef → amount → chip (chip TRAILING, matching `ProposalRow`).
2. Change the inline `gridTemplateColumns` so the chip lands in a
   content-hugging track and set the chip wrapper to not stretch.

Purpose: closes UIFIX-02 — the home chip reads identically to `/proposals`
(trailing, content-hugging) with no clipping/stretch.
Output: edited `app/(authed)/page.tsx` (single file; recent-list `.map()` block only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-status-pill-rendering-fix/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder the home recent-list row to a trailing, content-hugging chip</name>
  <read_first>
    - app/(authed)/page.tsx (the fix site — the `recentRows.map((row) => { ... })` block, currently ~lines 149-207. The returned `<Link className="list-row" style={{ display:'grid', gridTemplateColumns:'90px 1fr auto auto', alignItems:'center', gap:16, padding:'10px 12px', borderRadius:8, textDecoration:'none' }}>` has FOUR grid children in this order: (1) `<StatusChip variant=... label=.../>`, (2) clientCo `<span>` with 1fr truncation (overflow hidden + textOverflow ellipsis + whiteSpace nowrap, fontWeight 600), (3) lcRef `<span>` monospace fontSize 13, (4) amount `<span>` textAlign right + fontVariantNumeric tabular-nums. The chipKey/StatusChip variant logic and all other spans must be preserved verbatim. Note: this inline `gridTemplateColumns` overrides the `.list-row` CSS class grid — the fix is entirely inline here, do NOT touch globals.css.)
    - src/components/ui/StatusChip.tsx (renders a bare `<span className="chip chip-{variant}">{label}</span>` — owns no sizing; it inherits the grid item's justify-self, which is why a stretch track stretches it. No change here.)
    - app/globals.css §.chip ~lines 355-364 (display:inline-flex; padding:4px 8px; border-radius:9999px — already content-hugging in isolation; do NOT edit, especially not the .chip-* color rules below it).
  </read_first>
  <action>
    Edit ONLY the row returned inside `recentRows.map(...)` in app/(authed)/page.tsx.

    (a) Column order — move `<StatusChip variant={row.displayStatus} label={t(chipKey, lang)} />` from the FIRST grid child to the LAST grid child, so the children read in this order: clientCo span → lcRef span → amount span → StatusChip. This matches ProposalRow's clientCo → lcRef → amount → … → StatusChip ordering (D-02). Do not alter the inner markup/styles of the three text spans, the `chipKey` derivation, the `key={row.id}`, `href`, `aria-label`, or `className="list-row"`.

    (b) Grid track — change the inline `gridTemplateColumns` value from `'90px 1fr auto auto'` to `'1fr auto auto max-content'` so the columns are: clientCo `1fr` (keeps the existing ellipsis truncation), lcRef `auto`, amount `auto`, chip `max-content` (content-hugging — mirrors the `/proposals` `.list-row` chip track). Keep the other inline grid props (`display:'grid'`, `alignItems:'center'`, `gap:16`, `padding:'10px 12px'`, `borderRadius:8`, `textDecoration:'none'`) unchanged.

    (c) Chip must not stretch — wrap the trailing `<StatusChip>` in a `<span>` that sets `justifySelf: 'start'` (so the chip aligns to the start of its track and never stretches to fill it). Alternatively, since the chip is now in a `max-content` track it cannot exceed its content, but ADD the `justifySelf: 'start'` wrapper anyway as belt-and-suspenders so the chip never inherits a stretch from any future track change. Apply ONLY `justifySelf: 'start'` (and `display:'inline-flex'` if needed to keep the wrapper from itself stretching) on the wrapper — do not add padding, margins, width, or any color.

    Do not change StatusChip.tsx or globals.css. Do not touch chip colors / variants / `--brand-mark` / `--gd` / `.chip-active`. Do not touch the `recentRows.length === 0` empty-state block, the `<section className="card">`, the "Voir toutes →" link, the PageHero, or the MetricTile grid. No fenced code blocks.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npx tsc --noEmit 2>&1 | grep -i "page.tsx" || echo TYPECHECK_CLEAN_FOR_FILE</automated>
  </verify>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run lint:check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "90px 1fr auto auto" "app/(authed)/page.tsx"` returns 0 (old fixed-90px track gone).
    - Source: `grep -c "max-content" "app/(authed)/page.tsx"` returns >= 1 (chip now in a content-hugging track).
    - Source: `grep -c "justify-self\|justifySelf" "app/(authed)/page.tsx"` returns >= 1 (chip wrapper does not stretch).
    - Behavior: read the `recentRows.map` row — `<StatusChip>` is the LAST grid child; the three text spans (clientCo with overflow/ellipsis/nowrap, lcRef monospace, amount tabular-nums) precede it and are otherwise unchanged.
    - Build: `npx tsc --noEmit` reports no error in page.tsx; `npm run lint:check` passes with 0 warnings.
  </acceptance_criteria>
  <done>The home recent-list row renders clientCo → lcRef → amount → trailing content-hugging chip; tsc + lint:check green; globals.css and StatusChip.tsx untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (no page.tsx error).
- `npm run lint:check` passes (eslint --max-warnings=0).
- No change to globals.css `.chip*` rules, StatusChip.tsx, or any color token.
- Visual light/dark confirmation deferred to the Plan 27-02 human-verify checkpoint.
</verification>

<success_criteria>
- UIFIX-02: the home "Propositions récentes" chip is trailing (clientCo → lcRef → amount → chip) and sits in a content-hugging track with no fixed-width stretch — source-verified here; visual full-label / no-clipping confirmation in 27-02.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side CSS grid layout change only; no boundary added or crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | home recent-list row (`app/(authed)/page.tsx`) | n/a | Reorders existing children + changes a grid track + `justify-self`. No new field projected; `row.displayStatus`/`clientCo`/`lcRef`/`amountHT` were already rendered. No commission/paramsSnapshot exposure; ADMIN-09 grep suite stays green unmodified. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed. |

No Spoofing / Repudiation / DoS / Elevation surface: presentational sizing change, no inputs, endpoints, auth, or persistence touched. Chip colors/variants and `.chip` base frozen.
</threat_model>

<output>
Create `.planning/phases/27-status-pill-rendering-fix/27-01-SUMMARY.md` when done.
</output>
