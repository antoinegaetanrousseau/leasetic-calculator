---
phase: 38-shell-dialogs-visual-conventions
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - app/globals.css
  - src/components/proposals/LoadMoreButton.tsx
  - .planning/codebase/UI-CONVENTIONS.md
  - .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md
autonomous: true
requirements: [GAP-04]

must_haves:
  truths:
    - "The shared .btn-green/.btn-navy/.btn-out rule renders at an on-grid 8px vertical padding, landing the three classes on button.tsx's 36px default step"
    - "All six focus selectors render the same var(--ring) two-layer ring; no hardcoded focus colour survives anywhere in globals.css"
    - "The 'Charger plus' button's accessible name equals its visible text in both idle and loading states"
    - "UI-CONVENTIONS.md carries UIC-11, so a fifth hardcoded ring is a violation rather than an accident"
  artifacts:
    - path: "app/globals.css"
      provides: "on-grid button padding + the unified var(--ring) focus treatment, inside @layer components"
      contains: "color-mix(in oklab, var(--ring) 50%, transparent)"
    - path: ".planning/codebase/UI-CONVENTIONS.md"
      provides: "UIC-11 — focus ring rule"
      contains: "UIC-11"
    - path: ".planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md"
      provides: "Measured enumeration of every .btn-green/.btn-navy/.btn-out file, for the CLOSE-08 walk"
  key_links:
    - from: "app/globals.css .btn-* :focus-visible / .search-bar:focus-within"
      to: "--ring token (app/globals.css:52 light, :195 dark)"
      via: "box-shadow: 0 0 0 2px var(--ring), 0 0 0 5px color-mix(...)"
      pattern: "0 0 0 2px var\\(--ring\\)"
    - from: "src/components/proposals/LoadMoreButton.tsx"
      to: "its own visible text branch"
      via: "removal of the static aria-label so visible text becomes the accessible name"
      pattern: "aria-label"
---

<objective>
Close GAP-04: the shared legacy button rule stops declaring an off-grid vertical padding, the four
hardcoded teal focus literals are retired in favour of the app's existing `--ring` token, the
"Charger plus" control's accessible name stops disagreeing with its visible text, and
`UI-CONVENTIONS.md` gains UIC-11 so the next divergent ring is a violation rather than an accident.

Purpose: `padding: 0.6rem` computes to 9.6px — off UIC-01's 4px grid — and puts three legacy button
classes at a ~39px height that matches neither `button.tsx`'s `default` (36px) nor its `lg` (40px),
i.e. a fourth undeclared button height rendering next to real `Button`s across the app. Separately,
four `rgba(45, 122, 140, …)` literals give a fixed teal focus ring that does not adapt to theme and
measures 1.19:1 against the worst dark surface — the focus indicator is at its weakest in the theme
this phase is about to walk.

**GAP-04's escape hatch is NOT taken.** Success criterion 4 allowed either fixing the padding and
focus treatment or recording them as a dated exception. 38-CONTEXT.md D-38-13 explicitly rejects
recording `0.6rem` as an exception ("it would ratify a value matching neither the grid nor the
button scale"), and A-38-03 is an operator decision dated 2026-09-06 to retire the teal. This plan
therefore implements the fix branch. Do not add an exception entry to `UI-CONVENTIONS.md`.

Output: three edited `app/globals.css` regions, one deleted JSX prop, UIC-11, and a measured
surface enumeration that plan 38-04's walk consumes.

Verified on disk 2026-09-06 before planning — nothing here has already landed:
- `app/globals.css:379` still reads `padding: 0.6rem 1.5rem;`
- `grep -n "rgba(45, 122, 140" app/globals.css` returns **7** hits — lines 398, 416, 443, 463, 467,
  481, 516. Only **four** of those (398, 443, 467, 516) are focus rings and in scope.
- `src/components/proposals/LoadMoreButton.tsx:62` still carries the static `aria-label`
- `grep -c "^## UIC-11" .planning/codebase/UI-CONVENTIONS.md` → 0
- Measured blast radius today (`grep -rl … src app | wc -l`): 21 files use `.btn-green`, 3 use
  `.btn-navy`, 19 use `.btn-out`, **32 distinct files** overall. 38-UI-SPEC.md's A-38-01 states
  18/2/18/31 — that count predates Phase 37's additions. Re-measure at execution time; treat the
  measured number as authoritative and record any drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-PATTERNS.md
@.planning/codebase/UI-CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: globals.css — on-grid padding and the unified var(--ring) focus treatment</name>

  <files>app/globals.css</files>

  <read_first>
    - `app/globals.css` lines 40–60 (the `:root` token block containing `--ring: var(--brand-accent);`
      at line 52) and lines 185–200 (the `html[data-theme="dark"]` block containing the second
      `--ring: var(--brand-accent);` at line 195). Read these to confirm `--ring` resolves in BOTH
      themes before repointing anything at it.
    - `app/globals.css` lines 305–340 — the deliberately-unlayered `invalid` / `brand-logo` block
      (line 305's comment "Deliberately OUTSIDE `@layer components`, both of them"), the
      `color-mix(in oklab, var(--destructive) 12%, transparent)` idiom at line 325, the layering
      comment at lines 332–337, and the `@layer components {` opening at line 338. **Every rule this
      task edits sits inside that `@layer components` block (lines 338–~630).** Nothing moves layer.
    - `app/globals.css` lines 370–400 (the shared `.btn-green, .btn-navy, .btn-out` base rule at
      372–387 including `padding: 0.6rem 1.5rem;` at 379 and `border: none;`, the `.btn-out` border
      override, and the `:focus-visible` block at 394–399).
    - `app/globals.css` lines 410–420, 438–448, 458–470, 476–486, 510–520 — so you see ALL SEVEN
      `rgba(45, 122, 140, …)` occurrences and can tell the four focus rings from the three
      out-of-scope ones.
    - `src/components/ui/button.tsx` lines 1–35 — the `focus-visible:border-ring …ring-ring/50`
      base class and the `size` scale (`default` h-9 = 36px, `sm` h-8, `lg` h-10) this padding is
      being aligned to. Read only; `button.tsx` is NOT edited by this phase.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § Color and
      § Focus Treatment — the required CSS and the measured contrast table.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-PATTERNS.md` § "the six focus-ring
      selectors" — the four current blocks quoted verbatim with line numbers.
  </read_first>

  <action>
    Two independent edits to `app/globals.css`, both inside the existing `@layer components` block
    that opens at line 338. Do not create a new layer, do not move any rule between layers, and do
    not touch the deliberately-unlayered `invalid` / `brand-logo` rules above line 338.

    **Edit A — padding (D-38-13, blast radius per A-38-01).** In the shared
    `.btn-green, .btn-navy, .btn-out` base rule (line 372–387), change `padding: 0.6rem 1.5rem;` to
    `padding: 0.5rem 1.5rem;`. Vertical only — the horizontal `1.5rem` is unchanged. Math:
    `0.5rem × 2 + ~20px line content ≈ 36px`, which is `button.tsx`'s `size-default` (`h-9`) exactly.
    This is one declaration serving all three classes across 32 measured files, not 32 edits.
    Leave `border: none;` on the base rule and `border: 1px solid var(--border);` on the `.btn-out`
    override exactly as they are: per A-38-02 that border adds ~2px so `.btn-out` stays ~2px taller
    than its two siblings, and this phase **records that delta rather than fixing it** — equalising
    the three heights is outside GAP-04's padding+focus scope.

    **Edit B — focus treatment (A-38-03, supersedes D-38-14).** Apply the identical two-layer
    treatment to all six selectors across four rule blocks. In each block set `outline: none;` and
    replace the existing `box-shadow` declaration with a two-layer shadow whose first layer is
    `0 0 0 2px var(--ring)` and whose second is
    `0 0 0 5px color-mix(in oklab, var(--ring) 50%, transparent)`. The four blocks:
      1. `.btn-green:focus-visible, .btn-navy:focus-visible, .btn-out:focus-visible` (lines 394–399)
         — already has `outline: none;`, replace only the `box-shadow` line.
      2. `.search-bar:focus-within` (lines 441–444) — **keep** its existing
         `border-color: var(--teal);` line untouched (38-PATTERNS.md is explicit that only the
         `box-shadow` line is in scope), **add** an `outline: none;` line so it matches the other
         five, and replace its `0 0 0 3px rgba(45, 122, 140, 0.12)` shadow. Its weaker 0.12 tier
         collapses into the shared treatment; it was never a stated design decision.
      3. `.admin-nav-card:focus-visible` (lines 465–468).
      4. `.stepper-circle:focus-visible` (lines 514–517).

    `--ring` is `var(--brand-accent)` = `#01cc72` and is already declared in both themes at lines 52
    and 195. **Declare no new custom property** — not `--focus-ring`, not anything else. A-38-03 is
    an explicit operator decision against minting a token: the app already has 30 files consuming
    `--ring` through `button.tsx`/`input.tsx`'s Tailwind utilities, and a new token would create a
    third focus system. The raw `box-shadow` form is used rather than those utilities because the
    shared base rule declares `border: none`, so a `border-ring`-based treatment would collapse to
    the halo alone on `.btn-green` and `.btn-navy`. Because this is a shadow and not a border, it has
    zero layout impact and does not disturb Edit A's 36px height math.

    **Do not touch the three out-of-scope teal occurrences** — line 416
    (`background: rgba(45, 122, 140, 0.10)`), line 463
    (`box-shadow: 0 2px 8px rgba(45, 122, 140, 0.12)` — an elevation shadow, not a focus ring) and
    line 481 (`background: rgba(45, 122, 140, 0.07)`). GAP-04 names the focus treatment only.

    **Do not add any comment to `app/globals.css` containing the literal text `rgba(45, 122, 140`.**
    The acceptance criteria below count occurrences of that string, and per `UI-CONVENTIONS.md`'s
    "grep-based acceptance criteria measure prose too" note, a well-meaning comment naming the
    retired literal would silently inflate the count and make the gate unreadable.

    Also do not record the divergence between the app's `--ring` (`#01cc72`) and the design system's
    `--border-focus` (`#0b935b`) anywhere — A-38-05 is an explicit operator decision to leave it
    as-is and NOT record it as a finding, in UI-SPEC, UIC-11 or a backlog item.
  </action>

  <verify>
    <automated>grep -c "rgba(45, 122, 140" app/globals.css</automated>
    <automated>npm run build > /tmp/p38-02-build.log 2>&1; echo "build exit $?"; tail -4 /tmp/p38-02-build.log</automated>
    <automated>npm run test > /tmp/p38-02-test.log 2>&1; echo "test exit $?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "rgba(45, 122, 140" app/globals.css` prints exactly `3` (down from 7 — the four focus
      literals are gone, the three non-focus uses at the former lines 416/463/481 survive).
    - `grep -c "0 0 0 3px rgba(45, 122, 140" app/globals.css` prints `0`.
    - `grep -c "0 0 0 2px var(--ring)" app/globals.css` prints exactly `4` (one per rule block).
    - `grep -c "color-mix(in oklab, var(--ring) 50%, transparent)" app/globals.css` prints exactly `4`.
    - `grep -c "padding: 0.6rem" app/globals.css` prints `0`; `grep -c "padding: 0.5rem 1.5rem" app/globals.css`
      prints at least `1`.
    - `grep -c -- "--focus-ring" app/globals.css` prints `0` — no new token was minted (A-38-03).
    - `grep -c "border-color: var(--teal)" app/globals.css` prints at least `1` — `.search-bar`'s
      border-color line was preserved, not collateral damage.
    - `grep -c "0 2px 8px rgba(45, 122, 140, 0.12)" app/globals.css` prints `1` — the elevation
      shadow at the former line 463 was NOT rewritten.
    - `grep -c "border: 1px solid var(--border)" app/globals.css` is unchanged from before the edit —
      `.btn-out`'s border override survives (A-38-02: recorded, not fixed).
    - `awk 'NR>=338 && /^@layer components/{found=1} END{exit !found}' app/globals.css` succeeds AND
      every edited rule's line number is greater than the line number of the `@layer components {`
      opening — confirm by `grep -n "@layer components" app/globals.css` and
      `grep -n "0 0 0 2px var(--ring)" app/globals.css` and comparing; all four ring line numbers
      must exceed the layer-opening line number.
    - `npm run build` exits 0 (run as `npm run build > log 2>&1; echo "exit $?"` — never read a
      piped `tail`'s exit code, per the Phase 28 lesson in 38-CONTEXT.md).
    - `npm run test` exits 0 — in particular `tests/dark-palette.test.ts` (which asserts zero
      Colibris teal and pins the dark tokens) and `tests/radius-scale.test.ts` still pass.
    - `npm run lint:check` exits 0.
  </acceptance_criteria>

  <done>
    `app/globals.css` carries an on-grid `0.5rem` vertical padding on the shared legacy button rule
    and one identical `var(--ring)` two-layer focus shadow across all six selectors, all inside
    `@layer components`; exactly three non-focus teal literals remain; build, test and lint:check
    all exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove LoadMoreButton's static aria-label</name>

  <files>src/components/proposals/LoadMoreButton.tsx</files>

  <read_first>
    - `src/components/proposals/LoadMoreButton.tsx` in full (it is short) — specifically lines 55–73:
      the `className="btn-out"` at 59, the `aria-label={t('proposal.list.load.more', lang)}` at 62,
      and the idle/loading visible-text branch at 70–72.
    - `.planning/codebase/UI-CONVENTIONS.md` § UIC-10 (lines 391–403) — the icon-only-button rule,
      to confirm this control is NOT icon-only (it renders visible text beside its icon) and so
      removing the `aria-label` does not violate UIC-10.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § `LoadMoreButton.tsx`
      label mismatch — D-38-16.
  </read_first>

  <action>
    Delete the entire `aria-label={t('proposal.list.load.more', lang)}` prop line (currently line 62)
    from the `<button>` element. Change nothing else: keep `type="button"`, `className="btn-out"`,
    `onClick`, `disabled={loading}`, the inline `style`, the `LoaderIcon`/`PlusIcon` branch with its
    `aria-hidden="true"`, and the visible-text branch at lines 70–72 exactly as they are.

    Why deletion rather than making the label dynamic: an `aria-label` overrides visible text for
    assistive technology, so the static idle string kept announcing "Charger plus" while the visible
    text had switched to the loading string — a WCAG 2.5.3 *Label in Name* mismatch. Once the prop is
    gone the button's own visible text is its accessible name in both states, which is correct by
    construction and needs no new i18n key. UIC-10 does not apply: this button is not icon-only.

    Confirm after the edit that `lang` and `t` are both still used elsewhere in the file (they are —
    lines 70–72). If either becomes unused, `npm run lint:check` fails on `--max-warnings=0` even
    though `tsc` and `vitest` pass; that is the CI gate recorded in project memory
    `leasetic_ci_lint_gate`, and it is the specific risk of this deletion.

    Note for the SUMMARY: this file is also a `.btn-out` call site, so Task 1's padding change
    applies here too, and `/proposals` is a CLOSE-08 named surface — plan 38-04 walks it.
  </action>

  <verify>
    <automated>npm run lint:check > /tmp/p38-02-lint.log 2>&1; echo "lint exit $?"; tail -6 /tmp/p38-02-lint.log</automated>
    <automated>npm run typecheck 2>&1 | tail -3; echo "typecheck exit ${PIPESTATUS[0]}"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "aria-label" src/components/proposals/LoadMoreButton.tsx` prints `0`.
    - `grep -c "proposal.list.load.more.loading" src/components/proposals/LoadMoreButton.tsx` prints
      `1` and `grep -c "proposal.list.load.more'" src/components/proposals/LoadMoreButton.tsx` prints
      at least `1` — the visible-text branch is intact.
    - `grep -c 'aria-hidden="true"' src/components/proposals/LoadMoreButton.tsx` prints `2` — both
      icon branches keep their hidden attribute.
    - `git diff --stat src/components/proposals/LoadMoreButton.tsx` shows exactly 1 deleted line and
      0 added lines.
    - `npm run lint:check` exits 0 (no newly-unused `lang` or `t` binding).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>

  <done>
    The "Charger plus" button carries no `aria-label`; its visible text is its accessible name in
    both idle and loading states; lint:check and typecheck exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mint UIC-11 and enumerate the walk surfaces</name>

  <files>
    .planning/codebase/UI-CONVENTIONS.md,
    .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md
  </files>

  <read_first>
    - `.planning/codebase/UI-CONVENTIONS.md` lines 375–410 — UIC-09 and UIC-10's section shape
      (`## UIC-NN — …`, `**Status:**`, `**Recorded in:**`, `**Rule.**`, closing `---`) and where
      UIC-10 ends before the `## Plan-authoring note` heading at line 407.
    - `.planning/codebase/UI-CONVENTIONS.md` lines 430–445 — confirm plan 38-01's two re-import rows
      landed and that you are appending UIC-11 without disturbing them.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § "Focus Treatment —
      UIC-11" — the exact rule text to mint, and A-38-04's constraint that it carries no
      contrast-ratio clause.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § Spacing Scale — the
      `.btn-out` call-site table and the A-38-01 instruction to enumerate `.btn-green`/`.btn-navy`
      surfaces the same way before the walk.
    - `app/globals.css` as edited by Task 1 — so UIC-11's quoted CSS matches what actually shipped.
  </read_first>

  <action>
    **A — mint UIC-11.** Insert a new `## UIC-11 — Focus ring: `var(--ring)` via the two-layer
    shadow; never hardcode a focus colour.` section into `.planning/codebase/UI-CONVENTIONS.md`,
    positioned after UIC-10's closing `---` and before the `## Plan-authoring note: grep-based
    acceptance criteria measure prose too` heading. Follow UIC-10's own section shape exactly:
    a `**Status:**` line, a `**Recorded in:** `38-UI-SPEC.md` § Focus Treatment` line, then a
    `**Rule.**` paragraph. Status should read as an established, ratified rule dated 2026-09-06 with
    phase 38 as its origin, in the same voice the file's other Status lines use.

    The rule states: any `:focus-visible` or `:focus-within` treatment that renders a coloured ring
    uses `outline: none` plus a two-layer `box-shadow` whose inner layer is `0 0 0 2px var(--ring)`
    and whose outer layer is `0 0 0 5px color-mix(in oklab, var(--ring) 50%, transparent)`; that
    `--ring` is already declared in both themes in `app/globals.css`; and that a component-local
    hardcoded `rgba(...)` or other literal focus colour is a violation, not a case-by-case styling
    choice. Quote the CSS in a fenced block inside the document (fenced blocks are fine in
    UI-CONVENTIONS.md — the prohibition applies only to plan `<action>` bodies). Add one sentence
    noting the raw-`box-shadow` form exists because two of the six selectors declare `border: none`,
    so `button.tsx`'s `focus-visible:border-ring` utility form is not universally applicable.

    Per A-38-04 the rule text carries **no contrast-ratio clause** — the measured evidence stays in
    `38-UI-SPEC.md` § Color. Per A-38-05 do not mention the design system's `--border-focus`
    (`#0b935b`) or its divergence from `--ring` anywhere in the rule.

    **B — enumerate the walk surfaces.** Create
    `.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md`, the input plan 38-04
    consumes. Run, and paste the raw output of, these commands (do not hand-transcribe):
    `grep -rn "btn-green" src app`, `grep -rn "btn-navy" src app`, `grep -rn "btn-out" src app`, and
    the three corresponding `grep -rl … | wc -l` counts plus
    `grep -rl "btn-green\|btn-navy\|btn-out" src app | wc -l` for the distinct-file total. Then
    organise into two tables: (1) the five CLOSE-08 named surfaces from
    `28-CONTEXT.md` §"Carry-forward #3" — wizard step 1, `/proposals`, coefficients history,
    `/parametres`, and the `PartnersList` / `LcReferencesList` padding sites — each with its route
    and the `.btn-*` classes it renders; (2) every other file touching one of the three classes,
    marked incidental. Add a short "row-alignment spots" list naming the files where one of the
    three classes sits in the same row as a real shadcn `Button` (38-UI-SPEC.md names
    `CreatePartnerForm.tsx` and `SaveConfirmModal.tsx` — verify by reading them and add any others
    you find), because that is what the walk actually inspects after the 3px height change.

    Record the measured counts against 38-UI-SPEC.md's A-38-01 figures (18 / 2 / 18 / 31) and state
    the drift explicitly. Planning measured 21 / 3 / 19 / 32 on 2026-09-06; if execution measures
    something different again, the measured number wins and the delta is recorded, not smoothed.
  </action>

  <verify>
    <automated>grep -c "^## UIC-11" .planning/codebase/UI-CONVENTIONS.md</automated>
    <automated>test -s .planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md; echo "surfaces file exit $?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## UIC-11" .planning/codebase/UI-CONVENTIONS.md` prints `1`.
    - The UIC-11 section body contains the literal strings `0 0 0 2px var(--ring)` and
      `color-mix(in oklab, var(--ring) 50%, transparent)` — verify with
      `grep -c "0 0 0 2px var(--ring)" .planning/codebase/UI-CONVENTIONS.md` printing at least `1`.
    - UIC-11 contains no contrast ratio: `grep -cE "[0-9]+\.[0-9]+:1|1\.4\.11|contrast ratio" .planning/codebase/UI-CONVENTIONS.md`
      returns no match inside the UIC-11 section (check by `sed -n` on the UIC-11 line range).
    - `grep -c -- "0b935b\|border-focus" .planning/codebase/UI-CONVENTIONS.md` prints `0` (A-38-05:
      the DS divergence is deliberately not recorded).
    - `awk '/^## UIC-10/{a=NR} /^## UIC-11/{b=NR} /^## Plan-authoring note/{c=NR} END{exit !(a<b && b<c)}' .planning/codebase/UI-CONVENTIONS.md`
      succeeds — UIC-11 sits between UIC-10 and the plan-authoring note.
    - `.planning/codebase/UI-CONVENTIONS.md` still contains both re-import rows added by plan 38-01:
      `grep -c "tests/dialog-close-label.test.ts" .planning/codebase/UI-CONVENTIONS.md` prints at
      least `2`.
    - `38-WALK-SURFACES.md` exists, is non-empty, contains the five CLOSE-08 named surface routes,
      and contains a distinct-file count line whose number matches a live re-run of
      `grep -rl "btn-green\|btn-navy\|btn-out" src app | wc -l`.
    - `38-WALK-SURFACES.md` names at least two row-alignment inspection spots.
  </acceptance_criteria>

  <done>
    UIC-11 is minted in the canonical conventions file with no contrast clause and no DS mention;
    `38-WALK-SURFACES.md` holds a measured, command-derived enumeration of every surface plan 38-04
    must walk, with any drift from A-38-01's figures stated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stylesheet → rendered focus indicator | `app/globals.css` decides whether a keyboard user can see where focus is |
| Component → assistive technology | `LoadMoreButton`'s accessible name is computed from its DOM attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Denial of Service (accessibility availability) | the six focus selectors in `app/globals.css` | mitigate | A malformed or over-broad CSS edit that removes the focus indicator without replacing it would leave keyboard-only users unable to locate focus — a real availability loss for that population. Mitigated by pinning exact occurrence counts (`0 0 0 2px var(--ring)` = 4, `color-mix(… --ring 50% …)` = 4) so a partially-applied edit fails the gate, and by plan 38-04's keyboard tab-through in both themes. |
| T-38-06 | Tampering | `app/globals.css` non-focus teal literals (lines 416/463/481) | mitigate | An over-eager find-and-replace on `rgba(45, 122, 140` would silently rewrite a hover background and an elevation shadow that GAP-04 does not scope. Mitigated by the explicit count criterion (`3` survivors) and the named-literal criterion for the elevation shadow. |
| T-38-07 | Information Disclosure | — | accept | No user-controlled string is rendered into any accessible name by this plan. `LoadMoreButton`'s accessible name becomes its own visible text, which is two compile-time dictionary constants. No PII, no interpolation, no new data flow. |
| T-38-08 | Spoofing / Repudiation / Elevation | — | accept | CSS declarations and one deleted JSX attribute. No network surface, no auth path, no persisted state, no privilege boundary is crossed. Stated as a genuine "no new attack surface" finding rather than padded with invented threats — this phase is styling, a11y and documentation only. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; `package.json` and `package-lock.json` are untouched. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint:check` exits 0
- `npm run test` exits 0 (`dark-palette`, `radius-scale`, `container-radius`,
  `dialog-close-label`, `vendored-ui-integrity` all green)
- `npm run build > /tmp/p38-02-build.log 2>&1; echo "exit $?"` exits 0
- `grep -c "rgba(45, 122, 140" app/globals.css` prints `3`
- `git diff --stat` touches only the four files in `files_modified`
</verification>

<success_criteria>
- Shared legacy button padding is on-grid at `0.5rem` vertical, landing on `button.tsx`'s 36px step
- One focus treatment, sourced from `--ring`, across all six selectors; no new token minted
- `.btn-out`'s ~2px border-driven height delta is recorded and deliberately left in place
- "Charger plus" has no `aria-label`; visible text is the accessible name in both states
- UIC-11 exists in `UI-CONVENTIONS.md` with no contrast clause and no DS-divergence mention
- `38-WALK-SURFACES.md` gives plan 38-04 a measured surface list rather than a re-derived one
</success_criteria>

<output>
Create `.planning/phases/38-shell-dialogs-visual-conventions/38-02-SUMMARY.md` when done.
Record: the measured `.btn-green` / `.btn-navy` / `.btn-out` file counts and their drift from
A-38-01's 18/2/18/31; the confirmation that lines 416/463/481's teal literals were left alone; and
an explicit statement that the rendered focus ring has NOT been visually confirmed by this plan —
that is plan 38-04's leg.
</output>
