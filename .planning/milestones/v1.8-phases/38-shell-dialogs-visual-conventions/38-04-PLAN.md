---
phase: 38-shell-dialogs-visual-conventions
plan: 04
type: execute
wave: 4
depends_on: ["38-03"]
files_modified:
  - .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md
  - .planning/phases/38-shell-dialogs-visual-conventions/evidence/
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CLOSE-08, GAP-02, GAP-04]

must_haves:
  truths:
    - "Each of Phase 28's five named surfaces has a recorded light result and a recorded dark result"
    - "A dialog close control and the mobile sidebar sheet were observed announcing 'Fermer' in FR and 'Close' in EN"
    - "The new two-layer var(--ring) focus ring was observed on a keyboard-focused control in both themes"
    - "A row where a legacy .btn-* sits beside a real shadcn Button was observed at matching height after the 0.5rem change"
    - "Every defect found is either fixed in-phase or filed as a requirement — none is left as an unrouted observation"
  artifacts:
    - path: ".planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md"
      provides: "CLOSE-08 walk table — surface x theme x result — appended to the CLOSE-02 sections"
      contains: "CLOSE-08"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Any newly-filed requirement for a defect that fails D-38-07's in-phase test"
  key_links:
    - from: "38-UAT.md CLOSE-08 table"
      to: "38-WALK-SURFACES.md"
      via: "the measured surface enumeration produced by plan 38-02"
      pattern: "38-WALK-SURFACES"
    - from: "38-UAT.md defect rows"
      to: ".planning/REQUIREMENTS.md"
      via: "a filed requirement ID for any out-of-scope defect"
      pattern: "REQUIREMENTS"
---

<objective>
Close CLOSE-08: walk Phase 28's browser-verification backlog in light **and** dark with each result
recorded, over the state this phase actually ships. Carry the browser-verification legs of GAP-02
(the FR/EN close-label announcement) and GAP-04 (the new focus ring and the 36px row alignment) in
the same pass, since they need the same running session.

Purpose: Phase 28's own `28-01-SUMMARY.md` records the process lesson that green gates saw none of
six visual defects — which is why this backlog is a browser walk and not a test suite. Phase 28's
walk found six defects; this one probably finds some too, and D-38-07 gives a mechanical rule for
routing them so the phase stays bounded.

Scope note on `requirements`: GAP-02's and GAP-04's code lands in plans 38-01 and 38-02. This plan
carries only their **verification** legs — success criterion 3's "verified in FR and EN" and
success criterion 4's rendered focus/padding confirmation. Neither ID's implementation is repeated
here.

Output: the CLOSE-08 half of `38-UAT.md`, any failure screenshots, and either in-phase fixes or
filed requirements for every defect found.

Session continuity: plan 38-03 left the local production build running on port 3001 with an
authenticated session. Task 1 re-confirms it rather than rebuilding from scratch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md
@.planning/phases/28-reui-design-system-migration/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk the five named surfaces in light theme</name>

  <files>.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</files>

  <read_first>
    - `.planning/phases/28-reui-design-system-migration/28-CONTEXT.md` §"Carry-forward #3" — the
      authoritative CLOSE-08 surface list. Read it directly; do not rely on this plan's paraphrase.
    - `.planning/phases/28-reui-design-system-migration/28-01-SUMMARY.md` §"Process lesson" — why
      this is a browser walk. It names the six defects green gates missed; those are the defect
      classes to look for.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-WALK-SURFACES.md` — plan 38-02's
      measured enumeration: which routes render which of the three legacy button classes, plus the
      row-alignment inspection spots.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § Spacing Scale, subsection
      "Expected visual delta for a reviewer" — what specifically to look at after the 3px change, and
      A-38-02's note that `.btn-out`'s residual ~2px border-driven delta over `.btn-green`/`.btn-navy`
      is **expected and not a defect**.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` as written by plan 38-03 — you
      are appending to it, not replacing it.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-06 (evidence policy)
      and D-38-07 (bounded triage).
  </read_first>

  <action>
    First confirm the session from plan 38-03 is still live:
    `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/healthz` returns 200 and an
    authenticated page still renders rows. If the server died, re-run plan 38-03 Task 1's recipe
    (`mv .env.production.local .env.production.local.OFF`, `rm -rf .next`, build and start on 3001
    with `APP_URL`/`NEXT_PUBLIC_APP_URL` set to `http://localhost:3001`) and ask the operator to sign
    in again — Claude does not perform the login (D-38-02).

    Append a `## CLOSE-08 — Phase 28 browser-verification backlog` section to `38-UAT.md` with a
    table whose columns are: Surface | Route | Theme | `.btn-*` classes present | Result | Notes.
    Then walk these five surfaces in **light** theme and fill one row each:
      1. Wizard step 1 (the proposal wizard's first step — `ProposalForm.tsx`'s `.btn-out`)
      2. `/proposals` (the list, its `ExportButton` and the `LoadMoreButton` "Charger plus")
      3. Coefficients history (the admin coefficients page's `HistoryTable`)
      4. `/parametres` (`ParametresForm`)
      5. The `PartnersList` and `LcReferencesList` padding sites (the six sites changed in `7d5b2e2`)

    Per A-38-01 the padding change also lands on `.btn-green` and `.btn-navy`, so additionally walk
    the primary-CTA surfaces `38-WALK-SURFACES.md` enumerates — at minimum the sign-in form, the
    partner-invitation form and the coefficients save-confirm modal — and add a row for each.

    On each surface check, and record in the Notes column:
      - **Row alignment.** Where a legacy `.btn-green`/`.btn-navy`/`.btn-out` sits in the same row as
        a real shadcn `Button`, measure both heights in DevTools. Expect 36px for the legacy classes
        (and ~38px for `.btn-out` because of its 1px border — expected, not a defect, per A-38-02).
        Record the measured pixel values, not "looks aligned".
      - **Focus ring.** Tab to at least one control per surface and confirm the new two-layer ring
        renders: a solid 2px `#01cc72` inner ring plus a softer 5px halo, replacing the old flat 3px
        teal. Note that this is a **geometry change**, not just a recolour — the old
        `0 0 0 3px rgba(45,122,140,0.18)` is gone. Also tab into the `/proposals` search bar to
        confirm `.search-bar:focus-within` now renders the same treatment and that its
        `border-color: var(--teal)` is still present.
      - **Vertical rhythm.** Whether the ~3px height reduction has left any card, table row or button
        group with visibly wrong spacing.

    **Defect triage (D-38-07) — mechanical, not a judgement call.** For every defect: if it is a
    token, a spacing literal, a CSS rule or an i18n string, fix it in-phase (and note the fix in the
    row). If it needs a component change, a new primitive, or a data/logic change, file it as a
    requirement in `.planning/REQUIREMENTS.md` for a later phase and record the ID in the row. No
    defect is left as an unrouted observation.

    **Evidence (D-38-06):** commit a screenshot for each failing surface into
    `.planning/phases/38-shell-dialogs-visual-conventions/evidence/`. Do not commit screenshots of
    passing surfaces — they are table rows only. Review any committed screenshot for identifiable
    client data before committing it.

    Read-only throughout: observe wizard step 1, never submit it; create, edit and delete nothing.
  </action>

  <verify>
    <automated>grep -c "^| " .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3001/healthz</automated>
  </verify>

  <acceptance_criteria>
    - `38-UAT.md` contains a `## CLOSE-08` section with at least 8 table rows, each carrying a Theme
      value of `light` and a Result of `pass` or `fail` — no blank, no "n/a", no "assumed".
    - All five surfaces named in `28-CONTEXT.md` §"Carry-forward #3" appear as rows; verify by
      grepping `38-UAT.md` for each route string (`/proposals`, `/parametres`, `coefficients`,
      `partners`, `lc-references`).
    - At least two rows record measured pixel heights for a legacy-button/shadcn-`Button` pair (e.g.
      `36px / 36px`), not a prose judgement.
    - At least one row records the observed focus-ring geometry in words that distinguish the
      two-layer ring from the retired flat 3px teal.
    - Every row whose Result is `fail` has either an in-phase fix noted or a requirement ID recorded,
      and a matching screenshot under `evidence/`.
    - `git status --porcelain` shows no unexplained modification under `src/` or `app/` — any source
      change present must correspond to a D-38-07 in-phase fix that is named in a `38-UAT.md` row.
  </acceptance_criteria>

  <done>
    Every CLOSE-08 named surface plus the `.btn-green`/`.btn-navy` CTA surfaces has a recorded
    light-theme result with measured evidence for row alignment and focus geometry, and every defect
    is routed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-walk the same surfaces in dark theme and verify the FR/EN close label</name>

  <files>.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</files>

  <read_first>
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` — the light-theme rows from
      Task 1, so the dark rows are directly comparable.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § Color — the measured
      non-text contrast table. `--ring` `#01cc72` solid measures 7.12:1 against the worst dark
      surface versus the retired teal's 1.19:1; dark is where this change matters most.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-12 — the FR/EN
      verification scope: one representative `dialog.tsx` consumer plus the mobile sidebar sheet,
      four observations total.
    - `src/components/ui/sidebar.tsx` around line 199 — where `SheetContent` is mounted, so you know
      the mobile drawer is the sheet call site to open (narrow the viewport to trigger it).
    - `src/lib/i18n/dictionaries.ts` — the FR and EN values of `common.close.aria` as shipped by plan
      38-01, so you check against the actual strings.
  </read_first>

  <action>
    **A — dark-theme re-walk.** Switch the app to dark theme and repeat Task 1's walk over the same
    surface list, appending a `dark` row per surface to the same `38-UAT.md` table. Apply the same
    three checks (row alignment, focus ring, vertical rhythm). Pay particular attention to the focus
    ring here: this is the theme where the retired teal was materially weaker, so a control whose
    focus is now clearly visible against `#161616` / `#1e1e1e` / `#262626` is the observable payoff of
    A-38-03 and is worth recording explicitly. Route every defect through D-38-07 exactly as in
    Task 1.

    **B — GAP-02's FR/EN leg (D-38-12, four observations).** With `<html lang>` = `fr`:
      1. Open one `dialog.tsx` consumer — any of `CreateClientDialog`, `MarkWonDialog`,
         `MarkLostDialog`, `EditRelationDialog`, `EditCompanyDialog`, `ContactFormDialog`,
         `NextActionDialog`, `MergeDialog`. In DevTools, inspect the icon-only close button and read
         its **computed accessible name** from the Accessibility pane. It must be `Fermer`.
      2. Narrow the viewport so the sidebar renders as the mobile sheet drawer, open it, and read the
         accessible name of its close button. It must be `Fermer`.
    Then switch the app to EN (which triggers `setLang`'s `revalidatePath('/', 'layout')`, keeping
    `<html lang>` current — this is D-38-09's stated precondition) and repeat both observations,
    expecting `Close`. Confirm `document.documentElement.lang` actually reads `en` at that point;
    if it does not, that invalidates D-38-09's mechanism and is a blocking finding, not a nitpick.

    Record all four observations as their own rows in `38-UAT.md` under a
    `## GAP-02 — dialog close accessible name (FR/EN)` heading, each quoting the accessible name read
    from the Accessibility pane rather than the visible DOM text. Also confirm the close button is
    visually unchanged (icon, `absolute top-4 right-4` position, `icon-sm` size, `ghost` variant) —
    38-UI-SPEC.md's contract for GAP-02 is zero visual change.

    While a dialog is open, re-check the browser console for React hydration warnings from
    `resolveDomLang()`'s SSR `'fr'` default. Record what you see.
  </action>

  <verify>
    <automated>grep -c "dark" .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</automated>
    <automated>grep -c "Fermer" .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - The `## CLOSE-08` table has a `dark` row for every surface that has a `light` row — verify the
      count of `light` rows equals the count of `dark` rows.
    - `38-UAT.md` contains a `## GAP-02 — dialog close accessible name (FR/EN)` section with exactly
      four observation rows: {dialog, sheet} × {fr, en}.
    - Each of those four rows quotes the accessible name read from the DevTools Accessibility pane;
      the two FR rows read `Fermer` and the two EN rows read `Close`.
    - One row records the observed value of `document.documentElement.lang` in each language, proving
      the `<html lang>` attribute actually flips on locale toggle.
    - At least one dark row records the focus ring as visible against the dark surface, with the
      surface token named (`#161616`, `#1e1e1e`, or `#262626`).
    - Any hydration warning observed is recorded; if none, the absence is recorded explicitly rather
      than left unmentioned.
    - Every `fail` row has an in-phase fix or a filed requirement ID, and a screenshot under
      `evidence/`.
  </acceptance_criteria>

  <done>
    Every surface has both a light and a dark recorded result; the dialog and sheet close controls
    have been observed announcing `Fermer` in FR and `Close` in EN via the Accessibility pane; every
    defect is routed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator adjudicates the walk, confirms triage, and closes the session</name>

  <files>
    .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md,
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` in full — both the CLOSE-02
      sections from plan 38-03 and the CLOSE-08 / GAP-02 sections from Tasks 1 and 2.
    - Every failure screenshot under
      `.planning/phases/38-shell-dialogs-visual-conventions/evidence/`.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-07 — the triage rule,
      to check each defect was routed by the mechanical test rather than by convenience.
    - `.planning/REQUIREMENTS.md` — the CLOSE / GAP sections, to confirm any newly-filed requirement
      follows the existing ID and wording conventions and is added to the phase-mapping table.
  </read_first>

  <what-built>
    Tasks 1 and 2 walked every CLOSE-08 named surface plus the `.btn-green`/`.btn-navy` CTA surfaces
    in light and dark, measured row alignment and focus geometry in DevTools, and read the dialog and
    sheet close buttons' computed accessible names in FR and EN. Every defect found was routed —
    fixed in-phase if it was a token/spacing/CSS/i18n change, filed as a requirement otherwise.
  </what-built>

  <action>
    This is a blocking human-verify checkpoint. Claude presents `38-UAT.md` and every failure
    screenshot, waits for the resume signal, and only then performs the closing edits named in
    `<how-to-verify>` step 6: tick the closed requirements in `.planning/REQUIREMENTS.md` and update
    their rows in the requirement-to-phase mapping table, restore the env file with
    `mv .env.production.local.OFF .env.production.local`, and stop the port-3001 server. If the
    operator disputes any triage disposition, re-route that defect per D-38-07 and return here rather
    than closing. Do not tick any requirement before the signal.
  </action>

  <how-to-verify>
    1. Read the CLOSE-08 table. Confirm every surface you expect is there, in both themes, and that
       no row's Result is vague.
    2. Look at each failure screenshot and decide whether you agree with the verdict and with its
       triage disposition. Push back on any defect routed to "later phase" that is actually just a
       CSS value — D-38-07 exists to make that call mechanical, and mis-routing it is how a phase
       quietly sheds scope.
    3. Confirm the four GAP-02 accessible-name observations read `Fermer` / `Close` as expected.
    4. Confirm the row-alignment measurements: legacy buttons at 36px, `.btn-out` at ~38px because of
       its border (expected per A-38-02, not a defect).
    5. Approve, or name what needs re-walking.
    6. On approval, Claude updates `.planning/REQUIREMENTS.md`: tick CLOSE-08 (and CLOSE-02, GAP-02,
       GAP-04 if their plans are complete) in the checklist, and update their rows in the
       requirement-to-phase mapping table. Then restore the env file:
       `mv .env.production.local.OFF .env.production.local` — and verify with
       `ls .env.production.local` succeeding and `ls .env.production.local.OFF` failing. Stop the
       port-3001 server.
  </how-to-verify>

  <acceptance_criteria>
    - `38-UAT.md`'s frontmatter `status` is `resolved` (or an explicit blocked state naming what
      blocks it), with a current `updated` timestamp.
    - No row in `38-UAT.md` has an empty Result cell.
    - Every `fail` row references either a fix commit or a requirement ID that exists in
      `.planning/REQUIREMENTS.md` — verify each referenced ID with a grep against that file.
    - `grep -c "^- \[x\] \*\*CLOSE-08\*\*" .planning/REQUIREMENTS.md` prints `1` on approval.
    - `.env.production.local` exists and `.env.production.local.OFF` does not
      (`ls .env.production.local && ! ls .env.production.local.OFF`).
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/healthz` fails to connect — the
      walk server is stopped.
    - `npm run test`, `npm run lint:check` and `npm run typecheck` all exit 0 after any D-38-07
      in-phase fixes.
  </acceptance_criteria>

  <resume-signal>
    Type "approved", or "re-walk: {surface / theme / what was wrong}".
  </resume-signal>

  <verify>
    <automated>grep -c "CLOSE-08" .planning/REQUIREMENTS.md</automated>
    <automated>ls .env.production.local >/dev/null 2>&1; echo "env restored exit $?"</automated>
    <automated>npm run test > /tmp/p38-04-test.log 2>&1; echo "test exit $?"</automated>
  </verify>

  <done>
    The operator has approved the walk record, `.planning/REQUIREMENTS.md` reflects the closed and
    newly-filed requirements, `.env.production.local` is restored and the walk server is stopped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build → live database | The walk server on port 3001 is connected to a real Neon branch |
| Agent observation → requirement closure | Claude's browser observations become the basis for ticking CLOSE-08 as done |
| Committed screenshots → public repo | The repo is public (Phase 5 follow-up #2); evidence images ship with it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Information Disclosure | failure screenshots under `evidence/` | mitigate | This is the one genuinely material threat in this plan. The repo is public, and the walk is authenticated against real partner data — a screenshot of `/proposals` or `PartnersList` can contain client names, amounts and partner emails. Mitigated by D-38-06's commit-failures-only policy (which minimises the number of images), by a mandatory pre-commit review of each image, and by cropping to the defective element rather than capturing full pages of populated tables. Record that the review happened. |
| T-38-14 | Tampering | live data during the walk | mitigate | Same mitigation as plan 38-03 T-38-09: read-only constraint, wizard step 1 observed but never submitted, no create/edit/delete. Task 1's acceptance criteria assert no unexplained source or data mutation. |
| T-38-15 | Repudiation | ticking CLOSE-08 as complete | mitigate | A walk that records "pass" without measurements is indistinguishable from a walk that did not happen — the exact failure `28-01-SUMMARY.md`'s process lesson describes. Mitigated by requiring measured pixel values and DevTools-computed accessible names in the table rather than prose, and by a blocking human checkpoint over the evidence. |
| T-38-16 | Spoofing / Denial of Service / Elevation | — | accept | No code ships in this plan except possible D-38-07 in-phase CSS/i18n fixes, which are the same class as plan 38-02's and add no network surface, no auth path and no data flow. Stated as an honest "no new attack surface" finding. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No installs. If a D-38-07 in-phase fix ever needed a new package it would fail the mechanical triage test (that is a component change, filed as a requirement), so this plan structurally cannot introduce one. |
</threat_model>

<verification>
- `38-UAT.md` has matched light and dark rows for every surface, and four GAP-02 accessible-name rows
- Every defect is either fixed in-phase (with gates green) or filed with a requirement ID
- `.planning/REQUIREMENTS.md` reflects the closed requirements and any newly-filed ones
- `.env.production.local` is restored and the port-3001 server is stopped
- `npm run test`, `npm run lint:check`, `npm run typecheck` all exit 0
</verification>

<success_criteria>
- Phase 28's browser-verification backlog is walked in light and dark with each result recorded
- The dialog close control announces an accessible name in the viewer's language, observed in FR and EN
- The new focus ring and the on-grid padding are confirmed as rendered, not merely as declared
- No defect the walk surfaced is left unrouted
</success_criteria>

<output>
Create `.planning/phases/38-shell-dialogs-visual-conventions/38-04-SUMMARY.md` when done.
Record: the full defect list with each item's D-38-07 disposition and the reasoning for it; the
measured row-alignment values; any newly-filed requirement IDs; the evidence-image privacy review
result; and confirmation that the env file was restored and the walk server stopped.
</output>
