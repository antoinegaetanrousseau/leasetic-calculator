---
phase: 38-shell-dialogs-visual-conventions
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md
  - .planning/phases/38-shell-dialogs-visual-conventions/evidence/
  - .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md
autonomous: false
requirements: [CLOSE-02]

must_haves:
  truths:
    - "A throttled performance trace of a dark-theme first paint exists as committed image evidence, and a reader can see for themselves whether a light frame precedes the first dark frame"
    - "The PDF/print surface was observed rendering white-on-#1a2832 while the app was in dark theme"
    - "31.1-VERIFICATION.md reads status: passed only because both checks were observed passing, not as a bookkeeping edit"
  artifacts:
    - path: ".planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md"
      provides: "Recorded CLOSE-02 results — surface x theme x result, following 37-HUMAN-UAT.md"
      contains: "result:"
    - path: ".planning/phases/38-shell-dialogs-visual-conventions/evidence/"
      provides: "Committed filmstrip frames and any failure screenshots"
    - path: ".planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md"
      provides: "status: passed, gated on the walk"
      contains: "status: passed"
  key_links:
    - from: "38-UAT.md"
      to: "evidence/ filmstrip images"
      via: "relative markdown image or path reference"
      pattern: "evidence/"
    - from: "31.1-VERIFICATION.md frontmatter"
      to: "38-UAT.md CLOSE-02 rows"
      via: "citation of the walk that produced the pass"
      pattern: "38-UAT"
---

<objective>
Close CLOSE-02: perform Phase 31.1's two abandoned human checks against a running build of the code
this phase just shipped, record the results with evidence a later reader can re-examine, and flip
`31.1-VERIFICATION.md` from `status: human_needed` to `status: passed` **only if** both checks
actually pass.

Purpose: `31.1-VERIFICATION.md` scored 6/7 and stopped at `human_needed` because SHELL-C7's
rendering-level claim — "dark mode renders on a palette sampled from Colibris, with no flash of
light chrome" — has never been observed running, by any agent, human or automated. The prior
attempt (31.1-07) started and the browser session dropped mid-check. `tests/dark-palette.test.ts`
asserts the CSS declarations exist; it cannot assert they composite before first paint. That gap is
exactly why D-38-03 requires a *temporal* artifact rather than a settled-page screenshot.

**This plan does not self-certify.** Per D-38-02 the operator supplies the authenticated session and
Claude drives the browser; per D-38-06 the CLOSE-02 filmstrip is committed as image files so the
verdict rests on reviewable evidence rather than on Claude's assertion. Task 3 is a blocking human
checkpoint; the `31.1-VERIFICATION.md` edit happens inside it, after adjudication, never up front.

Output: `38-UAT.md` with the CLOSE-02 rows, committed evidence images, and a gated frontmatter flip.

Ordering note (D-38-04): this plan runs AFTER plans 38-01 and 38-02 land, deliberately. The
`var(--ring)` focus change and the 0.5rem padding change alter every surface the walk covers, so a
pre-fix walk would certify state that never ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md
@.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md
@.planning/phases/37-crm-stack-closure/37-HUMAN-UAT.md
@.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator brings up the local production build and hands over an authenticated session</name>

  <files>none — environment setup only</files>

  <read_first>
    - `.planning/phases/37-crm-stack-closure/37-HUMAN-UAT.md` — the `how_to_run` block of scenario 1
      and its `verified` block. This is the recipe, proven working on 2026-09-06 (an admin signed in
      successfully and rendered a real PDF).
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-01 and D-38-02 — the
      walk-environment decision and the read-only constraint.
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UI-SPEC.md` § "CLOSE-02 — Verification
      Method" — its restatement of the environment.
    - `scripts/check-local-db-branch.sh` — so you know what its output actually proves before
      quoting it as evidence.
  </read_first>

  <what-built>
    Plans 38-01 and 38-02 have landed: the dialog/sheet close labels are i18n-driven, the shared
    legacy button padding is `0.5rem`, and all six focus selectors read `var(--ring)` through the
    two-layer shadow. Nothing has been observed in a browser yet.
  </what-built>

  <decision-to-record>
    **There is a documented conflict between two source artifacts about which database the walk runs
    against. Resolve it here, explicitly, and record the answer in `38-UAT.md`'s frontmatter.**

    - `38-CONTEXT.md` D-38-01 says: local production build pointed at the **production** DB, because
      D-36-03 established that app-level login against the Neon `development` branch fails
      (`[Better Auth]: Invalid password` — it is a copy-on-write fork frozen at 2026-05-27 whose
      credential hashes predate every rotation since).
    - `38-UI-SPEC.md` § "CLOSE-02 — Verification Method" says: the **development** branch with
      `.env.production.local` moved aside, citing `37-HUMAN-UAT.md`'s recipe.
    - The tiebreaker is empirical and postdates D-38-01: `37-HUMAN-UAT.md` records an admin
      (`antoine.rousseau@leasetic.com`) signing in successfully against the development branch on
      **2026-09-06** and rendering a real PDF. D-36-03's failure is therefore no longer current.

    **Default: run the development-branch recipe.** It is proven working as of 2026-09-06 and is
    read-only by construction. **Fallback:** if login against the development branch fails, restore
    `.env.production.local`, rebuild against production, and keep the walk strictly read-only —
    observe wizard step 1 but never submit it, and create/edit/delete nothing. Whichever branch was
    used goes in `38-UAT.md`'s frontmatter as a `database:` field, with a one-line reason.
  </decision-to-record>

  <action>
    This is a blocking human-action checkpoint: the operator performs the steps in
    `<how-to-verify>` below, because Claude cannot sign in (D-38-02 reserves login to the operator)
    and cannot judge which database the walk should target. Claude's part is to record the
    `<decision-to-record>` outcome into `38-UAT.md`'s frontmatter as a `database:` field with its
    one-line reason, and to run the read-only confirmation commands in `<acceptance_criteria>`
    (`curl` against `/healthz`, checking which env file is in place) once the operator signals ready.
    Claude must not run the login flow, must not create the walk data, and must not proceed past
    this task until the resume signal is given.
  </action>

  <how-to-verify>
    1. Move the production env file aside — it outranks `.env.local` under `NODE_ENV=production`:
       `mv .env.production.local .env.production.local.OFF`
    2. `npm run check:local-db-branch` — note its output, but do NOT treat it as proof. Per project
       memory `leasetic_db_guard_false_negative` (OPS-05, tracked in Phase 39) this script reads only
       `.env.local` and is blind to `.env.production.local`; it prints "development" even when a
       production build is connected to production. Confirm the branch by reading which env file is
       actually in place, not by trusting this script.
    3. `rm -rf .next`
    4. `APP_URL=http://localhost:3001 NEXT_PUBLIC_APP_URL=http://localhost:3001 npm run build`
       — run as `cmd > /tmp/p38-03-build.log 2>&1; echo "exit $?"`. A stale `APP_URL` produces a
       login button that silently does nothing plus a console CORS error (project memory
       `leasetic_app_url_must_match_custom_domain`), which would look like a shell defect and
       poison the walk.
    5. `PORT=3001 APP_URL=http://localhost:3001 NEXT_PUBLIC_APP_URL=http://localhost:3001 npm run start`
    6. Open http://localhost:3001, sign in as an admin, and hand the authenticated browser session to
       Claude (Playwright / Browser MCP). Claude does not perform the login (D-38-02).
    7. Confirm out loud that the walk is **read-only**: wizard step 1 is observed but never submitted;
       no proposal is created, edited or deleted.
    8. Note the restore step for the end of plan 38-04:
       `mv .env.production.local.OFF .env.production.local`. Leave the server running — plan 38-04
       reuses this same session.
  </how-to-verify>

  <acceptance_criteria>
    - `http://localhost:3001/healthz` returns HTTP 200 (`curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/healthz`).
    - The build log at `/tmp/p38-03-build.log` ends with a successful Next.js build and the recorded
      `exit 0` line — not a piped `tail`'s exit code (Phase 28 lesson, 38-CONTEXT.md).
    - `ls .env.production.local.OFF` succeeds if the development-branch path was taken, OR the
      operator has explicitly stated the production fallback was used and why.
    - Claude can load an authenticated page (e.g. `/proposals`) in the browser session and see real
      rows, not the login form.
    - The chosen database branch and the reason are written into `38-UAT.md`'s frontmatter before any
      walking begins.
  </acceptance_criteria>

  <resume-signal>
    Type "session ready" (and state which DB branch is in play), or describe what failed.
  </resume-signal>

  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3001/healthz</automated>
    <automated>ls .env.production.local.OFF 2>/dev/null; echo "env-moved-aside exit $?"</automated>
  </verify>

  <done>
    The local production build is serving on port 3001, the operator has signed in and handed over
    the session, and `38-UAT.md`'s frontmatter records which Neon branch is in play and why.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture the dark first-paint filmstrip and the dark-mode PDF check</name>

  <files>
    .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md,
    .planning/phases/38-shell-dialogs-visual-conventions/evidence/
  </files>

  <read_first>
    - `.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md` frontmatter — the two
      `human_verification` entries verbatim: their `test`, `expected` and `why_human` fields. These
      are the exact claims being checked; do not paraphrase them into something easier.
    - `tests/dark-palette.test.ts` — the six pinned dark tokens and the `[data-pdf-surface]`
      assertion, so you know the precise expected values rather than eyeballing "looks dark".
    - `app/globals.css` lines 185–220 — the `html[data-theme="dark"]` token block and the
      `[data-pdf-surface]` override at lines 214–218 (white background / `#1a2832` text).
    - `app/layout.tsx` around line 56 — `<html lang>` and the inline no-flash theme script, so you
      know what mechanism the filmstrip is testing.
    - `.planning/phases/37-crm-stack-closure/37-HUMAN-UAT.md` — the table/record shape `38-UAT.md`
      follows (D-38-05).
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-03 and D-38-06 — the
      evidence method and the commit-failures-only policy.
  </read_first>

  <action>
    Create `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` following
    `37-HUMAN-UAT.md`'s shape: YAML frontmatter (`status`, `phase`, `source`, `started`, `updated`,
    plus the `database:` field decided in Task 1), then one section per check with `expected`,
    `why_human`, `how_to_run`, `result` and `verified` blocks. Then perform the two CLOSE-02 checks
    and fill them in.

    **Check 1 — no flash of light chrome on first paint, dark theme (D-38-03).** Set the app to dark
    theme so the theme cookie is dark. In Chrome DevTools, enable CPU throttling (4x or 6x — record
    which), start a Performance recording, hard-reload an authenticated page, stop the recording, and
    export the screenshot/filmstrip frames. Save the frames as image files under
    `.planning/phases/38-shell-dialogs-visual-conventions/evidence/` with names that encode their
    frame order and timestamp so a later reader can reconstruct the sequence. **Pass signature:** no
    light-chrome frame precedes the first painted dark frame. **Fail signature:** a light frame
    appears before a dark one. Also confirm in the settled frame that the shell reads the six pinned
    tokens — `--background` and `--sidebar` `#161616`, `--card` `#1e1e1e`, `--border` /
    `--sidebar-border` `oklch(1 0 0 / 8%)`, `--muted-foreground` `oklch(0.65 0 0)` — by reading
    computed styles in DevTools, not by judging the colour by eye.

    Interval screenshots during a throttled reload are **explicitly rejected** as the method (D-38-03):
    sampling can miss a sub-frame flash and yields absence-of-evidence, which is the exact weak claim
    `31.1-VERIFICATION.md` refused the first time. Source-level inspection of the inline theme script
    is also rejected — same class of evidence that verifier already declined. If the trace cannot be
    captured, record that fact and stop; do not substitute a weaker method.

    **Check 2 — PDF/print surface in dark mode.** With the app still in dark theme, open a proposal
    detail page and its APERÇU PDF panel. Confirm the PDF surface renders a pure white background
    with `#1a2832` text per `[data-pdf-surface]`, un-perturbed by this phase's `--ring` and padding
    changes. Read the computed `background-color` and `color` off the surface in DevTools and record
    the actual values. Read-only: do not download, regenerate or modify anything.

    **Evidence policy (D-38-06).** Commit the filmstrip frames regardless of outcome — the filmstrip
    IS the CLOSE-02 evidence. Commit a screenshot for any failure. Do NOT commit screenshots of
    passing surfaces; those are table rows only, so the repo does not carry ~20 PNGs that go stale on
    the next UI change.

    While in the browser, also check the console for React hydration warnings on any page that
    mounts a dialog or the mobile sheet — plan 38-01 introduced a `typeof document` guard that
    returns `'fr'` during SSR, and a hydration text mismatch would show up here. Record what you see;
    if a warning appears, it is a code defect and triages under D-38-07 as a component change (file
    it, do not silently patch it here).

    Record every result honestly. If a check fails, write the failure into `38-UAT.md` with its
    screenshot and stop — do NOT flip `31.1-VERIFICATION.md`. D-38-08 governs the fail path and it is
    handled in Task 3.
  </action>

  <verify>
    <automated>ls -1 .planning/phases/38-shell-dialogs-visual-conventions/evidence/ | wc -l</automated>
    <automated>grep -c "^result:" .planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` exists with YAML frontmatter
      containing `phase: 38-shell-dialogs-visual-conventions` and a `database:` field naming the Neon
      branch used.
    - `38-UAT.md` contains two CLOSE-02 check sections, each with a `result:` line whose value is
      `pass` or `fail` — never blank, never "n/a", never "assumed".
    - `.planning/phases/38-shell-dialogs-visual-conventions/evidence/` contains at least 3 image
      files from the performance trace filmstrip (`ls .../evidence/*.png | wc -l` ≥ 3).
    - The Check 1 `verified:` block names the CPU throttling factor used and states, frame by frame
      or by frame range, whether any light frame preceded the first dark frame.
    - The Check 1 `verified:` block quotes the computed values actually read from DevTools for
      `--background`, `--card` and `--muted-foreground`, not the values copied from
      `dark-palette.test.ts`.
    - The Check 2 `verified:` block quotes the computed `background-color` and `color` read off the
      PDF surface.
    - `31.1-VERIFICATION.md` is still `status: human_needed` at the end of this task —
      `grep -c "^status: human_needed" .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`
      prints `1`. Task 3 owns the flip.
    - `git status --porcelain` shows no modification to any file under `src/`, `app/`, or `tests/` —
      this task observes and records only.
  </acceptance_criteria>

  <done>
    Both CLOSE-02 checks have been performed against the running build, their results and the
    computed values behind them are recorded in `38-UAT.md`, and the filmstrip frames are on disk
    under `evidence/`. `31.1-VERIFICATION.md` is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator adjudicates the evidence; flip 31.1-VERIFICATION.md only on a real pass</name>

  <files>.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md</files>

  <read_first>
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-UAT.md` as written by Task 2.
    - Every image in `.planning/phases/38-shell-dialogs-visual-conventions/evidence/`.
    - `.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md` — its frontmatter, its
      `human_verification` block, and the SHELL-C7 row in the truths table (marked `⚠ PARTIAL`).
    - `.planning/phases/38-shell-dialogs-visual-conventions/38-CONTEXT.md` D-38-07 (bounded triage)
      and D-38-08 (the fail path).
  </read_first>

  <what-built>
    Task 2 captured a CPU-throttled performance-trace filmstrip of a dark-theme first paint and
    checked the PDF surface in dark mode, recording both in `38-UAT.md` with the computed values
    behind each verdict. The filmstrip frames are committed under `evidence/`.
  </what-built>

  <action>
    This is a blocking human-verify checkpoint. Claude does not adjudicate the filmstrip — D-38-02
    makes the operator the adjudicator precisely because Claude captured the evidence. Claude's part
    is to present the frames and `38-UAT.md`'s two `verified:` blocks, wait for the resume signal,
    and then execute exactly one of the two branches described in `<how-to-verify>`: on PASS, edit
    `.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`'s `status:`, `score:` and
    SHELL-C7 row, citing `38-UAT.md` and the `evidence/` path; on FAIL, leave that file byte-identical
    and route the defect through D-38-07. Under no circumstances flip the status before the signal.
  </action>

  <how-to-verify>
    1. Open the filmstrip frames in order. Decide for yourself whether any light-chrome frame
       precedes the first painted dark frame. This is the whole point of D-38-03's method — the
       adjudication is yours, not Claude's.
    2. Read `38-UAT.md`'s two `verified:` blocks and confirm the computed values quoted there match
       what `tests/dark-palette.test.ts` pins and what `app/globals.css:214-218` declares.
    3. Confirm the walk was read-only — no proposal was created, edited or deleted.
    4. Then instruct one of the two branches:

    **PASS branch.** Claude edits `.planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`:
    frontmatter `status: human_needed` → `status: passed`; update the `score:` line so it no longer
    says "1 partial — live pass pending"; update the SHELL-C7 row in the Observable Truths table from
    `⚠ PARTIAL` to VERIFIED, citing `38-UAT.md` and the `evidence/` filmstrip by path and naming the
    date and method. Leave the `human_verification:` block in place as the record of what was owed —
    do not delete it. Nothing else in that file changes. This is the phase's only cross-phase file
    write.

    **FAIL branch (D-38-08).** A failure means the shell is genuinely wrong in dark mode, and
    CLOSE-02 names both the file and the target status, so a fail cannot be recorded and moved past.
    Do NOT flip the status. Triage per D-38-07: if the cause is a token, a spacing literal, a CSS
    rule or an i18n string, fix it in-phase, then re-run Task 2's capture and return to this
    checkpoint. If it needs a component change, a new primitive, or a data/logic change, file it as a
    requirement for a later phase in `.planning/REQUIREMENTS.md`, record the block in `38-UAT.md`,
    and report to the orchestrator that CLOSE-02 cannot close in this phase. The repair's size is
    unknown until the failure is seen — that is why this is a checkpoint and not a scripted step.
  </how-to-verify>

  <acceptance_criteria>
    - On the PASS branch:
      `grep -c "^status: passed" .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`
      prints `1` and `grep -c "^status: human_needed" …` prints `0`.
    - On the PASS branch, `grep -c "38-UAT" .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`
      prints at least `1` — the flip cites the walk that produced it, so a later reader can trace the
      evidence rather than take the status on faith.
    - On the PASS branch, `grep -c "human_verification:" .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`
      still prints `1` — the record of what was owed survives the flip.
    - On the PASS branch, `git diff .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md`
      touches only the `status:` line, the `score:` line and the SHELL-C7 row.
    - On the FAIL branch, `31.1-VERIFICATION.md` is byte-identical to its pre-plan state
      (`git diff --quiet .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md` exits 0), and
      `38-UAT.md` records the failure plus the triage disposition (in-phase fix, or a new requirement
      ID in `.planning/REQUIREMENTS.md`).
    - `38-UAT.md`'s frontmatter `status` reflects the outcome and its `updated` timestamp is current.
  </acceptance_criteria>

  <resume-signal>
    Type "approved — flip to passed", or "failed: {what you saw in the filmstrip / PDF surface}".
  </resume-signal>

  <verify>
    <automated>grep -c "^status: " .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md</automated>
    <automated>grep -c "38-UAT" .planning/phases/31.1-app-shell-refresh/31.1-VERIFICATION.md</automated>
  </verify>

  <done>
    The operator has looked at the filmstrip and ruled. On a pass, `31.1-VERIFICATION.md` reads
    `status: passed` and cites `38-UAT.md`; on a fail, it is unchanged and the defect is routed with
    a recorded disposition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build → live database | A `npm start` on port 3001 connects to a real Neon branch; which branch depends on which env file is in place |
| Agent observation → durable verification record | Claude's browser observations become the basis for flipping a phase's status to `passed` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | Tampering | production Neon data during the walk | mitigate | The largest real risk in this plan is not an attacker but the walk itself: `.env.production.local` outranks `.env.local` under `NODE_ENV=production` (project memory `leasetic_env_production_local_outranks_guard`), and `check:local-db-branch` reports "development" even when serving production. Mitigated by Task 1's explicit branch decision recorded in `38-UAT.md`, the `mv .env.production.local .OFF` step, and the mandatory read-only constraint (wizard step 1 observed, never submitted). |
| T-38-10 | Repudiation | the `status: passed` flip on `31.1-VERIFICATION.md` | mitigate | A status flip with no reviewable evidence is unfalsifiable — precisely the failure `31.1-VERIFICATION.md` refused to commit the first time. Mitigated by committing the filmstrip frames, requiring `38-UAT.md` to quote computed DevTools values rather than impressions, and gating the flip behind a blocking human checkpoint that requires the operator to look at the frames themselves. |
| T-38-11 | Information Disclosure | committed evidence images | mitigate | Filmstrip frames of an authenticated session can contain real client names, amounts and partner emails. Before committing, review each frame and prefer frames of the shell/chrome over frames of populated data tables; if a frame that must be kept contains identifiable client data, redact it or capture the equivalent frame on a surface without live records. Record in the SUMMARY that this review was done. |
| T-38-12 | Spoofing / Denial of Service / Elevation | — | accept | This plan ships no code. It runs an existing build locally, observes it, and edits three markdown files. No new endpoint, no auth change, no dependency, no deployed artifact. Stated as a genuine "no new attack surface" finding. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No installs. `npm run build` uses the already-resolved lockfile; `package.json` and `package-lock.json` are untouched. |
</threat_model>

<verification>
- `38-UAT.md` exists with two CLOSE-02 sections, each carrying a `pass` or `fail` result and a
  `verified:` block quoting computed values
- At least 3 filmstrip frames committed under `evidence/`
- `31.1-VERIFICATION.md` reads `status: passed` **iff** both checks passed, and cites `38-UAT.md`
- No file under `src/`, `app/` or `tests/` was modified by this plan
- The local server is left running for plan 38-04, and the `.env.production.local` restore step is
  recorded as outstanding
</verification>

<success_criteria>
- Phase 31.1's two abandoned human checks have actually been performed against the shipped code
- The dark first-paint claim rests on a temporal artifact a later reader can re-examine, not on prose
- `31.1-VERIFICATION.md` reads `status: passed` only because it was earned
</success_criteria>

<output>
Create `.planning/phases/38-shell-dialogs-visual-conventions/38-03-SUMMARY.md` when done.
Record: which Neon branch the walk ran against and why; the CPU throttling factor; the frame-level
finding; the computed PDF-surface values; whether any hydration warning appeared in the console; the
result of the evidence-image privacy review; and the fact that the server is still running plus the
outstanding `mv .env.production.local.OFF .env.production.local` restore.
</output>
