---
phase: 37-crm-stack-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-fiche-client/34-VERIFICATION.md
autonomous: true
requirements: [CLOSE-04]

must_haves:
  truths:
    - "D-37-03: .planning/phases/34-fiche-client/34-VERIFICATION.md exists and is a FULL goal-backward verification covering all ten v1.6 requirements FICHE-01..05 and ACTV-01..05"
    - "D-37-03: every Evidence cell cites a file:line-range in the current codebase and, where testable, a named test case — never a SUMMARY reference"
    - "D-37-03: the four CI gate commands were re-run live during the verification and their literal output numbers are recorded"
    - "D-37-04: the document does not double as the operator walk — Phase 30's pending UAT scenarios are named as deferred to plan 37-05, not silently claimed as verified"
    - "Any genuine shortfall is RECORDED as a finding with a stated owner, not silently fixed inside Phase 37"
  artifacts:
    - path: ".planning/phases/34-fiche-client/34-VERIFICATION.md"
      provides: "goal-backward verification report for Phase 34"
      min_lines: 100
      contains: "FICHE-01"
  key_links:
    - from: ".planning/phases/34-fiche-client/34-VERIFICATION.md"
      to: ".planning/milestones/v1.6-REQUIREMENTS.md FICHE-01..05 / ACTV-01..05"
      via: "Requirements Coverage table, one row per requirement id"
      pattern: "ACTV-05"
    - from: ".planning/phases/34-fiche-client/34-VERIFICATION.md"
      to: "the Phase 34 source tree"
      via: "file:line-range citations in every Evidence cell"
      pattern: "\\.tsx?:\\d+"
---

<objective>
Close half of CLOSE-04. Phase 34 shipped 13 plans with no `34-VERIFICATION.md`. Per **D-37-03**
the verification is **full**: goal-backward against all ten v1.6 requirements — FICHE-01..05 and
ACTV-01..05 — **checked against the codebase rather than against the plans' own SUMMARY prose**.

`37-CONTEXT.md` sets the bar explicitly: Phase 35's verifier and Phase 36's both re-derived claims
rather than accepting SUMMARY prose. It also warns, from Phase 36's lesson, that deferral and
summary notes are routinely differently-shaped than the reality — two of Phase 36's five
requirements turned out that way. A verification that finds Phase 34 shipped everything cleanly is
a possible honest outcome; this plan must not presume it.

Purpose: the v1.6 CRM foundation gets the evidence record it never received, before Phase 40 tries
to formally close v1.6 on top of it.
Output: `.planning/phases/34-fiche-client/34-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-crm-stack-closure/37-CONTEXT.md
@.planning/phases/37-crm-stack-closure/37-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive FICHE-01..05 and ACTV-01..05 against the codebase</name>

  <files>
    .planning/phases/34-fiche-client/34-VERIFICATION.md
  </files>

  <read_first>
    - `.planning/milestones/v1.6-REQUIREMENTS.md` lines 136-150 — FICHE-01..05 and ACTV-01..05 as
      written. These ten sentences, not any plan's restatement of them, are what is being verified.
    - `.planning/phases/35-sales-motivation/35-VERIFICATION.md` — the structural and depth model
      named by D-37-03. Read it in full. Note the section order (frontmatter → Phase Goal →
      Goal Achievement / Observable Truths → Required Artifacts → Key Link Verification →
      Behavioral Spot-Checks → Requirements Coverage → Anti-Patterns Found → Human Verification
      Required → Gaps Summary → footer), the `# | Truth | Status | Evidence` column shape, and
      the depth of a single Evidence cell (line 36: a query function named with its file and line
      range, the tables it joins, its scoping, AND the named integration-test assertions that
      prove it against real Postgres).
    - `.planning/ROADMAP.md` § Phase 34: Fiche client (line 632 onward) — the phase goal and its
      success criteria, to be restated verbatim in the report.
    - `.planning/phases/34-fiche-client/34-CONTEXT.md` — Phase 34's own decisions, which
      constrain what "delivered" means for each requirement.
    - `.planning/phases/34-fiche-client/34-01-SUMMARY.md` .. `34-13-SUMMARY.md` — the CLAIMS.
      Read them to learn which files each plan touched, then verify against those files. Do NOT
      copy a SUMMARY sentence into an Evidence cell.
    - The Phase 34 surface itself, verified directly: `app/(authed)/clients/page.tsx`,
      `app/(authed)/clients/[id]/page.tsx` and its `_components`,
      `app/(admin)/[adminSegment]/companies/page.tsx`,
      `app/(admin)/[adminSegment]/companies/[id]/page.tsx`,
      `app/(admin)/[adminSegment]/companies/[id]/relations/[relationshipId]/page.tsx`,
      `src/lib/crm/actions.ts`, `src/lib/crm/registry-sync.ts`, `src/lib/crm/siren.ts`,
      `src/lib/crm/schemas.ts`, `src/lib/relationship/actions.ts`, `src/lib/relationship/kinds.ts`,
      `src/lib/db/queries/companies.ts`, `src/lib/db/queries/client-relationships.ts`,
      `src/lib/db/queries/relationship-events.ts`, `src/db/schema.ts`, and
      `app/(authed)/page.tsx`'s "à relancer" card call site (ACTV-05).
  </read_first>

  <action>
    Create `.planning/phases/34-fiche-client/34-VERIFICATION.md` and write sections 1-5 of the
    35-VERIFICATION.md structure.

    **Frontmatter:** `phase: 34-fiche-client`, `verified` (ISO-8601 UTC timestamp),
    `status` (`passed` / `gaps_found` / `human_needed` — decided by findings, not chosen up
    front), `score` in the form `N/N must-haves verified`, `verified_at_commit` (output of
    `git rev-parse HEAD`), `overrides_applied: 0`.

    **Phase Goal:** restated verbatim from `.planning/ROADMAP.md` § Phase 34.

    **`## Goal Achievement` → `### Observable Truths`:** a table with columns
    `# | Truth | Status | Evidence`. Derive truths from the ten requirement sentences, splitting
    where a requirement implies several separately-checkable behaviours (35-VERIFICATION.md turned
    five GAME requirements into nine truths). Cover at minimum, one truth each:
    - FICHE-01: SIREN lookup against the public SIRENE registry on create; a registry outage does
      NOT block creation; the record is marked as needing completion instead.
    - FICHE-02: the eight registry-sourced identity fields (legal name, address, legal form, NAF
      code, activity section, headcount band, founding date, administrative state) render
      read-only, with a last-synced date and a refresh control.
    - FICHE-03: display name / website / phone are partner-editable; a wrong SIREN can be
      corrected and re-runs the lookup; every such edit is audit-logged **because the fields are
      shared across partners**.
    - FICHE-04: source and description are private per relationship and invisible to another
      partner holding a relationship on the same company.
    - FICHE-05: the client page is a header plus the four tabs Informations / Contacts /
      Propositions / Activité, each edited in place through its own dialog.
    - ACTV-01: one timeline mixing manual notes with system events.
    - ACTV-02: stage change and proposal sent are recorded automatically with actor and timestamp.
    - ACTV-03: a dated note can be added.
    - ACTV-04: a relationship carries a next-action date.
    - ACTV-05: the follow-up list is driven by next-action date AND staleness.

    Every Evidence cell must carry a `path/file.ts:START-END` line range from the CURRENT tree and,
    where the behaviour is testable, the named test file plus the test case title or number that
    proves it. Verify each line range by reading it. A cell reading "see 34-07-SUMMARY" is a
    failure of this task.

    Set each Status honestly. Where a requirement is only partially delivered, or delivered
    differently from how its requirement sentence reads, say so in the cell — do not round up.
    Where the only remaining evidence is an operator observation, mark it and route it to the
    `### Human Verification Required` section rather than claiming it.

    **`### Required Artifacts`:** table `Artifact | Expected | Status | Details`, one row per file
    the 13 plans claimed to create or modify. Build the expected list from the plans' own
    `files_modified` frontmatter, then confirm each file's existence and current shape with `ls`
    and a read. A file that a plan claimed and that does not exist is a finding.

    **`### Key Link Verification`:** table `From | To | Via | Status | Details`, tracing the
    integration wiring — at minimum: the client detail page to its tab components; the tab
    dialogs to `src/lib/crm/actions.ts` / `src/lib/relationship/actions.ts`; the actions to the
    `relationship_events` write path in `src/lib/db/queries/relationship-events.ts`; the timeline
    read back out of that table; the "à relancer" call site in `app/(authed)/page.tsx` to
    `listRelationshipsNeedingFollowUp`; and the SIREN lookup path from `src/lib/crm/siren.ts`
    through `registry-sync.ts` to `companies`.
  </action>

  <verify>
    <automated>test -f .planning/phases/34-fiche-client/34-VERIFICATION.md &amp;&amp; grep -qE 'FICHE-0[1-5]' .planning/phases/34-fiche-client/34-VERIFICATION.md &amp;&amp; grep -qE 'ACTV-0[1-5]' .planning/phases/34-fiche-client/34-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/34-fiche-client/34-VERIFICATION.md` exists with YAML frontmatter carrying
      `phase`, `verified`, `status`, `score`, `verified_at_commit`, `overrides_applied`.
    - The `verified_at_commit` value equals the output of `git rev-parse HEAD` at write time.
    - The Observable Truths table has at least 10 numbered rows and every one of the strings
      `FICHE-01`, `FICHE-02`, `FICHE-03`, `FICHE-04`, `FICHE-05`, `ACTV-01`, `ACTV-02`,
      `ACTV-03`, `ACTV-04`, `ACTV-05` appears in the document.
    - CLI: `grep -oE '[A-Za-z0-9_/().[]-]+\.tsx?:[0-9]+' .planning/phases/34-fiche-client/34-VERIFICATION.md | sort -u | wc -l`
      returns at least 15 distinct file:line citations.
    - CLI: every path cited resolves — for each unique path in those citations,
      `test -f "$path"` exits 0. Report the check and its result in the SUMMARY.
    - CLI: `grep -c 'SUMMARY' .planning/phases/34-fiche-client/34-VERIFICATION.md` — any match must
      appear only in prose explaining that SUMMARY prose was NOT used as evidence, never inside an
      Evidence cell. State this explicitly in the SUMMARY.
    - The `### Required Artifacts` table has one row per file listed across the 13 plans'
      `files_modified` frontmatter, and the SUMMARY states how many rows that produced.
    - The `### Key Link Verification` table contains a row whose `To` cell names
      `src/lib/db/queries/relationship-events.ts`.
  </acceptance_criteria>

  <done>
    Ten requirements re-derived against the current tree, each with a verified file:line citation
    and, where testable, a named test case; artifacts and key links tabulated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run the gates live, then close the report honestly</name>

  <files>
    .planning/phases/34-fiche-client/34-VERIFICATION.md
  </files>

  <read_first>
    - `.planning/phases/34-fiche-client/34-VERIFICATION.md` — the sections written in Task 1.
    - `.planning/phases/35-sales-motivation/35-VERIFICATION.md` sections 6-11 — the models for
      Behavioral Spot-Checks (with the explicit "re-run independently during this verification
      (not taken on SUMMARY claims)" framing), Requirements Coverage, Anti-Patterns Found, Human
      Verification Required, Gaps Summary and the footer. Note line 108: the Gaps Summary
      distinguishes an accepted design decision from an actual gap rather than blurring them.
    - `.planning/phases/34-fiche-client/34-WALKTHROUGH.md` — the completed operator walk. What it
      already covers must NOT be re-claimed as fresh evidence; cite it as prior operator evidence
      where it genuinely applies.
    - `.planning/phases/34-fiche-client/34-SECURITY.md` — existing threat coverage, so the
      verification does not duplicate or contradict it.
    - `.planning/phases/30-company-contact-registry/30-UAT.md` § Bookkeeping Correction and
      scenarios 2, 9, 10, 12 — the four still-pending items. Per D-37-04 these are walked by plan
      37-05, NOT by this verification.
    - `.planning/milestones/v1.6-REQUIREMENTS.md` — the checkbox state for FICHE-01..05 and
      ACTV-01..05 (all currently `[x]`), needed for the orphaned-requirements statement.
  </read_first>

  <action>
    **`### Behavioral Spot-Checks`:** run the four CI gates live now — `npm run lint:check`,
    `npm run typecheck`, `npm test`, `npm run build` — and record a table row per command with its
    literal output numbers (exit code, and for `npm test` the exact passed/skipped counts).
    Additionally run the Phase 34 unit suites directly (`npx vitest run src/lib/crm
    src/lib/relationship src/lib/db/queries` and the client-page tests) and record their counts.
    State explicitly, as 35-VERIFICATION.md does, that these were re-run independently during this
    verification and not taken from SUMMARY claims.

    Do NOT run `npm run db:migrate`, `drizzle-kit push`, or any database write — `.env.local`
    points at the production Neon branch. Integration suites that require a live database are
    skip-by-default; record that as a stated limitation of this verification, not as a gap.

    **`### Requirements Coverage`:** table `Requirement | Source Plan(s) | Description | Status |
    Evidence`, one row per id FICHE-01..05 and ACTV-01..05. `Source Plan(s)` is derived from the
    13 plans' `requirements` frontmatter. Close with a sentence confirming whether any Phase 34
    requirement is orphaned against `.planning/milestones/v1.6-REQUIREMENTS.md`'s own checkbox
    state.

    **`### Anti-Patterns Found`:** state the exact grep run and over which paths. Use a pattern
    covering `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` plus stub-return shapes, scoped to the Phase 34
    artifact list from Task 1. Report the literal result. Filter comment-only header lines out of
    any count so the report's own prose cannot skew it.

    **`### Human Verification Required`:** name the items that genuinely still need an operator at
    a browser. Per D-37-04, explicitly record that `30-UAT.md` scenarios 2, 9, 10 and 12 are
    deferred to the consolidated walk in plan `37-05` and are therefore NOT claimed here — this
    verification must not double as that walk.

    **`### Gaps Summary`:** a final honest paragraph. Apply 35-VERIFICATION.md's distinction:
    an accepted design decision is not a gap; a genuine shortfall is.

    **Disposition of a genuine finding.** If re-derivation shows a FICHE/ACTV requirement is not
    actually delivered as its sentence reads, then within THIS phase the finding is **recorded, not
    fixed**: (a) write it into the Gaps Summary with its file:line evidence and a severity;
    (b) set the frontmatter `status` to `gaps_found`; (c) surface it in
    `37-03-SUMMARY.md` as a decision point for the operator, naming Phase 40 (which owns the v1.6
    formal close) as the phase that must dispose of it. Do NOT edit
    `.planning/milestones/v1.6-REQUIREMENTS.md` checkboxes, do NOT change Phase 34 source code,
    and do NOT create or edit any Phase 38/39/40 artifact — `37-CONTEXT.md` forbids reaching into
    adjacent phases.

    **Footer:** `_Verified: <timestamp>_` and `_Verifier: Claude (gsd-verifier)_`.

    Finally set the frontmatter `status` and `score` to match what the body actually found.
  </action>

  <verify>
    <automated>npm run lint:check &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - The document contains all of the headings `## Goal Achievement`, `### Observable Truths`,
      `### Required Artifacts`, `### Key Link Verification`, `### Behavioral Spot-Checks`,
      `### Requirements Coverage`, `### Anti-Patterns Found`, `### Human Verification Required`,
      `### Gaps Summary`.
    - The Behavioral Spot-Checks table contains a row for each of `lint:check`, `typecheck`,
      `test` and `build`, each with a literal recorded result; the `test` row carries an exact
      passed count and an exact skipped count.
    - The document contains the literal phrase `not taken on SUMMARY claims` (or a verbatim
      equivalent stating the gates were re-run independently during this verification).
    - The `### Human Verification Required` section names `30-UAT.md` scenarios 2, 9, 10 and 12
      and states they are deferred to plan `37-05`.
    - The document ends with `_Verifier: Claude (gsd-verifier)_`.
    - Frontmatter `status` is one of `passed`, `gaps_found`, `human_needed` and is consistent with
      the Gaps Summary: if the Gaps Summary names any genuine shortfall, `status` is NOT `passed`.
    - `git status --porcelain` shows changes to
      `.planning/phases/34-fiche-client/34-VERIFICATION.md` and to no other file — in particular
      `.planning/milestones/v1.6-REQUIREMENTS.md`, the Phase 34 source tree, and any Phase
      38/39/40 artifact are untouched.
    - `npm run lint:check && npm run typecheck && npm test && npm run build` all exit 0 (this plan
      changes no code; a failure here means the tree was already broken and must be reported).
  </acceptance_criteria>

  <done>
    `34-VERIFICATION.md` is complete, its gate numbers were produced by commands run during this
    verification, its status reflects what was actually found, and any shortfall is recorded with
    a named owner rather than quietly repaired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan SUMMARY prose → verification evidence | The primary integrity boundary of this plan: a claim must not become evidence by being restated. |
| verification report → Phase 40's v1.6 close | Phase 40 will formally close v1.6 on top of this document; a false "passed" propagates. |
| verifier → the repository | A verifier that edits code or requirement checkboxes converts an audit into an undisclosed change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Spoofing | SUMMARY prose masquerading as verification evidence | mitigate | Every Evidence cell must carry a `file.ts:START-END` citation from the current tree; the acceptance criteria require at least 15 distinct citations, require each cited path to resolve via `test -f`, and forbid `SUMMARY` inside an Evidence cell. |
| T-37-03-02 | Tampering | stale gate numbers copied from a prior report | mitigate | Task 2 re-runs `lint:check`, `typecheck`, `test` and `build` live and records literal exit codes and passed/skipped counts, with the explicit "not taken on SUMMARY claims" framing 35-VERIFICATION.md established. |
| T-37-03-03 | Repudiation | a report with no anchor to a tree state | mitigate | Frontmatter carries `verified_at_commit` from `git rev-parse HEAD` and an ISO-8601 `verified` timestamp, so every citation is checkable against a known commit. |
| T-37-03-04 | Information Disclosure | a verification that assumes clean and misses a real authorization or ownership shortfall in FICHE-04's per-partner privacy | mitigate | FICHE-04 gets its own Observable Truth requiring proof that source and description are invisible to another partner holding a relationship on the same company, cited to the owner-scoping code in `src/lib/db/queries/client-relationships.ts` and a named isolation test. Plan 37-04's scoped review independently covers the same code path. |
| T-37-03-05 | Elevation of Privilege | the verifier silently "fixing" a discovered gap, converting an audit into an unreviewed change | mitigate | The action explicitly forbids editing source, requirement checkboxes and adjacent-phase artifacts; the acceptance criteria assert `git status --porcelain` lists only `34-VERIFICATION.md`. A genuine gap is recorded, `status` is set to `gaps_found`, and disposition is routed to Phase 40 through the plan SUMMARY. |
| T-37-03-06 | Denial of Service | the verification absorbing the phase by expanding into the operator walk | mitigate | D-37-04 reserves the walk for plan 37-05; the `### Human Verification Required` section must name scenarios 2, 9, 10 and 12 as deferred there rather than attempting them. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages and touches no `package.json`. Enforced by the `git status --porcelain` acceptance criterion. |
</threat_model>

<verification>
- `.planning/phases/34-fiche-client/34-VERIFICATION.md` exists with all eleven structural sections.
- All ten requirement ids FICHE-01..05 and ACTV-01..05 appear, each with a Status and an Evidence
  cell citing verified file:line ranges.
- At least 15 distinct `file:line` citations, every cited path resolving on disk.
- The four CI gate commands were run during this plan and their literal numbers are in the report.
- `git status --porcelain` shows only `34-VERIFICATION.md` changed.
</verification>

<success_criteria>
Half of CLOSE-04 is closed: Phase 34 has a goal-backward `34-VERIFICATION.md` that re-derived every
one of its ten v1.6 requirements against the current codebase, ran its own gates, and recorded any
shortfall with evidence and a named owner instead of presuming a clean result.
</success_criteria>

<output>
Create `.planning/phases/37-crm-stack-closure/37-03-SUMMARY.md` when done.
It MUST record: the final `status` and `score`; the `verified_at_commit` hash; the number of
distinct file:line citations and the result of the path-resolution check; the literal gate numbers;
and — if any gap was found — the gap, its severity, and the explicit statement that it is routed to
Phase 40 rather than fixed here.
</output>
