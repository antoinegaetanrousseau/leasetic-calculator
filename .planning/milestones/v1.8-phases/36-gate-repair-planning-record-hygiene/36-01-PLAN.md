---
phase: 36-gate-repair-planning-record-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31.1-app-shell-refresh/deferred-items.md
  - .planning/milestones/v1.1-phases/06-auth-shell/06-CONTEXT.md
  - .planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md
  - .planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md
  - .planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md
  - .planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md
autonomous: true
requirements: [HOUSE-01, HOUSE-02]

must_haves:
  truths:
    - "A reader of `.planning/phases/31.1-app-shell-refresh/deferred-items.md` sees, in the file itself, that the 559-error lint finding is closed, on what date, by what means, and what prevents its recurrence"
    - "`npm run lint:check` exits 0 on the clean tree and the run is recorded as dated evidence"
    - "No `.claude/worktrees/**` entry is added to `eslint.config.mjs`, and no test is added pinning one"
    - "Every open question in 06-CONTEXT.md, 07-CONTEXT.md, 08-CONTEXT.md and 31-CONTEXT.md carries an inline RESOLVED or DEFERRED status naming a full relative reference path"
    - "A grep for un-annotated open-question bullets across those four files returns zero hits"
    - "The real audit detector reports both `31-CONTEXT.md` and `36-CONTEXT.md` under `auditOpenArtifacts().items.context_questions` BEFORE the edits and neither AFTER — read from `.items`, never the top level, which is `undefined`"
  artifacts:
    - path: ".planning/phases/31.1-app-shell-refresh/deferred-items.md"
      provides: "HOUSE-01 closure record, annotated in place under the original finding"
      contains: "CLOSED 2026-09-05"
    - path: ".planning/milestones/v1.1-phases/06-auth-shell/06-CONTEXT.md"
      provides: "Q4 annotated RESOLVED"
      contains: "RESOLVED"
    - path: ".planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md"
      provides: "Q2 + D-2 seed-coefficients bullets annotated with full relative refs"
      contains: "DEFERRED"
    - path: ".planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md"
      provides: "all four `<open_questions>` bullets annotated"
      contains: "DEFERRED"
    - path: ".planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md"
      provides: "all five numbered open questions annotated"
      contains: "DEFERRED"
  key_links:
    - from: ".planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md"
      to: ".planning/STATE.md § Deferred Items"
      via: "each annotation's status is copied from the v1.1-close resolution table, not invented"
      pattern: "RESOLVED|DEFERRED"
    - from: ".planning/phases/31.1-app-shell-refresh/deferred-items.md"
      to: "eslint.config.mjs `.claude/**` ignores entry"
      via: "the closure note names the pre-existing ignore entry as the recurrence guard"
      pattern: "\\.claude/\\*\\*"
---

<objective>
Close HOUSE-01 with recorded evidence and no code change, and make the four stale
planning records tell the truth about their own open questions (HOUSE-02).

Purpose: every v1.8 milestone audit re-reports the same v1.1-era questions and the same
lint finding, because the text in those files was never updated even though the underlying
items were resolved or formally deferred years of milestones ago. This plan edits the
records themselves, in place, so the audit stops resurfacing settled work.

Output: one closure annotation on the 31.1 deferred-items note, and inline status
annotations on every open question in `06-CONTEXT.md`, `07-CONTEXT.md`, `08-CONTEXT.md`
and `31-CONTEXT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md
</context>

<constraints>
- **Never edit `eslint.config.mjs` in this phase.** D-36-01 explicitly rejected adding a
  `.claude/worktrees/**` ignores entry and rejected adding a test pinning it. The file is
  read-only for the whole of Phase 36.
- **Annotate in place (D-36-05).** Do not append a "status update" section below the
  original text and do not delete the original question. Each question keeps its own
  wording and gains its status on the same bullet.
- **Do not invent a status.** Every RESOLVED/DEFERRED verdict for 06/07/08 comes from the
  v1.1-close resolution table in `.planning/STATE.md`. For 31-CONTEXT.md, the verdict comes
  from what actually shipped — read the Phase 31 summaries first.
- **Preserve the existing line wrapping** of each file. 31-CONTEXT.md wraps at ~95 columns
  with a 3-space continuation indent; a re-wrapped bullet makes the diff unreviewable.
- **Do not archive or move any file.** Archiving `.planning/phases/28..35` is Phase 40's
  job (CLOSE-07) and archiving is not an accepted substitute for annotating.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Close HOUSE-01 in place with dated evidence, changing no code</name>
  <files>.planning/phases/31.1-app-shell-refresh/deferred-items.md</files>
  <read_first>
    - `.planning/phases/31.1-app-shell-refresh/deferred-items.md` — the whole file (27 lines);
      it is the note that created HOUSE-01 and the target of this edit
    - `eslint.config.mjs` lines 40-45 — READ ONLY, never edit. The global `ignores` array
      already contains `'.claude/**'` with a three-line comment explaining that agent
      git-worktree scratch space is excluded there because `.git/info/exclude` is local and
      untracked so ESLint's flat config cannot inherit it from `.gitignore`. This entry is
      the recurrence guard the note must name.
    - `.planning/REQUIREMENTS.md` lines 109-111 — HOUSE-01's exact wording ("reports zero
      errors on a clean tree. It currently reports 559")
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md` § D-36-01
  </read_first>
  <action>
    Re-establish the two evidence facts by running them, then record the finding as closed.

    Step 1 — collect evidence. Run `npm run lint:check` and capture its exit code and full
    stdout/stderr. Run `git worktree list` and capture its output. Run
    `ls -la .claude/worktrees/` and capture its output. Do not proceed to step 2 if
    `lint:check` exits non-zero — instead stop and report the actual findings, because
    D-36-01's premise no longer holds and the plan needs revision.

    Step 2 — edit `.planning/phases/31.1-app-shell-refresh/deferred-items.md` in place.
    Immediately below the existing H2 heading line
    `## \`npm run lint:check\` reports 559 errors inside \`.claude/worktrees/*\``
    and above the existing `**Found during:**` line, insert a bold status line of the form
    `**Status: CLOSED 2026-09-05** (Phase 36, HOUSE-01 — D-36-01). No code change required.`

    Then append a new final section to the file titled `## Closure — 2026-09-05` carrying,
    in prose, exactly these four facts and nothing invented beyond them:
    (a) `npm run lint:check` (`eslint . --max-warnings=0`) exits 0 with no output on the
        clean tree, re-run on 2026-09-05 during Phase 36 execution;
    (b) `git worktree list` reports only `/Users/antoinerousseau/Developer/leasetic-calculator`
        on `main` — both `inspiring-benz-337233/` and `recursing-jang-941dda/` were removed
        on 2026-09-03, and `.claude/worktrees/` is now an empty directory;
    (c) the recurrence guard already exists and is not being added by this phase: the global
        `ignores` array in `eslint.config.mjs` already lists `'.claude/**'`, which covers any
        future worktree under `.claude/worktrees/` — so the note's own second remedy was
        already taken independently;
    (d) the narrower `.claude/worktrees/**` ignores entry the note suggested was explicitly
        REJECTED by the operator (D-36-01) as redundant with `'.claude/**'`, and no test
        pinning either entry was added.

    Also record the lesson named in `36-CONTEXT.md` § Specific Ideas: HOUSE-01 was written
    from a three-day-old deferral note describing a condition that had already stopped
    holding — an argument for scouting the live tree before planning from a note.

    Do not modify the original "Out of scope", "Not fixed here" or error-inventory prose.
    Those paragraphs are the historical record of why the finding was filed.
  </action>
  <verify>
    <automated>npm run lint:check; echo "exit=$?"</automated>
    <automated>test -z "$(git diff --name-only -- eslint.config.mjs)" &amp;&amp; echo "eslint.config.mjs untouched OK"</automated>
    <automated>grep -c 'Status: CLOSED 2026-09-05' .planning/phases/31.1-app-shell-refresh/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:check` exits 0 and prints no error lines.
    - `git status --porcelain eslint.config.mjs` prints nothing — the file is byte-identical
      to HEAD.
    - `grep -c "Status: CLOSED 2026-09-05" .planning/phases/31.1-app-shell-refresh/deferred-items.md`
      returns `1`.
    - `grep -c "## Closure — 2026-09-05" .planning/phases/31.1-app-shell-refresh/deferred-items.md`
      returns `1`.
    - The closure section contains the literal string `'.claude/**'` (the existing ignores
      entry it names) and the literal string `git worktree list`.
    - `grep -rn "worktrees/\*\*" eslint.config.mjs` returns no match.
    - `grep -rln "claude/worktrees" tests/` returns no match — no test was added.
    - The original paragraphs beginning `**Out of scope — not caused by this plan.**` and
      `**Not fixed here**` are still present verbatim.
  </acceptance_criteria>
  <done>HOUSE-01 reads as closed in its own origin note, backed by a re-run of the two evidence commands, with zero source-code diff.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the v1.1-era open questions in 06/07/08 CONTEXT.md</name>
  <files>.planning/milestones/v1.1-phases/06-auth-shell/06-CONTEXT.md, .planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md, .planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md</files>
  <read_first>
    - `.planning/STATE.md` § Deferred Items → the subsection headed
      `### Stale \`Open Questions\` markers in archived CONTEXT.md files` — the authoritative
      six-row resolution table (06/Q4, 07/Q2, 07/D-2, 08/Q1, 08/Q3, 08/Q5). This is the ONLY
      source for these verdicts.
    - `.planning/milestones/v1.1-phases/06-auth-shell/06-CONTEXT.md` around line 161 — the
      `### Open questions (per STATE.md and REQUIREMENTS.md — do NOT block on these)` heading
      nested inside `<canonical_refs>`, carrying ONE bullet (auth library version pinning).
    - `.planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md`
      lines 157-160 — `### Open questions (none blocking)` inside `<canonical_refs>`, TWO
      bullets. Bullet 1 is already annotated and is the in-repo shape precedent.
    - `.planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md`
      lines 178-186 — the only real `<open_questions>` XML block in the set, FOUR bullets
      (Q1, Q3, Q5 and an unnumbered Phase 7 carry-over).
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md` §5a, §5b, §5c
      — the verbatim current text of all three targets, to be used as exact match anchors.
  </read_first>
  <action>
    Three files, three different container shapes. Do not assume a uniform `<open_questions>`
    tag — only 08-CONTEXT.md has one.

    **State the real reason these three stopped surfacing in the audit, and do not overclaim.**
    The detector (`scanContextQuestions` in `~/.claude/get-shit-done/bin/lib/audit.cjs`) scans
    ONLY `.planning/phases/*/` for files whose name contains `-CONTEXT`. All three of
    06/07/08-CONTEXT.md moved to `.planning/milestones/v1.1-phases/` during the 2026-09-05
    cleanup, so they are already invisible to it — the annotation is NOT what stops the audit
    resurfacing them. Annotating them is still required by D-36-05 for a different and better
    reason: the files should tell the truth to a human reader. Say exactly that in the plan
    summary; do not write that the annotation cleared an audit finding for these three,
    because it did not.

    Annotation format, applied to every bullet: append to the END of the bullet's existing
    body, on the same bullet, one of
    `— RESOLVED &lt;YYYY-MM-DD&gt; — see &lt;full relative path from repo root&gt;.`
    or
    `— DEFERRED — see &lt;full relative path from repo root&gt;.`
    The path must be a full repo-relative path (e.g.
    `.planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-UI-SPEC.md §1.4`),
    NOT the bare `07-UI-SPEC §1.4` shorthand that 07-CONTEXT.md's existing bullet 1 uses.
    D-36-05 requires a full relative ref, so new annotations are stricter than that precedent.

    File 1 — `06-CONTEXT.md`, the single bullet under
    `### Open questions (per STATE.md and REQUIREMENTS.md — do NOT block on these)` beginning
    `- Auth library version pinning matrix (Open Q4)`. Status per STATE.md:
    RESOLVED 2026-05-08, Better Auth 1.6.9 pinned. Reference:
    `.planning/STATE.md § Decisions Log`. Append the annotation to the end of that bullet.
    Additionally amend the heading itself to
    `### Open questions (per STATE.md and REQUIREMENTS.md — do NOT block on these) — all resolved, see annotations`
    so a scanner reading only the heading is not misled.

    File 2 — `07-CONTEXT.md`, under `### Open questions (none blocking)`. Bullet 1
    (`Open Q2 ("v10 client_name field")`) is already annotated `RESOLVED 2026-05-08 in
    07-UI-SPEC §1.4 / D-7-06 / D-7-NN` — upgrade its bare ref to the full relative path
    `.planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-UI-SPEC.md §1.4`
    and leave the rest of the bullet as-is. Bullet 2
    (`Seed coefficient canonical values (D-2 placeholder)`) is the one needing work: status
    per STATE.md is DEFERRED — CUT-06 banner on first admin edit, Phase 10. Reference:
    `.planning/STATE.md § Deferred Items` (v1.1-close table) and
    `.planning/milestones/v1.1-REQUIREMENTS.md` CUT-06.

    File 3 — `08-CONTEXT.md`, the four bullets inside the `<open_questions>` block. Each
    bullet is `- **Bold label** — body.`; preserve the bold label untouched and append the
    annotation to the end of the body:
      * `Open Q1 (Cutover ownership)` → RESOLVED — Phase 10 D-10-21, Antoine owns partner
        comms. Reference `.planning/STATE.md § Deferred Items` (v1.1-close table).
      * `Open Q3 (Legal counsel sign-off on 10-year retention — DATA-11)` → DEFERRED —
        pending Thomas's reply, acknowledged in
        `.planning/milestones/v1.1-MILESTONE-AUDIT.md` under `deferred_acknowledged`; still
        carried, now owned by `.planning/REQUIREMENTS.md` OPS-04 (Phase 39).
      * `Open Q5 (OVH side stack — managed Postgres + S3-compatible)` → DEFERRED —
        September 2026, CUT-09 capability shipped; still carried, now owned by
        `.planning/REQUIREMENTS.md` OPS-03 (Phase 39).
      * `Phase 7 carry-over: Antoine's canonical coefficients` → DEFERRED, and note that it
        is the SAME item as 07-CONTEXT.md's bullet 2, resolving to the same CUT-06
        reference. Point at
        `.planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md`.

    Naming Phase 39 / OPS-03 / OPS-04 as the current owner of Q3 and Q5 is a cross-reference
    only. Do NOT do any Phase 39 work here: no credential change, no OVH action, no edit to
    `.planning/REQUIREMENTS.md`.

    Also amend the `## Open Questions Carried Forward (resolve before relevant phase)`
    heading inside 08-CONTEXT.md's `<open_questions>` block to append
    ` — statuses recorded inline 2026-09-05 (Phase 36, HOUSE-02)`.
  </action>
  <verify>
    <automated>grep -c 'RESOLVED\|DEFERRED' .planning/milestones/v1.1-phases/06-auth-shell/06-CONTEXT.md .planning/milestones/v1.1-phases/07-calc-engine-port-proposal-form/07-CONTEXT.md .planning/milestones/v1.1-phases/08-persistence-pdf-pipeline/08-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-CONTEXT.md`: the bullet starting `- Auth library version pinning matrix (Open Q4)`
      contains the substring `RESOLVED 2026-05-08` and the substring `.planning/STATE.md`.
    - `06-CONTEXT.md`: `grep -c "all resolved, see annotations" ` returns `1`.
    - `07-CONTEXT.md`: `grep -c "07-UI-SPEC.md §1.4"` returns `1` and
      `grep -c "07-UI-SPEC §1.4"` (the bare form) returns `0`.
    - `07-CONTEXT.md`: the bullet starting `- Seed coefficient canonical values` contains
      `DEFERRED` and contains `.planning/STATE.md`.
    - `08-CONTEXT.md`: all four bullets inside `<open_questions>` contain either `RESOLVED`
      or `DEFERRED` — verified by
      `awk '/<open_questions>/,/<\/open_questions>/' <file> | grep -c '^- \*\*'` equalling
      `awk '/<open_questions>/,/<\/open_questions>/' <file> | grep -c 'RESOLVED\|DEFERRED'`
      restricted to lines beginning `- **`, both returning `4`.
    - `08-CONTEXT.md`: `grep -c "statuses recorded inline 2026-09-05"` returns `1`.
    - Each of the three files still contains its original question text: `grep -c "Open Q4"`
      in 06, `grep -c "Open Q2"` in 07, and `grep -c "Open Q1\|Open Q3\|Open Q5"` ≥ 3 in 08.
    - This task touched only its three targets:
      `git status --porcelain -- .planning/milestones/v1.1-phases/` lists exactly
      `06-CONTEXT.md`, `07-CONTEXT.md` and `08-CONTEXT.md` and nothing else. Do NOT assert on
      a whole-tree `git diff` — sibling wave-1 plans (36-02, 36-03) run in the same working
      tree and legitimately have uncommitted changes of their own.
    - This task did not touch `.planning/REQUIREMENTS.md` — asserted by content signature, NOT
      by section scope. `git diff -U0 -- .planning/REQUIREMENTS.md` contains no added or
      removed line matching `RESOLVED|DEFERRED|Open Q`. Do NOT assert "no change inside the
      Housekeeping section": plan 36-03 Task 3 legitimately amends HOUSE-04's body at lines
      117-119, which sits inside `### Housekeeping (HOUSE)` (line 107 — note `###`, not `##`),
      and 36-03 is in the same wave and the same working tree. A section-scoped assertion here
      would fail on a sibling's correct work. This file is deliberately absent from this
      plan's `files_modified`; that is what actually carries the constraint.
  </acceptance_criteria>
  <done>Every one of the seven v1.1-era open questions across 06/07/08 carries an inline RESOLVED or DEFERRED status with a full relative reference, and no question text was deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate all five open questions in 31-CONTEXT.md</name>
  <files>.planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md, .planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md`
      lines 210-235 — the `## Open Questions (recorded, not guessed)` markdown heading that
      sits INSIDE the `<deferred>` block (there is no `<open_questions>` tag anywhere in this
      file), and the five numbered questions under it. Shape is
      `N. **Bold label.** Body sentences.` wrapped at ~95 columns with a 3-space
      continuation indent.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md` §5d — the
      verbatim current text of all five questions, to use as exact match anchors.
    - `.planning/phases/31-reconciliation-engine-proposal-extraction/` — every
      `31-*-SUMMARY.md` in the directory. These are the only evidence of what actually
      shipped and therefore the only source for each verdict.
    - `.planning/STATE.md` § Deferred Items → `### Re-acknowledged at v1.7 close (2026-09-05)`
      — the row `context_question | Phase 31 — 31-CONTEXT.md (3 open Qs: re-run idempotency,
      canonical name selection) | open`. **That '3' is not a real count.** It is an artifact of
      the detector's `questions.slice(0, 3)` truncation; the file has FIVE questions. Do not
      let the STATE.md figure override the file.
    - `~/.claude/get-shit-done/bin/lib/audit.cjs` — the `scanContextQuestions` function
      (around line 484). Read it before editing, because this task must satisfy the real
      detector, not a guess about it. Two facts matter: (a) it scans ONLY
      `.planning/phases/*/` for filenames containing `-CONTEXT`; (b) its body regex is
      `/##\s*Open Questions[^\n]*\n([\s\S]*?)(?=\n##\s|$)/i`, which is UNANCHORED and
      case-insensitive — so it matches a `### Open questions` heading and it matches the same
      literal quoted inside prose.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md` around line 113
      — the D-36-05 paragraph that quotes the literal string `### Open questions` while
      describing the three shapes. This quotation trips the same regex, which is why the live
      detector currently reports `36-CONTEXT.md` alongside `31-CONTEXT.md`.
  </read_first>
  <action>
    `36-CONTEXT.md` D-36-05 names only 3 questions for this file; the live file has FIVE
    (verified 2026-09-05). Annotate all five — annotating three leaves the file half-true,
    and HOUSE-02 requires that the block carry its real status.

    First establish the facts: read every `31-*-SUMMARY.md` in the phase directory and
    determine, per question, whether the shipped implementation settled it. Note that
    `.planning/phases/` contains NO `32-*` directory — Phase 32 was never executed, so any
    question whose stated resolution path was "IMPORT-07 lands in Phase 32" is DEFERRED, not
    resolved. Do not guess: if a summary does not decide a question, the verdict is DEFERRED.

    Then annotate each of the five numbered items in place, appending to the end of the
    question's body text, on the same numbered item, preserving the ~95-column wrap and the
    3-space continuation indent:
      1. **Re-run idempotency.** — verdict + reference
      2. **Canonical name selection.** — verdict + reference
      3. **Engine granularity.** — verdict + reference
      4. **Provenance scope.** — verdict + reference
      5. **Contact conflict across provenance.** — verdict + reference

    Annotation format, identical to Task 2:
    `— RESOLVED &lt;YYYY-MM-DD&gt; — see &lt;full repo-relative path&gt;.` or
    `— DEFERRED — see &lt;full repo-relative path&gt;.`
    For a DEFERRED verdict with no successor phase, the reference is
    `.planning/STATE.md § Deferred Items` and the annotation must say so explicitly rather
    than pointing at a phase that does not exist.

    **RETITLE the heading — do not merely append to it (operator decision, 2026-09-05).**
    Appending a suffix would leave the literal `## Open Questions` intact, and the detector's
    regex `/##\s*Open Questions[^\n]*\n.../i` matches the rest of the line, so the file would
    keep surfacing at every audit even though all five statuses are recorded. Replace the
    whole heading line
    `## Open Questions (recorded, not guessed)`
    with
    `## Questions recorded at planning time — all statuses resolved inline 2026-09-05 (Phase 36, HOUSE-02)`.
    This is truthful (they were recorded at planning time and every one now carries a status)
    and it contains no `Open Questions` substring, so the detector stops matching. The two
    explanatory lines directly beneath the heading ("These were surfaced during discussion and
    deliberately left for the planner...") stay verbatim — the retitle removes an audit false
    positive, it does not erase the file's history.

    Then fix the OTHER file that trips the same regex. In
    `.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md`, the D-36-05
    paragraph around line 113 quotes the literal string `### Open questions` while describing
    the three container shapes; because the detector's regex is unanchored and
    case-insensitive, that quotation alone makes `36-CONTEXT.md` report as carrying open
    questions. Reword just that quotation so it no longer contains the literal — for example
    `an Open-questions heading` (hyphenated) — leaving the sentence's meaning and the rest of
    the paragraph untouched. Check the whole file for any second occurrence and reword those
    too. Do not delete or restructure any decision text in `36-CONTEXT.md`; this is a
    one-token wording change so Phase 40's audit is clean.

    Do NOT touch anything else in `31-CONTEXT.md`, and do not move or archive the file —
    archiving `.planning/phases/28..35` is Phase 40's job (CLOSE-07).
  </action>
  <verify>
    <automated>awk '/^## Questions recorded at planning time/,/^&lt;\/deferred&gt;/' .planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md | grep -c 'RESOLVED\|DEFERRED'</automated>
    <automated>node -e 'const a=require(process.env.HOME+"/.claude/get-shit-done/bin/lib/audit.cjs");const f=a.auditOpenArtifacts(process.cwd()).items.context_questions.map(x=&gt;x.file);const want=["31-CONTEXT.md","36-CONTEXT.md"].filter(n=&gt;f.includes(n));if(want.length!==2){console.error("BEFORE-STATE GATE FAILED — expected both files to be detected, got:",JSON.stringify(f));process.exit(1)}console.log("before: both detected as expected ->",JSON.stringify(f))'</automated>
    <automated>node -e 'const a=require(process.env.HOME+"/.claude/get-shit-done/bin/lib/audit.cjs");const f=a.auditOpenArtifacts(process.cwd()).items.context_questions.map(x=&gt;x.file);const bad=f.filter(n=&gt;n==="31-CONTEXT.md"||n==="36-CONTEXT.md");if(bad.length){console.error("STILL DETECTED:",JSON.stringify(bad));process.exit(1)}console.log("after: detector clean for 31/36 CONTEXT; remaining ->",JSON.stringify(f))'</automated>
  </verify>
  <acceptance_criteria>
    - The heading was retitled, not appended to:
      `grep -c '^## Open Questions' .planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md`
      returns `0`, and
      `grep -c '^## Questions recorded at planning time — all statuses resolved inline 2026-09-05' <file>`
      returns `1`.
    - **The real detector is clean — asserted BEFORE and AFTER, not just after.** The list is
      read from `.items.context_questions`, NOT from the top level. `auditOpenArtifacts()`
      returns `{scanned_at, has_open_items, counts, items}`; a top-level
      `.context_questions` is `undefined`, and `undefined || []` makes any after-only check
      pass vacuously against an untouched tree. Verified 2026-09-05: top-level → `undefined`,
      `items.context_questions` → `["31-CONTEXT.md", "36-CONTEXT.md"]`.
      * **Before-state gate (run first, before editing either file):**
        `node -e 'const a=require(process.env.HOME+"/.claude/get-shit-done/bin/lib/audit.cjs");console.log(JSON.stringify(a.auditOpenArtifacts(process.cwd()).items.context_questions.map(x=>x.file)))'`
        MUST print a list containing both `31-CONTEXT.md` and `36-CONTEXT.md`. If it does
        not, STOP — either the object path is wrong again or the files were already edited,
        and in both cases the after-state check below would prove nothing.
      * **After-state gate (run once both edits have landed):** the same command MUST print a
        list containing NEITHER `31-CONTEXT.md` NOR `36-CONTEXT.md`.
      A gate that only checks the after-state cannot distinguish "the edit worked" from "the
      gate is blind". This phase's entire subject is claims with nothing behind them; the gate
      that proves HOUSE-02 must not be one of them.
    - Within the retitled section, the count of lines matching `RESOLVED|DEFERRED` is ≥ 5.
    - All five bold labels are still present verbatim: `grep -c "Re-run idempotency\.\|Canonical name selection\.\|Engine granularity\.\|Provenance scope\.\|Contact conflict across provenance\."`
      returns `5`.
    - `grep -c "all statuses resolved inline 2026-09-05" .planning/phases/31-reconciliation-engine-proposal-extraction/31-CONTEXT.md`
      returns `1`.
    - The two explanatory lines under the old heading survive:
      `grep -c 'deliberately left for the planner' <file>` returns `1`.
    - `36-CONTEXT.md` no longer contains the literal that tripped the regex:
      `grep -c '### Open questions' .planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md`
      returns `0`, while the surrounding D-36-05 decision text is otherwise unchanged
      (`grep -c 'D-36-05' <file>` unchanged from HEAD, `git diff --numstat` on that file shows
      deletions ≤ 2).
    - Every annotation contains a path beginning `.planning/` — verified by
      `grep -o 'see \.planning/[^ ]*' <file> | wc -l` returning ≥ 5.
    - No annotation references a `.planning/phases/32-` path (Phase 32 does not exist):
      `grep -c 'phases/32-' <file>` returns `0`.
    - No line in the annotated section exceeds 100 characters:
      `awk 'length > 100' <file> | wc -l` does not increase relative to HEAD.
    - Scope check, path-scoped rather than whole-tree (sibling wave-1 plans share this working
      tree and have their own uncommitted changes):
      `git status --porcelain -- .planning/phases/31-reconciliation-engine-proposal-extraction/`
      lists exactly `31-CONTEXT.md`, and
      `git status --porcelain -- .planning/phases/29-migration-safety-net/ .planning/phases/30-company-contact-registry/ .planning/phases/33-pipeline/ .planning/phases/34-fiche-client/ .planning/phases/35-sales-motivation/`
      prints nothing.
  </acceptance_criteria>
  <done>All five of 31-CONTEXT.md's open questions carry an inline verdict traced to a shipped summary or to STATE.md, with the original wording and line wrapping intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → planning record | Markdown-only edits; no runtime, no network, no credential crosses here |
| developer machine → ESLint config | The gate that later phases prove themselves against; a config change here silently weakens every downstream phase's evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | `eslint.config.mjs` | mitigate | The file is read-only for this phase. Task 1 acceptance requires `git status --porcelain eslint.config.mjs` to print nothing, so a widened `ignores` array cannot be smuggled in under a housekeeping commit and hide real findings from every later v1.8 phase. |
| T-36-02 | Repudiation | HOUSE-01 closure claim | mitigate | The closure record is not an assertion: Task 1 re-runs `npm run lint:check`, `git worktree list` and `ls -la .claude/worktrees/` at execution time and records the observed results with the date. A future auditor can re-run the same three commands. |
| T-36-03 | Tampering | v1.1-era planning records | mitigate | Every verdict is copied from `.planning/STATE.md`'s v1.1-close resolution table or from a `31-*-SUMMARY.md`; inventing a status is forbidden by the task constraint. Original question text must survive the edit (asserted by grep in acceptance criteria), so the annotation can never be used to erase an unresolved item. |
| T-36-04 | Information disclosure | annotated markdown | accept | These files contain no credentials, no customer data and no hostnames. The annotations add only status verdicts and repo-relative paths. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `git diff -- package.json package-lock.json` must be empty; no supply-chain surface is introduced. |
</threat_model>

<verification>
1. `npm run lint:check` exits 0.
2. `git status --porcelain eslint.config.mjs package.json package-lock.json` prints nothing.
3. Path-scoped scope check (never a whole-tree `git diff` — plans 36-02 and 36-03 run in the
   same wave and the same working tree):
   `git status --porcelain -- .planning/phases/31.1-app-shell-refresh/ .planning/milestones/v1.1-phases/ .planning/phases/31-reconciliation-engine-proposal-extraction/ .planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md`
   lists exactly the six files in `files_modified` and nothing else.
4. No question text was deleted: the total count of open-question bullets across the four
   CONTEXT files is unchanged from HEAD (1 + 2 + 4 + 5 = 12).
6. `auditOpenArtifacts(process.cwd()).items.context_questions` (note `.items` — the top-level
   `.context_questions` is `undefined` and makes any check on it pass vacuously) lists both
   `31-CONTEXT.md` and `36-CONTEXT.md` before Task 3 runs, and neither afterwards.
5. Every annotated bullet carries a repo-relative `.planning/` or `docs/` reference path.
</verification>

<success_criteria>
- HOUSE-01 is closed in its origin note with three re-run evidence commands and zero source diff.
- HOUSE-02's four files each carry inline RESOLVED/DEFERRED verdicts on every open question.
- `eslint.config.mjs` is byte-identical to HEAD.
- No file was archived, moved or deleted.
</success_criteria>

<output>
Create `.planning/phases/36-gate-repair-planning-record-hygiene/36-01-SUMMARY.md` when done.
</output>
