---
phase: 36-gate-repair-planning-record-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.1-REQUIREMENTS.md
  - package.json
autonomous: true
requirements: [HOUSE-03]

must_haves:
  truths:
    - "CALC-07 and PROP-01 read `[x]` in the v1.1 requirements list, with a narrative naming Phase 8 as the completing phase"
    - "The CALC-07 and PROP-01 traceability rows read `Complete` instead of `Partial`"
    - "The PROP-01 rationale prose at line 345 is explicitly reconciled with the flip, not silently contradicted"
    - "`npm run db:seed:partner-launch -- <email>` reaches `scripts/seed-partner-launch.ts` with the positional argument intact"
    - "No other `[~]` marker in the file was swept"
  artifacts:
    - path: ".planning/milestones/v1.1-REQUIREMENTS.md"
      provides: "CALC-07 + PROP-01 flipped to [x], both traceability rows Complete, line-345 prose reconciled"
      contains: "- [x] **CALC-07**"
    - path: "package.json"
      provides: "db:seed:partner-launch npm script entry"
      contains: "db:seed:partner-launch"
  key_links:
    - from: "package.json"
      to: "scripts/seed-partner-launch.ts"
      via: "tsx -r ./scripts/_preload-mock-server-only.cjs"
      pattern: "db:seed:partner-launch.*_preload-mock-server-only\\.cjs scripts/seed-partner-launch\\.ts"
---

<objective>
Flip the two stale `[~]` markers on CALC-07 and PROP-01 to `[x]` everywhere they appear in
the v1.1 requirements snapshot, and make `scripts/seed-partner-launch.ts` reachable through
an npm script instead of by path (HOUSE-03).

Purpose: both requirements were satisfied by Phase 8 work but the checkbox and the
traceability table were never updated, so every milestone audit re-reports them as partial.
The seed script has been reachable only by typing its path since Phase 10 (WR-AUDIT-02).

Output: four line edits plus one prose reconciliation in
`.planning/milestones/v1.1-REQUIREMENTS.md`, and one new `db:seed:*` entry in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-CONTEXT.md
@.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md
</context>

<constraints>
- **Four line edits, not two** (D-36-06, corrected against the live file 2026-09-05):
  bodies at lines 81 and 86, traceability rows at lines 283 and 285. A fifth site — the
  prose at line 345 — must be explicitly reconciled or explicitly retained as a historical
  note; silence there is a plan failure.
- **Do not sweep-replace `[~]`.** The marker is used legitimately elsewhere in this file
  (line 24, BOOT-03). Only lines 81 and 86 change.
- **Do not run any migration or DB command.** Adding an npm script is a `package.json` edit;
  the script is never executed against a database in this plan. `npm run db:migrate` and
  every migration command remain forbidden project-wide.
- **Do not touch `eslint.config.mjs`.**
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Flip CALC-07 and PROP-01 from [~] to [x] at all four sites and reconcile the line-345 prose</name>
  <files>.planning/milestones/v1.1-REQUIREMENTS.md</files>
  <read_first>
    - `.planning/milestones/v1.1-REQUIREMENTS.md` lines 74-90 — the CALC-07 body (line 81),
      the PROP-01 body (line 86), and their immediate `[x]` neighbours CALC-06 (line 80) and
      PROP-02 (line 87), which establish the house annotation style: a completed-with-nuance
      requirement keeps its narrative after an em dash on the same bullet. CALC-05 (line 74)
      is the canonical long-form example.
    - `.planning/milestones/v1.1-REQUIREMENTS.md` lines 280-290 — the traceability rows.
      CALC-07 is line 283, PROP-01 is line 285. The neighbouring rows CALC-08 (line 284,
      `Complete (07-01)`) and PROP-02 (line 286, `Complete`) show both target shapes, with
      and without a plan ID.
    - `.planning/milestones/v1.1-REQUIREMENTS.md` lines 340-350 — the PROP-01 rationale
      prose at line 345, beginning `- **PROP-01 (home page CTA) lives in Phase 7**`.
    - `.planning/milestones/v1.1-REQUIREMENTS.md` line 24 — the BOOT-03 `[~]` that must NOT
      be touched.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md` §6 — the
      verbatim current text of all four lines, to use as exact match anchors.
    - `.planning/STATE.md` § Deferred Items → the v1.1-audit row
      `Stale [~] markers in REQUIREMENTS.md (CALC-07, PROP-01) | This audit | Both
      functionally satisfied` — the authority for the flip.
  </read_first>
  <action>
    Five edits in one file. Use the full current line as the match anchor for each; the file
    has near-duplicate bullets and a partial match will hit the wrong one.

    Edit 1 — line 81, CALC-07 body. Change the leading `- [~]` to `- [x]`. Rewrite the
    `PARTIAL (07-05):` clause into the house `— <narrative>` form used by CALC-06 on line 80:
    keep the existing description of the client-side preview seam (`<LiveLoyerPreview>` calls
    `computeLoyer(...)` from `@/lib/calc`, display only, no DB write, D-7-07) and replace the
    forward-looking "Server-side recompute on save is Phase 8 territory" sentence with a
    completed statement naming Phase 8 as the phase that shipped the server-side recompute.
    The narrative must name the concrete artefacts that satisfy it — the server route parsing
    with `proposalInputSchema`, calling `computeLoyer`, and writing
    `params_snapshot + inputs + computed` jsonb to the proposals row — and must state that
    the client's display value is never trusted. Include the flip attribution
    `(flipped 2026-09-05, Phase 36 HOUSE-03 / D-36-06)`.

    Edit 2 — line 86, PROP-01 body. Change `- [~]` to `- [x]`. Same treatment: keep the
    empty-state shell description (greeting + CTA Link + `.card` recent-proposals empty state
    with FileText icon at `app/(authed)/page.tsx`) and replace "Full requirement (with
    populated row data) blocks on Phase 8 PROP-02..05" with a completed statement that
    Phase 8's PROP-02..05 populated the list. Include the same flip attribution.

    Edit 3 — line 283, the CALC-07 traceability row. Replace the third cell
    `Partial (07-05 — client preview seam shipped; server recompute is Phase 8)` with a
    `Complete` cell in the shape of the CALC-08 row on line 284, retaining the plan
    attribution and adding the completing phase — e.g.
    `Complete (07-05 client preview + Phase 8 server recompute)`. Leave cells 1 and 2
    (`CALC-07`, `Phase 7`) untouched — the requirement was still authored against Phase 7.

    Edit 4 — line 285, the PROP-01 traceability row. Replace
    `Partial (07-03 — empty-state shell shipped; populated rows block on Phase 8)` with the
    equivalent `Complete (...)` cell. Leave cells 1 and 2 untouched.

    Edit 5 — line 345, the PROP-01 rationale prose. This sentence already argues that "the
    empty CTA satisfies PROP-01; the populated list satisfies PROP-02..05", which SUPPORTS
    the `[x]`. Per D-36-06 the plan must state which way this goes: **reconcile it, do not
    delete it.** Append to that bullet a short clause recording that this reasoning is why
    PROP-01 is `[x]` as of 2026-09-05 (Phase 36, HOUSE-03), so a reader hitting line 345
    after the flip sees agreement rather than a contradiction. Keep the original two
    sentences verbatim.

    Do not touch line 24's BOOT-03 `[~]`, and do not touch any other line in the file.
  </action>
  <verify>
    <automated>! grep -qE '^- \[~\] \*\*(CALC-07|PROP-01)\*\*' .planning/milestones/v1.1-REQUIREMENTS.md &amp;&amp; echo 'no [~] left on CALC-07/PROP-01 OK'</automated>
    <automated>test "$(grep -cE '^- \[x\] \*\*(CALC-07|PROP-01)\*\*' .planning/milestones/v1.1-REQUIREMENTS.md)" = "2" &amp;&amp; echo 'both flipped to [x] OK'</automated>
    <automated>! grep -qE '\| (CALC-07|PROP-01) \|.*Partial' .planning/milestones/v1.1-REQUIREMENTS.md &amp;&amp; echo 'traceability rows no longer Partial OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[~\] \*\*CALC-07\*\*' <file>` returns `0`; `grep -c '^- \[x\] \*\*CALC-07\*\*' <file>` returns `1`.
    - `grep -c '^- \[~\] \*\*PROP-01\*\*' <file>` returns `0`; `grep -c '^- \[x\] \*\*PROP-01\*\*' <file>` returns `1`.
    - `grep -c '| CALC-07 |.*Partial' <file>` returns `0` and `grep -c '| CALC-07 |.*Complete' <file>` returns `1`.
    - `grep -c '| PROP-01 |.*Partial' <file>` returns `0` and `grep -c '| PROP-01 |.*Complete' <file>` returns `1`.
    - `grep -c 'PARTIAL (07-05)\|PARTIAL (07-03)' <file>` returns `0`.
    - Both flipped bullets contain the literal `Phase 8` and the literal `D-36-06`.
    - The line-345 bullet still contains `the empty CTA satisfies PROP-01` verbatim AND now
      contains `Phase 36`: `grep -c 'the empty CTA satisfies PROP-01' <file>` returns `1`.
    - BOOT-03 is untouched: `grep -c '^- \[~\] \*\*BOOT-03\*\*' <file>` returns the same
      value as at HEAD (`1`).
    - `git diff --numstat .planning/milestones/v1.1-REQUIREMENTS.md` shows at most 5 added
      and 5 removed lines — no reflow of unrelated content.
    - No file other than `.planning/milestones/v1.1-REQUIREMENTS.md` is modified by this task.
  </acceptance_criteria>
  <done>CALC-07 and PROP-01 read `[x]` in both the requirement list and the traceability table, the completing phase is named in each narrative, and the line-345 prose explicitly agrees with the flip.</done>
</task>

<task type="auto">
  <name>Task 2: Add the db:seed:partner-launch npm script and prove the positional arg reaches process.argv</name>
  <files>package.json</files>
  <read_first>
    - `package.json` lines 20-40 — the `scripts` block. Note the existing naming grouping
      (`check:local-db-branch` line 22, `db:backfill:coefficient-history` line 25,
      `db:backfill:partner-type` line 26, `purge:soft-deleted:dry` line 33, `smoke:ovh`
      line 34) and the three `db:seed:*` entries at lines 36-38, of which
      `db:seed:fiche-fixtures` (line 38) is currently the last entry in the block and
      therefore has no trailing comma.
    - `scripts/seed-partner-launch.ts` lines 1-45 — the docblock, including the env gate
      (`CONFIRM=SEED-PARTNER-<email>`, `INITIAL_PASSWORD`, `DATABASE_URL`), the documented
      invocation `npx tsx scripts/seed-partner-launch.ts <email> [--display-name "Full Name"]`,
      and **line 39, `import './_load-env';`** — which matters for the safety argument below.
    - `scripts/seed-partner-launch.ts` lines 90-125 — the gate ladder, in execution order.
      **Gate 0 runs FIRST** (lines ~97-104): `const TEST_EMAIL_RE = /^.+@test\.leasetic\.com$/;`
      and, on failure,
      `[seed-partner] REFUSE: email does not match @test.leasetic.com pattern.` +
      `[seed-partner] Got: ${email}` + `process.exit(2)`. Gate 1 is the typed-confirmation
      check (`CONFIRM !== SEED-PARTNER-<email>` → `process.exit(2)`), gate 2 is
      `DATABASE_URL` / `INITIAL_PASSWORD`. Read this before choosing a probe address — the
      domain gate fires before CONFIRM is even looked at.
    - `scripts/seed-partner-launch.ts` lines 126-140 — `console.log('[seed-partner] Connected
      to: ...')` and the lazy `await import('../src/lib/db/index')` /
      `await import('../src/db/schema')` calls that make the
      `-r ./scripts/_preload-mock-server-only.cjs` preload mandatory. Note that these sit
      AFTER all three gates.
    - `scripts/_load-env.ts` — `config({ path: '.env.local' })`. This is why `DATABASE_URL` is
      populated in the probe run whether or not the operator exports it.
    - `scripts/_preload-mock-server-only.cjs` — the CJS preload that neutralises the
      `server-only` guard.
    - `.planning/phases/36-gate-repair-planning-record-hygiene/36-PATTERNS.md` §4 — the
      verbatim analog entries and the exact line to add.
  </read_first>
  <action>
    Add one entry to the `scripts` block of `package.json`, placed immediately after
    `db:seed:fiche-fixtures` so it stays inside the `db:seed:*` grouping. The key is
    `db:seed:partner-launch` (D-36-07 discretion: this name fits the established `db:*`
    grouping). The value is
    `tsx -r ./scripts/_preload-mock-server-only.cjs scripts/seed-partner-launch.ts`
    — identical in shape to the three neighbouring `db:seed:*` entries. Add the trailing
    comma to the now-penultimate `db:seed:fiche-fixtures` line.

    Do NOT bake `CONFIRM=` or `INITIAL_PASSWORD=` into the script value. The
    `purge:soft-deleted:dry` entry does prefix an env var inside its value, but that is a
    *dry-run safety* prefix; here the env vars are the operator's confirmation gate and
    hard-coding an empty `CONFIRM=` would only mask the gate's error message.

    Then prove the invocation form actually works. `seed-partner-launch.ts` is the first
    `db:seed:*` script that takes a positional argument, so `npm run <script> -- <arg>` must
    be verified to reach `process.argv` rather than assumed. Run
    `npm run db:seed:partner-launch -- probe@test.leasetic.com` with NO `CONFIRM` and NO
    `INITIAL_PASSWORD` exported.

    **Use the `@test.leasetic.com` address, not an `example.invalid` one.** Gate 0 (D-10-09,
    lines ~97-104) is a domain check that runs BEFORE the typed-confirmation gate: any address
    outside `@test.leasetic.com` produces
    `[seed-partner] REFUSE: email does not match @test.leasetic.com pattern.` and exits 2. That
    would still echo the address back, but it would prove only that gate 0 saw it — a weaker
    result, and it would not exercise the CONFIRM path at all. `probe@test.leasetic.com` passes
    gate 0 and is rejected by gate 1 with
    `[seed-partner] Expected: CONFIRM=SEED-PARTNER-probe@test.leasetic.com`, which is the
    proof that the positional argument reached `process.argv` AND was used to build the
    email-scoped CONFIRM token. If instead the script reports a *missing email* / usage error,
    the `--` form is not reaching `process.argv` and the entry must be adjusted before the
    task is done.

    **Why this run cannot touch a database — state the guarantee correctly.** It is NOT that
    `DATABASE_URL` is unset: `scripts/seed-partner-launch.ts:39` is `import './_load-env';`,
    and that shim runs `config({ path: '.env.local' })`, so `DATABASE_URL` is populated from
    `.env.local` regardless of what the operator exports. The real guarantee is ordering:
    gate 0 and gate 1 both `process.exit(2)` BEFORE the lazy
    `await import('../src/lib/db/index')` at ~line 135, so no client is ever constructed and
    the `[seed-partner] Connected to: ...` line is never printed. Record the guarantee in that
    form in the summary — if someone later relaxes it on the false premise that an unset
    `DATABASE_URL` is what protects the run, they will remove the wrong guard.

    Never run `npm run db:migrate` or any migration command.

    Record the working invocation form — `npm run db:seed:partner-launch -- <email> --display-name "Full Name"` —
    together with the required `CONFIRM` / `INITIAL_PASSWORD` / `DATABASE_URL` env vars in the
    plan summary, so the next operator does not have to rediscover the `--` separator.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts; if(s['db:seed:partner-launch']!=='tsx -r ./scripts/_preload-mock-server-only.cjs scripts/seed-partner-launch.ts'){console.error('BAD:',s['db:seed:partner-launch']);process.exit(1)};console.log('script entry OK')"</automated>
    <automated>OUT=$(npm run db:seed:partner-launch -- probe@test.leasetic.com 2>&amp;1); printf '%s' "$OUT" | grep -q 'CONFIRM=SEED-PARTNER-probe@test.leasetic.com' &amp;&amp; echo 'positional arg reaches process.argv and builds the CONFIRM token OK'</automated>
    <automated>OUT=$(npm run db:seed:partner-launch -- probe@test.leasetic.com 2>&amp;1); test "$(printf '%s' "$OUT" | grep -c '\[seed-partner\] Connected to:')" = "0" &amp;&amp; echo 'no DB client constructed OK'</automated>
    <automated>npm run typecheck &amp;&amp; npm run lint:check</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').scripts['db:seed:partner-launch'])"`
      prints exactly
      `tsx -r ./scripts/_preload-mock-server-only.cjs scripts/seed-partner-launch.ts`.
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 —
      the file is still valid JSON after the trailing-comma fix.
    - Running `npm run db:seed:partner-launch -- probe@test.leasetic.com` with no `CONFIRM`
      and no `INITIAL_PASSWORD` exits non-zero AND its combined output contains the literal
      `CONFIRM=SEED-PARTNER-probe@test.leasetic.com` — proving the positional argument reached
      `process.argv`, passed gate 0's domain check, and was used to build the email-scoped
      CONFIRM token.
    - That same run's output does NOT contain `[seed-partner] Connected to:` — the gate exited
      before the lazy `await import('../src/lib/db/index')`, so no client was constructed.
      Assert absence with a count, not an inverted line filter:
      `test "$(printf '%s' "$OUT" | grep -c '\[seed-partner\] Connected to:')" = "0"`.
    - It also does NOT contain gate 0's refusal
      (`grep -c 'REFUSE: email does not match'` returns `0`) — confirming the probe address
      was chosen to exercise gate 1, not to stop at gate 0.
    - Nothing was written: `git status --porcelain -- src/ app/ drizzle/` prints nothing.
    - `git diff -- package.json` touches only the `scripts` block: `git diff -U0 -- package.json | grep -c '^[+-]' ` is ≤ 6 and no `+`/`-` line matches `dependencies|devDependencies|"version"`.
    - `package-lock.json` is unmodified: `git status --porcelain package-lock.json` prints nothing.
    - `npm run typecheck` exits 0 and `npm run lint:check` exits 0.
  </acceptance_criteria>
  <done>`npm run db:seed:partner-launch -- <email>` reaches the script with its positional argument, the env gate still fires, and no dependency or lockfile changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → `package.json` script → `scripts/seed-partner-launch.ts` | A newly-discoverable command that creates a partner user with a chosen password; the script's env gate is the only thing standing between `npm run` and a real account write |
| repo → `package.json` | The dependency manifest; any diff outside the `scripts` block is a supply-chain event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Elevation of privilege | `db:seed:partner-launch` npm script | mitigate | The script value carries no `CONFIRM=` and no `INITIAL_PASSWORD=` prefix, so `scripts/seed-partner-launch.ts`'s existing three-variable env gate (`CONFIRM=SEED-PARTNER-<email>`, `INITIAL_PASSWORD`, `DATABASE_URL`) remains the sole authorisation path. Task 2's acceptance criteria require the un-gated invocation to exit non-zero and write nothing, proving discoverability did not become executability. |
| T-36-06 | Information disclosure | probe invocation output | mitigate | The verification run uses `probe@test.leasetic.com` — the domain D-10-09 reserves for test accounts and which gate 0 already enforces — so no real partner address enters the transcript. The script masks the URL it prints (`maskUrl`) and the run never reaches that line anyway. |
| T-36-10a | Elevation of privilege | probe run reaching a real database | mitigate | The guarantee is ORDERING, not an unset variable: `scripts/seed-partner-launch.ts:39` is `import './_load-env';`, so `DATABASE_URL` is populated from `.env.local` whichever shell runs it. Gates 0 and 1 both `process.exit(2)` before the lazy `await import('../src/lib/db/index')` at ~line 135, so no client is constructed. Acceptance asserts `[seed-partner] Connected to:` is absent by count. Recording the guarantee this way prevents a later maintainer from removing the gates on the false premise that an unset `DATABASE_URL` was the protection. |
| T-36-07 | Tampering | `package.json` / `package-lock.json` | mitigate | Acceptance requires the diff to be confined to the `scripts` block and `package-lock.json` to be unmodified — no dependency can be added under cover of a script entry. |
| T-36-08 | Repudiation | v1.1 requirements flip | mitigate | Each flipped bullet carries the attribution `(flipped 2026-09-05, Phase 36 HOUSE-03 / D-36-06)`, so a future auditor can trace who changed a shipped-status marker and on what authority, rather than finding an unexplained `[x]`. |
| T-36-09 | Tampering | unrelated `[~]` markers | mitigate | Sweep-replacement is forbidden; acceptance pins BOOT-03's `[~]` at its HEAD count so a global substitution cannot silently mark unfinished work complete. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed. `npm run db:seed:partner-launch` invokes the already-present `tsx` 4.19.2 devDependency; `package-lock.json` must be byte-identical to HEAD. |
</threat_model>

<verification>
1. `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0.
2. `npm run typecheck` exits 0.
3. `npm run lint:check` exits 0.
4. `git status --porcelain package-lock.json` prints nothing.
5. `grep -c '\[~\]' .planning/milestones/v1.1-REQUIREMENTS.md` is exactly 2 lower than at
   HEAD — CALC-07 and PROP-01 flipped, everything else untouched.
</verification>

<success_criteria>
- CALC-07 and PROP-01 read `[x]` at all four sites and the line-345 prose agrees with the flip.
- `npm run db:seed:partner-launch -- <email>` reaches `scripts/seed-partner-launch.ts` with
  its positional argument, demonstrated by an echoed email in the gate's rejection message.
- `package-lock.json` and `eslint.config.mjs` are byte-identical to HEAD.
- No database was contacted and no migration command was run.
</success_criteria>

<output>
Create `.planning/phases/36-gate-repair-planning-record-hygiene/36-02-SUMMARY.md` when done.
</output>
