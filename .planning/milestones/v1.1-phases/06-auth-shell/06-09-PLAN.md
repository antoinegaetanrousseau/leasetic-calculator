---
phase: 06-auth-shell
plan: 09
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-01
files_modified:
  - scripts/grant-admin.ts
  - package.json
autonomous: true
requirements:
  - AUTH-12
  - AUTH-07

must_haves:
  truths:
    - "scripts/grant-admin.ts is a tsx-runnable CLI that accepts an email + optional --display-name flag, gated by CONFIRM=GRANT-ADMIN-{email} env var (D-16) and DATABASE_URL env var"
    - "If a user with that email exists AND role='admin' AND deletedAt IS NULL: log 'no-op (already admin)' and exit 0"
    - "If a user exists AND role='partner' AND deletedAt IS NULL: update users SET role='admin', log 'upgraded partner to admin', exit 0"
    - "If a user does not exist: insert users (id text nanoid, email lowercased, role='admin', display_name from --display-name or capitalized local-part of email), insert passwordResets (kind='invite', tokenHash sha256, expiresAt = NOW + 24h), print the invitation URL to stdout, exit 0"
    - "If a user exists AND deletedAt IS NOT NULL (disabled): re-enable + ensure role='admin' + emit a fresh invitation URL, log 'admin re-enabled, invitation issued'"
    - "Idempotent: re-running the script with the same email is safe — it does the right thing based on current DB state without ever overwriting an active user's password or active session"
    - "Mirrors scripts/migrate.ts: postgres-js client (max=1, prepare=false), URL hostname masking, SIGINT-safe cleanup, exits with non-zero status on error"
    - "package.json scripts has new 'grant:admin' npm script: 'tsx scripts/grant-admin.ts'"
  artifacts:
    - path: "scripts/grant-admin.ts"
      provides: "CLI script that grants admin role on prod DB; the ONLY path AUTH-12 allows for admin role assignment"
      contains: "CONFIRM=GRANT-ADMIN-"
    - path: "package.json"
      provides: "New 'grant:admin' script entry — usage: CONFIRM=GRANT-ADMIN-x@y.com npm run grant:admin -- x@y.com [--display-name 'Name']"
      contains: "grant:admin"
  key_links:
    - from: "scripts/grant-admin.ts"
      to: "src/db/schema.ts (users + passwordResets)"
      via: "drizzle(client, { schema }) with the same schema import as the app"
      pattern: "import \\* as schema"
    - from: "scripts/grant-admin.ts"
      to: "src/lib/auth/tokens.ts (generateToken)"
      via: "imports the same generateToken used by the app's createInvitation server action"
      pattern: "generateToken"
    - from: "scripts/grant-admin.ts"
      to: "process.env.CONFIRM + process.env.DATABASE_URL"
      via: "typed-confirmation gate (D-16) + masked URL connect"
      pattern: "process\\.env\\.CONFIRM"
---

<objective>
Build the production CLI script that grants admin role — the ONLY path AUTH-12 allows for admin assignment (never via the app UI). Mirrors the `scripts/migrate.ts` pattern from Phase 5: typed-confirmation gate, postgres-js client, URL hostname masking, idempotent.

Per D-13–D-16:
- Location: `scripts/grant-admin.ts`
- Invocation: `CONFIRM=GRANT-ADMIN-<email> npx tsx scripts/grant-admin.ts <email> [--display-name "Name"]`
- Behavior tree:
  - User exists AND role=admin AND active → no-op log
  - User exists AND role=partner AND active → upgrade role to admin
  - User exists AND deletedAt set (disabled) → re-enable + ensure admin role + emit fresh invitation URL
  - User does not exist → create user (role=admin, no password) + emit invitation URL
- The script never sees plaintext passwords. Admins set their own password via the invitation URL flow (already built in Plan 06-05).

Per D-15: at v1.1 launch, run twice — once for Antoine, once for Emmanuel. Each run prints an invitation URL the operator copies and shares out-of-band.

Output: 1 source file + 1 package.json script entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-shell/06-CONTEXT.md
@.planning/phases/06-auth-shell/06-RESEARCH.md
@.planning/phases/06-auth-shell/06-PATTERNS.md
@.planning/phases/06-auth-shell/06-03-SUMMARY.md
@.planning/phases/06-auth-shell/06-01-SUMMARY.md
@scripts/migrate.ts
@src/db/schema.ts
@src/lib/auth/tokens.ts
@package.json

<interfaces>
- Phase 5's scripts/migrate.ts: pattern source — typed-confirmation gate (`MIGRATE PROD`), `import 'dotenv/config'`, `postgres(url, { max: 1, prepare: false })`, hostname masking via `new URL(url).hostname`, SIGINT-safe `client.end({ timeout: 5 })` in finally
- 06-01 Drizzle schema: `users` (text id, role, displayName, deletedAt, email), `passwordResets` (uuid id default-random, userId text, kind, tokenHash unique, expiresAt, usedAt)
- 06-03 tokens.ts: `generateToken()` returns `{ plaintext, hash }` — 32 bytes URL-safe base64 + sha256 hex

Per CONTEXT.md D-15: Antoine + Emmanuel are the v1.1 launch admins. The script is run separately for each (the script is idempotent on re-runs).

Per CONTEXT.md D-14: when creating a user from scratch, displayName falls back to a sane default if --display-name is not passed. The fallback chosen here is: capitalize the local-part of the email (e.g. `antoine.rousseau@memento.eco` → `Antoine Rousseau`). The admin can edit displayName later via Phase 9 partner-edit UI.

Per RESEARCH §3 NOTE on "user creation without a password": create users via direct Drizzle insert; do NOT use Better Auth's signUpEmail (which requires a password). The user has no `accounts` row until they redeem the invitation URL via Plan 06-05's `redeemToken`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/grant-admin.ts CLI + register npm script</name>
  <files>scripts/grant-admin.ts, package.json</files>
  <read_first>
    - /Users/antoinerousseau/Developer/leasetic-calculator/scripts/migrate.ts (Phase 5 — pattern to mirror)
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-RESEARCH.md §8 "CLI grant-admin.ts Script"
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-PATTERNS.md "scripts/grant-admin.ts" section
    - /Users/antoinerousseau/Developer/leasetic-calculator/.planning/phases/06-auth-shell/06-CONTEXT.md D-13..D-16 (script location, behavior, idempotence, typed confirmation)
    - /Users/antoinerousseau/Developer/leasetic-calculator/src/lib/auth/tokens.ts (Plan 06-03)
    - /Users/antoinerousseau/Developer/leasetic-calculator/package.json
  </read_first>
  <action>
Create `scripts/grant-admin.ts`. Mirror the migrate.ts boilerplate (dotenv, postgres-js, masked URL, SIGINT-safe cleanup) but add the typed-confirmation gate per D-16 and the idempotent decision tree per D-14.

```
/**
 * scripts/grant-admin.ts — Phase 6 / AUTH-12
 *
 * The ONLY path for granting admin role on the production database.
 * Never grant admin via the app UI (REQUIREMENTS.md AUTH-12 is hard-locked).
 *
 * Usage:
 *   CONFIRM=GRANT-ADMIN-<email> npx tsx scripts/grant-admin.ts <email> [--display-name "Full Name"]
 *
 * Behavior tree (idempotent):
 *   - User exists AND role='admin' AND deletedAt IS NULL  → no-op (log + exit 0)
 *   - User exists AND role='partner' AND deletedAt IS NULL → upgrade role='admin'
 *   - User exists AND deletedAt IS NOT NULL (disabled)     → re-enable + role='admin' + emit fresh invitation URL
 *   - User does not exist                                   → create users row + emit invitation URL
 *
 * The script never sees plaintext passwords. New admins set their password by
 * redeeming the invitation URL (which routes through app/(public)/invite/[token]/
 * — Plan 06-05's flow).
 *
 * Per D-16: typed-confirmation gate (CONFIRM=GRANT-ADMIN-<email>) — defends
 * against accidental runs against the wrong DATABASE_URL.
 *
 * Per D-15: at v1.1 launch, run twice — once for Antoine, once for Emmanuel.
 */

import 'dotenv/config';
import postgres from 'postgres';
import { drizzle } from 'drizzle-orm/postgres-js';
import { and, eq, isNull } from 'drizzle-orm';
import { randomBytes } from 'node:crypto';
import * as schema from '../src/db/schema';
import { generateToken } from '../src/lib/auth/tokens';

const TOKEN_TTL_MS = 24 * 60 * 60 * 1000;

function parseArgs(argv: string[]): { email: string; displayName?: string } {
  const args = argv.slice(2);
  const email = args[0];
  if (!email || email.startsWith('--')) {
    console.error('Usage: CONFIRM=GRANT-ADMIN-<email> npx tsx scripts/grant-admin.ts <email> [--display-name "Full Name"]');
    process.exit(1);
  }
  let displayName: string | undefined;
  for (let i = 1; i < args.length; i++) {
    if (args[i] === '--display-name' && args[i + 1]) {
      displayName = args[i + 1];
      i++;
    }
  }
  return { email, displayName };
}

function defaultDisplayName(email: string): string {
  const local = email.split('@')[0] ?? email;
  return local
    .split(/[._-]+/)
    .filter(Boolean)
    .map((part) => part[0]!.toUpperCase() + part.slice(1))
    .join(' ');
}

function maskUrl(url: string): string {
  try { return new URL(url).hostname; } catch { return '(malformed URL)'; }
}

async function main() {
  const { email: rawEmail, displayName: cliDisplayName } = parseArgs(process.argv);
  const email = rawEmail.toLowerCase();

  // D-16: typed-confirmation gate
  const expectedConfirm = `GRANT-ADMIN-${email}`;
  if (process.env.CONFIRM !== expectedConfirm) {
    console.error(`FATAL: typed-confirmation gate not satisfied.`);
    console.error(`Expected:  CONFIRM=${expectedConfirm}`);
    console.error(`Received:  CONFIRM=${process.env.CONFIRM ?? '(unset)'}`);
    process.exit(2);
  }

  const url = process.env.DATABASE_URL;
  if (!url) {
    console.error('FATAL: DATABASE_URL is not set');
    process.exit(2);
  }

  console.log(`[grant-admin] Connecting to ${maskUrl(url)} ...`);
  console.log(`[grant-admin] Target email: ${email}`);

  const baseUrl = process.env.APP_URL ?? 'http://localhost:3000';
  const client = postgres(url, { max: 1, prepare: false, onnotice: () => {} });

  try {
    const db = drizzle(client, { schema });

    const existing = await db.query.users.findFirst({
      where: eq(schema.users.email, email),
      columns: { id: true, role: true, deletedAt: true, displayName: true },
    });

    if (existing && existing.role === 'admin' && existing.deletedAt === null) {
      console.log('[grant-admin] No-op: user already an active admin.');
      console.log(`[grant-admin] User id: ${existing.id}`);
      return;
    }

    if (existing && existing.deletedAt === null) {
      // Active partner → upgrade to admin
      await db.update(schema.users)
        .set({ role: 'admin' })
        .where(eq(schema.users.id, existing.id));
      console.log('[grant-admin] Upgraded partner to admin.');
      console.log(`[grant-admin] User id: ${existing.id}`);
      return;
    }

    if (existing && existing.deletedAt !== null) {
      // Disabled user → re-enable + ensure admin + emit fresh invitation URL
      await db.update(schema.users)
        .set({ role: 'admin', deletedAt: null, ...(cliDisplayName ? { displayName: cliDisplayName } : {}) })
        .where(eq(schema.users.id, existing.id));
      // Invalidate any prior unused tokens
      await db.delete(schema.passwordResets).where(eq(schema.passwordResets.userId, existing.id));
      const { plaintext, hash } = generateToken();
      await db.insert(schema.passwordResets).values({
        userId: existing.id,
        kind: 'invite',
        tokenHash: hash,
        expiresAt: new Date(Date.now() + TOKEN_TTL_MS),
      });
      console.log('[grant-admin] Re-enabled disabled user as admin; invitation URL issued:');
      console.log(`[grant-admin]   ${baseUrl}/invite/${plaintext}`);
      console.log('[grant-admin] (URL valid for 24 hours, single-use. Share via secure channel.)');
      return;
    }

    // Brand new user → insert + invitation URL
    // Better Auth-compatible string id (~21 chars, base64url-ish from random bytes).
    const userId = randomBytes(16).toString('base64url').slice(0, 21);
    const dn = cliDisplayName ?? defaultDisplayName(email);

    await db.insert(schema.users).values({
      id: userId,
      email,
      name: dn,
      displayName: dn,
      role: 'admin',
      // No accounts row — created by the invitation redemption flow (Plan 06-05).
    });

    const { plaintext, hash } = generateToken();
    await db.insert(schema.passwordResets).values({
      userId,
      kind: 'invite',
      tokenHash: hash,
      expiresAt: new Date(Date.now() + TOKEN_TTL_MS),
    });

    console.log('[grant-admin] Created new admin user.');
    console.log(`[grant-admin] User id: ${userId}`);
    console.log(`[grant-admin] Display name: ${dn}`);
    console.log('[grant-admin] Invitation URL (24-hour validity, single-use):');
    console.log(`[grant-admin]   ${baseUrl}/invite/${plaintext}`);
    console.log('[grant-admin] Share this URL via a secure channel. The admin will set their password upon redemption.');
  } catch (e) {
    console.error('[grant-admin] FAILED:', e);
    process.exit(1);
  } finally {
    await client.end({ timeout: 5 });
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

Step 2: Update `package.json` to add the npm script. Read the file first; the new entry goes inside the existing `scripts` block, alphabetically before `lint`:

```
"grant:admin": "tsx scripts/grant-admin.ts",
```

Final `scripts` block (existing entries preserved):
```
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint .",
  "lint:check": "eslint . --max-warnings=0",
  "typecheck": "tsc --noEmit",
  "test": "vitest run",
  "test:watch": "vitest",
  "db:generate": "drizzle-kit generate",
  "db:check": "drizzle-kit check",
  "check:no-vercel-imports": "bash scripts/check-no-vercel-only-imports.sh",
  "check:no-drizzle-push": "bash scripts/check-no-drizzle-push.sh",
  "db:migrate": "tsx scripts/migrate.ts",
  "db:migrate:dry-run": "tsx scripts/migrate.ts --dry-run",
  "grant:admin": "tsx scripts/grant-admin.ts"
}
```

Smoke-test: invocation without CONFIRM env var must fail with exit code 2:

```
cd /Users/antoinerousseau/Developer/leasetic-calculator
npm run grant:admin -- antoine.rousseau@memento.eco
# Expected: exit code 2, message about CONFIRM gate
```

Smoke-test: invocation with CONFIRM but no DATABASE_URL must fail with exit code 2:

```
DATABASE_URL='' CONFIRM=GRANT-ADMIN-antoine.rousseau@memento.eco \
  npm run grant:admin -- antoine.rousseau@memento.eco
# Expected: exit code 2, FATAL: DATABASE_URL is not set
```

These two smoke tests can run as part of the verify command without touching a real DB. Don't run the actual mutation against any real DB during this plan's execution — that's an operational task at v1.1 launch (per D-15).

NOTE on the existing CI guard `scripts/check-no-drizzle-push.sh` (Phase 5): the new `grant-admin.ts` does NOT call `drizzle-kit push` — it does direct INSERT/UPDATE via `db.insert`/`db.update`. The grep guard matches the literal string `drizzle-kit push` and won't false-positive. If it does, follow the same exclusion-list pattern Phase 5 added to `migrate.ts`.

NOTE on the no-vercel-imports check (Phase 5): `scripts/` is in the ignores list; this script can use `postgres` and `dotenv` directly without ESLint complaining.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator &amp;&amp; ls scripts/grant-admin.ts &amp;&amp; grep -c '"grant:admin"' package.json &amp;&amp; npm run typecheck &amp;&amp; npm run lint:check &amp;&amp; npm run check:no-drizzle-push &amp;&amp; ( npm run grant:admin -- bad@example.com 2>&amp;1 ; echo "EXIT=$?" ) | grep -E "EXIT=2|typed-confirmation gate" &amp;&amp; ( DATABASE_URL='' CONFIRM=GRANT-ADMIN-bad@example.com npm run grant:admin -- bad@example.com 2>&amp;1 ; echo "EXIT=$?" ) | grep -E "EXIT=2|DATABASE_URL is not set" &amp;&amp; grep -c "GRANT-ADMIN-" scripts/grant-admin.ts &amp;&amp; grep -c "generateToken" scripts/grant-admin.ts &amp;&amp; grep -c "max: 1" scripts/grant-admin.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/grant-admin.ts` exists
    - The script imports from `../src/db/schema` and `../src/lib/auth/tokens` (relative paths same as scripts/migrate.ts)
    - The script enforces `CONFIRM=GRANT-ADMIN-<email>` typed gate; without it exits with code 2 and a clear FATAL message
    - The script enforces `DATABASE_URL` env var; without it exits with code 2
    - The script masks the DATABASE_URL hostname for logging (uses `new URL(url).hostname`); never logs the password component
    - Decision tree implemented: 4 branches (active admin no-op, active partner upgrade, disabled re-enable + invite, new create + invite)
    - On invitation URL emission, the URL has the form `${APP_URL}/invite/${plaintext}` with the 32-byte plaintext from `generateToken()`
    - Smoke test (no CONFIRM): `npm run grant:admin -- bad@example.com` exits with code 2 and prints the gate-failure message
    - Smoke test (no DATABASE_URL): exits with code 2 and prints the URL-missing message
    - `package.json` scripts has the `grant:admin` entry
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
    - `npm run check:no-drizzle-push` exits 0 (no `drizzle-kit push` reference introduced)
  </acceptance_criteria>
  <done>CLI grant-admin script live, idempotent, gated by typed CONFIRM; npm script registered; smoke-tests for both gate failure modes pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → DATABASE_URL | The script connects directly to prod. Wrong URL = catastrophic. |
| Process env → CONFIRM gate | Single env var prevents typos / muscle-memory mistakes from running against wrong account. |
| stdout invitation URL → operator's clipboard / channel | The plaintext URL is the keys to the kingdom for 24h. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Tampering | accidental run against wrong DATABASE_URL (e.g. dev pointed at prod) | mitigate | D-16 typed-confirmation gate (`CONFIRM=GRANT-ADMIN-<email>`) requires a deliberate per-email env var; copy-paste from a runbook ensures the operator has confirmed both the email AND the DB target. |
| T-06-09-02 | Information Disclosure | DATABASE_URL credentials in logs | mitigate | URL masked via `new URL(url).hostname`; only host echoed, never user/password. |
| T-06-09-03 | Information Disclosure | invitation URL leaked via shell history / scrollback | accept | Operator's responsibility to clear scrollback after copying the URL. The 24h TTL bounds damage. The URL is single-use — once redeemed, future leaks are inert. |
| T-06-09-04 | Elevation of Privilege | malicious local user runs the script (without prod DB access) | accept | The script requires DATABASE_URL — if the local user has prod DB credentials they are already trusted at the operator level. v1.1 has no separation between developer and operator (single-developer workflow). |
| T-06-09-05 | Tampering | re-running the script overwrites password / deletes accounts row | mitigate | The script never touches `accounts` table. Redemption flow (Plan 06-05) creates the accounts row on first password-set. Re-running grant-admin on an already-active admin is an explicit no-op — verified in test branch 1. |
| T-06-09-06 | Repudiation | no audit log of grant-admin runs | accept | Phase 6 deferred audit_log to Phase 9; the script does emit structured stdout that operators capture in their runbook. Phase 9 can backfill an audit_log row for grant-admin events if needed. |
| T-06-09-07 | Tampering | command-line argument injection (e.g. email contains shell metacharacters) | mitigate | The script reads argv[2] as a string; `email.toLowerCase()` is the only mutation. SQL injection is impossible because Drizzle parameterizes via postgres-js prepared statements. The CONFIRM env var must match the email exactly — including any metacharacters — which is by-construction safe. |
| T-06-09-08 | Spoofing | someone with shell access generates an invitation URL for themselves | accept | Same as T-06-09-04: shell access = trusted operator. v1.1 single-developer workflow. |
| T-06-09-09 | DoS | running the script in a loop creates many users | accept | The CONFIRM gate makes scripted abuse non-trivial; passwords still need to be set via redemption (cap-of-1 active token per user via invalidatePriorTokens-equivalent). Operationally bounded. |
</threat_model>

<verification>
At plan completion the following must all be true:

1. `cd /Users/antoinerousseau/Developer/leasetic-calculator && ls scripts/grant-admin.ts` succeeds
2. `grep -c '"grant:admin"' package.json` returns 1
3. Running `npm run grant:admin -- bad@example.com` (no CONFIRM, no DATABASE_URL) exits with non-zero status (2) and includes the typed-confirmation message in output
4. `npm run typecheck && npm run lint:check && npm run check:no-drizzle-push` all exit 0
5. `grep -c "max: 1" scripts/grant-admin.ts` returns 1 (postgres-js connection pool sized like migrate.ts)
6. `grep -c "generateToken" scripts/grant-admin.ts` ≥ 1 (uses the same token generator as the app)
7. `grep -c "GRANT-ADMIN-" scripts/grant-admin.ts` ≥ 1 (typed-confirmation pattern present)
</verification>

<success_criteria>
- AUTH-12 satisfied: the CLI script is the ONLY path for admin role assignment; the app UI never grants admin
- AUTH-07 (admin creates partner) — the CLI also handles the launch-day "Antoine + Emmanuel" provisioning per D-15
- The script is idempotent across all 4 branches (active admin / active partner / disabled / new)
- D-16 typed-confirmation gate prevents accidental misuse
- The invitation URL emitted by the script reuses the same `passwordResets` + `generateToken` machinery as the app's `createInvitation` server action — partners and admins follow the same redemption flow (Plan 06-05's /invite/[token])
- Phase 6 closure: at v1.1 launch, Antoine runs `CONFIRM=GRANT-ADMIN-antoine@... npm run grant:admin -- antoine@... --display-name "Antoine Rousseau"` then again for Emmanuel, copies each invitation URL, shares out-of-band, and each admin sets their own password via the /invite/[token] flow.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-shell/06-09-SUMMARY.md` covering: the actual nanoid generation approach used (whether Better Auth's nanoid was importable directly or the `randomBytes(16).base64url.slice(0,21)` fallback was used), and the smoke-test outputs for both gate-failure scenarios.
</output>
