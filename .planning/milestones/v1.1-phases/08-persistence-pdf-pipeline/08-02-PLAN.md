---
phase: 8
plan_id: "08-02"
slug: i18n-keys
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/dictionaries.ts
  - src/lib/i18n/dictionaries.test.ts
requirements:
  - PROP-02
  - PROP-03
  - PROP-04
  - PROP-11
  - PROP-12
  - PROP-13
  - PROP-15
  - PROP-16
  - PROP-18
  - PROP-20
  - PROP-22
  - PROP-26
autonomous: true
tags: [i18n, dictionaries, copy, fr, en]
objective: |
  Add the Phase 8 i18n delta to `src/lib/i18n/dictionaries.ts` per
  08-UI-SPEC.md §7. Target: ~56 NEW keys × 2 languages (FR + EN). Honor the
  reuse opportunities flagged in §7.9 (reuse existing v10 dict keys for 5
  PDF labels — see <reuse_table>). Extend `dictionaries.test.ts` with a
  parametric Phase-8 spot-check + parity assertions matching Phase 7's
  pattern. Single-purpose plan; runs in parallel with 08-01 (no file
  overlap).

truths:
  - "Every Phase 8 surface UI string referenced by 08-UI-SPEC §7 is present in dictionaries.fr AND dictionaries.en (compile-time _EnHasAllFrKeys parity proof intact)"
  - "List view keys present: proposal.search.placeholder, .aria, .clear, .empty.title, .empty.body, proposal.list.toggle.active, .toggle.deleted, .load.more, .load.more.loading, proposal.deleted.empty.title, .empty.body"
  - "Chip keys present: proposal.chip.active, .expired, .deleted, .language.tooltip, .tooltip.expires, .tooltip.expired"
  - "Detail page keys present: proposal.detail.title, .created.line, .section.inputs, .section.computed, .computed.expires.label, .computed.expired.label, .computed.loyer.suffix"
  - "Action button keys present: proposal.detail.action.download, .duplicate, .delete, .restore"
  - "PDF preview UI keys present: proposal.detail.pdf.preview.title, .preview.aria, .fallback.link"
  - "Deleted-view keys present: proposal.detail.deleted.banner, proposal.confirm.delete"
  - "Toast keys present: proposal.toast.submit.loading, .submit.success, .submit.error, .delete.success, .delete.action.view.deleted, .delete.error, .restore.success, .restore.error, .duplicate.prefilled"
  - "PDF document keys present: pdf.tagline, pdf.title, pdf.ref.label, pdf.section.recipient, pdf.section.project, pdf.section.interests, pdf.project.placeholder, pdf.project.ref.prefix, pdf.computed.coefficient.label, pdf.loyer.label, pdf.loyer.subtext, pdf.loyer.on.demand, pdf.validity.caption, pdf.footer.left"
  - "Reused v10 keys NOT redeclared (proposal.duree.label, proposal.duree.months, proposal.interests.slb, proposal.interests.eval, proposal.montant.label) — consumers reference them directly"
  - "Test: every Phase 8 key resolves non-empty in both languages; interpolation-contract keys (.created.line, .deleted.banner, .duplicate.prefilled, .delete.success/error+action, pdf.loyer.subtext, pdf.validity.caption, pdf.footer.left) contain {0} / {1} placeholders"
  - "npm run typecheck + npm run lint:check + npm test all exit 0"

must_haves:
  truths:
    - "Phase 8 dictionary delta lives in src/lib/i18n/dictionaries.ts"
    - "Both `fr` and `en` blocks contain every new key (TypeScript _EnHasAllFrKeys parity proof catches drift at compile time)"
    - "Reused v10 keys are referenced by their canonical names; no duplicate entries created"
    - "Test count grew by ≥ Phase8KeyCount × 2 (FR + EN non-empty assertions) plus interpolation-contract assertions"
  artifacts:
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "Phase 8 key set added to fr + en blocks"
      contains: "proposal.toast.submit.loading"
    - path: "src/lib/i18n/dictionaries.test.ts"
      provides: "Phase 8 parametric spot-check + interpolation contract assertions"
      contains: "phase8Keys"
  key_links:
    - from: "src/lib/i18n/dictionaries.ts"
      to: "src/lib/i18n/dictionaries.test.ts"
      via: "DictKey type drives the parametric test loop"
      pattern: "phase8Keys: DictKey\\[\\]"
---

<objective>
Phase 8 introduces 5 new screens worth of UI copy + a fully bilingual PDF
template. This plan ships the FR + EN keys before any consumer plan
needs them. Single concern; small file diff; runs in parallel with
schema (08-01) on Wave 1.

Purpose: Plans 08-09 (form submit), 08-10 (detail page), 08-11 (list),
08-12 (delete/restore), 08-13 (duplicate), 08-05 (PDF render) and
08-07 (server route — toast keys for the submit pipeline) all depend on
these keys at lint time. ESLint's `no-restricted-syntax` rule from Phase
6 fires on hardcoded JSXText, so consumers MUST pull strings from `t()`.

Output: ~56 keys × 2 langs added to dictionaries.ts; ~120 new test
assertions; zero new TypeScript compile errors (parity proof intact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-persistence-pdf-pipeline/08-UI-SPEC.md
@.planning/phases/08-persistence-pdf-pipeline/08-CONTEXT.md
@src/lib/i18n/dictionaries.ts
@src/lib/i18n/dictionaries.test.ts

<reuse_table>
Per UI-SPEC §7.9 — these v10 keys already exist in dictionaries.ts
(verbatim string match for the Phase 8 use case); reuse instead of
adding duplicates:

| Phase 8 candidate key | Reuse v10 key | Note |
|-----------------------|---------------|------|
| pdf.computed.duration.label | proposal.duree.label | Same FR + EN ("Durée" / "Term") |
| pdf.computed.duration.suffix | proposal.duree.months | Same FR + EN ("mois" / "months") |
| pdf.interests.slb | proposal.interests.slb | Same FR + EN |
| pdf.interests.eval | proposal.interests.eval | Same FR + EN |
| pdf.computed.amount.label | proposal.montant.label | Same FR + EN |

Consumers (Plan 08-05 PDF render + Plan 08-10 detail computed card)
import the existing keys; do NOT add the `pdf.computed.duration.*` /
`pdf.interests.*` / `pdf.computed.amount.label` aliases. Document the
reuse in dictionaries.ts inline comments at the Phase 8 block boundary.
</reuse_table>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Phase 8 keys to dictionaries.ts (FR + EN)</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - `src/lib/i18n/dictionaries.ts` (current 263 keys × 2 langs from Phase 7)
    - `.planning/phases/08-persistence-pdf-pipeline/08-UI-SPEC.md` §7.1 - §7.8 (the canonical key tables)
  </read_first>
  <action>
Append the Phase 8 keys to BOTH `dictionaries.fr` and `dictionaries.en` blocks of `src/lib/i18n/dictionaries.ts`. Place them at the END of each block (just before the closing brace), separated from existing keys by a comment header so the delta is visually traceable.

**Insert in BOTH `fr` and `en` blocks** (positioned after the existing Phase 7 keys; before the closing `};`):

```ts
    // ── Phase 8 — Persistence + PDF Pipeline (Plan 08-02) ──────────────────────
    // 56 new keys × 2 langs per UI-SPEC §7. Reuse v10 keys for 5 PDF labels
    // (see UI-SPEC §7.9 reuse table): proposal.duree.label, proposal.duree.months,
    // proposal.interests.slb, proposal.interests.eval, proposal.montant.label.
    // Plan 08-05 (PDF) + Plan 08-10 (detail) consume those existing keys directly.

    // 7.1 List view (11 keys)
    'proposal.search.placeholder': '...',
    'proposal.search.aria': '...',
    'proposal.search.clear': '...',
    'proposal.search.empty.title': '...',
    'proposal.search.empty.body': '...',
    'proposal.list.toggle.active': '...',
    'proposal.list.toggle.deleted': '...',
    'proposal.list.load.more': '...',
    'proposal.list.load.more.loading': '...',
    'proposal.deleted.empty.title': '...',
    'proposal.deleted.empty.body': '...',

    // 7.2 Validity / chips (6 keys)
    'proposal.chip.active': '...',
    'proposal.chip.expired': '...',
    'proposal.chip.deleted': '...',                       // contains {0}
    'proposal.chip.language.tooltip': '...',
    'proposal.chip.tooltip.expires': '...',               // contains {0}
    'proposal.chip.tooltip.expired': '...',               // contains {0}

    // 7.3 Detail page header + sections (7 keys)
    'proposal.detail.title': '...',                       // contains {0}
    'proposal.detail.created.line': '...',                // contains {0}, {1}
    'proposal.detail.section.inputs': '...',
    'proposal.detail.section.computed': '...',
    'proposal.detail.computed.expires.label': '...',      // contains {0}
    'proposal.detail.computed.expired.label': '...',      // contains {0}
    'proposal.detail.computed.loyer.suffix': '...',

    // 7.4 Action buttons (4 keys)
    'proposal.detail.action.download': '...',
    'proposal.detail.action.duplicate': '...',
    'proposal.detail.action.delete': '...',
    'proposal.detail.action.restore': '...',

    // 7.5 PDF preview + fallback (3 keys)
    'proposal.detail.pdf.preview.title': '...',
    'proposal.detail.pdf.preview.aria': '...',
    'proposal.detail.pdf.fallback.link': '...',

    // 7.6 Deleted-view banner + confirm (2 keys)
    'proposal.detail.deleted.banner': '...',              // contains {0}
    'proposal.confirm.delete': '...',

    // 7.7 Toasts (9 keys — submit pipeline + delete/restore/duplicate)
    'proposal.toast.submit.loading': '...',
    'proposal.toast.submit.success': '...',
    'proposal.toast.submit.error': '...',
    'proposal.toast.delete.success': '...',
    'proposal.toast.delete.action.view.deleted': '...',
    'proposal.toast.delete.error': '...',
    'proposal.toast.restore.success': '...',
    'proposal.toast.restore.error': '...',
    'proposal.toast.duplicate.prefilled': '...',

    // 7.8 PDF document copy (14 keys — used by @react-pdf/renderer only;
    //     5 v10 keys reused per §7.9 — pdf.computed.duration.* + pdf.interests.* + pdf.computed.amount.label
    //     are NOT redeclared; consumers reference proposal.duree.* / proposal.interests.* / proposal.montant.label.)
    'pdf.tagline': '...',
    'pdf.title': '...',
    'pdf.ref.label': '...',
    'pdf.section.recipient': '...',
    'pdf.section.project': '...',
    'pdf.section.interests': '...',
    'pdf.project.placeholder': '...',
    'pdf.project.ref.prefix': '...',
    'pdf.computed.coefficient.label': '...',
    'pdf.loyer.label': '...',
    'pdf.loyer.subtext': '...',                           // contains {0}
    'pdf.loyer.on.demand': '...',
    'pdf.validity.caption': '...',                        // contains {0}, {1}
    'pdf.footer.left': '...',                             // contains {0}, {1}
```

Replace each `'...'` placeholder with the canonical FR/EN value from UI-SPEC §7. Excerpts (transcribe exactly, including the U+202F narrow-no-break-space and en-quote conventions established by Phase 6/7 — French uses « guillemets », curly apostrophes `’`, and U+202F before `:` `;` `?` `!`):

**FR block (key → value):**

```
'proposal.search.placeholder' = 'Rechercher par client ou référence...'
'proposal.search.aria'        = 'Rechercher dans vos propositions'
'proposal.search.clear'       = 'Effacer la recherche'
'proposal.search.empty.title' = 'Aucune proposition trouvée'
'proposal.search.empty.body'  = 'Essayez un autre nom de client ou référence.'
'proposal.list.toggle.active' = 'Actives'
'proposal.list.toggle.deleted'= 'Supprimées'
'proposal.list.load.more'     = 'Charger plus'
'proposal.list.load.more.loading' = 'Chargement...'
'proposal.deleted.empty.title'= 'Aucune proposition supprimée récemment'
'proposal.deleted.empty.body' = 'Les propositions supprimées apparaissent ici pendant 30 jours.'

'proposal.chip.active'        = 'Active'
'proposal.chip.expired'       = 'Expirée'
'proposal.chip.deleted'       = 'Supprimée le {0}'
'proposal.chip.language.tooltip' = 'Document généré en français.'
'proposal.chip.tooltip.expires' = "Valable jusqu'au {0}"
'proposal.chip.tooltip.expired' = 'Expirée le {0}'

'proposal.detail.title'       = 'Proposition {0}'
'proposal.detail.created.line'= 'Créée le {0} à {1}'
'proposal.detail.section.inputs' = 'Saisie'
'proposal.detail.section.computed' = 'Calcul'
'proposal.detail.computed.expires.label' = "Valable jusqu'au {0}"
'proposal.detail.computed.expired.label' = 'Expirée le {0}'
'proposal.detail.computed.loyer.suffix' = '/ mois'

'proposal.detail.action.download'  = 'Télécharger le PDF'
'proposal.detail.action.duplicate' = 'Dupliquer'
'proposal.detail.action.delete'    = 'Supprimer'
'proposal.detail.action.restore'   = 'Restaurer'

'proposal.detail.pdf.preview.title' = 'APERÇU PDF'
'proposal.detail.pdf.preview.aria'  = 'Aperçu du document PDF de la proposition'
'proposal.detail.pdf.fallback.link' = 'Voir le PDF dans un nouvel onglet'

'proposal.detail.deleted.banner' = 'Cette proposition est supprimée. Restaurez-la dans les {0} jours restants.'
'proposal.confirm.delete'       = "Supprimer cette proposition ? Vous pourrez la restaurer dans les 30 jours depuis « Voir supprimées »."

'proposal.toast.submit.loading' = 'Génération du PDF en cours...'
'proposal.toast.submit.success' = 'Proposition créée.'
'proposal.toast.submit.error'   = 'Erreur lors de la génération — veuillez réessayer.'
'proposal.toast.delete.success' = 'Proposition supprimée.'
'proposal.toast.delete.action.view.deleted' = 'Voir'
'proposal.toast.delete.error'   = 'Erreur lors de la suppression. Réessayez.'
'proposal.toast.restore.success'= 'Proposition restaurée.'
'proposal.toast.restore.error'  = 'Erreur lors de la restauration. Réessayez.'
'proposal.toast.duplicate.prefilled' = "Champs pré-remplis depuis la proposition d'origine. Modifiez si nécessaire."

'pdf.tagline'                   = 'Location financière IT'
'pdf.title'                     = 'Proposition de location financière'
'pdf.ref.label'                 = 'N°'
'pdf.section.recipient'         = 'DESTINATAIRE'
'pdf.section.project'           = 'PROJET'
'pdf.section.interests'         = "POINTS D'INTÉRÊT IDENTIFIÉS :"
'pdf.project.placeholder'       = 'Projet non précisé'
'pdf.project.ref.prefix'        = 'Réf. partenaire :'
'pdf.computed.coefficient.label'= 'Coefficient appliqué'
'pdf.loyer.label'               = 'LOYER MENSUEL HT'
'pdf.loyer.subtext'             = 'sur {0} mois'
'pdf.loyer.on.demand'           = 'Sur demande'
'pdf.validity.caption'          = "Proposition valable jusqu'au {0} ({1} jours), sous réserve de l'accord du comité des engagements de Leasétic. Document non contractuel."
'pdf.footer.left'               = 'N° {0} · Créée le {1}'
```

**EN block — same keys, English values per UI-SPEC §7:**

```
'proposal.search.placeholder' = 'Search by client or reference...'
'proposal.search.aria'        = 'Search your proposals'
'proposal.search.clear'       = 'Clear search'
'proposal.search.empty.title' = 'No proposals found'
'proposal.search.empty.body'  = 'Try a different client name or reference.'
'proposal.list.toggle.active' = 'Active'
'proposal.list.toggle.deleted'= 'Deleted'
'proposal.list.load.more'     = 'Load more'
'proposal.list.load.more.loading' = 'Loading...'
'proposal.deleted.empty.title'= 'No recently deleted proposals'
'proposal.deleted.empty.body' = 'Deleted proposals appear here for 30 days.'

'proposal.chip.active'        = 'Active'
'proposal.chip.expired'       = 'Expired'
'proposal.chip.deleted'       = 'Deleted on {0}'
'proposal.chip.language.tooltip' = 'Document generated in English.'
'proposal.chip.tooltip.expires' = 'Valid until {0}'
'proposal.chip.tooltip.expired' = 'Expired on {0}'

'proposal.detail.title'       = 'Proposal {0}'
'proposal.detail.created.line'= 'Created on {0} at {1}'
'proposal.detail.section.inputs' = 'Input'
'proposal.detail.section.computed' = 'Computed'
'proposal.detail.computed.expires.label' = 'Valid until {0}'
'proposal.detail.computed.expired.label' = 'Expired on {0}'
'proposal.detail.computed.loyer.suffix' = '/ month'

'proposal.detail.action.download'  = 'Download PDF'
'proposal.detail.action.duplicate' = 'Duplicate'
'proposal.detail.action.delete'    = 'Delete'
'proposal.detail.action.restore'   = 'Restore'

'proposal.detail.pdf.preview.title' = 'PDF PREVIEW'
'proposal.detail.pdf.preview.aria'  = 'PDF preview of the proposal document'
'proposal.detail.pdf.fallback.link' = 'Open PDF in a new tab'

'proposal.detail.deleted.banner' = 'This proposal is deleted. Restore it within the next {0} days.'
'proposal.confirm.delete'       = "Delete this proposal? You can restore it within 30 days from 'Show deleted'."

'proposal.toast.submit.loading' = 'Generating PDF...'
'proposal.toast.submit.success' = 'Proposal created.'
'proposal.toast.submit.error'   = 'Error during generation — please try again.'
'proposal.toast.delete.success' = 'Proposal deleted.'
'proposal.toast.delete.action.view.deleted' = 'View'
'proposal.toast.delete.error'   = 'Could not delete. Please try again.'
'proposal.toast.restore.success'= 'Proposal restored.'
'proposal.toast.restore.error'  = 'Could not restore. Please try again.'
'proposal.toast.duplicate.prefilled' = 'Fields pre-filled from source proposal. Edit if needed.'

'pdf.tagline'                   = 'IT financial leasing'
'pdf.title'                     = 'Financial lease proposal'
'pdf.ref.label'                 = 'No.'
'pdf.section.recipient'         = 'RECIPIENT'
'pdf.section.project'           = 'PROJECT'
'pdf.section.interests'         = 'POINTS OF INTEREST IDENTIFIED:'
'pdf.project.placeholder'       = 'Project not specified'
'pdf.project.ref.prefix'        = 'Partner ref.:'
'pdf.computed.coefficient.label'= 'Applied coefficient'
'pdf.loyer.label'               = 'MONTHLY RENT (EX-VAT)'
'pdf.loyer.subtext'             = 'over {0} months'
'pdf.loyer.on.demand'           = 'On request'
'pdf.validity.caption'          = "Proposal valid until {0} ({1} days), subject to approval by Leasétic's credit committee. Non-contractual document."
'pdf.footer.left'               = 'No. {0} · Created on {1}'
```

After: `npm run typecheck` MUST exit 0. The `_EnHasAllFrKeys` parity-proof type fails-loud if any FR key is missing from EN. `npm run lint:check` MUST exit 0 (pre-existing seed-admins-launch.ts:42 warning OK).

NOTE on French typography:
- Use NARROW NO-BREAK SPACE (U+202F) before `:` only inside the canonical FR copy — UI-SPEC §3 string snapshots already represent these where present. Most Phase 8 FR copy uses regular spaces; do NOT mass-substitute.
- Curly apostrophe `'` (U+2019) in `d'origine`, `l'accord`, `jusqu'au` — UI-SPEC strings already use them; copy verbatim from this file.
- Guillemets `« »` (U+00AB / U+00BB) in `proposal.confirm.delete` — copy verbatim.
- The Phase 7 dictionary already passes ESLint's no-restricted-syntax rule; the new keys add identical-shape entries and inherit the bless.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npm run lint:check && grep -c "proposal.search.placeholder" src/lib/i18n/dictionaries.ts && grep -c "proposal.toast.submit.loading" src/lib/i18n/dictionaries.ts && grep -c "pdf.tagline" src/lib/i18n/dictionaries.ts && grep -c "pdf.validity.caption" src/lib/i18n/dictionaries.ts && grep -c "pdf.footer.left" src/lib/i18n/dictionaries.ts && grep -c "proposal.detail.created.line" src/lib/i18n/dictionaries.ts && grep -c "proposal.detail.deleted.banner" src/lib/i18n/dictionaries.ts && grep -c "proposal.toast.delete.action.view.deleted" src/lib/i18n/dictionaries.ts && [ "$(grep -c "proposal.search.placeholder" src/lib/i18n/dictionaries.ts)" = "2" ] && [ "$(grep -c "pdf.tagline" src/lib/i18n/dictionaries.ts)" = "2" ]</automated>
  </verify>
  <done>
    - 56 new keys × 2 languages added to dictionaries.fr and dictionaries.en
    - Reused v10 keys (proposal.duree.label, proposal.duree.months, proposal.interests.slb, proposal.interests.eval, proposal.montant.label) NOT redeclared (zero new entries for these)
    - Inline header comment marks Phase 8 block boundary in both FR and EN sub-objects
    - npm run typecheck exits 0 (parity proof intact)
    - npm run lint:check exits 0 (no-restricted-syntax rule respected)
    - Each new FR key has a matching EN key (grep -c returns 2 for any new key, one in each block)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend dictionaries.test.ts with Phase 8 spot-check + interpolation contract</name>
  <files>src/lib/i18n/dictionaries.test.ts</files>
  <read_first>
    - `src/lib/i18n/dictionaries.test.ts` (current Phase 7 spot-check pattern with `phase7Keys: DictKey[]`)
    - The new Phase 8 keys committed in Task 1
  </read_first>
  <action>
Mirror Phase 7's parametric pattern. Add a `phase8Keys` array and a parametric `for...of` loop yielding 2 assertions per key (FR non-empty + EN non-empty). Add separate tests for the interpolation contract (keys with `{0}` / `{1}` placeholders).

**Append to the existing `dictionaries.test.ts`** (after the Phase 7 block):

```ts
// ── Phase 8 — Persistence + PDF Pipeline (Plan 08-02) ────────────────────────

// Every Phase 8 key MUST resolve non-empty in BOTH languages. Catches drift
// where someone adds a FR-only key (compile-time _EnHasAllFrKeys catches the
// missing-EN case; this runtime check catches empty-string regressions in
// either language).
const phase8Keys: DictKey[] = [
  // 7.1 List view (11)
  'proposal.search.placeholder',
  'proposal.search.aria',
  'proposal.search.clear',
  'proposal.search.empty.title',
  'proposal.search.empty.body',
  'proposal.list.toggle.active',
  'proposal.list.toggle.deleted',
  'proposal.list.load.more',
  'proposal.list.load.more.loading',
  'proposal.deleted.empty.title',
  'proposal.deleted.empty.body',
  // 7.2 Chips (6)
  'proposal.chip.active',
  'proposal.chip.expired',
  'proposal.chip.deleted',
  'proposal.chip.language.tooltip',
  'proposal.chip.tooltip.expires',
  'proposal.chip.tooltip.expired',
  // 7.3 Detail header + sections (7)
  'proposal.detail.title',
  'proposal.detail.created.line',
  'proposal.detail.section.inputs',
  'proposal.detail.section.computed',
  'proposal.detail.computed.expires.label',
  'proposal.detail.computed.expired.label',
  'proposal.detail.computed.loyer.suffix',
  // 7.4 Action buttons (4)
  'proposal.detail.action.download',
  'proposal.detail.action.duplicate',
  'proposal.detail.action.delete',
  'proposal.detail.action.restore',
  // 7.5 PDF preview UI (3)
  'proposal.detail.pdf.preview.title',
  'proposal.detail.pdf.preview.aria',
  'proposal.detail.pdf.fallback.link',
  // 7.6 Deleted-view banner + confirm (2)
  'proposal.detail.deleted.banner',
  'proposal.confirm.delete',
  // 7.7 Toasts (9)
  'proposal.toast.submit.loading',
  'proposal.toast.submit.success',
  'proposal.toast.submit.error',
  'proposal.toast.delete.success',
  'proposal.toast.delete.action.view.deleted',
  'proposal.toast.delete.error',
  'proposal.toast.restore.success',
  'proposal.toast.restore.error',
  'proposal.toast.duplicate.prefilled',
  // 7.8 PDF document copy (14)
  'pdf.tagline',
  'pdf.title',
  'pdf.ref.label',
  'pdf.section.recipient',
  'pdf.section.project',
  'pdf.section.interests',
  'pdf.project.placeholder',
  'pdf.project.ref.prefix',
  'pdf.computed.coefficient.label',
  'pdf.loyer.label',
  'pdf.loyer.subtext',
  'pdf.loyer.on.demand',
  'pdf.validity.caption',
  'pdf.footer.left',
];

describe('Phase 8 i18n delta', () => {
  for (const key of phase8Keys) {
    it(`fr.${key} resolves non-empty`, () => {
      expect(t(key, 'fr')).not.toBe('');
      expect(t(key, 'fr').length).toBeGreaterThan(0);
    });
    it(`en.${key} resolves non-empty`, () => {
      expect(t(key, 'en')).not.toBe('');
      expect(t(key, 'en').length).toBeGreaterThan(0);
    });
  }

  // Interpolation contract: keys with {0} / {1} placeholders for caller-side
  // .replace() interpolation. The dictionary value MUST contain the placeholder
  // literal so consumers can substitute it. Catches accidental rewrites that
  // drop the placeholder.
  describe('interpolation contract', () => {
    const singleArgKeys: DictKey[] = [
      'proposal.chip.deleted',
      'proposal.chip.tooltip.expires',
      'proposal.chip.tooltip.expired',
      'proposal.detail.title',
      'proposal.detail.computed.expires.label',
      'proposal.detail.computed.expired.label',
      'proposal.detail.deleted.banner',
      'pdf.loyer.subtext',
    ];
    const twoArgKeys: DictKey[] = [
      'proposal.detail.created.line',
      'pdf.validity.caption',
      'pdf.footer.left',
    ];

    for (const key of singleArgKeys) {
      it(`${key} contains {0} in fr + en`, () => {
        expect(t(key, 'fr')).toContain('{0}');
        expect(t(key, 'en')).toContain('{0}');
      });
    }
    for (const key of twoArgKeys) {
      it(`${key} contains {0} and {1} in fr + en`, () => {
        expect(t(key, 'fr')).toContain('{0}');
        expect(t(key, 'fr')).toContain('{1}');
        expect(t(key, 'en')).toContain('{0}');
        expect(t(key, 'en')).toContain('{1}');
      });
    }
  });

  // Sanity: the Phase 8 reuse table — these v10 keys must still exist.
  // Plan 08-05 + 08-10 reference them in place of new Phase 8 aliases.
  describe('reuse table integrity (UI-SPEC §7.9)', () => {
    const reusedKeys: DictKey[] = [
      'proposal.duree.label',
      'proposal.duree.months',
      'proposal.interests.slb',
      'proposal.interests.eval',
      'proposal.montant.label',
    ];
    for (const key of reusedKeys) {
      it(`${key} (v10 reused for Phase 8) still resolves`, () => {
        expect(t(key, 'fr')).not.toBe('');
        expect(t(key, 'en')).not.toBe('');
      });
    }
  });
});
```

After: `npm test` MUST exit 0. The Phase 8 block adds (56 keys × 2 langs) + (8 single-arg × 2 langs) + (3 two-arg × 2 langs in single tests, 4 contains assertions each) + 5 reused-key assertions. Test count grows by ~120.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm test -- --reporter=basic 2>&1 | tail -20 && grep -c "phase8Keys" src/lib/i18n/dictionaries.test.ts && grep -c "Phase 8 i18n delta" src/lib/i18n/dictionaries.test.ts && grep -c "interpolation contract" src/lib/i18n/dictionaries.test.ts && grep -c "reuse table integrity" src/lib/i18n/dictionaries.test.ts</automated>
  </verify>
  <done>
    - phase8Keys array contains all 56 new Phase 8 keys
    - Parametric test loop yields 2 assertions per key (FR + EN non-empty)
    - Single-arg interpolation test covers 8 keys
    - Two-arg interpolation test covers 3 keys (created.line, validity.caption, footer.left)
    - Reuse-table integrity test covers 5 reused v10 keys
    - npm test exits 0; total test count grew by ≥120
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dictionary entry → JSX child | React JSX child auto-escape covers any HTML-special chars in copy. |
| `{0}` / `{1}` placeholders → caller's `.replace()` | Caller is responsible for safe substitution; pre-formatted values from formatCurrency/formatDate are non-user-controlled output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | A future contributor adds a FR-only key, EN falls back to FR via t()'s safety net | mitigate | The `_EnHasAllFrKeys` compile-time type catches the missing-EN case at typecheck. Plan 08-02 preserves this mechanism unchanged. |
| T-08-02-02 | Tampering | Someone removes the `{0}` placeholder from a copy edit, breaking interpolation | mitigate | Task 2 interpolation-contract tests assert the placeholder text is present. CI catches the drift on the offending PR. |
| T-08-02-03 | XSS | A copy edit injects HTML into a value | accept | React JSX child-escape covers it; ESLint no-restricted-syntax rule prevents raw-HTML injection sinks; consumers always use plain JSX text. |
| T-08-02-04 | Tampering | A future contributor accidentally drops a reused v10 key (e.g., proposal.montant.label removed by a refactor) | mitigate | Task 2's `reuse table integrity` tests fail when the v10 keys vanish. Plan 08-05 + 08-10's TypeScript imports also fail-loud (DictKey union narrows). |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (parity proof intact)
- `npm run lint:check` exits 0
- `npm test` — total test count grew by ≥120 (56 keys × 2 + interpolation + reuse-integrity); all pass
- `grep -c "fr:\\|en:" src/lib/i18n/dictionaries.ts` still returns 1 each (one fr block + one en block)
- Manual spot-check: open dictionaries.ts in editor, scroll to Phase 8 block; FR + EN values visually match UI-SPEC §7
</verification>

<success_criteria>
- 56 NEW keys × 2 langs added to dictionaries.ts
- Reused v10 keys NOT redeclared (5 keys per UI-SPEC §7.9 reuse table)
- TypeScript parity proof catches missing-EN at compile time
- Test count grows by ≥120 with no failures
- Phase 8 consumer plans (08-05/08-09/08-10/08-11/08-12/08-13) can lint-clean reference these keys via t()
- PROP-04 / PROP-26 partial grounding: copy strings exist for the empty-state + validity caption surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/08-persistence-pdf-pipeline/08-02-SUMMARY.md` capturing:
- Test count before / after (227 expected before; 227 + ~120 after)
- Final FR + EN key totals (263 + 56 = 319 each, minus any reuse adjustments)
- List of reused v10 keys (the 5 from §7.9)
- Any keys whose canonical value differed between UI-SPEC and existing v10 dict (none expected; document if you found one)
</output>
