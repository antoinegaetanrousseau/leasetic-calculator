---
phase: 10-cutover-polish
plan: 06
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05]
files_modified:
  - docs/operations/deploy-ovh.md
  - docs/operations/launch-checklist.md
  - docs/legal/privacy-coverage-confirmation.md
  - .planning/PROJECT.md
  - scripts/check-no-v10-localstorage.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [CUT-01, CUT-02, CUT-03, CUT-07, CUT-08]
must_haves:
  truths:
    - "docs/operations/deploy-ovh.md describes the generic OVH deploy lifecycle (Provisioning → Configuration → Build → Migration → Smoke → Cron swap-out → Rollback) with provider-neutral commands (D-10-01/04 — generic shape until September)"
    - "docs/operations/launch-checklist.md lists the 9 ordered pre-launch steps (D-10-19) with GitHub-flavored checkboxes, and includes the CUT-02-vacuous one-liner"
    - "docs/legal/privacy-coverage-confirmation.md is a date-stamped stub holding the pending Thomas-question + empty Response section (D-10-18)"
    - "scripts/check-no-v10-localstorage.sh greps app/ + src/ ONLY (excludes .planning/, drizzle/, node_modules/, .next/, dist/) for the 5 v10 lt_* keys; CI grep gate fires on regression (CUT-03 / D-10-16)"
    - "CI workflow runs the new grep gate as a defense-in-depth step; an accidental v10 localStorage import fails CI"
    - "PROJECT.md carries a 1-line CUT-02 cross-reference pointing at launch-checklist.md (D-10-15)"
    - "Runbook tone is Antoine-context (technical voice; no 'the operator'; assumes shell + repo access; cites D-10 decisions inline) per D-10-21"
  artifacts:
    - path: docs/operations/deploy-ovh.md
      provides: "OVH deploy runbook (Antoine-context, generic shape — provider TBD)"
      contains: "smoke:ovh"
    - path: docs/operations/launch-checklist.md
      provides: "9-step pre-launch checklist with GFM checkboxes + CUT-02 vacuous note"
      contains: "purge:test-data"
    - path: docs/legal/privacy-coverage-confirmation.md
      provides: "Date-stamped paper-trail stub for Thomas's privacy-coverage reply"
      contains: "Thomas"
    - path: scripts/check-no-v10-localstorage.sh
      provides: "CI grep gate enforcing the no-v10-localstorage rule (CUT-03)"
      contains: "lt_pw"
    - path: .github/workflows/ci.yml
      provides: "CI step running the v10-localstorage grep gate"
      contains: "check:no-v10-localstorage"
  key_links:
    - from: docs/operations/deploy-ovh.md
      to: scripts/smoke-ovh.ts
      via: "runbook references npm run smoke:ovh as the portability proof"
      pattern: "smoke:ovh"
    - from: docs/operations/launch-checklist.md
      to: scripts/purge-test-data.ts
      via: "step 8 references npm run purge:test-data for post-pilot scrub"
      pattern: "purge:test-data"
    - from: docs/operations/launch-checklist.md
      to: docs/legal/privacy-coverage-confirmation.md
      via: "step 5 references the paper-trail file"
      pattern: "privacy-coverage-confirmation"
    - from: .github/workflows/ci.yml
      to: scripts/check-no-v10-localstorage.sh
      via: "npm run check:no-v10-localstorage"
      pattern: "check:no-v10-localstorage"
---

<objective>
Operational documentation + launch artifacts + the CUT-03 CI grep gate. Closes the remaining CUT requirements (CUT-01, CUT-02, CUT-03, CUT-07, CUT-08) and ties the previous plans (10-02..05) into a runnable launch operation. The runbook (deploy-ovh.md) is the document Antoine reads in September; the launch-checklist (9 ordered steps per D-10-19) is the document Antoine ticks off on launch day; the privacy-coverage stub holds Thomas's reply for the audit trail; the grep gate prevents future regressions on the v10 localStorage retirement (CUT-03).

Purpose: produce the operational paper trail v1.1 needs to launch defensibly. Phase 10 ships the *capability* + the *artifacts* — September runs the *proof*.
Output: 4 new docs (deploy-ovh.md ~280 lines, launch-checklist.md ~120 lines, privacy-coverage-confirmation.md ~40 lines, check-no-v10-localstorage.sh ~50 lines), 2 modified files (PROJECT.md +2 lines for CUT-02 cross-reference, ci.yml +3 lines for the new step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cutover-polish/10-CONTEXT.md
@.planning/phases/10-cutover-polish/10-PATTERNS.md
@.planning/phases/10-cutover-polish/10-01-SUMMARY.md
@.planning/phases/10-cutover-polish/10-02-SUMMARY.md
@.planning/phases/10-cutover-polish/10-03-SUMMARY.md
@.planning/phases/10-cutover-polish/10-04-SUMMARY.md
@.planning/phases/10-cutover-polish/10-05-SUMMARY.md
@docs/operations/migrations.md
@docs/operations/purge.md
@scripts/check-no-vercel-only-imports.sh
@.github/workflows/ci.yml

<interfaces>
<!-- Existing artifacts referenced or extended by this plan. -->

The closest analog for the new bash script is `scripts/check-no-vercel-only-imports.sh` — same shebang, `set -euo pipefail`, hardcoded PATTERNS array, narrow SEARCH_PATHS, fail counter, exit 1 on dirty / 0 on clean.

The closest analog for the OVH runbook is `docs/operations/migrations.md` — title + one-line scope, `## Locked rules`, lifecycle ASCII diagram, numbered workflow sections.

The closest analog for the launch-checklist is `docs/operations/purge.md` (workflow numbered-step pattern at lines 34-68) but with GitHub-flavored `- [ ]` checkboxes per D-10-19.

CI workflow insertion point: AFTER `check:seed-sql` step (line 50 area), BEFORE `Vitest unit tests` step.

The five v10 localStorage keys (per D-10-16):
- `lt_pw` — v10 password storage
- `lt_coeffs` — v10 coefficient cache
- `lt_commission` — v10 commission rate
- `lt_max` — v10 max threshold
- `lt_partner` — v10 partner-id storage

Confirmed present in v10 source HTML at lines 733, 734, 737. NOT present in v1.1 src/ or app/ — the grep gate is a regression preventer.

PROJECT.md location for CUT-02 cross-reference: locate the v10 distribution paragraph (search for "v10" + "emailed" or "HTML" or "distributed") and place the 1-line note immediately after; OR add a "Cutover notes" section near the bottom if no clean placement exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-no-v10-localstorage.sh + wire into ci.yml</name>
  <files>scripts/check-no-v10-localstorage.sh, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/check-no-vercel-only-imports.sh (closest analog — copy structure, swap PATTERNS array, swap exclude-dir flags, swap error wording)
    - .github/workflows/ci.yml (the file being modified — current grep gate steps at lines 39-50; insertion AFTER check:seed-sql, BEFORE Vitest)
    - .planning/phases/10-cutover-polish/10-PATTERNS.md (lines 280-330 for exact bash structure)
    - .planning/phases/10-cutover-polish/10-CONTEXT.md (D-10-16 — the 5 v10 keys; CUT-03 — clean-slate localStorage)
    - 10-01-SUMMARY.md (confirm npm script `check:no-v10-localstorage` was registered in package.json)
  </read_first>
  <action>
**Edit 1 — Create `scripts/check-no-v10-localstorage.sh`** (NEW FILE).

The script structure mirrors `scripts/check-no-vercel-only-imports.sh` exactly — same shebang, same `set -euo pipefail`, same `cd` to repo root, hardcoded PATTERNS array of the 5 v10 keys, SEARCH_PATHS narrowed to `("src" "app")`, fail counter, exit 1 on dirty / 0 on clean. Use grep -F (fixed strings) — no regex semantics needed for `lt_pw` etc.

Required content of the script (executor writes verbatim):

1. Shebang: `#!/usr/bin/env bash`
2. Header comment block explaining the rule (CUT-03 / D-10-16), what's INCLUDED in scope (`src/`, `app/`), and what's EXCLUDED (`.planning/`, `drizzle/`, `node_modules/`, `.next/`, `dist/`, `docs/`).
3. `set -euo pipefail`
4. `cd "$(dirname "$0")/.."` (anchor to repo root)
5. `PATTERNS=("lt_pw" "lt_coeffs" "lt_commission" "lt_max" "lt_partner")` — the 5 v10 localStorage keys.
6. `SEARCH_PATHS=("src" "app")` — narrow to where regression risk lives.
7. `fail=0`
8. For-loop over PATTERNS: `grep -rEnF --include='*.ts' --include='*.tsx' --include='*.js' --include='*.mjs' --include='*.cjs' --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=drizzle --exclude-dir=dist <pattern> <SEARCH_PATHS> 2>/dev/null || true`. If matches non-empty, echo ERROR + matches + set `fail=1`.
9. Final block: `if [ $fail -ne 0 ]; then echo "FAILED: CUT-03 no-v10-localstorage rule violated."; echo helpful guidance; exit 1; fi`
10. `echo "OK: no v10 localStorage keys found in src/ or app/."` and `exit 0`.

After writing the file, set the executable bit: run `chmod +x scripts/check-no-v10-localstorage.sh`. Git tracks the executable bit so this carries through.

**Edit 2 — Modify `.github/workflows/ci.yml`:** Insert the new step AFTER the existing `Defense-in-depth -- seed SQL in sync with seedParams (D-D1)` step (line 50 area), BEFORE the `Vitest unit tests` step. The insertion mirrors the existing grep-gate step formatting verbatim — same indentation, same name pattern.

Add this exact block (preserve YAML indentation = 6 spaces before the dash):

```yaml
      - name: Defense-in-depth grep -- no v10 localStorage keys (CUT-03)
        run: npm run check:no-v10-localstorage
```

**Critical invariants:**
- Hardcoded `PATTERNS=(lt_pw lt_coeffs lt_commission lt_max lt_partner)` — no operator input feeds the grep.
- `SEARCH_PATHS=(src app)` is INTENTIONALLY narrow — does NOT scan `.planning/` or `docs/` because those legitimately cite the v10 key names (false positives).
- The script's exit code (0 clean, 1 dirty) is the entire CI signal — no warnings, no soft failures.
- The CI step uses `npm run check:no-v10-localstorage` which (per 10-01) resolves to `bash scripts/check-no-v10-localstorage.sh` — keeps the bash invocation behind a single npm-script alias for consistency with the existing grep-gate steps.
- The script must be executable; otherwise CI's `bash scripts/...` invocation works but a bare invocation `./scripts/...` would not. Setting +x is operator-friendly.
  </action>
  <verify>
    <automated>test -x scripts/check-no-v10-localstorage.sh &amp;&amp; bash scripts/check-no-v10-localstorage.sh &amp;&amp; grep -cE 'lt_(pw|coeffs|commission|max|partner)' scripts/check-no-v10-localstorage.sh | grep -qE '^[5-9]' &amp;&amp; grep -c 'check:no-v10-localstorage' .github/workflows/ci.yml | grep -q '^1$' &amp;&amp; npm run check:no-v10-localstorage</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/check-no-v10-localstorage.sh` exists with shebang `#!/usr/bin/env bash` on line 1
    - File is executable: `test -x scripts/check-no-v10-localstorage.sh` exits 0 (`ls -l` shows the executable bit)
    - All 5 v10 keys present as bash array elements: `grep -cE '^\s*"lt_(pw|coeffs|commission|max|partner)"$' scripts/check-no-v10-localstorage.sh` returns exactly 5
    - Excludes `.planning/` and `docs/` and `drizzle/` from search paths via SEARCH_PATHS=("src" "app") narrowing: `grep -E 'SEARCH_PATHS=\("src" "app"\)' scripts/check-no-v10-localstorage.sh | wc -l` returns 1
    - Has `set -euo pipefail` near the top: `grep -c "set -euo pipefail" scripts/check-no-v10-localstorage.sh` returns 1
    - Exits 1 on dirty state, 0 on clean state — verified by running `bash scripts/check-no-v10-localstorage.sh` against the current clean tree (must exit 0)
    - `grep -c "check:no-v10-localstorage" .github/workflows/ci.yml` returns 1
    - The new ci.yml step appears AFTER the `check:seed-sql` step: `awk '/check:seed-sql/{a=NR} /check:no-v10-localstorage/{b=NR; if(a &amp;&amp; a&lt;b){exit 0}} END{exit 1}' .github/workflows/ci.yml` exits 0
    - The new ci.yml step appears BEFORE the `Vitest unit tests` step: `awk '/check:no-v10-localstorage/{a=NR} /Vitest unit tests/{b=NR; if(a &amp;&amp; a&lt;b){exit 0}} END{exit 1}' .github/workflows/ci.yml` exits 0
    - `npm run check:no-v10-localstorage` exits 0 (the script's npm wrapper resolves; the gate is currently clean)
  </acceptance_criteria>
  <done>scripts/check-no-v10-localstorage.sh exists, executable, hardcodes the 5 v10 keys, scopes search to src/ + app/ only; ci.yml runs it as a defense-in-depth step; gate currently passes (no v10 keys in v1.1 codebase).</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/operations/deploy-ovh.md (OVH deploy runbook, generic shape)</name>
  <files>docs/operations/deploy-ovh.md</files>
  <read_first>
    - docs/operations/migrations.md (closest analog — section ordering, lifecycle-diagram pattern, "Locked rules" section, Antoine-context tone)
    - docs/operations/purge.md (failure-modes table format at lines 88-95)
    - .planning/phases/10-cutover-polish/10-PATTERNS.md (lines 36-66 for runbook structure)
    - .planning/phases/10-cutover-polish/10-CONTEXT.md (D-10-01..04 — full scope; D-10-21 — Antoine-context tone; specifics section's "OVH runbook structure" — the section list to mirror)
    - 10-01-SUMMARY.md (confirm .env.example documents PURGE_CRON_SECRET + NEXT_PUBLIC_PRIVACY_URL_*; this runbook references those by name)
    - 10-02-SUMMARY.md (cron route + vercel.json swap-out path)
    - 10-05-SUMMARY.md (smoke script confirmed runnable today; references npm run smoke:ovh by exact command)
  </read_first>
  <action>
Create new file `docs/operations/deploy-ovh.md`. Follow `docs/operations/migrations.md` structurally: title + one-line scope + `## Locked rules` + lifecycle-diagram + numbered workflow sections + failure-modes table.

**Tone:** Antoine-context per D-10-21 — technical voice; says "you" or "Antoine" (Antoine is both author and sole reader); cites D-10 decisions inline; assumes shell + repo access; no "the operator" generic prose.

**Required sections (in this order):**

1. **H1 title:** `# OVH Deployment Runbook — Phase 10 / September 2026 Target` + 1-paragraph scope explaining v1.1 ships on Vercel; September lands the same git ref on OVH; the portability invariant holds via env-var-only swap.

2. **`## Locked rules`** (4 numbered rules):
   - Adapter discipline (BOOT-05) — `lib/storage` + `lib/db` already abstract over hosting primitive; OVH cutover swaps env vars only; CI grep gate prevents `@vercel/*` regression.
   - No automated rollback — forward-only; same as DB migrations.
   - Smoke script is the proof — `npm run smoke:ovh` (D-10-02 / D-10-03) is the FIRST thing run against OVH-target post-provisioning, before announcing.
   - OVH-side cron replaces Vercel Cron — `vercel.json`'s twice-monthly schedule (D-10-05/06) doesn't apply on OVH; wire same `/api/internal/purge-soft-deleted` endpoint to OVH-side scheduler with same `Authorization: Bearer ${PURGE_CRON_SECRET}` header.

3. **Lifecycle ASCII diagram** mirroring `docs/operations/migrations.md` lines 18-36 — 3 columns (today / September provisioning / cutover day) with vertical pipe + horizontal dash + arrow steps.

4. **`## Prerequisites`** — bulleted list: Open Q5 resolved, S3-compat blob provider chosen, domain + TLS plan, AUTH_SECRET / PURGE_CRON_SECRET / ADMIN_URL_SEGMENT generated, Privacy URLs finalized with Thomas (D-10-17/18).

5. **`## Provisioning`** — 3 sub-sections (Postgres / Blob storage / Node host). For each, list 3 candidate providers (OVH Managed PG, Scaleway, self-host) with placeholder commands `(TBD; provider-specific command before September)` per D-10-04.

6. **`## Configuration`** — fenced bash block listing every env var with comments. Group: `# === Core ===`, `# === DB (BOOT-09) ===`, `# === Blob storage (BOOT-04 / BOOT-05) ===`, `# === Auth (Phase 6) ===`, `# === Phase 10 (D-10-05/17) ===`. Cross-reference `.env.example` as the canonical inventory.

7. **`## Build & Deploy`** — fenced bash with `git clone`, `git checkout v1.1.<release-tag>`, `npm ci`, `npm run build`, `node .next/standalone/server.js`. Note: reverse-proxy nginx → localhost:3000.

8. **`## Migration Application`** — `DATABASE_URL=<ovh-postgres-url> npm run db:migrate -- --confirm "MIGRATE PROD"` from local workstation. Same files as Vercel/Neon. Subsequent migrations via `gh workflow run db-migrate.yml` re-pointed at OVH OR local invocation.

9. **`## Smoke Test`** — exact bash to run `scripts/smoke-ovh.ts` against the new OVH URL with `APP_URL=` + `ADMIN_EMAIL=` + `ADMIN_PASSWORD=` + `CONFIRM=SMOKE-OVH`. Expected output: 7 [ok] lines + `OVH-portability: PROVEN`. PDF SHA-256 drift = catastrophic, BLOCKS cutover.

10. **`## Cron Setup (replaces Vercel Cron)`** — at LEAST 2 swap-out options:
    - Option A: cron job — `crontab -e` line: `0 3 1,15 * * curl -s -X POST -H "Authorization: Bearer $PURGE_CRON_SECRET" https://matrice.leasetic.fr/api/internal/purge-soft-deleted >> /var/log/leasetic-purge.log 2>&amp;1`
    - Option B: systemd timer — `.service` + `.timer` units with `OnCalendar=*-*-1,15 03:00:00 UTC`.
    - Option C: OVH Cloud Scheduler — TBD.

11. **`## Rollback`** — explicit forward-only stance; DNS revert; OVH and Vercel/Neon are SEPARATE databases; mitigation strategy = don't cut over until smoke + 1-week pilot.

12. **`## Smoke Test Failure Diagnosis`** — markdown failure-modes table mirroring `docs/operations/purge.md` lines 88-95: `| Mode | Symptom | Recovery |` with at least 4-6 rows covering healthz fail, sign-in fail, create fail, SHA-256 drift, session/auth issues, ECONNREFUSED.

13. **`## Final pre-cutover checklist`** — 6 GFM checkbox items: OVH DB migrations applied, blob bucket policies, smoke 7/7, cron timer + manual fire-test, DNS TTL reduced 24h before, Antoine login + admin coefficients verification on OVH side. Cross-reference `docs/operations/launch-checklist.md` for the full operator checklist.

14. **Footer line:** `*Runbook last updated: 2026-05-10. Phase 10 ships the capability; September 2026 runs the proof. — Antoine*`

**Notes for the executor:**
- Length target: ~280 lines (acceptable range 200–400 lines).
- Antoine-context tone: do not apologize, do not over-explain, do not use "the operator." Direct technical voice. Cite D-10 decisions inline.
- The lifecycle ASCII diagram mirrors `docs/operations/migrations.md` lines 18-36's pattern (no fancy unicode).
- Specific OVH CLI invocations are TBD — write them as `(TBD; provider-specific command before September)`. The runbook is the *frame*; September fills in the blanks.
- Cross-reference `scripts/smoke-ovh.ts` (Plan 10-05) by exact command (`npm run smoke:ovh`) so the runbook stays in sync if the script is renamed.
- Cross-reference `docs/operations/launch-checklist.md` (Task 3 below) by name — they are sister documents.
  </action>
  <verify>
    <automated>test -f docs/operations/deploy-ovh.md &amp;&amp; grep -c "smoke:ovh" docs/operations/deploy-ovh.md | grep -qE '^[2-9]' &amp;&amp; grep -c "PURGE_CRON_SECRET" docs/operations/deploy-ovh.md | grep -qE '^[2-9]' &amp;&amp; grep -c "STORAGE_DRIVER=s3" docs/operations/deploy-ovh.md | grep -qE '^[1-9]' &amp;&amp; grep -c "Locked rules" docs/operations/deploy-ovh.md | grep -q '^1$' &amp;&amp; grep -c "Smoke Test" docs/operations/deploy-ovh.md | grep -qE '^[1-9]' &amp;&amp; grep -c "Cron Setup" docs/operations/deploy-ovh.md | grep -q '^1$' &amp;&amp; grep -c "Rollback" docs/operations/deploy-ovh.md | grep -qE '^[1-9]' &amp;&amp; wc -l docs/operations/deploy-ovh.md | awk '{exit ($1&lt;150||$1&gt;500)?1:0}' &amp;&amp; ! grep -q "the operator" docs/operations/deploy-ovh.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/deploy-ovh.md` exists
    - Has H1 title containing both "OVH" and "Deploy" or "Deployment"
    - Has `## Locked rules` section: `grep -c "^## Locked rules" docs/operations/deploy-ovh.md` returns 1
    - Has all required sections: `grep -cE "^## (Provisioning|Configuration|Build|Migration|Smoke|Cron|Rollback)" docs/operations/deploy-ovh.md` returns >= 6 (allowing minor wording variation)
    - Mentions `npm run smoke:ovh` at least twice (rules + Smoke Test section): `grep -c "smoke:ovh" docs/operations/deploy-ovh.md` returns >= 2
    - References PURGE_CRON_SECRET at least twice: `grep -c "PURGE_CRON_SECRET" docs/operations/deploy-ovh.md` returns >= 2
    - References STORAGE_DRIVER=s3: `grep -c "STORAGE_DRIVER=s3" docs/operations/deploy-ovh.md` returns >= 1
    - References both OVH and a provider alternative (Scaleway, self-host): `grep -ciE "scaleway|self-host|self host" docs/operations/deploy-ovh.md` returns >= 1
    - Cron section presents at least 2 swap-out options (cron job + systemd timer at minimum): `grep -ciE "(option a|option b|crontab|systemd)" docs/operations/deploy-ovh.md` returns >= 3
    - Has a failure-modes markdown table (pipe-separated): `grep -cE "^\\| .* \\| .* \\| .* \\|" docs/operations/deploy-ovh.md` returns >= 4 (header + separator + at least 2 rows)
    - Length: 150 ≤ wc -l ≤ 500 (target range)
    - Antoine-context tone check: `grep -c "the operator" docs/operations/deploy-ovh.md` returns 0 (per D-10-21)
  </acceptance_criteria>
  <done>docs/operations/deploy-ovh.md exists with the full Provisioning → Configuration → Build → Migration → Smoke → Cron swap-out → Rollback → Failure modes structure; Antoine-context tone; cross-references to smoke-ovh.ts + launch-checklist.md; all required env vars documented.</done>
</task>

<task type="auto">
  <name>Task 3: Create launch-checklist.md (9 ordered steps + CUT-02 vacuous note) + privacy-coverage stub + PROJECT.md update</name>
  <files>docs/operations/launch-checklist.md, docs/legal/privacy-coverage-confirmation.md, .planning/PROJECT.md</files>
  <read_first>
    - docs/operations/migrations.md (numbered-step pattern at "How to apply a migration" section)
    - docs/operations/purge.md (workflow numbered-step pattern at lines 34-68)
    - .planning/phases/10-cutover-polish/10-PATTERNS.md (lines 70-94 for launch-checklist structure; lines 99-108 for privacy-coverage stub)
    - .planning/phases/10-cutover-polish/10-CONTEXT.md (D-10-15 — CUT-02 vacuous; D-10-18 — Thomas-question text; D-10-19 — exact 9-step list; D-10-20 — phased pilot; D-10-21 — Antoine voice)
    - 10-03-SUMMARY.md (confirm npm run purge:test-data works; checklist step 8 references it)
    - 10-04-SUMMARY.md (confirm SeedBanner ships; checklist step 3 references "Vérifier les coefficients" banner)
    - .planning/PROJECT.md (locate the v10 distribution paragraph for the CUT-02 cross-reference placement)
  </read_first>
  <action>
**Edit 1 — Create `docs/operations/launch-checklist.md`** (NEW FILE).

Required structure:

1. H1 title: `# Launch Checklist — v1.1 Pre-launch + Pilot + Batch-onboard`
2. Scope paragraph: 1-3 sentences explaining D-10-19 — Antoine ticks each box; the file is committed in its checked state on launch day as an audit artifact.
3. **`## Pre-launch`** section with steps 1-6 (GFM checkbox + bold step number + body):
   - **Step 1.** Apply migrations 0002 + 0003 to production. Cite the gh workflow command (`gh workflow run db-migrate.yml --field confirm="MIGRATE PROD"`) + verify command (`psql $DATABASE_URL_PROD -c "\dt"`).
   - **Step 2.** Verify production `/healthz` returns `{db: ok, blob: ok}`. Include the curl command + recovery hints.
   - **Step 3.** Admin login → "Vérifier les coefficients" banner check → customize. Reference D-10-13/14. Verify via psql showing 2 global_params rows.
   - **Step 4.** Seed 2-3 pilot partners via the regular invitation flow (NOT scripts/seed-partner-launch.ts which is for @test.leasetic.com only — D-10-10).
   - **Step 5.** Confirm Thomas's privacy-coverage signoff at `docs/legal/privacy-coverage-confirmation.md`. Response section MUST be filled. Open Q3 escalation halts here.
   - **Step 6.** Send pilot partner comms — Antoine drafts + sends per D-10-21; Thomas reviews tone if needed.
4. **`## Pilot observation (1-2 weeks)`** with step 7:
   - **Step 7.** Daily spot-checks — `/healthz`, Vercel logs, audit_log row counts, soft-delete activity. v10 file remains accessible during pilot (D-10-20).
5. **`## Batch-onboard`** with steps 8-9:
   - **Step 8.** Run `CONFIRM=PURGE-TEST-DATA npm run purge:test-data` to scrub test users. Predicate cites D-10-09; cascade is documented in 10-03.
   - **Step 9.** Send batch-onboard email to remaining channel partners (Antoine).
6. **`## Notes &amp; vacuous resolutions`** with 3 sub-bullets:
   - **CUT-02 — Resolved vacuously.** v10 was never deployed at a hosted URL. Per D-10-15.
   - **CUT-01 — Communicated, not technically enforced.** Retirement carried by partner emails (steps 6 + 9).
   - **CUT-03 — Defended by CI.** No localStorage migration; `scripts/check-no-v10-localstorage.sh` runs in CI on every PR.
7. Footer: `*Last updated: 2026-05-10 (Phase 10 plan 10-06). Antoine commits this file in its checked state on launch day.*`

**Format requirements:**
- All 9 steps use GitHub-flavored `- [ ]` checkboxes per D-10-19.
- Step numbers in the body use `**1.**`, `**2.**`, ..., `**9.**` (bold + period) so the regex `^- \[ \] \*\*[1-9]\.\*\*` matches each step.
- Each step body is a short paragraph with the exact command + expected outcome (mirror docs/operations/purge.md §Workflow style).
- Antoine-context tone — no "the operator."

**Edit 2 — Create `docs/legal/privacy-coverage-confirmation.md`** (NEW FILE; create the parent `docs/legal/` directory if it doesn't already exist).

Required structure (planner-authored from scratch per D-10-18 — no analog):

1. H1 title: `# Privacy Coverage Confirmation — v1.1 Launch`
2. Header metadata (markdown bold):
   - **Created:** 2026-05-10
   - **Status:** Pending Thomas's reply (Open Q3)
3. **`## Question on record (D-10-18)`** section quoting Antoine's email to Thomas verbatim. The blockquote text is the exact wording from D-10-18:
   > Thomas — quick legal check before we onboard pilot partners on v1.1: does Leasétic's existing privacy notice cover (a) hosting partner data on Vercel/Neon EU regions and (b) 10-year PDF retention as a new processing activity tied to commercial-document compliance? If yes, please reply confirming. If your read is "needs counsel review," flag and we'll route to legal.
4. **`## Response`** section with placeholder fields:
   - **Date:** _pending_
   - **Confirmation status:** _pending_
   - **Thomas's verbatim reply:** > _pending_
5. **`## Resolution`** section with 2 GFM unchecked checkboxes:
   - ☐ Confirmed by Thomas — proceed with launch.
   - ☐ Counsel review required — Open Q3 escalates; launch path adjusts.
6. Footer line citing D-10-17 / D-10-18 + a planner's note about reviewing for sensitive context before commit.

**Edit 3 — Modify `.planning/PROJECT.md`** (1-2 line cross-reference).

Locate the existing v10 distribution paragraph (search for `v10` near `emailed`, `HTML`, or `distributed`/`distribution`). After that paragraph (or at the end of the relevant subsection), insert this exact line:

```markdown
**CUT-02 / D-10-15 (cutover note):** v10 was never hosted at a URL — there is no v10 hosted URL to redirect from. CUT-02's "redirect" requirement is satisfied vacuously; documented in `docs/operations/launch-checklist.md`.
```

If the v10 distribution paragraph cannot be located, append a new section near the bottom of PROJECT.md:

```markdown

## Cutover notes (Phase 10 / D-10-15)

**CUT-02 / D-10-15:** v10 was never hosted at a URL — there is no v10 hosted URL to redirect from. CUT-02's "redirect" requirement is satisfied vacuously; documented in `docs/operations/launch-checklist.md`.
```

Either placement satisfies the requirement that PROJECT.md carries a discoverable CUT-02 cross-reference.

**Critical invariants:**
- Launch-checklist uses GitHub-flavored `- [ ]` checkboxes per D-10-19; Antoine ticks them on launch day.
- All 9 steps explicitly numbered (1..9) per D-10-19; the CUT-02/01/03 notes appear AFTER step 9 as resolution context, not as additional steps.
- Privacy-coverage stub is committed BEFORE Thomas replies — the stub is the document Antoine fills in once Thomas writes back. Filename + structure committed today; content updated when reply arrives.
- The Antoine-context tone (D-10-21) carries through both files: technical voice, no "the operator," cite D-10 decisions inline.
- Step 5 explicitly handles the Open Q3 escalation path (legal counsel review) per D-10-18 — the checklist halts at step 5 if Thomas escalates.
  </action>
  <verify>
    <automated>test -f docs/operations/launch-checklist.md &amp;&amp; test -f docs/legal/privacy-coverage-confirmation.md &amp;&amp; grep -cE '^- \[ \] \*\*[1-9]\.\*\*' docs/operations/launch-checklist.md | grep -q '^9$' &amp;&amp; grep -c 'purge:test-data' docs/operations/launch-checklist.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'CUT-02' docs/operations/launch-checklist.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'Thomas' docs/legal/privacy-coverage-confirmation.md | grep -qE '^[2-9]' &amp;&amp; grep -cE 'D-10-15|CUT-02' .planning/PROJECT.md | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/launch-checklist.md` exists
    - All 9 numbered checklist items present: `grep -cE '^- \[ \] \*\*[1-9]\.\*\*' docs/operations/launch-checklist.md` returns exactly 9
    - References `purge:test-data` (step 8): `grep -c 'purge:test-data' docs/operations/launch-checklist.md` returns >= 1
    - References "Vérifier les coefficients" or the SeedBanner (step 3): `grep -ciE "vérifier les coefficients|seed.banner" docs/operations/launch-checklist.md` returns >= 1
    - Carries the CUT-02 vacuous note: `grep -c 'CUT-02' docs/operations/launch-checklist.md` returns >= 1
    - Carries the CUT-03 reference: `grep -c 'CUT-03' docs/operations/launch-checklist.md` returns >= 1
    - References `docs/legal/privacy-coverage-confirmation.md`: `grep -c 'privacy-coverage-confirmation' docs/operations/launch-checklist.md` returns >= 1
    - File `docs/legal/privacy-coverage-confirmation.md` exists
    - Quotes Thomas verbatim (D-10-18 text): `grep -c 'needs counsel review' docs/legal/privacy-coverage-confirmation.md` returns >= 1
    - Has both `## Question on record` and `## Response` sections: `grep -cE '^## (Question on record|Response|Resolution)' docs/legal/privacy-coverage-confirmation.md` returns >= 3
    - References Thomas at least twice (header + email quote): `grep -c 'Thomas' docs/legal/privacy-coverage-confirmation.md` returns >= 2
    - PROJECT.md carries the CUT-02 cross-reference: `grep -cE 'D-10-15|CUT-02.*vacuous|vacuous.*CUT-02' .planning/PROJECT.md` returns >= 1
    - The launch-checklist Antoine-context tone check: `grep -c 'the operator' docs/operations/launch-checklist.md` returns 0
  </acceptance_criteria>
  <done>launch-checklist.md exists with 9 GFM-checkbox steps + CUT-02/01/03 vacuous-resolution notes; privacy-coverage stub holds Thomas's question verbatim with empty Response/Resolution sections; PROJECT.md carries a 1-line CUT-02 cross-reference pointing at the launch-checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → CI grep gate | The grep gate trusts its hardcoded PATTERNS; runs on every PR; no operator input feeds the search |
| Repo → operator workstation | Runbook + checklist are read-only documents; operator follows them; trust is placed in the operator (Antoine) |
| Privacy-coverage doc → audit trail | Document committed to git carries Thomas's reply for audit forensics; sensitive-content review before commit is the operator's responsibility |
| PROJECT.md → future readers | A 1-line note serves as a discoverability anchor; no enforcement, just signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | grep gate false positive — `.planning/` or `docs/` directory containing v10 key names triggers a CI fail | mitigate | SEARCH_PATHS=(src app) intentionally narrow; --exclude-dir flags exclude node_modules, .next, drizzle, dist. The script's scope is "production code only" — false-positive surface eliminated by construction |
| T-10-06-02 | Tampering | grep gate false negative — a v10 key obfuscated as `lt_` + `pw` string concatenation slips through | accept | The grep gate is defense-in-depth (CUT-03 already satisfied by Phase 6's clean-slate flow). Bypassing requires intentional obfuscation, which is operator-context anyway. ESLint and code review remain primary controls |
| T-10-06-03 | Information disclosure | privacy-coverage doc commits Thomas's email content (potentially including Leasétic-internal context) to the repo | mitigate | Stub is committed empty BEFORE reply arrives. When Thomas replies, the planner's note in the file ("review before commit") prompts Antoine to redact sensitive context before the next commit. Worst case: doc lives in `.gitignore` or `docs/legal/` excluded from public mirroring |
| T-10-06-04 | Tampering | runbook references env vars that don't exist | mitigate | Runbook env-var list cross-references `.env.example` (the canonical inventory). 10-01 added the 3 new vars to `.env.example`; runbook lists exactly those + existing Phase 5/6 vars. Deviation between runbook and `.env.example` is a doc-drift bug, not a security issue |
| T-10-06-05 | Information disclosure | runbook documents PURGE_CRON_SECRET in commands without redaction | mitigate | Runbook shows `Authorization: Bearer $PURGE_CRON_SECRET` (variable reference, NOT literal value). Operator instantiates the variable from OVH-side secret manager when running. No literal secret committed |
| T-10-06-06 | Repudiation | launch-checklist commits in its checked state but the actual launch differs from the recorded steps | accept | This is intentional — the committed checked file IS the launch record per D-10-19. Antoine's responsibility to tick boxes truthfully. If a step was skipped, Antoine documents the deviation in the launch commit message |
| T-10-06-07 | Tampering | CI workflow YAML indentation error breaks the entire workflow | mitigate | Acceptance criterion verifies the new step appears between two existing steps (awk-based ordering check). YAML parser failures cascade to ALL CI jobs failing — visible immediately on the PR that introduces the regression |
| T-10-06-08 | Elevation of privilege | grep script scope expanded later to include test files where v10 keys live as fixture data | accept | Future scope expansion is a deliberate design decision — would require updating `--exclude-dir` flags and PATTERNS. Currently no test files reference v10 keys (verified by grep gate passing on the clean tree) |
| T-10-06-09 | Information disclosure | runbook leaks ADMIN_URL_SEGMENT (the hidden admin URL) to a future-public document | mitigate | Runbook references `process.env.ADMIN_URL_SEGMENT` as a variable name; never the literal value. `.env.example` ships placeholder. Operator's local `.env.local` (gitignored) holds the real value |
| T-10-06-10 | Repudiation | privacy-coverage doc placeholder content gets accidentally promoted to "confirmed" without Thomas actually replying | mitigate | The doc structure forces an ACTIVE update: Date, Confirmation status, and verbatim reply fields are explicit. A "confirmed" state without a corresponding email-quote in the doc is visibly malformed. Antoine reviews the diff before committing the update |
</threat_model>

<verification>
- All 3 tasks pass their `<automated>` gates
- `npm run check:no-v10-localstorage` exits 0 on the current clean tree
- `npm run typecheck` green (no script-level type leakage; bash and markdown changes don't affect ts)
- `npm run lint:check` green
- `npm run check:no-vercel-imports` green (no @vercel/* imports introduced by docs)
- File diff stats:
  - scripts/check-no-v10-localstorage.sh NEW ~50 lines
  - .github/workflows/ci.yml +3 lines
  - docs/operations/deploy-ovh.md NEW ~280 lines
  - docs/operations/launch-checklist.md NEW ~120 lines
  - docs/legal/privacy-coverage-confirmation.md NEW ~40 lines
  - .planning/PROJECT.md +1..3 lines (cross-reference)
- Manual sanity (planner discretion):
  - Open `docs/operations/deploy-ovh.md` in a markdown previewer; section ordering renders cleanly; ASCII diagram displays correctly.
  - Open `docs/operations/launch-checklist.md`; all 9 checkboxes render unticked.
</verification>

<success_criteria>
- `scripts/check-no-v10-localstorage.sh` exists, executable, hardcodes 5 v10 keys, scopes to src/ + app/ only
- CI workflow runs the new grep gate as a step between `check:seed-sql` and `Vitest unit tests`
- `docs/operations/deploy-ovh.md` exists with full Provisioning → Configuration → Build → Migration → Smoke → Cron swap-out → Rollback → Failure modes structure; Antoine-context tone; cross-references smoke-ovh.ts + launch-checklist.md
- `docs/operations/launch-checklist.md` lists 9 GFM-checkbox steps + CUT-02/01/03 vacuous-resolution notes; references purge:test-data + Vérifier les coefficients banner + privacy-coverage stub
- `docs/legal/privacy-coverage-confirmation.md` is a date-stamped stub holding Thomas's question verbatim with empty Response/Resolution sections
- `.planning/PROJECT.md` carries a 1-line CUT-02 cross-reference pointing at launch-checklist.md
- All CUT requirements (CUT-01, CUT-02, CUT-03, CUT-07, CUT-08) closed across plans 10-01..06; Phase 10 ready for execute-phase
- typecheck + lint + grep gates all green
</success_criteria>

<output>
After completion, create `.planning/phases/10-cutover-polish/10-06-SUMMARY.md` with:
- Files modified/created with line counts
- Confirmation that the new CI grep gate fires on a synthetic regression test (executor adds a `lt_pw` string to a temporary file in src/, runs the script, sees it fail; reverts the test)
- Confirmation that all 9 launch-checklist steps render with unticked GFM checkboxes
- The exact placement of the CUT-02 cross-reference in PROJECT.md (which paragraph it landed near)
- Note any deviations: e.g., if PROJECT.md required a new "Cutover notes" section because no clean placement existed
- Phase 10 closure note: with 10-06 complete, all 9 CUT requirements (CUT-01..09) are addressed across plans 10-01..06; the phase is ready for the orchestrator's verification + UAT pass
</output>
