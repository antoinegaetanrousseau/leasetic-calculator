---
phase: 18-admin-surfaces
plan: 07
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - docs/accessibility/18-contrast-addendum.md
autonomous: false
requirements: [THEME-02, ADMIN-10, ADMIN-11, ADMIN-12, ADMIN-13, ADMIN-14, HELP-01]
must_haves:
  truths:
    - "Every Phase 18 surface (Admin Home, Partners list, Créer partenaire, Coefficients, Aide landing, Aide article) ships visually correct in BOTH light and dark — 12 manual checkpoints total per D-29"
    - "If any new token pair surfaces a contrast issue, docs/accessibility/18-contrast-addendum.md is created/updated with the measurement + accept-with-deviation OR opacity-elevated variant per UI-SPEC §Contrast addendum"
    - "ADMIN-09 9-gate grep-contract suite remains green across the phase (full test run)"
    - "LC-2026 commission scrub equivalent: verify no commission leakage in any Phase 18 surface, especially Aide article screenshots (per UI-SPEC §ADMIN-09 envelope verification)"
    - "Antoine signs off the 12-checkpoint visual verification + any contrast addendum"
  artifacts:
    - path: docs/accessibility/18-contrast-addendum.md
      provides: "Phase 18 contrast addendum if any new token pair issues; OR a brief note stating no new pairs introduced per D-30 palette stability"
      contains: "Phase 18"
  key_links:
    - from: docs/accessibility/18-contrast-addendum.md
      to: docs/accessibility/16-contrast-audit.md (Phase 16 baseline) + 17-08 Phase 17 addendum
      via: "incremental addendum pattern — same format and sign-off section"
      pattern: "Phase 16|Phase 17|Phase 18"
---

<objective>
Closing-out verification for Phase 18 — analogous to Phase 17's 17-08 plan:
- **THEME-02 (D-29):** Every Phase 18 surface delivered by Plans 02-06 ships both light and dark variants. Verified by manual 12-checkpoint sweep (6 surfaces × 2 modes) since NO painted Figma dark frames exist for admin surfaces (D-29). The token-cascade inheritance must work end-to-end without layout shift, broken borders, or unreadable text.
- **CONTRAST addendum (if needed):** Per UI-SPEC §Light/Dark Mode Checklist + D-30 — Phase 18 introduces NO new color tokens. If during sign-off any token pair surfaces a contrast issue (below WCAG 2.1 AA 4.5:1), create `docs/accessibility/18-contrast-addendum.md` per UI-SPEC line 955 with either (a) accept-with-deviation noting inherited Phase 17/Phase 8 baseline, or (b) opacity-elevated variant. NO new color tokens permitted.
- **ADMIN-09 9-gate final check:** Full test run including `tests/admin-09-grep-contracts.test.ts` to confirm the 9-gate suite stays green across all 6 surfaces. Special focus: the 3 Aide article screenshots must not contain commission references (UI-SPEC §ADMIN-09 envelope verification + Plan 18-06 Task 3 checkpoint).

Purpose: ROADMAP §Phase 18 success criterion 5 — "Every admin-side screen ships both light and dark variants; the 9-gate grep-contract suite remains green." Plus the net-new HELP-01 success criterion 6 (Aide landing + article × 2 modes).
Output: 1 optional doc (contrast addendum) + 1 12-checkpoint human-verify sweep + 1 automated full-test-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/18-admin-surfaces/18-02-PLAN.md
@.planning/phases/18-admin-surfaces/18-03-PLAN.md
@.planning/phases/18-admin-surfaces/18-04-PLAN.md
@.planning/phases/18-admin-surfaces/18-05-PLAN.md
@.planning/phases/18-admin-surfaces/18-06-PLAN.md
@docs/accessibility/16-contrast-audit.md

<interfaces>
<!-- From app/globals.css -->
- All Phase 18 tokens are inherited from Phase 16 — no new tokens per D-30
- `--paper`, `--surface`, `--ink`, `--muted`, `--gd`, `--teal`, `--gold`, `--border`, `--gd-text`, `--gold-text`, `--danger`

<!-- From Phase 17 17-08 contrast addendum baseline (rows 8-11) -->
- Row 11 dark-mode active filter pill ~3.9:1 accepted-with-deviation (Option 1 from UI-SPEC); Phase 18 Partners list inherits this same composite (active filter pill gd-text on rgba(18,150,87,0.10)) per UI-SPEC line 790 — no regression introduced
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 12-checkpoint visual sweep (THEME-02 D-29 sign-off)</name>
  <what-built>
    Plans 02-06 delivered the 6 Phase 18 surfaces:
    - Admin Home `/[adminSegment]` (Plan 02): PageHero + 3 stat tiles (all teal D-04) + 3 AdminNavCards + Recent activity card
    - Partners list `/[adminSegment]/partners` (Plan 03): PageHero + SearchBar + 4-tab FilterPillTabs + 6-col styled table + cursor pagination + overflow ⋯ menu per row
    - Créer partenaire `/[adminSegment]/partners/new` (Plan 04): breadcrumb + PageHero + form card (3 sections, inline red error state) + separate action card (D-15)
    - Coefficients `/[adminSegment]/coefficients` (Plan 05): PageHero + dismissable warning banner + refreshed history sidebar (click-to-diff removed)
    - Aide landing `/aide` (Plan 06): PageHero + 3-card placeholder grid
    - Aide article `/aide/commencer-ici` (Plan 06): breadcrumb + article container + H1/H2/body prose + 3 wizard screenshots + end CTA

    All 6 surfaces inherit the dark mode via the `html[data-theme="dark"]` token cascade established in Phase 16 — **no per-component dark CSS exists**. **CRITICAL per D-29:** unlike Phase 17, there are NO painted Figma dark frames for admin surfaces. Dark variants are entirely DERIVED. This checkpoint confirms the inheritance works end-to-end without layout shift, broken borders, or unreadable text.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (from project root). Default port 3000.
    2. Log in as a test admin account (use credentials from scripts/seed-admins-launch.ts or your existing admin account).
    3. For EACH of the 6 surfaces below, visit the URL TWICE — once with the OS in light mode, once with dark mode (or use the sidebar tri-state theme toggle from Phase 16: System → Light → Dark).

    **Checkpoint 1-2: Admin Home `/[adminSegment]` (light + dark)**
    - [ ] PageHero renders ADMIN eyebrow + "Administration" title + subtitle + Nouvelle proposition CTA
    - [ ] 3 stat tiles render in a 3-up grid; ALL 3 values render in --teal per D-04 (NOT gold for "Dernière modif. coeffs")
    - [ ] Partenaires actifs sublabel shows "sur N comptes" with actual N
    - [ ] Propositions ce mois sublabel shows current month label (e.g. "mai 2026" / "May 2026")
    - [ ] Dernière modif. coeffs shows relative time + date + author (or "—" if no history)
    - [ ] 3 AdminNavCards render (Coefficients / Partenaires / Historique) — Historique RETAINED here despite removal from sidebar D-27
    - [ ] Recent activity card shows top 5 rows OR empty state "Aucune activité récente."
    - [ ] Voir tout → link at bottom-right of Recent activity card colored --teal
    - [ ] DARK MODE: no white-on-white, no dark-on-dark, no broken borders; --teal stat values legible over dark --surface

    **Checkpoint 3-4: Partners list `/[adminSegment]/partners` (light + dark)**
    - [ ] PageHero with "Partenaires" title + "Inviter un partenaire" green CTA top-right
    - [ ] SearchBar below the hero
    - [ ] 4 filter pills: Tous / Actifs / Invités / Désactivés (D-09 deviation from Figma 3-tab)
    - [ ] Clicking Invités pill: URL changes to ?status=invited AND list filters
    - [ ] Table renders 6 columns: PARTENAIRE / EMAIL / DATE CRÉATION / DERNIÈRE ACTIVITÉ / STATUT / ⋯
    - [ ] DERNIÈRE ACTIVITÉ shows "—" for partners with no proposals
    - [ ] StatusChip variants render correctly (active/invited/inactive)
    - [ ] Clicking ⋯ opens menu with correct items per row status (D-10)
    - [ ] Clicking "Voir les propositions du partenaire" navigates to /proposals?user_id={id} and renders that partner's proposals
    - [ ] Empty state (filter to a non-matching status combo): shows "Aucun partenaire ne correspond aux filtres." + "Effacer les filtres →" link
    - [ ] DARK MODE: table borders visible but not harsh; active filter pill ~3.9:1 (inherited Phase 17 deviation — accepted)

    **Checkpoint 5-6: Créer partenaire `/[adminSegment]/partners/new` (light + dark)**
    - [ ] Breadcrumb "← PARTENAIRES" above PageHero (hover changes color to --teal)
    - [ ] PageHero "Créer un partenaire" + subtitle (NO actions slot in hero — submit lives in action card)
    - [ ] Form card with 3 sections (● INFORMATIONS PERSONNELLES / SOCIÉTÉ / MESSAGE D'INVITATION)
    - [ ] Separate action card BELOW the form card (D-15) with ← Annuler (left) + Envoyer l'invitation → (right)
    - [ ] Submit a form with invalid email: input gets red border + inline red error text below; aria-invalid="true"
    - [ ] Type something then click ← Annuler: confirm dialog appears with "Vous avez des changements non enregistrés. Continuer ?"
    - [ ] Confirm the dialog: navigates to /partners
    - [ ] Cancel the dialog: stays on form, field values preserved
    - [ ] Successful submit (valid data): InviteUrlModal opens with the copyable invite link (unchanged from Phase 14)
    - [ ] DARK MODE: form input focus ring visible; --danger red border visible on invalid; action card background distinct from page bg

    **Checkpoint 7-8: Coefficients `/[adminSegment]/coefficients` (light + dark)**
    - [ ] PageHero "Coefficients" (shortened from "Coefficients & Commission" per Open Q9)
    - [ ] Warning banner BETWEEN hero subtitle and 2-column layout
    - [ ] Banner has AlertTriangle icon (gold-text), body copy locked from Figma, × close button
    - [ ] Click × on banner: banner disappears, sessionStorage gets the dismiss key. Reload page: banner still hidden. CLOSE the tab and reopen: banner returns (D-20 per-session not per-permanent)
    - [ ] 2-column layout: editor card (left) + history sidebar (right) — sidebar fixed 360px width
    - [ ] History sidebar rows are NOT clickable (D-22 click-to-diff removed); row cursor: default
    - [ ] History sidebar bottom: "Voir tout l'historique →" link to /[adminSegment]/history
    - [ ] Editing a coefficient still works (editor card structurally unchanged from Phase 14)
    - [ ] DARK MODE: warning banner tint visible but subtle; --gold-text icon legible on banner bg; left-accent stripe --gold visible

    **Checkpoint 9-10: Aide landing `/aide` (light + dark)**
    - [ ] PageHero "Centre d'aide" + subtitle
    - [ ] 3-card grid: Commencer ici / Créer une proposition / Contact
    - [ ] Card 1 (Commencer ici): clickable, "Lire le guide →" link navigates to /aide/commencer-ici
    - [ ] Card 2 (Créer une proposition): visually disabled (opacity 0.7), "Bientôt disponible" badge, NOT clickable
    - [ ] Card 3 (Contact): mailto: link with the confirmed SUPPORT_EMAIL (from Plan 18-06 Task 1 decision)
    - [ ] Visit /aide from a partner login: sidebar shows Aide nav item; landing accessible
    - [ ] Visit /aide from an admin login: sidebar shows Aide nav item; landing accessible
    - [ ] DARK MODE: Card 2 disabled opacity (0.7) still readable in dark; all 3 cards distinct from page bg

    **Checkpoint 11-12: Aide article `/aide/commencer-ici` (light + dark)**
    - [ ] Breadcrumb "← Centre d'aide" at top
    - [ ] Article container card (centered, max 720px wide)
    - [ ] H1 "Commencer ici" + lead paragraph
    - [ ] 5 H2 sections: Aperçu / Étape 1 / Étape 2 / Étape 3 / Et après ?
    - [ ] 3 wizard screenshots embedded between H2 sections — verify they ACTUALLY render (not 404)
    - [ ] **CRITICAL ADMIN-09 envelope check**: visually inspect step-2 screenshot — does NOT contain a visible commission value (e.g. "Commission apporteur 1.00%"). If commission row visible, FAIL and re-capture per Plan 18-06 Task 3.
    - [ ] End-of-article green CTA "Créer ma première proposition" links to /proposals/new/parametres
    - [ ] DARK MODE: screenshots are light-mode captures (accepted partial per UI-SPEC line 626-630 Option 1); prose typography legible; article card chrome visible

    4. If ANY surface fails, capture screenshot(s), note the failure mode, and run `/gsd-execute-phase 18 --gaps` to generate a gap-closure plan. Do NOT approve until all 12 checkpoints pass.

    5. If a contrast issue surfaces (e.g. --gold-text icon over warning banner bg fails AA in dark): document in `docs/accessibility/18-contrast-addendum.md` per Task 2 OR escalate via the contrast appendix.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 checkpoints (6 surfaces × 2 modes) pass per the checklist. If any checkpoint fails, describe the failure (which surface, which mode, what went wrong) and the planner will trigger gap-closure mode. If a commission value visible in the Aide step-2 screenshot, re-capture and re-verify before approving.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create/update Phase 18 contrast addendum if any new pairs surface issues</name>
  <files>docs/accessibility/18-contrast-addendum.md</files>
  <read_first>
    - docs/accessibility/16-contrast-audit.md (Phase 16 baseline + Phase 17 addendum rows 8-11 — verify exists)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Light/Dark Mode Checklist + §Color Token Audit — Per Surface (lines 932-955)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-29 derived dark; D-30 palette stability — no new tokens)
  </read_first>
  <action>
    Per UI-SPEC line 955 + D-30:

    1. **Determine if an addendum is needed:** Phase 18 introduces NO new color tokens per D-30. Per UI-SPEC §Color Token Audit (lines 759-879), every Phase 18 token pair is INHERITED from Phase 16/17 with one exception — the warning banner tint (`rgba(224,133,48,0.10)` over `--surface`) is a NEW USAGE of the existing tint pattern from Phase 14's `.chip-invited` and Phase 16's diff-panel changed-row background.

    2. **If Task 1 sign-off identified a contrast issue**, CREATE `docs/accessibility/18-contrast-addendum.md` with:
       - Phase 18 header + summary
       - Each new composite as a table row with: composite description, FG hex, blended BG hex, mode, ratio, pass?
       - For each failing pair: either (a) accept-with-deviation noting inherited Phase 17/Phase 8 baseline, or (b) opacity-elevated token variant proposal
       - Antoine sign-off section
       - Reference to UI-SPEC line 955 for the addendum protocol

    3. **If Task 1 sign-off finds no contrast issues** (expected — no new tokens), still CREATE a brief `docs/accessibility/18-contrast-addendum.md` stating:
       - "Phase 18 introduces NO new color tokens per D-30 palette stability invariant."
       - "All token pairs inherited from Phase 16/17 — no new contrast measurements required."
       - "Warning banner tint (`rgba(224,133,48,0.10)` over `--surface`) is a NEW USAGE of an existing Phase 14 tint pattern; verified visually during 12-checkpoint sweep without measurable issues."
       - Reference Phase 17 17-08 addendum (rows 8-11) and Phase 16 baseline.
       - Antoine sign-off line.

    4. Use `Write` tool (file is net-new). Keep under 60 lines.
  </action>
  <verify>
    <automated>test -f docs/accessibility/18-contrast-addendum.md && grep -c "Phase 18" docs/accessibility/18-contrast-addendum.md | { read count; [ "$count" -ge 1 ]; }</automated>
  </verify>
  <done>
    - `docs/accessibility/18-contrast-addendum.md` exists
    - File references Phase 18 + D-30 invariant
    - Antoine sign-off line present
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite gate — ADMIN-09 9-gate stays green + no regression</name>
  <files></files>
  <read_first>
    - tests/admin-09-grep-contracts.test.ts (verify the 9 gates expected to remain green; understand what substrings each gate flags)
    - package.json (verify the test scripts — typically `npm test` runs the full Vitest suite)
  </read_first>
  <action>
    Per ROADMAP §v1.3 §2 + UI-SPEC §ADMIN-09 Envelope Verification:

    1. Run `npm test` (or the project's full-test command per package.json) and capture the output.
    2. Confirm `tests/admin-09-grep-contracts.test.ts` passes all 9 gates — the suite gates on commission_pct and Commission substrings across PDF, audit_log, server logs, pre-finalize traces.
    3. Confirm all Phase 18 plan tests pass:
       - `src/lib/db/queries/proposal-aggregates.test.ts` (Plan 18-01 Task 1 — cross-partner monthly count)
       - `src/lib/db/queries/partner-aggregates.test.ts` (Plan 18-01 Task 1 — active partner count)
       - `src/lib/db/queries/admin-activity.test.ts` (Plan 18-01 Task 2 — 3-source union)
       - `src/lib/api/proposals/list.test.ts` (Plan 18-01 Task 2 — adminUserIdOverride + IDOR negative tests)
       - `src/components/ui/SidebarNav.test.tsx` (Plan 18-01 Task 3 — per-role nav)
       - `src/components/ui/MetricTile.test.tsx` (Plan 18-02 Task 1 — valueColor prop)
       - `app/(admin)/[adminSegment]/_components/RecentActivityRow.test.tsx` (Plan 18-02 Task 2)
       - `app/(admin)/[adminSegment]/page.test.tsx` (Plan 18-02 Task 3 — Admin Home)
       - `src/lib/db/queries/partners.test.ts` (Plan 18-03 Task 1 — last-activity JOIN)
       - `app/(admin)/[adminSegment]/partners/PartnersList.test.tsx` (Plan 18-03 Task 1 — rename + 6-col)
       - `app/(admin)/[adminSegment]/partners/_components/PartnersFilterPillTabs.test.tsx` + `PartnerRowActions.test.tsx` (Plan 18-03 Task 2)
       - `app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx` (Plan 18-04 Task 1 — visual refresh + D-16 + D-18)
       - `app/(admin)/[adminSegment]/coefficients/_components/CoefficientWarningBanner.test.tsx` (Plan 18-05 Task 1)
       - `app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx` (Plan 18-05 Task 2 — click-to-diff removed)
       - `app/(authed)/aide/page.test.tsx` (Plan 18-06 Task 2 — landing)
       - `app/(authed)/aide/commencer-ici/page.test.tsx` (Plan 18-06 Task 4 — article)
    4. Confirm the i18n parity proof (`_EnHasAllFrKeys` at dictionaries.ts) compiles cleanly via `npx tsc --noEmit`.
    5. Confirm D-14 rename scrub: `grep -rn "AccountsList" app/ src/ | grep -v node_modules` returns 0.
    6. Confirm sidebar D-27: `grep -n "Historique" src/components/ui/SidebarNav.tsx | grep -v '^#'` does NOT appear in the admin items array.
    7. Confirm Aide commission-free per UI-SPEC line 968: `grep -ni "commission" src/lib/i18n/dictionaries.ts | grep "aide\\."` returns 0.
    8. If any test fails, report which file + which test + the failure message; do NOT proceed to git commit until all green.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && grep -rn "AccountsList" app/ src/ | grep -v node_modules | wc -l | awk '$1 == 0 { exit 0 } $1 != 0 { exit 1 }'</automated>
  </verify>
  <done>
    - `npm test` exits 0 (full suite green)
    - `npx tsc --noEmit` exits 0 (TS + _EnHasAllFrKeys parity proof green)
    - `tests/admin-09-grep-contracts.test.ts` 9 gates green (verify via test output)
    - D-14 scrub gate green (0 AccountsList references in app/ src/)
    - D-27 sidebar gate green (Historique not in admin nav items)
    - Aide commission-free gate green (no "commission" substring in aide.* i18n keys)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → CI/local | mechanical gate; no untrusted input |
| Documentation → reviewer | static markdown; human reads and signs off |
| Browser visual verification → human | human-in-the-loop confirms token cascade renders correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | Information disclosure (commission leak across phase) | full Phase 18 surface set | mitigate | Task 3 runs the 9-gate ADMIN-09 suite which gates on commission substrings across PDF / audit_log / server logs / pre-finalize traces; failure blocks merge. Additional check (Task 1 + Task 3) on Aide article screenshots and i18n keys |
| T-18-07-02 | Information disclosure (commission via screenshot) | Aide article wizard-step-2.png | mitigate | Task 1 checkpoint explicitly inspects the step-2 screenshot for visible commission values; failure triggers re-capture per Plan 18-06 Task 3 |
| T-18-07-03 | Repudiation (visual sign-off without record) | Antoine human-verify | mitigate | Task 2 contrast addendum captures the sign-off line; SUMMARY.md at end of plan records the date + approved/rejected decision per the resume-signal |
| T-18-07-04 | Tampering (D-14 rename incomplete leaves dead AccountsList ref) | rename completeness | mitigate | Task 3 explicit grep gate (`grep -rn "AccountsList"`) returns 0; CI-enforceable; also blocks PR merge |
</threat_model>

<verification>
- `npm test && npx tsc --noEmit` exits 0
- Antoine's "approved" signal received per Task 1 checkpoint (or gap-closure mode triggered if rejected)
- `docs/accessibility/18-contrast-addendum.md` exists with Phase 18 attribution
- D-14 rename scrub gate green
- ADMIN-09 9-gate suite green
</verification>

<success_criteria>
- All 6 Phase 18 surfaces verified light + dark by Antoine — 12-checkpoint sweep (THEME-02 D-29)
- 18-contrast-addendum.md committed (either with new pair documentation OR "no new tokens" note per D-30)
- ADMIN-09 9-gate suite remains green across the phase (ROADMAP §v1.3 §2)
- D-14 rename scrub complete (0 AccountsList references in app/ src/)
- HELP-01 net-new requirement satisfied (landing + article × 2 modes)
- Full test suite + TypeScript compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-07-SUMMARY.md` capturing:
- Antoine's sign-off date + outcome for each of the 12 checkpoints
- Any deviations or follow-ups noted during visual verification
- Final test counts (before/after Phase 18)
- Phase 18 closure summary referencing the 7 requirements satisfied (ADMIN-10..14 + THEME-02 + HELP-01)
</output>
