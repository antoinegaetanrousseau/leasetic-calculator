---
phase: 18-admin-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/queries/proposal-aggregates.ts
  - src/lib/db/queries/proposal-aggregates.test.ts
  - src/lib/db/queries/partner-aggregates.ts
  - src/lib/db/queries/partner-aggregates.test.ts
  - src/lib/db/queries/admin-activity.ts
  - src/lib/db/queries/admin-activity.test.ts
  - src/lib/api/proposals/list.ts
  - src/lib/api/proposals/list.test.ts
  - app/(authed)/proposals/page.tsx
  - src/lib/i18n/dictionaries.ts
  - src/components/ui/SidebarNav.tsx
autonomous: true
requirements: [ADMIN-10, ADMIN-11, ADMIN-12, HELP-01]
must_haves:
  truths:
    - "getMonthlyProposalCountAll() returns count of all non-deleted proposals (any status incl. draft) created during current Europe/Paris month, cross-partner"
    - "getActivePartnerCount() returns count of users.role='partner' AND users.status='active'; getTotalPartnerAccountCount() returns count of non-deleted partner accounts (active + invited + inactive)"
    - "getRecentAdminActivity({limit:5}) returns up to 5 rows merged-and-sorted DESC across coefficient_history + user status transitions + invitations sources"
    - "buildListResponse accepts optional adminUserIdOverride; only honored when caller has admin role; rejects/ignores override when caller is partner (no IDOR)"
    - "Sidebar nav config returns 6 items for admin role (Accueil, Nouvelle proposition, Propositions, Partenaires, Coefficients, Aide) and 4 items for partner role (Accueil, Nouvelle proposition, Propositions, Aide); Historique removed from admin sidebar"
    - "Net-new i18n keys added (~70 FR + ~70 EN) per UI-SPEC Copywriting Contract; _EnHasAllFrKeys parity proof compiles clean"
  artifacts:
    - path: src/lib/db/queries/proposal-aggregates.ts
      provides: "getMonthlyProposalCountAll() cross-partner variant of Phase 17 helper (D-02)"
      contains: "getMonthlyProposalCountAll"
    - path: src/lib/db/queries/partner-aggregates.ts
      provides: "getActivePartnerCount + getTotalPartnerAccountCount (D-01)"
      contains: "getActivePartnerCount"
    - path: src/lib/db/queries/admin-activity.ts
      provides: "getRecentAdminActivity 3-source union (D-05)"
      contains: "getRecentAdminActivity"
    - path: src/lib/api/proposals/list.ts
      provides: "BuildListParams.adminUserIdOverride with admin-role gate (D-11)"
      contains: "adminUserIdOverride"
    - path: src/components/ui/SidebarNav.tsx
      provides: "per-role nav config per D-27 (admin 6-item / partner 4-item)"
      contains: "Aide"
    - path: src/lib/i18n/dictionaries.ts
      provides: "~70 net-new keys in admin.home.stats.*, admin.home.activity.*, admin.partners.*, admin.partners.form.*, admin.coefficients.warning.*, aide.landing.*, aide.commencer-ici.*"
      contains: "admin.home.activity.title"
  key_links:
    - from: src/lib/db/queries/admin-activity.ts
      to: src/lib/db/schema.ts (coefficient_history, users, invitations)
      via: "Drizzle SELECT on 3 tables, UNION at app layer, sort DESC, slice 5"
      pattern: "coefficient_history|invitations|users"
    - from: app/(authed)/proposals/page.tsx
      to: src/lib/api/proposals/list.ts
      via: "searchParams.user_id passed as adminUserIdOverride when requireAdmin() succeeds"
      pattern: "adminUserIdOverride|user_id"
    - from: src/components/ui/SidebarNav.tsx
      to: src/lib/i18n/dictionaries.ts
      via: "t('sidebar.nav.help', lang) reuses existing key; t('sidebar.nav.proposals') verifies/adds"
      pattern: "sidebar.nav.help|sidebar.nav.proposals"
---

<objective>
DB/API + i18n + sidebar foundation that unblocks every Wave-2 surface plan in Phase 18. Adds the 3 cross-partner aggregate query helpers (Admin Home stats D-01/D-02 + Recent activity D-05), the admin-scoped `/proposals` query parameter (D-11), the per-role sidebar nav config (D-27), and the ~70 net-new i18n keys from the UI-SPEC Copywriting Contract (verify-and-reuse per D-21 Phase 17 discipline).

Purpose: All Wave-2 surface plans (18-02 through 18-06) consume one or more outputs of this plan. Building these in Wave 1 means surface plans can run in parallel without contract-discovery overhead.
Output: 3 query modules (1 modified, 2 created) + 1 list-API extension + 1 SSR route update + 1 sidebar config update + dictionaries.ts net-new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-surfaces/18-CONTEXT.md
@.planning/phases/18-admin-surfaces/18-UI-SPEC.md
@.planning/phases/17-partner-surfaces/17-03-SUMMARY.md
@.planning/phases/17-partner-surfaces/17-04-SUMMARY.md

<interfaces>
<!-- From src/lib/db/queries/proposal-aggregates.ts (Phase 17 17-03) -->
- `getMonthlyProposalCount(userId: string): Promise<number>` — Europe/Paris current-month count, scoped to userId. Phase 18 adds the cross-partner variant `getMonthlyProposalCountAll()` (NO userId filter) per D-02.
- Same Europe/Paris boundary logic must be reused (DRY); refactor a private `monthBoundsParis()` helper if needed.

<!-- From src/lib/db/schema.ts (existing) -->
- `users` table: `id`, `email`, `name`, `role` ('partner'|'admin'), `status` ('active'|'invited'|'inactive'), `createdAt`, `updatedAt`, `deletedAt` (nullable)
- `coefficient_history` table: append-only with TRIGGER (Phase 12); columns include `id`, `createdAt`, `createdBy` (user id), and the changed fields snapshot
- `invitations` table (Phase 6/14): `id`, `email`, `createdAt`, `createdBy`, `consumedAt` (nullable). Verify exact table name with `grep -rn "invitations" src/lib/db/schema.ts` before coding.

<!-- From src/lib/api/proposals/list.ts (Phase 17 17-01) -->
- `BuildListParams { userId, q?, cursorEncoded?, archived?, deleted?, limit? }` — Phase 18 adds `adminUserIdOverride?: string` per D-11. When set AND caller role is admin, the query SELECTs WHERE user_id = adminUserIdOverride (NOT the session userId). Authorization is enforced at the SSR layer in `app/(authed)/proposals/page.tsx` before calling buildListResponse.

<!-- From src/components/ui/SidebarNav.tsx (existing — verify path with grep) -->
- Existing per-role nav config OR hardcoded items by role. Phase 18 ensures: admin = [Accueil, Nouvelle proposition, Propositions, Partenaires, Coefficients, Aide]; partner = [Accueil, Nouvelle proposition, Propositions, Aide]. Historique REMOVED from admin sidebar (D-27).

<!-- From src/lib/i18n/dictionaries.ts (Phase 17 D-21 verify-and-reuse) -->
- Existing reusable keys: `sidebar.nav.help`, `admin.home.eyebrow`, `admin.home.title`, `admin.home.subtitle`, `admin.nav.coefficients.title/description`, `admin.nav.partners.title/description`, `admin.nav.history.title/description`, `admin.nav.open`, `dashboard.cta.new`, `admin.accounts.last_login.minutes_ago/hours_ago/days_ago`, `partners.new.title/subtitle/section.*/cancel/toast.error`, `admin.coefficients.page.title/sub`, `admin.coefficients.history.load_more`, `chip.invited`, `admin.accounts.status.*`, `admin.accounts.action.reissue/disable/enable`, `admin.accounts.empty.title`, `admin.accounts.col.email/created/status`, `admin.accounts.modal.error.*`, `admin.accounts.search.placeholder`, `admin.accounts.page.title/sub`.
- Net-new keys: see UI-SPEC §Copywriting Contract rows 5-11, 19-28 (Admin Home), 29-50 (Partners list), 51-63 (Créer partenaire), 67-69 (Coefficients), 70-79 (Aide landing), 81-97 (Aide article). Approximately 70 net-new key pairs (FR+EN).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-partner aggregates — Admin Home stats foundation (D-01/D-02)</name>
  <files>src/lib/db/queries/proposal-aggregates.ts, src/lib/db/queries/proposal-aggregates.test.ts, src/lib/db/queries/partner-aggregates.ts, src/lib/db/queries/partner-aggregates.test.ts</files>
  <read_first>
    - src/lib/db/queries/proposal-aggregates.ts (existing Phase 17 helpers — read full file, note the Europe/Paris month-boundary logic to reuse)
    - src/lib/db/queries/proposal-aggregates.test.ts (existing test scaffolding to extend with the new helper)
    - src/lib/db/schema.ts (verify users.role / users.status / users.deletedAt column names + types; verify proposals.deletedAt + proposals.createdAt)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-01 active partner definition; D-02 cross-partner monthly count semantics)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Admin Home component contract stat tile rows (lines 222-228 area)
  </read_first>
  <behavior>
    - Test 1 (getMonthlyProposalCountAll): seed 3 proposals across 2 different user_ids in current Europe/Paris month → returns 3 (cross-partner, ignores userId)
    - Test 2 (getMonthlyProposalCountAll status-inclusive): includes proposals with status='draft' AND status='active' (D-02 explicitly counts ALL non-deleted statuses including drafts); seed 1 draft + 1 active in current month → returns 2
    - Test 3 (getMonthlyProposalCountAll soft-delete exclusion): proposals with deletedAt IS NOT NULL are EXCLUDED; seed 1 soft-deleted + 1 active in current month → returns 1
    - Test 4 (getMonthlyProposalCountAll timezone): proposals created 2 minutes before the Europe/Paris month boundary (the last 2 minutes of the prior UTC day in a CET/CEST DST window) are EXCLUDED from the current-month count
    - Test 5 (getActivePartnerCount): seed users role='partner' status='active' (3 rows) + status='invited' (1) + status='inactive' (1) + role='admin' status='active' (1) → returns 3 (only role=partner AND status=active)
    - Test 6 (getTotalPartnerAccountCount): given the same seed, returns 5 (role=partner regardless of status; excludes role=admin; excludes deletedAt IS NOT NULL)
    - Test 7 (cross-test isolation): no test reads from another test's seed (each test uses unique email scope or clean DB transaction)
  </behavior>
  <action>
    Per D-01/D-02, implement cross-partner aggregate helpers. Reuse the existing Europe/Paris month-boundary logic from Phase 17's `getMonthlyProposalCount` — refactor into a private `monthBoundsParis(): { startUtc: Date; endUtc: Date }` helper inside `proposal-aggregates.ts` if it isn't already extracted; then call from both the user-scoped and cross-partner variants.

    1. In `src/lib/db/queries/proposal-aggregates.ts`:
       - Add `export async function getMonthlyProposalCountAll(): Promise<number>` — same SQL shape as `getMonthlyProposalCount` but WITHOUT the `eq(proposals.userId, userId)` clause. Keep the `isNull(proposals.deletedAt)` filter. Keep the `>= startOfMonthUtc AND < endOfMonthUtc` boundary. NO status filter (D-02 explicit: includes drafts).
       - Add a brief JSDoc comment citing D-02: "Cross-partner monthly proposal count for Admin Home stat tile (D-02). Includes all non-deleted statuses including drafts. Bounded by Europe/Paris calendar month."

    2. CREATE `src/lib/db/queries/partner-aggregates.ts`:
       - `export async function getActivePartnerCount(): Promise<number>` — SELECT count(*) FROM users WHERE role='partner' AND status='active' AND deletedAt IS NULL. Use Drizzle `and(eq(users.role, 'partner'), eq(users.status, 'active'), isNull(users.deletedAt))`.
       - `export async function getTotalPartnerAccountCount(): Promise<number>` — SELECT count(*) FROM users WHERE role='partner' AND deletedAt IS NULL (any status). Per D-01 sublabel `sur N comptes`.
       - JSDoc: cite D-01.

    3. Create `partner-aggregates.test.ts` with Test 5 + 6 + 7 from the behavior block above. Use Vitest + existing DB test fixture pattern (look at how `proposal-aggregates.test.ts` sets up the DB and clears users between tests; mirror it). DO NOT touch other tables' rows.

    4. Extend `proposal-aggregates.test.ts` with Tests 1-4. Add a `describe('getMonthlyProposalCountAll')` block. Reuse the existing user-seeding helpers if present; otherwise inline minimal `db.insert(users).values({...})` calls with role='partner'.

    5. NO new schema migration. All columns referenced (users.role, users.status, users.deletedAt, proposals.userId, proposals.deletedAt, proposals.createdAt) already exist.
  </action>
  <verify>
    <automated>npm test -- src/lib/db/queries/proposal-aggregates.test.ts src/lib/db/queries/partner-aggregates.test.ts</automated>
  </verify>
  <done>
    - `grep -n "getMonthlyProposalCountAll" src/lib/db/queries/proposal-aggregates.ts` returns at least 2 hits (declaration + export)
    - `grep -n "getActivePartnerCount\|getTotalPartnerAccountCount" src/lib/db/queries/partner-aggregates.ts` returns 2 hits each
    - 7 behavior tests all pass; existing proposal-aggregates tests remain green
    - JSDoc on each new helper cites the D-NN decision implemented (D-01 or D-02)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recent activity 3-source union (D-05) + admin user_id query param on /proposals (D-11)</name>
  <files>src/lib/db/queries/admin-activity.ts, src/lib/db/queries/admin-activity.test.ts, src/lib/api/proposals/list.ts, src/lib/api/proposals/list.test.ts, app/(authed)/proposals/page.tsx</files>
  <read_first>
    - src/lib/db/schema.ts (verify coefficient_history, users, invitations table names + columns; especially invitations table — confirm exists or note Phase 14 alternative)
    - src/lib/db/queries/proposals.ts (deriveDisplayStatus pattern — reuse approach for app-side filtering)
    - src/lib/api/proposals/list.ts (Phase 17 17-01 modified — full file; verify how BuildListParams threads to listProposalsByUser)
    - src/lib/api/proposals/list.test.ts (existing test structure; extend with adminUserIdOverride tests)
    - app/(authed)/proposals/page.tsx (Phase 17 SSR route; verify how session.user.id is currently passed to buildListResponse and what searchParams are read)
    - src/lib/auth/require.ts (verify requireAdmin() helper signature and behavior — used for D-11 gate)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-05 3-source union; D-06 top-5 read-only; D-07 row read-only; D-11 admin user_id query param + authorization gate)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Admin Home Recent activity card data shape (ActivityRow type around line 247-258)
  </read_first>
  <behavior>
    - Test 1 (admin-activity 3-source merge): seed 2 coefficient_history rows + 2 user status transitions (status_updated_at within current week) + 2 invitations within current week → getRecentAdminActivity({limit:5}) returns 5 rows merged + sorted DESC by timestamp
    - Test 2 (admin-activity LIMIT 10 per source): seed 20 coefficient_history rows + 0 others → returns 5 rows (top 5 from the 10 most-recent coefficient_history rows)
    - Test 3 (admin-activity ActivityRow shape): each row matches `{ id, source: 'coefficient_history'|'partner_status'|'invitation', actorName, actorEmail?, sentenceKey, sentenceArgs, timestamp }` shape exactly
    - Test 4 (admin-activity empty state): empty DB → returns []
    - Test 5 (list.ts adminUserIdOverride accepted): buildListResponse({ userId: adminId, adminUserIdOverride: partnerId, _callerRole: 'admin' }) returns rows WHERE user_id = partnerId (the override)
    - Test 6 (list.ts adminUserIdOverride rejected for partner): buildListResponse({ userId: partnerAId, adminUserIdOverride: partnerBId, _callerRole: 'partner' }) returns rows WHERE user_id = partnerAId (override IGNORED; no IDOR — verify with cross-user isolation assertion that partnerB's rows are NOT in the result)
    - Test 7 (page.tsx admin SSR honors user_id when admin): visiting /proposals?user_id={partnerB} as admin renders partnerB's rows; verify via a server-component render test or by asserting buildListResponse was called with adminUserIdOverride=partnerB and _callerRole='admin'
    - Test 8 (page.tsx partner ignores user_id query): visiting /proposals?user_id={partnerB} as partnerA renders partnerA's rows ONLY (query param silently ignored or rejected)
  </behavior>
  <action>
    Implement per D-05, D-11.

    1. CREATE `src/lib/db/queries/admin-activity.ts`:
       - Define `export type ActivityRow = { id: string; source: 'coefficient_history' | 'partner_status' | 'invitation'; actorName: string; actorEmail?: string; sentenceKey: string; sentenceArgs: string[]; timestamp: Date }` exactly per UI-SPEC line 247-258.
       - `export async function getRecentAdminActivity(args?: { limit?: number }): Promise<ActivityRow[]>` — default limit=5.
       - Implementation: 3 separate Drizzle SELECTs each capped at `LIMIT 10`:
         (a) `coefficient_history` JOIN `users` ON created_by — produces `sentenceKey: 'admin.home.activity.sentence.coefficientModified'`, `sentenceArgs: [user.name || user.email]`, `actorName: user.name || user.email`, `actorEmail: user.email`, `timestamp: row.createdAt`, `source: 'coefficient_history'`, `id: 'ch-' + row.id`
         (b) `users` filtered by role='partner' AND status IN ('active','inactive') AND updatedAt >= NOW() - INTERVAL '30 days' (status transitions; bounded recency to avoid scanning all rows) — produces `sentenceKey: 'admin.home.activity.sentence.partnerActivated' | '.partnerDeactivated'` based on current status, `sentenceArgs: [actor.name, target.name]` where actor is derived from `updatedBy` if column exists OR fallback to a generic "Admin" sentinel + the target user.name/email. **Verify `updatedBy` column existence with `grep -n "updatedBy" src/lib/db/schema.ts`; if absent, treat actorName as the constant `'Admin'` and document the partial in a code comment citing D-05 deferred.** `timestamp: target.updatedAt`, `source: 'partner_status'`, `id: 'ps-' + target.id`
         (c) `invitations` JOIN `users` ON createdBy — produces `sentenceKey: 'admin.home.activity.sentence.partnerInvited'`, `sentenceArgs: [creator.name, invitation.email]`, `actorName: creator.name`, `actorEmail: creator.email`, `timestamp: invitation.createdAt`, `source: 'invitation'`, `id: 'inv-' + invitation.id`. **If invitations table does not exist in current schema, document and skip source (c) — Recent activity becomes a 2-source union; note in JSDoc citing D-05 partial.** Verify with `grep -n "invitations" src/lib/db/schema.ts` before coding.
       - Merge all 3 arrays, sort `(a,b) => b.timestamp.getTime() - a.timestamp.getTime()`, slice top `limit`. Return.
       - JSDoc cites D-05 explicitly and notes any source that had to be deferred.

    2. CREATE `admin-activity.test.ts` with Tests 1-4 from behavior block. Use the DB fixture pattern from existing query tests. Seed via `db.insert(coefficient_history).values({...})`, `db.update(users).set({status:'inactive', updatedAt: new Date()})`, `db.insert(invitations).values({...})` (only if invitations exists).

    3. In `src/lib/api/proposals/list.ts`:
       - Extend `BuildListParams` interface: add `adminUserIdOverride?: string` and `_callerRole?: 'admin' | 'partner'` (the underscore prefix marks it as a server-internal hint, not a user-facing parameter).
       - In `buildListResponse`, compute `effectiveUserId = (_callerRole === 'admin' && adminUserIdOverride) ? adminUserIdOverride : userId`. Use `effectiveUserId` in the downstream `listProposalsByUser` call.
       - Add inline comment: `// D-11: admin can scope /proposals to another partner via ?user_id=; gate is admin role enforced at SSR layer. _callerRole must be passed in by the route — never derived from request params.`

    4. In `app/(authed)/proposals/page.tsx`:
       - Read `searchParams.user_id` (with proper App-Router searchParams typing — see Phase 17 pattern in this file).
       - Determine caller role: `const session = await auth().api.getSession({...}); const isAdmin = session?.user?.role === 'admin';`
       - When `isAdmin && searchParams.user_id` is a valid string: pass `adminUserIdOverride: searchParams.user_id, _callerRole: 'admin'` to `buildListResponse`. Otherwise pass `_callerRole: session?.user?.role ?? 'partner'` and omit adminUserIdOverride.
       - DO NOT derive role from any client-supplied value. session.user.role only.

    5. Extend `list.test.ts` with Tests 5 + 6. Add a separate test file (or describe block) for the SSR route if a page-level testing harness exists; otherwise document the SSR-layer expectation as a manual verify step in a comment and rely on Tests 5+6 to enforce the library-level contract.

    SECURITY: Tests 6 + 8 are IDOR negative tests. They are NON-NEGOTIABLE — must pass before this task is considered done.
  </action>
  <verify>
    <automated>npm test -- src/lib/db/queries/admin-activity.test.ts src/lib/api/proposals/list.test.ts</automated>
  </verify>
  <done>
    - `grep -n "getRecentAdminActivity\|ActivityRow" src/lib/db/queries/admin-activity.ts` returns at least 4 hits (type, function, source enum, default limit)
    - `grep -n "adminUserIdOverride\|_callerRole" src/lib/api/proposals/list.ts` returns at least 4 hits
    - `grep -n "adminUserIdOverride\|user_id" app/\(authed\)/proposals/page.tsx` shows the searchParam is read AND gated on session.user.role
    - Tests 1-8 all pass; existing list.test.ts tests remain green
    - IDOR negative tests (6 + 8) pass — partner CANNOT scope to another partner's rows
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-role sidebar nav (D-27) + ~70 net-new i18n keys (D-21 verify-and-reuse)</name>
  <files>src/components/ui/SidebarNav.tsx, src/components/ui/SidebarNav.test.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/components/ui/SidebarNav.tsx (existing — verify exact path with `find src/components -name "Sidebar*.tsx" -o -name "Shell*.tsx"`; the component may be named differently. Phase 17 sidebar work touched this area — check 17-CONTEXT.md if path unclear)
    - src/lib/i18n/dictionaries.ts (read the existing keys to verify which ones already exist; CRITICAL: Phase 17 D-21 verify-and-reuse — do NOT add a duplicate key)
    - src/lib/i18n/index.ts (verify `t()` signature)
    - .planning/phases/18-admin-surfaces/18-CONTEXT.md (D-27 sidebar nav per-role contract)
    - .planning/phases/18-admin-surfaces/18-UI-SPEC.md §Copywriting Contract — full table rows 1-97 to identify which keys are NEW vs EXISTING vs RE-KEY of admin.accounts.*
    - Existing `_EnHasAllFrKeys` parity-proof block at the end of dictionaries.ts (around line 1353-1357 per Phase 17 SUMMARYs) — compile-time check; new keys must appear in both FR and EN
  </read_first>
  <behavior>
    - Test 1 (sidebar admin): given role='admin', sidebar config returns exactly 6 items in order: [Accueil, Nouvelle proposition, Propositions, Partenaires, Coefficients, Aide]. Historique is NOT present.
    - Test 2 (sidebar partner): given role='partner', sidebar config returns exactly 4 items in order: [Accueil, Nouvelle proposition, Propositions, Aide]. Partenaires and Coefficients are NOT present.
    - Test 3 (i18n parity): `_EnHasAllFrKeys` compile-time proof remains satisfied; `npx tsc --noEmit` exits 0
    - Test 4 (i18n no duplicates): each net-new key appears EXACTLY ONCE in dictionaries.ts (grep -c per key === 1 in each language block)
    - Test 5 (i18n reuse honored): existing keys identified in the Copywriting Contract as EXISTING are NOT duplicated; existing keys identified as RE-KEY are added to admin.partners.* but the original admin.accounts.* values remain in place for back-compat (Phase 14 callers continue to work)
  </behavior>
  <action>
    Per D-27 + D-21:

    1. Update `src/components/ui/SidebarNav.tsx` (verify exact path):
       - Define a per-role nav config. Recommended shape: a `SIDEBAR_NAV` object keyed by role ('admin' | 'partner'), each value being an array of `{ key, href, labelKey, icon }` items. Admin gets exactly 6 items in this order: home (href '/'), new (href '/proposals/new/parametres'), proposals (href '/proposals'), partners (href `/${adminSegment}/partners`), coefficients (href `/${adminSegment}/coefficients`), help (href '/aide'). Partner gets exactly 4 items in this order: home, new, proposals, help. Historique is REMOVED from admin per D-27. LabelKey values reuse existing keys where possible: `sidebar.nav.home`, `sidebar.nav.newProposal`, `sidebar.nav.proposals`, `admin.nav.partners.title`, `admin.nav.coefficients.title`, `sidebar.nav.help`.
       - Lucide icon for Aide: `HelpCircle` (per UI-SPEC line 577 recommendation).
       - Render uses `t(item.labelKey, lang)`.

    2. Add `SidebarNav.test.tsx` (or extend if exists) with Tests 1 + 2. Mock the role and assert the rendered nav items by their labelKey or data-testid.

    3. In `src/lib/i18n/dictionaries.ts`, add the net-new keys per UI-SPEC §Copywriting Contract. Group additions in BOTH the FR and EN blocks at structurally appropriate locations (preserve existing alphabetic/grouped ordering — if the file already groups by `admin.*`, `aide.*`, etc., follow that pattern).

       BEFORE adding each key, run `grep -n "'<key>'" src/lib/i18n/dictionaries.ts` to verify it does NOT already exist. The Copywriting Contract pre-marked status for each key (EXISTING vs NEW vs NEW (re-key)). Trust the contract but spot-check 5 random keys to confirm.

       Net-new key groups (~70 keys):
       - `admin.home.stats.partenairesActifs` + `.sublabel` (2)
       - `admin.home.stats.propositionsCeMois` + `.sublabel` (2)
       - `admin.home.stats.derniereModifCoeffs` + `.value` + `.sublabel` (3)
       - `admin.home.activity.title` + `.empty` + `.viewAll` (3)
       - `admin.home.activity.sentence.coefficientModified` + `.partnerInvited` + `.partnerActivated` + `.partnerDeactivated` (4)
       - `admin.home.activity.time.minutesAgo` + `.hoursAgo` + `.daysAgo` (3 — values can REUSE `admin.accounts.last_login.*_ago` literal strings; add the new key with the same value)
       - `admin.partners.page.title` + `.subtitle` + `.invite.cta` + `.search.placeholder` (4)
       - `admin.partners.filter.all` + `.active` + `.invited` + `.inactive` (4)
       - `admin.partners.col.partner` + `.email` + `.created` + `.lastActivity` + `.status` (5)
       - `admin.partners.action.resendInvitation` + `.disableAccount` + `.enableAccount` + `.viewProposals` (4)
       - `admin.partners.empty.zero` + `.filter` + `.clearFilters` (3)
       - `admin.partners.breadcrumb.label` + `.form.submit` + `.form.submit.spinner` + `.form.cancel.confirm` (4)
       - `admin.coefficients.warning.body` + `.warning.dismiss.aria` + `.history.viewAll` (3)
       - `aide.landing.title` + `.subtitle` (2)
       - `aide.landing.card.commencerIci.title` + `.body` + `.cta` (3)
       - `aide.landing.card.creerProposition.title` + `.body` + `.badge` (3)
       - `aide.landing.card.contact.title` + `.body` (2)
       - `aide.commencer-ici.title` + `.lead` + `.breadcrumb` + `.cta.create` (4)
       - `aide.commencer-ici.section.overview.title` + `.body` (2)
       - `aide.commencer-ici.section.step{1,2,3}.title` + `.body` + `.screenshot.alt` (9)
       - `aide.commencer-ici.section.next.title` + `.body` (2)
       - `sidebar.nav.home` + `.newProposal` + `.proposals` (3 — verify-and-reuse first; if any already exist with the correct value, use existing and remove from the add-list)

       For Aide article body content (`aide.commencer-ici.lead`, `.section.overview.body`, `.section.step{1,2,3}.body`, `.section.next.body`): use these draft values for Phase 18 (~500-1000 word total target per D-26; user reviews before execute):

       FR `aide.commencer-ici.lead`: "Vous decouvrez Leasetic ? Ce guide vous accompagne pas a pas dans la creation de votre premiere proposition de financement, depuis la saisie des informations client jusqu'a la generation du PDF."

       EN `aide.commencer-ici.lead`: "New to Leasetic? This guide walks you through creating your first financing proposal step by step, from entering client information to generating the PDF."

       FR `aide.commencer-ici.section.overview.body`: "La creation d'une proposition se deroule en trois etapes. Vous renseignez d'abord les informations du client et les parametres du projet, puis vous validez le calcul du loyer mensuel, et enfin vous verifiez l'apercu PDF avant de finaliser. A chaque etape, votre travail est sauvegarde automatiquement sous forme de brouillon — vous pouvez quitter et reprendre plus tard sans rien perdre."

       EN `aide.commencer-ici.section.overview.body`: "Creating a proposal happens in three steps. You first enter client information and project parameters, then validate the monthly rent calculation, and finally review the PDF preview before finalizing. At each step your work is automatically saved as a draft — you can leave and come back later without losing anything."

       FR `aide.commencer-ici.section.step1.body`: "L'etape Parametres rassemble toutes les informations de base : nom et email du client, intitule du projet, montant HT a financer et duree du leasing (24, 36 ou 48 mois). Validez ces champs pour generer un brouillon — vous recevez immediatement une reference LC unique au format LC-2026-NNN, visible des l'etape de verification. Cette reference est definitive : elle restera attachee a la proposition meme si vous modifiez d'autres parametres par la suite."

       EN `aide.commencer-ici.section.step1.body`: "The Parameters step gathers all the basics: client name and email, project title, amount excl. tax to finance, and lease duration (24, 36, or 48 months). Validating these fields creates a draft — you immediately receive a unique LC reference in the format LC-2026-NNN, visible from the verification step onward. This reference is permanent: it stays attached to the proposal even if you change other parameters later."

       FR `aide.commencer-ici.section.step2.body`: "L'etape Calcul affiche le resultat principal : le loyer mensuel, calcule a partir du coefficient applicable a votre tranche de montant et de duree. La carte centrale met en valeur ce montant en grand format, accompagne de la tranche et du coefficient appliques. Le detail du calcul, juste en dessous, vous permet de comprendre comment le loyer est obtenu — utile pour expliquer la proposition au client. Cliquez sur Modifier pour revenir a l'etape 1 si vous souhaitez ajuster les parametres saisis."

       EN `aide.commencer-ici.section.step2.body`: "The Calculation step shows the main result: the monthly rent, computed from the coefficient that applies to your amount tier and duration. The central card highlights this amount in large format, alongside the applied tier and coefficient. The calculation detail just below helps you understand how the rent is derived — useful for explaining the proposal to the client. Click Edit to go back to step 1 if you need to adjust the parameters."

       FR `aide.commencer-ici.section.step3.body`: "L'etape Verifier affiche un recapitulatif complet a gauche (informations client, details du projet, parametres de calcul) et un apercu du PDF a droite. Verifiez la duree de validite de votre proposition (15, 30 ou 60 jours — 30 jours par defaut). Une fois satisfait, cliquez sur le bouton de finalisation : le PDF est genere, archive, et la proposition apparait dans votre liste avec son numero de reference definitif."

       EN `aide.commencer-ici.section.step3.body`: "The Review step shows a full recap on the left (client information, project details, calculation parameters) and a PDF preview on the right. Check your proposal's validity duration (15, 30, or 60 days — 30 days by default). Once satisfied, click the finalize button: the PDF is generated, archived, and the proposal appears in your list with its final reference number."

       FR `aide.commencer-ici.section.next.body`: "Votre premiere proposition est prete. Vous la retrouverez dans la liste des Propositions, ou vous pouvez la rouvrir pour telecharger a nouveau le PDF. Pour les questions courantes, consultez les autres articles du Centre d'aide ou ecrivez-nous a l'adresse de contact."

       EN `aide.commencer-ici.section.next.body`: "Your first proposal is ready. You'll find it in the Propositions list, where you can reopen it to download the PDF again. For common questions, check the other articles in the Help Center or email us at the contact address."

       Note: the user will polish the French accents during review before execute; planner emits ASCII-safe forms to avoid encoding pitfalls during commit.

    4. Run `npx tsc --noEmit` after additions to confirm `_EnHasAllFrKeys` parity proof compiles cleanly. If a key is in FR but not EN (or vice-versa), the proof FAILS at compile time — fix before committing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/components/ui/SidebarNav.test.tsx</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0 (parity proof green)
    - `grep -c "admin.home.stats.partenairesActifs" src/lib/i18n/dictionaries.ts` returns 2 (one FR, one EN)
    - `grep -c "aide.commencer-ici.section.step1.body" src/lib/i18n/dictionaries.ts` returns 2
    - Sidebar test passes: admin role renders 6 items including Aide, partner role renders 4 items including Aide; Historique NOT in admin nav
    - No duplicate keys introduced (verify by `grep -c "<key>'" file === 2` for a 5-key sample)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → SSR /proposals | `searchParams.user_id` is untrusted; admin-role gate is server-derived from session |
| /proposals SSR → buildListResponse | `_callerRole` MUST be derived from session.user.role, never from request params |
| buildListResponse → DB | userId filter MUST be present in every branch; adminUserIdOverride only honored when _callerRole === 'admin' |
| dictionaries.ts → all consumers | XSS-safe — i18n values are plain strings rendered as React text; no raw HTML injection sinks |
| sessionStorage (downstream Plan 18-05) | client-only; no server trust; no PII stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information disclosure (IDOR via `?user_id=`) | app/(authed)/proposals/page.tsx | mitigate | Per D-11: `?user_id=` is only honored when `session.user.role === 'admin'`. Partner caller's override is IGNORED (Tests 6 + 8 enforce). _callerRole NEVER derived from request params — always from session |
| T-18-01-02 | Information disclosure (cross-partner partner-count leak) | partner-aggregates.ts | accept | Active partner count is admin-only data — only consumed by Admin Home stat tile which is rendered inside `(admin)/[adminSegment]/` route (already gated by `requireAdmin()`). Helper function is not exposed via any API route |
| T-18-01-03 | Information disclosure (cross-partner activity feed leak) | admin-activity.ts | accept | Same as T-18-01-02 — getRecentAdminActivity is consumed only by Admin Home. No API endpoint surfaces this data |
| T-18-01-04 | Information disclosure (commission via Recent activity) | admin-activity.ts sentence projection | mitigate | ActivityRow.sentenceArgs MUST NOT include commission values. The `coefficientModified` sentence references "modified coefficients" but does NOT include numeric commission deltas (those live on /history). Verify ADMIN-09 9-gate suite stays green after Phase 18 |
| T-18-01-05 | Tampering (i18n key shadowing) | dictionaries.ts | mitigate | Verify no duplicate keys via grep gate in <done>. Duplicate keys silently override and can mask bugs. _EnHasAllFrKeys parity proof catches FR/EN mismatch at compile time |
| T-18-01-06 | Denial of service (unbounded UNION scan) | admin-activity.ts | accept | Each source bounded LIMIT 10; partner-status branch bounded `updatedAt >= NOW() - INTERVAL '30 days'`; v1.3 admin volume is small (<100 partner accounts expected). Future v1.4+ may add composite indexes if measured |
| T-18-01-07 | Tampering (sidebar nav config drift) | SidebarNav.tsx | mitigate | Tests 1+2 lock the per-role item set to exactly 6 (admin) and 4 (partner) items in deterministic order; future additions must update both the config AND the tests |
</threat_model>

<verification>
- `npm test -- src/lib/db/queries/ src/lib/api/proposals/ src/components/ui/SidebarNav.test.tsx` exits 0
- `npx tsc --noEmit` exits 0 (TS + _EnHasAllFrKeys parity proof green)
- `grep -n "getMonthlyProposalCountAll\|getActivePartnerCount\|getRecentAdminActivity\|adminUserIdOverride" src/lib/` returns hits in expected files
- `grep -c "Historique" src/components/ui/SidebarNav.tsx | grep -v '^#' | awk '{ if ($1 == 0) exit 0; else exit 1 }'` — Historique must NOT appear in admin sidebar items list (D-27)
- ADMIN-09 9-gate suite remains green: `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
</verification>

<success_criteria>
- 3 query modules ready for Wave-2 surface consumption (proposal-aggregates extended, partner-aggregates + admin-activity created)
- /proposals SSR route gates admin user_id query on session.user.role (D-11)
- Sidebar shows 6 items for admin (incl. Aide) and 4 items for partner (incl. Aide); Historique removed from admin per D-27
- ~70 net-new i18n keys added in FR + EN with parity proof clean; existing admin.accounts.* values preserved for back-compat
- All 8 behavior tests + sidebar tests + existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/18-admin-surfaces/18-01-SUMMARY.md`
</output>
