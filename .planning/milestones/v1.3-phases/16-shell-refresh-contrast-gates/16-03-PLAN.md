---
phase: 16-shell-refresh-contrast-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Shell.tsx
autonomous: true
requirements:
  - SHELL-05
requirements_addressed:
  - SHELL-05
tags:
  - shell
  - footer
  - i18n-reuse
  - light-dark

must_haves:
  truths:
    - "Authed footer renders both the copyright string (existing `shell.footer.copyright` key) and a Mentions légales link (existing `shell.footer.privacy` key) — per D-17."
    - "Footer uses flex layout with justify-content: space-between, align-items: center, padding: 0 24px — per D-18 + Claude's Discretion recommendation."
    - "Copyright sits on the left of the footer; Mentions légales link sits on the right."
    - "Mentions légales link target is `https://leasetic.fr/mentions-legales` (matches the public layout footer at app/(public)/layout.tsx line 81), opens in a new tab with rel='noopener noreferrer'."
    - "Mentions légales link uses inline style { color: 'var(--muted)', textDecoration: 'underline', fontSize: '10.5px' }."
    - "Reuses existing `shell.footer.privacy` i18n key — NO new key created (UI-SPEC discovery supersedes CONTEXT.md D-18's assumption of `shell.footer.mentionsLegales`)."
    - "Footer height stays at --footer-h (48px); background stays --paper; border-top stays 1px var(--border); base text size stays 10.5px; color stays var(--muted) — all pre-existing, unchanged."
    - "Light + dark pair: footer uses --paper / --border / --muted tokens — token cascade handles dark mode automatically."
    - "ADMIN-09 9-gate grep-contract suite stays green (footer is pure presentational, no commission surface)."
  artifacts:
    - path: "src/components/ui/Shell.tsx"
      provides: "Footer extended to flex space-between layout with copyright (left) + Mentions légales link (right)"
      contains: "shell.footer.privacy"
  key_links:
    - from: "src/components/ui/Shell.tsx"
      to: "src/lib/i18n/dictionaries.ts via t('shell.footer.privacy', lang)"
      via: "t() call referencing existing FR/EN key"
      pattern: "t\\(['\"]shell\\.footer\\.privacy['\"], lang\\)"
    - from: "src/components/ui/Shell.tsx"
      to: "https://leasetic.fr/mentions-legales"
      via: "anchor href"
      pattern: "https://leasetic\\.fr/mentions-legales"
---

<objective>
Extend the authed `<Shell>` footer to include a `Mentions légales` link alongside the existing copyright (D-17, D-18). This delivers the footer half of SHELL-05 (Topbar half is covered in Plan 16-02). The implementation reuses the EXISTING `shell.footer.privacy` i18n key (UI-SPEC §Files Modified row explicitly supersedes CONTEXT.md D-18's `shell.footer.mentionsLegales` assumption — the key already exists in `dictionaries.ts` at FR line 284 + EN line 950, and is already used by `app/(public)/layout.tsx` line 81 for the public footer).

Purpose: Parity between authed footer and public footer; ship the Figma `9:46` footer treatment.
Output: One source file modified (`src/components/ui/Shell.tsx` footer block). No new i18n keys. No new routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md
@.planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md

<interfaces>
<!-- Current authed footer code (Shell.tsx lines ~102-118, confirmed 2026-05-22): -->
```tsx
<footer
  style={{
    gridRow: '3',
    gridColumn: '2',
    background: 'var(--paper)',
    borderTop: '1px solid var(--border)',
    height: 'var(--footer-h)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '10.5px',
    color: 'var(--muted)',
  }}
>
  {t('shell.footer.copyright', lang)}
</footer>
```

<!-- Existing i18n key (confirmed via grep on src/lib/i18n/dictionaries.ts 2026-05-22): -->
<!--   FR line 284: 'shell.footer.privacy': 'Mentions légales' -->
<!--   EN line 950: 'shell.footer.privacy': 'Privacy notice'  -->
<!-- Reference public footer for analog link pattern: app/(public)/layout.tsx around line 81. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Shell.tsx footer with Mentions légales link (D-17, D-18)</name>
  <files>src/components/ui/Shell.tsx</files>
  <read_first>
    - src/components/ui/Shell.tsx (full file — confirm current footer block, grid wiring, t() import, lang prop sourcing)
    - app/(public)/layout.tsx (read around line 81 — the analog Mentions légales link to mirror: target URL, target attribute, rel attribute, inline style)
    - src/lib/i18n/dictionaries.ts (confirm `shell.footer.privacy` key exists at FR line 284 and EN line 950; confirm `shell.footer.copyright` is the existing key for the copyright text)
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decisions D-17, D-18; Claude's Discretion recommendation for flex space-between)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (Component Contracts > Shell footer extension subsection — the locked layout values + key-reuse discovery note)
  </read_first>
  <action>
    Modify the existing `<footer>` element in `src/components/ui/Shell.tsx` (currently lines ~102-118). Apply these specific changes to the inline style object:

    1. Change `justifyContent: 'center'` to `justifyContent: 'space-between'` (D-18 layout — copyright left, link right).
    2. Add `padding: '0 24px'` to the inline style (D-18 — 24px horizontal padding so left/right content sits inside the viewport edge by 24px, matching UI-SPEC §Spacing).
    3. Keep all other style properties identical: `gridRow: '3'`, `gridColumn: '2'`, `background: 'var(--paper)'`, `borderTop: '1px solid var(--border)'`, `height: 'var(--footer-h)'`, `display: 'flex'`, `alignItems: 'center'`, `fontSize: '10.5px'`, `color: 'var(--muted)'`. These are pre-existing values and stay.

    4. Replace the current `{t('shell.footer.copyright', lang)}` body with two sibling elements:
       - Left: `<span>{t('shell.footer.copyright', lang)}</span>`
       - Right: `<a href="https://leasetic.fr/mentions-legales" target="_blank" rel="noopener noreferrer" style={{ color: 'var(--muted)', textDecoration: 'underline', fontSize: '10.5px' }}>{t('shell.footer.privacy', lang)}</a>`

    5. Do NOT add a new i18n key. The `shell.footer.privacy` key already exists in both FR and EN dictionaries — confirmed via grep (FR line 284: 'Mentions légales'; EN line 950: 'Privacy notice'). UI-SPEC §Files Modified explicitly supersedes CONTEXT.md D-18's `shell.footer.mentionsLegales` assumption.

    6. Do NOT create a new in-app route. The link goes to the existing public-marketing URL `https://leasetic.fr/mentions-legales` — same target as `app/(public)/layout.tsx` (line ~81). No `target="_blank"` warning needed since this is intentional (external-marketing link parity with the public layout).

    7. Do NOT modify any other Shell.tsx grid wiring (sidebar slot, topbar slot, main slot, footer height, --footer-h, --topbar-h, --shell-sidebar-* tokens). Only the `<footer>` element's inline style and its children change.

    8. Preserve the `lang` prop propagation through the existing `Shell` component signature; the `t(...)` calls already receive `lang` per the current implementation.

    9. ADMIN-09: the footer is pure presentational and renders no commission-related strings. The 9-gate grep-contract suite must stay green.
  </action>
  <verify>
    <automated>grep -c "shell.footer.privacy" src/components/ui/Shell.tsx; grep -c "justify-content:\s*space-between\|justifyContent: 'space-between'" src/components/ui/Shell.tsx; grep -c "leasetic.fr/mentions-legales" src/components/ui/Shell.tsx; grep -c "rel=\"noopener noreferrer\"\|rel='noopener noreferrer'" src/components/ui/Shell.tsx; npm run build 2>&amp;1 | tail -10; npm run test -- --run tests/admin-09-grep-contracts.test.ts 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Footer references the existing privacy i18n key: `grep -c "shell.footer.privacy" src/components/ui/Shell.tsx` returns at least 1.
    - Footer uses space-between layout: `grep -c "justifyContent: 'space-between'\|justify-content:\s*space-between" src/components/ui/Shell.tsx` returns at least 1.
    - Footer contains the link target: `grep -c "leasetic.fr/mentions-legales" src/components/ui/Shell.tsx` returns at least 1.
    - Link uses external-safe attributes: `grep -c "rel=\"noopener noreferrer\"\|rel='noopener noreferrer'" src/components/ui/Shell.tsx` returns at least 1, AND `grep -c "target=\"_blank\"\|target='_blank'" src/components/ui/Shell.tsx` returns at least 1.
    - Footer contains 24px horizontal padding: `grep -c "padding: '0 24px'\|padding:\s*0\s*24px" src/components/ui/Shell.tsx` returns at least 1.
    - Existing copyright key is preserved: `grep -c "shell.footer.copyright" src/components/ui/Shell.tsx` returns at least 1.
    - NO new i18n key was added — `shell.footer.mentionsLegales` substring must NOT appear in Shell.tsx OR dictionaries.ts: `grep -c "shell.footer.mentionsLegales" src/components/ui/Shell.tsx src/lib/i18n/dictionaries.ts` returns 0.
    - No commission strings introduced: `grep -ci "commission" src/components/ui/Shell.tsx` returns 0.
    - Project builds: `npm run build` exits 0.
    - ADMIN-09 9-gate suite stays green: `npm run test -- --run tests/admin-09-grep-contracts.test.ts` exits 0.
    - Existing Shell composition tests (if any) still pass: `npm run test -- --run src/components/ui/Shell` exits 0 OR "no tests found" (Shell.tsx may not have a dedicated test file in v1.2; that's acceptable).
  </acceptance_criteria>
  <done>Shell.tsx footer renders flex space-between with copyright left + Mentions légales link right (existing `shell.footer.privacy` key, external leasetic.fr target); no new i18n key; build passes; 9-gate suite green.</done>
</task>

</tasks>

<verification>
- `grep -c "shell.footer.privacy" src/components/ui/Shell.tsx` → ≥1
- `grep -c "shell.footer.mentionsLegales" src/components/ui/Shell.tsx src/lib/i18n/dictionaries.ts` → 0 (no new key)
- `grep -c "leasetic.fr/mentions-legales" src/components/ui/Shell.tsx` → ≥1
- `npm run build` passes
- `npm run test -- --run tests/admin-09-grep-contracts.test.ts` passes
</verification>

<success_criteria>
- Footer matches Figma `9:46` visual treatment: flex row, copyright left, Mentions légales link right, 24px horizontal padding, 10.5px muted text, link with underline.
- No new i18n key added — `shell.footer.privacy` reused from existing FR/EN dictionary entries.
- Link target matches the public layout's external `leasetic.fr/mentions-legales` URL — parity achieved.
- Light + dark behavior preserved via existing token cascade.
- 9-gate grep-contract suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shell-refresh-contrast-gates/16-03-SUMMARY.md` recording: the exact line-range edited in Shell.tsx (before/after), confirmation that `shell.footer.privacy` was reused (not duplicated), confirmation that no new i18n key was added, and confirmation that the 9-gate suite remained green.
</output>
