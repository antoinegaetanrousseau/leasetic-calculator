---
phase: 16-shell-refresh-contrast-gates
plan: 04
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - app/(admin)/[adminSegment]/page.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements:
  - SHELL-03
requirements_addressed:
  - SHELL-03
tags:
  - shell
  - admin
  - reference-adopter
  - page-hero
  - i18n

must_haves:
  truths:
    - "app/(admin)/[adminSegment]/page.tsx consumes PageHero as its hero element — replacing the inline h1 + p block that was there before — per D-06."
    - "Per D-07: foundation-only scope. Phase 16 migrates ONLY the admin home to PageHero — partner home (app/(authed)/page.tsx), wizard steps, /proposals, /partners, /partners/new, /coefficients, and /history are explicitly OUT of scope for Phase 16 (Phase 17 / Phase 18 own those migrations as part of their own page refreshes). This plan is the foundation-only reference-adopter exemplar."
    - "PageHero is called with eyebrow=t('admin.home.eyebrow', lang), title=t('admin.home.title', lang), subtitle=t('admin.home.subtitle', lang) — per UI-SPEC §Reference adopter."
    - "PageHero is NOT given an actions prop on admin home in Phase 16 — the Figma 41:46 'Nouvelle proposition' CTA is a Phase 18 enhancement, deferred (CONTEXT.md §Specifics)."
    - "New i18n key `admin.home.eyebrow` is added to BOTH FR and EN dictionaries with value 'ADMIN' (Claude's Discretion recommendation in CONTEXT.md)."
    - "Existing `admin.home.title` (FR/EN: 'Administration') and `admin.home.subtitle` (FR: 'Gérez les paramètres globaux et les comptes' / EN: 'Manage global parameters and accounts') keys are reused — no duplication."
    - "The existing 3-card AdminNavCard grid below the hero (coefficients / partners / history) is preserved unchanged (marginTop 32, gap 24, gridTemplateColumns 'repeat(3, 1fr)', maxWidth 1040)."
    - "Existing requireAdmin() defense-in-depth call is preserved at the top of the page component."
    - "Existing `export const dynamic = 'force-dynamic'` is preserved."
    - "Existing `export const metadata` block is preserved unchanged."
    - "Light + dark pair: PageHero uses --ink / --muted / --gd tokens, automatic via token cascade — no per-page dark CSS."
    - "ADMIN-09 9-gate grep-contract suite stays green (admin home was already commission-free; PageHero adoption introduces no new commission surface)."
    - "i18n parity proof `_EnHasAllFrKeys` (compile-time) stays green after adding the new key to both FR and EN."
  artifacts:
    - path: "app/(admin)/[adminSegment]/page.tsx"
      provides: "Admin home migrated to PageHero consumer (reference adopter for SHELL-03)"
      contains: "PageHero"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "New `admin.home.eyebrow` FR + EN entries (both with value 'ADMIN')"
      contains: "admin.home.eyebrow"
  key_links:
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "src/components/ui/PageHero.tsx"
      via: "import { PageHero } from '@/components/ui/PageHero'"
      pattern: "import.*PageHero.*from.*['\"]@/components/ui/PageHero['\"]"
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "src/lib/i18n/dictionaries.ts via t('admin.home.eyebrow', lang)"
      via: "t() call referencing the newly-added key"
      pattern: "t\\(['\"]admin\\.home\\.eyebrow['\"], lang\\)"
---

<objective>
Migrate the admin home page `app/(admin)/[adminSegment]/page.tsx` to consume the new PageHero primitive (Plan 16-01), replacing its current inline h1 + p heading block. This is the ONE reference adopter that validates the primitive's contract in Phase 16 (D-06, D-07 — foundation-only scope). Phase 17 migrates partner home + /proposals + wizard steps; Phase 18 migrates /partners + /partners/new + /coefficients + /history.

Purpose: Prove the PageHero interface works against a real consumer with all three optional text props (eyebrow + title + subtitle), and lock the admin-home eyebrow text ("ADMIN") into the i18n dictionary.
Output: Two source files modified — the admin home page (swap inline hero for PageHero) and the i18n dictionaries (add one new FR + EN key pair).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md
@.planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md
@.planning/phases/16-shell-refresh-contrast-gates/16-01-PLAN.md

<interfaces>
PageHero (from Plan 16-01):

  import { PageHero } from '@/components/ui/PageHero';
  // Props: title (required string), subtitle (optional string), eyebrow (optional string), actions (optional ReactNode), children (reserved).

Current admin home inline hero (to be replaced — from page.tsx lines ~42-65 confirmed 2026-05-22):

  <h1 style with fontSize 32, fontWeight 700, lineHeight 1.2, color var(--ink), margin 0> { t('admin.home.title', lang) } </h1>
  <p style with fontSize 16, lineHeight 1.55, color var(--muted), marginTop 8, marginBottom 0> { t('admin.home.subtitle', lang) } </p>

The 3-card AdminNavCard grid below (lines ~67-95) stays unchanged.

Existing i18n keys (confirmed via grep on src/lib/i18n/dictionaries.ts 2026-05-22):
  FR line 437: 'admin.home.title': 'Administration'
  FR line 438: 'admin.home.subtitle': 'Gérez les paramètres globaux et les comptes'
  EN line 1091: 'admin.home.title': 'Administration'
  EN line 1092: 'admin.home.subtitle': 'Manage global parameters and accounts'

NEW key needed:
  FR + EN: 'admin.home.eyebrow': 'ADMIN'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin.home.eyebrow FR + EN i18n keys to dictionaries.ts</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts (around FR lines 437-438 and EN lines 1091-1092 — confirm the surrounding `admin.home.*` keys for insertion placement; also confirm the `_EnHasAllFrKeys` compile-time proof structure at module load so the new key is added symmetrically)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (§Copywriting Contract row: `admin.home.eyebrow` FR 'ADMIN' / EN 'ADMIN')
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (Claude's Discretion recommendation: eyebrow text = 'ADMIN' for consistency with the Topbar ADMIN pill)
  </read_first>
  <action>
    Add ONE new i18n key pair to src/lib/i18n/dictionaries.ts:

    1. FR dictionary: Insert the literal entry `'admin.home.eyebrow': 'ADMIN',` immediately above the existing `'admin.home.title': 'Administration',` line (around FR line 437). Maintain alphabetical/grouping conventions used elsewhere in the file (group all `admin.home.*` keys together; if the file orders keys lexicographically within a sub-group, `eyebrow` comes before `subtitle` and `title` alphabetically).

    2. EN dictionary: Insert the literal entry `'admin.home.eyebrow': 'ADMIN',` in the symmetric position relative to the FR insertion — immediately above the existing `'admin.home.title': 'Administration',` line (around EN line 1091).

    The FR and EN values are both the literal string 'ADMIN' (same in both languages, since it is a single-word badge label; UI-SPEC §Copywriting Contract row confirms FR='ADMIN', EN='ADMIN'; CONTEXT.md Claude's Discretion locks this choice).

    Do NOT modify any other key. Do NOT introduce new sub-objects. Do NOT add namespace nesting (the file uses flat dotted keys per the existing convention).

    After the insertion, the file's compile-time `_EnHasAllFrKeys` proof MUST still pass — both FR and EN now have `admin.home.eyebrow` and the type-level set-equality remains satisfied.
  </action>
  <verify>
    <automated>grep -c "'admin.home.eyebrow': 'ADMIN'" src/lib/i18n/dictionaries.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both FR and EN now define `admin.home.eyebrow` with value 'ADMIN': `grep -c "'admin.home.eyebrow': 'ADMIN'" src/lib/i18n/dictionaries.ts` returns exactly 2.
    - The pre-existing `admin.home.title` and `admin.home.subtitle` keys remain present in both languages: `grep -c "'admin.home.title':" src/lib/i18n/dictionaries.ts` returns 2; `grep -c "'admin.home.subtitle':" src/lib/i18n/dictionaries.ts` returns 2.
    - Project type-check passes (i18n parity proof intact): `npx tsc --noEmit` exits 0.
    - No commission strings introduced by this edit: a `git diff src/lib/i18n/dictionaries.ts` after the change must NOT contain the substring `commission` (case-insensitive).
  </acceptance_criteria>
  <done>dictionaries.ts has the new FR + EN admin.home.eyebrow entries (both = 'ADMIN'); tsc passes; i18n parity proof intact.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate admin home page to PageHero consumer (D-06)</name>
  <files>app/(admin)/[adminSegment]/page.tsx</files>
  <read_first>
    - app/(admin)/[adminSegment]/page.tsx (full file — to see current inline h1 + p at lines ~42-65, the surrounding outer div wrapper, and the 3-card AdminNavCard grid at lines ~67-95)
    - src/components/ui/PageHero.tsx (the just-created primitive from Plan 16-01 — to confirm the import path and the PageHeroProps surface)
    - .planning/phases/16-shell-refresh-contrast-gates/16-CONTEXT.md (decision D-06 + §Specifics > "PageHero usage on admin home" example + Claude's Discretion: no `Nouvelle proposition` CTA on admin home in Phase 16)
    - .planning/phases/16-shell-refresh-contrast-gates/16-UI-SPEC.md (§Reference adopter section — the locked PageHero call signature with the 3 t() props)
    - .planning/phases/16-shell-refresh-contrast-gates/16-01-PLAN.md (Plan 16-01 — confirm the PageHero export name and module path are exactly `import { PageHero } from '@/components/ui/PageHero'`)
  </read_first>
  <action>
    Modify app/(admin)/[adminSegment]/page.tsx:

    1. Add the PageHero import to the import block at the top of the file. The new import line: `import { PageHero } from '@/components/ui/PageHero';`. Place it adjacent to the existing `import { AdminNavCard } from '@/components/ui/AdminNavCard';` line (grouping the `ui` primitive imports together).

    2. Replace the inline h1 + p block (currently lines ~42-65 inside the returned outer div) with a single PageHero call. The call has exactly three props in this order: eyebrow set to t('admin.home.eyebrow', lang), title set to t('admin.home.title', lang), subtitle set to t('admin.home.subtitle', lang). Self-closing JSX tag (no children).

    Do NOT pass an `actions` prop in Phase 16 — the Figma 41:46 'Nouvelle proposition' CTA is a Phase 18 enhancement, explicitly deferred per CONTEXT.md §Specifics ("keep parity with v1.2 admin home (no Nouvelle proposition CTA from admin)").

    3. Preserve the AdminNavCard grid block below the hero (the wrapper div with inline style: display 'grid', gridTemplateColumns 'repeat(3, 1fr)', gap 24, maxWidth 1040, marginTop 32 — containing three AdminNavCard instances for coefficients / partners / history). The marginTop 32 on this grid relies on the hero block above ending with marginBottom 32 — PageHero already provides marginBottom 32 (D-04), so the 32px gap between hero and grid is preserved.

    4. Preserve all other page-level code: the `export const dynamic = 'force-dynamic'` line, the `export const metadata = { ... }` block, the `interface PageProps` declaration, the `async function AdminHomePage({ params }: PageProps)` signature, the `const { adminSegment } = await params` destructure, the `await requireAdmin()` call (AUTH-15 defense-in-depth), the `const lang = await getCurrentLang()` call, and the outer return-wrapper div. None of these change.

    5. No new state, no new data fetching, no new server actions. The migration is purely a JSX swap.

    6. ADMIN-09: the admin home was already commission-free (confirmed via the existing top-of-file JSDoc block). PageHero is also commission-free per Plan 16-01. The 9-gate grep-contract suite must remain green.

    7. After the swap, the inline h1 and p elements (used only for the hero) are REMOVED from this file. There must be zero `<h1` substrings remaining in app/(admin)/[adminSegment]/page.tsx (the hero is now rendered by PageHero internally as h1 — but the literal `<h1` tag no longer appears in this consumer file).
  </action>
  <verify>
    <automated>grep -c "import { PageHero } from '@/components/ui/PageHero'" 'app/(admin)/[adminSegment]/page.tsx' &amp;&amp; grep -c "PageHero" 'app/(admin)/[adminSegment]/page.tsx' &amp;&amp; npm run build &amp;&amp; npm run test -- --run tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The page imports PageHero from the canonical alias path: `grep -c "import { PageHero } from '@/components/ui/PageHero'" 'app/(admin)/[adminSegment]/page.tsx'` returns 1.
    - The page renders a PageHero element: `grep -c "<PageHero" 'app/(admin)/[adminSegment]/page.tsx'` returns 1.
    - The page references the new eyebrow key: `grep -c "admin.home.eyebrow" 'app/(admin)/[adminSegment]/page.tsx'` returns 1.
    - The inline h1 tag was removed from this consumer (hero is now rendered by PageHero): `grep -c "<h1" 'app/(admin)/[adminSegment]/page.tsx'` returns 0.
    - The page still references the pre-existing title and subtitle keys: `grep -c "admin.home.title\|admin.home.subtitle" 'app/(admin)/[adminSegment]/page.tsx'` returns at least 2.
    - The page still calls requireAdmin (defense-in-depth preserved): `grep -c "requireAdmin" 'app/(admin)/[adminSegment]/page.tsx'` returns at least 1.
    - The page still exports dynamic = 'force-dynamic': `grep -c "export const dynamic = 'force-dynamic'" 'app/(admin)/[adminSegment]/page.tsx'` returns 1.
    - The 3-card AdminNavCard grid is preserved: `grep -c "<AdminNavCard" 'app/(admin)/[adminSegment]/page.tsx'` returns 3.
    - No commission strings introduced: `grep -ci "commission" 'app/(admin)/[adminSegment]/page.tsx'` matches the pre-edit baseline (Phase 16 introduces zero commission references in this consumer file).
    - Project builds: `npm run build` exits 0.
    - The 9-gate grep-contract suite stays green: `npm run test -- --run tests/admin-09-grep-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>Admin home page renders PageHero with eyebrow/title/subtitle from i18n; inline h1+p removed; AdminNavCard grid preserved; requireAdmin and force-dynamic preserved; build passes; 9-gate suite green.</done>
</task>

</tasks>

<verification>
- `grep -c "'admin.home.eyebrow': 'ADMIN'" src/lib/i18n/dictionaries.ts` returns 2 (FR + EN).
- `grep -c "<PageHero" 'app/(admin)/[adminSegment]/page.tsx'` returns 1.
- `grep -c "<h1" 'app/(admin)/[adminSegment]/page.tsx'` returns 0.
- `npm run build` passes.
- `npm run test -- --run tests/admin-09-grep-contracts.test.ts` passes (9-gate suite stays green).
- `npx tsc --noEmit` exits 0 (i18n parity proof intact).
</verification>

<success_criteria>
- Admin home page renders the v1.3 hero via PageHero with eyebrow "ADMIN" + existing title/subtitle keys.
- ONE new i18n key pair added (admin.home.eyebrow FR + EN, both 'ADMIN'); no other key changes.
- AdminNavCard grid, requireAdmin defense-in-depth, force-dynamic, and metadata block all preserved.
- Light + dark behavior preserved via PageHero's token-cascade design.
- ADMIN-09 9-gate grep-contract suite remains green.
- PageHero primitive contract (Plan 16-01) validated against a real consumer with all three optional text props in play.
</success_criteria>

<output>
After completion, create `.planning/phases/16-shell-refresh-contrast-gates/16-04-SUMMARY.md` recording: the exact line-ranges edited in both files, the diff size in lines, confirmation that requireAdmin and force-dynamic and metadata are preserved, confirmation that the new i18n key has values 'ADMIN' in both languages, and confirmation that the 9-gate suite stayed green.
</output>
