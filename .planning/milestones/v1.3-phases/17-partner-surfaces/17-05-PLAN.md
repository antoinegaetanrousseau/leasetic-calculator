---
phase: 17-partner-surfaces
plan: 05
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - app/(authed)/proposals/new/parametres/page.tsx
autonomous: true
requirements: [WIZ-01]
must_haves:
  truths:
    - "Wizard step 1 renders <PageHero eyebrow='ÉTAPE 1 SUR 3' title=... subtitle=... /> at the top"
    - "<Stepper> sits as a sibling BELOW PageHero (not composed inside)"
    - "Form card JSX shape (sections ● INFORMATIONS CLIENT, ● DÉTAILS DU PROJET, segmented duration pill, WizardActionBar) is UNCHANGED structurally"
    - "Existing wizard tests continue to pass; no regression on form behavior"
  artifacts:
    - path: app/(authed)/proposals/new/parametres/page.tsx
      provides: "WIZ-01 visual refresh — PageHero adopter + repositioned Stepper; form structure preserved"
      contains: "PageHero"
  key_links:
    - from: app/(authed)/proposals/new/parametres/page.tsx
      to: src/components/ui/PageHero.tsx (Phase 16 primitive)
      via: "import + JSX call replacing inline <h1>+<p> block"
      pattern: "PageHero"
---

<objective>
Repaint wizard step 1 to adopt `<PageHero eyebrow="ÉTAPE 1 SUR 3" title=... subtitle=... />` (WIZ-01 + D-15 + D-19), replacing the inline `<h1>+<p>` heading block. Per D-15 explicit instruction this is **REPAINT ONLY** — no JSX restructure to the form card, no semantic changes to inputs. Stepper repositions as a sibling below PageHero.

Purpose: Brings step 1 onto the v1.3 PageHero adopter pattern (consistent with Partner Home, /proposals, and the other two wizard steps). The form-card content stays intact so all existing wizard tests + ADMIN-09 invariants are preserved by construction.
Output: One file modified. Minimal diff (≈15-line replacement + 1 import).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/17-partner-surfaces/17-PATTERNS.md
@.planning/phases/17-partner-surfaces/17-UI-SPEC.md
@.planning/phases/17-02-SUMMARY.md

<interfaces>
<!-- From src/components/ui/PageHero.tsx (Phase 16) -->
export interface PageHeroProps {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  actions?: ReactNode;
  children?: ReactNode;  // reserved
}
// Renders with marginBottom: 32 baked in

<!-- From src/components/ui/Stepper.tsx (Phase 11) -->
- Sibling of PageHero (NOT composed inside)
- Props: currentStep, completedSteps, lang, hrefForStep

<!-- Existing inline heading block at app/(authed)/proposals/new/parametres/page.tsx:178-197 -->
- <h1>{t('wizard.step1.title', lang)}</h1>
- <p>{t('wizard.step1.subtitle', lang)}</p>
- These two lines are the ONLY block being replaced
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageHero on wizard step 1 + reposition Stepper (WIZ-01, D-15)</name>
  <files>app/(authed)/proposals/new/parametres/page.tsx</files>
  <read_first>
    - app/(authed)/proposals/new/parametres/page.tsx (FULL file — verify the exact current location of the inline <h1>+<p> heading block, currently around lines 178-197; verify the Stepper block at lines ~199-208; verify the form card structure at lines ~211-217 STAYS UNCHANGED)
    - src/components/ui/PageHero.tsx (props interface verbatim)
    - app/(admin)/[adminSegment]/page.tsx (the Phase 16 PageHero adopter pattern — copy the import line + the PageHero call style)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-15 explicit "repaint only — NO JSX restructure"; D-19 PageHero + Stepper sibling rule)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (`Wizard step 1` section — layout sequence; existing keys verification)
    - .planning/phases/17-partner-surfaces/17-PATTERNS.md (`app/(authed)/proposals/new/parametres/page.tsx MODIFY` section — exact BEFORE/AFTER replacement excerpt)
    - .planning/phases/17-02-SUMMARY.md (verify which i18n keys exist: `wizard.step1.eyebrow`, `wizard.step1.title`, `wizard.step1.subtitle`)
  </read_first>
  <action>
    Make exactly these changes per D-15 + UI-SPEC `Wizard step 1` + 17-PATTERNS.md `parametres/page.tsx MODIFY` section:

    1. ADD import (top of file): `import { PageHero } from '@/components/ui/PageHero';`

    2. REPLACE the inline heading block (currently around lines 178-197): the two-element block consisting of an `<h1>` rendering `t('wizard.step1.title', lang)` and a `<p>` rendering `t('wizard.step1.subtitle', lang)` becomes a single `<PageHero>` call with three props:
       - `eyebrow={t('wizard.step1.eyebrow', lang)}` (resolves to `ÉTAPE 1 SUR 3` FR / `STEP 1 OF 3` EN — key added in Plan 02; per UI-SPEC hardcoded `"ÉTAPE 1 SUR 3"` is an acceptable alternative — choose the i18n-keyed approach for EN consistency)
       - `title={t('wizard.step1.title', lang)}`
       - `subtitle={t('wizard.step1.subtitle', lang)}`

       Refer to the exact code excerpt in 17-PATTERNS.md `parametres/page.tsx MODIFY` section for the precise BEFORE/AFTER strings.

    3. VERIFY the Stepper block (around lines 199-208) stays where it is — as a sibling BELOW PageHero. PageHero has `marginBottom: 32` baked in, so the Stepper inherits proper spacing automatically. Do NOT compose Stepper inside PageHero (D-19 rule).

    4. DO NOT TOUCH:
       - The form card JSX (lines ~211-217 with `<ProposalFormProvider>` + `<WizardStep1Wiring>`)
       - The `● INFORMATIONS CLIENT` / `● DÉTAILS DU PROJET` section headers (already use `.ctitle` + `--gd-text` per Phase 16 contrast remediation — verify, no change needed)
       - The segmented duration pill (24/36/48 mois — Phase 13 D-13 schema-locked whitelist)
       - The WizardActionBar at the bottom
       - Any imports beyond adding `PageHero`

    5. The diff for this task should be approximately: 1 added import line + ~8-line block replacement = ~10 net lines changed. Anything more = scope creep — revisit.
  </action>
  <verify>
    <automated>npm test -- "app/(authed)/proposals/new/parametres/"</automated>
  </verify>
  <done>
    - `grep -c "PageHero" "app/(authed)/proposals/new/parametres/page.tsx"` returns >= 2 (1 import + 1 JSX usage)
    - `grep -c "^[[:space:]]*<h1" "app/(authed)/proposals/new/parametres/page.tsx" | grep -v '^#'` returns 0 (no leftover inline <h1>)
    - `grep -c "wizard.step1.eyebrow" "app/(authed)/proposals/new/parametres/page.tsx"` returns 1 (eyebrow keyed)
    - Existing wizard step 1 tests continue to pass (no behavior regression)
    - `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
    - Form card structure (sections, segmented pill, WizardActionBar) UNCHANGED — diff confined to header block + 1 import
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | session via requireUser (trusted); no new untrusted input introduced |
| PageHero primitive → caller | server-side text only; React auto-escapes; PageHero ships zero scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Information disclosure (XSS via i18n) | PageHero title/subtitle/eyebrow props | mitigate | All values pass through `t(key, lang)` which returns static dictionary strings; React auto-escapes when rendered; no raw-HTML injection paths used |
| T-17-05-02 | Tampering (form contract drift) | wizard step 1 form structure | mitigate | D-15 explicitly requires REPAINT ONLY — no form structure changes; existing wizard tests catch any regression in form behavior; ADMIN-09 9-gate suite unchanged (this surface has no commission data) |
| T-17-05-03 | Information disclosure (commission leak) | wizard step 1 | mitigate | Step 1 is the PARAMETERS-INPUT step — commission is calculated server-side at step 2; this plan does not touch step 2 or 3; ADMIN-09 envelope untouched by repaint |
</threat_model>

<verification>
- `npm test -- "app/(authed)/proposals/new/parametres/"` exits 0 (all existing wizard step 1 tests green)
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0
- Diff inspection: `git diff app/(authed)/proposals/new/parametres/page.tsx` shows ≤ ~15 net lines changed
- Visual (Plan 08): `npm run dev` → visit `/proposals/new/parametres` → see ÉTAPE 1 SUR 3 eyebrow + title + subtitle in PageHero chrome, Stepper directly below, form card unchanged
</verification>

<success_criteria>
- Wizard step 1 PageHero-adopted with `ÉTAPE 1 SUR 3` eyebrow per Figma `35:46`
- Form structure UNCHANGED — no regression on input behavior, validation, or save-as-draft
- ADMIN-09 9-gate suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/17-partner-surfaces/17-05-SUMMARY.md`
</output>
