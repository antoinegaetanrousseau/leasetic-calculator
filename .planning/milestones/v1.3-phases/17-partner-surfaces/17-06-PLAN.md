---
phase: 17-partner-surfaces
plan: 06
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - app/(authed)/proposals/new/calcul/page.tsx
autonomous: true
requirements: [WIZ-02, WIZ-05]
must_haves:
  truths:
    - "Wizard step 2 renders <PageHero eyebrow='ÉTAPE 2 SUR 3' title=... subtitle=... /> at the top"
    - "<Stepper> sits as a sibling BELOW PageHero with marginBottom 32px"
    - "Net-new loyer-mensuel hero card: 36px/700/--teal value + sublabel + Tranche/Coefficient pill chip top-right"
    - "Net-new Détail du calcul card with 5 rows including Commission apporteur (non visible client) — D-12 partner-facing relaxation preserved"
    - "Paramètres saisis recap card with ← Modifier link → /proposals/new/parametres"
    - "WizardActionBar at bottom UNCHANGED"
    - "ADMIN-09 D-12 envelope + 9-gate grep-contract suite stay green"
  artifacts:
    - path: app/(authed)/proposals/new/calcul/page.tsx
      provides: "WIZ-02 visual refresh + restructure — PageHero adopter + loyer hero card + Détail card + Paramètres saisis recap"
      contains: "PageHero"
  key_links:
    - from: app/(authed)/proposals/new/calcul/page.tsx
      to: existing wizard step 2 compute pipeline (lines 90-172)
      via: "preserved synchronous calc results threaded into the new card layout"
      pattern: "computeLoyer|trancheNumber|coefficientNumber"
    - from: app/(authed)/proposals/new/calcul/page.tsx
      to: src/components/ui/PageHero.tsx + RecapSection.tsx (existing primitives)
      via: "imports + JSX calls"
      pattern: "RecapSection|PageHero"
---

<objective>
Restructure wizard step 2 per Figma `39:46` (WIZ-02 + D-16): adopt `<PageHero>`, add the net-new loyer-mensuel hero card with 36px/700/--teal value + Tranche/Coefficient pill chip, add the net-new Détail du calcul card (5 rows including the D-12 partner-facing Commission row), and refresh the Paramètres saisis recap. The Phase 13 ADMIN-09 D-12 envelope (partner-facing commission relaxation on step 2/3 ONLY) is preserved — no other commission surfaces created (WIZ-05).

Purpose: WIZ-02 net-new card structure is the largest visual change in the wizard refresh. The Tranche/Coefficient chip + Détail card are new JSX structures the partner sees pre-finalize.
Output: One file restructured. The existing compute pipeline (lines ~90-172) is preserved verbatim — only the JSX layout below it changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-partner-surfaces/17-CONTEXT.md
@.planning/phases/17-partner-surfaces/17-PATTERNS.md
@.planning/phases/17-partner-surfaces/17-UI-SPEC.md
@.planning/phases/17-02-SUMMARY.md

<interfaces>
<!-- From src/components/ui/PageHero.tsx (Phase 16 — reuse verbatim) -->
PageHeroProps: { eyebrow?, title, subtitle?, actions?, children? }

<!-- From app/(authed)/proposals/new/_components/RecapSection.tsx (Phase 13 — reused) -->
- Existing primitive: sectionTitle, modifierLink, rows: { label, value }[], rowSublabels?: { [index]: string }
- Per UI-SPEC: may need optional `lastRowDivider?: boolean` prop — planner's choice (UI-SPEC allows custom JSX wrapper as alternative)

<!-- From existing app/(authed)/proposals/new/calcul/page.tsx -->
- Lines 90-172: compute pipeline (computeLoyer + tranche detection + coefficient lookup) — DO NOT TOUCH
- Lines 292-311: current inline <h1>+<p> heading block → replaced by PageHero
- Lines 333-451: current hero loyer card with conditional state-based rendering → restructure per UI-SPEC
- Lines 381-400: existing Tranche/Coefficient pill chip implementation (.chip-language; position:absolute) → relocate to flexbox top-right
- Lines 459-472: existing Détail du calcul `<RecapSection>` call with detailRows + commission sublabel — UI-SPEC §Step 2 step 4 confirms this primitive call stays; only the i18n key renames per Plan 02 verify
- Lines 479-490: existing Paramètres saisis recap — UNCHANGED structurally

<!-- ADMIN-09 D-12 partner-facing envelope (Phase 13) -->
- Allowed exception: wizard step 2 + step 3 commission visibility to deal-owner partner ONLY
- The Détail du calcul row 2 (Commission apporteur (non visible client)) is the allowed exception
- 9-gate suite at tests/admin-09-grep-contracts.test.ts must remain green

<!-- From app/globals.css -->
- .chip-language at lines 390-393: rgba(45,122,140,0.10) bg + var(--teal) color (reused for Tranche/Coefficient chip — NO new class needed)
- .card chrome: 28px padding (locked)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageHero + restructure step 2 hero card (D-16 part 1)</name>
  <files>app/(authed)/proposals/new/calcul/page.tsx</files>
  <read_first>
    - app/(authed)/proposals/new/calcul/page.tsx (FULL file — confirm exact line ranges before editing; compute pipeline 90-172 stays; inline heading 292-311 → replace; hero card 333-451 → restructure; chip 381-400 → relocate; Détail 459-472 → minor i18n key updates; Paramètres saisis 479-490 → unchanged)
    - src/components/ui/PageHero.tsx (Phase 16 — props interface verbatim)
    - app/(authed)/proposals/new/_components/RecapSection.tsx (existing primitive — verify rowSublabels API + whether a `lastRowDivider?: boolean` prop addition is needed for the bold loyer row separator; alternative is custom JSX)
    - app/globals.css (lines 390-393 .chip-language — token reference for Tranche/Coefficient chip)
    - .planning/phases/17-partner-surfaces/17-CONTEXT.md (D-16 explicit "net-new JSX structure"; D-18 ADMIN-09 D-12 envelope rule)
    - .planning/phases/17-partner-surfaces/17-UI-SPEC.md (`Wizard step 2` section — full layout sequence; Détail du calcul 5-row table; Paramètres saisis recap shape)
    - .planning/phases/17-partner-surfaces/17-PATTERNS.md (`app/(authed)/proposals/new/calcul/page.tsx MODIFY` section — pill chip code excerpt; existing RecapSection reuse; "no restructure needed at component-call level — only key rename")
    - .planning/phases/17-02-SUMMARY.md (verify which i18n keys exist: wizard.step2.eyebrow/.title/.subtitle/.hero.sublabel/.detail.title/.detail.montantHT/.detail.commission/.detail.commissionNote/.detail.coefficient/.detail.duree/.detail.loyer/.recap.title)
    - tests/admin-09-grep-contracts.test.ts (verify which substrings the 9 gates flag — confirm the commission row label `Commission apporteur` is acceptable copy that does NOT match `commission_pct` or numeric commission values)
  </read_first>
  <action>
    Restructure step 2 per UI-SPEC `Wizard step 2` (D-16) + 17-PATTERNS.md `calcul/page.tsx MODIFY`. The full sequence:

    1. ADD import: `import { PageHero } from '@/components/ui/PageHero';`

    2. DO NOT TOUCH the compute pipeline (lines ~90-172) — `computeLoyer`, `trancheNumber`, `coefficientNumber`, all draft-loading + calc logic stays verbatim.

    3. REPLACE inline heading block (lines ~292-311 — `<h1>` + `<p>`) with:
       `<PageHero eyebrow={t('wizard.step2.eyebrow', lang)} title={t('wizard.step2.title', lang)} subtitle={t('wizard.step2.subtitle', lang)} />`

    4. POSITION Stepper as sibling BELOW PageHero (D-19): if Stepper was previously inside the heading block, move it out so PageHero and Stepper are at the same JSX depth. Stepper container marginBottom: 32px (UI-SPEC §Layout step 2).

    5. RESTRUCTURE the loyer-mensuel hero card per UI-SPEC §Loyer-mensuel hero card (step 3):
       - Outer: `.card` (existing 28px padding) with inner layout `display:flex; justifyContent:space-between; alignItems:flex-start; marginBottom:16px`
       - LEFT block (sublabel + value):
         - Sublabel: render `t('wizard.step2.hero.sublabel', lang)` with `{durationMonths}` interpolation (the FR copy from Plan 02 is `par mois pendant {durationMonths} mois`); style `fontSize:13px; fontWeight:500; color:var(--muted); marginBottom:8px`
         - Value: render `{loyerDisplay}` (the formatted EUR value from compute) with `fontSize:36px; fontWeight:700; lineHeight:1.1; color:var(--teal)` — this is the **net-new 36px/700** typography (single new size per UI-SPEC Subsection A)
       - RIGHT block (Tranche/Coefficient pill chip):
         - When `trancheNumber !== null`: render an inline-flex element using the `.chip-language` chrome (or inline equivalent: `background: rgba(45,122,140,0.10); color: var(--teal); fontSize:11.8px; fontWeight:600; padding:4px 10px; borderRadius:9999px; alignSelf:flex-start`)
         - Content: `Tranche {trancheLabel} • Coefficient {coefficientPct}%` — humanize trancheLabel (e.g. `75K€`, `100K€`) using existing helper if available; 2-decimal coefficient format
         - The existing pill at lines ~381-400 is currently absolute-positioned — relocate it INSIDE the new flex row's right block, alignSelf:flex-start. The class reuse from `.chip-language` is preferred over inline style for consistency with Phase 13 chip patterns.

    6. Détail du calcul card structure preserved (lines ~459-472) — the existing `<RecapSection>` call with detailRows + rowSublabels[1] for the commission `(non visible client)` annotation is the right shape per UI-SPEC. Only changes:
       - Rename i18n keys per UI-SPEC Copywriting Contract — `wizard.step2.detail.title`, `.montantHT`, `.commission`, `.commissionNote`, `.coefficient`, `.duree`, `.loyer` (added in Plan 02; verify which keys may have been previously named differently and update the references)
       - For the BOLD separator on the last row (`Loyer mensuel calculé`): per UI-SPEC's planner's-choice option, either (a) add `lastRowDivider?: boolean` prop to `RecapSection.tsx` and pass `lastRowDivider={true}`, or (b) render the last row as a custom `<div style={{borderTop:'1px solid var(--border)', paddingTop:12, marginTop:12, display:'flex', justifyContent:'space-between', fontWeight:600}}>` block AFTER the RecapSection call. Recommendation: option (b) — avoids touching RecapSection.tsx (which is reused verbatim by other plans).
       - The 5 rows per UI-SPEC Détail du calcul section: Montant HT du projet (formatted EUR), Commission apporteur (formatted EUR, with `(non visible client)` sublabel via rowSublabels[1]), Coefficient appliqué (e.g. `3.69%`), Durée du contrat (`{durationMonths} mois`), Loyer mensuel calculé (formatted EUR, bold).

    7. Paramètres saisis recap (lines ~479-490) — keep the existing RecapSection call shape. Update `sectionTitle` to `t('wizard.step2.recap.title', lang)` and `modifierLink` to `{ href: '/proposals/new/parametres?draft_id={draftId}', label: t('wizard.action.modifier', lang) }`. Rows: client name, project ref, montant HT, duration — populate from current `draft.inputs` fields per existing pattern.

    8. WizardActionBar at the bottom — UNCHANGED.

    9. ADMIN-09 PRESERVATION (D-18, WIZ-05): the only commission surface introduced is row 2 of the Détail du calcul card (label `Commission apporteur (non visible client)` + numeric value). This is the Phase 13 D-12 allowed exception — no new commission surfaces beyond this. Do NOT expose commission in:
       - PageHero title/subtitle/eyebrow (text only — no compute data)
       - Loyer hero card (only loyer + tranche + coefficient — no commission)
       - Tranche/Coefficient pill chip (tranche + coefficient pct — no commission)
       - Paramètres saisis recap (client + project + montant HT + duration — no commission)
       The 9-gate suite at `tests/admin-09-grep-contracts.test.ts` MUST remain green; verify after restructure.

    10. The diff is large — ≈40-60 net lines changed across the hero card + Détail bold-row + i18n key renames + import. Restructure but do NOT change the compute pipeline or page-level scaffolding.
  </action>
  <verify>
    <automated>npm test -- "app/(authed)/proposals/new/calcul/" tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <done>
    - `grep -c "PageHero" "app/(authed)/proposals/new/calcul/page.tsx"` returns >= 2 (1 import + 1 JSX usage)
    - `grep -c "wizard.step2.eyebrow\|wizard.step2.detail.title\|wizard.step2.hero.sublabel" "app/(authed)/proposals/new/calcul/page.tsx"` returns >= 3 (Plan 02 keys consumed)
    - `grep -c "chip-language\|rgba(45,122,140" "app/(authed)/proposals/new/calcul/page.tsx"` returns >= 1 (Tranche/Coefficient chip styling)
    - `grep -c "36" "app/(authed)/proposals/new/calcul/page.tsx" | grep -v '^#'` shows the 36px loyer value style applied (or use a more specific grep like `fontSize:[[:space:]]*36`)
    - All existing wizard step 2 tests continue to pass
    - `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 — 9-gate suite stays green
    - Compute pipeline (lines ~90-172) UNCHANGED — verify via diff inspection
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → server component | session + draftId from session/URL (trusted); draft loaded by ownership check |
| Server → calc pipeline | inputs from draft.inputs (validated at step 1 save); compute is pure-TS |
| Server → PDF render (downstream) | commission stripped from PDF data (Phase 13 finalize-helpers isolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | Information disclosure (commission to non-owner) | wizard step 2 Détail card commission row | mitigate | Step 2 route already enforces `draft.userId === session.user.id` (Phase 13 D-12 envelope); the commission row is the Phase 13 allowed exception and visible ONLY to the deal-owner partner; cross-user access requires the draft.userId === session.user.id check which is pre-existing on this route |
| T-17-06-02 | Information disclosure (commission via new surface) | Tranche/Coefficient pill, loyer hero, Paramètres recap | mitigate | New surfaces explicitly do NOT render commission data — they show only loyer / tranche / coefficient / input parameters; Done block grep checks confirm; ADMIN-09 9-gate suite verifies no string leakage |
| T-17-06-03 | Repudiation (no audit_log on step 2 view) | wizard step 2 read-only | accept | Step 2 is a calc preview — no lifecycle event; audit_log fires only at finalize (Phase 13 D-16); existing invariant preserved |
| T-17-06-04 | Tampering (compute pipeline regression) | computeLoyer + tranche detection | mitigate | Compute pipeline lines 90-172 explicitly DO NOT change; existing 30-case golden test corpus at src/lib/calc/calc.golden.test.ts catches any regression |
| T-17-06-05 | Information disclosure (XSS via interpolated values) | loyerDisplay, trancheLabel, coefficientPct text | mitigate | All values are server-formatted strings (formatCurrency, toFixed(2), known tranche labels); React auto-escapes in JSX; no raw-HTML injection paths used |
</threat_model>

<verification>
- `npm test -- "app/(authed)/proposals/new/calcul/"` exits 0 (all existing wizard step 2 tests green)
- `npm test -- tests/admin-09-grep-contracts.test.ts` exits 0 (9-gate suite green)
- Diff inspection: compute pipeline lines ~90-172 unchanged (review `git diff`)
- Visual (Plan 08): `npm run dev` → visit `/proposals/new/calcul?draft_id={id}` after creating a draft → see ÉTAPE 2 SUR 3 eyebrow + loyer hero card with big teal value + Tranche/Coefficient chip top-right + Détail du calcul with 5 rows (commission row visible to partner per D-12) + Paramètres saisis recap with ← Modifier link
</verification>

<success_criteria>
- Wizard step 2 visually matches Figma `39:46` — PageHero + loyer hero card + Détail + Paramètres saisis
- Phase 13 D-12 partner-facing commission visibility preserved on step 2 (only — no other commission surfaces)
- ADMIN-09 9-gate suite remains green throughout
- Compute pipeline + calc results unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-partner-surfaces/17-06-SUMMARY.md`
</output>
