---
phase: 30-company-contact-registry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/dictionaries.ts
  - src/lib/i18n/dictionaries.test.ts
  - src/components/proposals/SearchBar.tsx
  - src/components/proposals/SearchBar.test.tsx
  - src/components/icons/BuildingIcon.tsx
  - src/components/icons/PhoneIcon.tsx
  - src/components/ui/icons.tsx
  - src/lib/route-meta.ts
  - src/lib/route-meta.test.ts
  - src/components/ui/AppSidebar.tsx
  - src/components/ui/AppSidebar.test.tsx
  - src/components/ui/Shell.tsx
autonomous: true
requirements: [CRM-07, ROLE-02]
must_haves:
  truths:
    - "A partner or sales user sees a 'Clients' entry in the sidebar that routes to /clients."
    - "An admin sees a 'Sociétés' entry in the sidebar that routes to /{adminSegment}/companies."
    - "The client book's search box shows client-specific French copy while /proposals is byte-identical to before."
    - "Every new user-visible string resolves through t() in both fr and en."
  artifacts:
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "clients.* and admin.companies.* namespaces in fr and en"
      contains: "clients.page.title"
    - path: "src/components/proposals/SearchBar.tsx"
      provides: "optional placeholderKey / ariaKey props defaulting to the proposal.search.* keys"
      contains: "placeholderKey"
    - path: "src/components/icons/BuildingIcon.tsx"
      provides: "product-vocabulary building glyph"
    - path: "src/components/icons/PhoneIcon.tsx"
      provides: "product-vocabulary phone glyph"
  key_links:
    - from: "src/components/ui/AppSidebar.tsx"
      to: "/clients"
      via: "partnerNavItems() entry"
      pattern: "'/clients'"
    - from: "src/components/ui/Shell.tsx"
      to: "src/components/ui/AppSidebar.tsx"
      via: "adminHrefs.companies"
      pattern: "companies:"
    - from: "src/lib/route-meta.ts"
      to: "src/components/ui/AppSidebar.tsx"
      via: "ActiveNav union members 'clients' and 'admin-companies'"
      pattern: "'admin-companies'"
---

<objective>
Ship the shared, surface-agnostic foundations every Phase 30 UI plan depends on: the full
`clients.*` / `admin.companies.*` i18n namespaces, two new product icons, the `SearchBar`
placeholder widening, and the sidebar / route-meta navigation entries for the two new route trees.

Purpose: these edits touch high-traffic shared files (`dictionaries.ts`, `AppSidebar.tsx`,
`Shell.tsx`). Landing them once, in wave 1, keeps every later UI plan free of merge contention
and lets those plans reference stable `DictKey` values instead of inventing copy.

Output: a widened dictionary, a widened `SearchBar`, two icon components and a five-item partner
nav / seven-item admin nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-company-contact-registry/30-UI-SPEC.md
@src/lib/i18n/dictionaries.ts
@src/components/ui/AppSidebar.tsx
@src/lib/route-meta.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the clients.* and admin.companies.* i18n namespaces</name>
  <files>src/lib/i18n/dictionaries.ts, src/lib/i18n/dictionaries.test.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts (the FR block's dotted-namespace convention, the `_EnHasAllFrKeys`
      compile-time parity type at the bottom, and the existing `error.field.siren.invalid` /
      `error.field.email.invalid` / `admin.accounts.toast.create.error` /
      `proposal.toast.duplicate.prefilled` entries that must be REUSED, not duplicated)
    - src/lib/i18n/dictionaries.test.ts (asserts `frKeys.length >= 220` and FR/EN parity)
    - .planning/phases/30-company-contact-registry/30-UI-SPEC.md §"Copywriting Contract" and
      §"i18n Key Plan" — the exact French strings and the key list are binding
  </read_first>
  <behavior>
    - Every key listed in the UI-SPEC's i18n Key Plan resolves in `dictionaries.fr` and `dictionaries.en`
    - `t('clients.page.title', 'fr')` returns `'Clients'`
    - `t('clients.empty.search.title', 'fr')` returns `'Aucun résultat ne correspond à votre recherche.'`
      and contains no wording implying a record exists elsewhere (CRM-02 non-leakage in copy)
    - No new key duplicates `error.field.siren.invalid` or `error.field.email.invalid`
  </behavior>
  <action>
Append a `// ── Phase 30 — CRM registry (clients.* / admin.companies.*) ──` block to the FR
dictionary and a mirrored block to the EN dictionary. Add every key named in the UI-SPEC's
i18n Key Plan, using the exact French copy from the Copywriting Contract:

Partner-facing `clients.*`: `page.title` ("Clients"), `page.subtitle` ("Retrouvez vos clients et
les propositions que vous leur avez faites."), `cta.new` ("Nouveau client"), `search.placeholder`
("Rechercher un client…"), `search.aria`, `col.company` ("CLIENT"), `col.siren` ("SIREN"),
`col.proposals` ("PROPOSITIONS"), `col.lastActivity` ("DERNIÈRE ACTIVITÉ"),
`empty.zero.title` ("Aucun client pour le moment."),
`empty.search.title` ("Aucun résultat ne correspond à votre recherche."),
`detail.section.contacts` ("Contacts"), `detail.section.proposals` ("Propositions"),
`detail.empty.proposals.title` ("Aucune proposition pour ce client."),
`detail.empty.contacts.title` ("Aucun contact enregistré pour ce client."),
`detail.cta.newProposal` ("Nouvelle proposition"),
`contact.cta.add` ("Ajouter un contact"), `contact.cta.edit`,
`contact.field.name` ("Nom"), `contact.field.role` ("Fonction"),
`contact.field.role.placeholder` ("Ex. Directeur achats"), `contact.field.phone` ("Téléphone"),
`contact.field.email` ("Email"),
`contact.action.edit`, `contact.action.delete` (both `aria-label` strings carrying a `{0}`
placeholder for the contact name — follow the existing `{0}` interpolation convention used by
`proposal.detail.deleted.banner`),
`contact.submit.create` ("Enregistrer le contact"),
`contact.submit.edit` ("Enregistrer les modifications"),
`contact.confirm.delete.title` ("Supprimer ce contact ?"),
`contact.confirm.delete.description` ("Cette action est définitive et retirera {0} de la fiche
client."), `contact.confirm.delete.cancel` ("Annuler"), `contact.confirm.delete.confirm`
("Supprimer"), `contact.toast.deleted` ("Contact supprimé."),
`modal.create.title`, `modal.create.field.name` ("Nom de la société"),
`modal.create.field.name.placeholder` ("Ex. Dupont Menuiserie"),
`modal.create.field.siren` ("SIREN"),
`modal.create.field.siren.helper` ("Facultatif. 9 chiffres, sans espaces."),
`modal.create.submit` ("Créer le client"), `modal.create.cancel` ("Annuler"),
`toast.created` ("Client créé."), `toast.error` ("Une erreur est survenue. Réessayez.").

Admin-facing `admin.companies.*`: `page.title` ("Sociétés"), `page.subtitle`,
`col.company` ("SOCIÉTÉ"), `col.siren`, `col.relations` ("RELATIONS"), `col.lastActivity`,
`empty.zero.title` ("Aucune relation active pour cette société."),
`detail.section.relations` ("Relations"),
`relation.col.owner` ("TITULAIRE"), `relation.col.type` ("TYPE"), `relation.col.created`
("CRÉÉE LE"), `relation.col.proposals` ("PROPOSITIONS"), `relation.col.contacts` ("CONTACTS"),
`relation.col.view` ("Voir →"), `relation.type.partner` ("Partenaire"),
`relation.type.sales` ("Interne").

Sidebar: `sidebar.nav.clients` ("Clients"), `sidebar.nav.adminCompanies` ("Sociétés").

Do NOT add any key whose value duplicates an existing entry — specifically reuse
`error.field.siren.invalid`, `error.field.email.invalid` and
`proposal.search.clear`. Write English counterparts for every new key in the EN block; the
`_EnHasAllFrKeys` type fails compilation otherwise.

Extend `src/lib/i18n/dictionaries.test.ts` with a Phase 30 describe block asserting the FR values
above for `clients.page.title`, `clients.empty.search.title`, `admin.companies.relation.type.sales`
and `sidebar.nav.clients`, plus their EN counterparts. Raise the existing
`frKeys.length >= 220` floor to the new post-addition count rounded down to the nearest ten.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- src/lib/i18n/dictionaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (proves `_EnHasAllFrKeys` FR/EN parity for every new key)
    - `npm test -- src/lib/i18n/dictionaries.test.ts` exits 0
    - `grep -c "'clients\." src/lib/i18n/dictionaries.ts` is at least 70 (FR + EN halves)
    - `grep -c "'admin.companies\." src/lib/i18n/dictionaries.ts` is at least 32
    - `grep -c "error.field.siren.invalid" src/lib/i18n/dictionaries.ts` is exactly 2 (the pre-existing FR + EN entries — no third, duplicated key was minted)
  </acceptance_criteria>
  <done>Every Phase 30 string exists once, in both languages, and typecheck proves parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen SearchBar with placeholderKey/ariaKey and add the two product icons</name>
  <files>src/components/proposals/SearchBar.tsx, src/components/proposals/SearchBar.test.tsx, src/components/icons/BuildingIcon.tsx, src/components/icons/PhoneIcon.tsx, src/components/ui/icons.tsx</files>
  <read_first>
    - src/components/proposals/SearchBar.tsx (currently hardcodes `t('proposal.search.placeholder', lang)`
      and `t('proposal.search.aria', lang)`; already sets `autoComplete="off"`)
    - src/components/icons/MailIcon.tsx (the exact icon template: `size` prop defaulting to 24,
      24×24 viewBox, `fill="none"` on the svg, `currentColor` fill on the path group,
      `aria-hidden` derived from `props["aria-label"]`, per-icon namespaced `id` attributes,
      named export plus `export default`)
    - src/components/ui/icons.tsx (the header comment explaining the two-tier icon policy and the
      one-line re-export convention)
    - .planning/phases/30-company-contact-registry/30-UI-SPEC.md §"Component Inventory" and
      Assumption A-7
  </read_first>
  <behavior>
    - `<SearchBar lang="fr" />` (no new props) renders placeholder `t('proposal.search.placeholder','fr')` — unchanged for /proposals
    - `<SearchBar lang="fr" placeholderKey="clients.search.placeholder" ariaKey="clients.search.aria" />` renders the client-book copy
    - `<BuildingIcon size={16} />` renders an svg with width and height 16
    - `<PhoneIcon aria-label="x" />` renders without `aria-hidden`
  </behavior>
  <action>
Add two optional props to `SearchBarProps`: `placeholderKey?: DictKey` and `ariaKey?: DictKey`,
imported as a type from `@/lib/i18n/dictionaries`. Default them in the destructuring signature to
`'proposal.search.placeholder'` and `'proposal.search.aria'` so every existing call site
(`app/(authed)/proposals/page.tsx`, `app/(admin)/[adminSegment]/partners/page.tsx`) keeps its
current output with no edit. Replace the two hardcoded `t(...)` calls with `t(placeholderKey, lang)`
and `t(ariaKey, lang)`. Leave the clear-button label on `proposal.search.clear`, and leave
`autoComplete="off"`, `spellCheck={false}` and the `next.delete('cursor')` reset untouched —
the cursor reset is what makes server-side sorting and search composable in plan 30-06.

Create `src/components/icons/BuildingIcon.tsx` and `src/components/icons/PhoneIcon.tsx`
structurally identical to `MailIcon.tsx`: `export interface BuildingIconProps extends
React.SVGProps<SVGSVGElement> { size?: number | string }`, `viewBox="0 0 24 24"`, `width={size}`
`height={size}`, `fill="none"`, `aria-hidden={props["aria-label"] ? undefined : true}`, one
`<g>` wrapper with `fill="currentColor"`, and per-icon namespaced ids (`building-1/2/3`,
`phone-1/2/3`) so two icons on one page cannot collide. The licensed Iconly source SVGs are not
vendored in this repo, so author the two outline paths by hand in the same 1.5px-stroke-weight
outline style as the existing 42 glyphs — a simple office block with window rows for
`BuildingIcon`, a handset for `PhoneIcon`. Per Assumption A-7, do NOT import from
`@hugeicons/*` — Hugeicons is reserved for primitive-internal chrome.

Re-export both from `src/components/ui/icons.tsx` alongside the existing one-line exports.

Create `src/components/proposals/SearchBar.test.tsx` covering the four behaviors above, following
the render/query conventions of an existing component test such as
`app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.test.tsx`.
  </action>
  <verify>
    <automated>npm test -- src/components/proposals/SearchBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components/proposals/SearchBar.test.tsx` exits 0
    - `src/components/proposals/SearchBar.tsx` contains `placeholderKey` and `ariaKey` with default values `'proposal.search.placeholder'` and `'proposal.search.aria'`
    - `grep -rn "SearchBar" "app/(authed)/proposals/page.tsx" "app/(admin)/[adminSegment]/partners/page.tsx"` shows no prop was added at either call site
    - `src/components/ui/icons.tsx` contains `export { BuildingIcon }` and `export { PhoneIcon }`
    - `grep -rn "hugeicons" src/components/icons/BuildingIcon.tsx src/components/icons/PhoneIcon.tsx` returns no matches
    - `npm run typecheck` and `npm run lint:check` both exit 0
  </acceptance_criteria>
  <done>SearchBar accepts per-surface copy without changing /proposals, and the product icon vocabulary has the two glyphs Phase 30 surfaces need.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register the /clients and /{adminSegment}/companies nav entries</name>
  <files>src/lib/route-meta.ts, src/lib/route-meta.test.ts, src/components/ui/AppSidebar.tsx, src/components/ui/AppSidebar.test.tsx, src/components/ui/Shell.tsx</files>
  <read_first>
    - src/lib/route-meta.ts (the `ActiveNav` union and the ordered prefix-matching in `getRouteMeta`;
      note the admin branch matches on `tail.startsWith(...)`)
    - src/lib/route-meta.test.ts
    - src/components/ui/AppSidebar.tsx lines 55-160 (`AppSidebarProps.adminHrefs`, `partnerNavItems()`,
      `adminNavItems()`, and the `isAdmin && effectiveView === 'admin' && adminHrefs` branch that
      is the ONLY nav-set switch — a `sales` user is simply `isAdmin === false`)
    - src/components/ui/AppSidebar.test.tsx (asserts partner nav has exactly 4 links and admin nav
      exactly 6 — both counts change in this task)
    - src/components/ui/Shell.tsx lines 55-75 (the `adminHrefs` object literal)
    - .planning/phases/30-company-contact-registry/30-UI-SPEC.md §5 "Role surfacing" and
      §4 (sidebar entry positioned after `admin-partners`, before `admin-coefficients`)
  </read_first>
  <behavior>
    - `getRouteMeta('/clients')` returns `activeNav: 'clients'` and `titleKey: 'sidebar.nav.clients'`
    - `getRouteMeta('/clients/abc-123')` also returns `activeNav: 'clients'`
    - `getRouteMeta('/sekret/companies', 'sekret')` returns `activeNav: 'admin-companies'` and `titleKey: 'sidebar.nav.adminCompanies'`
    - Partner nav renders exactly 5 links, in order: Accueil, Nouvelle proposition, Propositions, Clients, Aide
    - Admin nav renders exactly 7 links, with Sociétés between Partenaires and Coefficients
    - `getRouteMeta('/proposals')` and `getRouteMeta('/sekret/partners','sekret')` are unchanged
  </behavior>
  <action>
Extend the `ActiveNav` union in `src/lib/route-meta.ts` with `'clients'` and `'admin-companies'`.
In `getRouteMeta`, add `tail.startsWith('/companies')` to the admin branch — place it BEFORE the
`/coefficients` and `/partners` checks is unnecessary (no prefix collision), but it must sit
inside the admin branch and above the admin-home fallback. In the partner branch, add
`pathname.startsWith('/clients')` returning `{ titleKey: 'sidebar.nav.clients', activeNav: 'clients' }`,
placed after the `/proposals` checks and before `/aide`.

In `src/components/ui/AppSidebar.tsx`: add `companies: string` to the `adminHrefs` prop type;
add a `{ key: 'clients', icon: BuildingIcon, labelKey: 'sidebar.nav.clients', href: '/clients' }`
entry to `partnerNavItems()` between the `proposals` and `help` entries; add
`{ key: 'admin-companies', icon: BuildingIcon, labelKey: 'sidebar.nav.adminCompanies', href: hrefs.companies }`
to `adminNavItems()` between `admin-partners` and `admin-coefficients`. Import `BuildingIcon`
from `@/components/ui/icons`. Update the two doc comments above those functions to say 5 and 7
items. Do NOT add any `role === 'sales'` branch — ROLE-02 is satisfied by construction because
`isAdmin === false` already selects `partnerNavItems()`.

In `src/components/ui/Shell.tsx`: add `companies: \`/${adminSegment}/companies\`` to the
`adminHrefs` object literal, alongside `home` / `coefficients` / `partners` / `history`.

Update `src/lib/route-meta.test.ts` and `src/components/ui/AppSidebar.test.tsx` for the six
behaviors above, including the two count assertions that change from 4→5 and 6→7 (there are
several such assertions in AppSidebar.test.tsx — update all of them) and the `adminHrefs` fixture
which now needs a `companies` key.
  </action>
  <verify>
    <automated>npm test -- src/lib/route-meta.test.ts src/components/ui/AppSidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 across the whole suite (no other AppSidebar or route-meta assertion left stale)
    - `src/lib/route-meta.ts` contains `'admin-companies'` and `'clients'` in the `ActiveNav` union
    - `src/components/ui/Shell.tsx` contains `companies: \`/${adminSegment}/companies\``
    - `src/components/ui/AppSidebar.tsx` contains no occurrence of the string `sales` (ROLE-02 is by construction, not by branch)
    - `npm run typecheck` and `npm run lint:check` both exit 0
  </acceptance_criteria>
  <done>Both new route trees are reachable from the sidebar and correctly highlighted, and no role-specific branch was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar nav set → user role | a client component deciding which links to render |
| i18n copy → partner's mental model | strings that could confirm the existence of another partner's record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Information Disclosure | `clients.empty.search.title` copy | mitigate | The zero-results string is deliberately generic ("Aucun résultat ne correspond à votre recherche.") and never states that a matching record exists elsewhere; asserted by a dictionary test (CRM-02) |
| T-30-02-02 | Elevation of Privilege | `AppSidebar` nav-set selection | mitigate | No new role branch is added; the existing `isAdmin && effectiveView === 'admin' && adminHrefs` gate is untouched, so a `sales` user cannot reach `adminNavItems()`. Acceptance criterion greps the file for the literal `sales` and requires zero matches. Authorization remains server-side in the `(admin)` route group (ROLE-03) |
| T-30-02-03 | Information Disclosure | `sidebar.nav.adminCompanies` link visible to non-admins | mitigate | The admin entry lives only in `adminNavItems()`, which is unreachable without `isAdmin`; the `[adminSegment]` layout independently `notFound()`s (AUTH-14/D-18) |
| T-30-02-04 | Tampering | `SearchBar` widening regressing /proposals | mitigate | Both new props default to the current keys and neither existing call site is edited; asserted by grep on the two call sites |
| T-30-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package install occurs; the two icons are hand-authored SVG components, explicitly not sourced from `@hugeicons/*` (Assumption A-7) |
</threat_model>

<verification>
1. `npm run typecheck` exits 0
2. `npm run lint:check` exits 0 (the `no-restricted-syntax` hardcoded-JSX rule catches any string that bypassed `t()`)
3. `npm test` exits 0
4. `npm run build` completes and `.next/standalone/server.js` exists
</verification>

<success_criteria>
- All Phase 30 copy exists in both dictionaries and typecheck proves parity
- `/proposals` search rendering is byte-identical to before this plan
- Partner nav is 5 items, admin nav is 7, both correctly highlighted for the new routes
- No role-conditional branch was added to any client component
</success_criteria>

<output>
Create `.planning/phases/30-company-contact-registry/30-02-SUMMARY.md` when done.
</output>
