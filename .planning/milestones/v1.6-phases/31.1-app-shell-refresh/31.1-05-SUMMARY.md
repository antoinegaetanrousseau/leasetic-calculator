---
phase: 31.1-app-shell-refresh
plan: 05
subsystem: ui
tags: [css, design-tokens, dark-mode, tailwind-v4, regression-test]

# Dependency graph
requires:
  - phase: 31.1-01
    provides: "The explicit --radius-* scale and --radius-container ladder this plan does not touch — confirms the token block layout used for the read_first line ranges"
provides:
  - "Six dark-mode shell-surface tokens (--background, --card, --muted-foreground, --border, --sidebar, --sidebar-border) carrying Colibris-sampled values with inline provenance comments"
  - "Three unsampled surfaces (dialog/popover elevation, input fill, sidebar hover/active) left explicitly unchanged and named DARK-GAP-01/02/03 for Plan 31.1-07's operator checkpoint"
  - "tests/dark-palette.test.ts — a permanent 6-assertion gate pinning the print/PDF white override, the no-flash mechanism, the dark selector, the six sampled values, the three unsampled gaps, and Leasétic's accent surviving into dark with zero Colibris-teal leakage"
affects: [31.1-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Source-assertion test pattern (readFileSync + comment-stripping + regex, no jsdom) reused a second time from tests/radius-scale.test.ts's shape, this time scoping declaration assertions to a regex-extracted dark-block substring so a light-mode value of the same name can never satisfy a dark-mode check."
    - "Provenance-in-CSS-comment idiom extended to dark-mode values: every screenshot-sampled declaration in app/globals.css now carries an inline same-line comment naming the surface it was sampled from, and a block comment above the whole dark section names the source, date and confidence."

key-files:
  created:
    - tests/dark-palette.test.ts
  modified:
    - app/globals.css

key-decisions:
  - "Left --popover, --input and --sidebar-accent completely unchanged, per the plan's explicit instruction and 31.1-UI-SPEC.md's Unsampled table — no value was invented for any of the three gap surfaces (criterion 7's defining constraint)."
  - "tests/dark-palette.test.ts asserts the three gap tokens' CURRENT values as an equality pin, not just their absence-of-change relative to some prior commit — this makes any future silent fill of a gap fail loudly, matching the plan's stated intent for assertion 5."
  - "The test extracts the dark token block via regex before asserting on individual declarations, rather than grepping the whole file, so a same-named light-mode token can never accidentally satisfy a dark-mode assertion."

requirements-completed: [SHELL-C7]

# Metrics
duration: ~5min
completed: 2026-09-02
---

# Phase 31.1 Plan 05: Dark Mode Shell Palette Summary

**Six dark-mode shell tokens (background/card/sidebar/border/muted-text) recolored to Colibris's screenshot-sampled palette with inline provenance, while the three surfaces the screenshot couldn't answer (dialog, input, sidebar hover) ship byte-identical and are pinned by a new 6-assertion regression test.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-09-02T22:29:53+02:00 (approx, first edit)
- **Completed:** 2026-09-02T22:34:03+02:00
- **Tasks:** 2/2
- **Files modified:** 2 (1 modified, 1 created)

## Accomplishments

- `app/globals.css`'s `html[data-theme="dark"]` block now carries six Colibris-sampled values (`--background: #161616`, `--card: #1e1e1e`, `--muted-foreground: oklch(0.65 0 0)`, `--border`/`--sidebar-border: oklch(1 0 0 / 8%)`, `--sidebar: #161616`) — each traced to the one operator-supplied screenshot dated 2026-09-02, with the "~a shade, confirm live" confidence caveat preserved in the CSS comments, exactly as `31.1-UI-SPEC.md` § Dark Mode records it.
- `--popover`, `--input` and `--sidebar-accent` ship byte-identical to before this plan (verified below) — the three explicit gaps (DARK-GAP-01/02/03) are documented in-file as a decision, not an oversight.
- The accent (`--primary`/`--sidebar-primary` = `var(--brand-accent)`, `#01cc72`) and the print/PDF white override (`app/globals.css:244-247`) are untouched; no occurrence of Colibris's `#009999` teal exists anywhere in `app`/`src`.
- `tests/dark-palette.test.ts` — a new 6-assertion source-only test suite that fails the build if: the PDF-white override is removed or weakened, the no-flash script/injection is altered, the dark selector mechanism changes, any of the six sampled values drifts, any of the three gap tokens is silently filled, or Colibris's teal is introduced anywhere.

## Task Commits

Each task was committed atomically:

1. **Task 1: Apply the six sampled dark declarations with provenance** - `006eb98` (feat)
2. **Task 2: Add the dark-mode invariant gate** - `8775e21` (test)

## Files Created/Modified

- `app/globals.css` - Six dark-mode token values changed inside the existing `html[data-theme="dark"]` block; two new comment blocks added (sampling provenance above the block, unsampled-gaps note inline). No new selector, no new mechanism, `--foreground`/`--primary`/`--sidebar-primary`/print rule/no-flash script untouched.
- `tests/dark-palette.test.ts` - New. Permanent source-assertion regression gate (6 `it()` blocks), no jsdom/rendering/network — same pattern as `tests/radius-scale.test.ts`.

## Decisions Made

- The three unsampled surfaces (dialog/popover elevation, input fill, sidebar hover/active) were left at their pre-existing values rather than estimated from adjacent tokens — the plan and `31.1-UI-SPEC.md` are explicit that inventing a plausible value for any of them would violate ROADMAP criterion 7 while appearing to satisfy it. Closure path for each: a second, targeted Colibris screenshot (see Known Gaps below).
- `tests/dark-palette.test.ts` pins the three gap tokens' current values as a hard equality assertion, not merely "these three names are absent from the diff" — this is deliberate per the plan's Task 2 instructions, so a later contributor who fills a gap must consciously edit this test rather than have it pass silently.

## Deviations from Plan

None - plan executed exactly as written. Six declarations changed, provenance comments added, three gaps left explicitly open, permanent test added — matching the plan's `<action>` blocks and acceptance criteria verbatim.

## Known Gaps

Recorded per the plan's `<output>` instruction, for Plan 31.1-07's operator acceptance checkpoint to read:

- **DARK-GAP-01 — Dialog/popover elevation.** `--popover`/`--popover-foreground` remain `oklch(0.205 0 0)`/`oklch(0.985 0 0)` (unchanged registry defaults). No dialog was open in the one available screenshot. Closure path: a second Colibris screenshot with a dialog/popover open in dark mode, or an operator-accepted "unchanged is fine" decision.
- **DARK-GAP-02 — Input fill.** `--input` remains `oklch(1 0 0 / 15%)` (unchanged). No form was visible in the screenshot. Closure path: a second screenshot of a Colibris form in dark mode, or accept-as-is with a manual contrast check.
- **DARK-GAP-03 — Sidebar hover/active-item treatment.** `--sidebar-accent` remains `oklch(0.269 0 0)` (unchanged, pre-existing — not a Colibris-alignment claim, per `31.1-UI-SPEC.md`). No hovered/active nav row was visible in the screenshot. Closure path: same as above, lowest priority since this value predates Phase 31.1 entirely.

## Issues Encountered

- `gsd-sdk query requirements.mark-complete SHELL-C7` reported `not_found` — `.planning/REQUIREMENTS.md`'s traceability table has no `SHELL-C*` rows, same pre-existing gap Plan 31.1-01's SUMMARY already flagged for the orchestrator/operator. `SHELL-C7` remains logically satisfied by this plan's work (six sampled tokens, three gaps left explicit, permanent regression test) but is not reflected in `REQUIREMENTS.md`'s checkbox/traceability state.
- `gsd-sdk query state.record-session` reported "No session fields found in STATE.md" — this project's `STATE.md` has no `Last session`/`Stopped At`/`Resume File` fields (uses a freeform `## Session Notes` section instead), so the session-timestamp update was skipped as a no-op rather than forced through a raw edit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 31.1-07's operator acceptance checkpoint should read the three `DARK-GAP-*` entries above and decide: accept as-is, or request a second targeted Colibris screenshot per surface.
- `tests/dark-palette.test.ts` will fail loudly if a later plan (this phase or beyond) silently fills one of the three gaps, weakens the print/PDF invariant, or disturbs the no-flash mechanism — no manual re-verification of this contract needed per-plan.
- No blockers opened for `31.1-06` or `31.1-07` by this plan.

---
*Phase: 31.1-app-shell-refresh*
*Completed: 2026-09-02*

## Self-Check: PASSED

All claimed files and commits verified present:
- `app/globals.css` — FOUND
- `tests/dark-palette.test.ts` — FOUND
- `.planning/phases/31.1-app-shell-refresh/31.1-05-SUMMARY.md` — FOUND
- Commit `006eb98` — FOUND
- Commit `8775e21` — FOUND
- `src/lib/theme/no-flash-script.ts` and `app/layout.tsx` — zero diff across both task commits (no-flash mechanism untouched)
- `[data-pdf-surface]` rule — zero diff across both task commits (print/PDF invariant untouched)
