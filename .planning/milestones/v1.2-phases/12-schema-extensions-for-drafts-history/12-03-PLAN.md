---
phase: 12-schema-extensions-for-drafts-history
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/lib/db/queries/users.ts
  - src/lib/db/queries/users.test.ts
autonomous: true
requirements: [DB-02]
must_haves:
  truths:
    - "src/lib/db/queries/users.ts exports a new function listInvitedPartners() returning Promise<InvitedPartnerRow[]>"
    - "listInvitedPartners filters rows on role='partner' AND deleted_at IS NULL AND last_login_at IS NULL (D-10 — fully derived, no new column)"
    - "Existing listPartnersWithCounts() function is unchanged — both helpers coexist"
    - "Vitest unit tests in users.test.ts (NEW file) cover the predicate composition and pass with mocked DB"
    - "The returned shape is admin-facing safe — NO commission_pct, NO password_hash fields exposed (ADMIN-09)"
  artifacts:
    - path: src/lib/db/queries/users.ts
      provides: "Existing listPartnersWithCounts + NEW listInvitedPartners helper"
      contains: "export async function listInvitedPartners"
    - path: src/lib/db/queries/users.test.ts
      provides: "Vitest unit tests for listInvitedPartners with mocked DB"
  key_links:
    - from: src/lib/db/queries/users.ts
      to: src/db/schema.ts (users table)
      via: "Drizzle query against schema.users with predicate role='partner' AND deletedAt IS NULL AND lastLoginAt IS NULL"
      pattern: "eq\\(schema\\.users\\.role, 'partner'\\)"
    - from: src/lib/db/queries/users.ts
      to: app/(admin)/[adminSegment]/accounts/AccountsList.tsx (Phase 14)
      via: "Phase 14 admin partners surface calls listInvitedPartners to render the gold 'invited' StatusChip"
      pattern: "listInvitedPartners"
---

<objective>
Ship `listInvitedPartners()` in `src/lib/db/queries/users.ts` plus Vitest unit tests. The helper encapsulates the D-10 derivation rule: `invited` is fully derived from `role='partner' AND deleted_at IS NULL AND last_login_at IS NULL` — NO new column on users. Phase 14 will consume this helper to render the gold `invited` `StatusChip` in the Liste des partenaires; Phase 12 only ships the helper, not the UI integration.

Purpose: Give downstream consumers a single, named, testable choke point for the "invited" derivation. The same predicate could be inlined at the call site, but a named helper documents the rule and gives us one place to test it.

Output: One source-file edit + one new test file. Atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md
@src/lib/db/queries/users.ts
@src/lib/db/queries/proposals.test.ts
@src/db/schema.ts

<interfaces>
<!-- Existing users.ts (lines 1-86 today) shape: listPartnersWithCounts() returns PartnerWithCount[] with proposalsCount + hasUnredeemedInvite. This plan adds a NEW exported function alongside. -->

From src/lib/db/queries/users.ts:
- `export interface PartnerWithCount { id, email, displayName, name, role, deletedAt, lastLoginAt, createdAt, language, proposalsCount, hasUnredeemedInvite }`
- `export async function listPartnersWithCounts(): Promise<PartnerWithCount[]>` (D-09-10)

From src/db/schema.ts:
- `users.role: text NOT NULL DEFAULT 'partner' CHECK IN ('partner','admin')`
- `users.deletedAt: timestamp with time zone NULL`
- `users.lastLoginAt: timestamp with time zone NULL`  ← this is the field plan 12-07 starts WRITING on login (Phase 6 follow-up #3 / WR-AUDIT-01 closure)

Predicate per D-10:
- WHERE `users.role = 'partner'`
- AND `users.deleted_at IS NULL`
- AND `users.last_login_at IS NULL`

Returned shape (recommend `InvitedPartnerRow` — narrower than PartnerWithCount; this query has no need to compute proposalsCount or hasUnredeemedInvite):
- `id`, `email`, `displayName`, `name`, `language`, `createdAt`
- (Phase 14 calling site renders the chip + name; if it needs invite-token status, it can call listPartnersWithCounts separately or merge later)

ADMIN-09 invariant (carried from Phase 9):
- This query MUST NOT select commission_pct (lives on global_params, not users — defense in depth).
- The returned shape MUST NOT include any `password` / `password_hash` / argon2 columns (those live on accounts.password — also not selected here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listInvitedPartners() to src/lib/db/queries/users.ts</name>
  <files>src/lib/db/queries/users.ts</files>
  <read_first>
    - src/lib/db/queries/users.ts (current — read in full to match style; listPartnersWithCounts is the sister function)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-10, D-11)
    - src/db/schema.ts (users table definition)
    - src/lib/db/queries/proposals.ts lines 1-10 (imports pattern: `import 'server-only'; import { ... } from 'drizzle-orm'; import { db, schema } from '@/lib/db';`)
  </read_first>
  <behavior>
    - The function returns an array (no pagination per CONTEXT recommendation — partner count is bounded; add pagination if it grows past ~200).
    - Default sort: `users.createdAt DESC` (newest invited first — admin view ergonomics).
    - The function is `'use server'`-safe (the module already declares `import 'server-only'` at the top).
    - Predicate uses Drizzle's `and(eq(...), isNull(...), isNull(...))` — the same style as listPartnersWithCounts at line 56-57.
    - Returned columns: id, email, displayName, name, language, createdAt. Narrower than PartnerWithCount.
  </behavior>
  <action>
Edit `src/lib/db/queries/users.ts`:

1. Add a new exported interface (placed near the existing PartnerWithCount interface):
   ```
   export interface InvitedPartnerRow {
     id: string;
     email: string;
     displayName: string | null;
     name: string;
     language: string;
     createdAt: Date;
   }
   ```

2. Add a new exported async function `listInvitedPartners()` returning `Promise<InvitedPartnerRow[]>`:
   - Build a Drizzle select with the six columns above from `schema.users`.
   - WHERE: `and(eq(schema.users.role, 'partner'), isNull(schema.users.deletedAt), isNull(schema.users.lastLoginAt))` (per D-10).
   - ORDER BY: `desc(schema.users.createdAt)`.
   - No limit — admin list is bounded (CONTEXT recommendation).

3. JSDoc above the function MUST state:
   - "DB-02 per 12-CONTEXT.md D-10. Invited = role='partner' AND deleted_at IS NULL AND last_login_at IS NULL — fully derived from existing columns; no new schema. Closure depends on plan 12-07 writing users.last_login_at on every successful login (WR-AUDIT-01); without that, every partner forever appears as 'invited'."
   - "ADMIN-09: this query MUST NOT select commission_pct or any password column. The returned shape is bounded above to id/email/displayName/name/language/createdAt."

4. Ensure imports cover `isNull`, `desc`, `and`, `eq` from `drizzle-orm` (all already imported per the existing file's `import { and, desc, eq, gt, inArray, isNull, sql } from 'drizzle-orm'`).

5. Do NOT modify the existing listPartnersWithCounts function. Do NOT add pagination args (CONTEXT recommendation).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --quiet src/lib/db/queries/users.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep `export async function listInvitedPartners` returns exactly 1 hit in `src/lib/db/queries/users.ts`.
    - Source: grep `export interface InvitedPartnerRow` returns exactly 1 hit.
    - Source: grep `eq(schema.users.role, 'partner')` returns ≥1 hit AND appears within the listInvitedPartners function body.
    - Source: grep `isNull(schema.users.deletedAt)` returns ≥1 hit in the listInvitedPartners body.
    - Source: grep `isNull(schema.users.lastLoginAt)` returns ≥1 hit in the listInvitedPartners body.
    - Source: grep `commissionPct\|commission_pct\|password` in `src/lib/db/queries/users.ts` filtered to the file content returns 0 hits.
    - Source: grep `export async function listPartnersWithCounts` still returns exactly 1 hit (existing function untouched).
    - CLI: `npm run typecheck` exits 0.
    - CLI: `npm run lint -- --quiet src/lib/db/queries/users.ts` exits 0.
  </acceptance_criteria>
  <done>
listInvitedPartners exists alongside listPartnersWithCounts; ADMIN-09 invariant held (no commission/password selection); typecheck + lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest unit tests for listInvitedPartners with mocked DB</name>
  <files>src/lib/db/queries/users.test.ts</files>
  <read_first>
    - src/lib/db/queries/users.ts (just-edited — the function under test)
    - src/lib/db/queries/proposals.test.ts (canonical mock-DB pattern; copy the `vi.mock('@/lib/db', ...)` block; `vi.mock('server-only', () => ({}))` at the top)
    - src/lib/db/queries/global-params.test.ts (sister test file for additional pattern reference)
  </read_first>
  <behavior>
    - Use the same `vi.mock('@/lib/db', ...)` builder-stub pattern as proposals.test.ts.
    - Mock the `select().from().where().orderBy()` chain to capture the `where` payload AND return a controllable fixture array.
    - Tests verify (a) the function returns the fixture rows, (b) the where predicate references role, deletedAt, lastLoginAt (capture-the-call assertion), (c) the orderBy references createdAt.
  </behavior>
  <action>
Create `src/lib/db/queries/users.test.ts`:

1. Top of file (matching proposals.test.ts):
   - `import { describe, it, expect, vi, beforeEach } from 'vitest';`
   - `vi.mock('server-only', () => ({}));`
   - `vi.mock('@/lib/db', async () => { ... })` block defining a stub builder + capturing `where` / `orderBy` / `limit` calls into a `calls` array. The stub MUST also expose `schema` re-exported from the real `@/db/schema` so the production code's `schema.users.role` references resolve correctly.

2. Import `listInvitedPartners` from `./users`.

3. Test cases:
   - `'returns an empty array when DB returns empty'` — stub the terminal `orderBy` to resolve `[]`; assert `expect(await listInvitedPartners()).toEqual([])`.
   - `'queries with role + deletedAt + lastLoginAt predicate composition'` — stub returns `[]`; after calling listInvitedPartners(), assert `calls.some((c) => c.kind === 'where')` is true. (Note: the proposals.test.ts mock only captures kind+payload, not the contents of the SQL fragments — so the assertion is "where was called", not "the predicate text is X". This is the established pattern.)
   - `'queries with orderBy createdAt desc'` — assert `calls.some((c) => c.kind === 'orderBy')` is true.
   - `'returns the rows from the DB unchanged'` — adjust the mock to make the chain resolve to a fixed fixture array of two InvitedPartnerRow-shaped objects; assert the return equals that fixture.
   - `'does NOT include commission_pct or password in the returned shape'` — assert the fixture-shaped rows' keys are a subset of `['id','email','displayName','name','language','createdAt']`; this is a defensive assertion that the FUNCTION CONTRACT documents the bounded shape.

4. Vitest discovery: the file is auto-included via `src/**/*.test.ts` in vitest.config.ts.

Do NOT add a real-Postgres integration test in this plan — the unit-test-with-mocked-DB pattern is what CONTEXT D-20 prescribes for query helpers (the real-Postgres integration test is reserved for the coefficient_history trigger in plan 12-06).
  </action>
  <verify>
    <automated>npx vitest run src/lib/db/queries/users.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npx vitest run src/lib/db/queries/users.test.ts` exits 0.
    - CLI: stdout reports ≥4 tests passing.
    - Source: grep `vi.mock\('server-only'` returns ≥1 hit.
    - Source: grep `vi.mock\('@/lib/db'` returns ≥1 hit.
    - Source: grep `listInvitedPartners` in the test file returns ≥4 hits (one per test calling it).
    - CLI: `npm test` (full suite) exits 0 — no regressions.
  </acceptance_criteria>
  <done>
≥4 unit tests pass; the test file follows the established proposals.test.ts mock-DB pattern; the full Vitest suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin route → query helper | Caller (Phase 14 admin list) is already gated by requireAdmin(); this helper does not re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information disclosure | exposing commission_pct via admin lists | mitigate | Helper SELECTs a bounded column list (id, email, displayName, name, language, createdAt); commission_pct lives on global_params and is never joined here. Test asserts the bounded shape. |
| T-12-03-02 | Information disclosure | exposing accounts.password via SELECT * | mitigate | Helper SELECTs an explicit column list; never `select(*)`. `accounts` table is never joined. |
| T-12-03-03 | EoP | non-admin caller reads partner list | mitigate | Caller (Phase 14 admin route) gates with requireAdmin() per Phase 9 pattern (sister helper listPartnersWithCounts inherits the same gating). This helper has `import 'server-only'` from the module-level import, blocking client-component import. |
| T-12-03-04 | Tampering | `invited` derivation drifts because last_login_at is never written | mitigate | Plan 12-07 closes WR-AUDIT-01 by writing users.last_login_at = now() on every successful login. Without plan 12-07, this helper returns every partner forever (D-11 — explicit dependency). |
</threat_model>

<verification>
- Vitest tests pass for both this file and the full suite.
- typecheck + lint exit 0.
- Helper signature exactly matches `listInvitedPartners(): Promise<InvitedPartnerRow[]>` with no userId arg (admin-list semantics).
</verification>

<success_criteria>
- listInvitedPartners exists with the D-10 predicate.
- ≥4 unit tests pass with mocked DB.
- ADMIN-09 invariant held (no commission, no password in the returned shape).
- Function is consumable by Phase 14 admin surface without further wiring beyond import.
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-extensions-for-drafts-history/12-03-SUMMARY.md` recording:
- The exact returned-row shape (column list)
- Whether any pagination args were considered (recommend documenting "deferred per CONTEXT — admin list is bounded")
- A pointer to plan 12-07 for the WR-AUDIT-01 closure that makes the derivation correct
</output>
