---
phase: 12-schema-extensions-for-drafts-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0004_phase12_drafts_and_history.sql
  - src/db/schema.ts
autonomous: true
requirements: [DB-01, DB-02, DB-03]
must_haves:
  truths:
    - "drizzle/0004_phase12_drafts_and_history.sql exists and is the next-ordinal migration after 0003_seed_global_params.sql"
    - "proposals.status text column exists with NOT NULL DEFAULT 'active' and CHECK (status IN ('draft','active','deleted'))"
    - "proposals.lc_ref, proposals.idempotency_key, proposals.params_snapshot, proposals.computed are nullable"
    - "proposals_finalized_completeness_check CHECK constraint exists with expression (status = 'draft' OR (lc_ref IS NOT NULL AND idempotency_key IS NOT NULL AND params_snapshot IS NOT NULL AND computed IS NOT NULL))"
    - "proposals_user_id_idempotency_key_uq and proposals_user_id_lc_ref_uq are partial unique indexes WHERE <col> IS NOT NULL"
    - "Existing soft-deleted proposals rows are aligned (status='deleted' where deleted_at IS NOT NULL) by the same migration"
    - "coefficient_history table exists with columns (id uuid pk default gen_random_uuid, changed_at timestamptz default now, changed_by_user_id text FK→users.id ON DELETE SET NULL, before_json jsonb NULL, after_json jsonb NOT NULL, summary text NOT NULL)"
    - "coefficient_history_no_modify() plpgsql function exists and RAISES EXCEPTION 'coefficient_history is append-only — UPDATE and DELETE forbidden'"
    - "coefficient_history_no_update (BEFORE UPDATE FOR EACH ROW) and coefficient_history_no_delete (BEFORE DELETE FOR EACH ROW) triggers exist"
    - "coefficient_history_changed_at_idx descending btree index exists for History sidebar reads"
    - "src/db/schema.ts exports coefficientHistory pgTable + types CoefficientHistoryRow / NewCoefficientHistoryRow"
    - "src/db/schema.ts proposals definition has the new status column + nullability loosenings + partial unique indexes + completeness check"
    - "npm run db:migrate -- --dry-run exits 0 and lists 0004_phase12_drafts_and_history.sql among the migrations"
    - "npm run typecheck exits 0; npm run build exits 0"
  artifacts:
    - path: drizzle/0004_phase12_drafts_and_history.sql
      provides: "DDL — ALTER proposals + CREATE coefficient_history + function + 2 triggers + indexes + soft-delete alignment UPDATE"
      contains: "ALTER TABLE \"proposals\" ADD COLUMN \"status\""
    - path: src/db/schema.ts
      provides: "Drizzle source-of-truth — proposals (with status + loosened nullability + partial indexes + completeness check) + coefficientHistory table + types"
      contains: "export const coefficientHistory = pgTable('coefficient_history'"
  key_links:
    - from: drizzle/0004_phase12_drafts_and_history.sql
      to: src/db/schema.ts
      via: "Drizzle-orm generated SQL must reflect the TypeScript schema definition; both edited together; check by running drizzle-kit check (no schema drift)"
      pattern: "ALTER TABLE \"proposals\" ADD COLUMN \"status\""
    - from: drizzle/0004_phase12_drafts_and_history.sql
      to: scripts/migrate.ts
      via: "scripts/migrate.ts reads drizzle/ directory and applies migrations; the new file is picked up automatically"
      pattern: "0004_phase12"
---

<objective>
Ship the single Drizzle migration file `drizzle/0004_phase12_drafts_and_history.sql` and the corresponding `src/db/schema.ts` source-of-truth updates that together implement the full Phase 12 schema contract (D-01..D-09, D-12..D-13, D-18). This plan ships DDL only — no data backfill, no application-code changes. The migration is verified by `npm run db:migrate -- --dry-run` (SC4).

Purpose: Provide the schema foundation that every other Phase 12 plan and downstream Phase 13/14 work depends on. Without this plan landed, the draft-status query helpers, coefficient-history query helpers, and the wizard's draft persistence all have nowhere to write.

Output: One new migration file, one extended schema source file, both committed atomically. After this plan ships, the schema is ready; data migration (backfill) and code helpers come in later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md
@.planning/phases/08-persistence-pdf-pipeline/08-CONTEXT.md
@src/db/schema.ts
@drizzle/0002_phase8_persistence.sql
@drizzle/0003_seed_global_params.sql
@scripts/migrate.ts

<interfaces>
<!-- Existing schema.ts shape (lines 185-245 — Phase 8 proposals table) the migration is extending. Do NOT re-discover; use these as the contract. -->

From src/db/schema.ts (Phase 8 proposals — current state to extend):
- `proposals.userId` text NOT NULL → users.id (ON DELETE restrict)
- `proposals.language` text NOT NULL (CHECK in 'fr','en')
- `proposals.lcRef` text NOT NULL  ← LOOSEN TO NULL allowed (D-03)
- `proposals.idempotencyKey` text NOT NULL  ← LOOSEN TO NULL allowed (D-03)
- `proposals.schemaVersion` text NOT NULL DEFAULT '1.0.0' (CHECK ~ semver)
- `proposals.inputs` jsonb NOT NULL  ← STAYS NOT NULL; drafts INSERT inputs = '{}'::jsonb
- `proposals.paramsSnapshot` jsonb NOT NULL  ← LOOSEN TO NULL allowed (D-03)
- `proposals.computed` jsonb NOT NULL  ← LOOSEN TO NULL allowed (D-03)
- `proposals.deletedAt` timestamptz NULL (DATA-10 soft delete)
- Unique indexes: `proposals_user_id_idempotency_key_uq`, `proposals_user_id_lc_ref_uq` (currently UNQUALIFIED — must become partial WHERE NOT NULL)
- Partial index: `proposals_deleted_at_idx WHERE deleted_at IS NOT NULL` (KEEP AS-IS — reference for partial-index syntax)

From src/db/schema.ts users table (Phase 6 — exists, do not modify):
- `users.id` text primary key (Better Auth nanoid)
- `users.role` text NOT NULL DEFAULT 'partner' (CHECK in 'partner','admin')

New table to add (D-12, D-13, D-18 step 3):
- `coefficient_history` columns:
  - `id` uuid PK DEFAULT gen_random_uuid() NOT NULL
  - `changed_at` timestamptz NOT NULL DEFAULT now()
  - `changed_by_user_id` text NULL → users.id (ON DELETE SET NULL)
  - `before_json` jsonb NULL (NULL for the seed-row first entry)
  - `after_json` jsonb NOT NULL
  - `summary` text NOT NULL
- Index: `coefficient_history_changed_at_idx` on (`changed_at` DESC)

New CHECK on proposals (D-04):
- `proposals_finalized_completeness_check`: `status = 'draft' OR (lc_ref IS NOT NULL AND idempotency_key IS NOT NULL AND params_snapshot IS NOT NULL AND computed IS NOT NULL)`

Append-only trigger contract (D-12):
- Function name: `coefficient_history_no_modify`
- Language: plpgsql
- Body: `RAISE EXCEPTION 'coefficient_history is append-only — UPDATE and DELETE forbidden'` (exact string with em-dash; the integration test in plan 12-06 binds to this message)
- Returns: NULL (BEFORE triggers may return NULL to abort)
- Trigger 1: `coefficient_history_no_update` BEFORE UPDATE FOR EACH ROW
- Trigger 2: `coefficient_history_no_delete` BEFORE DELETE FOR EACH ROW
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the Drizzle migration file</name>
  <files>drizzle/0004_phase12_drafts_and_history.sql</files>
  <read_first>
    - drizzle/0002_phase8_persistence.sql (canonical syntax: ALTER TABLE, CHECK, partial unique indexes, FK ON DELETE clauses, statement-breakpoint comments)
    - drizzle/0003_seed_global_params.sql (data-migration pattern reference; this plan does NOT do a data backfill but Phase 12-06 will)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-01 through D-09, D-12, D-13, D-18 — locked decisions)
    - src/db/schema.ts (current proposals shape; the migration must align with the schema.ts edits done in Task 2)
  </read_first>
  <behavior>
    - Migration is a single .sql file that, when applied via scripts/migrate.ts, performs ALL of: alter proposals (status column + CHECKs + partial indexes + nullability), align soft-deleted rows, create coefficient_history table + index, create the no-modify function, create both BEFORE-UPDATE and BEFORE-DELETE triggers.
    - Migration is idempotent-safe for re-apply only via drizzle's __drizzle_migrations tracking (the file itself does NOT need IF NOT EXISTS guards — drizzle records applied filenames).
    - The Drizzle migrator separator `--> statement-breakpoint` appears between every top-level statement (matches 0002_phase8_persistence.sql convention; postgres-js executes each separated chunk independently).
    - Migration does NOT add a data backfill of global_params → coefficient_history (deferred to plan 12-06's script per D-14).
  </behavior>
  <action>
Create drizzle/0004_phase12_drafts_and_history.sql containing, in this order with `--> statement-breakpoint` between statements:

1. `ALTER TABLE "proposals" ADD COLUMN "status" text NOT NULL DEFAULT 'active';` (per D-01, D-09 — existing rows get 'active' automatically)
2. `ALTER TABLE "proposals" ADD CONSTRAINT "proposals_status_check" CHECK ("status" IN ('draft','active','deleted'));` (per D-01 — stored enum is 3 values; 'expired' is derived per D-07)
3. `ALTER TABLE "proposals" ALTER COLUMN "lc_ref" DROP NOT NULL;` (per D-03)
4. `ALTER TABLE "proposals" ALTER COLUMN "idempotency_key" DROP NOT NULL;` (per D-03)
5. `ALTER TABLE "proposals" ALTER COLUMN "params_snapshot" DROP NOT NULL;` (per D-03)
6. `ALTER TABLE "proposals" ALTER COLUMN "computed" DROP NOT NULL;` (per D-03 — note: `inputs` and `language` stay NOT NULL)
7. `ALTER TABLE "proposals" ADD CONSTRAINT "proposals_finalized_completeness_check" CHECK ("status" = 'draft' OR ("lc_ref" IS NOT NULL AND "idempotency_key" IS NOT NULL AND "params_snapshot" IS NOT NULL AND "computed" IS NOT NULL));` (per D-04)
8. `DROP INDEX "proposals_user_id_idempotency_key_uq";` then `CREATE UNIQUE INDEX "proposals_user_id_idempotency_key_uq" ON "proposals" USING btree ("user_id","idempotency_key") WHERE "idempotency_key" IS NOT NULL;` (per D-05)
9. `DROP INDEX "proposals_user_id_lc_ref_uq";` then `CREATE UNIQUE INDEX "proposals_user_id_lc_ref_uq" ON "proposals" USING btree ("user_id","lc_ref") WHERE "lc_ref" IS NOT NULL;` (per D-05)
10. `UPDATE "proposals" SET "status" = 'deleted' WHERE "deleted_at" IS NOT NULL;` (per D-09 — align soft-deleted rows so status and deleted_at stay in lockstep going forward; D-08)
11. `CREATE TABLE "coefficient_history" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL, "changed_at" timestamp with time zone DEFAULT now() NOT NULL, "changed_by_user_id" text, "before_json" jsonb, "after_json" jsonb NOT NULL, "summary" text NOT NULL);` (per D-18 step 3 + canonical-refs spec)
12. `ALTER TABLE "coefficient_history" ADD CONSTRAINT "coefficient_history_changed_by_user_id_users_id_fk" FOREIGN KEY ("changed_by_user_id") REFERENCES "users"("id") ON DELETE SET NULL;` (FK per CONTEXT canonical-refs spec — same ON DELETE SET NULL pattern as global_params.created_by)
13. `CREATE INDEX "coefficient_history_changed_at_idx" ON "coefficient_history" USING btree ("changed_at" DESC);` (newest-first reads — matches global_params_effective_from_idx pattern)
14. `CREATE OR REPLACE FUNCTION "coefficient_history_no_modify"() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE EXCEPTION 'coefficient_history is append-only — UPDATE and DELETE forbidden'; END; $$;` (per D-12 — the EXACT exception string is the integration-test gate in 12-06)
15. `CREATE TRIGGER "coefficient_history_no_update" BEFORE UPDATE ON "coefficient_history" FOR EACH ROW EXECUTE FUNCTION "coefficient_history_no_modify"();` (per D-12)
16. `CREATE TRIGGER "coefficient_history_no_delete" BEFORE DELETE ON "coefficient_history" FOR EACH ROW EXECUTE FUNCTION "coefficient_history_no_modify"();` (per D-12)

Top of file MUST include a leading comment block (matching 0003 pattern):
"-- Plan 12-01 — schema extensions for drafts (DB-01) + invited derivation prep (DB-02) + coefficient_history append-only history (DB-03).
-- Per 12-CONTEXT.md decisions D-01..D-09, D-12..D-13, D-18.
-- DDL ONLY. The coefficient_history backfill of existing global_params rows is performed by scripts/backfill-coefficient-history.ts (plan 12-06) AFTER this migration applies.
-- DO NOT EDIT BY HAND once committed — superseded by a follow-up migration if changes needed."

Do NOT include any `npx drizzle-kit push` invocation or any other migration-runner shell-out. The only application path is `scripts/migrate.ts` per BOOT-10.
  </action>
  <verify>
    <automated>npm run db:migrate -- --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Source: `drizzle/0004_phase12_drafts_and_history.sql` exists.
    - Source: grep `ALTER TABLE "proposals" ADD COLUMN "status" text NOT NULL DEFAULT 'active'` returns ≥1 hit.
    - Source: grep `proposals_status_check` AND grep `'draft','active','deleted'` both return ≥1 hit.
    - Source: grep `DROP NOT NULL` returns exactly 4 hits (lc_ref, idempotency_key, params_snapshot, computed).
    - Source: grep `proposals_finalized_completeness_check` AND grep `'draft' OR` both return ≥1 hit.
    - Source: grep `WHERE "idempotency_key" IS NOT NULL` AND grep `WHERE "lc_ref" IS NOT NULL` both return ≥1 hit (partial unique indexes).
    - Source: grep `UPDATE "proposals" SET "status" = 'deleted' WHERE "deleted_at" IS NOT NULL` returns ≥1 hit.
    - Source: grep `CREATE TABLE "coefficient_history"` returns ≥1 hit; the CREATE TABLE block contains `id uuid`, `changed_at timestamp with time zone`, `changed_by_user_id text`, `before_json jsonb`, `after_json jsonb NOT NULL`, `summary text NOT NULL`.
    - Source: grep `coefficient_history_changed_by_user_id_users_id_fk` returns ≥1 hit AND the FK clause contains `ON DELETE SET NULL`.
    - Source: grep `coefficient_history_changed_at_idx` returns ≥1 hit (index uses `changed_at DESC`).
    - Source: grep `coefficient_history_no_modify` returns ≥3 hits (function + 2 triggers); the function body grep `RAISE EXCEPTION 'coefficient_history is append-only — UPDATE and DELETE forbidden'` returns ≥1 hit (exact match including em-dash).
    - Source: grep `BEFORE UPDATE ON "coefficient_history"` AND grep `BEFORE DELETE ON "coefficient_history"` both return ≥1 hit.
    - Source: grep `--> statement-breakpoint` returns ≥15 hits (every statement separated).
    - Source: grep `drizzle-kit push` against this file returns 0 hits.
    - CLI: `npm run db:migrate -- --dry-run` (with `DATABASE_URL` set to the dev DB) exits 0 and the stdout includes `0004_phase12_drafts_and_history.sql`.
  </acceptance_criteria>
  <done>
The migration file is committed; the dry-run lists it; the proposals + coefficient_history shapes match the locked CONTEXT decisions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update src/db/schema.ts to mirror the migration</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (current — read in full; the proposals table at lines 185-245 is what we modify; the global_params + users tables stay unchanged)
    - drizzle/0004_phase12_drafts_and_history.sql (just-authored — the source of truth the schema.ts must mirror)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-01..D-09, D-12)
  </read_first>
  <behavior>
    - Drizzle generate (if re-run) MUST produce zero diff against 0004_phase12_drafts_and_history.sql — verify by running `npx drizzle-kit check` after edits.
    - Type exports for the new table land in the existing type-exports block at the bottom of schema.ts (matches `ProposalRow / NewProposalRow` pattern).
    - Existing imports stay intact; only add identifiers actually used (no unused imports — ESLint catches this).
  </behavior>
  <action>
Edit src/db/schema.ts:

(a) Inside the `proposals` pgTable definition (currently lines 185-245):
- Add a new column: `status: text('status').notNull().default('active')` immediately after `userId` (or at a position that mirrors the migration's ALTER ordering — placement within the JS object is cosmetic but match the migration).
- Drop `.notNull()` from `lcRef`, `idempotencyKey`, `paramsSnapshot`, `computed` (4 columns total per D-03). `inputs` and `language` stay `.notNull()` per D-03.
- Add to the table's constraints-array argument:
  - `check('proposals_status_check', sql\`${table.status} IN ('draft','active','deleted')\`)` (D-01)
  - `check('proposals_finalized_completeness_check', sql\`${table.status} = 'draft' OR (${table.lcRef} IS NOT NULL AND ${table.idempotencyKey} IS NOT NULL AND ${table.paramsSnapshot} IS NOT NULL AND ${table.computed} IS NOT NULL)\`)` (D-04)
- Convert the two `uniqueIndex` calls to partial via `.where(sql\`${table.idempotencyKey} IS NOT NULL\`)` and `.where(sql\`${table.lcRef} IS NOT NULL\`)` respectively (D-05 — same `.where(sql\`...\`)` pattern as the existing `proposals_deleted_at_idx` at lines 242-244).
- TypeScript-level: the `paramsSnapshot.$type<{...}>` jsonb generic stays as-is; the column-level nullability is signalled by removing `.notNull()` — Drizzle's `$inferSelect` then types the field as `T | null`. Update consuming code in later plans accordingly (Plan 12-05 reads/writes drafts where these are null).

(b) Add a new `coefficientHistory` pgTable definition immediately after the existing `auditLog` table (around line 278):

```
export const coefficientHistory = pgTable('coefficient_history', {
  id: uuid('id').defaultRandom().primaryKey(),
  changedAt: timestamp('changed_at', { withTimezone: true }).notNull().defaultNow(),
  changedByUserId: text('changed_by_user_id').references(() => users.id, { onDelete: 'set null' }),
  beforeJson: jsonb('before_json').$type<Record<string, unknown> | null>(),
  afterJson: jsonb('after_json').$type<Record<string, unknown>>().notNull(),
  summary: text('summary').notNull(),
}, (table) => [
  index('coefficient_history_changed_at_idx').on(sql`${table.changedAt} DESC`),
]);
```

(Note: the `<action>` block above shows the structure without claiming this is "the implementation" — see read_first migration file for the exact column types and FK rules. The executor pastes the equivalent valid TypeScript inline.)

Add to the type exports at the bottom of schema.ts:
- `export type CoefficientHistoryRow = typeof coefficientHistory.$inferSelect;`
- `export type NewCoefficientHistoryRow = typeof coefficientHistory.$inferInsert;`

DOCSTRING: Add a JSDoc block on the new table explaining:
- "DB-03 per 12-CONTEXT.md D-12, D-13. Append-only at the DB layer via triggers `coefficient_history_no_update` and `coefficient_history_no_delete` (defined in drizzle/0004_phase12_drafts_and_history.sql). Any UPDATE or DELETE raises 'coefficient_history is append-only — UPDATE and DELETE forbidden'."
- "Backfill from existing global_params rows is performed by scripts/backfill-coefficient-history.ts (idempotent, see plan 12-06)."

Do NOT change the `globalParams`, `users`, `sessions`, `accounts`, `verifications`, `passwordResets`, `auditLog` table definitions. Do NOT remove or rename any existing index.
  </action>
  <verify>
    <automated>npm run typecheck && npx drizzle-kit check</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep `export const coefficientHistory = pgTable('coefficient_history'` returns exactly 1 hit in `src/db/schema.ts`.
    - Source: grep `export type CoefficientHistoryRow` returns exactly 1 hit.
    - Source: grep `proposals_status_check` returns ≥1 hit in `src/db/schema.ts`.
    - Source: grep `proposals_finalized_completeness_check` returns ≥1 hit in `src/db/schema.ts`.
    - Source: in the `proposals` pgTable definition, the four columns `lcRef`, `idempotencyKey`, `paramsSnapshot`, `computed` MUST NOT have `.notNull()` — grep `lcRef: text\('lc_ref'\)\.notNull` returns 0 hits; grep `idempotencyKey: text\('idempotency_key'\)\.notNull` returns 0 hits; grep `paramsSnapshot: jsonb\([^)]+\)\.\$type<[^>]+>\(\)\.notNull` returns 0 hits; grep `computed: jsonb\('computed'\)\.\$type<[^>]+>\(\)\.notNull` returns 0 hits.
    - Source: the two `uniqueIndex` calls for `proposals_user_id_idempotency_key_uq` and `proposals_user_id_lc_ref_uq` each include `.where(sql\`` — grep `proposals_user_id_idempotency_key_uq.*\.where` returns ≥1 hit (multiline-aware grep, e.g. ripgrep `-U`).
    - CLI: `npm run typecheck` exits 0.
    - CLI: `npx drizzle-kit check` exits 0 (no schema-drift error; the schema.ts and the 0004 migration agree).
    - CLI: `npm run lint` exits 0.
  </acceptance_criteria>
  <done>
schema.ts is the TypeScript twin of the migration; typecheck + drizzle-kit check + lint all 0. No compilation regressions in consumers (any TS errors in proposals.ts query helpers from nullability changes are resolved here OR documented as known-broken-and-fixed-in-12-05; recommend documenting in commit message since 12-05 fixes them).
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Verify the migration applies cleanly via dry-run gate</name>
  <files></files>
  <read_first>
    - scripts/migrate.ts (the only authorized migration path; verify the --dry-run handling in `main()` does NOT call `migrate()` — it just lists files and exits 0)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-19 — SC4 binds to this command)
  </read_first>
  <behavior>
    - The dry-run prints the migration filename, exits 0, and does NOT mutate the database.
    - For follow-up confidence, also run `npm run db:migrate` against the LOCAL development DB (NOT prod) so the trigger function and table are actually created — this enables Plan 12-06's integration test to run against a real Postgres later.
    - PROD apply is deferred to `.github/workflows/db-migrate.yml` (typed-confirmation `MIGRATE PROD` + GitHub Environment reviewer approval per BOOT-10); this plan does NOT trigger that workflow.
  </behavior>
  <action>
With `DATABASE_URL` set to a development or preview Postgres URL (NOT production), run:

1. `npm run db:migrate -- --dry-run` — must list `0004_phase12_drafts_and_history.sql` among the pending migrations and exit 0.
2. `npm run db:migrate` — apply the migration to the dev DB so subsequent plans (esp. 12-06 integration test) can verify the trigger behavior against a real Postgres. (If the executor lacks a dev DB at this point, this step may be deferred to 12-06's environment; record the choice in the plan SUMMARY.)
3. After apply, verify with a one-off psql/pg-client query (the executor's choice of client; e.g., `psql $DATABASE_URL -c "\d coefficient_history"`):
   - `coefficient_history` table exists
   - both triggers `coefficient_history_no_update` and `coefficient_history_no_delete` exist
   - `proposals.status` column exists with the CHECK constraint

Do NOT propose `npx drizzle-kit push` anywhere. Do NOT add any new npm script for migration push. The only authorized migration commands are `npm run db:migrate` and `npm run db:migrate -- --dry-run`.

If interactive prompts surface (none expected — scripts/migrate.ts is non-interactive), record that observation and mark the plan as `autonomous: false` in a follow-up commit; this is not expected.
  </action>
  <verify>
    <automated>npm run db:migrate -- --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npm run db:migrate -- --dry-run` (with `DATABASE_URL` set to dev/preview) exits 0; stdout contains the string `0004_phase12_drafts_and_history.sql`.
    - CLI (when applied to dev DB): `npm run db:migrate` exits 0; "Migrations applied successfully." appears in stdout.
    - Behavior (after dev-apply, optional but recommended): `psql $DATABASE_URL -c "SELECT 1 FROM pg_trigger WHERE tgname = 'coefficient_history_no_update'"` returns 1 row; the same for `coefficient_history_no_delete`.
    - Behavior (after dev-apply, optional): `psql $DATABASE_URL -c "INSERT INTO coefficient_history (after_json, summary) VALUES ('{\"a\":1}'::jsonb, 'test')"` succeeds; the follow-up `UPDATE coefficient_history SET summary = 'x'` returns error containing the string `coefficient_history is append-only`.
  </acceptance_criteria>
  <done>
SC4 satisfied: the migration is dry-run-clean against the configured DB; the executor records in the plan SUMMARY which environment was used for the dry-run (and the optional apply).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → DB | DDL applied with elevated privileges; only `scripts/migrate.ts` is authorized (BOOT-10) |
| coefficient_history rows | Append-only at DB; no application code may UPDATE or DELETE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | coefficient_history rows | mitigate | DB-level TRIGGER (coefficient_history_no_modify) raises EXCEPTION on UPDATE/DELETE. Function + 2 triggers created in this migration. Empirical proof via integration test in plan 12-06. |
| T-12-01-02 | Tampering | proposals.status drift vs deleted_at | mitigate | proposals_finalized_completeness_check CHECK constraint forbids active-row inserts with NULL snapshot/lc_ref/idempotency_key/computed. Application-layer lockstep enforced by softDeleteProposal/restoreProposal helpers (modified in plan 12-05). |
| T-12-01-03 | Spoofing | unauthorized status values | mitigate | proposals_status_check CHECK constraint restricts to ('draft','active','deleted'); any INSERT/UPDATE with another value fails at the DB. |
| T-12-01-04 | Repudiation | coefficient changes lack actor attribution | mitigate | coefficient_history.changed_by_user_id FK → users.id provides actor identity; combined with the append-only trigger, history rows are tamper-evident and attributable. |
| T-12-01-05 | EoP | drizzle-kit push bypass of migration discipline | mitigate | scripts/check-no-drizzle-push.sh CI grep gate (existing, BOOT-10) blocks any commit containing `drizzle-kit push`. This plan adds zero `drizzle-kit push` references. |
| T-12-01-06 | DoS | jsonb beforeJson/afterJson unbounded size | accept | global_params jsonb is bounded by admin form Zod (existing ADMIN-01); each history row is at most ~1 KB. Even hundreds of edits/year are <1 MB total. Re-evaluate if growth exceeds expectations. |
| T-12-01-07 | Information disclosure | commission_pct leaks via coefficient_history reads | accept | coefficient_history is admin-only (reads gated by requireAdmin() in plan 14 surface). No partner-facing path exists. ADMIN-09 commission-invisibility does not apply to admin audit surfaces by design. |
</threat_model>

<verification>
- Migration file exists at the correct ordinal; dry-run lists it; schema.ts compiles.
- After dev-apply, psql confirms presence of `coefficient_history` table, both triggers, the `proposals.status` column and its CHECK constraints.
- `npm run typecheck && npm run lint && npm run build` all 0 after schema.ts edits (note: consumers of `proposals.lcRef` / `idempotencyKey` / `paramsSnapshot` / `computed` may have type errors due to nullability changes — these are resolved in plan 12-05; if errors block the build, scope this plan to schema-only + record consumer-fix as a known-pending in the SUMMARY).
- `scripts/check-no-drizzle-push.sh` exits 0 on the committed changes.
</verification>

<success_criteria>
- drizzle/0004_phase12_drafts_and_history.sql exists with all 14 statements per the Action of Task 1.
- src/db/schema.ts has the new `coefficientHistory` table, the new `status` column on proposals, the nullability loosenings, the two new CHECK constraints, the two partial unique indexes, and the type exports.
- `npm run db:migrate -- --dry-run` (dev DB) exits 0 and includes 0004 in the listing — SC4 satisfied.
- `npx drizzle-kit check` exits 0 (no schema drift between TS and SQL).
- Zero `drizzle-kit push` invocations anywhere in this plan's commits.
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-extensions-for-drafts-history/12-01-SUMMARY.md` recording:
- Whether the migration was also applied to the dev DB (yes/no, which environment)
- Any consumer-side TypeScript errors that surfaced from the nullability changes (typed list — plan 12-05 will fix these)
- The exact PostgreSQL version of the dev DB used for the dry-run (e.g., Neon's 15 vs 16)
</output>
