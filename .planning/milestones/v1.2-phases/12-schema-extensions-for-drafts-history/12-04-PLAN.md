---
phase: 12-schema-extensions-for-drafts-history
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - src/lib/db/queries/coefficient-history.ts
  - src/lib/db/queries/coefficient-history.test.ts
autonomous: true
requirements: [DB-03]
must_haves:
  truths:
    - "src/lib/db/queries/coefficient-history.ts exports createCoefficientHistoryEntry(args) returning Promise<CoefficientHistoryRow>"
    - "src/lib/db/queries/coefficient-history.ts exports listCoefficientHistory(args?) returning Promise<CoefficientHistoryListResult>"
    - "createCoefficientHistoryEntry accepts { before, after, userId, summary? }; if summary is undefined or empty/whitespace, it calls generateDiffSummary(before, after) and uses that"
    - "createCoefficientHistoryEntry passes summary verbatim if non-empty (D-16)"
    - "listCoefficientHistory returns rows ordered changed_at DESC; cursor-paginated like listGlobalParamsHistory (default limit 20, +1 fetch for hasMore)"
    - "Vitest unit tests with mocked DB cover: (a) summary-arg present is stored verbatim, (b) summary-arg empty/undefined falls back to generateDiffSummary, (c) list returns cursor-paginated rows"
  artifacts:
    - path: src/lib/db/queries/coefficient-history.ts
      provides: "Server-only query helpers for coefficient_history reads + writes (DB-03)"
      exports: ["createCoefficientHistoryEntry", "listCoefficientHistory", "CoefficientHistoryCursor", "encodeCoefficientHistoryCursor", "decodeCoefficientHistoryCursor"]
    - path: src/lib/db/queries/coefficient-history.test.ts
      provides: "Vitest unit tests for both helpers with mocked DB"
  key_links:
    - from: src/lib/db/queries/coefficient-history.ts
      to: src/lib/admin/coefficient-diff.ts
      via: "createCoefficientHistoryEntry imports generateDiffSummary for the auto-fallback when summary arg is empty (per D-16)"
      pattern: "import .*generateDiffSummary.*from '@/lib/admin/coefficient-diff'"
    - from: src/lib/db/queries/coefficient-history.ts
      to: src/db/schema.ts (coefficientHistory table)
      via: "Drizzle insert into schema.coefficientHistory; select from same table for the history list"
      pattern: "schema\\.coefficientHistory"
---

<objective>
Ship the two server-only query helpers for `coefficient_history` (DB-03) in a new file `src/lib/db/queries/coefficient-history.ts`. `createCoefficientHistoryEntry` is the single authorized write path; it auto-falls-back to `generateDiffSummary(before, after)` from plan 12-02 when the admin doesn't provide a manual summary (D-16). `listCoefficientHistory` is the read path the Phase 14 History sidebar will consume.

Purpose: Give Phase 14 a clean read/write API for the audit table. Encapsulating the diff fallback inside the write helper means the admin form (Phase 14) only needs to pass the optional summary string — no diff logic in the UI layer.

Output: One new source file, one new test file. Atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md
@src/lib/db/queries/global-params.ts
@src/lib/db/queries/proposals.test.ts
@src/lib/admin/coefficient-diff.ts
@src/db/schema.ts

<interfaces>
<!-- The coefficientHistory table type added by plan 12-01 in src/db/schema.ts: -->

From src/db/schema.ts (added by plan 12-01):
- `coefficientHistory` columns: id (uuid), changedAt (timestamptz default now), changedByUserId (text FK→users.id ON DELETE SET NULL, nullable), beforeJson (jsonb nullable), afterJson (jsonb NOT NULL), summary (text NOT NULL)
- Type exports: `CoefficientHistoryRow`, `NewCoefficientHistoryRow`
- Index `coefficient_history_changed_at_idx` on (changed_at DESC) for newest-first reads

From src/lib/admin/coefficient-diff.ts (plan 12-02):
- `export function generateDiffSummary(before: GlobalParamsSnapshot | null, after: GlobalParamsSnapshot): string`
- Returns `'Configuration initiale'` when before is null, `'Aucun changement'` when no diff, otherwise FR semicolon-separated diff string

From src/lib/db/queries/global-params.ts (pattern reference):
- `listGlobalParamsHistory(args)` — cursor-paginated newest-first, default limit 20, fetch limit+1 for hasMore, returns `{ rows, hasMore, nextCursor }`
- Cursor shape: `{ effectiveFrom: string; id: string }`; encode via Buffer.from(JSON.stringify(c)).toString('base64url')

CreateCoefficientHistoryEntry args contract per D-16:
- `before: GlobalParamsSnapshot | null` — snapshot of the global_params row immediately prior to the edit (null for the seed row)
- `after: GlobalParamsSnapshot` — snapshot of the newly-inserted row
- `userId: string | null` — the admin who edited (null if system-initiated, e.g. backfill seed row)
- `summary?: string` — optional admin-provided label; falls back to generateDiffSummary if empty/undefined/whitespace
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/db/queries/coefficient-history.ts with both helpers</name>
  <files>src/lib/db/queries/coefficient-history.ts</files>
  <read_first>
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-12, D-14, D-16, D-17)
    - src/lib/db/queries/global-params.ts (pattern: imports, cursor encode/decode, listX cursor-paginated shape, JSDoc style)
    - src/lib/admin/coefficient-diff.ts (the generateDiffSummary signature + types — plan 12-02 output)
    - src/db/schema.ts (after plan 12-01 edits — verify coefficientHistory table + types exist)
  </read_first>
  <behavior>
    - File begins with `import 'server-only';` per the established Phase 8 convention.
    - createCoefficientHistoryEntry never UPDATEs — it only INSERTs (the trigger would block UPDATE anyway, but the helper signature reflects the intent).
    - createCoefficientHistoryEntry returns the newly-inserted row via `.returning()`.
    - listCoefficientHistory mirrors `listGlobalParamsHistory` pagination shape exactly: cursor `{ changedAt: string; id: string }`, default limit 20, fetch limit+1 for hasMore.
    - Both helpers are typed against `CoefficientHistoryRow` / `NewCoefficientHistoryRow` from `@/db/schema`.
  </behavior>
  <action>
Create `src/lib/db/queries/coefficient-history.ts`:

1. Top-of-file imports (mirror global-params.ts):
   - `import 'server-only';`
   - `import { and, desc, sql } from 'drizzle-orm';`
   - `import { db, schema } from '@/lib/db';`
   - `import type { CoefficientHistoryRow, NewCoefficientHistoryRow } from '@/db/schema';`
   - `import { generateDiffSummary, type GlobalParamsSnapshot } from '@/lib/admin/coefficient-diff';`

2. Top-of-file JSDoc block: state DB-03 + reference to 12-CONTEXT.md D-12, D-14, D-16. Call out: "The DB-level trigger `coefficient_history_no_modify` (drizzle/0004_phase12_drafts_and_history.sql) raises EXCEPTION on any UPDATE or DELETE — both helpers below avoid those paths by construction. createCoefficientHistoryEntry is INSERT-only; listCoefficientHistory is SELECT-only."

3. Export the args interface:
   ```
   export interface CreateCoefficientHistoryArgs {
     before: GlobalParamsSnapshot | null;
     after: GlobalParamsSnapshot;
     userId: string | null;
     summary?: string;
   }
   ```

4. Export `createCoefficientHistoryEntry(args)` returning `Promise<CoefficientHistoryRow>`:
   - Compute `summary`: if `args.summary` is a string with non-empty trim, use it verbatim. Else, call `generateDiffSummary(args.before, args.after)` and use the result. Per D-16.
   - INSERT one row into schema.coefficientHistory:
     - `changedByUserId: args.userId`
     - `beforeJson: args.before` (jsonb $type may need cast — match the proposals.paramsSnapshot cast pattern in src/lib/db/queries/proposals.ts:90 if needed)
     - `afterJson: args.after`
     - `summary: <resolved summary>`
   - `.returning()`; return the first row.

5. Export the cursor type and encode/decode (mirror global-params.ts:40-79):
   ```
   export type CoefficientHistoryCursor = { changedAt: string; id: string };
   export function encodeCoefficientHistoryCursor(c: CoefficientHistoryCursor): string { ... }
   export function decodeCoefficientHistoryCursor(encoded: string): CoefficientHistoryCursor | null { ... }
   ```
   Same ISO_RE + UUID_RE validation pattern as global-params.ts:53-69.

6. Export the list args interface:
   ```
   export interface ListCoefficientHistoryArgs {
     cursor?: CoefficientHistoryCursor | null;
     limit?: number; // default 20
   }
   export interface CoefficientHistoryListResult {
     rows: CoefficientHistoryRow[];
     hasMore: boolean;
     nextCursor: CoefficientHistoryCursor | null;
   }
   const DEFAULT_HISTORY_LIMIT = 20;
   ```

7. Export `listCoefficientHistory(args?)` returning `Promise<CoefficientHistoryListResult>`:
   - Mirror listGlobalParamsHistory exactly (global-params.ts:91-136), substituting:
     - `schema.coefficientHistory` for `schema.globalParams`
     - cursor column `changedAt` for `effectiveFrom`
   - Cursor predicate: `sql\`(${schema.coefficientHistory.changedAt}, ${schema.coefficientHistory.id}) < (${cursor.changedAt}::timestamptz, ${cursor.id}::uuid)\``
   - ORDER BY: `desc(schema.coefficientHistory.changedAt), desc(schema.coefficientHistory.id)`
   - Fetch limit+1, slice off the overflow, set hasMore and nextCursor

8. Do NOT join users to surface the actor display name in this plan — the Phase 14 surface can do it (Phase 9 listGlobalParamsHistory does it via LEFT JOIN; the parallel work is fine here, but to keep this plan focused on the write+read path, leave the JOIN for Phase 14 OR include it now if the executor judges it trivial. Recommend: include the same LEFT JOIN as global-params.ts:107-126 for parity, returning `createdByDisplay: COALESCE(users.display_name, users.email)` as the optional augmentation. Document the decision in the SUMMARY).

Do NOT add an UPDATE or DELETE helper — the trigger blocks them; the absence of a helper is the documentation. Do NOT add a soft-delete column.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --quiet src/lib/db/queries/coefficient-history.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/db/queries/coefficient-history.ts` exists.
    - Source: grep `export async function createCoefficientHistoryEntry` returns exactly 1 hit.
    - Source: grep `export async function listCoefficientHistory` returns exactly 1 hit.
    - Source: grep `import .* generateDiffSummary` returns ≥1 hit.
    - Source: grep `import 'server-only'` returns exactly 1 hit (top of file).
    - Source: grep `schema\.coefficientHistory` returns ≥4 hits (insert + select + cursor + order).
    - Source: grep `\.update\(schema\.coefficientHistory\)` returns 0 hits (no UPDATE helper).
    - Source: grep `\.delete\(schema\.coefficientHistory\)` returns 0 hits (no DELETE helper).
    - Source: grep `\.trim\(\)` returns ≥1 hit (summary fallback check — empty/whitespace handling).
    - CLI: `npm run typecheck` exits 0.
    - CLI: `npm run lint -- --quiet src/lib/db/queries/coefficient-history.ts` exits 0.
  </acceptance_criteria>
  <done>
File exists with both helpers; auto-fallback wired via generateDiffSummary; cursor pagination mirrors the existing listGlobalParamsHistory pattern; no UPDATE/DELETE helpers; typecheck + lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest unit tests for both helpers with mocked DB</name>
  <files>src/lib/db/queries/coefficient-history.test.ts</files>
  <read_first>
    - src/lib/db/queries/coefficient-history.ts (just-authored — the functions under test)
    - src/lib/db/queries/proposals.test.ts (canonical mock-DB pattern — copy the vi.mock blocks; structure: `select()`, `from()`, `where()`, `orderBy()`, `limit()`, `insert()`, `values()`, `returning()`)
    - src/lib/db/queries/global-params.test.ts (sister test pattern reference)
    - src/lib/admin/coefficient-diff.ts (the diff function the test uses unmocked — it's pure)
  </read_first>
  <behavior>
    - Use the proposals.test.ts mock-DB pattern: `vi.mock('@/lib/db', ...)` with a stub builder + a `calls` array recording each method invocation.
    - generateDiffSummary is NOT mocked — it's pure; tests rely on its real behavior to verify the fallback.
    - The insert path mock returns a fixed CoefficientHistoryRow shape from `.returning()`.
  </behavior>
  <action>
Create `src/lib/db/queries/coefficient-history.test.ts`:

1. Header (mirror proposals.test.ts):
   - `import { describe, it, expect, vi, beforeEach } from 'vitest';`
   - `vi.mock('server-only', () => ({}));`
   - `vi.mock('@/lib/db', async () => { ... })` — the stub builder + calls array, identical pattern to proposals.test.ts:9-42. The `.returning()` should resolve to a single mock CoefficientHistoryRow with id='mock-uuid', changedAt='2026-05-12T10:00:00Z', summary captured-from-insert.

2. Import functions under test:
   - `import { createCoefficientHistoryEntry, listCoefficientHistory, encodeCoefficientHistoryCursor, decodeCoefficientHistoryCursor } from './coefficient-history';`

3. Fixture pattern (matching the seed values from drizzle/0003_seed_global_params.sql):
   ```
   const baseSnapshot = {
     commissionPct: '5.0000',
     maxAmount: '500000.00',
     validityDays: 30,
     coefficients: {
       t1: { '36': '3.0000', '48': '2.3000', '60': '1.8765' },
       t2: { '36': '2.9000', '48': '2.2500', '60': '1.8500' },
       t3: { '36': '2.8000', '48': '2.2000', '60': '1.8000' },
       t4: { '36': '2.7000', '48': '2.1500', '60': '1.7500' },
     },
   };
   ```

4. Test cases for `createCoefficientHistoryEntry`:
   - `'uses provided summary verbatim when non-empty'` — call with `summary: 'Manual admin label'`; capture the `insert.values` call from the calls array; assert `.payload.summary === 'Manual admin label'`.
   - `'uses generateDiffSummary fallback when summary is undefined'` — call without summary; the diff between `before=baseSnapshot` and `after={...baseSnapshot, commissionPct: '5.5000'}` is `'Commission: 5.0000% → 5.5000%'`; assert `.payload.summary === 'Commission: 5.0000% → 5.5000%'`.
   - `'uses generateDiffSummary fallback when summary is empty string'` — pass `summary: ''`; assert fallback applied.
   - `'uses generateDiffSummary fallback when summary is whitespace-only'` — pass `summary: '   '`; assert fallback applied.
   - `'passes "Configuration initiale" when before is null'` — call with `before: null`; assert `.payload.summary === 'Configuration initiale'`.
   - `'persists before/after as jsonb in beforeJson/afterJson'` — assert the captured `insert.values` payload includes the before and after objects under the right column names.
   - `'records the userId in changedByUserId'` — call with `userId: 'u-abc'`; assert `.payload.changedByUserId === 'u-abc'`.
   - `'accepts userId=null for system-initiated entries'` — call with `userId: null`; assert no crash, `.payload.changedByUserId === null`.

5. Test cases for `listCoefficientHistory`:
   - `'queries with limit = limit + 1 to compute hasMore'` — assert the captured `limit` payload === `21` when default; === `<n+1>` when caller passes `n`.
   - `'orderBy is called (descending)'` — assert `calls.some((c) => c.kind === 'orderBy')` is true.
   - `'no cursor predicate when cursor null/undefined'` — call with no args; the only `where` call (if any) has no cursor predicate.
   - `'cursor predicate applied when cursor provided'` — pass `cursor: { changedAt: '2026-05-12T10:00:00.000Z', id: '11111111-2222-3333-4444-555555555555' }`; assert `where` was called.
   - `'returns { rows: [], hasMore: false, nextCursor: null } when db returns empty'`.

6. Test cases for cursor encode/decode:
   - `'round-trips a cursor'` — encode then decode returns the same object.
   - `'returns null for malformed cursor'` — `decodeCoefficientHistoryCursor('garbage')` returns null.
   - `'returns null for partial cursor missing id'`.

Do NOT mock generateDiffSummary — its purity is the test point. Do NOT add a real-Postgres integration test here (that's plan 12-06's job).
  </action>
  <verify>
    <automated>npx vitest run src/lib/db/queries/coefficient-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npx vitest run src/lib/db/queries/coefficient-history.test.ts` exits 0.
    - CLI: stdout reports ≥12 tests passing (8 createCoefficientHistoryEntry + 5 listCoefficientHistory + 3 cursor — adjust counts to match the action's listed cases).
    - Source: grep `'Manual admin label'` returns ≥1 hit.
    - Source: grep `'Commission: 5.0000% → 5.5000%'` returns ≥1 hit (with Unicode arrow).
    - Source: grep `'Configuration initiale'` returns ≥1 hit.
    - Source: grep `summary: ''` returns ≥1 hit (empty-string fallback case).
    - Source: grep `summary: '   '` OR `summary: '    '` returns ≥1 hit (whitespace fallback case).
    - Source: grep `vi.mock\('server-only'` AND grep `vi.mock\('@/lib/db'` both return ≥1 hit.
    - CLI: `npm test` (full suite) exits 0 — no regressions.
  </acceptance_criteria>
  <done>
≥12 tests pass; the auto-fallback contract is empirically verified for present/empty/whitespace/undefined summary; the full suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin save flow → createCoefficientHistoryEntry | Caller (Phase 14 admin form) is gated by requireAdmin(); helper does not re-check |
| coefficient_history rows | Append-only at DB; the absence of UPDATE/DELETE helpers documents the contract at the type level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | application code circumvents append-only by directly db.update(schema.coefficientHistory) | mitigate | DB-level trigger blocks the UPDATE regardless of the call path. Helper file deliberately exposes no UPDATE/DELETE function — convention + grep gate could be added in a follow-up (Phase 14 candidate). |
| T-12-04-02 | Repudiation | history rows lack actor attribution | mitigate | createCoefficientHistoryEntry takes userId (caller passes session.user.id) and writes to changedByUserId FK→users.id. Backfill (plan 12-06) passes null for the seed row (no actor existed at seed time) — documented as expected. |
| T-12-04-03 | Information disclosure | listCoefficientHistory exposes commission_pct to non-admin | mitigate | Admin-only surface (Phase 14 gates with requireAdmin()). `import 'server-only'` blocks client-component import. Note: commission_pct IS the audit value here — admin-facing exposure is by design (per CONTEXT canonical-refs). |
| T-12-04-04 | DoS | listCoefficientHistory unbounded result set | mitigate | Cursor-paginated with default limit 20; +1 fetch for hasMore (same pattern as listGlobalParamsHistory). |
| T-12-04-05 | Spoofing | malformed cursor input from client crashes the SQL | mitigate | decodeCoefficientHistoryCursor validates ISO date + UUID v4 shape with regex; returns null on malformed input. Callers treat null as "no cursor" (start from newest). |
| T-12-04-06 | Tampering on summary | admin injects HTML/script via the summary field | accept | summary is text stored in DB; rendering surface (Phase 14 history sidebar) is responsible for escaping. Standard React JSX text escaping handles it; document in 14 plan. |
</threat_model>

<verification>
- Vitest run for `coefficient-history.test.ts` exits 0 with ≥12 passing tests.
- typecheck + lint exit 0 for the new source + test files.
- `npm test` full suite stays green.
- Auto-fallback empirically verified (present-summary verbatim; empty/undefined/whitespace falls back).
- No UPDATE/DELETE helpers in the module (grep evidence in acceptance criteria).
</verification>

<success_criteria>
- createCoefficientHistoryEntry has the exact D-16 fallback contract.
- listCoefficientHistory matches the listGlobalParamsHistory cursor-pagination shape (no surprises for the Phase 14 sidebar consumer).
- Tests bind to the four fallback variants (present, undefined, '', '   ') and to the null-before seed case.
- File is consumable by plan 12-06's backfill script (createCoefficientHistoryEntry is the single insertion path used by the script).
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-extensions-for-drafts-history/12-04-SUMMARY.md` recording:
- Whether the listCoefficientHistory LEFT JOIN to users (createdByDisplay) was included now or deferred to Phase 14 (recommend now for parity with global-params.ts; document either choice)
- The exact count of unit tests added
- Any deviation from the proposals.test.ts mock-DB pattern (recommend: none)
</output>
