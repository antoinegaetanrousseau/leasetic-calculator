---
phase: 12-schema-extensions-for-drafts-history
plan: 06
type: execute
wave: 3
depends_on: [12-04]
files_modified:
  - scripts/backfill-coefficient-history.ts
  - package.json
  - docs/operations/launch-checklist.md
  - src/lib/db/queries/coefficient-history.integration.test.ts
autonomous: true
requirements: [DB-03]
must_haves:
  truths:
    - "scripts/backfill-coefficient-history.ts exists and is idempotent — re-running on an already-backfilled DB exits 0 with a 'Already backfilled' message"
    - "backfill iterates global_params rows ordered by effective_from ASC and inserts one coefficient_history row per global_params row"
    - "first row (seed) is inserted with before_json=NULL → generateDiffSummary returns 'Configuration initiale' as the auto-summary"
    - "subsequent rows are inserted with before_json = previous row's snapshot and after_json = current row's snapshot; auto-summary via generateDiffSummary"
    - "backfill uses the typed-confirmation gate BACKFILL_CONFIRM=YES when DATABASE_URL host matches *.neon.tech (per D-15)"
    - "package.json scripts include `db:backfill:coefficient-history` invoking the script via tsx with the server-only preload"
    - "docs/operations/launch-checklist.md includes a new step: 'after npm run db:migrate, run npm run db:backfill:coefficient-history'"
    - "src/lib/db/queries/coefficient-history.integration.test.ts uses a real Postgres (DATABASE_URL_TEST or equivalent) and verifies the coefficient_history_no_modify trigger raises EXCEPTION on UPDATE and on DELETE (empirical proof of D-12)"
  artifacts:
    - path: scripts/backfill-coefficient-history.ts
      provides: "Idempotent one-shot backfill of global_params rows into coefficient_history"
      contains: "BACKFILL_CONFIRM"
    - path: package.json
      provides: "New script entry db:backfill:coefficient-history"
      contains: "db:backfill:coefficient-history"
    - path: docs/operations/launch-checklist.md
      provides: "Launch checklist with backfill step appended"
      contains: "db:backfill:coefficient-history"
    - path: src/lib/db/queries/coefficient-history.integration.test.ts
      provides: "Integration test verifying append-only trigger raises on UPDATE and DELETE (D-12 empirical proof)"
  key_links:
    - from: scripts/backfill-coefficient-history.ts
      to: src/lib/db/queries/coefficient-history.ts (createCoefficientHistoryEntry)
      via: "The script imports createCoefficientHistoryEntry and uses it as the sole insertion path — auto-summary via generateDiffSummary works automatically"
      pattern: "import.*createCoefficientHistoryEntry"
    - from: src/lib/db/queries/coefficient-history.integration.test.ts
      to: drizzle/0004_phase12_drafts_and_history.sql (coefficient_history_no_modify function)
      via: "Integration test asserts the trigger raises with the exact exception message string"
      pattern: "coefficient_history is append-only"
---

<objective>
Ship the operational tail of Phase 12:
1. The idempotent one-shot backfill script `scripts/backfill-coefficient-history.ts` (D-14, D-15).
2. The `package.json` script wiring.
3. The launch-checklist documentation update.
4. The single Vitest **integration test** (against a real Postgres) verifying the `coefficient_history_no_modify` trigger raises EXCEPTION on both UPDATE and DELETE — the empirical proof that D-12 works as intended (D-20).

Purpose: Without the backfill, the History sidebar in Phase 14 would render only NEW coefficient edits (post-launch) and the pre-launch global_params history (seed + any v1.1 edits) would be invisible. Without the integration test, the append-only guarantee is asserted but not proven.

Output: 1 new script + 1 package.json edit + 1 doc edit + 1 new integration test. Atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md
@src/lib/db/queries/coefficient-history.ts
@src/lib/db/queries/global-params.ts
@scripts/seed-admins-launch.ts
@scripts/migrate.ts
@package.json
@drizzle/0004_phase12_drafts_and_history.sql

<interfaces>
<!-- The script wraps existing plan-12-04 helpers. The integration test runs against a real Postgres (Neon dev branch or local Postgres). -->

From src/lib/db/queries/coefficient-history.ts (plan 12-04):
- `createCoefficientHistoryEntry({ before, after, userId, summary? })` — INSERT helper that auto-falls-back to generateDiffSummary

From src/lib/db/queries/global-params.ts (existing):
- `listGlobalParamsHistory({ cursor?, limit? })` — paginated history of global_params rows newest-first
- For backfill: we need OLDEST-first iteration. The script can either (a) page through listGlobalParamsHistory and reverse, (b) write a small ascending-order query inline. Recommend (b) — direct SQL `ORDER BY effective_from ASC` since the backfill is a one-shot script.

From scripts/seed-admins-launch.ts (typed-confirmation pattern):
- Gate 1: `if (process.env.CONFIRM !== REQUIRED_CONFIRM) { process.exit(2); }`
- Gate 2: required env check
- For this backfill, per CONTEXT D-15 + Claude's Discretion: env var name is `BACKFILL_CONFIRM=YES` (NOT generic `CONFIRM`); the gate fires ONLY when `DATABASE_URL` host matches `*.neon.tech` (production). For local/preview Postgres, no confirmation needed.

Integration test infra:
- vitest.config.ts uses `environment: 'jsdom'` for the whole project. Integration tests can override with `// @vitest-environment node` directive at the top of the file (Vitest supports per-file environment override).
- Real-Postgres connection: the test SHOULD use a separate env var (e.g., `DATABASE_URL_TEST`) so it doesn't accidentally hit prod. If missing, the test SKIPS with `it.skip` and logs a clear message.
- Schema must already be migrated on that DB (plan 12-01's dev-apply step is the prerequisite). The integration test should NOT run migrations — it assumes 0004 is already applied.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scripts/backfill-coefficient-history.ts as an idempotent one-shot</name>
  <files>scripts/backfill-coefficient-history.ts</files>
  <read_first>
    - scripts/seed-admins-launch.ts (typed-confirmation gate pattern; env var validation; lazy import discipline)
    - src/lib/db/queries/coefficient-history.ts (the import target — createCoefficientHistoryEntry)
    - src/lib/db/queries/global-params.ts (helper read patterns; the script can query schema.globalParams directly with desc/asc)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-14, D-15, Claude's Discretion on env var name)
  </read_first>
  <behavior>
    - The script is SAFE TO RE-RUN. On a non-empty coefficient_history table, it exits 0 with "Already backfilled — N existing rows" and does no inserts (per D-15).
    - The script gates on `BACKFILL_CONFIRM=YES` when `DATABASE_URL` host matches `*.neon.tech` (production). Local Postgres or `*.neon.build` preview branches don't trigger the gate (per CONTEXT).
    - The script uses lazy imports for the DB module so env-var validation runs first.
    - The script reads global_params rows in ASCENDING effective_from order, then inserts one coefficient_history row per global_params row. The first row's `before_json` is NULL; subsequent rows' `before_json` is the previous row's column snapshot.
    - The script uses `createCoefficientHistoryEntry({ before, after, userId: row.createdBy ?? null, summary: undefined })` — letting the auto-fallback compute the summary (the first row gets 'Configuration initiale'; subsequent rows get the diff).
    - The script logs each insertion clearly: `[backfill] + inserted history row for global_params id=<id> changed_at=<effective_from> summary="<resolved summary>"`.
    - The script wraps all inserts in a transaction OR (simpler — Drizzle's BEGIN/COMMIT isn't trivial via the @/lib/db memoized singleton) inserts sequentially; if any insert fails, the script exits 1 (drizzle's default behavior since we don't catch). Idempotency on retry: the count-check at the start ensures a partial-failure rerun does NOT double-insert (it sees count > 0 and aborts; recommended to document: "if a partial backfill needs cleanup, the admin deletes all coefficient_history rows manually — which the trigger blocks; so partial failure mode is: manual psql `ALTER TABLE coefficient_history DISABLE TRIGGER ALL; DELETE FROM coefficient_history; ALTER TABLE coefficient_history ENABLE TRIGGER ALL;`". Add this caveat to the script header comment.)
  </behavior>
  <action>
Create `scripts/backfill-coefficient-history.ts`:

1. Header comment block (matching seed-admins-launch.ts style) explaining:
   - "Idempotent one-shot backfill of existing global_params rows into coefficient_history. Per 12-CONTEXT.md D-14, D-15."
   - "Run AFTER npm run db:migrate (plan 12-01 migration must be applied first; coefficient_history table must exist)."
   - "Typed-confirmation gate: requires BACKFILL_CONFIRM=YES when DATABASE_URL host matches *.neon.tech. Local/preview Postgres skips the gate."
   - "Idempotency: SELECT count(*) FROM coefficient_history; if > 0, exit 0 with 'Already backfilled' message."
   - "Partial-failure recovery: if a backfill aborts mid-run (e.g., network drop), the next run will see a non-empty table and refuse. To force a fresh backfill, manually clean the table via psql with triggers disabled: `ALTER TABLE coefficient_history DISABLE TRIGGER ALL; DELETE FROM coefficient_history; ALTER TABLE coefficient_history ENABLE TRIGGER ALL;` — then re-run this script."

2. Imports + constants:
   - `import 'dotenv/config';`
   - Top-of-file `const REQUIRED_CONFIRM_VALUE = 'YES';`

3. Top-level `async function main()`:
   - Step 1: validate DATABASE_URL is set. If not, log and `process.exit(2)`.
   - Step 2: check the host. Parse with `new URL(process.env.DATABASE_URL)`. If `u.host.endsWith('.neon.tech')`, require `process.env.BACKFILL_CONFIRM === 'YES'`. Else, skip the gate (log "Non-prod DB — gate not enforced").
   - Step 3: lazy import: `const { db, schema } = await import('../src/lib/db/index');`
   - Step 4: idempotency check: `SELECT count(*) FROM coefficient_history`. If `> 0`, log "Already backfilled — <N> rows exist. Exiting 0." and `process.exit(0)`.
   - Step 5: lazy import: `const { createCoefficientHistoryEntry } = await import('../src/lib/db/queries/coefficient-history');`
   - Step 6: read all global_params rows ordered by effective_from ASC. Use `db().select().from(schema.globalParams).orderBy(schema.globalParams.effectiveFrom)`. (No `desc()` — ascending is Drizzle default.)
   - Step 7: iterate. For each row (zero-indexed `i`):
     - `before = i === 0 ? null : extractSnapshot(previousRow)` where `extractSnapshot(r)` returns `{ commissionPct: r.commissionPct, maxAmount: r.maxAmount, validityDays: r.validityDays, coefficients: r.coefficients }`.
     - `after = extractSnapshot(currentRow)`.
     - `userId = currentRow.createdBy ?? null`.
     - Call `await createCoefficientHistoryEntry({ before, after, userId, summary: undefined })`. The auto-fallback computes 'Configuration initiale' for the seed and a diff for each subsequent row.
     - Log the result.
   - Step 8: log totals (`[backfill] Done. Inserted <N> rows.`) and exit 0.

4. The script entrypoint:
   ```
   main().catch((err) => {
     console.error('[backfill] FATAL:', err);
     process.exit(1);
   });
   ```

Do NOT shell out to psql. Do NOT add any auto-migration step (the script assumes 0004 is already applied). Do NOT add a `--dry-run` flag in this plan; can be added later if needed.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- --quiet scripts/backfill-coefficient-history.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `scripts/backfill-coefficient-history.ts` exists.
    - Source: grep `BACKFILL_CONFIRM` returns ≥2 hits (the env var name + the value check).
    - Source: grep `'.neon.tech'` returns ≥1 hit (the host-match gate).
    - Source: grep `import .*createCoefficientHistoryEntry.* from '\\.\\./src/lib/db/queries/coefficient-history'` returns exactly 1 hit (via lazy await import or static — either is fine; grep with `import .*createCoefficientHistoryEntry` returns ≥1 hit).
    - Source: grep `Already backfilled` returns ≥1 hit (the idempotency message).
    - Source: grep `Configuration initiale` returns 0 hits in this file (the string is produced by generateDiffSummary, not literal here).
    - Source: grep `schema.globalParams` returns ≥1 hit.
    - Source: grep `process.exit\(0\)` AND grep `process.exit\(1\)` AND grep `process.exit\(2\)` each return ≥1 hit.
    - CLI: `npm run typecheck` exits 0.
    - CLI: `npm run lint -- --quiet scripts/backfill-coefficient-history.ts` exits 0.
  </acceptance_criteria>
  <done>
Script exists with idempotency gate, typed-confirmation gate for Neon prod, lazy imports, sequential insertion via createCoefficientHistoryEntry; typecheck + lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add package.json script + launch-checklist documentation</name>
  <files>package.json, docs/operations/launch-checklist.md</files>
  <read_first>
    - package.json (current scripts block; the new entry slots in alongside `db:migrate`, `purge:soft-deleted`, etc.)
    - docs/operations/launch-checklist.md (current content — read in full; the backfill step goes after the migrate step)
    - scripts/backfill-coefficient-history.ts (just-authored — the script being wired)
  </read_first>
  <behavior>
    - The package.json entry uses the same `tsx -r ./scripts/_preload-mock-server-only.cjs` preload as `purge:soft-deleted` IF the script imports any `'server-only'` modules transitively (it imports createCoefficientHistoryEntry → coefficient-history.ts which has `import 'server-only'`). Verify by reading the existing patterns. The preload mocks server-only for Node script execution.
    - The launch-checklist edit adds ONE new step immediately after `npm run db:migrate`; the step references the npm script name verbatim.
  </behavior>
  <action>
(a) Edit `package.json`:

Add a new line in the `scripts` block, placed near `db:migrate`:
```
"db:backfill:coefficient-history": "tsx -r ./scripts/_preload-mock-server-only.cjs scripts/backfill-coefficient-history.ts",
```

(b) Edit `docs/operations/launch-checklist.md`:

Read the file; locate the section where `npm run db:migrate` is invoked (it should be in the launch sequence — pre-partner-onboarding). Immediately AFTER that step, insert one new step:

```
- [ ] Backfill coefficient_history from existing global_params rows:
  - For local/preview: `npm run db:backfill:coefficient-history`
  - For production (Neon main): `DATABASE_URL=<prod-url> BACKFILL_CONFIRM=YES npm run db:backfill:coefficient-history`
  - Idempotent: safe to re-run. Skips if coefficient_history is already non-empty.
  - Required for the Phase 14 History sidebar to render pre-launch coefficient edits.
```

If the launch-checklist file does NOT exist (file new for v1.2), the executor should:
1. Check the current content (`cat docs/operations/launch-checklist.md` if exists; else `mkdir -p docs/operations` and create a minimal stub with just this section + a TODO header pointing back to the Phase 10 cutover-polish summary for any other missing items).
2. Append the new step inside a `## Database` section (or whatever H2 best fits the file's existing structure).

Do NOT delete or reorder existing checklist items. Do NOT remove any existing `npm run db:migrate` reference.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['db:backfill:coefficient-history']) {console.error('missing script'); process.exit(1)} else process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep `db:backfill:coefficient-history` in `package.json` returns exactly 1 hit (the script entry).
    - Source: grep `db:backfill:coefficient-history` in `docs/operations/launch-checklist.md` returns ≥1 hit (the documentation step).
    - Source: grep `BACKFILL_CONFIRM=YES` in `docs/operations/launch-checklist.md` returns ≥1 hit (the prod-Neon instruction).
    - Source: grep `db:migrate` in `docs/operations/launch-checklist.md` STILL returns ≥1 hit (the existing migrate step was not removed).
    - CLI: `npm run db:backfill:coefficient-history -- --help 2>&1 || true` does not crash on the npm-resolve side (the script may print its own usage; the important thing is that npm resolves the script).
    - CLI: `node -e "require('./package.json').scripts['db:backfill:coefficient-history']"` prints the script command and exits 0.
  </acceptance_criteria>
  <done>
package.json has the new script entry; launch-checklist documents the backfill step alongside the migrate step.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest integration test verifying the append-only TRIGGER raises on UPDATE and DELETE</name>
  <files>src/lib/db/queries/coefficient-history.integration.test.ts</files>
  <read_first>
    - src/lib/db/queries/coefficient-history.ts (the createCoefficientHistoryEntry helper used to seed a row)
    - drizzle/0004_phase12_drafts_and_history.sql (the exact RAISE EXCEPTION message string the test binds to)
    - .planning/phases/12-schema-extensions-for-drafts-history/12-CONTEXT.md (D-12 + D-20 — the integration test is the empirical proof)
    - vitest.config.ts (the existing config; we override env per-file)
    - scripts/migrate.ts (the migration apply path; the test assumes the schema is already migrated to the test DB)
  </read_first>
  <behavior>
    - Test file uses `// @vitest-environment node` at the top to override jsdom (the postgres-js client needs Node, not jsdom).
    - Test connects to a real Postgres via `DATABASE_URL_TEST` env var; if missing, the test SKIPS with `it.skip` (clear log message: "DATABASE_URL_TEST not set — skipping integration test. Set it to your dev/preview DB to run.").
    - Test seeds ONE coefficient_history row via the helper, then attempts UPDATE and DELETE via raw SQL through postgres-js; both attempts must throw with the exact message `coefficient_history is append-only — UPDATE and DELETE forbidden`.
    - Test cleans up by NOT cleaning up (the trigger blocks DELETE anyway). The integration test deliberately leaves one row — running the test multiple times accumulates rows. Mitigation: use a unique summary string per run (e.g., timestamped) so accumulating rows is harmless. Document in the test file.
  </behavior>
  <action>
Create `src/lib/db/queries/coefficient-history.integration.test.ts`:

1. Top of file:
   ```
   // @vitest-environment node
   /**
    * INTEGRATION TEST — real Postgres required.
    *
    * Verifies D-12 empirically: the coefficient_history_no_modify trigger raises
    * EXCEPTION with the message 'coefficient_history is append-only — UPDATE and DELETE forbidden'
    * on every UPDATE and every DELETE against the coefficient_history table.
    *
    * Setup:
    *   1. Apply 0004_phase12_drafts_and_history.sql to a dev/preview Postgres
    *   2. Export DATABASE_URL_TEST=<that DB url>
    *   3. Run: npx vitest run src/lib/db/queries/coefficient-history.integration.test.ts
    *
    * If DATABASE_URL_TEST is unset, the test SKIPS (does not fail CI).
    *
    * The test leaves one row per run in coefficient_history (the trigger blocks
    * cleanup anyway). Each run uses a unique timestamped summary so duplicates
    * are harmless.
    */
   ```

2. Imports:
   - `import { describe, it, expect, beforeAll, afterAll } from 'vitest';`
   - `import postgres from 'postgres';`
   - Use the postgres-js client directly (matches scripts/migrate.ts driver choice) — avoid going through the @/lib/db memoized singleton which is jsdom-environment-tuned.

3. Test setup:
   ```
   const DATABASE_URL = process.env.DATABASE_URL_TEST;
   const shouldRun = !!DATABASE_URL;

   describe.skipIf(!shouldRun)('coefficient_history append-only TRIGGER (real Postgres)', () => {
     let sql: ReturnType<typeof postgres>;

     beforeAll(() => {
       sql = postgres(DATABASE_URL!, { max: 1, prepare: false, onnotice: () => {} });
     });

     afterAll(async () => {
       await sql.end({ timeout: 5 });
     });

     // ... tests below
   });
   ```

4. Test case 1: `'INSERT succeeds (baseline)'`:
   - Insert one row via raw SQL: `INSERT INTO coefficient_history (after_json, summary) VALUES ('{"commissionPct":"5.0000"}'::jsonb, ${'integration-test-' + Date.now()})`.
   - Assert the insertion did not throw.
   - Capture the returned id for later: `RETURNING id`.

5. Test case 2: `'UPDATE raises coefficient_history is append-only'`:
   - Attempt: `UPDATE coefficient_history SET summary = 'x' WHERE id = ${capturedId}` wrapped in try/catch.
   - Assert the catch fires AND the error message includes the substring `coefficient_history is append-only`.
   - (Optional, recommend) Also assert the message includes `UPDATE and DELETE forbidden` for full-string verification.

6. Test case 3: `'DELETE raises coefficient_history is append-only'`:
   - Attempt: `DELETE FROM coefficient_history WHERE id = ${capturedId}`.
   - Assert the catch fires AND the error message includes the substring `coefficient_history is append-only`.

7. Optional Test case 4: `'INSERT after a blocked UPDATE attempt still succeeds (trigger does not corrupt connection state)'`:
   - After the UPDATE-blocked test, do another INSERT with a different summary. Assert it succeeds.
   - This catches a class of bugs where the trigger state corrupts the connection.

Do NOT modify `scripts/migrate.ts` or `vitest.config.ts` to make this test run automatically. It SKIPS unless `DATABASE_URL_TEST` is set — CI is expected to set this env var in a follow-up workflow (or it remains skipped, which is acceptable; the dev still runs it locally before merge per SC5 + D-20).

Document the skip-vs-run logic clearly in the test file header so reviewers don't think the test is missing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/db/queries/coefficient-history.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/db/queries/coefficient-history.integration.test.ts` exists.
    - Source: grep `@vitest-environment node` returns exactly 1 hit at the top.
    - Source: grep `DATABASE_URL_TEST` returns ≥2 hits.
    - Source: grep `describe.skipIf` returns ≥1 hit.
    - Source: grep `coefficient_history is append-only` returns ≥2 hits (the two assertion strings — UPDATE and DELETE).
    - Source: grep `UPDATE coefficient_history` returns ≥1 hit (the attempted-update raw SQL).
    - Source: grep `DELETE FROM coefficient_history` returns ≥1 hit (the attempted-delete raw SQL).
    - Source: grep `import postgres from 'postgres'` returns exactly 1 hit (using postgres-js directly, not @/lib/db).
    - CLI without DATABASE_URL_TEST: `npx vitest run src/lib/db/queries/coefficient-history.integration.test.ts` exits 0 (test SKIPS cleanly — no failures).
    - CLI with DATABASE_URL_TEST set to a dev DB (where the 0004 migration is applied): `DATABASE_URL_TEST=$DATABASE_URL_TEST npx vitest run src/lib/db/queries/coefficient-history.integration.test.ts` exits 0 with ≥3 passing tests.
    - Behavior (with DATABASE_URL_TEST set, manual verification): tests output shows the trigger raised the exact message string.
  </acceptance_criteria>
  <done>
Integration test exists, skips cleanly without the env var, passes with the env var pointed at a migrated dev DB. D-12 empirically proven.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → backfill script | Operator has prod DB credentials; typed-confirmation gate (BACKFILL_CONFIRM=YES + Neon-host check) is the human-error guard |
| test → real DB | Integration test runs against a separate DATABASE_URL_TEST; the host-check pattern in the backfill script does NOT apply (the test is run by the developer or CI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | EoP | accidental prod backfill double-run | mitigate | Idempotency check (SELECT count(*) FROM coefficient_history) + typed-confirmation gate BACKFILL_CONFIRM=YES required for *.neon.tech. Operator manually types the confirmation env var. |
| T-12-06-02 | Tampering | append-only trigger silently disabled in prod | mitigate | The integration test, when run against prod (or a copy of prod schema), would fail if the trigger were missing. Run before each production migration apply. CI candidate: gate on DATABASE_URL_TEST set to a freshly-migrated branch. |
| T-12-06-03 | Repudiation | backfilled rows show wrong actor | accept | The backfill uses `userId: row.createdBy ?? null` from global_params; pre-migration rows have whatever was originally written (Antoine for seed, admin for edits). System-initiated rows (seed) pass null — the FK ON DELETE SET NULL accepts this. Documented in the script header. |
| T-12-06-04 | Information disclosure | integration test prints commission_pct to test logs | accept | Integration test runs against dev/preview Postgres; the only commission values present are the seed (5.0000) which is non-secret. Production-prod integration runs would require care, but the test SKIPs by default and DATABASE_URL_TEST is operator-set. Document in test header. |
</threat_model>

<verification>
- Script + npm script + launch-checklist + integration test all land in the same commit.
- typecheck + lint + build all 0.
- Integration test SKIPS cleanly without DATABASE_URL_TEST (no false failures).
- With DATABASE_URL_TEST pointing at a dev DB with 0004 applied, integration test PASSES with ≥3 cases.
- Manual dev verification: run `DATABASE_URL=<dev> npm run db:backfill:coefficient-history` once (after 0004 applied to dev) — observe the rows inserted; re-run — observe "Already backfilled" exit 0.
</verification>

<success_criteria>
- Backfill script is idempotent, type-confirmation-gated for Neon prod, uses createCoefficientHistoryEntry as the sole insertion path.
- package.json wires the npm script.
- launch-checklist documents the new step alongside the migrate step.
- Integration test empirically proves the trigger raises on UPDATE and DELETE — SC3 + D-12 + D-20 satisfied.
- D-15 idempotency verified by manual dev re-run.
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-extensions-for-drafts-history/12-06-SUMMARY.md` recording:
- The dev DB used for the integration-test run (if applicable)
- Whether the integration test was run in CI yet (likely no — DATABASE_URL_TEST set up is a follow-up; document the gap)
- The exact rows inserted by the backfill against the dev DB (count)
- Any deviation from the seed-admins-launch.ts confirmation pattern (recommend: none — but document if BACKFILL_CONFIRM vs CONFIRM naming surprised)
</output>
