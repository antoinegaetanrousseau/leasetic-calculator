---
phase: 13-3-step-proposal-wizard
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/lib/wizard/stepperBehavior.test.ts
  - src/lib/pdf/no-commission.test.ts
  - docs/security/13-stride-addendum-admin09-step2.md
  - docs/smoke/13-wizard-runbook.md
autonomous: false
requirements: [ROUTE-01]

must_haves:
  truths:
    - "stepperBehavior.test.ts asserts D-21 edit-invalidates-downstream: a partner who completes steps 1+2 and edits a step-1 field has their _completedSteps reduced to numbers < 1 → next render shows step 1 ACTIVE with steps 2+3 PENDING"
    - "stepperBehavior.test.ts asserts D-22 navigate-preserves-state: clicking ← Précédent or ← Modifier or a Stepper done-link does NOT modify _completedSteps"
    - "stepperBehavior.test.ts asserts D-20 completion derivation: completedSteps array contains a step number ONLY after a Continuer click on that step"
    - "no-commission.test.ts asserts: for each of the 30 golden corpus fixtures from Phase 7, running the finalize-wizard.ts pipeline produces a PDF buffer whose decoded text content contains NO substring matching the commission amount (D-12 + D-28 mandatory ADMIN-09 test)"
    - "no-commission.test.ts asserts: the persisted computed jsonb passed to finalizeDraft (per plan 13-02) contains NO 'commission' key (verified via mocked finalizeDraft call assertion)"
    - "STRIDE addendum document docs/security/13-stride-addendum-admin09-step2.md exists, signed-and-dated, with one row per affected threat from Phase 9's 97-threat closure noting how the D-12 + D-28 step-2/step-3 partner-facing relaxation interacts with each threat (mitigated / unchanged / NEW)"
    - "Manual smoke runbook docs/smoke/13-wizard-runbook.md walks an operator (Antoine) through all 5 ROUTE-01 success criteria end-to-end in Chrome + Edge (auth as Partner A → /proposals/new/parametres mints draft → fill 7 fields → Continuer → step 2 shows loyer + commission with sublabel → save-as-draft + close browser + reopen → state persists → click Stepper done-link back to step 1 → edit amountHT → Stepper steps 2+3 revert to PENDING → walk forward through Continuer x2 → Confirmer → PDF generated + redirect to /proposals/{id} with active chip → draft no longer in any list)"
    - "Manual smoke includes the cross-partner test: Partner A saves a draft; auth as Partner B; hit /proposals/new/parametres → empty form (no Partner A data); Partner B's URL doesn't reveal Partner A's draft existence"
    - "All Vitest tests pass after this plan: full suite npm test exits 0 (399 existing + ~100 new from plans 13-01..13-05 + ~20 new from this plan = ~520+ tests)"
    - "Phase 13 ships behind ZERO new schema migrations (Phase 12 shipped DB-01; this plan verifies via grep that no new migration file was added to drizzle/ during Phase 13)"
  artifacts:
    - path: "src/lib/wizard/stepperBehavior.test.ts"
      provides: "Integration test of D-20/D-21/D-22/D-23 Stepper state semantics — the algorithm in src/lib/wizard/completedSteps.ts (plan 13-02) exercised end-to-end via saveAndAdvanceAction + saveAsDraftAction sequences"
      exports: []
    - path: "src/lib/pdf/no-commission.test.ts"
      provides: "Critical ADMIN-09 regression test asserting NO commission value appears in the rendered PDF for any of the 30 golden fixtures (D-12 + D-28 mandatory deliverable)"
      exports: []
    - path: "docs/security/13-stride-addendum-admin09-step2.md"
      provides: "STRIDE re-review addendum to Phase 9's 97-threat closure documenting the partner-facing step-2/step-3 commission relaxation + reaffirming PDF/audit_log/server-log invariants (D-28 mandatory deliverable)"
    - path: "docs/smoke/13-wizard-runbook.md"
      provides: "Manual smoke runbook (Chrome + Edge) covering the 5 ROUTE-01 success criteria end-to-end + the cross-partner draft isolation test"
  key_links:
    - from: "stepperBehavior.test.ts"
      to: "src/lib/wizard/completedSteps.ts (plan 13-02)"
      via: "exercises deriveCompletedSteps + markStepCompleted under realistic Continuer/Modifier/Save sequences"
      pattern: "deriveCompletedSteps\\(|markStepCompleted\\("
    - from: "no-commission.test.ts"
      to: "src/lib/api/proposals/finalize-wizard.ts (plan 13-02)"
      via: "drives finalize-wizard with each of 30 golden fixtures and asserts decoded PDF + persisted computed jsonb contain no commission"
      pattern: "finalizeWizard\\(|decode.*pdf"
    - from: "docs/security/13-stride-addendum-admin09-step2.md"
      to: "docs/security/09-stride-admin09-threat-model.md (Phase 9 — if exists) OR Phase 9 SUMMARY"
      via: "addendum cross-references the 97-threat closure and identifies the bounded relaxation envelope"
      pattern: ""
---

<objective>
Close Phase 13 with the cross-cutting integration layer: D-20/D-21/D-22/D-23 Stepper state semantics behavior tests, the ADMIN-09 no-commission-in-PDF golden-corpus test (D-12 + D-28 mandatory deliverable), the formal STRIDE addendum document (D-28), and the manual smoke runbook that walks all 5 ROUTE-01 success criteria.

Purpose: plans 13-01 through 13-05 ship the per-file work. This plan stitches them together with the integration tests + security artifacts the phase cannot pass review without. The STRIDE addendum is explicitly called out by D-28 as a phase-blocking deliverable.

Output: 2 new Vitest test files + 1 markdown security document + 1 markdown smoke runbook + the 1 user verification checkpoint that executes the runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-3-step-proposal-wizard/13-CONTEXT.md
@.planning/phases/13-3-step-proposal-wizard/13-UI-SPEC.md
@.planning/phases/13-3-step-proposal-wizard/13-PATTERNS.md

<!-- This plan depends on every prior plan's outputs being in place. -->

@.planning/phases/09-admin-surface/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stepper state semantics integration test (D-20/D-21/D-22/D-23)</name>
  <files>src/lib/wizard/stepperBehavior.test.ts</files>
  <read_first>
    - 13-CONTEXT.md D-20 (completion derivation: a step is "completed" iff partner clicked Continuer at least once on that step since their last edit on that step OR any earlier step)
    - 13-CONTEXT.md D-21 (edit-invalidates-downstream: editing inputs on step 1 clears _completedSteps for step 1 AND all subsequent steps)
    - 13-CONTEXT.md D-22 (navigate-preserves-state: ← Précédent or Stepper done-link navigation does NOT touch _completedSteps)
    - 13-CONTEXT.md D-23 (← Modifier same semantics as ← Précédent)
    - src/lib/wizard/completedSteps.ts (plan 13-02) — deriveCompletedSteps + markStepCompleted unit tests already cover the algorithm. This plan's test exercises realistic sequences.
    - app/(authed)/proposals/new/_actions/saveAndAdvance.action.ts (plan 13-02)
    - app/(authed)/proposals/new/_actions/saveAsDraft.action.ts (plan 13-02)
  </read_first>
  <action>
  Create `src/lib/wizard/stepperBehavior.test.ts`. This is a SCENARIO test — not a pure-function test — that exercises the bookkeeping helpers through realistic sequences mirroring the user flow.

  Test scenarios (use describe/it with explicit narrative names):

  1. **`D-20: a step is only completed after Continuer click`** — simulate: partner enters step-1 inputs → Save-as-draft → assert `_completedSteps` is `[]` (Save is not advance). Then partner clicks Continuer (saveAndAdvanceAction with fromStep=1) → assert `_completedSteps = [1]`.

  2. **`D-21: editing a step-1 field after completing steps 1+2 clears steps 2+3`** — simulate: partner advances through both steps (_completedSteps becomes [1, 2]); then partner returns to step 1 (via ← Précédent or Stepper link — pure navigation, _completedSteps unchanged); then partner edits clientCo and clicks Continuer → assert `_completedSteps = [1]` (step 2's done-mark cleared because step 1 input changed).

  3. **`D-21: editing an accordion field also clears downstream`** — simulate: partner has _completedSteps=[1,2]; partner returns to step 1; partner adds clientSiren (an accordion field, owned by step 1); Continuer → assert _completedSteps=[1].

  4. **`D-21: editing _uiAccordionOpen does NOT clear downstream`** — simulate: partner has _completedSteps=[1,2]; partner toggles the accordion (persistAccordionOpenAction fires fire-and-forget, updates only _uiAccordionOpen); → assert _completedSteps STILL = [1,2] (toggling chrome state is not an "edit" — completedSteps.ts excludes bookkeeping keys per BOOKKEEPING_KEYS constant).

  5. **`D-22: ← Précédent navigation does not change _completedSteps`** — simulate: partner has _completedSteps=[1,2]; partner navigates back via the WizardActionBar's ← Précédent Link (this is a pure Next.js <Link> — no server action fires); → assert _completedSteps STILL = [1,2].

  6. **`D-23: ← Modifier from step 3 to step 2 preserves _completedSteps`** — simulate: partner has _completedSteps=[1,2]; clicks ← Modifier in the ● CALCUL recap → navigates to /calcul; → assert _completedSteps STILL = [1,2].

  7. **`D-21 + D-20 combined: edit + revisit + Continuer rebuilds the chain`** — partner has _completedSteps=[1,2]; goes back to step 1 + edits + Continuer (now [1]); then Continuer on step 2 again (now [1,2]); → final _completedSteps = [1,2].

  8. **`Save-as-draft on step 2 does NOT advance _completedSteps`** — partner has _completedSteps=[1]; goes to step 2; clicks Save-as-draft (NOT Continuer); → _completedSteps stays [1] (D-17 — Save preserves draft, does not record progress).

  Implementation strategy: each scenario is a sequence of pure helper invocations (`deriveCompletedSteps` + `markStepCompleted` from `src/lib/wizard/completedSteps.ts`). Use vi.fn mocks for any DB calls if needed; otherwise pure-function level is sufficient. Test file should be ~150 lines.

  Reference D-20 / D-21 / D-22 / D-23 in inline comments next to each scenario.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/wizard/stepperBehavior.test.ts</automated>
  </verify>
  <done>stepperBehavior.test.ts exists; 8 scenario tests pass; covers D-20/D-21/D-22/D-23.</done>
</task>

<task type="auto">
  <name>Task 2: ADMIN-09 no-commission-in-PDF golden-corpus test (D-12 + D-28 mandatory)</name>
  <files>src/lib/pdf/no-commission.test.ts</files>
  <read_first>
    - 13-CONTEXT.md D-12 (ADMIN-09 partial relaxation — commission visible to partner on step 2 + step 3; INVISIBLE in PDF + audit_log + server logs + traces)
    - 13-CONTEXT.md D-28 (mandatory STRIDE addendum + mandatory Vitest test asserting rendered PDF for any of 30 golden cases contains no commission value)
    - src/lib/api/proposals/finalize-wizard.ts (plan 13-02) — the pipeline this test exercises
    - src/lib/pdf/templates/ProposalPdf.tsx OR equivalent — the PDF template (Phase 8 — unchanged in Phase 13; verify by grep)
    - tests/calc/golden-corpus.ts OR equivalent — Phase 7's 30-case fixture file (find via grep — should live under src/lib/calc/ or tests/)
    - tests for Phase 8's byte-determinism CI gate — pattern for decoding the rendered PDF buffer (likely uses pdf-parse, pdf-extract, or pdfjs-dist; grep package.json for the dep)
  </read_first>
  <action>
  Create `src/lib/pdf/no-commission.test.ts`. This is the LOAD-BEARING ADMIN-09 regression test mandated by D-28.

  Test structure:

  ```
  import { describe, it, expect, vi } from 'vitest';
  import { finalizeWizard } from '@/lib/api/proposals/finalize-wizard';
  // ... import the 30-fixture golden corpus from wherever Phase 7 stored it
  ```

  For each of the 30 fixtures:
  1. Mock getDraftById to return a draft with `inputs = fixture.inputs`.
  2. Mock getLatestGlobalParams to return the fixture's expected globalParams (or a representative one).
  3. Mock the storage adapter to capture the PDF buffer passed to .put().
  4. Mock finalizeDraft to capture the args (paramsSnapshot, computed, pdfBlobKey, etc.).
  5. Run finalizeWizard.
  6. Decode the captured PDF buffer (use whichever pdf-text-extraction lib the project already has; if none exists, parse the raw PDF binary as text and search — PDF text streams are not always plain-text but the @react-pdf/renderer output is usually parseable; alternative: use pdf-parse via dev dependency add if needed — flag to user before adding deps).
  7. Compute the expected commission AMOUNT for the fixture: `commission = fixture.expected.commission` (or recompute via computeLoyer if golden corpus doesn't store it).
  8. Assert: the decoded PDF text does NOT contain the commission AMOUNT formatted as either `formatCurrency(commission, 'fr')` or as a bare number string.
  9. Assert: the captured `computed` jsonb arg passed to finalizeDraft does NOT contain a `commission` key.
  10. Assert: the captured paramsSnapshot DOES contain `commissionPct` (the PERCENTAGE — required for byte-determinism per Phase 8 Stripe Option A — but the percentage alone does not allow back-deriving the amount without amountHT, and the amount IS expected to be in the PDF).

  Additional assertions across all fixtures (run once at the end of the describe block):
  - `await finalizeWizard(...)` was called 30 times total.
  - No fixture's decoded PDF leaked commission.

  Pre-flight check: if the project has no PDF text-extraction library (`pdf-parse`, `pdfjs-dist`, etc.), the test file's first action should be `it('precondition: pdf-parse or pdfjs-dist available', () => { expect(() => require('pdf-parse')).not.toThrow(); })` — if this fails, surface a precondition failure clearly and ask Antoine to install the dep before the test can run. Add `pdf-parse` as a dev dependency if the grep on package.json shows it's absent — flag in the SUMMARY.

  Reference D-12 + D-28 in inline comments.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/pdf/no-commission.test.ts</automated>
  </verify>
  <done>no-commission.test.ts exists; runs 30+ assertions (one per fixture); zero leaks found; precondition check confirms a PDF text-extraction lib is available (or `pdf-parse` is added as a dev dependency).</done>
</task>

<task type="auto">
  <name>Task 3: STRIDE addendum document (D-28 mandatory deliverable)</name>
  <files>docs/security/13-stride-addendum-admin09-step2.md</files>
  <read_first>
    - 13-CONTEXT.md D-12 + D-28 (the relaxation + the required addendum)
    - .planning/phases/09-admin-surface/09-CONTEXT.md (Phase 9 ADMIN-09 cluster + the 97-threat closure)
    - .planning/phases/09-admin-surface/09-SUMMARY.md or 09-*-SUMMARY.md (Phase 9 closure documentation — find via grep)
    - docs/security/09-stride-admin09-threat-model.md (Phase 9's formal threat model — find via grep; if it doesn't exist, the addendum references the Phase 9 SUMMARY instead and notes that the formal threat model lives in the planning SUMMARY)
    - All 6 plan files for Phase 13 (13-01..13-06) — extract every threat-model entry that mentions ADMIN-09 / commission / D-12 / D-28
  </read_first>
  <action>
  Create `docs/security/13-stride-addendum-admin09-step2.md`. This is a markdown document.

  Required sections:

  1. **Header + Date + Author** — "STRIDE Addendum to Phase 9 ADMIN-09 Threat Model — Partner-Facing Step-2/Step-3 Commission Relaxation (Phase 13)", dated, owned by Antoine + Claude.

  2. **Context** — 1-2 paragraphs: Phase 9 closed 97 STRIDE threats around commission invisibility across PDF + audit_log + server logs + traces + admin views + partner-facing UI surfaces. Phase 13 D-12 carves out a SINGLE bounded relaxation: the deal-owner partner can see their OWN commission amount on /proposals/new/calcul (step 2 Détail du calcul row) AND on /proposals/new/verification (step 3 ● CALCUL recap row). All other ADMIN-09 invariants remain in force.

  3. **Relaxation Envelope** — a precise description of WHAT is now visible WHERE:
     - Surface: /proposals/new/calcul → Détail du calcul row 1 → commission amount visible to deal-owner partner (NOT visible to client, NOT in the eventual PDF)
     - Surface: /proposals/new/verification → ● CALCUL recap row 3 → same
     - All other surfaces UNCHANGED.

  4. **Invariants That Remain In Force (Reaffirmation)** — a table:
     | Surface | Commission visibility | Phase 13 verification |
     | Rendered PDF (any of 30 golden corpus + production proposals) | INVISIBLE | src/lib/pdf/no-commission.test.ts |
     | audit_log payloads (proposal.create + every other action) | INVISIBLE | finalize-wizard.ts grep + Phase 12's finalizeDraft contract |
     | Server logs (Next.js prod logs on Vercel) | INVISIBLE | finalize-wizard.ts grep "console.log" returns 0 commission references; route.ts returns only bounded safeCodes |
     | Pre-finalize traces (any logging during step-3 finalize POST handler) | INVISIBLE | route.ts error path returns only bounded safeCodes; computed jsonb passed to finalizeDraft has no commission key |
     | Admin "Explain calculation" tool (Phase 9 D-09-X) | VISIBLE per existing Phase 9 exception | UNCHANGED |
     | Admin "Liste des partenaires" + admin proposal lists | INVISIBLE per Phase 9 | UNCHANGED |
     | Admin "Coefficient editor" + "History sidebar" | INVISIBLE per Phase 9 | UNCHANGED |
     | Partner home (post-Phase 14) Brouillons + Active proposal list rows | INVISIBLE | Phase 14 to verify; out-of-scope for this addendum |
     | Cross-user view (Partner A's commission visible to Partner B) | INVISIBLE | requireUser + getDraftById userId predicate; plan 13-03/04/05 cross-user tests verify silent redirect |

  5. **STRIDE Re-Review of Phase 9's 97 Threats** — group the 97 threats from Phase 9 into clusters and note disposition under D-12:
     - **Cluster A: PDF binary leakage threats** — UNCHANGED (no-commission.test.ts proves Phase 13's finalize-wizard preserves Phase 8's PDF byte-determinism contract; commission is excluded from the persisted computed jsonb).
     - **Cluster B: audit_log leakage threats** — UNCHANGED (finalizeDraft writes audit_log; Phase 12 D-discretion-bullet locks the entry shape; Phase 13's finalize-wizard does NOT write a second audit entry).
     - **Cluster C: Server-log leakage threats** — UNCHANGED (no console.log calls in any Phase 13 finalize path).
     - **Cluster D: Cross-user disclosure threats** — UNCHANGED (D-03 silent redirect; tested in plans 13-03/04/05).
     - **Cluster E: Admin-surface disclosure threats** — UNCHANGED (Phase 9 admin surfaces untouched in Phase 13).
     - **Cluster F: Partner-facing UI threats** — NEW BOUNDED RELAXATION on /proposals/new/calcul + /proposals/new/verification per D-12. All other partner-facing surfaces (/proposals/{id} detail, partner home, /proposals lists) remain UNCHANGED.

  6. **New Threats Introduced By The Relaxation (and their mitigations)**:
     | Threat ID | Description | Mitigation |
     | T-13-NEW-01 | Partner A's commission visible to anyone with screen-share access while Partner A is mid-wizard | Accept — physical security + audit_log proves only the deal-owner accessed the surface; commission is in pre-finalize state, not yet a binding offer |
     | T-13-NEW-02 | Partner copies the displayed commission value into an external tool (e.g. email to a colleague) | Accept — partner is the deal-owner; same risk exists for any business-private number |
     | T-13-NEW-03 | Browser DOM inspection or copy-paste from /calcul or /verification → no addtl disclosure path | Mitigated — the value is server-rendered into JSX; copy-paste only reveals what's already on screen; no hidden inputs or data attributes carry extra info |
     | T-13-NEW-04 | Browser-extension keylogger or screen capture | Accept — out of scope for application-layer security |
     | T-13-NEW-05 | Cached browser history of /calcul URL → cross-session leak via shared device | Accept — same risk profile as any logged-in app; partners should log out on shared devices |

  7. **Verification Artifacts** (locked):
     - `src/lib/pdf/no-commission.test.ts` — 30 golden-corpus assertions
     - `src/lib/api/proposals/finalize-wizard.ts` grep results: no `commission` substring in `computed` construction; no `console.log` of commission
     - Plan 13-04 page.test.tsx — Test 15 (commission appears EXACTLY ONCE on step 2)
     - Plan 13-05 page.test.tsx — Test 15 (commission appears EXACTLY ONCE on step 3, NOT in PdfPreviewMock)

  8. **Sign-Off**:
     - Date drafted: (today)
     - Author: Claude Code via plan 13-06
     - Reviewer: Antoine Rousseau (manual review required before phase close)
     - Status: drafted, pending Antoine sign-off

  Reference D-12 + D-28 throughout. This document IS the addendum D-28 demands.

  Add the document to docs/security/ (create the directory if it doesn't exist).
  </action>
  <verify>
    <automated>test -f docs/security/13-stride-addendum-admin09-step2.md && grep -c "Cluster F" docs/security/13-stride-addendum-admin09-step2.md</automated>
  </verify>
  <done>STRIDE addendum exists at docs/security/13-stride-addendum-admin09-step2.md; contains all 8 sections; references D-12 + D-28 explicitly; the 5 new T-13-NEW-* threats enumerated with dispositions.</done>
</task>

<task type="auto">
  <name>Task 4: Manual smoke runbook (Chrome + Edge — 5 ROUTE-01 success criteria + cross-partner test)</name>
  <files>docs/smoke/13-wizard-runbook.md</files>
  <read_first>
    - 13-CONTEXT.md ROUTE-01 5 success criteria (in the planning_context provided by the orchestrator + ROADMAP.md §Phase 13)
    - .planning/REQUIREMENTS.md ROUTE-01 full text
  </read_first>
  <action>
  Create `docs/smoke/13-wizard-runbook.md`. Step-by-step manual runbook for an operator (Antoine) to walk through Phase 13's full surface in Chrome + Edge.

  Required sections:

  ## Prerequisites
  - Logged in as Partner A on a fresh browser session (Chrome).
  - Have a second test partner account (Partner B) ready for cross-partner test.
  - Production or staging URL: (Antoine fills in at runbook time)

  ## Test 1 — ROUTE-01 criterion 1: All 3 routes render with Stepper showing correct state
  1. Navigate to /proposals/new/parametres (logged in as Partner A).
  2. Expected: page renders with the Stepper at top. Step 1 ACTIVE (green-filled with white "1"); steps 2 + 3 PENDING (outlined circles with muted "2" / "3"); no done-check on any step.
  3. Visually confirm the page title `Paramètres du projet` + subtitle below.
  4. Visually confirm the WizardActionBar at the bottom: NO ← Précédent (omitted on step 1); `Enregistrer comme brouillon` ghost button + `Continuer vers le calcul →` primary CTA.
  5. Navigate directly to /proposals/new/calcul (no draft_id) → expected: redirected to /proposals/new/parametres (D-03 self-heal).
  6. Same for /proposals/new/verification.

  ## Test 2 — ROUTE-01 criterion 2: Draft persistence (session-survives + close-and-reopen)
  1. /parametres mints a fresh draft and redirects with ?draft_id=<uuid> in the URL.
  2. Fill in all 7 default fields (any valid values).
  3. Click `Continuer vers le calcul →`.
  4. Step 2 renders with the computed loyer in the hero card; Détail du calcul has 5 rows including `Commission apporteur` with `(non visible client)` sublabel.
  5. Close the browser tab entirely (NOT just navigate away).
  6. Open a new tab, navigate to /proposals/new/parametres?draft_id=<the uuid from step 1>.
  7. Expected: the form is pre-populated with all 7 fields from step 2 — data persisted across the browser-close (D-02 + D-03 + Phase 12 DB-01 draft row in Postgres).

  ## Test 3 — ROUTE-01 criterion 3: Stepper completed-step navigation
  1. From step 2 (Test 2 state), click the Stepper step 1 circle (which should now show a done-check).
  2. Expected: navigate to /parametres?draft_id=<uuid>; Stepper shows step 1 ACTIVE, step 2 DONE-check, step 3 PENDING.
  3. Try clicking step 3 (pending) — expected: nothing happens; no Link wrapping pending step circles.
  4. Edit the `Montant HT` field (change it to a different valid value).
  5. Click Continuer vers le calcul → expected: step 2 re-renders with the NEW computed loyer.
  6. NOW look at the Stepper: step 1 DONE-check, step 2 ACTIVE, step 3 PENDING (the previously-completed step 3 is back to pending — wait, step 3 was never reached. Re-do this test: go forward to step 3, return to step 1, edit, expect step 2+3 to revert to pending).
  7. **Full edit-invalidates-downstream test:** From a state where _completedSteps = [1, 2] (you reached step 3 once), navigate back to step 1 via the Stepper, edit a field (e.g. clientCo), click Continuer. Step 2 should render but its Stepper now shows steps 2 + 3 as PENDING (the done-check was cleared by the edit per D-21).

  ## Test 4 — ROUTE-01 criterion 4: Finalize transitions draft → active + PDF generation + appears in partner list
  1. From step 3 (any complete draft), click `Confirmer & Générer le PDF`.
  2. Expected: CTA disables, label morphs to `Génération en cours…`, Loader2 spinner shows.
  3. After ~1-3 seconds, the browser redirects to /proposals/<new-uuid>.
  4. Sonner success toast at bottom-right: `Proposition générée ✓`.
  5. Navigate to / (partner home).
  6. The newly-created proposal appears in the partner list with an `active` StatusChip (green).
  7. The draft row no longer appears anywhere (drafts list will be a Phase 14 surface — for now, query the DB or trust that the draft row's status flipped to 'active').
  8. Inspect the generated PDF: download from /proposals/<uuid>; expected: no `Commission` text, no commission amount anywhere in the PDF body (D-12 + D-28 invariant).

  ## Test 5 — ROUTE-01 criterion 5: Drafts only visible to creator (cross-partner isolation)
  1. As Partner A, create a fresh draft on /parametres, fill in some fields, save-as-draft.
  2. Note the ?draft_id=<A-uuid> in the URL.
  3. Log out as Partner A.
  4. Log in as Partner B.
  5. Navigate to /proposals/new/parametres?draft_id=<A-uuid>.
  6. Expected: silently redirected to /proposals/new/parametres (no error, no 404); a fresh draft is minted for Partner B with empty fields.
  7. Confirm: Partner B's URL shows a NEW draft_id (B's own), not A's.
  8. Partner B sees an empty form — no leak of A's clientCo, clientName, or any other field.

  ## Test 6 — ADMIN-09 commission invisibility outside step 2/step 3 (D-12 envelope)
  1. While on step 2 (with a non-zero commission), confirm: commission amount visible in Détail du calcul row.
  2. Open browser DevTools → Inspect Element on the commission row. Confirm: the commission value is rendered into normal HTML text (not a data attribute, not aria-label, not hidden input).
  3. Network tab: any /api/* requests during step 2 navigation — expected: no commission value in response payloads (the page is server-rendered, so the commission is in the HTML body, NOT in a JSON API response).
  4. View source: search for the commission value (the formatted string e.g. "1 500 €") — expected: appears EXACTLY ONCE on step 2 (in the Détail du calcul row).
  5. Navigate to step 3. View source. Search for commission value: appears EXACTLY ONCE (in the ● CALCUL recap row, NOT in the PdfPreviewMock).
  6. Click Confirmer & Générer le PDF. After redirect to /proposals/<uuid>, view source. Search for commission value: expected ZERO occurrences (the saved proposal detail page does not surface commission).
  7. Open the generated PDF binary. Use a text-search tool (e.g. pdftotext via brew install poppler) to extract text. Search for commission value: expected ZERO occurrences.
  8. Optional: query Postgres `audit_log` for the proposal.create entry; verify its payload column contains no `commission` field.

  ## Cross-browser verification
  Repeat Tests 1-6 in Microsoft Edge (the second required browser per PROJECT.md). Note any rendering differences.

  ## Pass/Fail
  All 6 tests + 5 ROUTE-01 criteria must pass to mark Phase 13 complete.

  Reference ROUTE-01 (REQUIREMENTS.md) + D-12 + D-28 throughout.
  </action>
  <verify>
    <automated>test -f docs/smoke/13-wizard-runbook.md && grep -c "ROUTE-01" docs/smoke/13-wizard-runbook.md</automated>
  </verify>
  <done>Runbook exists at docs/smoke/13-wizard-runbook.md; covers all 5 ROUTE-01 success criteria + the ADMIN-09 invariant test 6 + cross-browser instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 13 has shipped:
    - WAVE 1: 4 wizard primitives (WizardActionBar, PlusDeDetailsAccordion, PdfPreviewMock, RecapSection) + ~45 wizard.* i18n keys; 3 server actions + finalize API route + completedSteps helper + legacy redirect
    - WAVE 2: 3 wizard route pages (parametres, calcul, verification) + ParametresFormCard + FinalizeButton
    - WAVE 3 (this plan): stepperBehavior integration tests + no-commission PDF golden corpus test + STRIDE addendum + manual smoke runbook

    All Vitest tests passing (target ~520 total). Phase 13 ships ZERO new schema migrations (verified by grep on drizzle/).
  </what-built>
  <how-to-verify>
    1. Open Chrome. Log in as Partner A on the staging URL.
    2. Walk through docs/smoke/13-wizard-runbook.md Tests 1-6 in order.
    3. Repeat in Microsoft Edge.
    4. Validate that all 5 ROUTE-01 success criteria pass per the runbook's pass/fail section.
    5. Confirm: the STRIDE addendum at docs/security/13-stride-addendum-admin09-step2.md reads correctly and the relaxation envelope matches your expectations.
    6. Confirm: the generated PDF (Test 4 step 7) contains no commission text when extracted via pdftotext.

    If all pass, type "approved" to mark Phase 13 ready for /gsd-transition.
    If any test fails, describe the failure inline — the executor will iterate on the failing surface.
  </how-to-verify>
  <resume-signal>Type "approved" to ship Phase 13, or describe failures inline.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 13 (full surface) → ADMIN-09 envelope | This plan FORMALIZES the boundary via the STRIDE addendum document. The relaxation is bounded to /proposals/new/calcul + /proposals/new/verification surfaces; all other surfaces (PDF, audit_log, server logs, traces, admin views, partner home, /proposals detail) remain commission-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-I-Audit | Information Disclosure — STRIDE addendum is missing or incomplete | Phase 13 review gate | mitigate | This plan's Task 3 explicitly produces the addendum at docs/security/13-stride-addendum-admin09-step2.md per D-28. Phase 13 cannot pass review without it. The checkpoint Task 5 includes human review of the document. |
| T-13-06-I-PDF | Information Disclosure — commission leaks via the PDF render path | finalize-wizard.ts + golden corpus | mitigate | Task 2 (no-commission.test.ts) provides 30+ regression assertions across the golden corpus. CI fails if any fixture leaks. The test is positioned at src/lib/pdf/no-commission.test.ts so the team grep'ing for `pdf` tests finds it. |
| T-13-06-T | Tampering — _completedSteps bookkeeping diverges between server-derived and visual state | stepperBehavior.test.ts | mitigate | Task 1 (stepperBehavior.test.ts) exercises D-20/D-21/D-22/D-23 across 8 realistic scenarios. Any regression in completedSteps.ts (plan 13-02) breaks these tests. |
| T-13-06-Process | Process — Phase 13 ships without manual smoke verification | smoke runbook + checkpoint | mitigate | The checkpoint Task 5 is BLOCKING per the gate attribute. Phase 13 cannot complete without Antoine running the runbook in Chrome + Edge. |
</threat_model>

<verification>
- npm run typecheck exits 0
- npm run lint exits 0
- npm test exits 0 (full suite — 520+ tests including the 8 new stepperBehavior + 30 new no-commission tests)
- test -f docs/security/13-stride-addendum-admin09-step2.md (STRIDE addendum exists)
- test -f docs/smoke/13-wizard-runbook.md (smoke runbook exists)
- grep -c "ROUTE-01" docs/smoke/13-wizard-runbook.md returns ≥ 5 (all 5 criteria referenced)
- grep -c "D-12\|D-28" docs/security/13-stride-addendum-admin09-step2.md returns ≥ 4
- ls drizzle/0005* 2>/dev/null || echo "no new migration" → expected "no new migration" (Phase 13 ships zero schema work)
- The checkpoint Task 5 has been explicitly approved by Antoine
</verification>

<success_criteria>
- 4 deliverables exist: stepperBehavior.test.ts, no-commission.test.ts, docs/security/13-stride-addendum-admin09-step2.md, docs/smoke/13-wizard-runbook.md
- All ROUTE-01 5 success criteria are tested (3 via Vitest in plans 13-03/04/05; criteria 4 + 5 closed via the smoke runbook + the no-commission test)
- The D-28 STRIDE addendum is signed by Antoine
- Full Vitest suite (520+ tests) passes
- Phase 13 ships ZERO new schema migrations (only application code + tests + docs)
- The user has manually verified all 5 ROUTE-01 criteria in Chrome + Edge per the runbook
- ADMIN-09 cluster integrity is preserved: commission visible ONLY on step 2 + step 3 partner-facing surfaces; invisible everywhere else (verified by no-commission.test.ts + the runbook's test 6)
</success_criteria>

<output>
After completion (post-checkpoint approval), create `.planning/phases/13-3-step-proposal-wizard/13-06-SUMMARY.md` documenting:
- Final file list + line counts for the 4 new deliverables
- Full Vitest run output (test count, pass/fail, any skips)
- Confirmation of the manual smoke runbook execution (date + browsers tested + any anomalies)
- Confirmation that the STRIDE addendum has been signed by Antoine
- Phase 13 completion artifacts ready for `/gsd-transition`:
  - All 5 ROUTE-01 success criteria verified
  - ADMIN-09 envelope formalized
  - Zero schema work
  - All plans 13-01..13-06 complete
</output>
