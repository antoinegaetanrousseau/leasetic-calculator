---
phase: 14-admin-polish-partners-history-home
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/lib/admin/schemas.ts
  - src/lib/admin/actions.ts
  - src/lib/admin/index.ts
  - app/(admin)/[adminSegment]/partners/new/page.tsx
  - app/(admin)/[adminSegment]/partners/new/page.test.tsx
  - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx
  - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "Navigating to /<seg>/partners/new (as an admin) renders a server-component page with the title 'Créer un partenaire' (FR) or 'Create a partner' (EN)."
    - "The page renders a single .card containing 3 ●-bulleted sections: Informations personnelles, Informations société, Message d'invitation, separated by <hr> dividers."
    - "Each input is RHF-registered with zodResolver(createPartnerSchema) at mode='onBlur'; invalid blur produces a red ring + .error-msg."
    - "On valid submit, the form invokes a server action that calls adminCreateInvitation (Phase 9 helper) and returns { ok: true, url, kind: 'invitation' }; the existing <InviteUrlModal> opens with the returned URL."
    - "On modal close, the form resets and the router pushes /<seg>/partners (the renamed list)."
    - "Non-admin users (or unauthenticated) hitting /<seg>/partners/new are redirected by requireAdmin() — defence-in-depth identical to Phase 9 admin routes."
    - "Both FR and EN dictionaries contain the ~30 partners.new.* keys defined in UI-SPEC §6.3."
  artifacts:
    - path: "src/lib/admin/schemas.ts"
      provides: "Extended createPartnerSchema accepting firstName, lastName, email, companyName, siret, phone, invitationMessage (UI-SPEC §5.1)"
      contains: "createPartnerSchema"
    - path: "src/lib/admin/actions.ts"
      provides: "adminCreateInvitation wired to accept the extended args (firstName+lastName→displayName composition; companyName/siret/phone/invitationMessage in audit-log payload)"
      contains: "adminCreateInvitation"
    - path: "app/(admin)/[adminSegment]/partners/new/page.tsx"
      provides: "Server-component admin route with requireAdmin + dynamic='force-dynamic' (D-05); renders <CreatePartnerForm>"
      contains: "requireAdmin"
    - path: "app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx"
      provides: "'use client' RHF + zodResolver + 3-section layout; submit → server action → <InviteUrlModal>"
      contains: "useForm"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "~30 new partners.new.* keys × FR + EN per UI-SPEC §6.3"
      contains: "partners.new.title"
  key_links:
    - from: "CreatePartnerForm.tsx submit handler"
      to: "src/lib/admin/actions.ts adminCreateInvitation"
      via: "server action import"
      pattern: "adminCreateInvitation\\("
    - from: "CreatePartnerForm.tsx success path"
      to: "src/components/InviteUrlModal.tsx"
      via: "<InviteUrlModal url={…} kind='invitation' onClose={…}/>"
      pattern: "InviteUrlModal"
    - from: "app/(admin)/[adminSegment]/partners/new/page.tsx"
      to: "src/lib/auth/require.ts requireAdmin"
      via: "await requireAdmin() server-side gate"
      pattern: "requireAdmin\\("
---

<objective>
Ship the `/<seg>/partners/new` route per D-05..D-09 and UI-SPEC §5.1:

1. Extend `createPartnerSchema` in `src/lib/admin/schemas.ts` from its current 3-field shape (`email`, `displayName`, `language`) to the 7-field shape required by UI-SPEC §5.1 (`firstName`, `lastName`, `email`, `companyName`, `siret`, `phone`, `invitationMessage`). The existing `CreatePartnerModal.tsx` (shelved per D-10) continues to use the old shape — DO NOT break the modal. Resolve by introducing a NEW `createPartnerFormSchema` distinct from the existing `createPartnerSchema`, OR by extending the existing schema in a backward-compatible way (recommendation: NEW schema name to keep blast radius small).

2. Extend `adminCreateInvitation` in `src/lib/admin/actions.ts` to accept the extended args. The `users` table has no `firstName`/`lastName`/`companyName`/`siret`/`phone` columns (verified — schema.ts §`users` only has `name`, `displayName`, `email`, `language`, `role`, …). Resolve by:
   (a) composing `displayName = \`${firstName} ${lastName}\`.trim()` and passing it to the existing `createInvitation(email, displayName)` Phase 6 call;
   (b) writing the additional fields (`companyName`, `siret`, `phone`, `invitationMessage`) into the existing `audit_log` payload alongside the `user.create` and `invitation.create` writes (ADMIN-08 audit pattern preserved; ADMIN-09 commission redaction unchanged — none of the new fields are commission values).
   The `invitationMessage` is NOT yet sent in the invite email (the email-template-injection of the message is explicitly deferred to v1.3+ per CONTEXT.md §Deferred Ideas — but the message MUST be persisted in the audit log so v1.3+ can wire it).

3. Create the route at `app/(admin)/[adminSegment]/partners/new/page.tsx` per PATTERNS.md §`partners/new/page.tsx`. Server component, `await params`, `await requireAdmin()` (AUTH-15 defence-in-depth), `export const dynamic = 'force-dynamic'`, `export const metadata` with `robots: { index: false, follow: false }`. Renders the h1+subtitle hero block + `<CreatePartnerForm lang={lang} adminSegment={adminSegment} createPartnerAction={…} />`.

4. Create the form at `app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx`. `'use client'`. RHF + zodResolver(createPartnerFormSchema) at `mode: 'onBlur'`, `shouldFocusError: true`, 7 default-empty string fields. Per UI-SPEC §5.1: single `.card` containing 3 `<section>` blocks separated by `<hr style="border:none; border-top:1px solid var(--border); margin:24px 0;">`. Each section has a `<div className="ctitle"><span className="dot" /><span>{SECTION_TITLE}</span></div>` header with `margin-bottom: 16px`. Field rhythm 12px (md) between fields. Action bar at the bottom: left `← Annuler` Link to `/${adminSegment}/partners`, right `Créer le partenaire` submit button with spinner state. Submit handler per D-08: server action call → success toast + `<InviteUrlModal>` open → on modal close, `reset()` + `router.push(\`/${adminSegment}/partners\`)`. Error path: sonner toast with key-mapped message; retain user input.

5. Per D-11 (this plan ships the route but does NOT touch AccountsList.tsx CTA — that swap lives in Plan 14-06's StatusChip rollout to keep file ownership clean for parallel execution). This plan's deliverable is the ROUTE; 14-06 wires the CTA to it.

6. Add the ~30 `partners.new.*` i18n keys × FR + EN to `src/lib/i18n/dictionaries.ts` per UI-SPEC §6.3 (covers title, subtitle, 3 section headers, 7 field labels + their placeholders, char-counter message, cancel/submit/spinner labels, 4 toast messages, the `partners.new.cancel.aria` accessibility label, and `partners.new.toast.validation.errors`).

7. Two Vitest test files: `page.test.tsx` (server-component harness; mocks `requireAdmin`, `getCurrentLang`; asserts h1 + form mount) and `CreatePartnerForm.test.tsx` (client-component harness; tests required-field validation, server-action wiring via mock, InviteUrlModal mount on success, error-toast path, char-counter behavior past 1000).

Purpose: This plan delivers the route + form + supporting server-action extension that ROUTE-02 success criterion #1 demands. The route is fully wired end-to-end except for the CTA on the partner list (which Plan 14-06 swaps in parallel using D-11).

Output: A working `/<seg>/partners/new` route an admin can visit, fill out, submit, and see a one-time invite URL in the existing `<InviteUrlModal>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md

# Required source files (read once at execution start)
@app/(admin)/[adminSegment]/partners/page.tsx
@app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx
@app/(admin)/[adminSegment]/partners/AccountsList.tsx
@app/(authed)/proposals/new/parametres/ParametresFormCard.tsx
@app/(authed)/proposals/new/verification/page.tsx
@app/(authed)/proposals/new/verification/page.test.tsx
@src/lib/admin/actions.ts
@src/lib/admin/schemas.ts
@src/lib/admin/index.ts
@src/components/InviteUrlModal.tsx
@src/components/proposal/PhoneInput.tsx
@src/lib/auth/require.ts
@src/lib/i18n/dictionaries.ts

<interfaces>
<!-- Existing adminCreateInvitation (src/lib/admin/actions.ts lines 178-249) — Phase 14 EXTENDS its args -->
export interface AdminCreateInvitationArgs {
  email: string;
  displayName: string;
  language: 'fr' | 'en';
}
export interface AdminCreateInvitationResult extends InviteResult {
  userId: string;
}
export async function adminCreateInvitation(
  args: AdminCreateInvitationArgs,
): Promise<AdminCreateInvitationResult>;

<!-- After Phase 14 extension: -->
export interface AdminCreateInvitationArgs {
  // Existing (still required for backward compat with CreatePartnerModal):
  email: string;
  displayName: string;
  language: 'fr' | 'en';
  // NEW (Phase 14, optional so the modal call site keeps working):
  firstName?: string;
  lastName?: string;
  companyName?: string;
  siret?: string;
  phone?: string;
  invitationMessage?: string;
}

<!-- Existing createPartnerSchema (src/lib/admin/schemas.ts lines 50-57) — Phase 14 keeps it AS-IS, adds NEW schema -->
export const createPartnerSchema = z.object({
  email: z.string().email({ message: 'admin.accounts.modal.error.email.invalid' }),
  displayName: z.string().min(1, { message: 'admin.accounts.modal.error.name.required' }),
  language: z.enum(['fr', 'en']).default('fr'),
});
export type CreatePartnerValues = z.infer<typeof createPartnerSchema>;

<!-- NEW createPartnerFormSchema (Phase 14, UI-SPEC §5.1): -->
export const createPartnerFormSchema = z.object({
  firstName: z.string().min(1, 'error.field.required').max(100),
  lastName: z.string().min(1, 'error.field.required').max(100),
  email: z.string().min(1, 'error.field.required').email('error.field.email.invalid'),
  companyName: z.string().min(1, 'error.field.required').max(200),
  siret: z.string().regex(/^\d{14}$/, 'error.field.siret.invalid').optional().or(z.literal('')),
  phone: z.string().min(1, 'error.field.required').regex(/^[\d\s+()-]{6,20}$/, 'error.field.phone.invalid'),
  invitationMessage: z.string().max(1000, 'partners.new.message.tooLong').optional(),
});
export type CreatePartnerFormValues = z.infer<typeof createPartnerFormSchema>;

<!-- InviteUrlModal (src/components/InviteUrlModal.tsx) — consumed verbatim -->
export interface InviteUrlModalProps {
  url: string;
  kind: 'invitation' | 'password_reset';
  lang: Lang;
  onClose: () => void;
  triggerRef?: React.RefObject<HTMLButtonElement | null>;
}

<!-- PhoneInput (src/components/proposal/PhoneInput.tsx) — reuse for the phone field per UI-SPEC §5.1.3 -->
export interface PhoneInputProps {
  // RHF register-compatible
  value: string;
  onChange: (v: string) => void;
  onBlur?: () => void;
  name: string;
  // … (consumer reads the full signature on first read)
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend createPartnerSchema (new schema) + wire adminCreateInvitation for the 7-field form</name>
  <files>src/lib/admin/schemas.ts, src/lib/admin/schemas.test.ts (new or existing), src/lib/admin/actions.ts, src/lib/admin/actions.test.ts (existing), src/lib/admin/index.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (full file — 60 lines)
    - src/lib/admin/actions.ts lines 175-250 (adminCreateInvitation block)
    - src/lib/admin/actions.test.ts — existing test cases for adminCreateInvitation (locate by grep)
    - src/lib/admin/index.ts (the public-export barrel)
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.1 (the full Zod schema definition + invitationMessage handling)
  </read_first>
  <behavior>
    - Test 1 (schema unit — RED first): `createPartnerFormSchema.parse({...full valid object...})` succeeds; `parse({...missing firstName...})` throws with issue path `firstName` and message key `error.field.required`; `parse({...siret = '12345'...})` throws with key `error.field.siret.invalid`; `parse({...siret = ''...})` succeeds (empty string is allowed per `.optional().or(z.literal(''))`); `parse({...invitationMessage of length 1001...})` throws with key `partners.new.message.tooLong`.
    - Test 2 (action unit — RED first): calling `adminCreateInvitation({ email, displayName, language, firstName, lastName, companyName, siret, phone, invitationMessage })` succeeds and the audit-log mock receives a payload containing all the extra fields under a `profile` sub-key (or equivalent — document the chosen shape in the SUMMARY).
    - Test 3 (action unit): calling `adminCreateInvitation({ email, displayName, language })` (the old 3-field shape) STILL succeeds — the new fields are optional and the modal's existing call site keeps working.
    - Test 4 (security): when `invitationMessage` is provided, the audit-log payload contains it; when `siret` is the empty string, the audit-log payload either omits it OR records it as null (decision: omit — empty string is "not provided"; document in SUMMARY).
  </behavior>
  <action>
    Three changes in TDD order:

    (a) RED — add the failing tests first. In `src/lib/admin/schemas.test.ts` (create if absent; otherwise extend), add a `describe('createPartnerFormSchema')` block exercising the 5 cases in Behavior Test 1. In `src/lib/admin/actions.test.ts`, add a `describe('adminCreateInvitation — Phase 14 extended args')` block with the 2 success-path cases (extended + legacy shape) + the audit-log payload assertion. Both files MUST fail (`npx vitest run src/lib/admin`) because the new schema + extended args don't yet exist.

    (b) GREEN — implement the schema + action extensions per UI-SPEC §5.1.
       - In `src/lib/admin/schemas.ts`: ADD (do NOT modify) a new `export const createPartnerFormSchema = z.object({ ... 7 fields ... })` exactly as in the `<interfaces>` block above. Export type `CreatePartnerFormValues`. The existing `createPartnerSchema` stays untouched so `CreatePartnerModal.tsx` continues to compile.
       - In `src/lib/admin/actions.ts`: extend `AdminCreateInvitationArgs` interface to ADD optional `firstName`, `lastName`, `companyName`, `siret`, `phone`, `invitationMessage` fields. In the function body, modify the two `writeAuditLog` calls (`user.create` and `invitation.create`) to include a new `profile` sub-key in the payload object — a plain object containing the optional fields whose values are non-empty (drop any field whose value is `undefined` or `''`). Preserve the existing ADMIN-09 redaction comment ("D-09-09b: ADMIN-09 redaction — this payload intentionally excludes financial rate fields") — the new profile fields are PII not financial rate; the redaction note still holds.
       - In `src/lib/admin/index.ts`: re-export `createPartnerFormSchema` and the new `CreatePartnerFormValues` type alongside the existing exports.
       Run `npx vitest run src/lib/admin` — all cases pass (GREEN).

    (c) Compile-time check: `npm run build && npm run lint` — no TypeScript errors. `CreatePartnerModal.tsx` continues to compile against the unchanged `createPartnerSchema`.

    Document the audit-log shape choice in the SUMMARY (chosen: `payload: { userId, email, displayName, language, profile: { firstName?, lastName?, companyName?, siret?, phone?, invitationMessage? } }` with optional fields omitted when empty).
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin &amp;&amp; npm run build &amp;&amp; bash -c "grep -q 'createPartnerFormSchema' src/lib/admin/schemas.ts" &amp;&amp; bash -c "grep -q 'invitationMessage' src/lib/admin/actions.ts"</automated>
  </verify>
  <done>`createPartnerFormSchema` exported from `src/lib/admin/schemas.ts` per UI-SPEC §5.1; `adminCreateInvitation` accepts the extended optional args and writes them into the audit-log payload under a `profile` sub-key; the legacy 3-field call site (CreatePartnerModal) still compiles + works; all Vitest cases under `src/lib/admin/` pass; build + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build /partners/new server-page + CreatePartnerForm client component + tests</name>
  <files>app/(admin)/[adminSegment]/partners/new/page.tsx, app/(admin)/[adminSegment]/partners/new/page.test.tsx, app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx, app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.test.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`partners/new/page.tsx` + §`partners/new/CreatePartnerForm.tsx` — concrete file:line excerpts for imports, RHF setup, section headers, hr dividers, field pattern, submit button, modal mount
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.1 — full layout + interaction + accessibility spec
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §6.3 — the ~30 partners.new.* i18n keys × FR + EN (copy verbatim — DO NOT paraphrase)
    - app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx (the analog — RHF setup, error-rendering pattern, submit-button spinner state). Reads at lines 32-43 (RHF config), 164-207 (field pattern), 274-297 (submit button), 49-78 (modal-only Escape handler — NOT applicable to the page form).
    - app/(authed)/proposals/new/parametres/ParametresFormCard.tsx lines 86-95 (●-bulleted section header) + 183-190 (hr divider)
    - app/(authed)/proposals/new/verification/page.test.tsx lines 36-60 (server-component test harness; hoisted mocks pattern)
    - src/components/InviteUrlModal.tsx — props shape (consumed verbatim)
    - src/components/proposal/PhoneInput.tsx — for the phone field per UI-SPEC §5.1.3
    - app/(admin)/[adminSegment]/partners/AccountsList.tsx lines 137-152 (sonner toast pattern) + 507-515 (InviteUrlModal mount pattern)
  </read_first>
  <behavior>
    - Test 1 (page.test.tsx — RED first): server-component renders an `<h1>` containing the resolved `partners.new.title` text AND mounts `<CreatePartnerForm>`. Mock `requireAdmin` to resolve; mock `getCurrentLang` to return `'fr'`; mock `t` to identity.
    - Test 2 (page.test.tsx — auth gate): when `requireAdmin` throws (Next.js redirect), the page DOES NOT render the form. Use the redirect-throws pattern from `verification/page.test.tsx`.
    - Test 3 (CreatePartnerForm.test.tsx — required-field validation): rendering the form, focusing then blurring the `firstName` input with empty value, asserts `aria-invalid="true"` AND a `.error-msg` with text matching the i18n key `error.field.required`.
    - Test 4 (CreatePartnerForm.test.tsx — happy path submit): fill all 6 required fields, leave SIRET empty, leave invitationMessage empty, click submit. The mocked `createPartnerAction` is called with the expected payload; on `{ ok: true, url, kind: 'invitation' }` the success toast fires AND `<InviteUrlModal>` mounts.
    - Test 5 (CreatePartnerForm.test.tsx — duplicate-email error): mock the server action to throw the string `'admin.accounts.modal.error.email.exists'`; assert the duplicate-error toast key is the one displayed (`partners.new.toast.error.duplicate`).
    - Test 6 (CreatePartnerForm.test.tsx — char counter): typing 1001 chars into the invitationMessage textarea shifts the counter to `--danger` color AND sets `aria-invalid="true"` on the textarea. (Color assertion: the counter element has the danger class OR computed style — pick one and document in SUMMARY.)
    - Test 7 (CreatePartnerForm.test.tsx — cancel link): the `← Annuler` link has `href={\`/${adminSegment}/partners\`}` and `aria-label` resolved from `partners.new.cancel.aria`.
    - Test 8 (i18n parity — compile-time): `_EnHasAllFrKeys` continues to typecheck after adding ~30 new keys.
  </behavior>
  <action>
    Three-step TDD build, then dictionary expansion.

    (a) RED — write the 7 failing tests above first (page.test.tsx + CreatePartnerForm.test.tsx). Use the harness patterns from `verification/page.test.tsx` (hoisted mocks) and `ParametresFormCard.test.tsx` (RHF + zodResolver client form). `npx vitest run app/(admin)/[adminSegment]/partners/new` MUST fail (the source files don't exist yet).

    (b) GREEN — implement `page.tsx` per PATTERNS.md §`partners/new/page.tsx` exactly:
       - Imports: `Metadata` from next, `requireAdmin` from `@/lib/auth/require`, `getCurrentLang` + `t` from `@/lib/i18n`, the local `CreatePartnerForm`, and the server action wrapper described below.
       - `export const dynamic = 'force-dynamic'`.
       - `export const metadata: Metadata = { title: 'Créer un partenaire — Leasétic Matrice', robots: { index: false, follow: false } }`.
       - `interface PageProps { params: Promise<{ adminSegment: string }> }`.
       - Default export: `async function CreatePartnerPage({ params })`. Body: `await params` → destructure `adminSegment`; `await requireAdmin()`; `const lang = await getCurrentLang()`; return a 1040px-max-width container with the h1 + subtitle hero block + `<CreatePartnerForm lang={lang} adminSegment={adminSegment} createPartnerAction={createPartnerInvitationAction} />`.
       - Define a server action `createPartnerInvitationAction` (either inline `'use server'` function or imported from `src/lib/admin/actions.ts` — pick imported; that's where the rest of the admin actions live). The action: `'use server'`, validates input with `createPartnerFormSchema.parse(data)`, composes `displayName = \`${firstName} ${lastName}\`.trim()`, picks language from session (or hardcode 'fr' if session doesn't carry partner-language preference — verify by reading `getCurrentLang` impl), calls `adminCreateInvitation({ email, displayName, language, firstName, lastName, companyName, siret, phone, invitationMessage })`, returns `{ ok: true, url: result.url, kind: 'invitation' }`. On thrown error: catch and return `{ ok: false, error: e.message }`. Export this action from `src/lib/admin/index.ts`.

    (c) GREEN — implement `CreatePartnerForm.tsx` per PATTERNS.md §`partners/new/CreatePartnerForm.tsx` + UI-SPEC §5.1:
       - `'use client'`. Imports: `useState`, `useRef` from React; `Link`, `useRouter` from next; `useForm` from `react-hook-form`; `zodResolver` from `@hookform/resolvers/zod`; `Loader2` from `lucide-react`; `toast` from `sonner`; `createPartnerFormSchema` + `CreatePartnerFormValues` from `@/lib/admin`; `t` + `type Lang, type DictKey` from `@/lib/i18n/dictionaries`; `<InviteUrlModal>` from `@/components/InviteUrlModal`; `<PhoneInput>` from `@/components/proposal/PhoneInput` for the phone field.
       - Props: `lang: Lang`, `adminSegment: string`, `createPartnerAction: (data: CreatePartnerFormValues) => Promise<{ ok: true; url: string; kind: 'invitation' } | { ok: false; error: string }>`.
       - RHF setup per `<interfaces>` block: `mode: 'onBlur'`, `shouldFocusError: true`, all 7 fields defaulted to empty strings.
       - Layout: single `<form className="card" onSubmit={handleSubmit(onSubmit)}>` containing 3 `<section>` blocks. Each section opens with `<div className="ctitle"><span className="dot" style={{ background: 'var(--gd)' }} aria-hidden="true" /><span>{t('partners.new.section.{slug}', lang)}</span></div>` (margin-bottom 16px). Between sections: `<hr style={{ border: 'none', borderTop: '1px solid var(--border)', margin: '24px 0' }} />`. Inside each section, fields stack with the `.fld` class (Phase 11 chrome).
       - Section 1 fields (personal): firstName (required, `autoComplete="given-name"`), lastName (required, `autoComplete="family-name"`), email (required, `type="email"`, `autoComplete="email"`).
       - Section 2 fields (company): companyName (required, `autoComplete="organization"`), siret (NOT required — no red asterisk; `inputMode="numeric"`, `autoComplete="off"`, placeholder `14 chiffres`), phone (required, use `<PhoneInput>` from src/components/proposal/PhoneInput).
       - Section 3: a `<textarea>` for invitationMessage with `name="invitationMessage"`, min-height 120px, line-height 1.55, padding 12px. Below it, a right-aligned char counter `<span aria-live="polite">{watchedLength}/1000</span>` that switches to `--danger` color when `watchedLength > 1000`. Use RHF's `watch('invitationMessage')` for the live count.
       - Action bar (inside the same card, 24px top margin): `display: flex; align-items: center; justify-content: space-between;`. Left: `<Link href={\`/${adminSegment}/partners\`} aria-label={t('partners.new.cancel.aria', lang)}>{t('partners.new.cancel', lang)}</Link>`. Right: `<button type="submit" className="btn-green">` with `Loader2` spinner inside when `isSubmitting`, label morphs to `partners.new.submit.spinner` during submit.
       - Submit handler per D-08: `const result = await createPartnerAction(data);` — on `result.ok === true`: `toast.success(t('partners.new.toast.success', lang))` + `setInviteUrl({ url: result.url, kind: 'invitation' })`. On `result.ok === false`: map known error keys (`admin.accounts.modal.error.email.exists` → `partners.new.toast.error.duplicate`) and fire `toast.error(...)`; fall back to `partners.new.toast.error`.
       - InviteUrlModal mount per PATTERNS.md analog: `{inviteUrl && (<InviteUrlModal url={inviteUrl.url} kind={inviteUrl.kind} lang={lang} onClose={() => { setInviteUrl(null); reset(); router.push(\`/${adminSegment}/partners\`); }} triggerRef={submitBtnRef} />)}`.
       - Accessibility per UI-SPEC §5.1 + §7.3: every input has a `<label htmlFor>` + `aria-invalid` + `aria-describedby` linking to the `.error-msg`. Submit button has `aria-busy={isSubmitting}`. Cancel link has the `aria-label` resolved from `partners.new.cancel.aria`. Textarea char-counter has `aria-live="polite"`.

    (d) GREEN — add the ~30 `partners.new.*` keys to `src/lib/i18n/dictionaries.ts` in BOTH `fr` and `en` per UI-SPEC §6.3 (verbatim from the table). Run `npm run build` — `_EnHasAllFrKeys` typecheck passes.

    Run `npx vitest run app/(admin)/[adminSegment]/partners/new` — all 7 cases pass (GREEN). Commit incrementally per the TDD discipline: `test(14-02): add failing tests for /partners/new` → `feat(14-02): ship /partners/new route + form` → `feat(14-02): add partners.new.* i18n keys`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/partners/new &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -q 'requireAdmin' app/(admin)/[adminSegment]/partners/new/page.tsx" &amp;&amp; bash -c "grep -q 'dynamic.*force-dynamic' app/(admin)/[adminSegment]/partners/new/page.tsx" &amp;&amp; bash -c "grep -q 'createPartnerFormSchema' app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx" &amp;&amp; bash -c "grep -q 'partners.new.title' src/lib/i18n/dictionaries.ts"</automated>
  </verify>
  <done>`/[adminSegment]/partners/new` route renders for admins with the 3-section form; submitting valid data triggers the server action, opens `<InviteUrlModal>` with the returned invite URL, and on modal close resets + redirects to `/partners`. Vitest cases for the page + form all pass. The ~30 partners.new.* i18n keys land in both dictionaries with `_EnHasAllFrKeys` typecheck passing. Build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /[adminSegment]/partners/new (GET) | Untrusted: an unauthenticated or non-admin user MUST be rejected by `requireAdmin()` before any data fetch. |
| Browser → createPartnerInvitationAction (POST via Next.js server action) | Untrusted: form fields cross the wire; server-side re-validation with `createPartnerFormSchema.parse()` is mandatory. |
| Server action → `adminCreateInvitation` → audit_log | The newly-extended payload fields (firstName, lastName, companyName, siret, phone, invitationMessage) are PII; audit_log access is admin-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Spoofing/Authorization | `partners/new/page.tsx` | mitigate | `await requireAdmin()` AUTH-15 defence-in-depth gate at the top of the server component. Vitest case in `page.test.tsx` covers the redirect-throws path. |
| T-14-02-02 | Spoofing/Authorization | `createPartnerInvitationAction` (server action) | mitigate | The server action calls `adminCreateInvitation` which internally calls `await requireAdmin()` (existing Phase 9 contract at actions.ts line 197). Verified by the existing `adminCreateInvitation.test.ts` cases. |
| T-14-02-03 | Tampering | Form input → server action | mitigate | Server-side `createPartnerFormSchema.parse(input)` re-validates EVERY field — never trust the client-side RHF state. The schema enforces email format, SIRET regex, phone regex, max-length on companyName/firstName/lastName, and the 1000-char cap on invitationMessage. Vitest unit tests on `createPartnerFormSchema` cover the boundary cases. |
| T-14-02-04 | Information disclosure (PII leakage) | `audit_log` payload extended fields | mitigate | The audit_log is admin-only (Phase 6 RLS / Phase 9 RBAC). New fields are PII not financial — D-09-09b ADMIN-09 commission redaction comment preserved unchanged. The plan adds NO commission values to the audit payload. Grep gate (covered by Plan 14-06): the rendered /partners/new HTML contains zero commission strings. |
| T-14-02-05 | Information disclosure (ADMIN-09 D-29) | `/partners/new` form HTML | mitigate | Form renders no rate/commission values. Grep gate in Plan 14-06: `grep -c 'commission\|commission_pct' rendered-partners-new.html` returns 0. Per Phase 14 D-29 strict. |
| T-14-02-06 | Repudiation | Invite issuance | mitigate | Existing ADMIN-08 audit writes (`user.create` + `invitation.create`) continue with the extended payload. Both writes are inside `adminCreateInvitation`; the wrapping server action does NOT swallow errors. |
| T-14-02-07 | DoS / Resource exhaustion | invitationMessage 1000-char cap | mitigate | Schema cap (1000) prevents arbitrary-length writes to audit_log. Vitest covers the >1000 reject case. Client-side counter is UX guidance only; server-side parse is the authoritative gate. |
| T-14-02-08 | CSRF | Server action POST | mitigate | Better Auth session SameSite cookie + Next.js built-in server-action CSRF token (framework-default). No new POST endpoint introduced beyond the server action. |
| T-14-02-09 | XSS (stored, via invitationMessage future email-template injection) | Persisted `invitationMessage` field | accept | The message is currently persisted to audit_log only; it is NOT yet rendered in the invite email (deferred to v1.3+ per CONTEXT.md §Deferred Ideas — "/partners/new email-preview block"). When v1.3+ wires the email template, that phase MUST add HTML-escaping at render time. Phase 14 stores the raw user input — the audit log displays it inside `<pre>` blocks in the admin audit viewer (Phase 9), which inherits the existing audit-log escaping behavior. |
| T-14-02-10 | Information disclosure (D-30 invariant) | InviteUrlModal display | accept | The InviteUrlModal renders only the one-time invite URL — not commission values. Phase 9 contract preserved. |

**ADMIN-09 grep gate (covered by Plan 14-06):** the rendered HTML of `/[adminSegment]/partners/new` (server-render snapshot) contains ZERO commission-related strings (`commission`, `commission_pct`, percentage symbol applied to commission, etc.).
</threat_model>

<verification>
- `npx vitest run app/(admin)/[adminSegment]/partners/new` — all 7+ cases pass.
- `npx vitest run src/lib/admin` — schema + actions cases pass (including the new extended-args tests).
- `npm run build` — production build succeeds, `_EnHasAllFrKeys` compile-time proof holds.
- `npm run lint` — clean.
- Manual smoke (NOT automated in CI but documented in SUMMARY): visit `/<seg>/partners/new` as an admin, fill the form with valid data, submit, confirm InviteUrlModal opens with a URL, close modal, confirm redirect to `/<seg>/partners`. Visit as a non-admin → confirm redirect by `requireAdmin()`.
</verification>

<success_criteria>
- ROUTE-02 success criterion #1: route exists, 3-section form (personal/company/message), issues invite via existing Phase 9 path, InviteUrlModal opens.
- `createPartnerFormSchema` distinct from existing `createPartnerSchema` (back-compat with shelved modal preserved per D-10).
- `adminCreateInvitation` accepts the extended optional args and writes them into the audit-log `profile` sub-key.
- Server-component admin route: `requireAdmin` + `dynamic = 'force-dynamic'` + `robots: { index: false, follow: false }`.
- Client-component form: RHF + zodResolver + onBlur, 3 ●-bulleted sections separated by `<hr>`, action bar with Cancel Link + Submit button + spinner state, sonner toasts for success/error, `<InviteUrlModal>` mount on success → on close reset + router push to `/partners`.
- ~30 partners.new.* i18n keys land verbatim per UI-SPEC §6.3 in both FR and EN; `_EnHasAllFrKeys` typecheck passes.
- All Vitest cases for the plan pass; total suite count increases by approximately 12-15 cases (7 form + 2 page + ~3-5 schema/action).
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-02-SUMMARY.md` documenting:
- The schema split decision (new schema vs extending existing): chosen path + rationale
- The audit-log payload shape with the new `profile` sub-key
- Exact list of i18n keys added (count + names)
- The InviteUrlModal kind value used (`'invitation'` per Phase 9 contract)
- Test count delta
- Any deviations from D-05..D-09 (none expected; flag if so)
</output>
