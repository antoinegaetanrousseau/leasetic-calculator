---
phase: 14-admin-polish-partners-history-home
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - app/(admin)/[adminSegment]/coefficients/page.tsx
  - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx
  - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx
  - app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebarRow.tsx
  - app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx
  - app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "Visiting /<seg>/coefficients as admin shows a 2-column layout (editor 1fr + sidebar 360px) at max-width 1040px."
    - "The sidebar renders a card with header `● HISTORIQUE`, up to 5 most-recent coefficient_history rows, and a footer link `Voir tout l'historique →` to /<seg>/history."
    - "Clicking a sidebar row toggles inline expansion showing the CoefficientDiffPanel in 'condensed' mode (single-column before→after) below the row meta."
    - "Multiple sidebar rows can be expanded simultaneously (per D-20 — differs from /history single-active)."
    - "When listCoefficientHistory returns no rows, the sidebar renders the empty-state copy `coefficients.history.empty` and the footer link is hidden."
    - "The CoefficientDiffPanel component is reusable in both 'condensed' (sidebar) and 'full' (Plan 14-05 /history) modes via a `mode` prop."
    - "Each diff row marks isChanged with a gold-10% tint pill on the after-value (condensed) or the changed cells (full)."
    - "The diff panel renders commission values from before_json/after_json (admin-only D-30 — allowed inside the diff panel since requireAdmin gates the surface)."
  artifacts:
    - path: "app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx"
      provides: "Server-component sidebar shell that fetches listCoefficientHistory({ limit: 5 }) and renders 5 row instances + footer link"
      contains: "listCoefficientHistory"
    - path: "app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebarRow.tsx"
      provides: "'use client' per-row component owning expanded state + rendering CoefficientDiffPanel in condensed mode when expanded"
      contains: "useState"
    - path: "app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx"
      provides: "Shared client component with mode='condensed'|'full' driving layout — condensed = single-column before→after; full = 2-col grid Avant|Après"
      contains: "CoefficientDiffPanelProps"
    - path: "app/(admin)/[adminSegment]/coefficients/page.tsx"
      provides: "Modified server component wrapping editor + sidebar in a 2-col 1040px grid (D-17)"
      contains: "minmax(0, 1fr) 360px"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "4 new coefficients.history.* keys + history.diff.* keys consumed by the shared diff panel"
      contains: "coefficients.history.title"
  key_links:
    - from: "CoefficientHistorySidebar.tsx"
      to: "src/lib/db/queries/coefficient-history.ts listCoefficientHistory"
      via: "await listCoefficientHistory({ limit: 5 })"
      pattern: "listCoefficientHistory\\(\\{\\s*limit:\\s*5"
    - from: "CoefficientHistorySidebarRow.tsx"
      to: "app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx"
      via: "<CoefficientDiffPanel mode=\"condensed\" row={…} lang={…} />"
      pattern: "mode=\"condensed\""
    - from: "coefficients/page.tsx"
      to: "CoefficientHistorySidebar"
      via: "right column of 2-col grid"
      pattern: "<CoefficientHistorySidebar"
---

<objective>
Ship the `/coefficients` 2-column layout + History sidebar + the SHARED `<CoefficientDiffPanel>` component (consumed by both the sidebar here in condensed mode AND the `/history` standalone route in Plan 14-05 in full mode).

1. Build the shared `<CoefficientDiffPanel>` at `app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx` per UI-SPEC §5.4. This component lives in the `history/` directory (not in `coefficients/`) because (a) the file is logically associated with history rendering, and (b) Plan 14-05's `/history` route needs to import it without crossing into a sibling-directory's "internals" — colocating it with the standalone /history page is the cleaner home. The sidebar imports it via relative path `../history/CoefficientDiffPanel`.
   - Props: `{ row: CoefficientHistoryListRow; mode: 'condensed' | 'full'; lang: Lang; onClose?: () => void }`.
   - Reuse `computeDiffPairs` helper from existing `app/(admin)/[adminSegment]/coefficients/HistoryDiff.tsx` (per PATTERNS.md §`CoefficientDiffPanel.tsx`) — import directly OR extract to a shared module (recommendation: import directly via `../coefficients/HistoryDiff` for minimum churn).
   - Flatten paths render order per UI-SPEC §5.4: `commission_pct` → `max_amount` → `validity_days` → 12 coefficient cells in `t1.36, t1.48, t1.60, t2.36, t2.48, t2.60, t3.36, t3.48, t3.60, t4.36, t4.48, t4.60` order.
   - Value formatting: `commission_pct` as `{value}%`, `max_amount` via `formatCurrency(value, lang)`, `validity_days` as `{value} jours` (FR) / `{value} days` (EN), coefficients as the raw 8-decimal string from `before_json/after_json`.
   - `mode === 'full'` (used by Plan 14-05): 2-col grid `1fr 1fr`, column headers `AVANT` / `APRÈS`, changed cells get `rgba(224, 133, 48, 0.10)` bg + `--radius-md` rounding + font-weight 600, summary line at bottom (italic), `Fermer ×` close button at bottom-right with onClose handler + Escape key listener (Escape handler imported from the CreatePartnerModal pattern per PATTERNS.md — drop the focus-trap, keep only the keydown listener).
   - `mode === 'condensed'` (used by THIS plan's sidebar): single-column, each row shows `path` label on line 1 then `before → after` on line 2, with the after-value getting the gold-pill treatment when isChanged. NO close button (the sidebar parent row owns expansion state).
   - Seed-row case (`before_json === null`): full mode renders `(aucun)` as the AVANT column header (i18n `history.diff.before.none`) and `—` em-dash in each AVANT cell; condensed mode renders `—` as the "before" value.

2. Build `<CoefficientHistorySidebar>` (server component) + `<CoefficientHistorySidebarRow>` (client component) per UI-SPEC §5.2 and PATTERNS.md §`CoefficientHistorySidebar.tsx`:
   - Server component shell fetches `await listCoefficientHistory({ limit: 5 })` (Phase 12 helper), renders the outer `.card` with header `● HISTORIQUE`, maps rows to `<CoefficientHistorySidebarRow row={r} lang={lang} />` instances, renders the footer `Voir tout l'historique →` Link to `/${adminSegment}/history`.
   - Per-row client component owns `const [expanded, setExpanded] = useState(false)` (per D-20: multi-expand allowed in sidebar). Row layout per UI-SPEC §5.2: italic summary on top line, meta line below (`formatDate(row.changedAt, lang) · row.changedBy`). On click/Enter: toggle expanded; when expanded, render `<CoefficientDiffPanel mode="condensed" row={row} lang={lang} />` below the meta line with 16px top margin.
   - Empty state: when `listCoefficientHistory` returns `{ rows: [] }`, render header + an italic muted `coefficients.history.empty` line, hide the footer link.
   - Accessibility per UI-SPEC §7.3: outer `<section aria-label={t('coefficients.history.aria.label', lang)}>`; each row `<div role="button" tabIndex={0} aria-expanded={open} aria-controls={\`history-diff-\${row.id}\`}>` with click + Enter + Escape handlers; expanded panel `<div id={...} role="region">`.

3. Modify `app/(admin)/[adminSegment]/coefficients/page.tsx` per UI-SPEC §5.6 + PATTERNS.md §`coefficients/page.tsx`:
   - Wrap existing editor + ExplainTool content + the new sidebar in a 2-col grid: `display: grid; grid-template-columns: minmax(0, 1fr) 360px; gap: 24px; max-width: 1040px;`.
   - **Decision per UI-SPEC §5.6 open question (planner ruling):** REMOVE the Phase 9 `<HistoryTable>` from `/coefficients`. The sidebar covers the 5-most-recent at-a-glance need; Plan 14-05's `/history` route covers the full historical view. Keeping both is redundant (UI-SPEC §5.6 recommendation). Document the removal in the SUMMARY. The Phase 9 `HistoryTable.tsx` file STAYS on disk (Phase 9 component, may be reused) — only the JSX usage in `coefficients/page.tsx` is removed.
   - Pass `adminSegment` to `<CoefficientHistorySidebar>` for the footer-link href.
   - Sidebar scrolls WITH the editor (NOT sticky) per CONTEXT.md Claude's Discretion (locked: scroll-with-editor).
   - Responsive collapse at `<1024px` is handled by the existing 1040px outer wrapper + an inline style flag (per UI-SPEC §5.6 — desktop-primary, mobile-tracking out of Phase 14 scope; the inline `grid-template-columns: 'minmax(0, 1fr) 360px'` is acceptable).

4. Add ~10 new i18n keys per UI-SPEC §6.5 + §6.6 (the diff-panel subset that Plan 14-05 also consumes; placed here so the diff panel ships with its strings):
   - `coefficients.history.title` (`HISTORIQUE` / `HISTORY`)
   - `coefficients.history.aria.label` (`Historique des coefficients` / `Coefficient history`)
   - `coefficients.history.viewAll` (`Voir tout l'historique →` / `View full history →`)
   - `coefficients.history.empty` (`Aucun changement de coefficient pour le moment.` / `No coefficient changes yet.`)
   - `history.diff.before` (`AVANT` / `BEFORE`)
   - `history.diff.after` (`APRÈS` / `AFTER`)
   - `history.diff.before.none` (`(aucun)` / `(none)`)
   - `history.diff.close` (`Fermer ×` / `Close ×`)
   - `history.diff.close.aria` (`Fermer le détail` / `Close details`)
   - `history.diff.panel.aria.label` (`Détail du changement de coefficient du {0} par {1}` / `Coefficient change details from {0} by {1}`)
   Plan 14-05 adds the remaining `history.*` keys (title, subtitle, row.viewDetail, pagination.*, empty) — partitioned per file ownership.

5. Vitest tests:
   - `CoefficientHistorySidebar.test.tsx` — server-component harness asserting (a) 5 rows render when listCoefficientHistory returns 5, (b) empty-state copy renders when [] returned (footer hidden), (c) footer link has href `/<seg>/history`, (d) header is the `● HISTORIQUE` chrome.
   - `CoefficientDiffPanel.test.tsx` — component harness asserting per UI-SPEC §7.3 + §4.3:
     - Full mode renders 2-col grid with `AVANT` + `APRÈS` headers + 15 diff rows (3 top-level + 12 coefficient cells).
     - Condensed mode renders single-column with `before → after` pattern per row.
     - Changed cell has `data-changed="true"` AND background style matching `rgba(224, 133, 48, 0.10)`.
     - Seed-row case (before_json=null): AVANT cells show em-dash, AVANT header text becomes `(aucun)`.
     - Close button in full mode calls onClose; Escape key in full mode calls onClose.
     - The diff panel renders commission_pct (this is the D-30 admin-only exception: the diff panel is INSIDE admin-gated surfaces).

Purpose: ROUTE-02 success criterion #5 (Coefficients page surfaces History sidebar populated from coefficient_history showing past 5 edits; each row links to a full diff). The shared diff panel unlocks Plan 14-05 to reuse the same component for `/history`.

Output: `/<seg>/coefficients` shows the new 2-col layout with the working history sidebar; the shared `<CoefficientDiffPanel>` is ready for Plan 14-05 to import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-polish-partners-history-home/14-CONTEXT.md
@.planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md
@.planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md

# Source files this plan consumes / modifies
@app/(admin)/[adminSegment]/coefficients/page.tsx
@app/(admin)/[adminSegment]/coefficients/HistoryTable.tsx
@app/(admin)/[adminSegment]/coefficients/HistoryDiff.tsx
@app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx
@src/lib/db/queries/coefficient-history.ts
@src/lib/i18n/dictionaries.ts
@src/lib/i18n/format.ts
@src/lib/admin/coefficient-diff.ts

<interfaces>
<!-- listCoefficientHistory (src/lib/db/queries/coefficient-history.ts) — already shipped Phase 12 -->
export interface CoefficientHistoryListRow {
  id: string;
  changedAt: Date;
  changedById: string;
  changedByDisplay: string | null;  // displayName ?? name ?? email fallback chain
  beforeJson: GlobalParamsSnapshot | null;  // null for the seed row
  afterJson: GlobalParamsSnapshot;
  summary: string;
}
export interface CoefficientHistoryListResult {
  rows: CoefficientHistoryListRow[];
  hasMore: boolean;
  nextCursor: CoefficientHistoryCursor | null;
}
export interface ListCoefficientHistoryArgs {
  cursor?: CoefficientHistoryCursor;
  limit: number;
}
export async function listCoefficientHistory(args: ListCoefficientHistoryArgs): Promise<CoefficientHistoryListResult>;

<!-- computeDiffPairs (existing in HistoryDiff.tsx — partial analog per PATTERNS.md) -->
// Flattens before/after snapshots into rows {path, before, after} pairs.
// Import via: import { computeDiffPairs } from '../coefficients/HistoryDiff';

<!-- formatDate (src/lib/i18n/format.ts) -->
export function formatDate(value: Date, lang: 'fr' | 'en'): string;

<!-- CoefficientDiffPanel props (NEW, this plan ships) -->
export interface CoefficientDiffPanelProps {
  row: CoefficientHistoryListRow;
  mode: 'condensed' | 'full';
  lang: 'fr' | 'en';
  onClose?: () => void;  // consumed only in 'full' mode
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shared CoefficientDiffPanel (condensed + full modes) with tests</name>
  <files>app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx, app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx, src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.4 (full props + layout for both modes) + §4.3 (gold-10% tint contract for changed rows) + §6.6 (history.diff.* i18n keys) + §6.8 (diff panel field labels — TECHNICAL keys, not i18n)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`CoefficientDiffPanel.tsx` (fresh-write notes + computeDiffPairs reuse + Escape-key pattern from CreatePartnerModal lines 49-78)
    - app/(admin)/[adminSegment]/coefficients/HistoryDiff.tsx — full file; locate `computeDiffPairs` export and understand the GlobalParamsSnapshot shape it consumes
    - src/lib/db/queries/coefficient-history.ts — types `CoefficientHistoryListRow` + `GlobalParamsSnapshot`
    - src/lib/i18n/format.ts — `formatCurrency` + `formatDate` signatures
    - app/(admin)/[adminSegment]/partners/CreatePartnerModal.tsx lines 49-78 — Escape key keydown handler pattern (drop the focus-trap, keep only the keydown listener)
    - src/components/ui/StatusChip.test.tsx — vitest component test pattern reference
  </read_first>
  <behavior>
    - Test 1 (RED first — full mode): rendering `<CoefficientDiffPanel mode="full" row={mockRow} lang="fr" onClose={mockClose} />` produces a `<div role="region">` containing two grid columns with header text `AVANT` + `APRÈS` (resolved from i18n).
    - Test 2 (RED first — changed-cell highlight): when before.commission_pct=5.00 and after.commission_pct=5.50, the rendered after-cell has `data-changed="true"` AND a computed background style containing `rgba(224, 133, 48` (10% gold tint). Unchanged rows have `data-changed="false"` and NO background style.
    - Test 3 (RED first — seed row): when `row.beforeJson === null`, the AVANT header text resolves to `(aucun)` (FR) AND every AVANT cell renders `—` (em-dash U+2014). The APRÈS cells render normally with all `isChanged === true`.
    - Test 4 (RED first — condensed mode): `<CoefficientDiffPanel mode="condensed" row={mockRow} lang="fr" />` renders single-column rows with `before → after` (U+2192 arrow). The after-value cell gets the gold-pill when isChanged; the before-value stays plain.
    - Test 5 (RED first — close button + Escape — full mode only): clicking the `Fermer ×` button calls `onClose`. Pressing Escape on `document` calls `onClose`. Condensed mode has NO close button (assert it does NOT render in condensed mode).
    - Test 6 (RED first — summary line): both modes render the `row.summary` value below the diff rows in italic body/default style.
    - Test 7 (RED first — i18n parity): the 10 new keys are wired with FR + EN values; `_EnHasAllFrKeys` typecheck passes (compile-time).
    - Test 8 (RED first — commission rendering: D-30 explicit case): the diff panel DOES render commission_pct values in both AVANT and APRÈS cells. This is the D-30 admin-only exception — the diff panel is admin-only by virtue of living under `/[adminSegment]/`. Verify the rendered HTML CONTAINS the commission percentage string (e.g. `5.00%` or `5.50%`).
  </behavior>
  <action>
    Two-step TDD:

    (a) RED — write all 8 test cases in `CoefficientDiffPanel.test.tsx` BEFORE implementing the component. Use `vitest` + `@testing-library/react`. Mock data: build a `CoefficientHistoryListRow` fixture with realistic before/after snapshots (commission_pct changing 5.00 → 5.50; one coefficient cell t2/48 changing 2.85 → 2.90; validity_days unchanged at 30; max_amount unchanged at 50000). Also build a seed-row fixture (`beforeJson: null`). The 10 new i18n keys MUST be added to dictionaries first (so the mock `t` function can resolve them) — do this as part of the RED step. Run `npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx` — all cases fail.

    (b) GREEN — implement `CoefficientDiffPanel.tsx` per UI-SPEC §5.4:
       - `'use client'` (needs the Escape keydown effect + close button onClick).
       - Imports: `useEffect`, `useRef` from React; `computeDiffPairs` from `../coefficients/HistoryDiff` (reuses the existing Phase 9 flatten helper per PATTERNS.md); `formatCurrency` + `formatDate` from `@/lib/i18n/format`; `t`, `type Lang` from `@/lib/i18n/dictionaries`; types from `@/lib/db/queries/coefficient-history`.
       - Implement `formatValue(path, raw, lang)` helper: `commission_pct` → `${raw}%`, `max_amount` → `formatCurrency(Number(raw), lang)`, `validity_days` → FR `${raw} jours` / EN `${raw} days`, coefficients → `String(raw)` (raw 8-decimal string).
       - Implement the diff-rows derivation: call `computeDiffPairs(beforeJson, afterJson)` and map each pair through `formatValue`. If `computeDiffPairs` doesn't already produce the 15-row shape (commission_pct, max_amount, validity_days, 12 coefficients), wrap or extend it inline so the render iteration is over a flat array.
       - Full-mode layout: `<div role="region" aria-labelledby={...} aria-label={t('history.diff.panel.aria.label', lang).replace('{0}', formatDate(row.changedAt, lang)).replace('{1}', row.changedByDisplay ?? '')}>` containing: column-headers row (grid 1fr 1fr; left=AVANT text, right=APRÈS text; AVANT becomes `(aucun)` via `history.diff.before.none` when beforeJson is null); then map diff pairs into a grid 1fr 1fr row per pair with `<div className="diff-row" data-changed={isChanged}>` cells; then a summary line `<p style={{ fontStyle: 'italic', marginTop: 20 }}>{row.summary}</p>`; then the close button row `<div style={{ display: 'flex', justifyContent: 'flex-end', marginTop: 16 }}><button type="button" onClick={onClose} aria-label={t('history.diff.close.aria', lang)}>{t('history.diff.close', lang)}</button></div>`.
       - Changed-cell inline style per UI-SPEC §4.3: `{ background: 'rgba(224, 133, 48, 0.10)', borderRadius: 6, padding: '2px 4px', fontWeight: 600 }`. Unchanged cells: no special styling.
       - Condensed-mode layout: single column; per pair render `<div className="diff-row-condensed" data-changed={isChanged}><div className="diff-row__label">{path}</div><div><span>{before ?? '—'}</span><span className="diff-row__arrow"> → </span><span style={isChanged ? goldPillStyle : undefined}>{after}</span></div></div>`. Vertical rhythm: 12px (md) between consecutive rows; summary line at the bottom with 20px top margin. NO close button.
       - Escape-key keydown handler (full mode only, per PATTERNS.md §`CoefficientDiffPanel.tsx`): in a `useEffect` that depends on `mode` + `onClose`, attach a `document.addEventListener('keydown', onKey)` listener that calls `onClose()` when `e.key === 'Escape'`. Remove the listener on cleanup. The hook returns early if `mode !== 'full'` OR if `onClose` is undefined (don't attach the listener at all in condensed mode).
       - Aria: full mode panel has `role="region"` + `aria-label` (computed from the panel-aria template key); close button has `aria-label={t('history.diff.close.aria', lang)}`.

       Run `npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx` — all 8 cases pass (GREEN). Commit: `feat(14-04): ship shared CoefficientDiffPanel (condensed + full modes)`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx &amp;&amp; npm run build &amp;&amp; bash -c "grep -q 'CoefficientDiffPanelProps' app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx" &amp;&amp; bash -c "grep -q \"'condensed' | 'full'\" app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx" &amp;&amp; bash -c "grep -q 'history.diff.before' src/lib/i18n/dictionaries.ts"</automated>
  </verify>
  <done>Shared `<CoefficientDiffPanel>` exports both 'condensed' and 'full' modes; all 8 vitest cases pass; the 10 history.diff.* + coefficients.history.* i18n keys land in both dictionaries; build clean; the component is ready for Plan 14-05 to import for the /history route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CoefficientHistorySidebar + per-row component, wire into /coefficients 2-col layout, remove Phase 9 HistoryTable usage</name>
  <files>app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.tsx, app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebarRow.tsx, app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx, app/(admin)/[adminSegment]/coefficients/page.tsx</files>
  <read_first>
    - .planning/phases/14-admin-polish-partners-history-home/14-UI-SPEC.md §5.2 (sidebar layout + interaction + empty state + ARIA) + §5.6 (2-col page layout) + §6.5 (4 coefficients.history.* i18n keys — already added by Task 1)
    - .planning/phases/14-admin-polish-partners-history-home/14-PATTERNS.md §`CoefficientHistorySidebar.tsx` + §`coefficients/page.tsx` (concrete code excerpts)
    - app/(admin)/[adminSegment]/coefficients/page.tsx (full file — Phase 9 layout to modify)
    - app/(admin)/[adminSegment]/coefficients/HistoryTable.tsx (the Phase 9 history table — note the `Set<string>` toggle-expand pattern at lines 34, 50-57; copy for the sidebar row)
    - src/lib/db/queries/coefficient-history.ts — `listCoefficientHistory` signature (verified in `<interfaces>`)
    - src/lib/i18n/format.ts — `formatDate` signature
    - app/(authed)/proposals/new/verification/page.test.tsx — server-component test harness
  </read_first>
  <behavior>
    - Test 1 (RED first — sidebar renders 5 rows): when `listCoefficientHistory` mock returns 5 rows, the rendered output contains 5 `<CoefficientHistorySidebarRow>` mounts. Use mocked component or assert via testid.
    - Test 2 (RED first — empty state): when mock returns `{ rows: [] }`, the sidebar renders the `coefficients.history.empty` text AND the footer link is NOT in the DOM.
    - Test 3 (RED first — footer link): when rows exist, footer link has `href={\`/${adminSegment}/history\`}` and text `Voir tout l'historique →` (resolved from i18n).
    - Test 4 (RED first — header): outer card has the `● HISTORIQUE` chrome (the `.ctitle` + `.dot` pattern). Aria-label on the outer section resolves from `coefficients.history.aria.label`.
    - Test 5 (RED first — row toggle): rendering `<CoefficientHistorySidebarRow row={mockRow} lang="fr" />`, clicking the row toggles `aria-expanded` from `false` to `true` AND the `<CoefficientDiffPanel mode="condensed">` appears in the DOM. Re-clicking collapses.
    - Test 6 (RED first — multi-expand): rendering 3 rows in a sidebar, clicking row 1 then row 2 leaves BOTH expanded simultaneously (per D-20).
    - Test 7 (manual but documented in SUMMARY — visual): visit `/<seg>/coefficients` as admin; sidebar appears on the right at 360px; editor on the left.
    - Test 8 (page.tsx — Phase 9 HistoryTable removal): grep `app/(admin)/[adminSegment]/coefficients/page.tsx` for `<HistoryTable` — ZERO matches (the JSX usage is removed; the .tsx file itself stays on disk).
  </behavior>
  <action>
    Two-step TDD:

    (a) RED — write the 7 automated test cases in `CoefficientHistorySidebar.test.tsx`. The file uses both server-component harness (for the outer sidebar) and client-component harness (for the row). Mock `listCoefficientHistory` via `vi.mock('@/lib/db/queries/coefficient-history', () => ({ ... }))`. Use the `t = (k: string) => k` identity mock for i18n so tests can assert by key name. Mock `<CoefficientDiffPanel>` to a placeholder `<div data-testid="diff-panel" data-mode={mode} />` so we assert prop passing without exercising the full panel. Run `npx vitest run app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx` — all cases fail.

    (b) GREEN — implement the three new files + modify `page.tsx`:

       (i) `CoefficientHistorySidebar.tsx` (server component) per PATTERNS.md §`CoefficientHistorySidebar.tsx`:
       - Imports: `listCoefficientHistory` from `@/lib/db/queries/coefficient-history`; `Link` from next; `t`, `type Lang` from `@/lib/i18n/dictionaries`; the local `<CoefficientHistorySidebarRow>`.
       - Props: `{ lang: Lang; adminSegment: string }`.
       - Body: `const { rows } = await listCoefficientHistory({ limit: 5 });` → render `<section className="card" aria-label={t('coefficients.history.aria.label', lang)}>` containing the `.ctitle` header, the row map (or empty-state line), and the footer link (hidden when rows empty).
       - Empty state: `{rows.length === 0 && <p style={{ fontStyle: 'italic', color: 'var(--muted)', padding: '20px 0' }}>{t('coefficients.history.empty', lang)}</p>}` — footer link wrapped in `{rows.length > 0 && (...)}`.
       - Footer link: `<div style={{ borderTop: '1px solid var(--border)', marginTop: 16, paddingTop: 12 }}><Link href={\`/${adminSegment}/history\`} style={{ color: 'var(--teal)', fontSize: 14.5, fontWeight: 500, textDecoration: 'none' }}>{t('coefficients.history.viewAll', lang)}</Link></div>`.

       (ii) `CoefficientHistorySidebarRow.tsx` (`'use client'`):
       - Imports: `useState` from React; `formatDate` from `@/lib/i18n/format`; `t`, `type Lang` from `@/lib/i18n/dictionaries`; `<CoefficientDiffPanel>` from `../history/CoefficientDiffPanel`; type `CoefficientHistoryListRow` from `@/lib/db/queries/coefficient-history`.
       - Props: `{ row: CoefficientHistoryListRow; lang: Lang }`.
       - State: `const [expanded, setExpanded] = useState(false);` (multi-expand at the parent level — each row owns its own state per D-20).
       - Container: `<div role="button" tabIndex={0} aria-expanded={expanded} aria-controls={\`history-diff-\${row.id}\`} style={{...row card chrome from UI-SPEC §5.2: bg var(--surface), 1px border, radius 12, padding 20...}} onClick={() => setExpanded(v => !v)} onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); setExpanded(v => !v); } if (e.key === 'Escape') setExpanded(false); }}>`.
       - Content (collapsed): top line italic body/default `{row.summary}`, bottom line meta `{formatDate(row.changedAt, lang)} · {row.changedByDisplay ?? '—'}` in body/small `--muted`. (Per UI-SPEC §5.2 displayName fallback chain — use `changedByDisplay` which already encodes the fallback per Phase 12 contract.)
       - Content (expanded): same collapsed content + `<div id={\`history-diff-\${row.id}\`} role="region" style={{ marginTop: 16 }}><CoefficientDiffPanel mode="condensed" row={row} lang={lang} /></div>`.

       (iii) Modify `app/(admin)/[adminSegment]/coefficients/page.tsx` per UI-SPEC §5.6 + PATTERNS.md §`coefficients/page.tsx`:
       - Import `<CoefficientHistorySidebar>` at the top.
       - Wrap existing editor + ExplainTool render block in a 2-col grid: `<div style={{ maxWidth: 1040, margin: '0 auto' }}>{ existing header } <div style={{ display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 360px', gap: 24, alignItems: 'start', marginTop: 24 }}><div>{<CoefficientsEditor /> + <ExplainTool />}</div><CoefficientHistorySidebar lang={lang} adminSegment={adminSegment} /></div></div>`.
       - REMOVE the `<HistoryTable />` JSX from the page (per UI-SPEC §5.6 ruling — the file `HistoryTable.tsx` stays on disk as Phase 9 code, only the import + usage in page.tsx is removed). Verify `grep -c '<HistoryTable' app/(admin)/[adminSegment]/coefficients/page.tsx` returns 0.

       Run `npx vitest run app/(admin)/[adminSegment]/coefficients/CoefficientHistorySidebar.test.tsx` + `npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx` — all pass (GREEN). Commit: `feat(14-04): ship coefficient history sidebar + 2-col /coefficients layout`.
  </action>
  <verify>
    <automated>npx vitest run app/(admin)/[adminSegment]/coefficients &amp;&amp; npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; bash -c "grep -q 'minmax(0, 1fr) 360px' app/(admin)/[adminSegment]/coefficients/page.tsx" &amp;&amp; bash -c "grep -q 'CoefficientHistorySidebar' app/(admin)/[adminSegment]/coefficients/page.tsx" &amp;&amp; bash -c "grep -c '<HistoryTable' app/(admin)/[adminSegment]/coefficients/page.tsx | grep -q '^0$'"</automated>
  </verify>
  <done>`/[adminSegment]/coefficients` renders the 2-col layout with `<CoefficientHistorySidebar>` on the right; the Phase 9 `<HistoryTable>` JSX usage is removed (file stays on disk); the sidebar fetches via `listCoefficientHistory({ limit: 5 })`, renders 5 rows or the empty state, has the footer link to /<seg>/history; per-row click expands inline with `<CoefficientDiffPanel mode="condensed">`; multi-expand works; all vitest cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /[adminSegment]/coefficients | Existing `requireAdmin()` AUTH-15 gate (Phase 9 contract, preserved). |
| Server component → `listCoefficientHistory` query | Read-only SELECT; no untrusted input passes through (limit is a hard-coded `5` in the sidebar caller). |
| Diff panel render → before_json / after_json | Per D-30: these snapshots contain commission values. The diff panel is rendered ONLY inside admin-gated surfaces (`/coefficients` + `/history` — both gated by `requireAdmin()` upstream). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Spoofing/Authorization | `/coefficients/page.tsx` | mitigate | Existing `requireAdmin()` call preserved at the top of the server component (Phase 9 contract). Sidebar inherits the gate transitively because it's only rendered inside the gated page. |
| T-14-04-02 | Information disclosure (ADMIN-09 D-29 + D-30) | Diff panel rendering commission values | accept (per D-30) | The diff panel MAY render commission values because it lives inside the admin-only `/coefficients` (Plan 14-04) and `/history` (Plan 14-05) routes — both gated by `requireAdmin()`. D-30 explicitly logs this as the single admin-only exception to the D-29 strict no-commission-visibility rule. The exception is documented in CONTEXT.md decisions section. |
| T-14-04-03 | Information disclosure (ADMIN-09 D-29 — sidebar SUMMARIES) | `generateDiffSummary` output rendered in sidebar row card | accept | The summary string is produced by Phase 12's `generateDiffSummary(before, after)` (`src/lib/admin/coefficient-diff.ts`). This string MAY include commission_pct value transitions (e.g. `Commission : 5.00% → 5.50%`). Per D-30 the surfaces are admin-only — the summary appearing in the admin-side sidebar is intentional. Plan 14-06's grep gates verify the partner-list HTML does NOT contain commission values; the sidebar gate is intentionally permissive. |
| T-14-04-04 | Information disclosure (ADMIN-09 D-29 — coefficients editor body) | The editor card on /coefficients (unchanged Phase 9 component) | accept | The CoefficientsEditor already displays commission_pct in its form fields per Phase 9 ADMIN-01. Phase 14 does NOT restyle the editor; ADMIN-09 D-30 contract preserved. |
| T-14-04-05 | DoS | `listCoefficientHistory({ limit: 5 })` | accept | Hard-coded limit; cursor not exposed to client. |
| T-14-04-06 | Tampering | Sidebar row click toggling expand | accept | Pure client-side `useState`; no server interaction. The diff panel renders the already-fetched JSON; no additional DB roundtrip on expand. |
| T-14-04-07 | XSS (stored, via row.summary) | Sidebar row + diff panel summary line | accept | `row.summary` originates from `generateDiffSummary(before, after)` — a pure-deterministic Phase 12 helper consuming admin-edit-time numeric values + a tiny set of FR field labels. No user-controlled input enters the pipeline. React's default escaping applies. |
| T-14-04-08 | XSS (stored, via row.changedByDisplay) | Sidebar row meta | mitigate | `changedByDisplay` is a fallback chain of `users.displayName ?? users.name ?? users.email`. ALL three are admin-controlled inputs (admins create partners via `/partners/new`); even if a hypothetical admin types HTML in `displayName`, React's default escaping renders it as text. ASVS L1 V5.3.3 satisfied. |
| T-14-04-09 | Information disclosure | Diff panel computeDiffPairs returns no PII | accept | `before_json` and `after_json` are `global_params` snapshots — no PII (no user emails, no IDs). The single PII surface is `changedByDisplay` which is the admin who made the edit — visible only to other admins. ASVS L1 V5.3.4 OK. |
| T-14-04-10 | Repudiation | Coefficient edit traceability | accept | Existing Phase 12 audit trail via `coefficient_history` append-only table; this plan only READS that table (via `listCoefficientHistory`). Write-side discipline unchanged. |

**ADMIN-09 grep gates relevant to this plan (covered + verified in Plan 14-06):**
- `/coefficients` page HTML (excluding the editor card body + the sidebar diff panel + sidebar summaries — those are all admin-gated D-30 exceptions): bare commission references outside those zones MUST be 0.
- The diff panel renders commission_pct freely (D-30 exception).
- The Phase 9 editor card on `/coefficients` already shows commission_pct fields (D-30 exception — pre-existing Phase 9 behavior, not introduced by Phase 14).
</threat_model>

<verification>
- `npx vitest run app/(admin)/[adminSegment]/coefficients` — all sidebar tests pass.
- `npx vitest run app/(admin)/[adminSegment]/history/CoefficientDiffPanel.test.tsx` — all 8 diff-panel tests pass.
- `npm run build` — succeeds; `_EnHasAllFrKeys` typecheck with the 10 new keys.
- `npm run lint` — clean.
- Manual smoke (documented in SUMMARY): visit `/<seg>/coefficients` → confirm 2-col layout, sidebar on right, click a row → diff panel expands inline; click another row → both rows expanded simultaneously.
</verification>

<success_criteria>
- ROUTE-02 success criterion #5: `/coefficients` surfaces History sidebar populated from coefficient_history showing past 5 edits with diff summaries; each row links to a full diff (via the inline-expansion `<CoefficientDiffPanel mode="condensed">`; the standalone full diff lives in Plan 14-05).
- Shared `<CoefficientDiffPanel>` component ships at `app/(admin)/[adminSegment]/history/CoefficientDiffPanel.tsx` with both `condensed` and `full` modes wired (Plan 14-05 imports it for /history).
- `<CoefficientHistorySidebar>` + `<CoefficientHistorySidebarRow>` ship at `app/(admin)/[adminSegment]/coefficients/`.
- `/coefficients` page uses the 2-col 1040px layout per UI-SPEC §5.6; Phase 9 `<HistoryTable>` JSX usage removed (file stays).
- 10 new i18n keys land in both dictionaries per UI-SPEC §6.5 + §6.6 (coefficients.history.* + history.diff.* subset).
- D-30 invariant honored: commission values appear ONLY inside the diff panel and inside the (pre-existing) coefficients editor — both admin-gated. No other Phase 14-introduced surface renders commission values.
- All Vitest cases pass; build + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-polish-partners-history-home/14-04-SUMMARY.md` documenting:
- Files created (CoefficientDiffPanel.tsx + .test.tsx, CoefficientHistorySidebar.tsx + .test.tsx, CoefficientHistorySidebarRow.tsx)
- The diff-pair derivation strategy (reused `computeDiffPairs` vs reimplemented)
- The /coefficients HistoryTable removal decision + confirmation that the Phase 9 file stays on disk
- Test count delta
- The 10 i18n keys added (count + names — should match UI-SPEC §6.5 + §6.6 subset)
- D-30 exception application: which surfaces render commission values in this plan + why that's OK
- Any deviations from D-17..D-20, D-30 (none expected; flag if so)
</output>
