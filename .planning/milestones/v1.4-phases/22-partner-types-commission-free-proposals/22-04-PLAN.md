---
phase: 22-partner-types-commission-free-proposals
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/lib/api/proposals/finalize-helpers.ts
  - src/lib/api/proposals/finalize-wizard.ts
  - app/api/proposals/finalize/route.ts
  - app/(authed)/proposals/new/calcul/page.tsx
  - app/(authed)/proposals/new/verification/page.tsx
  - src/components/proposal/LiveLoyerPreview.tsx
  - app/(authed)/proposals/new/parametres/ParametresFormCard.tsx
autonomous: true
requirements: [PTYPE-04, PTYPE-05, PTYPE-06]
must_haves:
  truths:
    - "For an Agent/Commercial owner, the wizard step-2 Détail du calcul and step-3 recap render NO commission row — surrounding rows close up with no gap or placeholder"
    - "For an Agent/Commercial owner, the live preview loyer equals montant HT × coefficient / 100 (commission-free), with no commission annotation"
    - "For a Partenaire owner, the existing D-12 commission row and the unchanged formula are preserved exactly"
    - "A finalized Agent/Commercial proposal computes the commission-free loyer and its params_snapshot records partnerType + commissionApplied: false"
    - "finalize-wizard.ts source remains grep-clean of the literal 'commission' token (the parameter name stays isolated in finalize-helpers.ts)"
  artifacts:
    - path: "src/lib/api/proposals/finalize-helpers.ts"
      provides: "buildComputeArgs + buildParamsSnapshot accept partnerType; commission-free branch + snapshot fields"
      contains: "partnerType"
    - path: "app/(authed)/proposals/new/calcul/page.tsx"
      provides: "commission row pushed only when partnerType === 'Partenaire'"
      contains: "partnerType"
    - path: "app/(authed)/proposals/new/verification/page.tsx"
      provides: "commission row in calculRows only when partnerType === 'Partenaire'"
      contains: "partnerType"
  key_links:
    - from: "app/api/proposals/finalize/route.ts"
      to: "finalizeWizard"
      via: "partnerType read from session.user and passed into FinalizeWizardArgs"
      pattern: "partnerType"
    - from: "src/lib/api/proposals/finalize-helpers.ts"
      to: "computeLoyer"
      via: "commissionPct: partnerType === 'Partenaire' ? params.commissionPct : 0"
      pattern: "commissionPct"
    - from: "src/components/proposal/LiveLoyerPreview.tsx"
      to: "computeLoyer"
      via: "commissionPct: 0 for Agent/Commercial"
      pattern: "commissionPct"
---

<objective>
Wire the commission-free branch through every proposal-economics surface owned by the proposal author: the live preview (step 1), wizard step-2 (calcul) Détail du calcul, wizard step-3 (verification) recap, and the finalize pipeline (compute + snapshot). For Agent/Commercial the commission row is STRUCTURALLY ABSENT (not CSS-hidden) — the rows array is simply shorter, surrounding rows close up (D-05). The loyer everywhere uses `commissionPct: 0`. The finalize snapshot records `partnerType` + `commissionApplied` so the PDF stays reproducible after a later type change (PTYPE-06, D-02).

Purpose: PTYPE-04 call-site (commission-free loyer), PTYPE-05 (commission absent from all UI for Agent/Commercial), PTYPE-06 (commission-free finalize compute + snapshot integrity).
Output: finalize-helpers branch + snapshot fields, finalize-wizard threading, route session-read, two wizard pages conditionally omitting the commission row, live-preview commission-free compute.

INVARIANTS:
- formula.ts is FROZEN — commission-free is achieved only by passing `commissionPct: 0` (verified against Plan 22-02's golden corpus).
- finalize-wizard.ts must stay grep-clean of the `commission` literal; the parameter name lives ONLY in finalize-helpers.ts (the D-12/D-28 grep-isolation barrier). Any new commission-free branch logic that must name `commissionPct` goes in finalize-helpers.ts, NOT finalize-wizard.ts.
- The D-12 partner-facing relaxation (Partenaire deal-owner sees commission on steps 2+3) is preserved for `Partenaire` ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md
@.planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md
@.planning/phases/22-partner-types-commission-free-proposals/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commission-free finalize compute + snapshot fields (PTYPE-06)</name>
  <files>src/lib/api/proposals/finalize-helpers.ts, src/lib/api/proposals/finalize-wizard.ts, app/api/proposals/finalize/route.ts</files>
  <read_first>
    - src/lib/api/proposals/finalize-helpers.ts (buildParamsSnapshot + buildComputeArgs — the ONLY file allowed to name the commission parameter; the grep-isolation docstring at the top)
    - src/lib/api/proposals/finalize-wizard.ts (FinalizeWizardArgs ~line 134; buildComputeArgs/buildParamsSnapshot call sites ~lines 171, 198; buildComputedJson/buildPdfComputed usage; the grep-clean discipline noted at line 170)
    - app/api/proposals/finalize/route.ts (requireUser session read ~lines 57-60; the finalizeWizard call; force-dynamic)
    - src/db/schema.ts (the paramsSnapshot type extension from Plan 22-01: partnerType + commissionApplied)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"finalize-wizard.ts", §Shared Patterns "commissionPct: 0 seam", "Structural absence")
  </read_first>
  <action>
    In src/lib/api/proposals/finalize-helpers.ts (the only file permitted to name the commission parameter): change `buildComputeArgs(parsed, params, partnerType)` to accept the partner type and set `commissionPct: partnerType === 'Partenaire' ? parseNumeric(params.commissionPct) : 0`. Extend `buildParamsSnapshot(params, partnerType)` to additionally return `partnerType` and `commissionApplied: partnerType === 'Partenaire'` alongside the existing commissionPct/maxAmount/validityDays/coefficients fields (the snapshot still records the percentage for byte-determinism; commissionApplied records whether it was factored in). Keep the snapshot shape aligned with the schema.ts paramsSnapshot type from Plan 22-01.

    In src/lib/api/proposals/finalize-wizard.ts: add `partnerType: 'Agent' | 'Commercial' | 'Partenaire'` to FinalizeWizardArgs. Pass it into the buildComputeArgs and buildParamsSnapshot calls. Do NOT introduce the literal string `commission` in this file's source — pass partnerType through; the helper does the branching. This preserves the grep-clean barrier.

    In app/api/proposals/finalize/route.ts: the session is already read via requireUser() (the proposal owner). Read `session.user.partnerType` and pass it into the finalizeWizard args. (Owner = author; partners create their own proposals, so the author's type drives the economics.)
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck 2>&1 | tail -3 && echo "grep-clean check:" && grep -v '^[[:space:]]*//' src/lib/api/proposals/finalize-wizard.ts | grep -c commission</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "partnerType" src/lib/api/proposals/finalize-helpers.ts` shows both helpers accept it
    - `grep -n "commissionApplied" src/lib/api/proposals/finalize-helpers.ts` shows the snapshot records it
    - `grep -v '^[[:space:]]*//' src/lib/api/proposals/finalize-wizard.ts | grep -c commission` returns 0 (grep-clean barrier intact — comments stripped before counting)
    - `grep -n "partnerType" app/api/proposals/finalize/route.ts` shows session.user.partnerType threaded into finalizeWizard
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>finalize-helpers branches on partnerType (commissionPct:0 for Agent/Commercial) and records partnerType + commissionApplied in the snapshot; finalize-wizard threads partnerType while staying grep-clean; route reads it from session.</done>
</task>

<task type="auto">
  <name>Task 2: Conditionally omit the commission row on wizard steps 2 + 3 (PTYPE-05 / D-05)</name>
  <files>app/(authed)/proposals/new/calcul/page.tsx, app/(authed)/proposals/new/verification/page.tsx</files>
  <read_first>
    - app/(authed)/proposals/new/calcul/page.tsx (session read ~line 106; the server computeLoyer call ~lines 131-142; the detailRows array with the D-12 commission entry ~lines 211-243; commissionDisplay/commissionAmount computation)
    - app/(authed)/proposals/new/verification/page.tsx (the calculRows array with the D-12 commission entry ~lines 233-247; the rowSublabels index-2 commission sublabel ~lines 326-329)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§"calcul/page.tsx", §"verification/page.tsx", §Shared Patterns "Structural absence (not CSS hiding)")
  </read_first>
  <action>
    In both pages, read `const partnerType = session.user.partnerType` and compute `const isPartenaire = partnerType === 'Partenaire'`.

    calcul/page.tsx: pass `commissionPct: isPartenaire ? parseNumeric(params.commissionPct) : 0` to the server computeLoyer call. Build detailRows so the commission entry is PUSHED ONLY when isPartenaire (use a conditional `.push` or a filtered array — NOT a row that renders empty). For Agent/Commercial the array is one entry shorter; the surrounding rows (montant HT, coefficient, durée, loyer) close up with no gap, no placeholder, no "sans commission" note (D-05 literal). Do NOT compute `commissionDisplay`/`commissionAmount` at all when !isPartenaire — guard those computations so the value is never materialized on the commission-free path.

    verification/page.tsx: same treatment. Add the commission entry to calculRows only when isPartenaire; pass the `rowSublabels` index-2 commission sublabel only when the commission row is present (so the sublabel map doesn't reference a non-existent row). For Agent/Commercial calculRows has 2 entries instead of 3. Server computeLoyer call uses `commissionPct: isPartenaire ? ... : 0`.

    Keep the D-12 ADMIN-09 relaxation comments intact on the Partenaire branch (they document the bounded partner-facing exposure that continues for Partenaire only).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck 2>&1 | tail -3 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "partnerType\|isPartenaire" app/(authed)/proposals/new/calcul/page.tsx` shows the gate
    - `grep -n "partnerType\|isPartenaire" app/(authed)/proposals/new/verification/page.tsx` shows the gate
    - Reading both files confirms the commission row is conditionally PUSHED/added (not rendered with display:none or an empty placeholder) and that commissionDisplay/commissionAmount are not computed on the !isPartenaire path
    - Both pages pass `commissionPct: isPartenaire ? <param> : 0` to computeLoyer
    - `npm run typecheck` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>For Agent/Commercial both wizard review surfaces omit the commission row entirely (structural absence, clean reflow) and compute the commission-free loyer; Partenaire surfaces unchanged including the D-12 relaxation.</done>
</task>

<task type="auto">
  <name>Task 3: Commission-free live preview loyer (PTYPE-04 / PTYPE-05 step 1)</name>
  <files>src/components/proposal/LiveLoyerPreview.tsx, app/(authed)/proposals/new/parametres/ParametresFormCard.tsx</files>
  <read_first>
    - src/components/proposal/LiveLoyerPreview.tsx (LiveLoyerPreviewProps ~lines 19-27; the useMemo computeLoyer call ~lines 84-90 which currently omits commissionPct and relies on the seedParams default)
    - app/(authed)/proposals/new/parametres/ParametresFormCard.tsx (where LiveLoyerPreview is rendered — this is the consumer that must pass partnerType down; check whether it has access to session/partnerType or must receive it as a prop from its server-page parent)
    - .planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md (§Shared Patterns "commissionPct: 0 seam")
  </read_first>
  <action>
    Add `partnerType: 'Agent' | 'Commercial' | 'Partenaire'` to LiveLoyerPreviewProps. In the useMemo computeLoyer call, pass `commissionPct: partnerType === 'Partenaire' ? undefined : 0` — for Partenaire keep the current default behavior (omit → seedParams.commissionPct, unchanged), for Agent/Commercial force 0 so the previewed loyer equals montant HT × coefficient / 100. The preview shows no commission annotation today (it only renders the loyer figure), so PTYPE-05's "no commission text" is already met; this change only corrects the loyer VALUE for Agent/Commercial.

    In ParametresFormCard.tsx, thread `partnerType` into the LiveLoyerPreview render. If ParametresFormCard does not already have partnerType in scope, accept it as a prop and have its server-page parent pass `session.user.partnerType` (read the parent page to confirm the wiring path — follow the same prop-drilling pattern already used for `lang`). Do not read session inside the client component.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck 2>&1 | tail -3 && npm run test -- LiveLoyerPreview 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "partnerType" src/components/proposal/LiveLoyerPreview.tsx` shows the prop and its use in the computeLoyer call
    - `grep -n "partnerType" app/(authed)/proposals/new/parametres/ParametresFormCard.tsx` shows it threaded to LiveLoyerPreview
    - The computeLoyer call passes `commissionPct: 0` (not the default) for Agent/Commercial — verified by reading the ternary
    - `npm run typecheck` exits 0; existing LiveLoyerPreview tests pass (update fixtures to pass partnerType if the test instantiates the component)
  </acceptance_criteria>
  <done>Live preview receives partnerType and previews the commission-free loyer for Agent/Commercial (no commission text shown, value corrected); Partenaire preview unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session.user.partnerType → calc/render | The author's persisted type drives proposal economics and what renders |
| finalize-wizard.ts source → CI grep gate | The commission parameter name must stay isolated in finalize-helpers.ts |
| saved proposal → future type change | A later type change must NOT mutate existing snapshots/PDFs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-I | Information disclosure | commission leakage to Agent/Commercial surfaces | mitigate | **HIGH** — commission row is structurally absent (array shorter), commissionDisplay/amount not computed on the commission-free path; finalize-wizard.ts stays grep-clean; the value never materializes. Verified by Plan 22-05 grep + no-commission gates. |
| T-22-04-T | Tampering | params_snapshot immutability after type change | mitigate | **HIGH** — snapshot records partnerType + commissionApplied at finalize time and is written once (Stripe Option A, never updated); a later adminUpdatePartnerType (Plan 22-03) does not touch existing proposals. PTYPE-06 reproducibility holds. |
| T-22-04-T2 | Tampering | frozen formula | mitigate | commission-free achieved only via commissionPct:0; formula.ts untouched (Plan 22-02 golden corpus is the regression guard). |
| T-22-04-E | Elevation of Privilege | author drives own economics | accept | The author's type is admin-assigned and client-immutable (Plan 22-01 input:false; Plan 22-03 admin-gated); the author cannot self-promote to commission-free, so reading their own type to drive their own proposal is safe. |
| T-22-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to audit. |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm run lint` exits 0
- `grep -v '^[[:space:]]*//' src/lib/api/proposals/finalize-wizard.ts | grep -c commission` returns 0
- Both wizard pages omit the commission row for Agent/Commercial (structural absence) and keep it for Partenaire (D-12)
- Live preview, step-2, step-3, and finalize all compute commissionPct:0 for Agent/Commercial
- Snapshot records partnerType + commissionApplied
</verification>

<success_criteria>
End-to-end, an Agent/Commercial proposal shows no commission anywhere on screen, computes montant HT × coefficient / 100, and finalizes with a snapshot that records the specific type + commissionApplied:false; a Partenaire proposal is byte-for-byte unchanged including the D-12 relaxation; the grep-isolation barrier and frozen formula are intact.
</success_criteria>

<output>
Create `.planning/phases/22-partner-types-commission-free-proposals/22-04-SUMMARY.md` when done.
</output>
