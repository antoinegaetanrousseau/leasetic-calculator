---
phase: 22-partner-types-commission-free-proposals
plan: 05
type: execute
wave: 3
depends_on: ["22-01", "22-03", "22-04"]
files_modified:
  - tests/admin-09-grep-contracts.test.ts
  - src/lib/pdf/no-commission.test.ts
autonomous: true
requirements: [PTYPE-06, PTYPE-07]
must_haves:
  truths:
    - "The admin-09 grep-contract suite gains gates asserting zero commission leakage on the partner-type surfaces (list with each of the 3 types, create form with the selector, row-action type-change UI)"
    - "All pre-existing 12+ admin-09 gates remain green"
    - "The no-commission PDF 4-layer corpus drives finalizeWizard with an Agent and a Commercial owner and asserts zero commission in render data, computed jsonb, audit payload, and server logs"
    - "An Agent/Commercial finalize records commissionApplied:false and the specific partnerType in the snapshot, proving PTYPE-06 reproducibility"
  artifacts:
    - path: "tests/admin-09-grep-contracts.test.ts"
      provides: "new gates for partner-type surfaces; makePartnerRow carries partnerType"
      contains: "partnerType"
    - path: "src/lib/pdf/no-commission.test.ts"
      provides: "Agent + Commercial variants of the 4-layer structural gate"
      contains: "partnerType"
  key_links:
    - from: "tests/admin-09-grep-contracts.test.ts"
      to: "PartnersList / CreatePartnerForm / PartnerRowActions"
      via: "renderToString + assertNoCommissionLeakage on each partner-type surface"
      pattern: "assertNoCommissionLeakage"
    - from: "src/lib/pdf/no-commission.test.ts"
      to: "finalizeWizard"
      via: "mock user partnerType Agent/Commercial drives the 4-layer assertions"
      pattern: "partnerType"
---

<objective>
Extend the two ADMIN-09 structural-absence test suites to assert zero commission leakage for the Agent/Commercial branch (PTYPE-07) and to prove the commission-free PDF + snapshot integrity (PTYPE-06). The grep-contract suite gains gates over the new partner-type surfaces (list badge for all three types, create-form selector, row-action type-change UI); the PDF no-commission 4-layer corpus gains Agent and Commercial finalize runs. All existing 12+ gates stay green.

Purpose: PTYPE-07 (grep suite extended; defense-in-depth across calc output, UI, PDF, logs, audit) + PTYPE-06 (PDF reproducibility — snapshot records type + commissionApplied:false, no commission in any layer).
Output: new grep gates + makePartnerRow partnerType field; Agent/Commercial no-commission corpus variants.

This plan is Wave 3 — it runs AFTER Plans 22-03 (admin surfaces) and 22-04 (commission-free presentation) so the surfaces it greps actually exist. It also re-touches the KNOWN_MIGRATIONS Set that Plan 22-01 seeded (file overlap on no-commission.test.ts → sequenced after 22-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md
@.planning/phases/22-partner-types-commission-free-proposals/22-PATTERNS.md
@.planning/phases/22-partner-types-commission-free-proposals/22-01-SUMMARY.md
@.planning/phases/22-partner-types-commission-free-proposals/22-03-SUMMARY.md
@.planning/phases/22-partner-types-commission-free-proposals/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend admin-09 grep-contract suite for partner-type surfaces (PTYPE-07)</name>
  <files>tests/admin-09-grep-contracts.test.ts</files>
  <read_first>
    - tests/admin-09-grep-contracts.test.ts (the assertNoCommissionLeakage helper with COMMISSION_PCT_RX/PCT_SUFFIX_RX ~lines 154-164; makePartnerRow fixture ~lines 129-139; Surface 1 partners-list test ~lines 170-182; Surface 2 create-form test; the existing 12+ it() gates)
    - app/(admin)/[adminSegment]/partners/PartnersList.tsx (final form after Plan 22-03 — the type badge column)
    - app/(admin)/[adminSegment]/partners/new/CreatePartnerForm.tsx (final form after Plan 22-03 — the selector)
    - app/(admin)/[adminSegment]/partners/_components/PartnerRowActions.tsx (final form after Plan 22-03 — the type-change menu item)
    - 22-03-SUMMARY.md (the exact prop shapes / fixture fields the new surfaces expect)
  </read_first>
  <action>
    Update `makePartnerRow` to include `partnerType: 'Partenaire'` in the default fixture (now required on PartnerRow after Plan 22-03). Add new `it()` gates inside the existing Surface 1 (partners list) describe block — one render per partner type ('Agent', 'Commercial', 'Partenaire') — each calling renderToString(PartnersList, ...) with the type set and `assertNoCommissionLeakage(html, 'partners list (<type>)')`. Extend the Surface 2 (create form) gate to render with the partnerType selector present and assert no commission strings. Add a new Surface/gate for the row-action type-change UI (PartnerRowActions) following the identical createElement + assertNoCommissionLeakage shape. Each new gate asserts the partner-type badge/selector/menu introduces NO `commission_pct` and NO `_pct` suffix token. Do NOT weaken assertNoCommissionLeakage; reuse it verbatim. Keep all existing gates untouched and green.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run test -- admin-09-grep-contracts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "partnerType" tests/admin-09-grep-contracts.test.ts` ≥ 1 (makePartnerRow + new gates)
    - New gates exist for all three partner-type values on the list, the create-form selector, and the row-action type-change UI
    - `npm run test -- admin-09-grep-contracts` passes with the gate count increased vs baseline (was 13 it()) and ALL prior gates still green
    - assertNoCommissionLeakage is reused unchanged (same COMMISSION_PCT_RX / PCT_SUFFIX_RX)
  </acceptance_criteria>
  <done>Grep-contract suite covers all partner-type surfaces with zero-commission assertions; existing 12+ gates remain green; helper unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend no-commission PDF 4-layer corpus for Agent/Commercial (PTYPE-06)</name>
  <files>src/lib/pdf/no-commission.test.ts</files>
  <read_first>
    - src/lib/pdf/no-commission.test.ts (the 4-layer structural gate ~lines 248-306: render data JSON, computed jsonb, paramsSnapshot, audit/log; the commissionFormatsFor helper ~lines 199-211; how finalizeWizard is driven with a mock user; the KNOWN_MIGRATIONS Set ~lines 565-592 — already updated by Plan 22-01)
    - src/lib/api/proposals/finalize-wizard.ts (final form after Plan 22-04 — FinalizeWizardArgs now carries partnerType)
    - src/lib/api/proposals/finalize-helpers.ts (final form after Plan 22-04 — snapshot records partnerType + commissionApplied)
    - 22-04-SUMMARY.md (the exact FinalizeWizardArgs shape and snapshot field names)
  </read_first>
  <action>
    Add a describe block (or extend the existing fixture loop) that drives finalizeWizard with a mock user carrying `partnerType: 'Agent'` and again `partnerType: 'Commercial'`. For each, assert the same 4 layers as the existing corpus: (1) render-data JSON contains no 'commission' substring (case-insensitive), (2) persisted `computed` jsonb has no 'commission' key and no 'commission' substring, (3) the audit payload contains no commission field, (4) server-log capture contains no commission string. Additionally assert PTYPE-06 reproducibility: the resulting `paramsSnapshot.commissionApplied === false` and `paramsSnapshot.partnerType` equals the specific mock type ('Agent' / 'Commercial'). Confirm the loyer in the render data equals the commission-free value (montant HT × coefficient / 100) for at least one fixture. Verify the KNOWN_MIGRATIONS Set already lists the Plan 22-01 migration filename (it should from Plan 22-01; correct only if drift). Keep the existing Partenaire/30-fixture assertions green.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run test -- no-commission 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "partnerType" src/lib/pdf/no-commission.test.ts` ≥ 1
    - New assertions drive finalizeWizard with partnerType 'Agent' AND 'Commercial' and run all 4 layers for each
    - At least one assertion checks `paramsSnapshot.commissionApplied === false` and `paramsSnapshot.partnerType` equals the mock type (PTYPE-06 reproducibility)
    - `npm run test -- no-commission` passes including the drizzle-guard test with the Plan 22-01 migration filename
    - The existing Partenaire corpus remains green
  </acceptance_criteria>
  <done>The 4-layer no-commission corpus covers Agent + Commercial finalize runs and proves snapshot records partnerType + commissionApplied:false with no commission leakage in any layer; existing corpus green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate</name>
  <files>tests/admin-09-grep-contracts.test.ts</files>
  <read_first>
    - tests/admin-09-grep-contracts.test.ts (confirm final gate count after Task 1)
    - .planning/ROADMAP.md (§"v1.4 Cross-Cutting Invariants" §1 — the 12-gate suite must stay green; §4 PDF byte-determinism)
  </read_first>
  <action>
    Run the full test suite and the static guards to confirm Phase 22 introduced no regressions: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run check:no-drizzle-push`, `npm run check:no-vercel-imports`. If the PDF byte-determinism gate (a golden SHA fixture test) fails because the commission-free loyer changed a fixture, STOP — that fixture regeneration belongs to Phase 23 (PDF-03), not here; report it as an expected cross-phase dependency rather than regenerating the fixture in this plan. (Phase 22 only ADDS commission-free coverage; the existing Partenaire fixtures must not change.) This task is verification-only — no source edits beyond confirming Task 1's count.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck 2>&1 | tail -2 && npm run lint 2>&1 | tail -2 && npm run check:no-drizzle-push 2>&1 | tail -1 && npm run test 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run check:no-drizzle-push` exits 0
    - `npm run test` passes (all suites including admin-09, no-commission, calc.golden, partners, dictionaries) OR any failure is solely the byte-determinism fixture flagged as a Phase 23 dependency with NO change to existing Partenaire fixtures
    - The admin-09 gate count is ≥ the baseline 12 and all original gates pass
  </acceptance_criteria>
  <done>Full suite + static guards green (or the only failure is the documented Phase 23 byte-determinism fixture dependency); Phase 22 ships with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered HTML / PDF data / audit / logs → grep CI gates | Every commission-bearing surface must structurally exclude commission for Agent/Commercial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-I | Information disclosure | commission leakage on any Agent/Commercial surface | mitigate | **HIGH** — this plan IS the mitigation: the extended grep-contract suite + 4-layer no-commission corpus are the CI gates that fail the build on any leakage path (calc output, UI, PDF render data, computed jsonb, audit, logs). |
| T-22-05-T | Tampering | params_snapshot reproducibility | mitigate | no-commission corpus asserts snapshot records partnerType + commissionApplied:false, proving PTYPE-06 reproducibility under a later type change. |
| T-22-05-R | Repudiation | weakened assertions silently passing | mitigate | assertNoCommissionLeakage reused verbatim; baseline gate count must increase (acceptance criterion), preventing accidental gate removal. |
| T-22-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to audit. |
</threat_model>

<verification>
- `npm run test -- admin-09-grep-contracts` green with increased gate count
- `npm run test -- no-commission` green including Agent/Commercial 4-layer runs + snapshot reproducibility
- Full `npm run test` + typecheck + lint + no-drizzle-push + no-vercel-imports green (modulo documented Phase 23 byte-determinism dependency)
</verification>

<success_criteria>
PTYPE-07 satisfied: the ADMIN-09 grep suite asserts zero commission leakage across the Agent/Commercial calc output, UI, PDF render path, logs, and audit, and all 12+ original gates stay green. PTYPE-06 proven: the no-commission corpus shows Agent/Commercial finalize records the specific type + commissionApplied:false with no commission in any layer.
</success_criteria>

<output>
Create `.planning/phases/22-partner-types-commission-free-proposals/22-05-SUMMARY.md` when done.
</output>
