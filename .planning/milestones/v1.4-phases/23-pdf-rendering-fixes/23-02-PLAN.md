---
phase: 23-pdf-rendering-fixes
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/lib/pdf/document.tsx
  - src/lib/i18n/dictionaries.ts
  - src/lib/i18n/dictionaries.test.ts
autonomous: true
requirements: [PDF-02]
must_haves:
  truths:
    - "The generated PDF no longer contains a Destinataire / Recipient block beneath the proposal title"
    - "The layout reflows cleanly — no orphaned blank gap or spacer where the block was"
    - "No dead code remains: the LABELS map, the lbl() helper, and the pdf.section.recipient dictionary keys are gone"
    - "The i18n FR/EN key-parity proof stays green after removing the recipient keys from both dictionaries and the parity test list"
  artifacts:
    - path: "src/lib/pdf/document.tsx"
      provides: "Proposal PDF with the Recipient <View> block deleted and clean reflow into the Project block"
      contains: "pdf.section.project"
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "FR + EN dictionaries with pdf.section.recipient removed from both"
    - path: "src/lib/i18n/dictionaries.test.ts"
      provides: "Phase 8 PDF-key parity list with pdf.section.recipient removed"
  key_links:
    - from: "src/lib/pdf/document.tsx"
      to: "title row (pdf.title)"
      via: "Project block follows the title directly; title marginBottom 24 provides spacing"
      pattern: "pdf\\.title"
---

<objective>
Fix PDF-02: remove the "Destinataire" block from the generated proposal PDF.

"Destinataire" is the `pdf.section.recipient` SectionLabel (FR 'DESTINATAIRE', EN
'RECIPIENT') rendered inside the Recipient `<View>` at src/lib/pdf/document.tsx lines
~182-204, directly beneath the title `t('pdf.title')` ("Proposition de location
financière"). Removing it means deleting that entire `<View>` and cleaning up the dead
code it leaves behind.

Purpose: The recipient block is being dropped from the proposal layout. The
clientName/clientRole/clientTel/clientEmail/clientSiren fields STAY in the data interface
(still persisted) but are no longer rendered.

Output: document.tsx with the Recipient block gone and clean reflow into the Project
block; the now-unused LABELS map + lbl() helper removed; the pdf.section.recipient keys
removed from both dictionaries and from the parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/pdf/document.tsx
@src/lib/pdf/components/section-label.tsx
@src/lib/pdf/components/key-value-row.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Recipient block + its dead code from document.tsx</name>
  <files>src/lib/pdf/document.tsx</files>
  <read_first>
    - src/lib/pdf/document.tsx (the Recipient View lines ~182-204; the LABELS map + lbl() helper lines ~87-104; the title row line ~174-180 with marginBottom 24; the Project block lines ~206-226)
    - src/lib/pdf/components/section-label.tsx (still used by Project / Interests blocks — DO NOT remove)
    - src/lib/pdf/components/key-value-row.tsx (still used by the Computation block — DO NOT remove)
  </read_first>
  <action>
    Delete the entire Recipient `<View style={{ marginBottom: 12 }}>...</View>` block
    (the `{/* Recipient block */}` View containing the recipient SectionLabel + the
    partnerCo/partnerName/clientCo/contact/role/tel/email/siren KeyValueRows + the inner
    divider View). After deletion the Project block (`{/* Project block */}`) follows the
    Title row directly. Confirm the title's `marginBottom: 24` provides the spacing and that
    NO orphaned spacer View or stray marginTop remains where the block was — the reflow must
    leave no blank gap. Then remove the now-dead `LABELS` const map and the `lbl()` helper
    function (lines ~87-104) since they were used ONLY by the deleted block — verify via grep
    that `lbl(` and `LABELS` have zero remaining references in document.tsx before removing.
    Keep SectionLabel and KeyValueRow imports (still used by Project/Interests/Computation).
    Keep the clientName/clientRole/clientTel/clientEmail/clientSiren fields in the
    ProposalDocumentProps data interface (still persisted; just not rendered).
  </action>
  <verify>
    <automated>grep -c "pdf.section.recipient\|lbl(\|const LABELS" src/lib/pdf/document.tsx | grep -qx 0 && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pdf.section.recipient" src/lib/pdf/document.tsx` returns 0
    - `grep -c "lbl(" src/lib/pdf/document.tsx` returns 0
    - `grep -c "const LABELS" src/lib/pdf/document.tsx` returns 0
    - `grep -c "pdf.section.project" src/lib/pdf/document.tsx` returns 1 or more (Project block intact)
    - `grep -c "SectionLabel\|KeyValueRow" src/lib/pdf/document.tsx` is > 0 (still used elsewhere)
    - `npx tsc --noEmit` exits 0 (no unused-import or type errors after removal)
    - The clientName/clientRole/clientTel/clientEmail/clientSiren keys are still present in the data interface (`grep -c "clientSiren" src/lib/pdf/document.tsx` returns 1 — the interface field)
  </acceptance_criteria>
  <done>The Recipient block and its dead LABELS/lbl() helpers are gone; the layout reflows into the Project block with no gap; SectionLabel/KeyValueRow and the persisted client fields remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the now-unused pdf.section.recipient dictionary keys + parity test entry</name>
  <files>src/lib/i18n/dictionaries.ts, src/lib/i18n/dictionaries.test.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts (FR 'pdf.section.recipient': 'DESTINATAIRE' line ~451; EN 'pdf.section.recipient': 'RECIPIENT' line ~1360)
    - src/lib/i18n/dictionaries.test.ts (the phase8Keys list — 'pdf.section.recipient' line ~192, and the "7.8 PDF document copy (14)" count comment ~line 188)
  </read_first>
  <action>
    Confirm via grep that the ONLY remaining consumer of `pdf.section.recipient` (after Task 1)
    is the dictionaries + test (no src/ component references it). Then remove the
    'pdf.section.recipient' entry from BOTH the FR dictionary (~line 451) and the EN dictionary
    (~line 1360) so the FR/EN key-parity (_EnHasAllFrKeys-style proof) stays balanced. Remove
    the 'pdf.section.recipient' entry from the phase8Keys list in dictionaries.test.ts (~line
    192) and update the section-count comment "7.8 PDF document copy (14)" to "(13)" to match
    the new key count. Do NOT touch any other pdf.* key.
  </action>
  <verify>
    <automated>grep -rc "pdf.section.recipient" src/ | grep -vq ":[1-9]" && npm test -- src/lib/i18n/dictionaries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "pdf.section.recipient" src/` returns no matches (key fully removed from dictionaries + test)
    - `npm test -- src/lib/i18n/dictionaries.test.ts` exits 0 (FR/EN parity proof green)
    - The "7.8 PDF document copy" count comment in dictionaries.test.ts reads "(13)"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The pdf.section.recipient keys are removed from both dictionaries and the parity test; the FR/EN parity proof and the PDF-copy key-count test stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored proposal snapshot → PDF render tree | client contact fields remain in the persisted snapshot; this plan only stops rendering them in the PDF — no boundary is newly crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Information disclosure | recipient block removal | accept | Removing the recipient block REDUCES the data surfaced in the PDF (less client PII printed). Net-positive for disclosure risk. The persisted snapshot is unchanged (fields stay in the data interface) so audit/reproducibility is preserved. |
| T-23-05 | Tampering (audit/persisted data) | ProposalDocumentProps data interface | mitigate | The clientName/clientRole/clientTel/clientEmail/clientSiren fields MUST remain in the data interface — only the render is removed. Acceptance criteria assert `clientSiren` still present in document.tsx. No change to params_snapshot or audit_log shape. |
| T-23-06 | Tampering (ADMIN-09) | document.tsx render path | mitigate | Block removal touches recipient/client fields only — no commission data is added or exposed. Plan 23-03's no-commission gate re-run confirms no leak after all source changes. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Recipient block gone: `grep -c "pdf.section.recipient" src/lib/pdf/document.tsx` returns 0.
- No dead code: `grep -c "lbl(\|const LABELS" src/lib/pdf/document.tsx` returns 0.
- Keys removed everywhere: `grep -rn "pdf.section.recipient" src/` empty.
- Parity + type safety: `npm test -- src/lib/i18n/dictionaries.test.ts` and `npx tsc --noEmit` both exit 0.
- Byte-determinism gate is intentionally NOT regenerated here — Plan 23-03 handles that BLOCKING step after this plan lands.
</verification>

<success_criteria>
- The generated PDF no longer contains a Destinataire/Recipient block beneath the title.
- The layout reflows cleanly with no blank gap where the block was.
- No dead code (LABELS, lbl()) and no orphaned dictionary keys remain; the FR/EN parity proof stays green.
</success_criteria>

<output>
Create `.planning/phases/23-pdf-rendering-fixes/23-02-SUMMARY.md` when done.
</output>
