---
phase: 23-pdf-rendering-fixes
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - __pdf-fixtures__/fixtures.ts
  - __pdf-fixtures__/commission-free-fixture.test.ts
  - __pdf-fixtures__/expected.sha256.txt
autonomous: true
requirements: [PDF-03]
must_haves:
  truths:
    - "The byte-determinism CI gate passes with regenerated SHA-256 fixtures reflecting the new layout (sanitizer + Destinataire removal)"
    - "The golden corpus includes at least one Agent/Commercial commission-free fixture whose loyer = montant HT x coefficient / 100 (no commission factor)"
    - "A test asserts the commission-free fixture's rendered PDF contains no commission wording and the correct commission-free loyer"
    - "expected.sha256.txt has exactly one line per fixture (3 fixtures: happy-path-fr, happy-path-en, agent-commission-free)"
  artifacts:
    - path: "__pdf-fixtures__/fixtures.ts"
      provides: "pdfFixtures array extended with an Agent/Commercial commission-free fixture using a loyer computed without the commission factor"
      contains: "commission-free"
    - path: "__pdf-fixtures__/commission-free-fixture.test.ts"
      provides: "Assertions: commission-free loyer value is correct + no commission wording in the rendered PDF for the new fixture"
    - path: "__pdf-fixtures__/expected.sha256.txt"
      provides: "Regenerated content hashes for all fixtures reflecting the new layout"
  key_links:
    - from: "__pdf-fixtures__/expected.sha256.txt"
      to: "__pdf-fixtures__/render-fixtures.test.ts"
      via: "the gate reads expected.sha256.txt and asserts one line per fixture"
      pattern: "expected\\.sha256\\.txt"
---

<objective>
Fix PDF-03: keep the byte-determinism CI gate green after the Phase 23 layout changes and
extend the golden corpus to cover the commission-free Agent/Commercial render.

After Plan 23-01 (number sanitizer) and Plan 23-02 (Destinataire removal) change the
rendered layout, the two existing fixtures (happy-path-fr, happy-path-en) produce new
bytes, so `__pdf-fixtures__/expected.sha256.txt` must be regenerated. Additionally,
ROADMAP success criterion 3 requires at least one Agent/Commercial fixture in the
byte-determinism gate asserting the commission-free loyer and the absence of any
commission wording.

Context on existing coverage (do not duplicate): the calc layer already covers
commission-free arithmetic (`src/lib/calc/calc.golden.test.ts` "commission-free corpus"),
and the render-data layer already covers Agent/Commercial structural absence
(`src/lib/pdf/no-commission.test.ts` PTYPE-06/07 blocks). The GAP this plan fills is at
the BYTE-DETERMINISM FIXTURE layer (`__pdf-fixtures__/fixtures.ts`), which currently has
only Partenaire-economics fixtures. The PDF `data` shape carries no commission field at
all — commission-freeness is expressed purely by passing a `loyerHT`/`coeff` computed
WITHOUT the commission factor.

Purpose: prove a real commission-free PDF renders deterministically and carries no
commission trace.

Output: a third fixture in fixtures.ts, an assertion test for it, and a regenerated
expected.sha256.txt committed alongside the source changes from Plans 01+02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@__pdf-fixtures__/fixtures.ts
@__pdf-fixtures__/render-fixtures.test.ts
@__pdf-fixtures__/expected.sha256.txt
@scripts/update-pdf-fixture.ts
@src/lib/pdf/no-commission.test.ts
@.planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an Agent/Commercial commission-free byte-determinism fixture + assertion test</name>
  <files>__pdf-fixtures__/fixtures.ts, __pdf-fixtures__/commission-free-fixture.test.ts</files>
  <read_first>
    - __pdf-fixtures__/fixtures.ts (SHARED_BASE shape + the two existing fixtures; all fields are frozen constants for determinism)
    - __pdf-fixtures__/render-fixtures.test.ts (the gate that iterates pdfFixtures and asserts one line per fixture)
    - src/lib/pdf/no-commission.test.ts (the commission-free loyer semantics: commissionPct forced to 0 → loyer = amountHT x coeff / 100; and the binary visible-text reconstruction approach)
    - src/lib/calc/calc.golden.test.ts (expectedLoyerFree(amount, coeff) = +((amount * Number(coeff)) / 100).toFixed(2) — the exact commission-free loyer arithmetic)
    - .planning/phases/22-partner-types-commission-free-proposals/22-CONTEXT.md (D-01/D-02/D-06: Agent and Commercial behave identically commission-free; PDF reflows with no commission trace)
  </read_first>
  <action>
    Add a third entry to the `pdfFixtures` array named `agent-commission-free`. Reuse a frozen
    base (no Date.now / Math.random) but set `computed.loyerHT` to the COMMISSION-FREE value for
    its amountHT + coefficient: loyer = round2(Number(amountHT) * Number(coeff) / 100). Pick a
    deterministic amount/duration/tranche whose coefficient is known from the fixture coefficient
    table (e.g. amountHT '75000', durationMonths 48, trancheKey 't2', coeff '2.2500' →
    loyerHT = round2(75000 * 2.2500 / 100) = '1687.50'). Verify the arithmetic against
    expectedLoyerFree before committing. Set language 'fr' (one lang is sufficient for the
    commission-free coverage; the FR/EN byte coverage already exists via happy-path-*). Then
    create __pdf-fixtures__/commission-free-fixture.test.ts (pinned `// @vitest-environment node`,
    with vi.mock('server-only', () => ({}))) that: (a) asserts the fixture's loyerHT equals
    round2(amountHT * coeff / 100) and is strictly less than the Partenaire loyer for the same
    inputs (amountHT * (1 + commissionPct/100) * coeff / 100 with commissionPct 5) — proving the
    commission factor is dropped; and (b) renders the fixture via renderProposalPdf, reconstructs
    the visible text (reuse the inflate + bfchar/TJ helpers proven in no-commission.test.ts), and
    asserts the visible text + raw latin1 buffer contain no 'commission' substring (case-insensitive).
  </action>
  <verify>
    <automated>npm test -- __pdf-fixtures__/commission-free-fixture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __pdf-fixtures__/commission-free-fixture.test.ts` exits 0
    - `grep -c "agent-commission-free" __pdf-fixtures__/fixtures.ts` returns 1 or more
    - The test asserts loyerHT == round2(amountHT * coeff / 100) AND loyerHT < the Partenaire loyer for the same inputs
    - The test asserts the rendered PDF (visible text + raw buffer) contains no 'commission' substring
    - The fixture uses only frozen constants (no Date.now/Math.random); `grep -c "Date.now\|Math.random" __pdf-fixtures__/fixtures.ts` returns 0
  </acceptance_criteria>
  <done>A frozen Agent/Commercial commission-free fixture exists with a verified commission-free loyer, and a test proves the rendered PDF carries the correct loyer and no commission wording.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Regenerate expected.sha256.txt and verify the full gate</name>
  <files>__pdf-fixtures__/expected.sha256.txt</files>
  <read_first>
    - scripts/update-pdf-fixture.ts (the regenerator; runs non-interactively via --confirm UPDATE-FIXTURE and writes sorted name:contentHash lines)
    - __pdf-fixtures__/render-fixtures.test.ts (asserts each fixture contentHash matches + expected.sha256.txt has exactly one line per fixture)
    - __pdf-fixtures__/expected.sha256.txt (current 2-line file — will become 3 lines)
  </read_first>
  <action>
    THIS IS A BLOCKING TASK: it MUST run AFTER all source changes from Plans 23-01 and 23-02 are
    applied (sanitizer wired + Destinataire block removed) and AFTER Task 1 added the new fixture,
    and BEFORE final verification. Run the regenerator non-interactively:
    `npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE`. This re-renders every fixture and
    rewrites __pdf-fixtures__/expected.sha256.txt with the new sorted name:contentHash lines
    (now 3 lines: agent-commission-free, happy-path-en, happy-path-fr). Then run the gate test to
    confirm green. Commit the regenerated expected.sha256.txt ALONGSIDE the source changes (the
    gate and the source must land together so CI never sees a mismatched intermediate state).
    Do NOT hand-edit hashes — only the regenerator may write them.
  </action>
  <verify>
    <automated>npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE && npm test -- __pdf-fixtures__/render-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run pdf:update-fixture -- --confirm UPDATE-FIXTURE` exits 0 (APPLY mode, writes file)
    - `npm test -- __pdf-fixtures__/render-fixtures.test.ts` exits 0 (all fixture contentHashes match)
    - expected.sha256.txt has exactly 3 non-empty lines (one per fixture): verify with `grep -c . __pdf-fixtures__/expected.sha256.txt` returns 3
    - Re-running the regenerator a second time reports "No drift" (determinism holds): `npm run pdf:update-fixture` (dry-run) prints up-to-date
  </acceptance_criteria>
  <done>expected.sha256.txt is regenerated to reflect the new layout + new fixture, the byte-determinism gate passes, and the hashes are deterministic on re-run.</done>
</task>

<task type="auto">
  <name>Task 3: Full PDF + ADMIN-09 regression sweep</name>
  <files>__pdf-fixtures__/expected.sha256.txt</files>
  <read_first>
    - src/lib/pdf/no-commission.test.ts (the 4-layer ADMIN-09 / PTYPE-06/07 PDF gate)
    - tests/admin-09-grep-contracts.test.ts (the 12+ gate grep-contract suite that must stay green)
  </read_first>
  <action>
    Run the PDF-related and ADMIN-09 test suites to confirm Phase 23's layout changes introduced
    no commission leak and no regression: the byte-determinism gate, the no-commission 4-layer
    corpus, the PDF sanitizer tests, the commission-free fixture test, and the admin-09
    grep-contract suite. Resolve any failure before writing the SUMMARY. No source edits expected
    here unless a regression surfaces.
  </action>
  <verify>
    <automated>npm test -- __pdf-fixtures__ src/lib/pdf tests/admin-09-grep-contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __pdf-fixtures__ src/lib/pdf tests/admin-09-grep-contracts.test.ts` exits 0
    - The no-commission 4-layer gate and the admin-09 grep-contract suite are both green
    - No commission leak introduced by the Phase 23 layout changes
  </acceptance_criteria>
  <done>All PDF and ADMIN-09 suites are green after the Phase 23 layout changes; the byte-determinism gate, no-commission corpus, sanitizer tests, and grep-contract suite all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → PDF render tree → committed hash | frozen fixture constants render to bytes whose hash becomes the CI gate; no external/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information disclosure (ADMIN-09) | agent-commission-free fixture render | mitigate | The new commission-free fixture is itself a GUARD for ADMIN-09: Task 1 asserts the rendered PDF contains no 'commission' substring and the loyer is the commission-free value. Task 3 re-runs the no-commission 4-layer gate + the grep-contract suite to confirm no leak. |
| T-23-08 | Tampering | expected.sha256.txt regeneration | mitigate | Hashes are written ONLY by the gated regenerator (`--confirm UPDATE-FIXTURE`); hand-editing is prohibited by the action. Determinism is verified by a second dry-run reporting "No drift". The gate test confirms the rendered bytes match the committed hash. |
| T-23-09 | Repudiation | gate + source commit coupling | mitigate | The regenerated expected.sha256.txt MUST be committed alongside the Plan 01+02 source changes so CI never observes a mismatched intermediate state (a red gate that could be misattributed). Enforced by the BLOCKING ordering (Task 2 after all source edits). |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (uses existing vitest + @react-pdf/renderer + the existing pdf:update-fixture script). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- New fixture + assertion: `npm test -- __pdf-fixtures__/commission-free-fixture.test.ts` exits 0.
- Regenerated gate green: `npm test -- __pdf-fixtures__/render-fixtures.test.ts` exits 0; `grep -c . __pdf-fixtures__/expected.sha256.txt` returns 3.
- Determinism: dry-run regenerator reports "No drift".
- No regression: `npm test -- __pdf-fixtures__ src/lib/pdf tests/admin-09-grep-contracts.test.ts` exits 0.
</verification>

<success_criteria>
- The byte-determinism CI gate passes with regenerated SHA-256 fixtures reflecting the new layout.
- The golden corpus includes an Agent/Commercial commission-free fixture asserting the commission-free loyer and the absence of any commission wording.
- All PDF + ADMIN-09 suites remain green.
</success_criteria>

<output>
Create `.planning/phases/23-pdf-rendering-fixes/23-03-SUMMARY.md` when done.
</output>
