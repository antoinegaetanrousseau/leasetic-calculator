---
phase: 25-teal-rebrand-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: [UIFIX-01]
must_haves:
  truths:
    - "The proposal status pill sizes to its text content (hugs) in the proposals list, in both FR and EN"
    - "Longer FR labels (e.g. 'Brouillon', 'Supprimée', 'Expirée') are not clipped or wrapped by the status column"
    - "The fix applies to every .chip variant by sizing the chip's container to content (D-03), not just the proposal status pill"
    - "The chip pill chrome (.chip base padding/radius/colors) is unchanged — the fix targets the consumer container width, per CONTEXT.md D-03 research note"
  artifacts:
    - path: "app/globals.css"
      provides: ".list-row grid track for the status column changed from fixed 92px to content-hugging (max-content/auto)"
      contains: ".list-row"
  key_links:
    - from: "src/components/proposals/ProposalRow.tsx"
      to: "app/globals.css .list-row"
      via: "StatusChip rendered in the 5th grid column"
      pattern: "grid-template-columns"
---

<objective>
Fix UIFIX-01: the proposal status pill must hug its text content rather than render in a
fixed-width slot that clips longer FR labels. Per CONTEXT.md D-03, the `.chip` base
ALREADY hugs in isolation (`display: inline-flex; padding: 4px 8px`) — so the offending
constraint lives in the CONSUMER container, not in `.chip`. The container is the
`.list-row` CSS grid in `app/globals.css`, whose 5th column track is pinned to a fixed
`92px` (and `92px`/`auto` in the `.is-draft`/`.is-deleted` variants). That fixed track
clips FR labels like "Brouillon"/"Supprimée" which are wider than 92px.

Apply the hug-content behavior across all chip-bearing rows (D-03) by changing the status
column track to size to its content. Do NOT touch `.chip` padding (it already hugs — that
would be a no-op) and do NOT touch any color token (D-01 guardrail).

Purpose: status pills size to content consistently across locales.
Output: updated `.list-row` grid track in `app/globals.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-teal-rebrand-polish/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hug the status-column track in the .list-row grid</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css §`.chip` (~lines 355–391) — confirms the base already hugs
      (`display: inline-flex; padding: 4px 8px`). This is the proof the fix is NOT here.
      Do not modify any `.chip*` rule.
    - app/globals.css §`.list-row` (~lines 415–432) — the offending container. The status
      pill is the 5th grid cell. Current tracks:
        - `.list-row`: `grid-template-columns: 1fr 100px 130px 100px 92px;` (line ~417) — 5th = 92px
        - `.list-row.is-deleted`: `1fr 100px 130px 100px auto auto;` (line ~431) — 5th = auto (status), 6th = restore slot
        - `.list-row.is-draft`: `1fr 100px 130px 100px 92px auto;` (line ~432) — 5th = 92px (status), 6th = draft actions
    - src/components/proposals/ProposalRow.tsx — confirms `<StatusChip>` is the 5th child
      of the row grid (line ~99) across base/draft/deleted modes. No edit here.
    - app/(admin)/[adminSegment]/lc-references/_components/LcReferencesList.tsx (~lines 199–211)
      — the OTHER chip consumer; its status cell is a table `<td>` with no fixed width and
      centered content, so the chip already hugs there. Confirm during work; no edit expected.
  </read_first>
  <action>
    In `app/globals.css`, change the status-column track of the proposal-row grid from the
    fixed `92px` to a content-hugging track so the chip sizes to its label in any locale.

    Set the 5th `grid-template-columns` track to `max-content` (preferred over `auto`
    because `max-content` sizes precisely to the chip's intrinsic width and never clips):
      - `.list-row`: 5th track `92px` → `max-content`.
      - `.list-row.is-draft`: 5th track `92px` → `max-content` (keep the trailing `auto`
        for the draft-actions column unchanged).
      - `.list-row.is-deleted`: 5th track is already `auto`; leave it (it already hugs).
        Optionally normalize to `max-content` for consistency, but do not change column count.

    Do NOT alter the other tracks (`1fr 100px 130px 100px`), the `gap`, padding, or any
    `.chip*` rule. Do NOT touch `--gd`, `--teal`, `--brand-mark`, or `.chip-active`
    (D-01 guardrail). The grid `gap: 16px` already separates the date column from the chip,
    so a content-sized status track will not collide with the preceding column.
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run lint && npx vitest run "src/components/proposals/ProposalRow.test.tsx" "app/(authed)/proposals/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - In `app/globals.css`, the `.list-row` rule's `grid-template-columns` 5th track is `max-content` (not `92px`): `grep -A1 "^\.list-row {" app/globals.css` shows the status track as `max-content`.
    - `grep -c "92px" app/globals.css` for the `.list-row` and `.list-row.is-draft` rules shows the status-column `92px` is gone from those two rules (the only remaining `92px`, if any, is not in a `.list-row` status track).
    - `.chip` base rule (lines ~355–364) is byte-identical to before — no padding/display/width change (the fix is container-only, per D-03).
    - No diff line touches `--gd`, `--teal`, `--brand-mark`, `.chip-active`, or any `.chip-*` background/color.
    - `npm run lint` exits 0 and the ProposalRow + proposals-list render tests pass (no layout regression in the row markup contract).
  </acceptance_criteria>
  <done>
    The proposal-row status column hugs its chip content in both locales; longer FR labels
    are no longer clipped; the `.chip` base and all color tokens are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan changes one CSS grid-track value in a
stylesheet. There is no user input, no new route, no query, no auth surface, and no
data-flow change. CSS is static and not user-controlled. No new attack surface exists.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-NONE | n/a | app/globals.css grid track | accept | Pure presentational CSS edit; no input, no logic, no data. No mitigation required. |
</threat_model>

<verification>
- `.list-row` status-column track is `max-content`; `.chip` base unchanged.
- ProposalRow + proposals-list tests pass; lint green.
- Manual sanity (optional): longest FR label "Supprimée"/"Brouillon" renders without clip.
</verification>

<success_criteria>
UIFIX-01 satisfied: the proposal status pill (and all `.chip` variants via the shared
container fix, D-03) hugs its text content across all status variants and both languages,
with no clipping and no change to chip chrome or color tokens.
</success_criteria>

<output>
Create `.planning/phases/25-teal-rebrand-polish/25-02-SUMMARY.md` when done.
</output>
