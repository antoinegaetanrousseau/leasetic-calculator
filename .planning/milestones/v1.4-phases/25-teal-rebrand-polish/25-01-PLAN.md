---
phase: 25-teal-rebrand-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/dictionaries.ts
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, COPY-04]
must_haves:
  truths:
    - "Admin-home 'Références LC' nav card title reads 'Toutes les propositions' (FR) / 'All proposals' (EN)"
    - "The /[adminSegment]/lc-references page heading reads 'Toutes les propositions' (FR) / 'All proposals' (EN)"
    - "Admin-home 'Coefficients & commission' nav card title reads 'Coefficients & Commissions' (FR) / 'Coefficients & Commissions' (EN)"
    - "Admin-home stat tile label reads 'Dernière Modif Coef' (FR) / EN equivalent"
    - "The /[adminSegment]/lc-references route, query, and component wiring are unchanged (display label only)"
    - "EN dictionary has every key FR has (compile-time parity proof stays green)"
  artifacts:
    - path: "src/lib/i18n/dictionaries.ts"
      provides: "FR + EN entries for the three relabelled admin-home keys + lc-references heading key"
      contains: "Toutes les propositions"
  key_links:
    - from: "app/(admin)/[adminSegment]/page.tsx"
      to: "src/lib/i18n/dictionaries.ts"
      via: "t('admin.nav.lcReferences.title') / t('admin.nav.coefficients.title') / t('admin.home.stats.derniereModifCoeffs')"
      pattern: "admin\\.nav\\.lcReferences\\.title|admin\\.nav\\.coefficients\\.title|admin\\.home\\.stats\\.derniereModifCoeffs"
    - from: "app/(admin)/[adminSegment]/lc-references/page.tsx"
      to: "src/lib/i18n/dictionaries.ts"
      via: "t('admin.lcReferences.title')"
      pattern: "admin\\.lcReferences\\.title"
---

<objective>
Ship the three admin-home display-label changes (COPY-01..03) as FR + EN dictionary
edits, keeping the compile-time `_EnHasAllFrKeys` parity proof green (COPY-04). All
four strings are verbatim from REQUIREMENTS.md / CONTEXT.md D-02 — no interpretation.

These are display-only label changes. No routes, queries, or component wiring change.
The teal rebrand (BRAND-01/02/03) is descoped per D-01 and is NOT part of this plan —
do not touch `--gd`, `.chip-active`, `--brand-mark`, or `--teal`.

Purpose: Align admin-home copy with the agreed labels.
Output: Updated FR + EN dictionary entries in `src/lib/i18n/dictionaries.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-teal-rebrand-polish/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel the three admin-home keys + lc-references heading (FR + EN)</name>
  <files>src/lib/i18n/dictionaries.ts</files>
  <read_first>
    - src/lib/i18n/dictionaries.ts — the FR map (`dictionaries.fr`) and EN map
      (`dictionaries.en`); the four target keys exist in BOTH maps. Current FR/EN values:
        - `admin.nav.coefficients.title`: FR 'Coefficients & commission' (~line 485),
          EN 'Coefficients & commission' (~line 1393)
        - `admin.nav.lcReferences.title`: FR 'Références LC' (~line 494),
          EN 'LC References' (~line 1402)
        - `admin.lcReferences.title`: FR 'Références LC' (~line 498),
          EN 'LC References' (~line 1406) — this is the lc-references PAGE heading (PageHero title)
        - `admin.home.stats.derniereModifCoeffs`: FR 'Dernière modif. coeffs' (~line 786),
          EN 'Last coeff. update' (~line 1669)
    - app/(admin)/[adminSegment]/page.tsx — confirms `admin.nav.coefficients.title`,
      `admin.nav.lcReferences.title`, and `admin.home.stats.derniereModifCoeffs` are the
      admin-home consumers (lines ~154, ~168, ~192). No edit here.
    - app/(admin)/[adminSegment]/lc-references/page.tsx — confirms `admin.lcReferences.title`
      is the page heading (PageHero title, line ~59). No edit here.
  </read_first>
  <action>
    Edit ONLY string values in `src/lib/i18n/dictionaries.ts`. Do not add, rename, or
    remove keys; do not touch any other key.

    COPY-01 (label only — route `/[adminSegment]/lc-references` stays unchanged): set
    `admin.nav.lcReferences.title` AND `admin.lcReferences.title` to FR exactly
    "Toutes les propositions" and EN exactly "All proposals" in both the fr and en maps
    (four value edits total — the nav-card title and the page heading both read the new
    label per COPY-01).

    COPY-02: set `admin.nav.coefficients.title` to FR exactly "Coefficients & Commissions"
    and EN exactly "Coefficients & Commissions" (capital C on Commissions, plural) in both maps.

    COPY-03: set `admin.home.stats.derniereModifCoeffs` to FR exactly "Dernière Modif Coef"
    (no trailing period, title-case, abbreviated) and an EN equivalent exactly
    "Last Coef Update" in both maps.

    COPY-04: do NOT add or drop keys — every key already exists in both maps, so the
    `_EnHasAllFrKeys` parity proof stays satisfied by construction. Value-only edits keep it green.

    Guardrail: do NOT change `proposal.chip.*` or `chip.*` strings, and do NOT touch any
    color token — those are out of scope (D-01).
  </action>
  <verify>
    <automated>cd /Users/antoinerousseau/Developer/leasetic-calculator && npm run typecheck && npx vitest run "app/(admin)/[adminSegment]/page.test.tsx" "src/lib/i18n/dictionaries.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Toutes les propositions" src/lib/i18n/dictionaries.ts` returns 2 (FR nav title + FR page heading) and `grep -c "All proposals" src/lib/i18n/dictionaries.ts` returns 2 (EN nav title + EN page heading).
    - `grep -c "Coefficients & Commissions" src/lib/i18n/dictionaries.ts` returns 2 (FR + EN).
    - `grep -c "Dernière Modif Coef" src/lib/i18n/dictionaries.ts` returns 1; the old "Dernière modif. coeffs" string no longer appears as a value for `admin.home.stats.derniereModifCoeffs`.
    - `npm run typecheck` exits 0 — proves the `_EnHasAllFrKeys` / `_EnParityProof` compile-time check (dictionaries.ts ~line 1837) is green (COPY-04).
    - `grep -c "Références LC" src/lib/i18n/dictionaries.ts` returns 0 and `grep -c "LC References" src/lib/i18n/dictionaries.ts` returns 0 (both old labels replaced).
    - No diff lines touch `--gd`, `--teal`, `--brand-mark`, `.chip-active`, `proposal.chip.`, or `chip.` value strings.
  </acceptance_criteria>
  <done>
    Both FR and EN maps carry the four new label strings, the parity proof compiles, and
    the admin-home + lc-references render tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan edits static display strings inside a
compile-time-checked dictionary module. There is no new user input, no new route, no new
query, no auth or data-flow change. The strings are not user-controlled and are not
interpolated into HTML in any new way (existing `t()` rendering is unchanged).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-XSS | Tampering/Info | i18n string render | accept | New values are static literals with no markup; rendered via existing React text nodes (auto-escaped). No new injection surface. |
</threat_model>

<verification>
- `npm run typecheck` green (parity proof intact).
- Admin-home page test + dictionary test pass.
- Old labels ("Références LC", "LC References", "Coefficients & commission",
  "Dernière modif. coeffs") fully replaced.
</verification>

<success_criteria>
COPY-01..04 satisfied: the three admin-home labels (plus the lc-references page heading
for COPY-01) read the agreed FR + EN strings, the route is unchanged, and the
`_EnHasAllFrKeys` parity proof stays green.
</success_criteria>

<output>
Create `.planning/phases/25-teal-rebrand-polish/25-01-SUMMARY.md` when done.
</output>
